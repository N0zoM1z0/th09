# TH09 verified knowledge

This file stores durable TH09-scoped facts and important negative results.
Cross-game promotion is a later Factory review, never an automatic action from
this repository.

The table below is the maintained fact index. The later packet sections are
checkpoint-scoped investigation records: words such as "current", "now", or
"remains" there describe that historical checkpoint unless a maintained row,
ledger, or current report independently confirms them. Use
`scripts/report-reconstruction-status.py` and `docs/RE_HANDOFF.md` for live
totals and frontier state. Later evidence may explicitly correct an earlier
packet; do not stop at the first matching historical note.

For evolving large owners, especially EclManager::RunEcl, packet-local code
sizes, frame sizes, relocation counts and proposed source shapes are historical
measurements. The live config/functions.csv row and a fresh repository
diagnostic override older packet numbers. Packets 466-480 are intentionally
kept as the investigation trail, not as competing current baselines.

| ID | State | Fact | Evidence |
| --- | --- | --- | --- |
| TARGET-001 | observed / corroborated | The supported executable is original Japanese TH09 v1.50a, size 685,056, SHA-256 `10350095...fce6822`. | Direct hash/PE inspection plus pinned thcrap version database |
| PE-001 | observed | The target is relocation-stripped PE32 i386 at image base `0x00400000`, entry `0x0047D45F`, with five sections. | `scripts/verify-target.py`; `config/target.toml` |
| TOOLCHAIN-001 | observed / corroborated | The target belongs to the VC7.1 build-3077 family. | PE linker 7.10 and Rich-header product/build records; TH095 provider-family comparison |
| TOOLCHAIN-002 | partially observed / product-open | VC7.1 build 3077 and selected D3DX8, CRT, DXErr8, import, and compiler-generated contributions are established. The complete compiler-profile matrix, translation-unit partition, resources, data owners, library member selection, and link order remain open. | `config/build.toml`; pinned runtime-origin audits; canonical match-unit replays |
| TOOLCHAIN-172 | observed / replay infrastructure | TH09's current canonical function replay does not consume Capstone: `build-match-unit.py` invokes the pinned VC7.1 compiler and `compare-coff-function.py` parses PE/COFF data directly. The Factory restricted shell exposes system Capstone 4.0.2, but a cold `game-error-context-flush` build still reproduced all 80 bytes and all six relocation destinations; the formal Factory replay passed and was accepted. Do not add a TH10-style Capstone pin or Python router unless a real TH09 replay consumer is introduced and identity-checked. | Repository import/reference audit; host and Factory-shell exact replay; accepted Factory receipt `receipt:dc8c90fc4dc90d2b47ac3aaf694dd26efae39dfcdef19403599b2e722ebd1f5e` |
| ANALYSIS-001 | observed | At the recorded attestation checkpoint, the active IDA database matched the target hash, image base, mapped size, entry point, and sampled target bytes. Every new session must attest it again; IDA remains a provisional semantic provider rather than an exactness Oracle. | Direct `ida-pro-mcp` metadata/readback; Factory-native attestation when used from Web |
| INVENTORY-001 | reviewed / mixed origin | Every currently tracked candidate has received boundary/origin review. Current authored, excluded, and deliberately unresolved totals are ledger-derived; semantic names/types and codegen exactness remain independent. | `config/functions.csv`; `config/function-origins.csv`; `scripts/report-reconstruction-status.py` |
| CRT-001 | observed / corroborated | `__amsg_exit` at `0x0047D416-0x0047D43A` is a 37-byte CRT/library helper. | Attested TH09 control flow and bytes; VC7.1 `libcmt.lib` `wincrt0.obj` structural comparison with relocation fields excluded |
| CRT-002 | observed / corroborated | `_fast_error_exit` starts at `0x0047D43B` and continues through `0x0047D45E`; the imported IDA candidate omitted the final `pop ecx; pop ecx; ret`. | Attested TH09 bytes and adjacent entry at `0x0047D45F`; VC7.1 `libcmt.lib` `wincrt0.obj` structural comparison with relocation fields excluded |
| CRT-003 | observed / corroborated | The PE entry point at `0x0047D45F-0x0047D633` is the 469-byte VC7.1 `libcmt.lib` `wincrt0.obj` `_WinMainCRTStartup` contribution. | No callers; exact 0x1D5 COFF extent; attested TH09 body and COFF body have the same normalized SHA-256 across 321 non-relocation bytes after masking 37 relocation fields |
| CRT-004 | observed / corroborated | `__finite` at `0x0047D634-0x0047D648` is a 21-byte CRT/library helper. | Verified TH09 target body matches VC7.1 `libcmt.lib` `ieeemisc.obj` byte-for-byte; COFF extent is `0x15` with no relocations |
| CRT-005 | observed / corroborated | `__fpclass` at `0x0047D649-0x0047D6E4` is a 156-byte CRT/library helper. | Verified TH09 target body and VC7.1 `libcmt.lib` `ieeemisc.obj` match across all 148 non-relocation bytes after masking two COFF relocation fields |
| BOUNDARY-001 | observed | `0x0047D6E5-0x0047D6E7` is a three-byte `CC CC CC` gap between `__fpclass` and `_longjmp`; physical ownership remains unassigned. | Verified TH09 target bytes plus attested function starts at `0x0047D649` and `0x0047D6E8` |
| CRT-006 | observed / corroborated | `_longjmp` at `0x0047D6E8-0x0047D760` is a 121-byte CRT/library helper; the imported IDA inventory incorrectly split its final `ret` at `0x0047D760` into `nullsub_10`. | Verified TH09 target and VC7.1 `libcmt.lib` `longjmp.obj` match across all 101 non-relocation bytes after masking five COFF relocation fields; COFF extent is `0x79` |
| CRT-007 | observed / corroborated | `__setjmp3` at `0x0047D764-0x0047D7DE` is a 123-byte CRT/library helper. | Verified TH09 target and VC7.1 `libcmt.lib` `setjmp3.obj` match across all 119 non-relocation bytes after masking the COFF relocation at `+0x28`; COFF extent is `0x7B` |
| BOUNDARY-002 | observed | `0x0047D761-0x0047D763` is a three-byte `CC CC CC` gap between the corrected `_longjmp` extent and `__setjmp3`; physical ownership remains unassigned. | Verified TH09 target bytes plus corrected archive-backed function extents |
| GAMEERR-001 | exact | `GameErrorContext::ResetContext @ 0x0042D230-0x0042D239` sets the buffer-end pointer at `+0x2000` to the object base and clears the first byte. | Canonical pinned-VC7.1 match unit replays all 10 target bytes with zero differences; only `_WinMain@16 +0x456` calls the candidate |
| GAMEERR-002 | exact | `GameErrorContext::Flush @ 0x0042D240-0x0042D28F` appends the separator, conditionally displays the 8 KiB buffer when the byte at `+0x2004` is set, then writes the complete buffer to `./log.txt`. | Canonical pinned-VC7.1 match unit replays all 80 target bytes including six explicit DIR32/REL32 relocations |
| BOUNDARY-003 | observed | `0x0042D23A-0x0042D23F` is six bytes of `CC` after `GameErrorContext::ResetContext`; `Flush` ends at `0x0042D28F`. Independent `0x0042D290-0x0042D2A1` is a reviewed authored constructor recovered from the target global-initializer graph. | Verified target bytes; `scripts/review-static-init-targets.py`; reviewed ledger rows |
| TOOLCHAIN-003 | compiler-observed | Reset/Flush require optimized VC7.1 code shape: tested `/Od /Ob1` produces 0x23/0x85-byte bodies, `/O1` produces 0x0A/0x4E, and tested `/O2`/`/Ox` variants produce the target 0x0A/0x50 extents and exact bytes. This does not identify original global optimization flags. | Focused compiler matrix using pinned VC7.1 `13.10.3077`; canonical GameErrorContext units use one exact-producing `/O2 /Oy-` profile |
| GAMEERR-003 | exact | `GameErrorContext::Log @ 0x0042C5C0-0x0042C658` formats into an 8 KiB temporary buffer under the supervisor critical-section wrapper, appends only when the result fits before object offset `+0x1FFF`, advances `bufferEnd`, and returns the format pointer. | Canonical pinned-VC7.1 match unit replays all 153 target bytes including six explicit relocations to `__chkstk`, supervisor storage/wrappers, and `vsprintf` |
| GAMEERR-004 | exact | `GameErrorContext::Fatal @ 0x0042C660-0x0042C6F8` follows the same append path with a 512-byte temporary buffer and additionally sets the object byte at `+0x2004` before leaving the critical section. | Canonical pinned-VC7.1 match unit replays all 153 target bytes including five explicit relocations |
| BOUNDARY-004 | observed | `0x0042C659-0x0042C65F` and `0x0042C6F9-0x0042C6FF` are seven-byte `CC` gaps after the reviewed Log and Fatal extents. Their physical ownership remains unassigned. | Attested TH09 target bytes and reviewed function returns at `0x0042C658` and `0x0042C6F8` |
| TOOLCHAIN-004 | compiler-observed | For the variadic Log/Fatal source shape, tested `/O2` with frame-pointer omission produces 0xA9/0xA0-byte bodies and does not match; adding `/Oy-` produces the target 0x99/0x99 bodies and zero-difference relocation-aware comparisons. `/Ox /Oy-` is also exact while `/O1 /Oy-` remains short, so the original global optimization choice is still unknown. | Focused single-variable VC7.1 compiler probes and canonical match-unit replay |
| FILEIO-001 | exact | `FileSystem::WriteDataToFile @ 0x0042C4E0-0x0042C5B1` resolves the supplied path through `0x0042ADC0`, serializes file writes with supervisor lock 2, creates/truncates a normal file with read sharing, returns `-1` on open failure, `-2` on short write, and `0` on a complete write. | Canonical pinned-VC7.1 match unit replays all 210 target bytes including sixteen explicit relocations to the path helper, supervisor storage/wrappers, and Win32 file APIs |
| BOUNDARY-005 | observed | `0x0042C5B2-0x0042C5BF` is a fourteen-byte `CC` gap after the reviewed WriteDataToFile extent and before Log at `0x0042C5C0`. Physical ownership remains unassigned. | Attested TH09 target bytes and reviewed return at `0x0042C5AF-0x0042C5B1` |
| TOOLCHAIN-005 | compiler-observed | WriteDataToFile reaches the target 0xD2-byte body when the `LPSTR errorMessage` local is scoped only to the CreateFile failure branch, allowing VC7.1 to reuse the dead stack slot of the third fastcall argument. Hoisting the local produces a 0xD4-byte mismatch; tested `#pragma var_order` is unsupported by this compiler invocation and does not fix it. | Focused VC7.1 source-shape probes plus canonical 210/210 relocation-aware replay |
| FILEIO-002 | exact | `FileSystem::ResolvePath @ 0x0042ADC0-0x0042AE19` copies the executable-directory string stored at `0x004ACD08` into shared path scratch at `0x004ACC00`, appends the caller path, and returns the scratch buffer. The storage definitions and translation-unit ownership remain unknown. | Canonical pinned-VC7.1 match unit replays all 90 target bytes including four DIR32 data relocations; `_WinMain@16` independently initializes `0x004ACD08` from `GetModuleFileNameA` or `"./"` |
| BOUNDARY-006 | observed | `0x0042AE1A-0x0042AE1F` is a six-byte `CC` gap after the reviewed ResolvePath extent. Physical ownership remains unassigned. | Attested TH09 target bytes and reviewed `ret 4` at `0x0042AE17-0x0042AE19` |
| FILEIO-003 | exact | `FileSystem::CheckIfFileAlreadyExists @ 0x0042C480-0x0042C4DB` resolves the supplied path, opens it for `GENERIC_READ` with read sharing, `OPEN_EXISTING`, sequential-scan and normal-file flags under supervisor lock 2, closes a valid handle and returns 1, or returns 0 on open failure. | Canonical pinned-VC7.1 match unit replays all 92 target bytes including nine relocations; four TH09 callers independently use the candidate as a boolean file-presence query |
| BOUNDARY-007 | observed | `0x0042C4DC-0x0042C4DF` is a four-byte `CC` gap after CheckIfFileAlreadyExists and immediately before WriteDataToFile. Physical ownership remains unassigned. | Attested TH09 target bytes and reviewed returns at `0x0042C4CC` and `0x0042C4DB` |


| SUPERVISOR-001 | exact | `Supervisor::EnterCriticalSectionWrapper @ 0x0042B130-0x0042B157` indexes a five-slot `CRITICAL_SECTION` region beginning at object offset `+0x6C0` with 0x18-byte stride, calls `EnterCriticalSection`, then increments the lock-count byte at `+0x738 + id`. | Canonical pinned-VC7.1 match unit replays all 40 target bytes including the single IAT relocation to `EnterCriticalSection`; TH09 caller sites directly exercise lock ids 0, 2, 3, and 4 |
| SUPERVISOR-002 | exact | `Supervisor::LeaveCriticalSectionWrapper @ 0x0042B160-0x0042B187` mirrors the same lock slot calculation, calls `LeaveCriticalSection`, then decrements the lock-count byte at `+0x738 + id`. | Canonical pinned-VC7.1 match unit replays all 40 target bytes including the single IAT relocation to `LeaveCriticalSection` |
| BOUNDARY-008 | observed | `0x0042B158-0x0042B15F` and `0x0042B188-0x0042B18F` are eight-byte `CC` gaps after the reviewed lock wrappers; `0x0042B190` begins a separate candidate. Physical ownership of both gaps remains unassigned. | Attested TH09 target bytes, reviewed `ret 4` extents, and `th09-ida` function start at `0x0042B190` |
| LAYOUT-001 | observed | Only the Supervisor lock view is established here: `criticalSections @ +0x6C0`, five 0x18-byte slots ending immediately before `lockCounts @ +0x738`; earlier and later Supervisor fields remain unknown. | TH09 target effective-address arithmetic plus exact VC7.1 overlay replay; adjacent layouts were hypothesis material only |

| CHAIN-001 | exact | `Chain::AddToCalcChain @ 0x0042B1E0-0x0042B26E` invokes and clears a pending added callback, then inserts a 0x20-byte ChainElem into the first embedded list root in ascending signed 16-bit priority order under Supervisor lock 0. The callback result is returned unchanged. | Canonical pinned-VC7.1 match unit replays all 143 target bytes including four relocations to `g_Supervisor` and the exact lock wrappers; TH09 ChainElem constructor/destructor candidates independently corroborate the accessed member offsets |
| CHAIN-002 | exact | `Chain::AddToDrawChain @ 0x0042B270-0x0042B2FE` performs the same insertion using the second embedded root at `this+0x20`. | Canonical pinned-VC7.1 match unit replays all 143 target bytes including four Supervisor relocations; target callers use it as a distinct second scheduling chain |
| LAYOUT-002 | observed | The TH09 ChainElem view used by this packet is 0x20 bytes with signed 16-bit priority at `+0x0`, callback at `+0x4`, added/deleted callbacks at `+0x8/+0xC`, prev/next at `+0x10/+0x14`, release-target storage at `+0x18`, and callback argument at `+0x1C`. Field names are reconstruction names, not recovered target symbols. | Target accesses in the exact add functions plus attested TH09 candidates at `0x0042ABE0` and `0x0042AC00`; adjacent TH08/TH095 layouts were hypothesis material only |
| BOUNDARY-009 | observed | `0x0042B26F` and `0x0042B2FF` are single-byte `CC` gaps after the reviewed AddToCalcChain/AddToDrawChain extents; `0x0042B300` begins a separate candidate. Physical ownership remains unassigned. | Attested TH09 target bytes and function starts/extents |

| CHAIN-003 | exact | `ChainElem::ChainElem @ 0x0042ABE0-0x0042ABFD` initializes the target-observed 0x20-byte node layout: clears the heap-allocation bit, prev/next and callback fields, sets releaseTarget to `this`, and clears priority. | Canonical pinned-VC7.1 match unit replays all 30 bytes with no relocations |
| CHAIN-004 | exact | `ChainElem::~ChainElem @ 0x0042AC00-0x0042AC23` invokes `deletedCallback(arg)` when present, then clears prev/next and callback/added/deleted callback fields. | Canonical pinned-VC7.1 match unit replays all 36 bytes with no relocations |
| BOUNDARY-010 | observed | `0x0042ABFE-0x0042ABFF` is a two-byte `CC` gap after the ChainElem constructor, and `0x0042AC24-0x0042AC2F` is a twelve-byte `CC` gap after the destructor before the separate `0x0042AC30` candidate. Physical ownership remains unassigned. | Attested TH09 target bytes and reviewed function extents |

| CHAIN-005 | codegen-exact / origin-unknown | `0x0042AC30-0x0042AC41` has the exact code shape of an empty `Chain::Chain()` over two nontrivial 0x20-byte ChainElem members: first member construction followed by the member at `+0x20`, then return `this`. The stripped target does not currently distinguish an explicitly authored empty constructor from an implicitly compiler-generated special member, so origin remains `unknown/review` and no canonical match row is added. | Two pinned-VC7.1 replays under `/O2 /Ob1 /Oy-` match all 18 primary bytes and both ChainElem-constructor relocations; `/O2 /Ob0 /Oy-` is also exact while tested `/Ob2` inlines both members and mismatches |
| CHAIN-006 | codegen-exact / origin-unknown | `0x0042B190-0x0042B1DA` has the exact primary code shape of an empty `Chain::~Chain()`, destroying the second embedded ChainElem then the first with VC7.1 exception cleanup. Explicit authored versus implicit compiler-generated origin remains unresolved, so the candidate stays `unknown/review` and is not added to canonical match totals. | Two pinned-VC7.1 primary replays match all 75 bytes and six relocations; associated `.text$x` at `0x0048D340` matches 18/18 with three relocations and `.xdata$x` at `0x0049DC08` matches 36/36 with two relocations, including `___CxxFrameHandler @ 0x0047B2E5` |
| BOUNDARY-011 | observed | `0x0042AC42-0x0042AC4F` is fourteen bytes of `CC` after the Chain-constructor candidate, and `0x0042B1DB-0x0042B1DF` is five bytes of `CC` after the Chain-destructor primary extent before exact AddToCalcChain. Physical ownership remains unassigned. | Attested TH09 target bytes and reviewed primary returns |
| TOOLCHAIN-006 | compiler-observed | For the empty Chain special-member source shape with visible ChainElem definitions, tested `/O2 /Ob0 /Oy-` and `/O2 /Ob1 /Oy-` both reproduce the TH09 constructor/destructor primary bodies, while tested `/O2 /Ob2 /Oy-` inlines members and produces 0x3A/0x72-byte mismatches; `/O1 /Ob1 /Oy-` keeps the constructor exact but produces a 0x35-byte destructor. This does not identify the original TU-wide inlining switch. | Focused VC7.1 build-3077 matrix plus target-bound primary and associated-EH comparisons |
| CHAIN-007 | exact | `ChainElem::SetCallback @ 0x0041A889-0x0041A89D` stores the callback at `+0x4` and clears the added/deleted callbacks at `+0x8/+0xC`. The next candidate begins immediately at `0x0041A89E`. | Canonical pinned-VC7.1 `/O1 /Ob1 /Oy-` match unit replays all 21 bytes with no relocations; tested `/O2` variants keep the same extent but choose different instructions |
| CHAIN-008 | exact | `Chain::CreateElem @ 0x0042B300-0x0042B346` allocates and constructs a 0x20-byte ChainElem, passes it through the release-build ZunMemory registry hook with `"funcChainInf"`, installs the callback, sets the heap-allocation bit, and returns the element. | Canonical pinned-VC7.1 match unit replays all 71 bytes with six relocations to operator new, exact ChainElem construction, the literal, ZunMemory storage/registry hook, and exact SetCallback |
| COMPILER-001 | observed / corroborated | `0x0042AC50-0x0042AC70` is a VC7.1 scalar deleting destructor for ChainElem: it calls the exact ordinary destructor, conditionally calls operator delete when flag bit 0 is set, and returns `this`. It is compiler-generated and excluded from the authored denominator; canonical codegen exactness is not claimed. | A pinned VC7.1 delete-expression probe automatically emits `??_GChainElem@@QAEPAXI@Z`; all 25 non-relocation bytes match and its two relocation destinations resolve to `ChainElem::~ChainElem @ 0x0042AC00` and operator delete at `0x0047B249` |
| BOUNDARY-012 | observed | `0x0042B347-0x0042B34F` is a nine-byte `CC` gap after CreateElem before the candidate at `0x0042B350`; `0x0042AC71-0x0042AC7F` is a fifteen-byte `CC` gap after the scalar deleting destructor before `0x0042AC80`. Physical ownership remains unassigned. | Attested `th09-ida` memory bytes plus reviewed function starts/extents |
| TOOLCHAIN-007 | compiler-observed | SetCallback is exact under tested `/O1 /Ob1 /Oy-` (with or without `/EHsc`) while tested `/O2`/`/Ox` variants are same-size byte mismatches. CreateElem becomes exact under a separate-TU probe with SetCallback body unavailable and C++ EH disabled via `/GX-`; the same source with EH enabled grows substantially. These are local exact-producing conditions and do not identify project- or Chain-wide flags/TU partition. | Focused pinned-VC7.1 build-3077 profile and visibility matrix; compiling the existing Chain destructor without EH collapses it from the target 75-byte EH shape to 19 bytes, independently disproving a TU-wide inference |
| CHAIN-009 | exact | `Chain::CutImpl @ 0x0042B350-0x0042B40B` searches both embedded roots for a requested node, unlinks a linked node, clears its callback/linkage, and temporarily releases Supervisor lock 0 around heap deletion or a non-heap deleted callback before reacquiring the lock. | Canonical pinned-VC7.1 `/O2 /Ob0 /Oy-` match unit replays all 188 bytes with nine relocations, including the classified ChainElem scalar deleting destructor and exact Supervisor lock wrappers |
| CHAIN-010 | exact | `Chain::Cut @ 0x0042C8C0-0x0042C8ED` is the public lock wrapper around CutImpl: acquire Supervisor lock 0, invoke CutImpl, release lock 0. | Canonical pinned-VC7.1 match unit replays all 46 bytes with five relocations; all three target callers of CutImpl operate while lock 0 is held, corroborating the internal-helper contract |
| BOUNDARY-013 | observed | `0x0042B40C-0x0042B40F` is four bytes of `CC` after CutImpl before `0x0042B410`; `0x0042C8EE-0x0042C8EF` is two bytes of `CC` after Cut before `0x0042C8F0`. Physical ownership remains unassigned. | Attested TH09 function extents and memory bytes |
| TOOLCHAIN-008 | compiler-observed | The natural CutImpl source is exact under tested `/O2 /Ob0 /Oy-`; tested `/Ob1` and `/Ob2` expand the heap-delete path into ordinary destructor plus operator delete and produce a 195-byte body, while `/O2 /Os /Ob1` produces 166 bytes. This local lowering constraint does not establish the original Chain-TU inlining profile. | Focused pinned-VC7.1 matrix; the `/Ob0` object preserves a relocation to `??_GChainElem@@QAEPAXI@Z` exactly where the TH09 target calls `0x0042AC50` |
| SOURCE-001 | unknown | TH08 committed release source contains a `ZunMemory::RemoveFromRegistry` call before deleting a heap ChainElem, but TH09 target code has no observable registry call in CutImpl. The target cannot distinguish a TH09 source that omitted it from one where an empty release-only inline operation was optimized away. | TH09 target call graph/codegen versus committed TH08 HEAD `a45e99fb1942714e6edded20847e32a654d56f97`; no TH095 uncommitted evidence used |
| CHAIN-011 | exact | `Chain::RunCalcChain` has a 182-byte logical body at `0x0042C700-0x0042C7B5` and an associated same-COMDAT codegen tail at `0x0042C7B6-0x0042C7D3`: two alignment bytes followed by a seven-entry switch table. It runs the calc root under lock 0 and uniquely handles callback result 6 by restarting from the first job. | Canonical pinned-VC7.1 `/O2 /Ob1 /Oy-` match unit replays the complete 212-byte owned extent with 21 relocations, including the table-base relocation and all seven table-entry targets |
| CHAIN-012 | exact | `Chain::RunDrawChain` has a 186-byte logical body at `0x0042C7E0-0x0042C899` and an associated same-COMDAT tail at `0x0042C89A-0x0042C8B3`: two alignment bytes plus a six-entry switch table. It starts at the draw root (`this+0x20`) and handles callback results 0 through 5 without calc's result-6 restart case. | Canonical pinned-VC7.1 match unit replays the complete 212-byte owned extent with 20 relocations, including all switch-table data |
| BOUNDARY-014 | observed / corroborated | `0x0042C7B6-0x0042C7D3` and `0x0042C89A-0x0042C8B3` are not free padding: each is emitted inside the corresponding VC7.1 function COMDAT and is referenced only by that function's switch dispatch. The actual inter-function gaps are twelve `CC` bytes at `0x0042C7D4-0x0042C7DF` and twelve at `0x0042C8B4-0x0042C8BF`; those gaps remain physically unassigned. | Attested target bytes/xrefs plus exact COFF section sizes and local relocation graph |
| TOOLCHAIN-009 | compiler-observed | The natural RunCalcChain/RunDrawChain source under tested `/O2 /Ob1 /Oy-` emits 0xD4-byte COMDAT sections that exactly include body, alignment and switch table. Earlier comparisons against only the IDA logical body sizes (`0xB6`/`0xBA`) failed by construction; `/O1` instead produces shorter nonmatching bodies. This establishes an owned-extent rule for these units, not a project-wide optimization profile. | Pinned VC7.1 profile probes and complete target-bound 212/212 replays |
| CONTROLLER-001 | exact | `Controller::GetJoystickCaps @ 0x0042C8F0-0x0042C967` probes WinMM joystick IDs 0 and 1 with a `JOYINFOEX` using `JOY_RETURNALL`. It logs and returns 1 only when both probes fail; otherwise it requests capabilities for both IDs into two contiguous `JOYCAPSA` records and returns 0. | Canonical pinned-VC7.1 match unit replays all 120 bytes with seven relocations to WinMM imports, exact GameErrorContext::Log, external message storage, the GameErrorContext object, and one JOYCAPSA-array base with a `+0x194` addend for element 1 |
| CONTROLLER-002 | observed | Target relocations establish a two-element contiguous `JOYCAPSA` view beginning at `0x004AC8D8`, with the second element at `0x004ACA6C` (`+0x194`, exactly `sizeof(JOYCAPSA)`). The defining translation unit and physical/data ownership are not established. | Exact object relocation addend plus attested TH09 data references; each element currently has only the GetJoystickCaps write reference in IDA |
| BOUNDARY-015 | observed | `0x0042C968-0x0042C96F` is an eight-byte `CC` gap after GetJoystickCaps before `0x0042C970`. Physical ownership remains unassigned. | Attested TH09 target bytes and reviewed return at `0x0042C967` |
| TOOLCHAIN-010 | compiler-observed | The natural two-joystick GetJoystickCaps source is exact under tested `/O2 /Ob0 /Oy-`, `/O2 /Ob1 /Oy-`, and `/Ox /Ob1 /Oy-`; `/O1 /Ob1 /Oy-` instead emits a shorter 0x71-byte body. The canonical unit uses `/O2 /Ob1 /Oy-` without identifying original global optimization or inlining switches. | Focused pinned-VC7.1 matrix and 120/120 relocation-aware replay |
| FILEIO-004 | exact | `FileSystem::OpenFile @ 0x0042C970-0x0042CAD2` is a `/Gr` free-function path with `path` in ECX, `fileSize` in EDX, and the external-resource flag as the sole stack argument. Archive mode strips path components, queries/decompresses through the archive object, reports a missing entry through exact GameErrorContext::Fatal, allocates through ZunMemory, and post-processes the result; external mode resolves the path, opens/reads a Win32 file, then runs the same post-process. | Canonical pinned-VC7.1 match unit replays all 355 bytes with 28 relocations spanning exact FileSystem/Supervisor/GameErrorContext dependencies, CRT/Win32 calls, and ABI-only archive/allocator/post-process callees |
| FILEIO-005 | observed / mixed dependency state | Exact `OpenFile @ 0x0042C970` fixes the post-process ABI at `0x0042C290` as `(data in ECX, fileSize in EDX, size on stack)`. That callee is now maintained `FileSystem::TryDecryptFromTable` source-present/non-exact. `ZunMemory::Alloc @ 0x00401340` and archive methods `0x00433290/0x004331C0` remain independently unresolved ABI dependencies and are not promoted by the exact caller. | Attested TH09 call-site/disassembly ABI plus exact OpenFile relocation graph and maintained decrypt-table source. |
| BOUNDARY-016 | observed | `0x0042CAD3-0x0042CADF` is thirteen bytes of `CC` after OpenFile and before the candidate at `0x0042CAE0`. Physical ownership remains unassigned. | Attested TH09 target bytes and reviewed `ret 4` at `0x0042CAD0-0x0042CAD2` |
| TOOLCHAIN-011 | compiler-observed | The natural TH09-adjusted OpenFile source is exact under tested `/O2 /Ob0 /Oy-`, `/O2 /Ob1 /Oy-`, and `/Ox /Ob1 /Oy-`; tested `/O1 /Ob1 /Oy-` emits a 0x12F-byte body instead of target 0x163. The canonical unit uses `/O2 /Ob1 /Oy-` without identifying original global optimization/inlining switches. | Focused pinned-VC7.1 matrix and canonical 355/355 relocation-aware replay |
| FILEIO-006 | exact | `FileSystem::Decrypt @ 0x0042C180-0x0042C282` is a `/Gr` six-argument transform with input in ECX, size in EDX, and four stack arguments. It allocates `size` bytes through ZunMemory, emits each chunk by alternating backward output positions while incrementing the XOR value, copies the undecrypted tail, and returns the new allocation; allocation failure returns the original input. | Canonical pinned-VC7.1 match unit replays all 259 bytes with three relocations to `"./system\\global.h"`, `g_ZunMemory`, and `ZunMemory::Alloc`; tested `/O2 /Ob0`, `/O2 /Ob1`, and `/Ox /Ob1` are exact while `/O1` and `/Od` differ |
| BOUNDARY-017 | observed | `0x0042C283-0x0042C28F` is thirteen bytes of `CC` after Decrypt before `0x0042C290`. Physical ownership remains unassigned. | Attested TH09 target bytes and reviewed `ret 0x10` ending at `0x0042C282` |
| FILEIO-007 | observed / conflict | The TH09 crypt-parameter storage at `0x004A1E60` is eight contiguous 12-byte records and `0x004A1EC0-0x004A1EC2` is the signature `85 A4 DA`. Current committed TH08 source is not data-identical: at least its third and fourth parameter records use different chunk/max values, so TH08 data definitions must not be transferred into TH09. Data-definition and translation-unit ownership remain unknown. | Attested TH09 bytes versus committed TH08 HEAD `a45e99fb1942714e6edded20847e32a654d56f97`; TH09 target evidence takes precedence |
| TOOLCHAIN-012 | compiler-observed | Natural Decrypt source is exact under tested `/O2 /Ob0 /Oy-`, `/O2 /Ob1 /Oy-`, and `/Ox /Ob1 /Oy-`; `/O1 /Ob1 /Oy-` emits 244 bytes and `/Od /Ob0` emits 446 bytes. Removing an otherwise unused local does not change the exact optimized body, so the maintained source omits it. These are local codegen observations, not global build flags. | Focused pinned-VC7.1 matrix and canonical 259/259 relocation-aware replay |
| FILEIO-008 | source-present / stable non-exact | `FileSystem::TryDecryptFromTable @ 0x0042C290-0x0042C36B` is now maintained in `src/FileDecryptTable.cpp`. Exact OpenFile fixes its `/Gr` call shape; TH09 target fixes signature bytes `85 A4 DA @ 0x004A1EC0`, eight 12-byte records at `0x004A1E60`, the adjusted-key scan, exact `Decrypt @ 0x0042C180`, and freeing the original allocation. The natural source is exactly 220 bytes but differs broadly in register allocation (current baseline 82/172 ordinary comparable bytes). `/GB` and `/G6` do not improve it, `/G7` is 222 bytes, `/O1` shorter, `/Od` longer, and moving the scan index between block/function scope is byte-identical. Data arrays are extern declarations only; data/TU ownership stays open. | Exact OpenFile caller, target disassembly/data, clean TH08 source-shape comparison with conflicting table values, and bounded VC7.1 negative probes. |
| FILEIO-009 | exact | `FileSystem::Encrypt @ 0x0042C370-0x0042C47A` is the inverse `/Gr` six-argument transform paired with exact Decrypt. It allocates a same-size output buffer, walks each input chunk from alternating backward positions into sequential output while advancing the XOR value, copies any unencrypted tail, and returns the allocation or original input on allocation failure. | Canonical pinned-VC7.1 match unit replays all 267 bytes with three relocations to the same `"./system\\global.h"` literal, `g_ZunMemory`, and `ZunMemory::Alloc` used by Decrypt |
| BOUNDARY-018 | observed | `0x0042C47B-0x0042C47F` is five bytes of `CC` after Encrypt before exact `FileSystem::CheckIfFileAlreadyExists @ 0x0042C480`. Physical ownership remains unassigned. | Attested TH09 target bytes and reviewed `ret 0x10` ending at `0x0042C47A` |
| TOOLCHAIN-013 | compiler-observed | Natural Encrypt source is exact under tested `/O2 /Ob0 /Oy-`, `/O2 /Ob1 /Oy-`, and `/Ox /Ob1 /Oy-`; `/O1 /Ob1 /Oy-` emits 256 bytes and `/Od /Ob0` emits 439 bytes. The same optimized profile family also reproduces exact Decrypt, but this does not prove original TU co-location or project-wide flags. | Focused pinned-VC7.1 matrix and canonical 267/267 relocation-aware replay |
| ZUNMEM-001 | exact | `ZunMemory::Alloc @ 0x00401340-0x00401352` is a release façade over CRT `malloc`: it consumes `(size, debugText)` as two stack arguments, ignores the object/debug text in the body, forwards only size, and returns the allocation. | Canonical pinned-VC7.1 match unit replays 19/19 bytes including the `_malloc @ 0x0047B151` relocation; broad TH09 callers pass the shared ZunMemory object as ECX |
| ZUNMEM-002 | exact | `ZunMemory::Free @ 0x00401360-0x00401372` is the corresponding release façade over CRT `free`, consuming one pointer argument with `ret 4`. | Canonical pinned-VC7.1 match unit replays 19/19 bytes including the `_free @ 0x0047B163` relocation; many target subsystems call it through the shared ZunMemory object |
| ZUNMEM-003 | exact | `ZunMemory::AddToRegistry @ 0x00401380-0x00401389` is a release-build registry façade that returns `ptr` unchanged and consumes `(ptr, size, name)` with `ret 0x0C`; ECX, size, and name do not affect this release body. This does not establish what a DEBUG build did. | Canonical pinned-VC7.1 match unit replays 10/10 bytes; exact `Chain::CreateElem` independently calls it with the app-specific `"funcChainInf"` registry label |
| BOUNDARY-019 | observed | The ZunMemory wrapper cohort has separately unowned `CC` gaps at `0x00401353-0x0040135F` (13 bytes), `0x00401373-0x0040137F` (13 bytes), and `0x0040138A-0x0040138F` (6 bytes). | Attested target disassembly and starts at `0x00401340`, `0x00401360`, `0x00401380`, and `0x00401390` |
| TOOLCHAIN-014 | compiler-observed | Natural out-of-line release bodies for Alloc/Free/AddToRegistry are exact under tested `/O2 /Ob0 /Oy-`, `/O2 /Ob1 /Oy-`, and `/Ox /Ob1 /Oy-`. `/O1` shortens Alloc/Free to 16 bytes while AddToRegistry remains exact; `/Od` lengthens all three. The reconstruction keeps out-of-line definitions so existing exact callers do not gain new inlining; original TH09 header-inline status, emitting TU, and source ownership remain unknown. | Focused pinned-VC7.1 matrix plus 19/19, 19/19, and 10/10 canonical replays |
| PBG-001 | exact | `PbgArchive::FindEntry @ 0x00433050-0x00433099` treats the archive prefix as an entry-array pointer at `+0` and signed entry count at `+4`, walks 0x10-byte records, compares the caller filename against the record's first pointer with `_stricmp`, and returns the matching record or NULL. | Canonical pinned-VC7.1 match unit replays 74/74 bytes with the `_stricmp @ 0x004895F9` relocation; target control flow independently proves the two-field state prefix and record stride |
| PBG-002 | exact | `PbgArchive::GetEntryDecompressedSize @ 0x00433290-0x004332AC` calls FindEntry on the same object and returns the 32-bit field at entry offset `+0x8`, or zero when no entry exists. The prior handoff estimate of 27 bytes was incorrect; target bytes and the canonical object both establish a 29-byte extent. | Canonical pinned-VC7.1 match unit replays 29/29 bytes with its sole relocation to exact FindEntry |
| PBG-003 | observed-layout | The query packet proves only a PbgArchive state prefix (`entries @ +0`, `entryCount @ +4`) and a 0x10-byte entry record view with `filename @ +0` and decompressed-size field at `+8`. The reconstruction keeps the two remaining entry fields generically named and does not assert total PbgArchive class size or backend types. | Exact FindEntry/GetEntryDecompressedSize codegen plus target 0x10 entry stride; adjacent TH08 names are hypothesis support only |
| BOUNDARY-020 | observed | `0x0043309A-0x0043309F` is six bytes of `CC` after FindEntry; `0x004332AD-0x004332AF` is three bytes of `CC` after GetEntryDecompressedSize. Physical ownership remains unassigned. | Attested TH09 target bytes and reviewed `ret 4` extents |
| TOOLCHAIN-015 | compiler-observed | Natural FindEntry and GetEntryDecompressedSize source is exact under tested `/O2 /Ob0 /Oy-`, `/O2 /Ob1 /Oy-`, and `/Ox /Ob1 /Oy-`; `/O1` emits 54 and 26 bytes and `/Od` emits 101 and 46 bytes respectively. This is local profile evidence only. | Focused pinned-VC7.1 matrix plus canonical 74/74 and 29/29 replays |
| PBG-004 | exact | `PbgArchive::ReadDecompressEntry @ 0x004331C0-0x00433280` finds an entry, opens the archive through the backend at `this+0xC` using the filename at `this+8`, derives compressed size from `next.dataOffset - entry.dataOffset`, allocates temporary compressed storage with `GlobalAlloc`, seeks/reads through backend virtual slots, decodes into the caller buffer through `Lzss::Decode`, frees the temporary buffer, and returns the decoded pointer. | Canonical pinned-VC7.1 match unit replays all 193 bytes with seven relocations to exact FindEntry, open/seek mode storage, GlobalAlloc/GlobalFree, and Lzss::Decode |
| PBG-005 | observed-layout / ABI | ReadDecompressEntry extends the target-proven archive state prefix to `archiveFilename @ +8` and backend pointer `@ +0xC`. In this routine the backend vtable slots actually exercised are Open at `+0`, Read at `+8`, and Seek at `+0x18`; intermediate slots remain semantically unknown. The entry `+4` field is used as a backend seek offset and the next entry's `+4` field bounds compressed size, supporting the reconstructed `dataOffset` name. | Attested TH09 virtual-call/dataflow evidence plus exact ReadDecompressEntry codegen; full backend/PbgArchive layouts and ownership remain unknown |
| PBG-006 | observed-data / ABI | The archive open-mode storage at `0x004A1EC8` has element 0 pointing to the target string `"r" @ 0x00490F1C`; seek-mode storage begins at `0x00490F08` with element 0 equal to zero. ReadDecompressEntry uses those first elements. `Lzss::Decode @ 0x00433AA0` is called with compressed data in ECX, compressed size in EDX, and `(outBuffer, decompressedSize)` on the stack. Later LZSS review establishes repeatable 470/470 source codegen for that routine while keeping its origin noncanonical/unknown. | Attested target data/string reads, call-site ABI, and canonical relocation graph |
| BOUNDARY-021 | observed | `0x00433281-0x0043328F` is fifteen bytes of `CC` after ReadDecompressEntry before exact GetEntryDecompressedSize. Physical ownership remains unassigned. | Attested TH09 target bytes and reviewed `ret 8` ending at `0x00433280` |
| TOOLCHAIN-016 | compiler-observed | A TH09-shaped ReadDecompressEntry with separate `free_error`, `read_error`, and success blocks is exact under tested `/O2 /Ob0 /Oy-`, `/O2 /Ob1 /Oy-`, and `/Ox /Ob1 /Oy-`; `/O1` emits 174 bytes and `/Od` 270. The current committed TH08 shape initializes `compressedData` to NULL and uses a broader common error cleanup; that produces different optimized code for TH09 and is not copied. | Focused pinned-VC7.1 control-flow probes and canonical 193/193 replay |
| LZSS-001 | codegen-exact / origin-unknown | `0x00433AA0-0x00433C75` has the exact VC7.1 code shape of the reconstructed `Lzss::Decode`: `/Gr` carries compressed input in ECX and input size in EDX, with output pointer and output size on the stack; a NULL output requests `GlobalAlloc(0, outSize)`, and failure returns NULL. The stripped target does not currently establish authored-game versus third-party origin, so the candidate remains `unknown/review` and is excluded from canonical authored match totals. | Replayable pinned-VC7.1 match unit reproduces all 470 bytes with four relocations; exact `PbgArchive::ReadDecompressEntry` independently calls the same ABI |
| LZSS-002 | observed-data / ownership-open | Decode uses an 8192-byte circular dictionary beginning at `0x004CC430`, masking dictionary positions with `0x1FFF`. The same storage is referenced by candidates at `0x00433CB0`, `0x00433E00`, and `0x00434020`, so Decode does not establish the dictionary definition, translation-unit owner, or complete compression-state layout. | Attested TH09 data xrefs and effective-address arithmetic; three Decode relocation destinations all resolve to `0x004CC430` |
| LZSS-003 | observed | The decoder consumes an MSB-first flag stream, writes literal bytes directly to output and dictionary, decodes 13-bit dictionary offsets plus 4-bit length values, treats zero offset as the terminator, copies `decodedLength + 3` bytes from the circular dictionary, and drains the remainder of the current input byte through the same fetch/checksum path before returning the output pointer. The accumulated input checksum is discarded. | Attested TH09 control flow plus the natural source that reproduces the full function; adjacent TH08/TH095 algorithms supplied hypotheses only |
| BOUNDARY-022 | observed | `Lzss::Decode` ends with `ret 8` at `0x00433C73-0x00433C75`; `0x00433C76-0x00433C7F` is ten bytes of `CC` before the separate candidate at `0x00433C80`. Physical ownership of the gap remains unassigned. | Attested TH09 target bytes and reviewed function extent |
| TOOLCHAIN-017 | compiler-observed | Natural Decode source is structurally exact under tested `/O2 /Ob0 /Oy-`, `/O2 /Ob1 /Oy-`, `/O2 /Ob2 /Oy-`, and `/Ox /Ob1 /Oy-`; `/O1 /Ob1 /Oy-` emits 436 bytes and `/Od /Ob0` emits 852. No identifier-bucket or patched `var_order` technique is needed for TH09. A first probe that omitted braces around the multi-statement trailing-bit macro compiled to a 461-byte self-loop and was rejected as a source-shape bug. | Focused pinned-VC7.1 build-3077 matrix and 470/470 relocation-aware replay |
| LZSS-004 | codegen-exact / origin-unknown | `0x00433C80-0x00433CA4` has the exact code shape reconstructed as `Lzss::InitTree(int root)`: root arrives in ECX, the sentinel node at index 0x2000 stores root in its `right` field, and the selected root node receives parent 0x2000 with left/right cleared. Origin remains `unknown/review`, so no canonical authored match row is added. | Replayable pinned-VC7.1 unit reproduces 37/37 bytes with four relocations that all solve to tree base `0x004B4420` |
| LZSS-005 | codegen-exact / origin-unknown | `0x00433CB0-0x00433CD9` has the exact code shape reconstructed as `Lzss::InitEncoderState()`: it clears exactly 0x2000 bytes at dictionary `0x004CC430`, then clears parent/left/right for 0x2001 12-byte tree nodes beginning at `0x004B4420`. Origin and data ownership remain unresolved. | Replayable pinned-VC7.1 unit reproduces 42/42 bytes with one dictionary and two tree relocations |
| LZSS-006 | observed-layout / ownership-open | The tree view required by the two initialization helpers is 0x2001 records of `{parent,left,right}` with 12-byte stride at base `0x004B4420`. Relocation addends independently recover sentinel `right` at tree+0x18008 and the post-loop pointer at tree+0x18010. This establishes the storage geometry used by the helpers, not the original data definition or object/TU owner. | TH09 effective-address arithmetic plus relocation addends from exact codegen |
| BOUNDARY-023 | observed | `0x00433CA5-0x00433CAF` is eleven bytes of `CC` after the 37-byte InitTree candidate. `0x00433CDA-0x00433CDF` is six bytes of `CC` after the 42-byte InitEncoderState candidate; `0x00433CE0` immediately begins code that IDA associates with another function, so later bytes are not padding owned by InitEncoderState. | Attested TH09 bytes, reviewed returns, and native function-chunk lookup |
| TOOLCHAIN-018 | compiler-observed | Natural InitTree and InitEncoderState source is exact under tested `/O2 /Ob0 /Oy-`, `/O2 /Ob1 /Oy-`, and `/Ox /Ob1 /Oy-`. `/O1 /Ob1` changes InitTree while leaving InitEncoderState exact; `/Od` expands both. These are local codegen observations only. | Focused pinned-VC7.1 matrix and repeatable 37/37 plus 42/42 replays |
| LZSS-007 | boundary-corrected / codegen-exact / origin-unknown | `0x00433CE0-0x00433D32` is a distinct 83-byte candidate reconstructed as `Lzss::ContractNode(oldNode, newNode)`, not authored continuation of DeleteString. Native IDA currently attaches it as a chunk of `sub_433FC0` because the only code xref is DeleteString's tail jump; a separately compiled VC7.1 ContractNode symbol reproduces all 83 bytes, while the separately compiled DeleteString contains a REL32 relocation that resolves exactly to `0x00433CE0`. The tracking inventory therefore adds this candidate explicitly while keeping its origin `unknown/review`. | Target-bound 83/83 ContractNode replay, separate 83/83 DeleteString replay and tail-call relocation, native xref/chunk evidence |
| LZSS-008 | codegen-exact / origin-unknown | `Lzss::ReplaceNode @ 0x00433D40-0x00433DB2` relinks the old node's parent to a replacement, copies the complete 12-byte node record, repairs both child parent links, and clears the old parent field. `Lzss::FindNextNode @ 0x00433DC0-0x00433DF0` starts at the input node's left child and follows right links to the terminal descendant. Both names remain reconstructed hypotheses and origins remain unknown. | Repeatable target-bound 115/115 and 49/49 VC7.1 replays with tree relocations only |
| LZSS-009 | codegen-exact / origin-unknown | `Lzss::AddString @ 0x00433E00-0x00433FB5` compares up to 18 bytes in the 0x2000-byte circular dictionary, records the best candidate in the caller's match-position output, replaces a full-length match through ReplaceNode, or inserts the new node on the left/right search path. VC7.1 unrolls the 18-byte comparison into six-byte groups in the exact target body. | Repeatable 438/438 replay with 19 relocations, including the ReplaceNode call and neutral tree/dictionary bases |
| LZSS-010 | codegen-exact / origin-unknown | `Lzss::DeleteString @ 0x00433FC0-0x00434012` ignores already-detached nodes, contracts a one-child node through the separately recovered ContractNode, or obtains a replacement through FindNextNode, recursively deletes that replacement, and tail-calls ReplaceNode. Its logical source extent is the 83-byte main body only; the IDA-attached ContractNode chunk is separately owned. | Repeatable 83/83 replay with relocations to ContractNode, FindNextNode, recursive DeleteString, ReplaceNode, and the tree |
| BOUNDARY-024 | observed | Tree-helper gaps are `0x00433D33-0x00433D3F` (13 CC bytes), `0x00433DB3-0x00433DBF` (13), `0x00433DF1-0x00433DFF` (15), `0x00433FB6-0x00433FBF` (10), and `0x00434013-0x0043401F` (13). Physical ownership remains unassigned. | Attested TH09 target bytes and reviewed function returns |
| TOOLCHAIN-019 | compiler-observed | Natural ContractNode, ReplaceNode, FindNextNode, AddString, and DeleteString sources are all structurally exact under tested `/O2 /Ob0 /Oy-`, `/O2 /Ob1 /Oy-`, and `/Ox /Ob1 /Oy-`. `/O1 /Ob1` emits 58/97/29/183/79 bytes and `/Od /Ob0` emits 132/183/63/280/134 respectively, so the exact tree-helper family is constrained to the tested optimized profile family without a project-wide flag claim. | Focused pinned-VC7.1 build-3077 matrix plus five relocation-aware exact replays |
| LZSS-011 | source-present / codegen-blocked / origin-unknown | `0x00434020-0x0043425A` is reconstructed semantically as `Lzss::Encode(unsigned char *in, int inSize, int *outSize)`: ECX carries input, EDX input size, one stack argument receives compressed size, output is allocated with `GlobalAlloc(0, inSize * 2)`, the exact-codegen LZSS initialization/tree helpers drive lookahead and match maintenance, literal or offset/length tokens are packed MSB-first, a zero-offset terminator is emitted, and the final output length is stored through the caller pointer. The maintained source is intentionally noncanonical because stock VC7.1 does not yet reproduce the target register allocation. | Attested target caller/callee/dataflow evidence plus maintained source and focused compiler probes |
| TOOLCHAIN-020 | unresolved-codegen | The natural Encode source is 570 bytes under tested `/O2 /Ob0 /Oy-`, `/O2 /Ob1 /Oy-`, `/O2 /Ob2 /Oy-`, and `/Ox /Ob1 /Oy-`, versus target 571; `/O1 /Ob1` emits 469 and `/Od /Ob0` 965. The optimized candidate has the correct eight-dependency relocation graph but assigns `outBits` to EBX and `outBitMask` to a stack byte, while the target keeps the bit mask in BL and the 32-bit output accumulator at `[ebp-4]`, with related input-size/cursor homes shifted. Reordering C++ declarations according to the adjacent TH08 `var_order` list and deleting the dead checksum both leave the 570-byte stock-compiler body unchanged. TH08's `#pragma var_order` support is implemented by that reconstruction repository's replacement `C1XX.DLL` wrapper, so it is not introduced into TH09 merely to force equality. | Pinned VC7.1 matrix, target/probe instruction alignment, and committed TH08 `scripts/pragma_var_order.cpp` / `create_devenv.py` evidence |
| BOUNDARY-025 | observed | The Encode candidate ends with `ret 4` at `0x00434258-0x0043425A`; `0x0043425B-0x0043425F` is five bytes of `CC` before the separate candidate at `0x00434260`. Physical ownership of the gap remains unassigned. | Attested TH09 target bytes and reviewed function extent |
| ASCII-001 | exact | `AsciiManager::DeletedCallback @ 0x00434260-0x0043427C` is the calc-chain deleted callback for the TH09 AsciiManager lifecycle. It ignores its callback argument, calls `AnmManager::ReleaseAnm` for slots 1 and 3 through `g_AnmManager @ 0x004DC550`, and returns success. | Canonical pinned-VC7.1 match unit replays all 29 bytes with four relocations; TH09 `AsciiManager` added-callback code independently loads `"ascii.anm"` in slot 1 and `"capture.anm"` in slot 3, and the chain-registration routine stores this function at the calc ChainElem deleted-callback field. |
| ASCII-002 | exact | `AsciiManager::CutChain @ 0x00434280-0x0043429E` cuts the calc ChainElem at `0x004DC504` and the low-priority draw ChainElem at `0x004CE438` through exact `Chain::Cut`; it does not cut the high-priority draw ChainElem at `0x004DC524`. | Canonical pinned-VC7.1 match unit replays all 31 bytes with six relocations to the two ChainElem objects, `g_Chain @ 0x004ACFC8`, and exact `Chain::Cut @ 0x0042C8C0`; the broad shutdown caller at `0x00431B70` invokes this helper. |
| ASCII-003 | observed / corroborated | TH09 lifecycle wiring identifies the state at `0x004CE458` as an AsciiManager-sized object of `0xE0AC` bytes: its added callback clears exactly `0xE0AC`, loads `"ascii.anm"` and `"capture.anm"`, and `0x004CE458 + 0xE0AC == 0x004DC504`, the calc ChainElem used by registration and exact CutChain. The registration routine uses calc priority 1 and draw priorities 34 and 23. Complete AsciiManager layout and static-data/TU ownership remain open. | Attested TH09 decompilation/xrefs at `0x004344E0` and `0x00435CA0`, exact lifecycle relocation graphs, plus clean committed TH08 HEAD `a45e99fb1942714e6edded20847e32a654d56f97` as naming/source-shape corroboration only. |
| ASCII-004 | compiler-observed | Natural `AsciiManager::DeletedCallback` and `AsciiManager::CutChain` source is exact under tested `/O2 /Ob0 /Oy-`, `/O2 /Ob1 /Oy-`, `/O2 /Ob2 /Oy-`, and `/Ox /Ob1 /Oy-`. Tested `/O1 /Ob1` keeps DeletedCallback exact but makes CutChain 32 bytes; `/Od /Ob0` expands both. These are local exact-producing profiles, not original project-wide flag evidence. | Focused pinned VC7.1 build-3077 profile matrix and two repeated target-bound canonical replays. |
| BOUNDARY-026 | observed | `0x0043427D-0x0043427F` is three bytes of `CC` between exact DeletedCallback and exact CutChain; `0x0043429F` is one `CC` byte between exact CutChain and the candidate at `0x004342A0`. Physical ownership of both gaps remains unassigned. | Verified TH09 target bytes and reviewed exact function extents. |

| ASCII-005 | exact | `AsciiManager::AddString @ 0x004342A0-0x00434321` appends into a 256-entry text queue at `this+0x2264` with 0x60-byte records. The queue count is at `+0x8264`; each entry contains `text[64]`, a three-component position at `+0x40`, color at `+0x4C`, scales at `+0x50/+0x54`, selected state at `+0x58`, and GUI state at `+0x5C`. TH09 copies current manager color/scales/GUI mode from `+0x8268/+0x826C/+0x8270/+0x8274` and unconditionally clears the entry selected state. | Canonical pinned-VC7.1 match unit replays all 130 bytes with no relocations; target callers pass the AsciiManager object rooted at `0x004CE458`, a three-dword position, and a C string. Clean committed TH08 HEAD supplied only the method/field naming hypothesis; its conditional selected-state behavior differs from TH09 and was not transferred. |
| ASCII-006 | exact | `AsciiManager::AddFormatText @ 0x00434330-0x00434366` is the variadic text-queue wrapper. As a variadic member its target ABI places `this`, position, and format string at stack offsets `+8/+0xC/+0x10`; it formats into a 512-byte local buffer with `vsprintf` and forwards that buffer to exact AddString. | Canonical pinned-VC7.1 match unit replays all 55 bytes with two relocations, to CRT `vsprintf @ 0x0047C335` and exact AddString. Native IDA reports 35 call sites spread across gameplay/HUD routines, independently supporting a shared text-formatting service. |
| ASCII-007 | observed | The maintained `AsciiManager` type intentionally exposes only the TH09-proven text-state window inside the 0xE0AC state block: unknown bytes through `+0x2264`, 256 exact-size 0x60 text records, queue/style fields through `+0x8274`, then unknown bytes to `+0xE0AC`. The unknown regions are not claims about constituent fields or subobjects. | TH09 AddString exact codegen offsets plus the lifecycle memset size and ChainElem boundary from ASCII-003. |
| TOOLCHAIN-021 | compiler-observed | Natural AddString/AddFormatText are exact under tested `/O2 /Ob0 /Oy-`, `/O2 /Ob1 /Oy-`, `/O2 /Ob2 /Oy-`, and `/Ox /Ob1 /Oy-`; `/O1 /Ob1` emits 109/51 bytes and `/Od /Ob0` emits 234/68 instead of target 130/55. TH08's reconstruction-only `var_order` support is unnecessary for TH09 AddString. | Focused pinned VC7.1 build-3077 matrix and two repeated tracked-source replays. |
| BOUNDARY-027 | observed | `0x00434322-0x0043432F` is fourteen `CC` bytes after exact AddString; `0x00434367-0x0043436F` is nine `CC` bytes after exact AddFormatText before the candidate at `0x00434370`. Physical ownership of both gaps remains unassigned. | Verified TH09 target bytes and reviewed exact function extents. |

| ASCII-008 | exact | `AsciiManager::SetSpaceWidth @ 0x004343C0-0x004343CF` stores its sole integer argument at AsciiManager `+0x8280`. The Reset candidate calls it with the TH09 default value 9. | Canonical pinned-VC7.1 match unit replays all 16 bytes with no relocations; the target function ends immediately before the independent candidate at `0x004343D0`. |
| ASCII-009 | exact | `AsciiManager::AddedCallback @ 0x004344E0-0x00434538` clears exactly 0xE0AC bytes, preloads `"ascii.anm"` slot 1 into `+0x8288` and `"capture.anm"` slot 3 into `+0x828C`, returns -1 if either result is null, otherwise calls the Reset candidate at `0x004343E0` and returns 0. Unlike committed TH08, TH09 does not call a separate InitializeVms helper here. | Canonical pinned-VC7.1 match unit replays all 89 bytes with seven relocations to both strings, `g_AnmManager @ 0x004DC550`, candidate `0x0043C7A0`, and Reset. Registration at `0x00435CA0` independently installs this function as the AsciiManager calc-chain added callback. |
| ASCII-010 | exact / authored-game | `AnmManager::PreloadAnm @ 0x0043C7A0-0x0043C7F9` is independently repository-canonical exact. It starts ReadAnmEntries, marks the entry for main-thread postload, waits with `Sleep(1)` until completion or Supervisor shutdown, and returns the loaded entry or null on shutdown. The exact AsciiManager::AddedCallback relocation remains useful caller-side evidence but no longer carries the callee's status. | Current canonical `AnmManager::PreloadAnm` ledger/match unit, target body/callers, and clean committed TH08 source-family corroboration. |
| TOOLCHAIN-022 | compiler-observed | Natural SetSpaceWidth and AddedCallback are exact under tested `/O2 /Ob0 /Oy-`, `/O2 /Ob1 /Oy-`, `/O2 /Ob2 /Oy-`, and `/Ox /Ob1 /Oy-`. `/O1 /Ob1` keeps SetSpaceWidth exact but emits AddedCallback as 88 rather than 89 bytes; `/Od /Ob0` expands both. | Focused pinned VC7.1 build-3077 matrix and two repeated tracked-source replays. |
| BOUNDARY-028 | observed | SetSpaceWidth owns exactly `0x004343C0-0x004343CF`; `0x004343D0` begins an independent 3-byte candidate. AddedCallback owns `0x004344E0-0x00434538`; `0x00434539-0x0043453F` is seven `CC` bytes before candidate `0x00434540`. The CC gap remains physically unassigned. | Verified target bytes, IDA boundaries, and canonical replays. |

| ASCII-011 | exact | `AsciiManager::Reset @ 0x004343E0-0x004344DA` clears the three 0x2A4-byte VM states at `+0/+0x2A4/+0x548`, the 0x6000-byte text queue at `+0x2264`, and only the additional target-observed opaque regions `+0x82A0` size 0x1528, `+0x97C8` size 0x0FE0, and `+0xB240` size 0x15E0. It resets the observed queue/style/index fields while intentionally leaving `+0x8298` untouched, sets anchor 3 on the second and third VM flag words, initializes the second VM with sprite 97 and the first with sprite 32 through candidate `0x00412800`, sets second-VM `pos.z` to 0.1f, clears manager selected state again, and calls exact SetSpaceWidth(9). | Canonical pinned-VC7.1 match unit replays all 251 bytes with three relocations: two to candidate `0x00412800` and one to exact SetSpaceWidth. Exact AddedCallback calls Reset on successful ANM preload, independently fixing AsciiManager ownership. |
| ASCII-012 | observed | The maintained AsciiManager layout now exposes only Reset-proven VM detail before the text queue: three consecutive 0x2A4-byte VM records with a proven flag word at VM `+0x1F8`, two-bit anchor at bits 11-12, and `Float3 pos` at VM `+0x208`; the entire manager `+0x7EC-0x2264` interval remains opaque. The three later memset regions remain opaque arrays, and `+0x8298` remains unknown. | TH09 Reset offsets/codegen plus exact AddString queue offset and lifecycle object size; committed TH08 was used only to propose the `largeText/smallScoreText/popupText` and anchor/position field names after TH09 had fixed sizes and offsets. |
| ASCII-013 | exact / authored-game | `AnmLoaded::InitializeAndSetSprite @ 0x00412800-0x00412828` is independently repository-canonical exact. ECX is the loaded-ANM owner; stack arguments are VM pointer and sprite index; the body initializes the VM, publishes the owner at VM +0x204, calls exact SetSprite, and returns with `ret 8`. | Current canonical match unit, exact AsciiManager::Reset and Supervisor startup callers, and TH09 target disassembly. |
| TOOLCHAIN-023 | compiler-observed | Natural Reset with only TH09-proven partial layout is exact under tested `/O2 /Ob0 /Oy-`, `/O2 /Ob1 /Oy-`, `/O2 /Ob2 /Oy-`, and `/Ox /Ob1 /Oy-`; `/O1 /Ob1` emits 226 instead of 251 bytes and `/Od /Ob0` emits 379. The exact result persists after collapsing all unobserved VM flag bits into anonymous unknown bit ranges. | Focused pinned VC7.1 build-3077 matrix and two repeated tracked-source replays. |
| BOUNDARY-029 | observed | Reset owns `0x004343E0-0x004344DA`. `0x004344DB-0x004344DF` is five bytes of `CC` before exact AddedCallback at `0x004344E0`; the gap remains physically unassigned. | Verified target bytes, IDA extent, and canonical replay. |

| ASCII-014 | exact | `AsciiManager::DrawStrings @ 0x00434540-0x00434694` renders the queued non-high-priority text through the first 0x2A4-byte VM. It sets VM visible plus anchor 3, copies each 0x60-byte queue record position/scale, computes integer `spaceWidth * scaleX`, switches gameplay viewport mode 0/2 around `isGui` transitions with a vertex-buffer flush, interprets newline with TH09 line height 14.0f, advances spaces without drawing, maps other unsigned bytes with `glyph - 0x20`, copies record color, draws the VM, and restores viewport mode 2 when required. | Canonical pinned-VC7.1 replay is 341/341 bytes with twelve relocations. Callers at `0x00431540` and `0x00435C00` use the same AsciiManager state; the former first queues `"Press Shot Button"` through exact AddFormatText, fixing the text-render role independently from adjacent-game names. |
| ASCII-015 | observed | Exact DrawStrings constrains additional VM fields without establishing complete AnmVm ownership: `scale` is two floats at VM `+0x18/+0x1C`, color1 is a 32-bit field at `+0x1F0`, visible is flag bit 0 in the proven flag word at `+0x1F8`, and the loaded-sprite pointer is at `+0x224`. The remaining bytes stay opaque. | Target offsets in exact DrawStrings plus prior Reset-proven AnmVm size/flag/position offsets. |
| ASCII-016 | exact dependency closure | DrawStrings relocations originally only identified four independent callees. All four are now independently repository-canonical exact: `Supervisor::ConfigureGameplayViewport(int) @ 0x00401390`, `AnmLoaded::GetSprite(int) @ 0x00434390`, `AnmManager::FlushVertexBuffer() @ 0x004396A0`, and `AnmManager::DrawNoRotation(AnmVm *) @ 0x0043A950`. DrawStrings exactness is still independent and does not transfer between functions. | Current canonical ledgers and per-function target-bound replay units; original DrawStrings relocation graph remains the caller-side ownership evidence. |
| TOOLCHAIN-024 | compiler-observed | Natural TH09-specific DrawStrings source is exact under tested `/O2 /Ob0 /Oy-`, `/O2 /Ob1 /Oy-`, `/O2 /Ob2 /Oy-`, and `/Ox /Ob1 /Oy-`; `/O1 /Ob1` emits 328 rather than 341 bytes and `/Od /Ob0` emits 500. No adjacent reconstruction `var_order`/identifier-bucket mechanism is needed. | Focused pinned VC7.1 build-3077 matrix and repeated tracked-source replays. |
| TOOLCHAIN-025 | compiler-observed | Adding the target-supported `Supervisor::ConfigureGameplayViewport(int)` declaration to the shared Supervisor class leaves existing ChainRun target code, relocation offsets, and solved target destinations unchanged, but VC7.1 debug/COFF local-label symbol numbers in `ChainRun.obj` advance by two. The `chain-run-calc` and `chain-run-draw` replay manifests therefore track the new compiler-internal `$L...` names while preserving every target address. This is replay metadata maintenance, not new function exactness. | Before/after ChainRun COFF relocation tables plus exact 212/212 replays for both ChainRun functions and a 27-unit Supervisor/Ascii header-consumer replay. |
| BOUNDARY-030 | observed | DrawStrings owns `0x00434540-0x00434694`; `0x00434695-0x0043469F` is eleven bytes of `CC` before candidate `0x004346A0`. Physical ownership of the gap remains unassigned. | Verified TH09 target bytes, native IDA extent, and canonical replay. |

| ASCII-017 | exact | `AsciiManager::CreateScorePopup @ 0x004346A0-0x00434739` queues a numeric popup into a two-player array at manager `+0xB240`. A shared ring index at `+0x8290` wraps at 100; the target computes `(playerIndex * 100 + ringIndex) * 0x38`. Positive numbers are stored as reversed decimal digits, zero stores digit 0, and negative values store sentinel digit 10. The method then stores character count, color, resets the popup timer through candidate `ZunTimer::operator=(int) @ 0x00401500`, copies the Float3 position, and increments the ring index. | Canonical pinned-VC7.1 match unit replays all 154 bytes with one relocation. Target caller `0x0041D150` invokes it from score-growth logic and uses the same source index in nearby `1 - index` player selection, independently supporting the binary `playerIndex` role. |
| ASCII-018 | observed | `AsciiManagerPopup` is exactly 0x38 bytes in the TH09 score-popup path: `text[12] @ +0`, `Float3 position @ +0x0C`, 32-bit color `+0x18`, 0x0C-byte timer `+0x1C`, two-float scale `+0x28`, in-use byte `+0x30`, character-count byte `+0x31`, and otherwise unknown bytes through `+0x38`. Two 100-record banks occupy manager `+0xB240-0xDDFF`; bytes after `+0xDE00` remain opaque. Exact Reset clears only bank 0 (`+0xB240`, 0x15E0 bytes); no broader Reset clearing claim is made. | Exact CreateScorePopup address arithmetic and field stores plus the already exact Reset memset extent. Clean committed TH08 HEAD `a45e99fb1942714e6edded20847e32a654d56f97` corroborates the 0x38 record field names only; TH09 bank count/indexing is target-local. |
| TOOLCHAIN-026 | compiler-observed | Natural CreateScorePopup with the TH09 two-player/100-entry layout is exact under tested `/O2 /Ob0 /Oy-`, `/O2 /Ob1 /Oy-`, `/O2 /Ob2 /Oy-`, and `/Ox /Ob1 /Oy-`; `/O1 /Ob1` and `/Od /Ob0` are size mismatches. No `var_order`, target-byte embedding, padding, assembly, volatile, or register-directed source is used. | Focused pinned VC7.1 build-3077 profile matrix and two repeated tracked-source replays of all nine AsciiManager units. |
| BOUNDARY-031 | observed | CreateScorePopup owns `0x004346A0-0x00434739`; `0x0043473A-0x0043473F` is six bytes of `CC` before candidate `0x00434740`. Physical ownership of the gap remains unassigned. | Native IDA extent plus PE-aware `objdump` target-byte view and canonical replay. |

| ZUNTIMER-002 | exact | `ZunTimer::SetCurrent @ 0x004014D0-0x004014E8` stores `current=value` at `+8`, converts the same integer to float and stores it at `+4`, writes `previous=-999999` at `+0`, and returns with `ret 4`. | Canonical pinned-VC7.1 match unit replays all 25 bytes with no relocations. TH09 target instructions independently fix the `-999999` sentinel and float conversion; the TH08 `subFrame=0/-999` implementation was probed and rejected as a TH09 mismatch. |
| ZUNTIMER-003 | exact | `ZunTimer::operator=(int) @ 0x00401500-0x00401508` is a nine-byte member wrapper that emits a frame prologue/epilogue and tail-jumps exact SetCurrent. It is called broadly across gameplay and rendering subsystems, including exact AsciiManager::CreateScorePopup. | Canonical pinned-VC7.1 match unit replays all 9 bytes with one REL32 relocation to `0x004014D0`. Native IDA xrefs confirm the wrapper is the sole code xref into SetCurrent. |
| ZUNTIMER-004 | superseded by exact closure | Early boundary triage left `0x004014F0`, `0x00401510`, and `0x004014B0` unnamed/unknown. That status is superseded: ZUNTIMER-005/006/007 now identify and canonically close them as `ZunTimer::Tick`, `ZunTimer::operator float`, and postfix `ZunTimer::operator++(int)`. | Current canonical ledgers and ZUNTIMER-005/006/007; original boundary observations remain valid. |
| TOOLCHAIN-027 | compiler-observed | Natural TH09 SetCurrent is exact under tested optimized O1/O2/Ox profiles and mismatches under `/Od`. Natural integer assignment remains the exact 9-byte tail-jump under `/Ob0` or `/Ob1`; `/O2 /Ob2` inlines SetCurrent into a 25-byte body and mismatches the target wrapper. | Focused pinned VC7.1 build-3077 profile matrix plus two repeated tracked-source replays. |
| BOUNDARY-032 | observed | SetCurrent owns `0x004014D0-0x004014E8`, followed by seven unassigned `CC` bytes through `0x004014EF`; independent candidate `0x004014F0-0x004014F3` is followed by twelve `CC` bytes through `0x004014FF`; assignment owns `0x00401500-0x00401508`, followed by seven `CC` bytes through `0x0040150F` before independent candidate `0x00401510`. | PE-aware target bytes and native IDA candidate extents. |

| ZUNTIMER-005 | exact | `ZunTimer::Tick @ 0x004014B0-0x004014CB` copies `current` (`+8`) into `previous` (`+0`), calls candidate `Supervisor::TickTimer(int *, float *) @ 0x0042F4F0` with `&current` and `&subFrame`, then returns the updated current value. | Canonical pinned-VC7.1 replay is 28/28 bytes with two relocations, to `g_Supervisor @ 0x004B3100` and candidate `0x0042F4F0`. The target ABI/dataflow independently establish the timer-state relationship; committed TH08 Tick source is corroboration only. |
| ZUNTIMER-006 | exact | `ZunTimer::operator float @ 0x004014F0-0x004014F3` returns only the float field `subFrame @ +4` in ST0. | Canonical pinned-VC7.1 replay is 4/4 bytes with no relocations. TH08's `current + subFrame` conversion conflicts with TH09 target code and is rejected; committed-only TH095 at observed HEAD `b9937d8194520f645ff0b6f81140b78025b19988` corroborates the subFrame-only conversion. |
| ZUNTIMER-007 | exact | Postfix `ZunTimer::operator++(int) @ 0x00401510-0x00401517` calls exact Tick and returns with `ret 4`, consuming the dummy postfix argument. | Canonical pinned-VC7.1 replay is 8/8 bytes with one REL32 relocation to Tick `0x004014B0`; broad target call sites independently support timer increment usage. |
| TOOLCHAIN-028 | compiler-observed | Natural Tick and postfix increment are exact with tested `/O2 /Ob0\|1` and `/Ox /Ob1`; Tick becomes 27 rather than 28 bytes under `/O1 /Ob1`, postfix increment is inlined to 26 bytes under `/O2 /Ob2`, and `/Od` mismatches all three methods. Float conversion remains 4 bytes throughout the tested optimized profiles. | Focused pinned VC7.1 build-3077 profile matrix and two repeated tracked-source replays. |
| TOOLCHAIN-029 | compiler-observed | Adding the target-supported `Supervisor::TickTimer(int *, float *)` declaration changes no ChainRun target code, relocation offsets, or solved destinations, but VC7.1 renumbers the compiler-internal ChainRun `$L...`/restart symbols by +3. The calc/draw manifests were updated only to the observed local symbol names, after both 212-byte functions and all 25 affected Supervisor/ZunTimer consumers replayed exact twice. | Before/after COFF relocation tables and target-bound replay. This is metadata maintenance, not new ChainRun exactness. |
| BOUNDARY-033 | observed | Tick owns `0x004014B0-0x004014CB` followed by four unassigned `CC` bytes through `0x004014CF`; float conversion owns `0x004014F0-0x004014F3` followed by twelve unassigned `CC` bytes through `0x004014FF`; postfix increment owns `0x00401510-0x00401517` followed by eight unassigned `CC` bytes through `0x0040151F` before candidate `0x00401520`. | PE-aware target bytes, native IDA extents, and canonical replays. |

| ZUNTIMER-009 | exact | `ZunTimer::operator>=(int) @ 0x00401520-0x00401533` returns whether signed `current @ +8` is greater than or equal to the integer argument. | Canonical pinned-VC7.1 replay is 20/20 bytes with no relocations. Attested TH09 xrefs show broad gameplay/rendering use of the entry; committed TH08/TH095 comparison definitions were consulted only after TH09 fixed the operation. |
| ZUNTIMER-010 | exact | `ZunTimer::operator<(int) @ 0x00401540-0x00401553` returns whether signed `current @ +8` is less than the integer argument. | Canonical pinned-VC7.1 replay is 20/20 bytes with no relocations. Attested TH09 xrefs show broad use independent of address adjacency; committed adjacent definitions only corroborate the operator name/source shape. |
| TOOLCHAIN-030 | compiler-observed | Natural `operator>=` and `operator<` source is exact under tested `/O2 /Ob0`, `/O2 /Ob1`, `/O2 /Ob2`, and `/Ox /Ob1` with `/Oy-`. `/O1 /Ob1` emits 18 rather than 20 bytes for both, while `/Od /Ob0` emits 27. | Focused pinned VC7.1 build-3077 matrix plus two complete 55-unit tracked-source replay passes. These are local profile facts, not project-wide flags. |
| BOUNDARY-034 | observed | `operator>=` owns `0x00401520-0x00401533` followed by twelve unassigned `CC` bytes through `0x0040153F`; `operator<` owns `0x00401540-0x00401553` followed by twelve unassigned `CC` bytes through `0x0040155F` before candidate `0x00401560`. | PE-aware target bytes, native IDA extents, and canonical replays. |
| SUPERVISOR-003 | exact | `Supervisor::TickTimer(int *current, float *subFrame) @ 0x0042F4F0-0x0042F536` is a 71-byte `__thiscall` member. ECX is the Supervisor object; the two stack pointers are consumed by `ret 8`. The observed float at Supervisor `+0x5B8` is compared with 0.99f. At or below the threshold the function adds it to `*subFrame`, stores the float result, converts that value through `__ftol2`, and writes `*current`; above the threshold it increments `*current` and adds 1.0f to `*subFrame`. | Canonical pinned-VC7.1 replay is 71/71 bytes with three target-bound relocations: 0.99f at `0x004907E4`, `__ftol2 @ 0x0047B1D4`, and 1.0f at `0x0048E2A4`. Exact ZunTimer::Tick is the sole direct caller. Clean committed TH08 HEAD `a45e99fb1942714e6edded20847e32a654d56f97` corroborates the reconstructed method name/source shape only; TH095 HEAD `2060b0bcfb54b4d850471e33161a31454d671db7` was dirty and only committed references were consulted. |
| TOOLCHAIN-031 | compiler-observed | Natural `Supervisor::TickTimer` source in its own maintained translation unit reproduces the target under the configured pinned VC7.1 `/O2 /Ob1 /Oy-` profile. Two consecutive cold match-unit builds each replay all 71 bytes and all three relocations with zero differences. This demonstrates the configured unit only; original TU partition and project-wide flags remain unknown. | `scripts/build-match-unit.py --unit supervisor-tick-timer` plus `scripts/compare-coff-function.py --unit supervisor-tick-timer --json`, run twice. |
| BOUNDARY-035 | observed | `Supervisor::TickTimer` owns the reviewed logical extent `0x0042F4F0-0x0042F536`. Target bytes at `0x0042F537-0x0042F53F` are nine consecutive `CC` bytes before candidate `0x0042F540`; their physical ownership remains unassigned. | Native IDA reports a 0x47-byte function; attested target-byte read through 0x0042F53F and canonical object replay agree with the extent. |

| MATH-001 | exact | `Float3::Float3(float x, float y, float z) @ 0x004010B0-0x004010C9` is a 26-byte `__thiscall` constructor for a 12-byte three-float value. ECX is the destination object, the three stack floats are written at `+0/+4/+8`, EAX returns `this`, and the function consumes 12 stack bytes. | Canonical pinned-VC7.1 replay is 26/26 bytes with no relocations. Target-local helpers at `0x00401100`, `0x00401140`, and `0x00401180` independently form componentwise add/subtract/scalar-multiply results through this constructor. Clean committed TH08 HEAD `a45e99fb1942714e6edded20847e32a654d56f97` corroborates the reconstructed type/name only. |
| ANMLOADED-001 | exact | `AnmLoaded::ExecuteAnmIdx(AnmVm *vm, int scriptIndex) @ 0x00401560-0x004015E5` is a 134-byte `__thiscall` member, not another ZunTimer helper despite its address adjacency. It writes the low 16 bits of the script index at VM `+0x21A`, constructs zero Float3 values at `+0x208` and `+0x288`, writes `0x0F` at `+0x299/+0x298`, then calls candidate `AnmLoaded::SetAndExecuteScript @ 0x004395A0` with `scripts[scriptIndex]` loaded from owner `+0x10`. | Canonical pinned-VC7.1 replay is 134/134 bytes with REL32 relocations to exact `Float3::Float3` twice and candidate `0x004395A0` once. Broad TH09 callers plus target-local `AnmLoaded::GetSprite @ 0x00434390` and `0x004395A0` field use establish the owner hypothesis independently; committed TH08 source corroborates name/source shape only. |
| ANMLOADED-002 | exact / authored-game | `AnmLoaded::SetAndExecuteScript @ 0x004395A0-0x0043962E` is independently repository-canonical exact. Null script or pending-load state clears the full 0x2A4 VM; otherwise it initializes the VM, publishes owner/script pointers, resets script time, executes once through the global ANM manager, and increments `scriptsStartedThisFrame @ manager+0x0C`. | Two byte-identical cold 143/143 canonical replays with five reviewed relocations plus TH09 target body/call graph. |
| TOOLCHAIN-032 | compiler-observed | Natural maintained sources for `Float3::Float3` and `AnmLoaded::ExecuteAnmIdx` reproduce the target under the configured pinned VC7.1 `/O2 /Ob1 /Oy-` profile. Two complete replay passes also rebuilt all nine pre-existing exact units whose direct sources consume the modified `AsciiManager.hpp` or `AnmManager.hpp`; every affected unit remained exact. This demonstrates only these configured units, not original TU partition or project-wide flags. | Two complete `build-match-unit.py` + `compare-coff-function.py --json` passes for the two new units and nine affected AsciiManager units; `git diff --check` passed. |
| BOUNDARY-036 | observed | `Float3::Float3` owns `0x004010B0-0x004010C9`, followed by six unassigned `CC` bytes through `0x004010CF`. `AnmLoaded::ExecuteAnmIdx` owns `0x00401560-0x004015E5`, followed by ten unassigned `CC` bytes through `0x004015EF` before candidate `0x004015F0`. | Native IDA extents, attested target-byte reads, and canonical object replays agree with both reviewed logical extents. |

| MATH-002 | exact | `Float3::operator+(const Float3 &other) const @ 0x00401100-0x00401135` is a 54-byte `__thiscall` value-returning member. ECX is the left operand; the first stack slot is the hidden 12-byte return-object pointer and the second is the right-operand pointer/reference. The body adds x/y/z componentwise and constructs the result through exact `Float3::Float3 @ 0x004010B0`, then returns the hidden result pointer and consumes eight stack bytes. | Natural VC7.1 source with the `const Float3 &` / const-member declaration replays 54/54 bytes with one REL32 relocation to the exact constructor. Native TH09 callers establish broad use before adjacent-game naming was consulted; clean committed TH08 HEAD `a45e99fb1942714e6edded20847e32a654d56f97` and committed TH095 HEAD `27eee6327f1c65dee113a09cadc30e22ec03ca78` corroborate the source signature only. |
| MATH-003 | exact | `Float3::operator-(const Float3 &other) const @ 0x00401140-0x00401175` has the same hidden-return `__thiscall` ABI as MATH-002 and subtracts the right x/y/z components from the left before constructing the result through exact `Float3::Float3`. | Canonical pinned-VC7.1 replay is 54/54 bytes with one REL32 relocation to `0x004010B0`; native TH09 disassembly/callers fixed the operation and ABI before adjacent committed source was used for the operator/signature hypothesis. |
| MATH-004 | exact | `Float3::operator*(float scalar) const @ 0x00401180-0x004011B3` is a 52-byte value-returning `__thiscall` member. ECX is the source Float3; the first stack slot is the hidden result pointer and the second is the float scalar. The body multiplies x/y/z by the scalar and constructs the result through exact `Float3::Float3`. | Canonical pinned-VC7.1 replay is 52/52 bytes with one REL32 relocation to `0x004010B0`; target-local callers independently establish broad math-helper use. |
| TOOLCHAIN-033 | compiler-observed | The specific C++ declarations `Float3::operator+(const Float3 &) const`, `Float3::operator-(const Float3 &) const`, and `Float3::operator*(float) const` reproduce the TH09 symbols, sizes, hidden-return ABI, non-relocation bytes, and constructor relocations under the configured pinned VC7.1 `/O2 /Ob1 /Oy-` profile. Two complete tracked-source replay passes rebuilt these three units, the constructor, exact `AnmLoaded::ExecuteAnmIdx`, and all nine exact direct AsciiManager consumers of the modified Float3 header; every unit remained exact. | Probe symbols are `??HFloat3@@QBE?AU0@ABU0@@Z`, `??GFloat3@@QBE?AU0@ABU0@@Z`, and `??DFloat3@@QBE?AU0@M@Z`. These are local profile facts only; original TU partition and project-wide flags remain unknown. |
| BOUNDARY-037 | observed | `Float3::operator+` owns `0x00401100-0x00401135`, followed by ten unassigned `CC` bytes through `0x0040113F`; `operator-` owns `0x00401140-0x00401175`, followed by ten unassigned `CC` bytes through `0x0040117F`; `operator*` owns `0x00401180-0x004011B3`, followed by twelve unassigned `CC` bytes through `0x004011BF` before candidate `0x004011C0`. | Native IDA extents, attested target-byte reads, and canonical object replays agree on all three logical extents; physical padding ownership remains unresolved. |

| SUPERVISOR-004 | exact | `Supervisor::TakeSnapshot(const char *filePath) @ 0x0042F540-0x0042F75E` is a 543-byte `__thiscall` member. The sole direct caller is the target snapshot-key path at `0x0042DB50`, which creates `snapshot`, searches `snapshot/thNNN.bmp`, and calls this member with `g_Supervisor`. Within the member, Supervisor `+0x08` is observed as an `IDirect3DDevice8`-like pointer and `+0x358` as a `D3DFORMAT`-like back-buffer format. `D3DFMT_X8R8G8B8` is captured as a bottom-up 640x480 24-bit BMP; `D3DFMT_R5G6B5` is rejected; other formats log an error and return 1. | Two canonical pinned-VC7.1 replays are 543/543 bytes exact with 23 target-bound relocations to the three target strings, `g_GameErrorContext`, exact `GameErrorContext::Log`, `g_ZunMemory`, exact Alloc/Free wrappers, and Win32 CreateFileA/WriteFile/CloseHandle imports. Clean committed TH08 HEAD `a45e99fb1942714e6edded20847e32a654d56f97` corroborates the `TakeSnapshot` name and broad source shape only; TH09 target evidence decides all offsets, calls, strings, and exactness. |
| TOOLCHAIN-034 | compiler-observed | The exact `TakeSnapshot` source shape is unusually sensitive but naturally target-evidenced. A direct TH08-shaped loop first emitted 525 bytes; pointer-row recovery emitted 533; an explicit negative pitch delta reached 543 with residual stack-slot differences. Removing a separate `DWORD bytesWritten` local and passing `(LPDWORD)&filePath` to all three `WriteFile` calls reproduces the target's reuse of the parameter stack slot and closes every remaining byte. The maintained tracked source with a local Supervisor layout view then replays exact twice under pinned VC7.1 `/O2 /Ob1 /Oy-`. | Target decompilation itself shows all three `WriteFile` calls using `&filePath`; the final match unit proves 451/451 non-relocation bytes plus 92 relocation-field bytes. This is a function-local source/profile fact, not evidence for original TU partition or project-wide flags. |
| BOUNDARY-038 | observed | `Supervisor::TakeSnapshot` owns `0x0042F540-0x0042F75E`. The single byte at `0x0042F75F` is `CC` and remains physically unassigned; independent candidate `0x0042F760` starts immediately afterward. | Native IDA reports size `0x21F`; attested target-byte read through the return/padding boundary and two canonical 543-byte replays agree on the logical extent. |

Do not turn cross-game similarity, a decompiler type, or an attractive reused
name into a TH09 fact without target-local evidence.

| SUPERVISOR-005 | exact | `Supervisor::LoadConfig(char *configFile) @ 0x0042F7C0-0x0042FBDD` is a 1,054-byte `__thiscall` member. `_WinMain@16` passes `g_Supervisor @ 0x004B3100` as ECX and `"th09.cfg"` as the argument. The function initializes the 0xCC-byte configuration at Supervisor `+0x388`, loads or defaults it, validates version `0x90003` and bounded option bytes, transfers two 0x36-byte controller mappings, validates the `./thbgm.dat` header `(0x5641575A, 1, 0x900)`, emits option diagnostics, and rewrites the configuration through exact `FileSystem::WriteDataToFile`. | Two no-pragma pinned-VC7.1 canonical replays each match 1,054/1,054 bytes with all 131 relocation sites target-bound. TH09 `_WinMain@16`, target-local option helpers, exact FileSystem/ZunMemory/GameErrorContext dependencies, and `GameConfiguration::Initialize @ 0x0041A8A2` independently corroborate the ABI/dataflow. String/global data ownership, complete Supervisor layout, original TU, and whole-build placement remain unresolved. |
| SUPERVISOR-006 | exact / owner-corrected | `0x0042F760` and `0x0042F770` do not inherit Supervisor ownership from adjacency. They are now independently canonical exact as `AnmManager::ResetFrameDebugInfo` and `AnmManager::ResetForSupervisorFrame`; the sole external caller passes `g_AnmManager @ 0x004DC550` in ECX. The wrapper clears render caches/counters/camera state and screen-shake fields in target order. | Current AnmManager match units, TH09 caller/callee graph, and the original ownership-correction evidence. |
| TOOLCHAIN-035 | compiler-observed | The maintained natural `Supervisor::LoadConfig` source is exact under the configured pinned VC7.1 `/O2 /Ob1 /Oy-` profile only after the two 0x36-byte controller mappings are copied field-by-field, matching the target's emitted store sequence. A whole-struct assignment probe emitted a 702-byte body; changing only those copies produced the target 1,054-byte body. The inherited `#pragma var_order` is unsupported by this VC7.1 invocation and was removed; two fresh no-pragma builds remain exact. This is function-local source/profile evidence, not original project-wide flags or TU partition. | Focused source-shape probe, compiler warning evidence for unsupported `var_order`, and two fresh canonical 1,054/1,054 replays with 131 relocations. |
| BOUNDARY-039 | observed | `Supervisor::LoadConfig` owns the reviewed logical extent `0x0042F7C0-0x0042FBDD`; `0x0042FBDE-0x0042FBDF` are two `CC` bytes before the independent candidate at `0x0042FBE0`. Separately, `0x0042F760` ends at `0x0042F76E` with one `CC` byte at `0x0042F76F`, and `0x0042F770` ends at `0x0042F7B9` followed by six `CC` bytes through `0x0042F7BF`. Physical ownership of all padding remains unassigned. | Native IDA logical extents, attested target-byte reads, and the canonical LoadConfig replay. |
| TOOLCHAIN-036 | replay-metadata | Adding the target-supported `Supervisor::LoadConfig` and eight observed option-helper declarations shifts only VC7.1 compiler-internal ChainRun COFF labels: eight `chain-run-calc` and seven `chain-run-draw` `$L...`/restart symbols advance while every relocation offset, type, solved target address, function byte, and owned extent remains unchanged. The replay manifest tracks the new local names only. After that metadata refresh, all 53 canonical exact units rebuild and replay exact in one complete regression pass. | Positional structural comparison proved identical relocation offset/type/target maps for both 212-byte ChainRun functions; a full 53-unit pinned-VC7.1 regression then returned exact for every canonical unit. |
| GAMECONFIG-001 | exact / ABI-qualified | `GameConfiguration::Initialize @ 0x0041A8A2-0x0041A98A` is a 233-byte `__thiscall` initializer for a 0xCC-byte configuration object. It clears the complete object, installs TH09 defaults, and copies six 0x12-byte controller bindings from one 0xC4 controller layout rooted at `0x004ACE70`. Two canonical replays match 233/233 bytes with six relocations. The maintained source uses `void Initialize()`, but the target return contract remains unresolved because every observed caller ignores EAX and decompiler return inference is not ABI proof. | Attested target body/callers, 0xCC allocation call site, exact Supervisor::LoadConfig dependency, and two pinned-VC7.1 canonical replays. |
| GAMECONFIG-002 | exact | `GameConfiguration::GameConfiguration @ 0x0041A98B-0x0041A996` is a 12-byte constructor-shaped wrapper: save `this`, call the initializer, return `this` in EAX. `0x0041A997` begins an independent function, so there is no padding after the constructor. Two canonical replays match 12/12 bytes with one REL32 relocation to `0x0041A8A2`. | Attested target boundary/disassembly plus two pinned-VC7.1 canonical replays. |
| GAMECONFIG-003 | layout-observed | The controller data used by TH09 configuration code is naturally modeled as one 0xC4 layout rooted at `0x004ACE70`: three 0x12 bindings at `+0x00/+0x12/+0x24`, an opaque 0x58-byte middle span, and three more 0x12 bindings at `+0x8E/+0xA0/+0xB2`. Refactoring exact `Supervisor::LoadConfig` from two temporary 0x36 globals to this single-base layout preserves its 1,054-byte target body and resolves all 30 affected relocation sites back to base `0x004ACE70` with field addends. The original target identifier and semantics of the opaque middle span remain unknown. | Target addresses, `GameConfiguration::Initialize` six-block copies, and relocation-aware tracked-source probes for both Initialize and LoadConfig. |
| TOOLCHAIN-037 | compiler-observed | The natural `GameConfiguration::Initialize` source is 444 bytes under the established `/O2 /Ob1 /Oy-` profile, but exactly 233 bytes under both `/O1 /Ob1 /Oy-` and `/O2 /Os /Ob1 /Oy-`; the latter is the canonical replay profile. Conversely, exact `Supervisor::LoadConfig` shrinks from 1,054 to 957 bytes under `/O2 /Os`. This proves a local size-preference requirement for the initializer but does not decide original TU partition, source-level optimization pragmas, or project-wide flags. | Focused VC7.1 profile probes and exact canonical replays. |
| BOUNDARY-040 | observed | `GameConfiguration::Initialize` ends at `0x0041A98A`; the constructor starts immediately at `0x0041A98B` and ends at `0x0041A996`; independent candidate `0x0041A997` starts at the next byte. No padding is assigned between these three logical extents. | Native IDA function extents and attested target-byte read across `0x0041A980`. |
| GAMEMGR-001 | source-present / codegen-blocked / authored-game | `GameplaySetupThread @ 0x0041AF2D-0x0041B5C5` is the 0x699-byte thread entry registered by exact `GameManagerSetupLayout::AddedCallback @ 0x0041B76F` through the Supervisor thread wrapper with a NULL parameter. Its target-local body owns GameManager reset/configuration, two-side state allocation, stage-object setup, fourteen side-subsystem registrations, setup-mode parameter selection, success wait, and failure state transitions before exact `SupervisorCalculateFps(0)`. These game-specific ownership and lifecycle facts independently establish `authored_game / GameManager / high`; original TU/object ownership and exact source shape remain unresolved. Maintained source remains non-exact. | Exact TH09 AddedCallback function-pointer relocation, attested boundary/decompilation/disassembly/dataflow, exact GameConfiguration/AdvanceTimedState/SupervisorCalculateFps dependencies, and repeatable pinned-VC7.1 source probes. Adjacent TH08 supplies only naming/lifecycle corroboration after TH09 evidence. |
| GAMEMGR-002 | observed-layout / ABI | The gameplay setup packet proves a bounded GameManager-facing layout seam rooted at `0x004A7D90`: two 0x38-byte side records contain seven subsystem pointers at `+0x00..+0x18`, a 0xA0-state pointer at `+0x1C`, selector at `+0x28`, and flags at `+0x34`. The same packet observes manager fields including game mode `+0x118`, setup mode `+0x11C`, gameplay-setup state `+0x124`, flags `+0x134`, stage object `+0xD4`, and selected setup counters/limits. The Supervisor seam includes embedded `GameConfiguration @ +0x388`, setup flags `+0x5D4`, thread state around `+0x6AC`, total-play-time baseline `+0x798`, and system/game-time baseline `+0x79C`. These are packet-local field observations, not a complete original class layout or data-definition ownership claim. | Attested TH09 effective-address arithmetic, two-record 0x38-stride loop, exact GameConfiguration dependency, and success/failure stores |
| TOOLCHAIN-038 | unresolved-codegen | The maintained gameplay-setup source emits exactly 1689 bytes under tested `/O2 /Os /Ob1 /Oy-` and `/O1 /Ob1 /Oy-`, but relocation-masked comparison matches only 402 of 1005 directly comparable bytes and first diverges at function offset `+0x1DB`. The target repeatedly reloads the GameManager address into ECX around timed-state/stage-object calls and uses an ESI induction base at side-record flags, while stock VC7.1 hoists the manager address into ESI and chooses a different side-loop base. Rewriting the source to use the global object directly emits 1688 bytes without solving the register choice, and `/Og-` expands the function to 2564 bytes. No volatile address forcing, padding, assembly, patched compiler, or target-specific byte trick is introduced. | Pinned VC7.1 build-3077 profile probes and relocation-masked target comparison of `src/GameManagerSetup.cpp` |
| BOUNDARY-041 | observed | The gameplay setup thread owns exactly `0x0041AF2D-0x0041B5C5` and ends in a plain `ret`; independent candidate `0x0041B5C6` starts at the next byte, so no following padding is assigned to the setup routine. Its otherwise missing code caller is explained by the data-reference thread registration from `0x0041B80A`. | Attested native IDA boundary/xref evidence, target disassembly, and Supervisor thread-wrapper call site |
| GAMEMGR-003 | exact | `GameManagerSetupLayout::AdvanceTimedState @ 0x0041AA07-0x0041AA5E` is target-exact. It reads side-0 state through `g_GameManager + 0x1C`; while `value00 < 7.0f` it adds `1.0f` and calls the target-observed SoundPlayer object at `0x004DC698` through `PlaySoundByIdx @ 0x0043E2F0` with `(0x1C, 0)`, then increments the state counter at `+0x10`. Game mode `1` uses threshold `8`, other modes threshold `5`; reaching the threshold stores `99999`. The target `JL` proves the counter is compared as signed, correcting the provisional unsigned field. | Two repeatable canonical pinned-VC7.1 replays match all 88 bytes and eight relocation targets; packet 47 independently identifies the same object/callee as the TH09 SoundPlayer SFX queue path, corroborated by clean committed TH08 source. |
| BOUNDARY-042 | exact | `GameManagerSetupLayout::AdvanceTimedState` owns `0x0041AA07-0x0041AA5E`; independent candidate `0x0041AA5F` starts at the next byte with no padding assigned to the helper. | Attested native IDA extent plus target disassembly and canonical 88-byte replay. |
| GAMEMGR-004 | source-present / codegen-blocked / authored-game | `GameManagerSetupLayout::CleanupGameplayState @ 0x0041B5C6-0x0041B76E` is a 425-byte GameManager lifecycle routine called from two central TH09 lifecycle paths. It resets GameManager transient fields and exact `AsciiManager::Reset`, releases the two global gameplay objects plus seven subsystem pointers for each of two 0x38-byte side records and the shared subsystem, then rebalances `limitB4/valueC4/valueC8` from `gameMode`, `setupMode`, `counterFC`, and `value100`. It finally clears `valueB0/value348` and subtracts 1000 from `valueCC` with a zero clamp. Both reviewed callers ignore EAX, but packet 66 corrected the earlier inference that this proves a void source return: the target preserves one coherent pre-clamp integer result in EAX through the final `ret` on both special and normal paths. The return contract therefore remains unresolved; maintained source stays provisional and non-exact. | Attested TH09 callers/callees/disassembly plus pinned-VC7.1 source probes. Origin remains authored-game from TH09-local GameManager state and game-subsystem lifecycle use; IDA return inference has no exactness credit. |
| TOOLCHAIN-060 | unresolved-codegen / return-contract-open | Fresh packet-66 target evidence shows `CleanupGameplayState` leaves a coherent pre-clamp integer result in EAX on every path through `ret`; its two callers ignore EAX but do not determine the source return type. The maintained provisional void source still emits 399 bytes under pinned VC7.1 `/O2 /Os /Ob1 /Oy-`. A natural int-return probe emits 412 bytes and restores the target's independent `gameMode == 2` `JGE / store / JMP` topology in the correct instruction shape; `/O1 /Ob1 /Oy-` produces the same 412-byte object. A single-result probe emits 410 bytes and naturally recovers the target-style `NEG` plus `LEA` normal-tail arithmetic but swaps the target register roles, while chained assignment emits 413 bytes. Removing `/Os` produces a 512-byte jump-table form. Remaining non-exact drift is concentrated in case-0 scheduling/tail sharing and normal-tail register allocation, so no return ABI or canonical exactness promotion is made. | Complete 425-byte target disassembly, both fresh TH09 callers, pinned stock VC7.1 probes, and IDA readback. No source-shape probe is retained; no volatile/register forcing, padding, inline assembly, target bytes, or fake return is used. |
| BOUNDARY-043 | observed | `GameManagerSetupLayout::CleanupGameplayState` owns `0x0041B5C6-0x0041B76E` and ends in a plain `ret`; the independent routine at `0x0041B76F` starts immediately after it. | Attested native IDA extent, target disassembly, and the already-reviewed adjacent setup-thread boundary. |

| GAMEMGR-005 | exact | `GameManagerSetupLayout::AddedCallback @ 0x0041B76F-0x0041B829` is the GameManager calc-chain added callback. TH09 `0x0041B9D0` writes this address to `ChainElem::addedCallback` and `g_GameManager @ 0x004A7D90` to the element argument. The callback toggles the Supervisor initial-load bit from state `+0x590`, marks gameplay setup in progress, configures loading/capture position `(500, 440, 0)` or `(280, 430, 0)` from state `+0x594`, optionally starts loading effect 0, normalizes GameManager transition bits at `+0x134`, starts `GameplaySetupThread @ 0x0041AF2D`, then calls the target-observed Supervisor game-time and play-time update routines. | Two repeatable canonical pinned-VC7.1 replays reproduce all 187 bytes with thirteen reviewed relocations. The callback owner and registration are TH09-local; clean committed TH08 HEAD `a45e99fb1942714e6edded20847e32a654d56f97` corroborates the AddedCallback name and broad source shape only. |
| TOOLCHAIN-040 | compiler-observed | Natural maintained `GameManagerSetupLayout::AddedCallback` source reproduces the target under the established packet-local VC7.1 `/O2 /Os /Ob1 /Oy- /Gr` profile without padding, inline assembly, volatile address forcing, target-byte embedding, or custom compiler behavior. The tracked-source body matches 135/135 non-relocation bytes before canonical binding; the replay manifest then resolves thirteen relocation fields and reproduces 187/187 bytes twice. | Focused pinned-VC7.1 tracked-source probe plus two byte-identical canonical replay reports. This is function-local evidence only; original TU partition and project-wide flags remain unknown. |
| BOUNDARY-044 | exact | `GameManagerSetupLayout::AddedCallback` owns `0x0041B76F-0x0041B829`; `GameManager` deleted-callback candidate `0x0041B82A` begins at the next byte with no padding between them. | Attested native IDA extent, target-byte read across the boundary, ChainElem registration xref, and canonical 187-byte replay. |

| GAMEMGR-006 | exact | `GameManagerSetupLayout::DeletedCallback @ 0x0041B82A-0x0041B9CF` is the GameManager calc-chain deleted callback. TH09 `0x0041B9D0` stores this address in `ChainElem::deletedCallback` with `g_GameManager @ 0x004A7D90` as the argument. It snapshots side values and `+0xF4` to their paired fields, clears GameManager flag bit 2, derives Supervisor `+0x598` from state `+0x590`, clears the 0x2BC0-byte block at `0x004D9698`, conditionally constructs the observed 0x2C-byte `HSCR` record at `0x004AC858`, conditionally releases the stage object, releases and nulls both 0x38-byte subsystem sides plus shared/game/secondary objects, frees the GameManager `+0xE8` allocation, drains SoundPlayer queues until zero, and clears `0x004A80D8`. | Two consecutive canonical pinned-VC7.1 replays reproduce all 422 bytes with forty-one reviewed relocations. Ownership/registration, boundary, data accesses, and call targets are TH09-local target evidence. Clean committed TH08 source supports only the callback-role and SoundPlayer naming hypotheses where the games structurally agree. |
| SOUND-001 | mixed exact/source-present | The object at `0x004DC698` is the maintained SoundPlayer owner. `SoundPlayer::PlaySoundByIdx @ 0x0043E2F0` is independently canonical exact and queues one SFX request into the target-backed 12-slot queue. `SoundPlayer::ProcessQueues @ 0x0043F1F0` is maintained source-present/non-exact with the correct 0x586 logical body plus compiler-owned switch tail; GameManager teardown repeatedly calls it until zero. Data/TU ownership beyond the maintained object layout remains open. | Current functions/matches ledgers, TH09 target call graph, and clean committed TH08 source-family corroboration. |
| TOOLCHAIN-041 | compiler-observed | Natural maintained `GameManagerSetupLayout::DeletedCallback` source reproduces the target under the established packet-local VC7.1 `/O2 /Os /Ob1 /Oy- /Gr` profile without padding, inline assembly, target-byte embedding, volatile address forcing, or custom compiler behavior. The source shape requires explicit Supervisor state branches, direct semantic record-header assignments, and the natural `while (ProcessQueues()) {}` tail; these produce the target's register/store schedule rather than synthetic size adjustments. | Focused probe convergence from 407 to 424 to 422 bytes, then tracked-source 258/258 non-relocation-byte structural equality and two byte-identical canonical 422/422 replays with forty-one relocations. Function-local compiler evidence only; original TU partition and project-wide flags remain unknown. |
| BOUNDARY-045 | exact | `GameManagerSetupLayout::DeletedCallback` owns `0x0041B82A-0x0041B9CF`; independent candidate `0x0041B9D0` starts at the next byte with no padding. | Attested native IDA function extent and boundary bytes, target-local ChainElem registration, and repeatable canonical 422-byte replay. |

| GAMEMGR-007 | exact | `GameManagerSetupLayout::RegisterChain @ 0x0041B9D0-0x0041BA4E` is the GameManager chain-registration routine. It clears `g_GameManager + 0x348`, configures calc ChainElem `0x004A8120` with callback `0x0041AA5F`, exact added/deleted callbacks `0x0041B76F` / `0x0041B82A`, `g_GameManager @ 0x004A7D90` as argument, and priority 2; a nonzero `Chain::AddToCalcChain` result returns -1. On calc success it configures draw ChainElem `0x004A8140` with callback `0x0041A75F`, null lifetime callbacks, the same GameManager argument, and priority 5, ignores the `AddToDrawChain` result, and returns 0. | Two consecutive canonical pinned-VC7.1 replays reproduce all 127 bytes with nineteen reviewed relocations. TH09 target-local callers, ChainElem field addresses, exact Chain insertion APIs, and callback destinations establish the registration contract. `0x0041AA5F` / `0x0041A75F` are identified only by their registered calc/draw roles here; their own origin/exactness remain review pending. |
| TOOLCHAIN-042 | compiler-observed | Natural maintained `GameManagerSetupLayout::RegisterChain` source reproduces the target under the established packet-local VC7.1 `/O2 /Os /Ob1 /Oy- /Gr` profile without padding, inline assembly, target-byte embedding, volatile address forcing, or synthetic control flow. | The first independent probe already produced the target 127-byte extent with 51/51 non-relocation bytes matching and nineteen relocation destinations resolved; tracked source then produced the same structural equality and two byte-identical 127/127 canonical reports. Clean committed TH08 HEAD `a45e99fb1942714e6edded20847e32a654d56f97` corroborated only the broad RegisterChain source shape after TH09 established the field/callback/API contract. |
| BOUNDARY-046 | exact | `GameManagerSetupLayout::RegisterChain` owns `0x0041B9D0-0x0041BA4E`; independent candidate `0x0041BA4F` begins at the immediately following byte with no padding. | Attested native IDA function extents, explicit boundary-byte read, and repeatable canonical 127-byte replay. |
| GAMEMGR-008 | source-present / codegen-blocked / authored-game | `GameManagerSetupLayout::OnUpdate @ 0x0041AA5F-0x0041AF2C` is the GameManager calc-chain callback installed by exact `RegisterChain @ 0x0041B9D0`. TH09 target-local control flow returns Chain states 1/3/4 and covers gameplay-setup waiting, stage-startup resource/audio work, pause/demo transitions, periodic GameManager counters, paired side-state growth, lane-based object spawning, two-side score smoothing, and the exact `AdvanceTimedState` dependency. The input-history seam copies five 16-bit words from target base `0x004ACF34` into the first ten bytes at target bases `0x004ACE18` and `0x004ACEA6` when the mode/side-selector gate allows it. Maintained natural source is present but is not a canonical match. | Exact RegisterChain relocation ownership, native IDA extent/xrefs/disassembly/callees, boundary bytes, and two independent live-HEAD pinned stock VC7.1 rechecks (packets 49 and 68). Clean committed TH08 HEAD `a45e99fb1942714e6edded20847e32a654d56f97` corroborates lifecycle/source-shape hypotheses only; TH09 decides all offsets, widths, control flow, and origin. |
| TOOLCHAIN-043 | unresolved-codegen | Natural `GameManagerSetupLayout::OnUpdate` source still emits 1,236 bytes under the established pinned VC7.1 `/O2 /Os /Ob1 /Oy- /Gr` profile versus the 1,230-byte target. Packet 68 freshly reproduces the mismatch from live HEAD: target compares `gameMode` with immediate 2 before preserving the callback argument in EBX, then clears/reuses ESI after the setup-state branch; the retained object preloads input state, retains constant `2` in EBX, and keeps the GameManager pointer in ESI. Four natural input-copy forms emit 1,239 / 1,235 / 1,164 / 1,236 bytes; two target-grounded 0x8E owner-layout probes each emit 1,235 bytes. None is exact. Historical `/G6`, `/G7`, and `/GB` probes also leave the retained 1,236-byte result unchanged. | Fresh packet-68 cold compile/object comparison and target disassembly, plus bounded natural direct-field/local-struct/struct-assignment/local-pointer and 0x8E layout probes. No probe is retained in tracked source. No `volatile`, reconstruction-only `var_order` wrapper, padding, inline assembly, target-byte embedding, fake return, or register-directed source is retained. |
| INPUT-001 | observed / ownership-unknown | Target bases `0x004ACE18`, `0x004ACEA6`, and `0x004ACF34` are separated by exactly `0x8E` bytes and participate in the same input-state maintenance logic. `sub_420190 @ 0x00420190` passes each base to `sub_4200F0 @ 0x004200F0`, and its per-side loop advances corresponding fields by 71 16-bit elements (`0x8E` bytes). `sub_4200F0` accesses state beyond the first ten bytes, including fields at and after `+0x2C/+0x38`. This proves a repeated 0x8E-strided physical layout around the OnUpdate copy seam but does not prove whether the original C++ owner was an array, three globals, nested members, or another aggregate. | Passed native-IDA xrefs/decompilation under `target:th09-main` / `factory-native-stdio`, plus the exact numeric base differences. Packet-68 0x8E aggregate and nested-0x0A source probes both remained non-exact and therefore are hypotheses only. |
| BOUNDARY-047 | observed | `GameManagerSetupLayout::OnUpdate` owns the reviewed logical extent `0x0041AA5F-0x0041AF2C`. Exact `RegisterChain` stores `0x0041AA5F` in the calc callback slot, and independent `GameplaySetupThread @ 0x0041AF2D` starts at the immediately following byte with no padding gap. | Native IDA extent/xref, attested target bytes across `0x0041AF20`, and the independent next-function start. This boundary evidence gives no exactness credit to the current OnUpdate source. |

| GAMEMGR-009 | exact | `GameManagerSetupLayout::OnDraw @ 0x0041A75F-0x0041A795` is the GameManager draw-chain callback installed by exact `RegisterChain @ 0x0041B9D0`. It advances nonzero menu state `+0x13C` to 2, returns Chain BREAK when Supervisor `+0x590 != 2` or when GameManager transition bits `(+0x134 & 0x60) == 0x20`, otherwise returns BREAK while `gameplaySetupState @ +0x124` is nonzero and CONTINUE when it is zero. | Two consecutive pinned-VC7.1 canonical replays reproduce all 55 bytes. The sole relocation is DIR32 at function `+0x10` to `g_Supervisor @ 0x004B3100` with addend `+0x590`; exact RegisterChain independently binds this address as the draw callback with `g_GameManager` as the callback argument. |
| TOOLCHAIN-044 | compiler-observed | Natural maintained `GameManagerSetupLayout::OnDraw` source is exact under the same packet-local pinned VC7.1 `/O2 /Os /Ob1 /Oy- /Gr` profile used by the existing exact GameManager lifecycle/registration units. No padding, assembly, volatile forcing, target-byte embedding, fake return, or reconstruction-only compiler wrapper is required. | The first focused tracked-source diagnostic matched 51/51 non-relocation bytes and solved the single relocation; two independent canonical rebuild/replay passes then returned 55/55 exact. This is a function-local profile fact, not evidence for original project-wide flags or TU partition. |
| BOUNDARY-048 | exact | `GameManagerSetupLayout::OnDraw` owns `0x0041A75F-0x0041A795`. Independent candidate `0x0041A796` starts at the immediately following byte, so there is no padding or shared tail between them. | Native IDA reports the 0x37-byte extent; attested target bytes across the boundary and the complete 55-byte canonical replay agree. |


| SUPERVISOR-007 | source-present / codegen-blocked / authored-game | `Supervisor::OnUpdate @ 0x00431110-0x0043145B` is the central Supervisor calc-chain callback. Exact `SupervisorRegisterChain @ 0x00432180` stores this address with `g_Supervisor @ 0x004B3100` as callback argument after initializing `wantedState +0x58C`, `curState +0x590`, and `calcCount +0x454`. The body services startup/subthread state, ANM/loading/audio frame work, scene transitions, GameManager cut/register seams, the 4000-frame version check, screen-effect countdown, and Chain results 1/3/4/5. Maintained natural source is materially closer after packet 64 but remains non-exact: code before switch artifacts is 828 bytes versus target 844. | Native TH09 registration/xrefs, target decompilation/disassembly and switch tables, target-local dependencies, repeatable pinned VC7.1 source feedback, and exact caller regression. Clean committed TH08 corroborates the broad `Supervisor::OnUpdate` state-machine organization only; TH09 determines all state values, fields, helpers, physical blocks, and exactness. |
| BOUNDARY-049 | observed | `Supervisor::OnUpdate` owns the reviewed logical code extent `0x00431110-0x0043145B`. Its target switch artifacts immediately follow at `0x0043145C-0x004314F3`: a 10-entry table, 4-entry table, 6-entry table, 11-byte case-index table plus one alignment byte, and a 15-entry table. `0x004314F4-0x004314FF` are twelve `CC` bytes. Separate executable code begins at `0x00431500` and returns at `0x0043153D`; `0x0043153E-0x0043153F` are two `CC` bytes before the independent inventory candidate at `0x00431540`. | Attested target bytes, native IDA switch references, and PE-aware bounded disassembly. The switch artifacts are compiler-associated with the callback control flow but are not counted as authored-function bytes; physical TU/data ownership remains separate from the reviewed 844-byte code extent. |
| INVENTORY-002 | denominator-expanded / reviewed-authored | The initial 1.50a candidate inventory omitted executable code at `0x00431500-0x0043153D`. Packet 51 added it conservatively as `unknown/review`; the subsequent constructor packet proved a target static-initializer thunk at `0x0048D8F0` loads `g_Supervisor @ 0x004B3100` into ECX and tail-jumps to the entry, and the body has C++ member-construction plus authored initialization semantics. The candidate is now classified `authored_game / Supervisor` independently from its still-non-exact source. | Attested target thunk/xref, constructor control flow, exact GameConfiguration member-constructor dependency, bounded target disassembly, and focused pinned-VC7.1 source probes. |

| TOOLCHAIN-046 | replay-metadata | Adding the target-supported `Supervisor::OnUpdate` declaration to `Supervisor.hpp` shifts only VC7.1 compiler-internal ChainRun relocation symbol names by +2: calc labels such as `$L450`/`$L396` and restart `$385` become `$L452`/`$L398` and restart `$387`; the corresponding draw labels shift likewise. Every relocation offset/type, external symbol, target address, function byte, and 212-byte owned extent remains unchanged. After refreshing only those local symbol names, `chain-run-calc` and `chain-run-draw` replay 212/212 exact again. | A 35-unit regression covering every configured exact unit whose source directly includes `Supervisor.hpp` left 33 units exact immediately; the two ChainRun units failed only the manifest symbol-name precheck. Offset-specific manifest correction followed by target-bound replay restored both to exact with zero byte differences. |

| SUPERVISOR-008 | source-present / codegen-blocked / authored-game | `Supervisor::Supervisor @ 0x00431500-0x0043153D` is the construction entry reached by the target static-initializer thunk at `0x0048D8F0` after loading `g_Supervisor @ 0x004B3100` into ECX. Before the constructor body, VC7.1 invokes its generic vector-constructor helper on three 0xF0-byte members rooted at `+0x78` with constructor callback `0x004343D0`, then invokes exact `GameConfiguration::GameConfiguration @ 0x0041A98B` for the member at `+0x388`. The body clears exactly 0x7B0 bytes from `this`, sets mask `0x140` in the 32-bit flags storage at `+0x5D4`, returns `this` in EAX, and ends at `0x0043153D`. | TH09 target thunk/xref, bounded disassembly, VC7.1 vector-constructor helper dataflow, exact GameConfiguration constructor, 32-bit target flag RMW xrefs, and maintained source. Clean TH08 committed `Supervisor::Supervisor` corroborates only the broad memset-plus-flags constructor shape; TH09 establishes all offsets, member count/stride, size, and ownership. |
| TOOLCHAIN-047 | unresolved-codegen | The maintained natural `Supervisor::Supervisor` source, with the 0xF0 member constructor visible only as an external declaration in the constructor TU, emits 61 bytes under tested size-oriented pinned VC7.1 profiles versus the 62-byte target. Its first 47 bytes are structurally aligned and all three relocation destinations solve exactly to `0x004343D0`, vector-constructor helper `0x00401470`, and exact `GameConfiguration::GameConfiguration @ 0x0041A98B`; the sole code-shape blocker is the final flag update, where stock size optimization emits `or word ptr [esi+0x5D4], 0x140` and the target emits `or dword ptr [esi+0x5D4], 0x140`. `/Ob0`, `/Ob2`, `/G6`, `/G7`, `/GB`, `/Oy`, `/Ox /Os`, signed bitfields, and unsigned 32-bit bitfields preserve the 61-byte form. Speed-oriented `/O2` instead expands the 3-element construction into an explicit loop and emits 77 bytes. No volatile forcing, inline assembly, padding, hand-written helper call, or register-directed trick is retained. | Focused VC7.1 profile/source-shape probes and relocation-aware target comparison. |
| BOUNDARY-050 | observed | `Supervisor::Supervisor` owns the reviewed logical extent `0x00431500-0x0043153D`; `0x0043153E-0x0043153F` are two unassigned `CC` bytes and independent inventory candidate `0x00431540` begins immediately afterward. | Attested target bytes/xref and bounded PE disassembly. |
| TOOLCHAIN-048 | replay-metadata | Expanding `Supervisor.hpp` from an opaque method-only declaration to the target-supported 0x7B0 constructor layout changes only VC7.1 compiler-internal ChainRun COFF label names. A 35-unit cold regression left 33 directly affected canonical units exact immediately; `chain-run-calc` and `chain-run-draw` retained identical relocation offsets/types/target addresses and target bytes but local `$L...`/restart names advanced by 99. Section-local, offset-specific manifest refresh restored both 212/212 replays exact. | Full directly-affected header regression plus canonical target-bound replay after local-symbol refresh. |

| SUPERVISOR-009 | exact | `Supervisor::StartupThread @ 0x00431D10-0x00432081` is the Supervisor startup resource thread registered through the reviewed `CreateThread` wrapper at `0x00431AE0` from the startup path at `0x00432090`. It resets startup fields `+0x5A4/+0x5A8`, records `timeGetTime()` at `+0x798`, initializes the TH09 DirectInput seam, MIDI, SoundPlayer, text ANM and a 0x2A4 startup VM, registers the Ascii/etama/front preload seams, loads `bgm/thbgm.fmt`, derives SoundPlayer volume/attenuation state, selects/preloads `thbgm.dat` or `th09.dat`, initializes score data, optionally creates the dummy MIDI timer, then closes the startup thread and updates Supervisor/GameManager success state. Failure paths either return immediately when the close-request field `+0x6B4` is active or log and converge on startup-state 2 / flag `0x80`. | Two byte-identical canonical pinned-VC7.1 `/O2 /Ob1 /Oy- /Gr` replays reproduce all 882 logical function bytes with 106 target-bound relocations. The associated VC7.1 EH `.text$x` at `0x0048D380-0x0048D3AA` is 43/43 exact and `.xdata$x` at `0x0049DC50-0x0049DC83` is 52/52 exact after explicit relocation normalization. Clean committed TH08 source corroborates only the broad startup lifecycle shape; TH09 target evidence fixes this version's resource sequence, offsets, strings, callees, and branches. |
| SUPERVISOR-010 | exact | `SupervisorSetupDInputInternal @ 0x0042EEF0-0x0042F0F4` is the DirectInput setup dependency used only by exact `Supervisor::StartupThread`. VC7.1 privately passes the TU-local Supervisor pointer in EAX and returns the result in EAX. The helper uses input-Supervisor DirectInput interface `+0x0C`, keyboard `+0x10`, controller pointers `+0x14/+0x18`, and HWND `+0x74`; TH09 independently proves that the two 0x2C `DIDEVCAPS` outputs are absolute `g_Supervisor +0x1C/+0x48` storage rather than fields relative to the incoming pointer. It checks `IsDirectInputDisabled`, creates DirectInput/keyboard resources, configures/acquires the keyboard, enumerates two controllers, configures their data/cooperative levels and capabilities, installs object-enumeration callbacks, logs target-local status/error messages, and returns 0/-1. | Two byte-identical pinned-VC7.1 canonical replays reproduce all 517 bytes with 33 reviewed relocations; exact `Supervisor::StartupThread` remains 882/882 exact with 106 relocations on the same source revision. Original member/static spelling, DirectInput data definitions, and data/TU physical ownership remain unresolved; the two object-enumeration callback bodies/origins are now independently exact in SUPERVISOR-020. |
| TOOLCHAIN-049 | compiler-observed | The target EAX-only call into `0x0042EEF0` does not require an assembly calling-convention exception. A focused stock-VC7.1 experiment showed that `/O2` may privately rewrite the ABI of an internal-linkage C++ helper when all calls are visible in one TU. When the real TH09 DirectInput helper body is kept TU-local beside `Supervisor::StartupThread`, VC7.1 naturally chooses the target EAX input convention and the StartupThread caller becomes 882-byte structural exact. With a normal externally visible member-style declaration, the same startup source had only two non-relocation-byte differences and the wrong ECX call seam. | Pinned VC7.1 internal-linkage ABI probe followed by tracked `src/SupervisorStartup.cpp` structural and canonical target comparison. No inline assembly, naked function, custom compiler, volatile forcing, or register-directed source is used. |
| TOOLCHAIN-050 | compiler-observed / EH | `Supervisor::StartupThread` requires the optimized `/O2 /Ob1 /Oy-` family used by existing exact Supervisor units for its target EH lowering. Adding `/Os` changes the function to a 781-byte body and selects a helper-style EH prolog; removing `/Os` restores the target's inline SEH prolog and 882-byte code extent. The natural source also emits three operator-delete cleanup funclets plus the CxxFrameHandler thunk in a 43-byte `.text$x` and a 52-byte `.xdata$x`, both target-exact after relocation normalization. | Exact Chain-destructor calibration, startup profile probe, canonical StartupThread object, and explicit target/object EH-artifact normalization. This is local compiler evidence, not a project-wide flag claim. |
| BOUNDARY-051 | exact | `Supervisor::StartupThread` owns the logical authored code extent `0x00431D10-0x00432081`. The target bytes `0x00432082-0x0043208F` are fourteen `CC` bytes outside that logical function; independent startup-orchestration candidate `0x00432090` begins afterward. Compiler-associated EH code/data for the exact source lives non-contiguously at `0x0048D380-0x0048D3AA` and `0x0049DC50-0x0049DC83` and is recorded separately rather than added to authored-function byte totals. | Native IDA logical extent, bounded target bytes, canonical 882-byte replay, and normalized exact EH artifacts. |

| SUPERVISOR-011 | exact | `Supervisor::AddedCallback @ 0x00432090-0x0043217D` is the calc-chain lifetime callback installed by exact `SupervisorRegisterChain @ 0x00432180`. It sets game speed +0x5B8, runs the GameManager reset seam, requires the TH09 data/version loader, loads title/loading resources, sets FPS suppression +0x5A8, conditionally performs presentation validation, initializes loading resources, sets startupThreadState +0x6BC and starts exact `Supervisor::StartupThread`. Failure returns are -1 for data/loading failure and -2 for FPS/presentation validation. Exactness remains local to this function; dependency status is maintained independently in the current ledgers and must not be inferred from this historical caller graph. | Two byte-identical 238/238 canonical replays with 27 relocations plus current dependency ledgers. |
| TOOLCHAIN-051 | replay-metadata | Adding the target-proven `Supervisor::AddedCallback` declaration to `Supervisor.hpp` leaves the 37-unit cold replay set byte-stable. Thirty-five units replay exact immediately; only `chain-run-calc` and `chain-run-draw` advance their VC7 compiler-local `$L...`/restart relocation symbol names by two. Relocation offsets, types, external symbols, solved target destinations, and function bytes are unchanged. Offset-specific manifest refresh restores both ChainRun units to 212/212 exact, while `Supervisor::StartupThread` remains 882/882 exact and the new AddedCallback remains 238/238 exact. | Full 37-unit cold rebuild/target replay plus offset-specific ChainRun relocation-manifest refresh. This is compiler/debug metadata drift, not a source or target-byte difference. |
| BOUNDARY-052 | exact | `Supervisor::AddedCallback` owns `0x00432090-0x0043217D`. Target bytes `0x0043217E-0x0043217F` are two unowned `CC` bytes; independent 182-byte candidate `0x00432180` begins at the next address and stores this callback into the Supervisor calc ChainElem. | Native IDA function extent, explicit target-byte read across the boundary, xref from `0x004321BE`, and repeatable canonical 238-byte replay. |

| SUPERVISOR-012 | exact | `SupervisorCheckFps @ 0x0042F1A0-0x0042F3A4` is the Supervisor-subsystem presentation/FPS validator called only by exact `Supervisor::AddedCallback`. It renders/presents surface 8 through Direct3D8 while collecting up to four FPS samples of at least 57 over at most 600 frames. Each sample window accepts elapsed times from 500 through 699 ms; windows at least 700 ms reset without sampling. A failed `Present` resets the Direct3D8 device with Supervisor presentation parameters at `+0x350`. After sampling, exact `Supervisor::IsVsyncDisabled @ 0x0042ED50` bypasses validation. Otherwise fewer than two samples or an average above 160 log the first TH09 vertical-sync warning plus the forced-60-FPS message, while averages from 65 through 160 log the punctuated warning plus the same forced-60-FPS message; both failure classes set runtime `disableVsync +0x5AC` and return `-2`. Lower averages return 0. | Two byte-identical pinned-VC7.1 `/O2 /Ob1 /Oy- /Gr` canonical replays reproduce all 517 bytes with 34 reviewed relocations. The D3D8 device at `g_Supervisor +0x08`, presentation parameters at `+0x350`, runtime fallback at `+0x5AC`, exact `IsVsyncDisabled`, AnmManager surface-copy seam `0x0043BFA0`, WinMM timing imports, three target CP932 messages, and thresholds 57/65/160/1000 are all TH09-local observations. TH08/TH095 corroborate only the broad CheckFps shape. |
| BOUNDARY-053 | exact | The FPS/presentation validator owns `0x0042F1A0-0x0042F3A4`. Target bytes `0x0042F3A5-0x0042F3AF` are eleven `CC` bytes outside the function; independent candidate `0x0042F3B0` begins at the next address. The sole code xref to the 517-byte function is exact `Supervisor::AddedCallback @ 0x00432090`. | Native IDA function extent/xref, bounded target disassembly through the following candidate, and repeatable canonical 517-byte replay. |
| OWNERSHIP-006 | unresolved-spelling | TH08 and TH095 committed sources both spell their analogous no-argument routine `Supervisor::CheckFps`, and TH09 behavior is Supervisor-specific, but TH09 machine code has no argument or `this` register to distinguish a static class method from a free/TU helper. The maintained TH09 source therefore uses neutral `SupervisorCheckFps()` and leaves original member/free spelling plus the no-argument calling convention unresolved. | TH09 sole caller/relocations/global-state usage plus adjacent committed naming hypothesis. This uncertainty does not affect the 517-byte canonical code replay. |

| SUPERVISOR-013 | exact | `SupervisorRegisterChain @ 0x00432180-0x00432235` is the Supervisor lifecycle registration routine called only by `_WinMain@16`. It resets `g_Supervisor +0x58C` to 0, `+0x590` to -1, and `+0x454` to 0; creates a calc `ChainElem` for source-present `Supervisor::OnUpdate`; stores `g_Supervisor` as the callback argument, exact `Supervisor::AddedCallback @ 0x00432090` as the added callback, and exact `SupervisorDeletedCallback @ 0x00431B70` as the deleted callback; then inserts it at calc priority 0. A nonzero calc insertion result is returned unchanged. On success it creates three draw elements with the same Supervisor argument for exact `SupervisorDrawFps @ 0x00431B30`, exact `SupervisorDrawMain @ 0x00431540`, and exact `SupervisorDrawLoading @ 0x0042ED80` at priorities 31, 0, and 2 respectively, ignores their insertion results, and returns 0. | Two byte-identical canonical pinned-VC7.1 `/O2 /Ob1 /Oy- /Gr` replays reproduce all 182 bytes with 26 reviewed relocations. Exact TH09 `Chain::CreateElem`, `Chain::AddToCalcChain`, `Chain::AddToDrawChain`, the target-local ChainElem field layout, the sole `_WinMain@16` xref, and the complete target body establish the lifecycle wiring. All three registered draw callbacks are now independently canonical exact. |
| TOOLCHAIN-052 | compiler-observed | The natural `SupervisorRegisterChain` source is target-exact under the tested pinned VC7.1 `/O2 /Ob1 /Oy- /Gr` profile without `/Os`: 182/182 bytes, 78/78 non-relocation bytes, and 26 relocations all solve to the target. `/O2 /Ob0`, `/O2 /Ob2`, and `/O2 /Ob1 /Oy` preserve the exact 182-byte structure. In contrast, `/O2 /Os /Ob1 /Oy-`, `/O1 /Ob1 /Oy-`, and `/Ox /Os /Ob1 /Oy-` emit a 156-byte function. | Focused stock-VC7.1 profile matrix plus two byte-identical tracked-source canonical replays. This establishes only a function-local exact-producing optimization shape; it does not identify project-wide flags or translation-unit partition. |
| BOUNDARY-054 | exact | `SupervisorRegisterChain` owns `0x00432180-0x00432235`, ending in a plain `ret`. Target bytes `0x00432236-0x0043223F` are ten unowned `CC` bytes; independent candidate `0x00432240` begins next. | Native IDA extent, bounded attested target-byte reads across both ends, sole `_WinMain@16` xref, and canonical 182-byte replay. |
| OWNERSHIP-007 | unresolved-spelling | TH08 committed source spells its analogous routine `Supervisor::RegisterChain`, and TH09 behavior is unequivocally Supervisor-subsystem lifecycle registration, but the TH09 no-argument machine code does not independently distinguish a static class member from a free/TU helper or establish a no-argument calling convention. Maintained TH09 source therefore uses neutral `SupervisorRegisterChain()` and leaves the original member/free spelling and calling convention unresolved. | TH09 target-local global state, callback wiring, exact Chain dependencies, sole `_WinMain@16` caller, and adjacent committed naming hypothesis. This naming uncertainty does not affect canonical code equality. |

| SUPERVISOR-014 | exact | `SupervisorDeletedCallback @ 0x00431B70-0x00431D04` is the Supervisor calc-chain deletion callback installed by exact `SupervisorRegisterChain`; the ChainElem argument is `g_Supervisor`, and the routine consumes that pointer in ECX. It closes the Supervisor subthread, releases the version buffer through exact `ZunMemory::Free`, frees GameManager allocations at `+0x1C/+0x54` and the Supervisor startup VM at `+0x584`, releases ANM vertex/ANM/surface resources, cuts the exact AsciiManager chain, queues BGM release, conditionally stops MIDI playback, releases replay/text state, unacquires/releases the DirectInput keyboard plus two controllers and the DirectInput interface through the callback argument, releases the archive, stops/deletes the dummy MIDI timer at `+0x384`, calls three final subsystem-release seams, and returns 0. | Two byte-identical canonical pinned-VC7.1 `/O2 /Ob1 /Oy- /Gr` replays reproduce all 405 bytes with 44 reviewed relocations. Exact `SupervisorRegisterChain` relinks its offset-0x48 relocation to the canonical callback symbol and remains 182/182 exact. Referenced teardown helpers retain their independent review/exactness states. |
| TOOLCHAIN-053 | compiler-observed / source-shape | The natural Supervisor deletion source is sensitive to lifetime/source shape but requires no artificial codegen forcing. A first `/O2 /Ob1 /Oy- /Gr` source that cached `g_AnmManager` locally emitted 397 bytes; `/O2 /Os` and `/O1` emitted 391. Removing that unnecessary cache made VC7.1 use the target's EDI-zero/two-register shape and restored the 405-byte extent, leaving only six non-relocation differences. Placing the `startupVm +0x584` null assignment inside its non-null free block and the `dummyMidiTimer +0x384` stop/delete/null sequence inside its target-observed outer non-null block then produced 405/405 structural and canonical equality. | Focused pinned-VC7.1 profile/source-shape probes followed by two byte-identical structural reports and two byte-identical repository-defined canonical replays. No volatile forcing, padding, inline assembly, target-byte embedding, fake return, or register-directed trick is used. |
| BOUNDARY-055 | exact | `SupervisorDeletedCallback` owns `0x00431B70-0x00431D04`, ending in a plain `ret`. Target bytes `0x00431D05-0x00431D0F` are eleven unowned `CC` bytes; exact `Supervisor::StartupThread @ 0x00431D10` begins immediately afterward. | Native IDA extent/xref/disassembly, bounded target bytes through the following exact function, and canonical 405-byte replay. |
| OWNERSHIP-008 | unresolved-spelling | TH09 exact registration proves the Supervisor subsystem role and supplies `g_Supervisor` as the deletion callback argument; the target entry immediately preserves ECX as that pointer, matching the pinned `/Gr` one-argument callback ABI. Committed TH08 spells its analogous routine `Supervisor::DeletedCallback`, but TH09 machine code does not independently distinguish that static-member spelling from a free/TU helper. Maintained source and IDA therefore use neutral `SupervisorDeletedCallback` / `Supervisor_DeletedCallback` while preserving the original member/free spelling as unknown. | Exact TH09 registration linkage, ECX-consuming target prologue, complete callback body, pinned VC7.1 exact replay, and adjacent committed naming hypothesis. |

| SUPERVISOR-015 | exact | `SupervisorDrawFps @ 0x00431B30-0x00431B6A` is the priority-31 Supervisor draw callback installed by exact `SupervisorRegisterChain` with `g_Supervisor` as its ChainElem argument. It always calls exact `SupervisorCalculateFps @ 0x004316B0` with `shouldDraw=1`. When the callback Supervisor field at `+0x478` is nonzero, it calls the target-supported `Supervisor::ConfigureGameplayViewport(2)`, then calls the target-supported `AnmManager::DrawNoRotation` through `g_AnmManager` with the VM pointer stored at `0x004B3684`. It always returns Chain CONTINUE (1). | Two byte-identical canonical pinned-VC7.1 `/O2 /Ob1 /Oy- /Gr` replays reproduce all 59 bytes with six reviewed relocations; exact `SupervisorRegisterChain` relinks its offset-0x56 relocation to the canonical symbol and remains 182/182 exact. `0x004316B0`, `0x00401390`, `0x0043A950`, and the data owner at `0x004B3684` retain independent review/exactness states. |
| BOUNDARY-056 | exact | `SupervisorDrawFps` owns `0x00431B30-0x00431B6A`, ending in a plain `ret`. Target bytes `0x00431B6B-0x00431B6F` are five unowned `CC` bytes; exact `SupervisorDeletedCallback @ 0x00431B70` begins immediately afterward. | Native IDA extent/xref/disassembly, bounded target bytes through the following exact function, and repeatable canonical 59-byte replay. |
| OWNERSHIP-009 | unresolved-spelling | TH09 exact registration proves the Supervisor subsystem role and supplies `g_Supervisor` as the draw callback argument; the target entry immediately preserves ECX and reads its `+0x478` field, establishing the one-argument ECX callback ABI under the pinned `/Gr` profile. Committed TH095 names a related but materially different callback `Supervisor::DrawFpsCounter`; TH08 has no corresponding committed Supervisor draw implementation. TH09 machine code does not independently prove that static-member spelling, so maintained source and IDA use neutral `SupervisorDrawFps` / `Supervisor_DrawFps`. | Exact TH09 registration linkage, ECX-consuming target prologue, complete callback body, pinned VC7.1 exact replay, and adjacent committed naming comparison. |

| SUPERVISOR-016 | exact | `SupervisorDrawMain @ 0x00431540-0x00431677` is the priority-0 Supervisor draw callback installed by exact `SupervisorRegisterChain` with `g_Supervisor` as its ChainElem argument. It first calls target AnmManager seam `0x00439680`. When Supervisor `+0x740 >= 2` it increments the field; from state 5 onward it constructs `Float3(288,454,0)`, sets the target AsciiManager scale seam to 0.5, computes the two-branch `"Press Shot Button"` alpha fade, calls exact `AsciiManager::AddFormatText`, restores scale 1.0, calls exact `AsciiManager::DrawStrings`, and clears the normal string count through `0x00430220`. State 65 or greater is reset to 5. A nonzero state copies surface slot 8 through `0x0043BFA0`; zero state checks slot 8 through `0x0042F400` and releases it through `0x0043BF30` when present. All paths return Chain CONTINUE (1). | Two byte-identical canonical pinned-VC7.1 `/O2 /Ob1 /Oy- /Gr` replays reproduce all 312 bytes with 21 reviewed relocations; exact `SupervisorRegisterChain` relinks offset `0x75` to the canonical symbol and remains 182/182 exact. The short Ascii/Anm helper callees retain independent review/exactness states. |
| BOUNDARY-057 | exact | `SupervisorDrawMain` owns `0x00431540-0x00431677`, ending in a plain `ret`. Target bytes `0x00431678-0x0043167F` are eight unowned `CC` bytes; independent candidate `0x00431680` follows. | Native IDA extent/xref/disassembly, bounded target bytes through the next candidate, and repeatable canonical 312-byte replay. |
| TOOLCHAIN-054 | exact-local-profile | Natural TH09 `SupervisorDrawMain` under pinned VC7.1 `/O2 /Ob1 /Oy- /Gr` reaches the target 312-byte code only when the source preserves the target-visible lifetime/control-flow shape: the AnmManager pointer is not cached across the routine, and the two adjacent-game-style alpha branches each express their own equivalent fade expression and setter call, allowing VC7.1 `/O2` to tail-merge the shared arithmetic/call. A manually pre-combined alpha-delta expression emitted 314 bytes because VC7.1 inserted an extra register move; an early whole-routine AnmManager cache emitted 332 bytes. | Focused ignored probes: 332-byte initial cache shape, 314-byte target-control-flow shape with manually combined alpha tail, then 312/312 structural-exact adjacent-style branch source. This is a function-local source/codegen fact, not a project-wide flag claim. |
| OWNERSHIP-010 | unresolved-spelling | Exact registration and the target prologue establish the Supervisor subsystem role and one-argument ECX callback ABI for `0x00431540`. Committed TH08 and TH095 both use the name `Supervisor::OnDraw2` for closely related startup/title draw routines, but TH09 differs in field offset (`+0x740`), has an unconditional `0x00439680` AnmManager seam, and lacks TH095's viewport/backbuffer-clear preamble. TH09 machine code does not independently prove the original static-member spelling, so maintained source/IDA use neutral `SupervisorDrawMain` / `Supervisor_DrawMain`. | Exact TH09 registration linkage, complete target body, pinned VC7.1 exact replay, and adjacent committed comparison after TH09 evidence. |

| SUPERVISOR-017 | exact | `SupervisorDrawLoading @ 0x0042ED80-0x0042EDC6` is the priority-2 draw callback installed by exact `SupervisorRegisterChain`. Exact `Supervisor::AddedCallback` reaches target seam `0x004302B0`, which initializes three 0x2A4 loading VMs rooted at `0x004B38B0`, `0x004B3B54`, and `0x004B3DF8` and copies one input Float3 to each VM at `+0x208`. The draw callback loops exactly three times. For each VM it temporarily adds the Float3 at `+0x288` to the primary position at `+0x208`, draws the VM through `g_AnmManager` and candidate `0x0043AB50`, then subtracts the same offset so persistent position storage is restored. It always returns Chain CONTINUE (1). | Two byte-identical canonical pinned-VC7.1 `/O2 /Ob1 /Oy- /Gr` replays reproduce all 71 bytes with five reviewed relocations. The relocation to the loading-VM array resolves target field `0x004B3B38` as base `0x004B38B0` plus addend `0x288`; target-local `0x004302B0`, 0x2A4 VM stride, and the two in-place Float3 helper bodies independently close the layout. Helper callees and physical/TU data ownership remain separate review subjects. |
| BOUNDARY-058 | exact | `SupervisorDrawLoading` owns `0x0042ED80-0x0042EDC6`, ending in a plain `ret`. Target bytes `0x0042EDC7-0x0042EDCF` are nine unowned `CC` bytes; independent candidate `0x0042EDD0` starts next. | Native IDA extent/xrefs/disassembly, bounded target-byte read across both sides, explicit function lookup at `0x0042EDD0`, and repeatable canonical 71-byte replay. |
| TOOLCHAIN-055 | compiler-observed / source-shape | Natural TH09 loading-VM source is sensitive to the visible loop form but needs no register forcing. A forward `for (i=0; i<3; ++i)` source emitted 66 bytes because VC7.1 replaced the count with an end-pointer comparison. A pointer-based countdown restored EBX count and loop alignment but emitted 74 bytes because keeping the primary `+0x208` position in ESI required a 6-byte `+0x80` LEA. Expressing indexed VM accesses together with an independent three-iteration countdown makes VC7.1 keep the secondary `+0x288` Float3 in ESI, use the target 3-byte `-0x80` LEA for primary position, preserve `dec ebx / jnz`, and emit the exact 71-byte target. | Three focused pinned-VC7.1 ignored probes (66, 74, then 71 bytes) plus two repository-defined canonical replays. No pragma var-order, volatile forcing, padding, inline assembly, target-byte embedding, fake return, or explicit register direction is used. |
| OWNERSHIP-011 | unresolved-spelling / data-owner | Exact registration proves Supervisor-subsystem draw-callback ownership and supplies `g_Supervisor` as the ChainElem argument, but the 71-byte body never reads that argument; maintained source records the reconstructed one-argument `/Gr` Chain callback ABI while leaving original static-member/free spelling and source-level argument use unresolved. Clean committed TH08 names its analogous routine `Supervisor::DrawLoadingVms`; this is corroboration only. The three loading VM addresses and 0x2A4 stride are target-local facts, but their original global symbol name, data-definition translation unit, and physical source owner remain unresolved, so maintained source uses reconstruction-only extern `g_SupervisorLoadingVms`. | Exact TH09 registration and Chain callback protocol, target-local `0x004302B0` initializer, callback body/relocations, pinned VC7.1 exact replay, and committed TH08 comparison after TH09 facts were fixed. |

| SUPERVISOR-018 | exact | `SupervisorCalculateFps @ 0x004316B0-0x00431928` is the shared Supervisor FPS measurement/update routine. Exact `SupervisorDrawFps @ 0x00431B30` passes `shouldDraw=1`; source-present `GameplaySetupThread @ 0x0041AF2D` passes `0` before its loading-finish seam. The routine always increments its frame counter by unsigned Supervisor config byte `+0x43A` plus one. With Supervisor `+0x744 == 0`, it uses `timeGetTime` and a 500 ms window; otherwise it uses `QueryPerformanceCounter` and half of `+0x744` as the sample threshold. Both paths converge on FPS calculation and `"%.02ffps"` formatting. Under GameManager flag `0x4` plus `shouldDraw`, it adds 60 to Supervisor `+0x5C4`, adds 60/48/36/30 to `+0x5C0` according to FPS thresholds 54/42/30, and either updates signed-short Supervisor `+0x5C8` from rounded FPS or formats that recorded value to the debug buffer according to target replay-state predicate `0x00415D20`. When `suppressFpsDisplay +0x5A8` is clear and the draw/GameManager gates are active, it queues the FPS buffer through exact `AsciiManager::AddString` at `Float3(280,466,0)`. | Two byte-identical canonical pinned-VC7.1 `/O2 /Ob1 /Oy- /Gr` replays reproduce all 633 bytes with 65 reviewed relocations. TH09 replay recording at `0x004202A0` samples `+0x5C8` every 30 frames and replay playback at `0x004204B0` restores its low seven bits, independently corroborating the recorded-FPS role. Exact `Float3::Float3` and `AsciiManager::AddString`, both TH09 callers, target constants, WinMM/QPC imports, and complete target control flow are local evidence; committed TH08 `Supervisor::CalculateFps` was consulted only after these facts were established. |
| BOUNDARY-059 | exact | `SupervisorCalculateFps` owns `0x004316B0-0x00431928`, ending in a plain `ret`. Target bytes `0x00431929-0x0043192F` are seven unowned `CC` bytes; independent candidate `0x00431930` starts next. | Native IDA function extent/disassembly, bounded target-byte read through the following candidate, explicit lookup of `0x00431930`, and repeatable canonical 633-byte replay. |
| TOOLCHAIN-056 | compiler-observed / source-shape | Natural TH09 FPS source under the established Supervisor-local pinned VC7.1 `/O2 /Ob1 /Oy- /Gr` profile reached the correct 633-byte total on the first successful compile but initially matched 349/373 non-relocation bytes. The target uses one 0x0C-byte stack area by reusing the QPC local and tail `Float3` across disjoint lifetimes, while a function-scope `LARGE_INTEGER` caused VC7.1 to reserve 0x18 bytes. Moving that local into the QPC branch restored the target stack reuse. Writing the threshold chain as adjacent-style `constant < fps` caused reversed x87 operands (`test AH,5`); expressing target-visible `fps > threshold` branch direction produced the target `test AH,0x41` sequence. Those two natural source-shape changes yielded 633/633 structural equality and all 65 relocation destinations without changing compiler flags. | Focused pinned-VC7.1 first-successful 633-byte near-match, object/target disassembly alignment, corrected scoped-local/branch-expression probe, tracked-source structural replay, then two byte-identical canonical replays. No pragma var-order, volatile forcing, padding, assembly, target-byte embedding, fake return, or explicit register direction is used. |
| OWNERSHIP-012 | unresolved-spelling / data-owner | TH09 directly proves a one-integer ECX `/Gr` ABI for `0x004316B0`: exact `SupervisorDrawFps` calls with ECX=1 and `GameplaySetupThread` calls with ECX=0, and the routine uses that value only as a draw/statistics gate while accessing Supervisor state globally. Both callers ignore EAX, and exact natural `void __fastcall SupervisorCalculateFps(int shouldDraw)` source reproduces the target; committed TH08 spells its analogous routine `static void Supervisor::CalculateFps(ZunBool shouldDraw)`. TH09 does not independently encode that static-member spelling, so maintained source/IDA retain neutral `SupervisorCalculateFps` / `Supervisor_CalculateFps`. Static FPS timing globals at `0x004B42B8-0x004B42CC`, text buffers `0x004B40B8/0x004B41B8`, and their original data-definition/TU ownership remain unresolved despite exact relocation binding. | TH09 dual callers, exact body/replay, replay producer/consumer evidence for `+0x5C8`, and adjacent committed naming corroboration. |

| TOOLCHAIN-057 | compiler-observed / source-shape | The former natural DirectInput helper emitted 510 bytes because it expressed both controller capability records relative to the incoming Supervisor. Replacing only those four accesses with TH09-observed absolute `g_Supervisor +0x1C/+0x48` storage restores the 517-byte target extent. Restoring natural duplicated DirectInput8Create/CreateDevice error blocks then improves relocation-masked equality from 353/385 to 377/385 comparable bytes. Finally, placing the CreateDevice-failure null assignment only inside its non-null release block lets VC7.1 tail-merge the equivalent failure paths and produces 385/385 comparable-byte equality and full 517/517 canonical equality. | Focused pinned-VC7.1 probes plus two byte-identical repository-defined canonical replays. No volatile forcing, inline assembly, inert padding, target-byte embedding, fake return, register hint, identifier search, or custom compiler behavior is used. Clean committed TH08 and committed-only TH095 corroborate the broad natural error/resource source shape only after TH09 fixed the storage and control flow. |
| BOUNDARY-060 | exact | `SupervisorSetupDInputInternal` owns `0x0042EEF0-0x0042F0F4` and ends in a plain `ret`. Target bytes `0x0042F0F5-0x0042F0FF` are eleven `CC` bytes outside the function; independent candidate `0x0042F100` begins next and is 0x91 bytes in the current native-IDA inventory. | Native IDA function extent, bounded target bytes through the following candidate, sole exact StartupThread caller, and repeatable canonical 517-byte replay. |
| OWNERSHIP-013 | unresolved-spelling / split-storage | The DirectInput helper receives one Supervisor object through a compiler-private TU-local EAX ABI, and its interface/keyboard/controller/HWND accesses are relative to that input object. In contrast, both controller-capability records are physically addressed through global `g_Supervisor +0x1C/+0x48`. This distinction is target-observed and retained in maintained source; it does not establish the original source-level member/static spelling, whether the incoming object was always the global at source level, or the physical/TU owners of the GUID/data-format/message/callback objects. | Exact StartupThread caller, target disassembly/relocations, absolute capability relocations, and exact maintained-source replay. |

| SUPERVISOR-019 | exact | `StartupEnumControllers @ 0x0042EDD0-0x0042EE2C` is the DirectInput game-controller enumeration callback passed by exact `SupervisorSetupDInputInternal` to `IDirectInput8::EnumDevices`. It is a two-argument `BOOL __stdcall` callback and ignores its context argument. When global `g_Supervisor +0x14` is null, it calls the DirectInput interface at global `+0x0C` with `instance->guidInstance` and the controller0 slot, ignores that HRESULT, and returns TRUE to continue enumeration. Once controller0 exists, an existing controller1 at `+0x18` also returns TRUE; otherwise the callback creates controller1 and returns FALSE only when that second creation succeeds, stopping enumeration after two devices. | Two byte-identical pinned-VC7.1 `/O2 /Ob1 /Oy- /Gr` canonical replays reproduce all 93 bytes with six reviewed relocations, all resolving to `g_Supervisor` with addends `+0x14/+0x0C/+0x14/+0x18/+0x0C/+0x18`. Exact `SupervisorSetupDInputInternal` independently replays 517/517 with the callback relocation still bound to `0x0042EDD0`. TH08/TH095 committed single-controller callbacks corroborate only the DirectInput enumeration role/name family after TH09 fixed its distinct two-controller policy. |
| SUPERVISOR-020 | exact | `StartupEnumObjects0 @ 0x0042EE30-0x0042EE8D` and `StartupEnumObjects1 @ 0x0042EE90-0x0042EEED` are the paired DirectInput object-enumeration callbacks passed by exact `SupervisorSetupDInputInternal` to controller0 and controller1 respectively. Both are two-argument `BOOL __stdcall` callbacks and ignore their context argument. For objects whose `DIDEVICEOBJECTINSTANCEA::dwType` intersects `DIDFT_AXIS`, each builds a 24-byte `DIPROPRANGE` with a 16-byte header, `dwObj = dwType`, `DIPH_BYID`, and limits -1000 through 1000, then calls `SetProperty(DIPROP_RANGE, &range.diph)` on global `g_Supervisor` controller slot `+0x14` or `+0x18`; a negative HRESULT returns FALSE and all other paths return TRUE. | Two byte-identical pinned-VC7.1 `/O2 /Ob1 /Oy- /Gr` canonical replays reproduce 94/94 bytes for each callback, with exactly one reviewed DIR32 relocation at offset `+0x18` resolving to `g_Supervisor` plus `+0x14` or `+0x18`. Exact `SupervisorSetupDInputInternal` independently binds the callback symbols at its target offsets `+0x18C` and `+0x1E5`. Target bytes prove two unowned `CC` bytes after each callback. Clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` and committed TH095 `2e4e988e9747403286fe053245bcf3a0184d6289` corroborate only the natural `DIPROPRANGE` callback shape; TH09 decides the paired-device policy, addresses, extents, ownership, and exactness. Original static-member/free spelling and original translation-unit ownership remain unresolved. |
| BOUNDARY-061 | exact | `StartupEnumControllers` owns `0x0042EDD0-0x0042EE2C` and both exits use `ret 8`. Target bytes `0x0042EE2D-0x0042EE2F` are three unowned `CC` bytes; exact `StartupEnumObjects0 @ 0x0042EE30` begins immediately afterward. | Native IDA extent/xref/decompilation, bounded target disassembly/bytes through `0x0042EE30`, exact caller linkage, and repeatable canonical 93-byte replay. |
| BOUNDARY-062 | exact | `StartupEnumObjects0` owns `0x0042EE30-0x0042EE8D` and `StartupEnumObjects1` owns `0x0042EE90-0x0042EEED`; both exits use `ret 8`. Target bytes `0x0042EE8E-0x0042EE8F` and `0x0042EEEE-0x0042EEEF` are two-byte `CC` gaps outside the authored extents. Exact `SupervisorSetupDInputInternal @ 0x0042EEF0` begins immediately after the second gap. | Native IDA extents/xrefs/disassembly, bounded target bytes, exact setup callback linkage, and two byte-identical 94-byte canonical replays for each function. |
| OWNERSHIP-014 | unresolved-spelling / global-storage | The callback's machine ABI is independently `BOOL __stdcall(instance, context)` and exact SetupDInput uses it as an `IDirectInput8::EnumDevices` callback. The body ignores context and accesses the DirectInput interface and both controller output slots through absolute `g_Supervisor` storage, so callback storage ownership is global rather than context-relative. Committed TH08/TH095 spell analogous single-controller routines `Supervisor::EnumGameControllersCb`, but TH09 does not independently prove that original static-member spelling or the original translation-unit owner; maintained source therefore keeps neutral `StartupEnumControllers`. | TH09 callback ABI/bytes, six exact global relocations, exact SetupDInput function-pointer linkage, and adjacent committed naming corroboration only after TH09 evidence. |

| TOOLCHAIN-058 | compiler-observed / improved-nonexact | Fresh packet-64 alignment showed the old 32-byte explanation was too narrow: target preserves six physical `GameManager_CutChain` calls and three `GameManager_RegisterChain` calls across several transition tails, while the former source/object emitted 5/2 because VC7.1 folded the previous-state 10/13 path into the common re-register block. Target decompilation independently exposes a label boundary that shares only the final `curState = 2` write. Expressing exactly that natural topology restores the object to 6/3 calls and moves the first switch artifact from `+0x32C` to `+0x33C`, reducing the code deficit from 32 to 16 bytes. Two cold compiles produce byte-identical 980-byte `.text` sections (828 code + 152 switch artifacts), SHA-256 `c36dfd2bc50dc36d7fa937da7d412a2ec5ed395fb2a468932d38c930f2e9c583`; the five switch-artifact segment sizes and `00 05 05 01 05 02 05 03 05 05 04 90` case-index/alignment bytes remain exact. Residual layout is still compiler/source-shape sensitive: target has one physical `RegisterStateObject` call shared by mode0/mode1, whereas the best maintained source emits three and compensates with different basic-block placement/tail sharing. A single-mode-label probe recovered the third RegisterChain but merged two target-distinct Cut predecessors and worsened the code deficit; a decompiler-topology/common-helper probe matched helper-call counts but moved the first table earlier and was also rejected. No volatile/register forcing, padding, assembly, target bytes, fake returns, identifier search, or arbitrary flag search is retained. | Target switch tables/decompiler plus bounded target/object instruction alignment; focused pinned-VC7.1 probes; repeatable final source compile; exact `SupervisorRegisterChain` 182/182 regression. Exactness remains false. |

| TOOLCHAIN-059 | compiler-observed / persistent-nonexact | Packet 65 re-established `GameplaySetupThread` from clean `9af9013c...`: current source SHA-256 `4daa4cf22eb466fe08b312e6beddd7e446003455e1cc2eff0b091f1c91a0a4f7` emits the exact 1,689-byte target extent under both pinned `/O2 /Os /Ob1 /Oy- /Gr` and `/O1 /Ob1 /Oy- /Gr`, but remains exactly 402/1005 comparable non-relocation bytes with first difference at `+0x1DB`. `/G6`, `/G7`, and `/GB` preserve the same 1,689 / 402-of-1005 result. Fresh alignment explains the first difference: target does not keep `g_GameManager` in a callee-saved register across the reuse path, instead reloading its address into ECX for `AdvanceTimedState`/`UpdateProgress`; the maintained object loads the base into ESI at `+0x206`, shortening repeated member-call setup. Target subsequently uses ESI as a `+0x38` induction pointer rooted at side flags `g_GameManager+0x34`, while the object roots the same induction at side selector `+0x28`. Target and object otherwise have identical prologue/save set and the same sole four-byte `[ebp-4]` compiler temporary for the `valueAC` integer-to-float conversion. Focused natural probes did not improve this: reload-path direct globals remain byte-identical to baseline; a flags cursor emits 1,686 bytes; a record-pointer loop emits 1,691; moving flags before selector keeps 1,689/402 but reverses target instruction order; a shared function-scope side cursor emits 1,695. These negative probes rule out simple optimization-level, CPU-scheduling, direct-global, loop-condition, and adjacent statement-order explanations without justifying register forcing. | Fresh target/object disassembly and IDA decompilation/stack-frame evidence under passed `target:th09-main` attestation; focused VC7.1 probes; exact caller/dependency context. No volatile forcing, `var_order`, padding, assembly, target-byte embedding, fake return, or arbitrary flag search is retained. Exactness remains false. |
| OWNERSHIP-015 | authored-origin / source-owner-unresolved | Exact `GameManagerSetupLayout::AddedCallback` stores `GameplaySetupThread @ 0x0041AF2D` as the Supervisor thread entry while operating the same `g_GameManager` lifecycle, and the thread body directly creates/updates GameManager-owned configuration, side state, stage object, and subsystem graph. This is sufficient to classify the function origin as authored game code in the GameManager subsystem. It does not prove the original C++ member/static spelling, translation-unit partition, object-file owner, or codegen equality. | Exact TH09 registration relocation and callback, complete target-local setup behavior, maintained source, and independent non-exact codegen result. |
| INPUT-002 | exact / authored-game | `ReplayInputState::Update @ 0x004200F0-0x0042015E` updates one TH09 replay/input state from its 16-bit `historyCurrent +0x2C`: it clears `repeatOutput +0x04`, advances sixteen 16-bit held counters beginning at `+0x38`, uses repeat threshold 26 with an 8-frame rollback and repeat bit `+0x30`, then computes pressed/released edge words at `+0x32/+0x34` from `historyCurrent/historyPrevious +0x2C/+0x2E`. The maintained 0x8E `ReplayInputState` name/layout is a target-bound reconstruction view; original source-level type/member spelling and TU ownership remain unresolved. | TH09 native-IDA disassembly/callers plus two byte-identical repository-defined pinned-VC7.1 replays reproduce all 111 bytes with zero relocations; canonical report SHA-256 is `b356c87c2a709a487c08d11ed6c51e82d83810a29abc02775e68075e37358f54`. Committed TH095 `ReplayInputSource::Update` was consulted only after TH09 fixed every offset/constant and corroborates the readable field roles, not TH09 ownership or exactness. |
| TOOLCHAIN-061 | compiler-observed | The natural replay/input history update is locally profile-sensitive. The TH095-shaped index loop emits 101 bytes under pinned VC7.1 `/O2 /Os /Ob1 /Oy- /Gr`; a natural pointer/countdown form emits 98 bytes with `/Os`. The same pointer/countdown source without `/Os`, under `/O2 /Ob1 /Oy- /Gr`, reproduces the target 111-byte body exactly and remains exact when compiled in the maintained combined replay/input TU. | Focused stock-VC7.1 source/profile probes followed by two byte-identical canonical `replay-input-update` replays. This is function-local compiler evidence only; it does not establish project-wide flags or original translation-unit partition. |
| REPLAY-001 | source-present / codegen-nearmatch / authored-game | `ReplayManagerView::CaptureFrameSyncState @ 0x00420190-0x00420290` is the record-mode ReplayManager frame-sync callback. TH09 `0x00421670` allocates/zeros a 0x164 replay object, registers this address as a calc callback at priority 7 with that object as `arg`, and immediately invokes it; exact `GameplaySetupThread` reaches the ReplayManager creation path and stores the returned object at `0x004A7E64`. The callback stores RNG seed/event state at replay-object `+0x160/+0x162`, consumes GameManager pause-recorded state `+0x130`, rolls three 0x8E input histories at `0x004ACE18/0x004ACEA6/0x004ACF34`, calls exact `ReplayInputState::Update` three times, and maintains two side auxiliary counters at input-state `+0x2A` behind the `g_GameManager+0xE8` gate. Maintained source emits the complete 257-byte target extent and solves all 28 relocation destinations but remains non-exact at 144/145 comparable bytes. | TH09 registration/caller/xref/disassembly evidence plus pinned-VC7.1 maintained-source comparison. The sole residual is one commutative SIB byte at function `+0xBD`: object `[EDI+EBX+0xB4]` versus target `[EBX+EDI+0xB4]`. Natural struct-view, byte-view, and swapped pointer-addition forms all retain that encoding, so no register/encoding-directed trick is introduced. Clean committed TH08 `ReplayManager::CaptureFrameSyncState` corroborates the callback role after TH09 fixed the lifecycle; TH08/TH095 replay object sizes differ and are not transferred. |
| TOOLCHAIN-170 | bounded negative / CaptureFrameSyncState SIB plateau | `ReplayManagerView::CaptureFrameSyncState @ 0x00420190` remains a full-size 257-byte near match with all 28 relocation destinations solved and 144/145 ordinary comparable bytes equal. The sole residual is still the scale-1 commutative SIB at `+0xBD`: target encodes `[EBX+EDI+0xB4]` while the candidate encodes `[EDI+EBX+0xB4]`. The established `/EHsc` build, the EH-disabled profile independently motivated by exact `ReplayManagerView::Create`, and a TU-private free-static visibility probe all produce the same residual. Earlier target-bounded struct-view, byte-view, and swapped pointer-addition probes also retain it. With the signed loop shape already matching the target's `jl` tail, further pursuit would require source spelling chosen only to steer operand encoding, so the frontier is deliberately left NON-EXACT. | Baseline relocation-aware comparison, EH-disabled compile probe, internal-linkage probe, prior natural type/expression negatives in REPLAY-001, and target disassembly at `0x0042024B`. |
| TOOLCHAIN-062 | unresolved-codegen / near-exact | Natural frame-sync source under pinned VC7.1 `/O2 /Ob1 /Oy- /Gr` converges from 254 bytes to the exact 257-byte target extent by preserving target-visible per-state history update order and caching the `g_GameManager+0xE8` gate outside the two-side loop. The best maintained source has all 28 target relocation destinations and 144/145 comparable-byte equality; its only residual is the scale-1 SIB operand ordering at `+0xBD`, which is semantically commutative. Rewriting the gate as a raw byte view or reversing C++ pointer-addition order leaves the same byte, while broader local-temporary variants worsen size/scheduling. | Focused packet-69 source-shape probes and complete target/object alignment. No volatile forcing, `var_order`, padding, inline assembly, target-byte embedding, fake return, explicit register hint, or custom compiler behavior is retained. Exactness remains false. |
| BOUNDARY-063 | exact | `ReplayInputState::Update` owns `0x004200F0-0x0042015E`, ending in a plain `ret`; `0x0042015F` is one outside `CC` byte. Independent candidates at `0x00420160`, `0x00420170`, and `0x00420180` are now independently canonical exact as `ReplayRngView::SetSeed`, `ReplayRngView::ResetGenerationCount`, and `SupervisorReplayView::IsSpeedhackDetected`; their interstitial CC gaps remain unowned. | Verified target PE boundaries plus current canonical replay/input leaf match units. |
| BOUNDARY-064 | observed | `ReplayManagerView::CaptureFrameSyncState` owns the reviewed logical extent `0x00420190-0x00420290`; target bytes `0x00420291-0x0042029F` are fifteen `CC` bytes before independent candidate `0x004202A0`. This reviewed boundary does not grant codegen exactness to the current source. | Native IDA extent/xrefs, verified target PE bytes through both boundaries, and the maintained 257-byte source comparison. |
| REPLAY-002 | exact / authored-game | `ReplayManagerView::RecordInputAndFps @ 0x004202A0-0x0042046B` is the TH09 record-mode ReplayManager calc callback. `0x00421670` allocates and zeroes the 0x164 replay object, installs this callback with that object as `arg`, and adds it to the calc chain at priority 26. The callback snapshots GameManager flags `+0x134`, rejects the inactive/speedhack-gated path, applies a three-frame delay while flag `0x200` is active, conditionally writes side-0/side-1 `historyCurrent +0x2C` according to the two 0x38-stride selectors at GameManager `+0x28/+0x60`, always writes the third 0x8E replay-input history, samples Supervisor signed-short recorded FPS `+0x5C8` every 30 frames, and advances a 0x18-byte linked replay buffer. At 3598 frames it allocates a new `"ReplayBufLinkInf"` node, three 0x1C20 input streams and one 0x79 FPS stream using exact ZunMemory helpers, installs the pointers at link `+0/+4/+8/+0xC`, clears count/next at `+0x10/+0x14`, and zeroes all four streams. The maintained `ReplayManagerView`/`ReplayBufferLink` names are target-bound reconstruction views; original class/member spelling, translation-unit partition, and static data-definition ownership remain unresolved. | TH09 native-IDA registration/xref/disassembly/decompilation and record-stage initializer `0x00421140`, exact Chain/ZunMemory dependencies, and two byte-identical repository-defined pinned-VC7.1 replays reproduce all 460 bytes with 25 reviewed relocations. Both canonical reports have SHA-256 `47b28a44e06f3fe51c2bb4569056d4bc8ba126532331082b2088bd2eec9f4f55`. Clean committed TH08 `ReplayManager::RecordInputAndFps` was consulted only after TH09 fixed the owner/layout and corroborates the broad callback/source-shape family; TH08's 0xDC replay object and stream layout are not transferred. |
| TOOLCHAIN-063 | compiler-observed / source-shape | The natural TH09 record callback is locally sensitive to whether GameManager flags are read twice or visibly snapshotted once. With the packet-local pinned VC7.1 `/O2 /Ob1 /Oy- /Gr` profile, direct global reads emit 461 bytes: VC7.1 tests flag bit 4 in memory, reloads the flags after the Supervisor gate, and therefore changes the later cursor register allocation. Adding the semantically natural `u32 flags = g_GameManager.flags;` snapshot makes VC7.1 preserve that value across the gate call and then reuse the register after the flag lifetime ends, reproducing the target 460-byte function exactly. | Focused v1/v2 stock-VC7.1 probes, complete target/object instruction alignment, tracked-TU regression, and two canonical 460/460 replays with identical report hashes. This is function-local source/profile evidence only; it does not establish project-wide compiler flags or original TU partition. No volatile forcing, `var_order`, padding, inline assembly, target-byte embedding, fake return, register hint, or custom compiler behavior is used. |
| BOUNDARY-065 | exact | `ReplayManagerView::RecordInputAndFps` owns `0x004202A0-0x0042046B`, ending in a plain `ret`. Target bytes `0x0042046C-0x0042046F` are four unowned `CC` bytes; independent executable code begins at `0x00420470`. | Native IDA 0x1CC candidate extent, verified target bytes across the boundary, function-pointer registration xref, and repeatable canonical 460-byte replay. |
| REPLAY-003 | source-present / codegen-nearmatch / authored-game | `ReplayManagerView::BeginRecordingStage @ 0x00421140-0x0042166A` is the record-mode ReplayManager added callback installed by `0x00421670` on the same Chain element whose main callback is exact `ReplayManagerView::RecordInputAndFps`. The routine resets ReplayManager frame state, creates a 0x1EC replay header when absent, maintains four ten-entry per-stage pointer tables at header `+0x20/+0x48/+0x70/+0x98`, allocates three 0x20 frame-start records plus one one-byte FPS-start record for the selected stage, snapshots target-local stage/side values, allocates three 0x1C20 input streams plus one 0x79 FPS stream, frees any prior 0x18 linked-buffer chain for that stage, installs the new stream pointers, and zeroes the streams. Maintained natural source covers the complete 1,323-byte extent and resolves all 82 relocation fields, but remains non-exact at 991/995 comparable bytes. | TH09 native-IDA registration/xrefs/decompilation/disassembly, exact ZunMemory dependencies, packet-70 exact consumer linkage, and tracked-source VC7.1 comparison. The sole residual is the semantically equivalent `mov EDI,EBX; test EDI,EDI` versus `test EBX,EBX; mov EDI,EBX` scheduling at function `+0x42A..+0x42D`. Clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` supplied only the broad `BeginRecordingStage` naming/source-family hypothesis after TH09 fixed the lifecycle and layout; TH08's 0x134 replay data and 0xDC manager layout are not transferred. TH095 committed `8ec6431ce3db5fbc916aede84a1aa66b0983b6ca` uses another replay layout and did not decide any TH09 field. |
| TOOLCHAIN-064 | compiler-observed / unresolved-codegen | Packet 71 isolates two local source/profile facts without promoting project-wide flags. With the target-derived source shape, VC7.1 `/O2 /Ob1 /Oy- /Gr` plus `/EHsc` emits 1,359 bytes because the `new ReplayDataView` constructor path gains unwind scaffolding, while EH-disabled lowering emits 1,317 bytes before source-shape corrections. Target-proved manager reloads for replay-header `+0x1E8/+0x1E9`, pointer indirection for the 0xCC config snapshot, chained replay-data assignment, and explicit raw `operator new(size)` as the first `AddToRegistry` argument for the POD start blocks restore the target's evaluation order and exact 1,323-byte extent. `/O1` emits 1,103 bytes and `new Type()` value-initialization emits 1,445 bytes, so both are rejected for this packet. The maintained source then converges to 991/995 comparable bytes; a natural `if` plus `do/while` list form preserves the same four-byte scheduling residual, so the packet stops rather than forcing registers or encodings. | Focused stock-VC7.1 v1-v7 probes, TH09 exact `Chain::CreateElem` and packet-70 registry-allocation idioms as local controls, complete target/object alignment, and tracked-source replay. This is packet-local compiler evidence only; original translation-unit partition and project exception settings remain unresolved. No volatile forcing, register hint, pragma var-order, inert padding, inline assembly, target-byte embedding, fake return, or custom compiler behavior is retained. Exactness remains false. |
| BOUNDARY-066 | observed | `ReplayManagerView::BeginRecordingStage` owns the reviewed logical extent `0x00421140-0x0042166A`, ending in a plain `ret`. Target bytes `0x0042166B-0x0042166F` are five `CC` bytes outside the function; independent ReplayManager creation code begins at `0x00421670`. The reviewed boundary and high-confidence authored origin do not grant codegen exactness. | Native IDA 0x52B extent, verified target disassembly/bytes through the following creation routine, function-pointer registration xref, and maintained 1,323-byte source comparison. |
| REPLAY-004 | exact / authored-game | `ReplayManagerView::PlaybackInputAndFps @ 0x004204B0-0x004205DA` is the TH09 playback-mode main ReplayManager calc callback. `0x00421670` installs it at priority 6 with the 0x164 replay object as Chain `arg`. While GameManager replay input is active and transition bit `0x200` is clear, it rolls and restores the three 0x8E input histories from ReplayManager cursors `+0x0C/+0x10/+0x14`, calls exact `ReplayInputState::Update` three times, maintains the two side auxiliary counters through the GameManager `+0xE8` side-mode bytes without a null guard, and every 30 frames restores Supervisor `recordedFps +0x5C8` plus playback warning `+0x780` from the FPS stream at ReplayManager `+0x18`. Five `CC` bytes at `0x004205DB-0x004205DF` remain outside the reviewed extent. | Two repository-defined cold pinned-VC7.1 `/O2 /Ob1 /Oy- /Gr` replays from tracked `src/ReplayInput.cpp` reproduce 299/299 bytes and all 22 reviewed relocations; the reports are byte-identical with SHA-256 `9e0c259957c54356f6013d534a5b6c6fc133fcff7f7d7ad61f20b1330763a9ca`. The relocation audit also proves all fourteen replay-input DIR32 fields are consistently expressed from reconstruction base `0x004ACE18`. Clean committed TH08 corroborates only the playback callback role/source family after TH09 fixed the behavior and layout. |
| REPLAY-005 | source-present / codegen-nearmatch / authored-game | `ReplayManagerView::LoadReplayData @ 0x004205E0-0x0042082E` is the TH09 replay-data decode/validation helper. Target code validates magic `0x50523954`, version 2 and header byte `+0x06`, deobfuscates bytes beginning at `+0x18` using key `+0x15` with increment 7, checks checksum `+0x10` from seed `0x3F000318`, allocates a decoded image with a 0xC0 wire header, calls `Lzss::Decode`, preserves trailing file bytes, calls the Supervisor version checker, rebases four ten-entry stage pointer tables at `+0x20/+0x48/+0x70/+0x98`, frees the encoded image and returns the decoded image. Header fields observed here include file size `+0x0C`, checksum `+0x10`, obfuscation key `+0x15`, compressed size `+0x18`, and decompressed size `+0x1C`. One `CC` byte at `0x0042082F` remains outside the extent. | Maintained pinned-VC7.1 `/O2 /Ob1 /Oy- /Gr` source emits the complete 591-byte extent, resolves all 14 relocation destinations, and matches 530/535 comparable bytes. The only residual bytes are two semantically commutative register/SIB lowering choices in allocation-size and trailing-copy pointer arithmetic. Reordering the C++ operands leaves the same bytes and semantic-local temporaries worsen the result, so no register/encoding-directed source is retained. Exactness remains false. |
| REPLAY-006 | source-present / codegen-nearmatch / authored-game | `ReplayManagerView::BeginPlaybackStage @ 0x00420840-0x00420A5B` is the playback-mode added callback installed on the same priority-6 Chain element as exact `PlaybackInputAndFps`. It loads the replay through exact `FileSystem::OpenFile` plus source-present `LoadReplayData` when needed, restores the selected stage's four start pointers, gameplay-side start fields, ReplayManager input/FPS cursors, the 0xCC configuration snapshot and RNG seed, then clears input delay. TH09 notably tests the computed address `replayData->frameStart0 + stage * 10` rather than dereferencing `frameStart0[stage]`; maintained source preserves that target behavior rather than substituting the more familiar adjacent-game null check. Four `CC` bytes at `0x00420A5C-0x00420A5F` remain outside the extent. | Natural pinned-VC7.1 `/O2 /Ob1 /Oy- /Gr` source reproduces target topology from entry through the config copy but emits 538 bytes versus target 540. `/G6` and `/GB` remain 538. `/G7` reaches 540 but broadly mismatches from the entry (57/412 comparable), so it is rejected. The retained two-byte deficit is the target's `xor EDX; mov DX,[frame+4]; push EDX` seed zero-extension versus stock VC7.1 `movzx EAX,[frame+4]; push EAX`. No encoding/register forcing is introduced and exactness remains false. |
| TOOLCHAIN-065 | unresolved-codegen / near-exact | The TH09 replay loader is a complete 591-byte natural-source near match under the same `/O2 /Ob1 /Oy- /Gr` family as the exact playback callback. After correcting the trailing-copy destination to read decompressed size from the original encoded header, equality improves from 459/535 to 530/535 comparable bytes with all 14 relocation destinations aligned. Four residual bytes are the compiler choosing the opposite register assignment for a commutative allocation-size expression, and one is scale-1 SIB base/index ordering in the trailing destination. Textual operand reversal does not alter those choices; introducing explicit semantic locals worsens equality to 526/535. | Packet-72 v1-v4 loader probes, complete target/object instruction alignment, and final tracked-source comparison. The retained source is v2-shaped; no volatile, register hint, inline assembly, padding, target-byte embedding, fake return, or custom compiler behavior is used. |
| TOOLCHAIN-066 | unresolved-codegen / size-nearmatch | The TH09 playback-stage added callback naturally converges to 538 bytes versus the 540-byte target under `/O2 /Ob1 /Oy- /Gr` after preserving the target's computed stage-table address check and target-visible frame1/frame0 load order. The function is aligned through the selected-stage restore and 0xCC config copy; the remaining net two-byte deficit is target `xor EDX; mov DX,[EAX+4]` versus stock `movzx EAX,[EAX+4]` before the RNG-seed call. `/G6` and `/GB` preserve 538 bytes; `/G7` happens to emit 540 but changes the function broadly and matches only 57/412 comparable bytes, so it is not a valid profile. | Packet-72 BeginPlaybackStage source/profile probes and target/object alignment. No forced zero-extension local, volatile, register hint, padding, assembly, target bytes, or arbitrary flag search is retained. |
| REPLAY-007 | exact / authored-game | `ReplayManagerView::Create @ 0x00421670-0x004217D8` is the central TH09 ReplayManager creation/registration lifecycle. The only target callers are `GameplaySetupThread @ 0x0041B0FD/0x0041B114`, which pass record mode as ECX=0/EDX=0 and playback mode as ECX=1/EDX=`0x004A7ED5`, then publish EAX to the ReplayManager slot at `0x004A7E64`. The routine allocates/registers a 0x164 object as `"ReplayInf"`, zeroes the full object, stores mode `+0x114` and playback path `+0x118`, then uses exact Chain helpers. Record mode stores its main element at `+0x150`, installs exact `RecordInputAndFps` with added callback `BeginRecordingStage` at priority 26, clears `+0x158`, stores the frame-sync element at `+0x15C`, adds `CaptureFrameSyncState` at priority 7, and invokes that callback once immediately. Playback mode installs exact `PlaybackInputAndFps` plus `BeginPlaybackStage` at priority 6, then installs the target-observed frame-advance control callback `0x00420470` at priority 27 in `+0x158` and clears `+0x15C`. A nonzero main-chain insertion result returns NULL without local cleanup; TH09 does not write the ChainElem deleted-callback field. | Two cold repository-defined pinned-VC7.1 EH-disabled `/O2 /Ob1 /Oy- /Gr` replays from tracked `src/ReplayInput.cpp` reproduce 361/361 bytes with all 28 reviewed relocations and byte-identical canonical reports, SHA-256 `369d8d47de0169e4ddbf1a54096f8e4eb30091f53d26aa9d84883745f6681bef`. Native IDA callers/xrefs/disassembly independently establish the ABI, 0x164 layout and lifecycle edges. Clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` corroborates only the broad create/register source family; its 0xDC manager and deleted-callback write conflict with TH09 and are not transferred. TH095 live dirty state was preserved and not consulted. |
| TOOLCHAIN-067 | compiler-observed / function-local | The exact natural `ReplayManagerView::Create` source is locally exception-lowering sensitive because it contains `new ReplayManagerView` whose empty constructor remains an external call at `0x004343D0`. Under the otherwise matching pinned VC7.1 `/MT /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy-` profile, enabling `/EHsc` emits a 435-byte Create COMDAT with constructor-unwind scaffolding; omitting `/EHsc` emits the exact 361-byte target extent and all 28 target relocations. The same tracked TU continues to reproduce the established exact ReplayInput/record/playback callbacks under their existing EH-enabled match units, while the established BeginRecordingStage near match also remains target-sized under EH-disabled lowering. | Focused same-source `/EHsc` versus EH-disabled probe, complete relocation-masked comparison, two identical repository-defined canonical Create replays, and connected-function regressions. This is function-local compiler evidence only; it does not establish project-wide exception settings or original translation-unit partition. |
| BOUNDARY-067 | exact | `ReplayManagerView::Create` owns `0x00421670-0x004217D8`, ending in a plain `ret`. Target bytes `0x004217D9-0x004217DF` are exactly seven unowned `CC` bytes. Independent candidate `sub_4217E0 @ 0x004217E0` begins next and is 0x15 bytes; `GameplaySetupThread` later calls it on the published ReplayManager with EDX=0/1, so it is not a tail of the creation routine. | Native IDA candidate extent, bounded target-byte read across the return/padding/next entry, independent lookup of `0x004217E0`, caller disassembly, and repeatable canonical 361-byte Create replay. |
| REPLAY-008 | exact / authored-game | `ReleaseReplayManagerCore @ 0x00420A60-0x00420AC7` and `ReplayManagerView::Release @ 0x00420AD0-0x00420C0D` close the central TH09 ReplayManager teardown seam. The shared 104-byte core cuts and clears manager `+0x158/+0x15C`, frees `replayData +0x08` and optional storage `+0x110`, and returns zero. The 318-byte Release wrapper is null-tolerant, walks all ten embedded/linked 0x18 stage-buffer chains and frees their four owned buffers, conditionally frees and clears three ten-entry frame-start tables plus the FPS table when `GameManager::IsReplayNeutral @ 0x00415D20` is false, invokes the shared core, cuts `mainChain +0x150`, and CRT-frees the manager. Gameplay setup, Supervisor shutdown, and the title/UI transition path all call the Release wrapper on the published ReplayManager at `0x004A7E64` and clear that global afterward. | Two cold repository-defined pinned-VC7.1 `/O2 /Ob1 /Oy- /Gr` replays reproduce the core 104/104 bytes with 8 reviewed relocations and Release 318/318 bytes with 20 reviewed relocations. Canonical report SHA-256 values are `a80ee4b691426259ed72c16015bae33c2404618cdefa6368a4bae7e5e74f2484` and `e020c2feb8344d7b8a74bda34c10729ac033b468fd366cdd6bc5141b70d60cbb`. Clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` corroborates only the broad DeleteReplayManager cleanup family; TH09 target evidence determines the additional stage-buffer/header cleanup and all addresses. |
| TOOLCHAIN-068 | compiler-observed / source-shape | The TH09 replay header's three contiguous ten-entry frame-start pointer tables are naturally represented as `frameStart[3][10]` at `+0x20/+0x48/+0x70`; changing only this maintained layout expression and using a three-stream nested cleanup loop changes the Release probe from 370 bytes to the exact 318-byte target and reproduces the target count-3 / stride-0x28 loop. A file-static one-argument `ReleaseReplayManagerCore(ReplayManagerView *)` compiled under the ordinary pinned VC7.1 `/O2 /Ob1 /Oy- /Gr` profile is naturally lowered with its sole internal argument in EAX; both Release and independent `0x00420C10` use `mov eax, manager; call 0x00420A60`, without source ABI annotations, register hints, volatile forcing, pragma tricks, or assembly. | Same-source v1/v2 probes, complete target disassembly, two repeatable repository-defined canonical replays, and fresh xrefs to both helper callers. This is compiler/source-shape evidence and does not establish the original helper name, C++ declaration, or translation-unit partition. |
| BOUNDARY-068 | exact | The shared cleanup core owns exactly `0x00420A60-0x00420AC7` and ends in `ret`; `0x00420AC8-0x00420ACF` are eight unowned `CC` bytes. `ReplayManagerView::Release` independently owns `0x00420AD0-0x00420C0D` and ends in `ret`; `0x00420C0E-0x00420C0F` are two unowned `CC` bytes before independent `sub_420C10 @ 0x00420C10`, a 0x2A-byte alternate wrapper that also calls the shared core. | Attested native IDA candidate extents, bounded target-byte reads across both returns/padding regions, helper caller xrefs, independent `0x00420C10` lookup/decompilation, and repeatable canonical 104-byte/318-byte replays. |
| REPLAY-009 | source-present / codegen-nearmatch / authored-game | `ReplayManagerView::SaveReplay @ 0x00420C60-0x00421135` is the TH09 replay serializer. Its two target call sites are both inside `sub_429662`: they pass the published ReplayManager at `0x004A7E64` in ECX, a generated replay path in EDX, and an eight-character replay name on the stack. The routine serializes three linked 16-bit input streams plus the FPS stream from the ten embedded ReplayBuffer roots, builds a twelve-byte `USER` metadata header and Japanese metadata text, copies target fields into a local 0x1EC replay image, uses target RNG `0x004048B0` for obfuscation bytes, calls LZSS encode `0x00434020`, computes the `0x3F000318` checksum, obfuscates header/payload bytes with a key incremented by seven, then writes the 0xC0 wire header, compressed payload, USER header, and metadata text through CreateFileA/WriteFile. The target notably pre-scans the stack-local replay copy's frame-start pointer table before copying manager `replayData` into that local; the maintained source preserves this apparent original uninitialized read rather than silently repairing it. `AppendReplayFormat @ 0x004200C0-0x004200EC`, used eight times by this routine, is independently canonical exact. | Fresh native IDA callers/disassembly and target strings/tables establish the behavior and ABI shape. Maintained natural source under pinned VC7.1 `/O2 /Ob1 /Oy- /Gr` emits the complete 1,238-byte SaveReplay extent with 53 COFF relocation records and 793/1026 comparable non-relocation bytes, so SaveReplay remains non-exact. Two cold repository-defined replays of `AppendReplayFormat` reproduce 45/45 bytes with the sole relocation to `vsprintf @ 0x0047C335`; report SHA-256 `ba0e57f0ba9f9c688b2f4d4d0e110c64257e36e590ca6687aa650c57c9056108`. Clean committed TH08 HEAD `a45e99fb1942714e6edded20847e32a654d56f97` corroborates only the broad replay-save/USER/LZSS/checksum/obfuscation family; TH09 determines every local layout, string, address, and exactness state. |
| TOOLCHAIN-069 | compiler-observed / source-shape | Packet 75 progressively recovers the stock VC7.1 source shape without register-directed code. A first natural SaveReplay source emitted 1,230 bytes and used a 0x61C stack frame because it introduced a separate WriteFile byte-count local. Reusing the target-evidenced `replayName` parameter slot as `(LPDWORD)&replayName`—the same natural pattern already exact in TH09 `Supervisor::TakeSnapshot`—restores the target 0x618 stack frame and local replay-copy base. Expressing the compressed-payload checksum as the target-observed indexed loop `checksum += compressedData[i]` changes the same source from 1,230 to the exact 1,238-byte target extent. Moving the natural USER-header initialization before the metadata-buffer memset lets VC7.1 independently schedule the target-style immediate byte store and improves the complete comparison from 703/1026 to 793/1026 comparable bytes. A natural early-link-lifetime variant expands to 1,246 bytes and is rejected. The file-static varargs formatting helper naturally reproduces all 45 target bytes. | Focused same-profile VC7.1 v1-v5 probes, complete 1,238-byte target disassembly, tracked-TU rebuilds, and canonical helper replay. No `/G*` search, volatile/register hints, `var_order`, padding, inline assembly, target-byte embedding, fake return, or encoding-specific source was retained. Remaining SaveReplay divergence is compiler scheduling/register lifetime in the stream/metadata loops, so no canonical exact promotion is made. |
| BOUNDARY-069 | reviewed | `AppendReplayFormat` owns exactly `0x004200C0-0x004200EC`, ending in `ret`; target bytes `0x004200ED-0x004200EF` are three unowned `CC` bytes before exact `ReplayInputState::Update @ 0x004200F0`. `ReplayManagerView::SaveReplay` independently owns exactly `0x00420C60-0x00421135`, ending in `ret 4`; `0x00421136-0x0042113F` are ten unowned `CC` bytes before independent `ReplayManager_BeginRecordingStage @ 0x00421140`. | Native IDA candidate extents, fresh target-byte reads across both boundaries, all eight helper xrefs confined to SaveReplay, two SaveReplay callers in `sub_429662`, canonical 45-byte helper replay, and complete target-sized 1,238-byte SaveReplay source comparison. |
| RESULT-001 | source-present / codegen-nearmatch / authored-game | `TitleScreenView::UpdateReplaySave @ 0x00429662-0x00429D82` is the TH09 TitleScreen replay-save/UI state handler reached only from screen-mode dispatcher `sub_42A9CB` case `0xF`. Its states 0..4 initialize the replay UI, optionally write an automatic `th9_udn###.rpy`, load 25 `th9_##.rpy` slots, choose empty versus occupied replay slots, confirm overwrites, and edit an eight-character replay name on a 16x6 keyboard before calling `ReplayManager_SaveReplay`. The maintained target-bound TitleScreen view captures the observed state fields, 25 path slots, replay-record storage, UI handles, timers, and transition fields without claiming the original complete class layout. | Fresh native IDA caller/callee/decompilation/disassembly evidence, target strings and field offsets, and two cold pinned-VC7.1 source replays. Maintained natural source reproducibly emits the complete 1,825-byte extent with 100 COFF relocations and matches 1066/1425 comparable bytes; the function therefore remains non-exact. Clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` and committed TH095 `e32c7596004caa648fc405720e345daf42cecef3` corroborate only the broad title/result replay-selection/name-entry source family; later TH09 lifecycle evidence proves the physical owner is TitleScreen and determines all local facts. |
| TOOLCHAIN-070 | compiler-observed / function-local | The hard TitleScreen replay handler provides strong packet-local evidence for a different optimization/intrinsic profile than the ReplayManager TU. The same natural first source under `/O2 /Oi` emits 2,128 bytes and a switch jump table, while `/O1 /Oi` emits 1,861 bytes and restores the target decrement-chain state dispatch but inlines `strcpy`/`memset` that the target calls through the CRT. `/O1 /Ob1 /Oy-` without `/Oi` restores the target 0x154 stack frame and CRT calls. Replacing explicit 0x1EC `memcpy` with natural `ReplayDataView` assignment independently yields the target `rep movsd`; target-shaped state control flow and direct `selectedReplay = keyboardSelection` then converge to the exact 1,825-byte extent. | Focused VC7.1 v1-v6 probes and two cold identical maintained-source builds. Final function-code SHA-256 is `a4c60a7c08cdf9969fbbbf4b08239debcfd927b79a7eb12367505b73da809565`; relocation-masked report SHA-256 is `1e18e343732a22c515ca241256ede6ab410fa4b78ce4a97fe7a8c80c99cb149a`. This is function-local evidence only and does not establish project-wide flags, original TU partition, or equality. |
| BOUNDARY-070 | reviewed | `TitleScreenView::UpdateReplaySave` owns exactly `0x00429662-0x00429D82`, a 0x721-byte extent ending in a plain `ret`. Independent candidate `0x00429D83` begins immediately at the next byte; there is no inter-function `CC` padding to assign. | Native IDA function extent, bounded target-byte read across the return/next entry, unique dispatcher caller, and repeatable target-sized 1,825-byte maintained-source comparison. |

| TITLE-001 | owner-corrected / observed-lifecycle | TH09 target lifecycle proves the 0x1B388 front-end object containing `0x00429662` and `0x00429E23` is the TitleScreen owner, correcting packet 76's provisional ResultScreen label. `sub_42AB13` allocates/zeros 0x1B388 bytes, publishes the object at `0x004AC8C0`, installs `sub_42A9CB` as calc callback plus `sub_424898` as draw callback, and registers added/deleted lifecycle callbacks. The added path initializes `title/title00.png`; the calc dispatcher routes screen mode `0xF` to the replay-flow routine and mode `1` to the start-menu routine. | Fresh attested TH09 xrefs/decompilation for `0x0042AB13`, `0x00424BF7`, `0x0042A9CB`, title resource references, and shared 0x1B388 field geometry. Clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` corroborates only the TitleScreen lifecycle/source family; TH09 proves owner/layout. |
| TITLE-002 | source-present / codegen-nearmatch / authored-game | `TitleScreenView::OnUpdateStartMenu @ 0x00429E23-0x0042A43A` is the main TH09 start-menu state machine on the same TitleScreen object. It handles Init/Ready/Exit/Changing states, allocates 223 0x2A4 VMs, loads `title/title00.png`, releases stale ReplayManager state, updates eight menu choices/help VMs, launches idle demo replays after 1500 frames, enters Options, and copies the 0xCC configuration snapshot on the changing state. | Natural tracked-source family under pinned VC7.1 `/O1 /Ob1 /Oy-` without `/Oi` reproduces the complete 1,592-byte physical owned extent: 1,560 bytes of code plus the immediately following 32-byte eight-entry jump table. It carries 107 COFF relocations and matches 1131/1164 comparable bytes, so exactness remains false. Two cold tracked-source reports are byte-identical with SHA-256 `1b4f6fdb9eecc54a4e8b4296d716c025495ab5997871635ce8e2b0deb6e3c712`; the complete COFF-owned bytes have SHA-256 `73f65253047fb8636eb7ea0712145ac7183d04e2e43ccf3418f6280e9e59ceaf`. All eight object table labels correspond to the target switch cases; the retained mismatch is the case-1 basic-block ordering around its locked/unlocked branch. |
| TOOLCHAIN-071 | compiler-observed / function-local | The TitleScreen start-menu packet independently confirms the packet-76 `/O1` / no-`/Oi` family. Natural `/EHsc` source emits the target EH prologue, `operator new(0x24CDC)` plus VC7 vector construction for 223 0x2A4 VMs, CRT `free`, and a trailing compiler switch table. Target-backed source refinements remove a redundant ReplayManager null guard, remove a redundant MIDI-output null guard, use the 16-bit current-input word directly, preserve the two-step demo-index `++` then `%=` update, and use direct `vms[cursor+1]` expressions. These changes converge from 1,598 bytes to the exact 1,592-byte physical owner extent and improve structural equality to 1131/1164 comparable bytes. | Focused same-profile v1-v8 probes. A shared `nextScreen` local and an explicit shared-tail label both shrink the function below target and are rejected. No `/G*` search, register hints, volatile forcing, padding, assembly, target-byte embedding, fake return, or encoding-specific source is retained. |
| BOUNDARY-071 | reviewed / table-owned | `TitleScreenView::OnUpdateStartMenu` has code extent `0x00429E23-0x0042A43A` (0x618 bytes) ending in `ret`. The immediately adjacent `0x0042A43B-0x0042A45A` region is an eight-entry compiler jump table for the routine's menu switch, not padding and not an independent authored function. The next independent candidate begins at `0x0042A45B`. The maintained VC7.1 COMDAT naturally owns code plus table as one 0x638-byte physical extent. | Native IDA function extent, bounded target-byte read through the return/table/next entry, eight table values resolving to menu case entries, and VC7.1 COFF function/table relocations. |

| TITLE-003 | source-present / codegen-nearmatch / authored-game | `TitleScreenView::OnUpdateDifficultySelect @ 0x0042A45B-0x0042A7D0` is the TH09 difficulty-selection state machine shared by calc-dispatcher screen values 2, 4, and 7. Init loads `title/select00.png`, configures the four-item difficulty VM region at `vms[136..]`, restores the selected difficulty byte, and handles screen-7 resume into screen 8. Ready moves a four-choice cursor except for the fixed screen-4 path, records the selected difficulty, and routes screen 2/4 into the character-selection family or screen 7 into the mode-dependent screen-8 family. Changing returns to start-menu screen 1 after 20 frames. | Fresh target-attested TH09 callers, complete disassembly/decompilation, shared TitleScreen field geometry, and two byte-identical pinned-VC7.1 tracked-source builds. Clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` corroborates the `TitleScreen::OnUpdateDifficultySelect` Init/Ready/Changing and `title/select00.png` family only; TH09 proves all local screen values, fields, transitions, extent, origin, and non-exact status. |
| TITLE-004 | exact / authored-game | `TitleScreenView::OnUpdateModeSelect @ 0x0042A7D1-0x0042A9CA` is the TH09 screen-6 five-choice mode-selection state machine. Init optionally reloads `title/select00.png` unless returning directly from screen 7, starts script interrupt 11, resumes immediately into DifficultySelect when `resumeState` is nonzero, otherwise restores `g_TitleModeSelection @ 0x004A7EA4` and configures the five-item VM range beginning at 131. Ready moves a five-choice cursor, snapshots input once, stores the selected mode, and enters screen 7 plus `OnUpdateDifficultySelect`. Mode 4 calls canonical-exact `PrepareTitleMode4Network @ 0x004324C0` and canonical-exact `ResetTitleMode4Supervisor @ 0x0042EC30`, sets difficulty byte `0x004B3538` to 1, and writes 0/1 to the `+0x20` fields of the two 0x38 GameManager side records at `0x004A7DB0/0x004A7DE8`. Changing returns to start-menu screen 1 after 20 frames with cursor 2. | Fresh target-attested dispatcher/callee/disassembly evidence, existing GameManager side layout, current helper match units, and two cold 506/506 TitleScreen replays with 32 relocations. Original TU and physical data owners remain open. |
| TOOLCHAIN-072 | compiler-observed / persistent-nonexact | The natural TH09 difficulty-select source under the established TitleScreen `/O1 /Ob1 /Oy- /Gr` family emits 884 bytes with 45 COFF relocations versus the 886-byte target. Two cold tracked-source builds are byte-identical with function SHA-256 `81dc4da0bb4a1229bbc6e132a910f3a8327bf1bd6f2a39f881bfdfd8a81fe613`. `/O2 /Ob1 /Oy-` expands the same source to 996 bytes with 51 relocations, while adding `/Oi` to `/O1` leaves the 884-byte body byte-identical. Focused source-shape probes corrected the Ready branch direction, mode-4 semantics, signed cursor comparison, and target-backed mode global role; the remaining physical mismatch is concentrated in the two mode 0/1/2/3/4 dispatch regions where the maintained grouped cases compile to range-style tests while the target uses decrement chains. The two target mode switches are intentionally not identical: resume-state mode 4 performs no screen-8 helper call, while Ready-confirm mode 4 shares the mode-0 helper. | Focused packet-78 v1-v5 compiler probes, target/object instruction alignment, and repeatable final tracked-source builds. This is function-local evidence only; no `var_order`, register forcing, padding, assembly, target bytes, fake return, or arbitrary flag search is retained. |
| BOUNDARY-072 | reviewed | `TitleScreenView::OnUpdateDifficultySelect` owns exactly `0x0042A45B-0x0042A7D0` (0x376 bytes), ending in a plain `ret`. Independent candidate `0x0042A7D1` begins at the immediately following byte; there is no inter-function `CC` padding and no adjacent compiler jump table owned by the primary function. | Native IDA extent/full disassembly, bounded target-byte read across `0x0042A7D0/0x0042A7D1`, dispatcher/caller linkage, and repeatable maintained-source feedback. |
| BOUNDARY-073 | exact | `TitleScreenView::OnUpdateModeSelect` owns exactly `0x0042A7D1-0x0042A9CA` (0x1FA / 506 bytes), ending in a plain `ret`. Independent TitleScreen calc dispatcher `sub_42A9CB @ 0x0042A9CB` begins at the immediately following byte; there is no inter-function `CC` padding and no compiler jump table associated with the exact function. | Native IDA function extent/full disassembly, bounded target-byte read across the return/next entry, sole dispatcher caller, and two repeatable canonical 506-byte replays. |
| SCOREDATA-001 | mixed exact / source-present / authored-game | The TH09 `score.dat` loader cohort uses one decompressed chapter buffer whose header-size and total-size fields are at `+0x08/+0x0C`. `ScoreFile_LoadScoreRecords @ 0x004218E0-0x00421A98` initializes and imports version-2 `HSCR` records into a flat `16 x 5 x 5` table of 0x2C-byte records at `0x004A8380-0x004AC83F`, then resets the current 0x2C record at `0x004AC858`. `ScoreFile_LoadLastName @ 0x00421AA0-0x00421B76` initializes an eight-space 0x18-byte `LSNM` version-1 record and replaces it from a matching chapter. `ScoreFile_LoadPlayStats @ 0x00421B80-0x00421C31` initializes a 0x1FC-byte `PLST` version-3 record at `0x004A8180`, including the observed BGM flag array at `+0x2C` and two 16-byte character-availability arrays at `+0x4C/+0x5C`, then copies a matching chapter. Original type/member spelling, TU partition, and data-definition ownership remain unresolved. | Native IDA under passed `target:th09-main` / `factory-native-stdio` attestation, the common `sub_421EA0` caller, complete target disassembly/decompilation, table-span arithmetic, and tracked source. Clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` supplies only the chapter-walk `ParseLSNM`/`ParsePLST` source family; committed TH095 `2c1acc0402ddd793f77dd405718202bbe38e2477` uses a different score-record format and is only broad parser-family corroboration. |
| TOOLCHAIN-074 | mixed exact / unresolved-codegen | Under pinned VC7.1 `/O2 /Ob1 /Oy- /Gr`, the natural maintained `ScoreFileView::LoadPlayStats` source reproduces 178/178 target bytes with all 18 DIR32 relocations to `0x004A8180`; two fresh cold canonical replays are byte-identical with report SHA-256 `2b809ef4d0c9da3f595b5766192bae639c9114b7f789d5d516d0e31409139f0e`. The same tracked source reproducibly emits `LoadScoreRecords` as 437 bytes versus the 441-byte target and `LoadLastName` as 212 bytes versus 215. The primary four-byte residual is confined to default-table register/zero scheduling, while the three-byte LSNM residual remains around default-record scheduling. Prior natural v1-v7/O1 and LSNM v1-v4/memcpy probes did not close those gaps, so no pragma, register hint, volatile forcing, padding, assembly, or target-byte trick is retained. | Two current-turn cold rebuilds/replays, prior recoverable packet-83 probes, complete target/object alignment, and exact regressions for `ScoreRecord_InsertIntoTable` 216/216, `TitleScreenView::OnUpdateResultNameEntry` 966/966, and `TitleScreenView::OnUpdateModeSelect` 506/506. |
| BOUNDARY-074 | reviewed | `ScoreFile_LoadScoreRecords` owns `0x004218E0-0x00421A98`, followed by seven unowned `CC` bytes at `0x00421A99-0x00421A9F`. `ScoreFile_LoadLastName` independently owns `0x00421AA0-0x00421B76`, followed by nine unowned `CC` bytes at `0x00421B77-0x00421B7F`. `ScoreFile_LoadPlayStats` owns `0x00421B80-0x00421C31`, followed by fourteen unowned `CC` bytes at `0x00421C32-0x00421C3F` before independent `0x00421C40`. Padding ownership remains unassigned. | Fresh native IDA function extents plus attested bounded target-byte reads of all three padding regions. |
| SCOREDATA-002 | source-present / codegen-nearmatch / authored-game | `ScoreFileView::OpenScore @ 0x00421C80-0x00421E8A` is the upstream TH09 `score.dat` acquisition/decode/decompress gate. Under `/Gr` its filename is carried in ECX; the sole caller is `sub_421EA0 + 0x06`. It opens an external score file, requires at least a 0x18-byte header, applies `FileSystem::Decrypt` with `3A/CD/0x100/0xC00`, performs the score-specific rolling rotate/XOR decode and 16-bit checksum, requires header size 0x18 and version 4, allocates 0xA0018 bytes, copies the six-dword header, LZSS-decompresses the payload, walks variable-size chapters, rejects zero chapter sizes, and remembers a `TH9K` control chapter whose version must be 1; the writer independently places that control chapter first in the decompressed chapter stream, so the loader does not require a terminal position. Any failure recreates a minimal 0x18 buffer with header size/total size 0x18 and version 4. The short-file path preserves the target/TH08-family double-free control flow rather than silently repairing it. | Fresh attested TH09 extent/disassembly/decompilation/caller/callee evidence and target strings/constants; exact helper ABIs for `FileSystem::OpenFile`, `FileSystem::Decrypt`, `ZunMemory::Alloc/Free`, and `GameErrorContext::Log`; codegen-exact LZSS decode dependency. Clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` supplied the direct `ScoreDat::OpenScore` lifecycle source family; committed TH095 `2c1acc0402ddd793f77dd405718202bbe38e2477` was used only as a differing score-parser-family contrast. |
| TOOLCHAIN-075 | compiler-observed / persistent-nonexact | The natural TH08-derived, TH09-corrected `ScoreFileView::OpenScore` source under pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Gr /O2 /Ob1 /Oy- /I src` emits exactly the target 523-byte extent. From entry through the LZSS/free sequence at relative `+0x15D`, instruction shape and relocation destinations align; later chapter-scan lowering differs in register allocation. A compact natural expression `bytesToRead = totalSize - headerSize` closes the 524-byte first draft to 523 bytes, but the complete relocation-masked diagnostic remains 277/395 comparable bytes because scan-register choices also shift downstream control-flow/relocation offsets. Explicit semantic `headerSize` locals, declaration-order changes, and several statement-order variants do not restore the target register coloring. Probing stops here: no `var_order`, register hints, volatile forcing, padding, assembly, target-byte embedding, or arbitrary flag search is retained. | Packet-84 v1-v7 bounded VC7.1 probes plus target/object disassembly alignment. Existing `LoadPlayStats` independently remains 178/178 canonical exact and the two packet-83 near-matches remain 437/441 and 212/215 after the shared header refinement. |
| BOUNDARY-075 | reviewed | `ScoreFileView::OpenScore` owns exactly `0x00421C80-0x00421E8A` (0x20B / 523 bytes) and ends in a plain `ret`. Its left neighbors are independent `0x00421C40` (0x18 bytes) and `0x00421C60` (0x12 bytes), with eight and fourteen `CC` bytes respectively before subsequent starts. Five unowned `CC` bytes at `0x00421E8B-0x00421E8F` separate OpenScore from independent `0x00421E90`. | Fresh native IDA function extents plus attested bounded target-byte reads on both sides. |
| SCOREDATA-003 | source-present / codegen-nearmatch / authored-game | `SaveTitleScoreData @ 0x00421F10-0x004221F8` is the TH09 score-file writer paired with `ScoreFileView::OpenScore`. It returns 1 only when the published score header version at `0x004AC844` is zero. Otherwise it allocates 0x640000 bytes as `"scoretmp"`, copies the six-dword header, writes a 0x0C `TH9K` version-1 control chapter, and repeatedly chooses one of three record classes through the shared RNG at `0x004ACE0C`: the 0x44C0 HSCR table, the 0x18 LSNM record, or the 0x1FC PLST record after the target play-time updater. It then appends a 0x1C `VRSM` chapter containing `"0150a"` plus Supervisor executable size/checksum, LZSS-encodes the decompressed payload, writes random header bytes at +1/+6, computes the 16-bit checksum from header version +4 through the compressed file end, applies the inverse rolling transform, encrypts with `3A/CD/0x100/0xC00`, writes `score.dat`, frees both allocations, and returns 0. | Fresh target-attested full disassembly/decompilation, callers/callees, boundary bytes, exact FileSystem/ZunMemory dependencies, target-backed RNG/Supervisor data, and clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` `ResultScreen::WriteScore` source family. Committed TH095 `2c1acc0402ddd793f77dd405718202bbe38e2477` is parser/writer-family contrast only; its live dirty state is excluded. |
| TOOLCHAIN-076 | compiler-observed / one-byte-nearmatch | The direct TH08 `currentOffset=0; COPY(...)` writer shape is codegen-significant for TH09. A first explicit-output-cursor translation was only 374/585 comparable bytes. Restoring the committed TH08 offset-based copy family raises the same natural source to 567/585; preserving TH08's `chapterSizeCopy`-before-`chapterSize` assignment order for both TH9K and VRSM raises it to 584/585. Two fresh tracked-source pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Gr /O2 /Ob1 /Oy- /I src` builds are byte-identical, emit the complete 745-byte target extent, and expose exactly 40 COFF relocations whose target destinations all agree. The sole byte residual at relative +0x1C2 is an equivalent SIB encoding for `scoreData + currentOffset`: object `lea edi,[eax+ebx]`, target `lea edi,[ebx+eax]`. `&scoreData[currentOffset]` produces the same byte, so probing stops without register forcing, volatile, `var_order`, assembly, padding, or an encoding-specific workaround. | Packet-85 bounded v1-v6 probes, complete target/object instruction alignment, and fresh connected exact regressions. |
| BOUNDARY-076 | reviewed | `SaveTitleScoreData` owns exactly `0x00421F10-0x004221F8` (0x2E9 / 745 bytes), ending in a plain `ret`. Seven `CC` bytes at `0x004221F9-0x004221FF` remain unowned before independent `sub_422200 @ 0x00422200`. Its two target callers are exact `TitleScreen_OnUpdateResultNameEntry` and exact `Supervisor_DeletedCallback`; correcting the latter's previously provisional subsystem-A relocation name to `SaveTitleScoreData` preserves its 405/405 canonical replay. | Native IDA extent/callers, bounded tail-byte read, two repeatable 745-byte source probes, and fresh exact caller replay. |
| SUPERVISOR-035 | exact / authored-game | The TH09 Supervisor timekeeping pair is reconstructed canonically exact as `ScoreSupervisorView::UpdatePlayTime @ 0x0042FF70-0x00430058` and `ScoreSupervisorView::UpdateGameTime @ 0x00430060-0x0043014C`. Both sample `timeGetTime`; play time uses Supervisor `+0x798`, while game time uses `+0x79C`. A backwards tick resets the play-time baseline to the current tick but resets the game-time baseline to zero. Both decompose elapsed milliseconds into hours/minutes/seconds/milliseconds and normalize 1000/60/60 carries into the persisted PLST record. The local `ScoreSupervisorView` name is a reconstruction view, not a claim about original class spelling or TU ownership. | Fresh target-attested boundaries/callers/disassembly/decompilation, sibling control-flow comparison, clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` source-family migration, and two byte-identical cold canonical replays per function. |
| SCOREDATA-004 | exact-data-layout / authored-game | The previously opaque PLST bytes `g_PlayStatsRecord +0x0C..+0x2B` are eight target-backed `u32` time counters: total hours/minutes/seconds/milliseconds at `+0x0C/+0x10/+0x14/+0x18`, followed by game hours/minutes/seconds/milliseconds at `+0x1C/+0x20/+0x24/+0x28`. The existing BGM unlock array remains at `+0x2C`. Target xrefs to `+0x0C` are confined to the exact play-time updater and xrefs to `+0x1C` to the exact game-time updater; exact `ScoreFileView::LoadPlayStats` continues to copy the complete 0x1FC record. | Exact timekeeping relocation manifests, target data xrefs, exact LoadPlayStats regression, and maintained layout assertions in `src/ScoreData.cpp`. |
| TOOLCHAIN-077 | exact / function-local | Natural migrated timekeeping source reaches the target under pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Gr /O2 /Ob1 /Oy- /I src`. Adding `/Oi` leaves `UpdatePlayTime` byte-identical; adding the GameManager-local `/Os` preference shrinks the same source to 187 bytes versus the 233-byte target and is rejected for this cohort. `UpdatePlayTime` cold reports are byte-identical with SHA-256 `d2a980b427303cce28da05e554c6b24bd114b48cdded052a87deab455f241d7b`; `UpdateGameTime` reports are byte-identical with SHA-256 `603537fb25c2336af0c3a0a6cbbb5b782e5a3f7396d679f4c0912b0e6304dfc0`. TH08's committed source supplied the overall algorithm but its seconds-to-minutes carry expression references the millisecond field; TH09 target instructions unambiguously divide/modulo the seconds accumulator, so that conflicting adjacent expression is not transferred. No TH08 `#pragma var_order` is retained. | Bounded packet-86 profile probes, complete target/object comparison, 15 reviewed relocations per function, and adjacent-source conflict review. |
| BOUNDARY-077 | exact | `ScoreSupervisorView::UpdatePlayTime` owns exactly `0x0042FF70-0x00430058` (233 bytes), ending in `ret`; seven unowned `CC` bytes at `0x00430059-0x0043005F` precede independent `UpdateGameTime @ 0x00430060`. `UpdateGameTime` owns exactly `0x00430060-0x0043014C` (237 bytes), followed by three unowned `CC` bytes at `0x0043014D-0x0043014F` before independent `0x00430150`. | Native IDA extents, bounded target-byte reads across both boundaries, and repeatable canonical exact object replays. |
| SUPERVISOR-036 | exact / authored-game | `SupervisorVersionView::CheckVersion @ 0x00430150-0x00430216` is the TH09 version compatibility parser. It reads version-data size/pointer from Supervisor `+0x7A8/+0x7AC`; a missing buffer or caller version beginning with `debug` succeeds immediately. Otherwise it scans newline-delimited entries, compares the first five version characters, parses decimal executable size/checksum with `"%d %d"`, and returns 0 only for a matching pair; exhausted/malformed input returns -1. Replay loading passes replay header version at `+0x1DC` with executable fields `+0x1D4/+0x1D8`; `Supervisor_OnUpdate` periodically passes current `"0150a"` with Supervisor `+0x7A4/+0x7A0`. The version buffer is loaded by `sub_42F100` from `th09_0150a.ver` and released/cleared by exact `Supervisor_DeletedCallback`. | Fresh target-attested full disassembly/decompilation, callers/callees, field xrefs/lifecycle, direct TH08 source-family migration, and two byte-identical 199/199 cold canonical replays with eight reviewed relocations. |
| TOOLCHAIN-078 | exact / source-lifetime-sensitive | Natural TH08-derived CheckVersion source under pinned VC7.1 `/O2 /Ob1 /Oy- /Gr` emits the target 199-byte extent and all eight target relocations. A first TH09-corrected form kept `strchr` in a separate `newline` local and matched only 157/167 comparable bytes; restoring TH08's direct `versionData = strchr(versionData, '\\n')` lifetime while retaining TH09's target-proven NULL check makes VC7.1 place the return value directly in EBX and closes all 167 comparable bytes. Adding `/Oi` to the first form is byte-identical and does not explain the residual. The two canonical reports are byte-identical with SHA-256 `e13c6f3b1018aff7a6651cf4c8151b35302f99d86a3c9bbcd4c519abbc739404`. No `var_order`, volatile/register forcing, assembly, padding, or target-byte trick is retained. | Packet-87 bounded source-shape probes, target/object tail alignment, and canonical relocation-aware replay. |
| BOUNDARY-078 | exact | `SupervisorVersionView::CheckVersion` owns exactly `0x00430150-0x00430216` (0xC7 / 199 bytes) and returns with `ret 0x0C`. Nine unowned `CC` bytes at `0x00430217-0x0043021F` precede independent `sub_430220 @ 0x00430220`. | Native IDA extent/full disassembly, bounded target-byte read across the tail, and repeatable canonical exact replay. |
| SUPERVISOR-037 | exact / authored-game | `SupervisorAddedLoadDat @ 0x0042F100-0x0042F190` is the version-data producer consumed by exact `SupervisorVersionView::CheckVersion`. Its sole target caller is exact `Supervisor::AddedCallback`. It requires the target archive loader at `0x004335E0` to accept `"th09.dat"`, formats `"th09_%.4x%c.ver"` with `0x150` / `a`, loads `th09_0150a.ver` through exact `FileSystem::OpenFile`, publishes the returned pointer at Supervisor `+0x7AC` and size at `+0x7A8`, and returns -1 through exact `GameErrorContext::Fatal` on archive or version-file failure. Error-message arrays at `0x004906C8` and `0x004906F0` are maintained as target-facing extern views because their complete source text and data-definition owner remain unresolved. | Fresh target-attested disassembly/decompilation/caller/callee evidence, field lifecycle from exact CheckVersion/DeletedCallback, exact FileSystem/Fatal dependencies, clean committed TH08 LoadDat family, and two byte-identical 145/145 canonical replays with 14 relocations. |
| TOOLCHAIN-079 | exact / ABI-sensitive | The direct TH08 LoadDat source family under pinned VC7.1 `/O2 /Ob1 /Oy- /Gr` (with `/Oi` for the canonical unit) is target-sized at 145 bytes on the first natural TH09 translation. Its only non-relocation mismatch is `test eax,eax` versus target `test al,al` after the archive-load call. Clean committed TH08 declares `PbgArchive::Load(LPCSTR)` as `bool`, and TH09 independently consumes only AL; changing the provisional local declaration from `int` to `bool` closes the final byte without any source-shape forcing. `/Oi` does not otherwise alter the first probe. Canonical report SHA-256 is `3ad99f049f9647170c5d9de6c2ecf2b06bfeb48f87eac0c6508e765d3aa4aa21`. | Packet-88 bounded ABI/profile probe plus complete target/object comparison and 14 target-bound relocations. |
| BOUNDARY-079 | exact | `SupervisorAddedLoadDat` owns exactly `0x0042F100-0x0042F190` (0x91 / 145 bytes), ending in a plain `ret`. Fifteen unowned `CC` bytes occupy `0x0042F191-0x0042F19F` before independent `SupervisorCheckFps @ 0x0042F1A0`. | Native IDA extent/full disassembly, bounded tail-byte read, sole exact caller, and repeatable canonical replay. |
| PBG-007 | exact / authored-game | `PbgArchive::Load @ 0x004335E0-0x00433692` is the TH09 archive lifecycle entry used by exact `SupervisorAddedLoadDat`. It first releases prior archive state, allocates a 0x0C-byte backend object, invokes its constructor at `0x00433940`, stores it at archive `+0x0C`, calls exact `PbgArchive::ParseHeader`, then copies the archive filename through `0x004330A0` into `+0x08`. A non-NULL copied filename returns true; every other path releases the archive and returns false. The target contains the natural VC7.1 EH setup/cleanup for backend construction; maintained source relies on ordinary `new` rather than explicit exception scaffolding. | Fresh target-attested full disassembly/decompilation/call graph, exact `SupervisorAddedLoadDat` caller, clean committed TH08 `PbgArchive::Load` source family, TH095 committed lifecycle contrast, and two byte-identical 179/179 canonical replays with 12 reviewed relocations including EH/except-list fields. |
| PBG-008 | exact / authored-game | `PbgArchive::ParseHeader @ 0x004333F0-0x004335C2` parses the TH09 PBGZ archive header and compressed file table. It requires a backend at `+0x0C`, opens with `g_PbgFileOpenModes[0]`, reads magic `0x5A474250` and a 12-byte encrypted header, decrypts that header with `1B/37/0x0C/0x400`, and derives `entryCount = encodedEntryCount - 123456`, `fileTableOffset = encodedFileTableOffset - 345678`, and `fileTableDecompressedSize = encodedFileTableDecompressedSize - 567891`. It reads the remainder from `fileTableOffset`, decrypts it with `3E/9B/0x80/0x400`, decodes through `Lzss::Decode`, and builds the entry table through `0x004332B0`. Failure frees all currently owned buffers, invokes the backend deleting destructor at vtable `+0x1C`, clears archive `+0x0C`, and passes target data `0x00490E90` plus the filename to the `0x0043D2B0` debug/error seam. | Fresh target-attested full disassembly/decompilation, exact FileSystem/ZunMemory dependencies, target virtual-call/dataflow evidence, clean committed TH08 ParseHeader source family, TH095 format contrast, and two byte-identical 467/467 canonical replays with 19 reviewed relocations. |
| PBG-009 | exact-layout / ABI | The maintained TH09 `PbgArchive` now has a target-backed 0x10-byte object layout: entry-array pointer `+0`, signed entry count `+4`, owned archive filename `+8`, and backend pointer `+0x0C`. Existing exact FindEntry/GetEntryDecompressedSize/ReadDecompressEntry independently consume the same prefix. Packet 89 extends the backend ABI to Open `+0`, Read `+8`, GetSize `+0x14`, Seek `+0x18`, and deleting destructor `+0x1C`; Close/Write/Tell occupy the intervening virtual slots in the reconstructed interface because the TH08 family and target vtable ordering agree, but their individual implementations remain independently unresolved. `PbgFileView` is a reconstruction view, not a claim of original class spelling or TU ownership. | Exact five-function integrated PbgArchive compiler replay, TH09 virtual calls/object offsets, exact query/read functions, and committed TH08 PbgFile declaration. |
| TOOLCHAIN-080 | exact / cohort | Natural TH08-derived Load/ParseHeader source, changed only where TH09 target evidence requires different observable behavior, reaches both targets under the already-proven Pbg profile `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src`. `PbgArchive::Load` replays 179/179 with 12 relocations and canonical-report SHA-256 `083004a6a967ed3aeecc04166d9ed839f2a6bd4ed1d9e0447145f5e00f42a878`; `PbgArchive::ParseHeader` replays 467/467 with 19 relocations and SHA-256 `6f96d5b9f38fb229afef44c9b767605770a4c6affac7f48a995b18ac67ecc8dc`. Rebuilding exact FindEntry, GetEntryDecompressedSize, and ReadDecompressEntry against the shared 0x10-byte header/backend interface leaves all three exact. No TH08 `var_order`, target-byte embedding, register forcing, assembly, or inert padding is retained. | Packet-89 standalone and integrated natural probes, actual tracked-source comparison, two cross-entry cold canonical passes, and three connected exact regressions. |
| BOUNDARY-080 | exact | `PbgArchive::ParseHeader` owns exactly `0x004333F0-0x004335C2` (467 bytes), ending in `ret 4`; thirteen `CC` bytes at `0x004335C3-0x004335CF` remain unowned before an independent 5-byte thunk at `0x004335D0-0x004335D4`, followed by eleven more unowned `CC` bytes through `0x004335DF`. `PbgArchive::Load` then owns exactly `0x004335E0-0x00433692` (179 bytes), ending in `ret 4`; thirteen `CC` bytes at `0x00433693-0x0043369F` remain unowned before independent `0x004336A0`. | Native IDA extents/full disassembly, bounded target-byte reads across both seams, and repeatable canonical exact replay. |
| PBG-010 | exact / authored-game | The Packet-90 PbgArchive owner-helper cohort is repository-canonical exact: `CopyFileName @ 0x004330A0` (64), `PbgFileBackend::ReadInt @ 0x004330E0` (18), `SeekPastInt @ 0x00433100` (12), `SeekPastString @ 0x00433110` (26), `PbgArchiveEntry` constructor `0x00433130` (9), destructor `0x00433140` (24), `PbgArchive::AllocEntries @ 0x004332B0` (254), and `PbgArchive::Release @ 0x004333B0` (61). The eight authored functions total 468 bytes. AllocEntries creates `count+1` 0x10-byte entries through natural VC7.1 array construction, copies filename/dataOffset/decompressedSize/fieldC, and writes the sentinel offset/size pair; Release frees and clears filename, entries, backend, and count. | Fresh target-attested bodies/callers/boundaries, clean committed TH08 PbgArchive/PbgFile source families, actual tracked-source compilation, and two byte-identical cold canonical replays for every unit. TH095 is only a contrast source where its aligned filename stepping differs from TH09. |
| PBG-011 | exact / parser-layout | TH09 `PbgArchive::SeekPastString` advances the table cursor by exactly `strlen(*ptr)+1`; it does not round the cursor to a four-byte boundary. `SeekPastInt` advances by four and returns the dword at the new cursor. These helpers are called by exact AllocEntries for each filename plus three dword fields. This target-local cursor rule explicitly rejects the committed TH095 aligned-string helper for TH09 even though the surrounding module family is related. | Attested TH09 decompilation, complete target bytes, exact tracked helper replays, and the exact AllocEntries relocation/call sequence. |
| PBG-012 | compiler-generated / excluded | `0x00433160-0x004331B7` is the VC7.1 `PbgArchiveEntry` vector deleting destructor, not authored game source. Under the Packet-90 helper profile the natural entry destructor causes VC7.1 to emit the same 88-byte artifact; all 68 non-relocation bytes match and five relocations resolve to the entry destructor, EH vector destructor iterator, and operator delete. The candidate is classified `compiler_generated / exclude` and is deliberately absent from authored matches/implemented/reccmp. | Target body and boundary, exact compiler artifact probe, Release relocation to the generated symbol, and the existing repository convention for compiler-generated deleting destructors. |
| TOOLCHAIN-081 | exact / TU-local profile | The natural PbgArchive helper module requires pinned VC7.1 `/O2 /Ob0 /Oy- /Gr + /Oi` to reproduce the TH09 Release lowering: `/Ob0` preserves the separate 88-byte vector deleting destructor call and keeps CopyFileName 64/64 plus AllocEntries 254/254 exact. `/Ob1` and `/Ob2` inline array destruction and emit an 85-byte Release; `/Ob2` also expands AllocEntries to 291. `/Os` emits a 61-byte Release with only 9/53 comparable bytes equal and shrinks AllocEntries/CopyFileName, so it is rejected. Packet-89 Load/ParseHeader remain independently exact under `/Ob1`. This is evidence for a local source/TU profile boundary, not a project-wide flag claim. | Focused Packet-90 `/Ob0`/`/Ob1`/`/Ob2`/`/Os` probes plus eight target-bound A/B cold match units and Packet-89 regressions. |
| BOUNDARY-081 | exact / mixed-origin | The Pbg helper chain has separately owned extents and padding: ReadInt `0x004330E0-0x004330F1`, then 14 `CC`; SeekPastInt `0x00433100-0x0043310B`, then 4 `CC`; SeekPastString `0x00433110-0x00433129`, then 6 `CC`; entry ctor `0x00433130-0x00433138`, then 7 `CC`; entry dtor `0x00433140-0x00433157`, then 8 `CC`; compiler-generated vector deleting destructor `0x00433160-0x004331B7`, then 8 `CC` before exact ReadDecompressEntry. AllocEntries owns `0x004332B0-0x004333AD`, followed by 2 `CC`; Release owns `0x004333B0-0x004333EC`, followed by 3 `CC` before exact ParseHeader. CopyFileName owns `0x004330A0-0x004330DF` and is immediately followed by independent ReadInt. | Attested function extents, bounded target-byte reads across every seam, complete disassembly for the central helpers, and exact compiler replay. |
| PBG-013 | exact / authored-game | The TH09 PbgFile backend is a 0x0C concrete object whose target vtable at `0x00490EE4` binds Open `0x00433980`, Close `0x004336A0`, Read `0x004336D0`, Write `0x00433710`, Tell `0x00433760`, GetSize `0x00433780`, Seek `0x004337A0`, the compiler-generated scalar deleting destructor `0x00433A70`, and ReadWholeFile `0x004337D0` at slot `+0x20`. The object stores the Win32 handle at `+0x04` and desired access at `+0x08`. `PbgFileView::Open` closes prior state, parses `r/w/a`, builds a full path, calls `CreateFileA`, and seeks append mode to `FILE_END`; Close/Read/Write/Tell/GetSize/Seek reproduce the corresponding Win32 wrapper behavior. | Fresh attested TH09 vtable bytes and function bodies, exact Packet-89/90 consumers, clean committed TH08/TH095 PbgFile source families, and two cold canonical replays of every maintained backend method. |
| PBG-014 | exact / authored-game | Packet 91 reconstructs thirteen authored PbgFile functions canonically exact for 810 authored bytes: base constructor/destructor, Close, Read, Write, Tell, GetSize, Seek, ReadWholeFile, GetFullFilePath, concrete constructor/destructor, and Open. The two adjacent 33-byte scalar deleting destructors at `0x00433910` and `0x00433A70` are VC7.1 compiler artifacts and are classified `compiler_generated / exclude`, not authored exact functions. `ReadWholeFile` requires `GENERIC_READ`, bounds by `maxSize`, allocates with `GlobalAlloc(LPTR)`, preserves/restores position through `g_PbgFileSeekModes[0] @ 0x00490F08`, and frees on read failure. `GetFullFilePath` preserves the target's historical null-`strrchr` behavior instead of silently repairing it. | TH09 disassembly/decompilation and vtable evidence, natural tracked source in `src/PbgFile.cpp` / `src/PbgFileBackend.cpp`, 13 repository match units, two byte-identical cold exact replays per unit, and exact generated-destructor diagnostics. |
| TOOLCHAIN-082 | exact / TU-partition | Pinned VC7.1 `/O2 /Ob1 /Oy- /Gr + /Oi` reproduces the complete PbgFile backend, but concrete-constructor exactness additionally depends on source-unit visibility. When the empty base `PbgFileBackend` constructor is defined in the same TU, VC7.1 reuses ECX and emits a 28-byte concrete constructor. Keeping base constructor/destructor definitions in a separate TU makes the compiler preserve `this` in ESI across the external base call and reproduces the 32-byte target exactly. `/Ob0` does not change the 28-byte same-TU result. Separately, using literal seek origin zero in ReadWholeFile emits 141 bytes; using the target-backed `g_PbgFileSeekModes[0]` restores the exact 146-byte body and both data relocations. These are local TU/source-shape facts, not project-wide compiler or link-order closure. | Focused v1/v2/split-TU v3 VC7.1 probes, target/object instruction alignment, and the final repository-defined cold A/B replays. |
| BOUNDARY-082 | exact / mixed-origin | Reviewed PbgFile extents remain separate from padding: Close `0x004336A0-0x004336C1` then 14 `CC`; Read `0x004336D0-0x0043370C` then 3 `CC`; Write `0x00433710-0x00433751` then 14 `CC`; Tell `0x00433760-0x00433778` then 7 `CC`; GetSize `0x00433780-0x00433794` then 11 `CC`; Seek `0x004337A0-0x004337C7` then 8 `CC`; ReadWholeFile `0x004337D0-0x00433861` then 14 `CC`; GetFullFilePath `0x00433870-0x004338E8` then 7 `CC`; base ctor `0x004338F0-0x004338F8`, base dtor `0x00433900-0x00433906`, compiler-generated base scalar deleting destructor `0x00433910-0x00433930`, concrete ctor `0x00433940-0x0043395F`, concrete dtor `0x00433960-0x00433975`, Open `0x00433980-0x00433A61`, and compiler-generated concrete scalar deleting destructor `0x00433A70-0x00433A90`. Padding is never assigned to authored source. | Target bytes/objdump across every seam, provisional IDA extents, concrete vtable binding, and exact compiler replay. |
| LZSS-012 | third-party / provenance-closed | The TH09 compression cohort `0x00433AA0-0x0043425A` is derived from the published `LZSS.C` family in Mark Nelson and Jean-loup Gailly, *The Data Compression Book*, 2nd ed., Chapter 8, rather than being Touhou-authored game code. The provenance match is structural and specific: the published source uses the same `InitTree`, `ContractNode`, `ReplaceNode`, `FindNextNode`, `DeleteString`, and `AddString` helper family, the same `{parent, smaller_child, larger_child}` three-integer binary-tree representation, the same circular-window/tree algorithm, and explicitly documents an `Index/Length = 13/4` LZSS.C configuration. TH09 independently supplies its addresses, 13-bit/4-bit stream format, 0x2000-byte dictionary, 0x2001-node tree, allocation wrappers, callers, extents, and compiler behavior. All nine tracked LZSS candidates are therefore classified `third_party / exclude / high`; this supersedes only the earlier origin-unknown status and does not convert diagnostic codegen equality into authored exact credit. | TH09 target bodies, xrefs, extents and cold VC7.1 replays; bibliographic provenance from *The Data Compression Book*, 2nd ed., Chapter 8 LZSS.C and its published 13/4 configuration; committed TH08/TH095 source/history as non-authoritative corroboration. |
| LZSS-013 | target-observed / semantic-owner-closed | TH09 has one shared LZSS mutable-state family. The tree occupies `0x004B4420-0x004CC42B`: 0x2001 records at 12-byte stride (`0x1800C` bytes). Four bytes of alignment separate it from the 0x2000-byte dictionary at `0x004CC430-0x004CE42F`. Fresh target xrefs to `0x004B4420` are confined to InitTree and the encoder tree helpers; fresh xrefs to `0x004CC430` are confined to Decode, InitEncoderState, AddString, and Encode. Decode's target callers are replay load, score load, PbgArchive ReadDecompressEntry, and PbgArchive ParseHeader; Encode's are replay save and score save. This closes the semantic owner as the LZSS compression subsystem and proves encoder/decoder dictionary identity, but it does not establish the original source-file, COFF object, or complete BSS/link owner. | Passed native-ID A target xrefs/call graphs, target address geometry, and exact relocation manifests for the eight zero-difference LZSS units. |
| TOOLCHAIN-083 | negative-oracle / codegen-open | The committed TH095 encoder suggested one natural lifetime hypothesis: keep `dictHead` in the outer function and restrict `bitfieldMask` to one nested token-writing block. Applying only that lexical-lifetime change to TH09, without TH095's identifier-bucket macros or any register forcing, leaves the stock VC7.1 body unchanged at 570 bytes versus the 571-byte target. Target/object alignment confirms the persistent coloring difference: TH09 target keeps `outBitMask` in BL and `outBits` at `[ebp-4]`, while the natural object keeps the 32-bit accumulator in EBX and the bit mask in a stack byte. Earlier declaration-order, checksum, and optimized-profile probes were likewise negative. No pragma, volatile/register hint, assembly, inert storage, or target-specific forcing is introduced; `Lzss::Encode` remains source-present/non-exact. | Packet-92 baseline/nested probes under pinned VC7.1 `/O2 /Ob1 /Oy- /Gr + /Oi`, full target/object disassembly alignment, and Packet-80 prior negative probes. |
| ANM-001 | exact / authored-game | `AnmVm::IsVisible @ 0x00434370-0x00434379` returns bit 0 of the VM flag word at `+0x1F8`, while `AnmVm::SetInvisible @ 0x00434380-0x00434387` clears the same bit. The first helper is followed by six unowned `CC` bytes through `0x0043437F`; the second is followed by eight unowned `CC` bytes through `0x0043438F` before independent code at `0x00434390`. Both helpers are repeatably target-exact under the established AsciiManager VC7.1 profile and are classified `authored_game / AnmManager / high`. | Fresh attested TH09 bytes and helper semantics plus two independent cold canonical replays: 10/10 and 8/8 bytes, no relocations. Clean committed TH08 source corroborates only the semantic method family. |
| ASCII-028 | source-present / authored-game / codegen-blocked | `PauseMenu::OnUpdate @ 0x00434740-0x00434E05` operates on the embedded `AsciiManager +0x82A0` subobject. TH09 fixes its size at `0x1528`: state and frame counter followed by seven `0x2A4` menu VMs and one background VM. The target state machine spans states 0-9, including two top-level choices, paired confirmation states, close/resume handling, and two delayed action paths. The maintained source corrects the state-9 split: the ordinary branch resets to Supervisor state 10 and rejoins common VM updating, while the result-screen branch calls the target-observed result preparation path, sets Supervisor state 11, timestamps, and returns directly. | Fresh attested TH09 decompilation/disassembly/call/dataflow and target-bound VC7.1 probes. Clean committed TH08 `PauseMenu::OnUpdate` supplies source-shape and naming hypotheses only; TH09 independently determines VM count, offsets, script/sprite values, flags, input paths, and action semantics. |
| BOUNDARY-083 | observed / compiler-artifact-owned | `PauseMenu::OnUpdate` owns code `0x00434740-0x00434E05` ending in `ret`. Bytes `0x00434E06-0x00434E07` are two-byte alignment, followed by the method's ten-entry switch table at `0x00434E08-0x00434E2F`; independent code begins at `0x00434E30`. The reviewed physical packet is therefore 1734 code bytes plus 2 alignment bytes plus 40 switch-table bytes. | Full target disassembly, attested target-byte reads, and all ten switch destinations. |
| ASCII-029 | source-present / authored-game / codegen-blocked | `AsciiManager::OnUpdate @ 0x00435B00-0x00435BFC` is the calc-chain callback: `RegisterAsciiManagerStartup @ 0x00435CA0` installs it with `g_AsciiManager` as the argument and the already-exact AsciiManager added/deleted callbacks. It updates the two 100-record score-popup banks when menu gates are clear, dispatches the `+0x82A0`, `+0x97C8`, and `+0xA7A8` embedded state objects under GameManager bytes `+0x13C/+0x13D/+0x13E`, increments AsciiManager `+0x8284`, and returns Chain states 1/3. The best natural VC7.1 source is exactly 253 bytes with all fourteen relocation destinations aligned and 196/197 comparable bytes equal; the sole residual is object `JB` versus target `JL` at `+0xA2`. A natural explicit signed-index probe expands to 269 bytes, so the maintained source keeps the closer cursor form and does not force signedness/register allocation. | Fresh TH09 chain-registration evidence, full target disassembly, exact existing AsciiManager layout, and focused VC7.1 relocation-aware comparison. |
| TOOLCHAIN-171 | bounded negative / AsciiManager OnUpdate signed-loop plateau | `AsciiManager::OnUpdate @ 0x00435B00` remains exact-sized at 253 bytes with all fourteen relocation destinations solved and 196/197 ordinary comparable bytes equal. The sole residual is branch opcode `JB` versus target `JL` at `+0xA2`. Changing the flags cursor from `unsigned char *` to `char *` preserves the same 253-byte `JB` result; a natural `AsciiGameManagerSideView *` loop becomes 254 bytes; and a signed `int side < 2` induction paired with the flags cursor reproduces the earlier 269-byte expansion rather than the target loop. Producing `JL` while retaining the pointer-shaped target loop would therefore require an encoding-directed integer-address formulation, which is not retained. The frontier remains NON-EXACT. | Focused pinned-VC7.1 signed-pointee, struct-pointer, and signed-induction probes plus the existing 253-byte relocation-aware baseline. |
| TOOLCHAIN-084 | unresolved-codegen | Packet 93 reuses the established same-module VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` profile. The small visibility helpers reproduce exactly, and existing AsciiManager Reset/AddedCallback/DrawStrings/CreateScorePopup remain exact after the layout refinements. `PauseMenu::OnUpdate` remains non-exact because the natural object hoists zero into EBX and selects materially different register/basic-block/switch-table layout from the target; the apparent total-size proximity before semantic correction was therefore not evidence of near equality. No `var_order`, register hint, volatile forcing, padding, assembly, target bytes, or fake return is retained. | Repository-defined cold match units, connected exact regressions, target/object instruction alignment, and the rejected natural signed-index experiment. |

| ASCII-019 | source-present / authored-game / codegen-blocked | `AsciiMenuState5::OnUpdate @ 0x00434E90-0x0043534C` is the `AsciiManager +0x97C8` sibling state object dispatched only by `AsciiManager::OnUpdate` while GameManager byte `+0x13D` is active. TH09 fixes state/frames plus five `0x2A4` VMs and one background VM (`0xFE0` total), replay-neutral and side-runtime early exits, a two-choice selector, delayed mode-1 and cleanup paths, five-VM/background ANM execution, and game-state side effects. Clean committed TH08 `RetryMenu::OnUpdate` supplies only the broad retry/death-menu state-machine family; TH09 has a second distinct three-choice sibling and script/event code can independently set `+0x13D`, so the original TH09 menu name remains unresolved. Maintained source uses the neutral `AsciiMenuState5` name and remains non-exact. | Passed TH09 caller/callee/decompilation/disassembly/xref evidence, target object geometry, committed TH08 source-shape corroboration, and focused VC7.1 feedback. |
| BOUNDARY-084 | observed / compiler-artifact-owned | The logical `AsciiMenuState5::OnUpdate` body ends with `ret` at `0x0043534C` after 1213 code bytes. `0x0043534D-0x0043534F` is three-byte compiler alignment, `0x00435350-0x00435363` is the method's five-entry switch table, and `0x00435364-0x0043536F` is unowned `CC` before independent draw code. The target physical owned extent is therefore 1236 bytes. The best maintained VC7.1 source emits a 1228-byte COMDAT whose switch-table relocations begin at `+0x4B8` instead of target `+0x4C0`, an eight-byte pre-table code/alignment deficit; no padding or register forcing is introduced to close it. | Full TH09 code/tail bytes, target switch destinations, maintained-object section/relocation layout, and negative source-shape probes. |
| ASCII-020 | exact / authored-game | `AsciiMenuState4::OnUpdate @ 0x004355F0-0x00435A67` is the `AsciiManager +0xA7A8` sibling state object selected by GameManager byte `+0x13E`. Its storage is state/frames plus four `0x2A4` VMs (`0xA98` total), while the target method initializes and executes the first three. States 1/2/3 implement a three-choice selector; confirmation maps to delayed states 4/5/6, which after 20 frames set Supervisor states 10/1/6 respectively, clear the activation byte, timestamp, and return Chain break. Natural VC7.1 source plus its same-COMDAT seven-entry switch table replays the full 1172-byte owned extent with all 64 relocations exactly. The neutral `AsciiMenuState4` name does not claim an original symbol. | TH09 `AsciiManager::OnUpdate` ownership, full target state machine and draw companion, complete boundary/table review, and two byte-identical cold canonical replays. |
| BOUNDARY-085 | exact / compiler-artifact-owned | `AsciiMenuState4::OnUpdate` owns 1144 logical code bytes through `ret @ 0x00435A67`; its seven-entry switch table immediately follows at `0x00435A68-0x00435A83` with no alignment gap. Twelve `CC` bytes at `0x00435A84-0x00435A8F` remain physically unowned before independent code at `0x00435A90`. Canonical exactness compares the complete 1172-byte code-plus-table COMDAT, while authored-byte progress counts only the 1144-byte logical C++ body. | Target bytes, all seven case destinations, exact relocation manifest, and repository cold replay. |
| TOOLCHAIN-085 | negative-oracle / ownership-open | For the five-VM sibling, TH09 address `0x004A7E78` is numerically `g_GameManager +0xE8`, but target bytes do not establish the original C++ or data-definition owner. Collapsing that storage into the packet-local GameManager view causes stock VC7.1 to tail-share much more aggressively and shrinks the physical menu COMDAT from the best natural 1228 bytes to 1184 versus target 1236. The maintained exact-phase source therefore keeps a neutral external pointer view and records the ownership as unresolved instead of selecting the familiar aggregate. Reordering the delayed cases to match target physical order likewise triggers the same over-sharing. | Target address geometry, focused natural-source probes, VC7.1 object sizes/basic-block alignment, and exact State4 control experiment under the same TU/profile. |

| ANM-002 | exact / layout | TH09 `AnmLoaded::SetAndExecuteScriptIdx @ 0x00403E00` stores the loaded-ANM receiver at `AnmVm +0x204` and the 16-bit script index argument at `AnmVm +0x21A` before dispatching the indexed script. Exact `AsciiManager::OnDrawLowPrio @ 0x00435C00` independently tests `+0x21A` on the fourth VM of the `+0xA7A8` menu object before drawing it, closing both offsets without transferring the TH08 layout. | Canonical 41-byte replay of `0x00403E00`, exact Packet-95 low-priority draw replay, and maintained `AnmVm` size/offset assertions. |
| ASCII-021 | exact / authored-game | `AsciiMenuState5::OnDraw @ 0x00435370-0x004353E4` and `AsciiMenuState4::OnDraw @ 0x00435A90-0x00435AE2` are the draw companions for the two Packet-94 embedded menu states. The first is gated by GameManager `+0x13D`, flushes the ANM vertex buffer, selects gameplay viewport 2, conditionally draws the `+0xD3C` background under Supervisor flag `0x2` after the state advances, then draws five visible VMs. The second is gated by `+0x13E`, performs the same flush/viewport setup, and draws the first three visible VMs. | TH09-only caller/offset evidence, full target bodies, exact AnmVm visibility/draw dependencies, and two cold canonical replays: 117/117 with 11 relocations and 83/83 with 8. Clean committed TH08 draw methods supplied only the broad source-shape family. |
| ASCII-022 | exact / authored-game | `AsciiManager::OnDrawLowPrio @ 0x00435C00-0x00435C52` and `AsciiManager::OnDrawHighPrio @ 0x00435C60-0x00435C93` are the two draw Chain callbacks registered for `g_AsciiManager`. The low callback draws queued strings, clears `numStrings` through the `0x00430220` helper, draws PauseMenu plus both sibling menu states, then draws the fourth `+0xA7A8` VM when its target-proven `scriptIndex +0x21A` is nonzero. The high callback preserves the AsciiManager callback argument, selects gameplay viewports 0 then 1, and calls the per-player renderer `0x004353F0` with indices 0 then 1. Both return Chain continue (`1`). | Exact registration wiring, complete target bodies, TH09 AnmVm script-index evidence, and two byte-identical cold VC7.1 replays: 83/83 with 7 relocations and 52/52 with 6. |
| ASCII-023 | exact / authored-game | `RegisterAsciiManagerStartup @ 0x00435CA0-0x00435D3C` wires the full TH09 AsciiManager lifecycle: calc callback `AsciiManager::OnUpdate` plus exact Added/Deleted callbacks and `g_AsciiManager` arg are registered at priority 1; low/high draw callbacks use the same arg at priorities 34 and 23. Only failure of `Chain::AddToCalcChain` returns `-1`; draw registration results are ignored and success returns zero. Exact `Supervisor::StartupThread` is the sole direct caller. | Passed TH09 call/xref graph, complete target body, exact Chain dependencies, exact lifecycle callbacks, and two cold 157/157 replays with all 28 relocations. TH08/TH095 committed registration source corroborates only the three-Chain source family; TH09 independently fixes callbacks, priorities and failure behavior. |
| ASCII-024 | observed / next-frontier | `sub_4353F0 @ 0x004353F0-0x004355E6` is the per-player high-priority score-popup renderer called only by exact `AsciiManager::OnDrawHighPrio`, once for each side index. TH09 computes `scorePopups + playerIndex * 100 * 0x38`, iterates exactly 100 records, consumes the existing popup position/color/timer/text/characterCount fields, derives alpha from squared distance to the corresponding side/player position, selects digit sprites, and draws through the small-score VM. Its original source return type/name, full player-owner layout and exact codegen remain unresolved. | Passed target decompilation plus exact high-priority caller relocation/ABI; clean committed TH08 `OnDrawHighPrioImpl` supplies only the broader popup-rendering source family. |
| BOUNDARY-086 | exact / authored-only | Packet 95 closes five independent logical extents with no owned compiler tail: `AsciiMenuState5::OnDraw` `0x00435370-0x004353E4` then eleven unowned `CC`; `AsciiMenuState4::OnDraw` `0x00435A90-0x00435AE2` then thirteen `CC`; `AsciiManager::OnDrawLowPrio` `0x00435C00-0x00435C52` then thirteen `CC`; `AsciiManager::OnDrawHighPrio` `0x00435C60-0x00435C93` then twelve `CC`; and `RegisterAsciiManagerStartup` `0x00435CA0-0x00435D3C` then three `CC` before independent `0x00435D40`. | Verified target bytes/disassembly across every seam plus exact per-function replay. Padding is not counted as authored source. |
| TOOLCHAIN-086 | exact / migration-and-metadata | Under the established AsciiManager pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` profile, the first natural TH09-local adaptation of the committed TH08 draw/registration skeleton reproduces all five Packet-95 targets structurally exactly. Adding target-proven `AnmVm +0x204/+0x21A` fields and draw declarations changes only eight compiler-local `$L...` relocation symbol names in the already-exact `AsciiMenuState4::OnUpdate`; relocation offsets/types/destinations and all 1172 bytes remain unchanged after manifest refresh. Nine other existing AsciiManager exact units replay unchanged. | Two cold all-function Packet-95 replays, Packet-94 State4 cold regression, nine cross-TU AsciiManager exact regressions, and committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` / TH095 `a7e340d3ff00e81b56584bc03288953f754b1faf` source-shape comparison. TH095 live scratch was not read. |

| ANM-003 | observed / layout | Exact `AsciiManager::OnDrawHighPrioImpl @ 0x004353F0` copies the dword at the selected `AnmLoadedSprite +0x34` into `smallScoreText +0x28` immediately before drawing. The maintained TH09 view exposes `AnmVm::spriteSize` at `+0x28` and uses its `x` component; clean committed TH08 corroborates the same `spriteSize.x = loadedSprite->widthPx` field relationship. This establishes the consumed 32-bit field relation in TH09 without claiming the complete `AnmLoadedSprite` layout. | Target store/load pair, exact renderer replay, `AnmVm` size/offset assertions, and committed TH08 source-shape corroboration. |
| ASCII-025 | exact / authored-game | `AsciiManager::OnDrawHighPrioImpl @ 0x004353F0-0x004355E6` is the per-player score-popup renderer called exactly twice by exact `AsciiManager::OnDrawHighPrio`, with player indices 0 and 1. It selects `scorePopups[playerIndex][0]` using the target 100 * 0x38 bank stride, disables fog when required, sets render state 23 to value 8, scans 100 records, transforms popup x/y through target GameManager helpers, copies color into `smallScoreText`, and derives alpha 80/linear/208 from squared distance to the corresponding side-player position. For each reversed popup digit it preserves the target timer `<52` / `<56` calls, selects sprite `digit + 97`, writes alpha, copies the selected sprite field `+0x34` to VM `spriteSize.x +0x28`, draws, and advances x by 8.0f. | Exact high-priority caller, full target body/callees, exact ZunTimer `<`, exact maintained popup record layout, and two byte-identical cold 503/503 VC7.1 replays with 20 relocations. Clean committed TH08 supplies the broad popup-render source family only; TH09 independently fixes bank count, coordinate transforms, player split, constants and sprite family. |
| ASCII-026 | observed / owner-layout | The exact popup renderer proves a per-side pointer at GameManager side-record `+0x04` (0x38-byte stride); the pointed object supplies the two float coordinates consumed at `+0x1B88/+0x1B8C` for popup-distance alpha. The maintained packet-local view names this pointer `player` and those coordinates `position.x/y`, but the complete pointed-object type, lifetime owner and remaining side-record fields are not reconstructed by this packet. | Target addressing `g_GameManager + 0x04 + playerIndex*0x38`, exact alpha calculation, and two-player exact caller use. |
| BOUNDARY-087 | exact | `AsciiManager::OnDrawHighPrioImpl` owns exactly `0x004353F0-0x004355E6` (503 logical authored bytes) and ends in `ret 4`. Nine `CC` bytes at `0x004355E7-0x004355EF` remain unowned; exact `AsciiMenuState4::OnUpdate @ 0x004355F0` begins immediately afterward. There is no associated switch table or shared compiler tail. | Native IDA extent, full target disassembly, bounded PE bytes across the following exact function, and repeatable canonical replay. |
| TOOLCHAIN-087 | exact / adjacent-migration | The first natural TH09-local adaptation of the committed TH08 score-popup rendering core reaches `AsciiManager::OnDrawHighPrioImpl` exactly under the established AsciiManager VC7.1 `/O2 /Ob1 /Oy- /Gr + /Oi` profile. TH09-specific changes are target-driven: two 100-record banks selected by player index, GameManager coordinate transforms, side-player position lookup, sprite base 97, and the observed identical digit-sprite result across the `<52/<56` timer branches. No register forcing, `var_order`, volatile, padding, inline assembly, fake return or target-byte embedding is used. The exact source is maintained as `void`, matching the adjacent source family and ignored caller return, but target evidence still leaves the original source return contract open. Adding the renderer/source-layout declarations renumbers only eight compiler-local `$L...` symbols in exact Packet-94 State4; offsets/types/destinations and all 1172 bytes remain unchanged after manifest refresh, while Packet-95 draw/register units and nine cross-TU AsciiManager exact units remain exact. | First-pass structural equality, two cold canonical replays with identical receipts, connected exact regressions, and committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` source family after TH09-local role closure. |

| ASCII-027 | exact / authored-game | `PauseMenu::OnDraw @ 0x00434E30-0x00434E82` and `AsciiManager::ResetStrings @ 0x00430220-0x0043022A` close the remaining low-priority AsciiManager helper seam. Pause draw is gated by GameManager `+0x13C`, flushes ANM rendering, selects gameplay viewport 2, and draws seven visible menu VMs; ResetStrings clears `AsciiManager::numStrings @ +0x8264`. | TH09 exact low-draw caller/receiver offsets and full bodies, clean committed TH08 source-family migration, and two byte-identical cold VC7.1 replays: 83/83 with eight relocations and 11/11 with none. SupervisorDrawMain is also reconverged to the canonical ResetStrings method symbol without byte changes. |
| SUPERVISOR-021 | exact / authored-game | `Supervisor::IsFogDisabled @ 0x00401460-0x0040146C` extracts GameConfiguration options bit 2 at Supervisor `+0x450`; `Supervisor::DisableFog @ 0x0042FF00-0x0042FF37` flushes the ANM vertex buffer before testing/clearing fog state `+0x794` and forwards `D3DRS_FOGENABLE/FALSE` through the device at `+0x08`; `Supervisor::SetRenderState @ 0x0042FF40-0x0042FF69` flushes then forwards its two 32-bit arguments to the same D3D method. | Full TH09 bodies/callers and two cold exact replays: 13/13 with no relocations, 56/56 with two, and 42/42 with two. TH08 and TH095 committed source corroborate the family; where they differ, TH09 proves the TH08 ordering for DisableFog. Maintained SetRenderState source is void-shaped but original source return type remains unresolved. |
| GAMEMGR-010 | exact / owner-partial | Neutral `AsciiGameManagerView::TransformPopupX/Y @ 0x00401680/0x004016B0` are a TH09-specific coordinate pair used broadly across rendering. Each converts an unsigned coordinate from the object referenced by address `0x004B3448` (`+0xCC` for X, `+0xD0` for Y), adds one 4-byte floating argument, and subtracts `g_GameManager +0x350/+0x354`. The maintained source uses float returns and reproduces both targets exactly, but target code does not uniquely establish the original return precision/name or the physical owner/type of the pointer stored at `0x004B3448`. | Full TH09 disassembly/xrefs, exact popup-render caller, and two byte-identical 46/46 cold replays per function with the global-pointer and `2^32` float-constant relocations. Adjacent TH08 lacks this exact helper pair, so no adjacent name/owner was transferred. |
| ANM-004 | exact / authored-game | `AnmLoaded::GetSprite @ 0x00434390-0x004343A1` returns the sprite array at `AnmLoaded +0x0C` indexed at an exact `0x44`-byte stride. The maintained partial `AnmLoadedSprite` is therefore `0x44` bytes and exposes target-backed `widthPx @ +0x34`, which exact popup rendering copies to `AnmVm::spriteSize.x @ +0x28`. | TH09 exact DrawStrings/popup-render callers, complete target body, and two cold 18/18 replays with no relocations; clean committed TH08/TH095 source corroborates the GetSprite array family without transferring TH09 addresses. |
| BOUNDARY-088 | exact / authored-only | Packet 97 closes eight logical extents with no owned tables/tails: `0x00401460-0x0040146C` then 3 CC, `0x00401680-0x004016AD` then 2 CC, `0x004016B0-0x004016DD` then 2 CC, `0x0042FF00-0x0042FF37` then 8 CC, `0x0042FF40-0x0042FF69` then 6 CC, `0x00430220-0x0043022A` then 5 CC, `0x00434390-0x004343A1` then 14 CC, and `0x00434E30-0x00434E82` then 13 CC. | Bounded target disassembly across every seam plus repeatable canonical replay; all following padding remains unowned and is excluded from authored-byte progress. |
| TOOLCHAIN-088 | exact / reusable-migration | All eight Packet-97 helpers reproduce their TH09 targets on the first natural source attempt under the established pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` profile, then remain exact in two independent cold rebuilds. The packet also replaces provisional Packet-96 Supervisor method symbols and SupervisorDrawMain ResetStrings with canonical class-method symbols; direct structural checks prove the existing 503-byte popup renderer, 1172-byte State4 code+table, and 312-byte SupervisorDrawMain remain unchanged before manifest-name refresh. | Eight A/B canonical receipts plus connected exact regressions. No `var_order`, register/volatile forcing, padding, assembly, fake return, target-byte embedding or adjacent address/layout transfer is used. |

| SUPERVISOR-022 | exact / authored-game | `Supervisor::ConfigureGameplayViewport @ 0x00401390-0x004013D6` selects one of three 0xF0-byte viewport records at Supervisor +0x78, publishes the selected pointer at +0x348, calls canonical exact `SupervisorViewportView::ApplyGameplayViewport @ 0x004306B0`, forwards selected record +0xCC to D3D SetViewport, then records the index at +0x34C. Maintained source is void-shaped and byte-exact; original source return type remains unresolved because target preserves the D3D result in EAX and callers do not uniquely prove the source contract. | Current 71/71 ConfigureGameplayViewport and 257/257 ApplyGameplayViewport replays plus TH09 caller/layout evidence. |
| ANM-005 | exact / authored-game | `AnmManager::FlushVertexBuffer @ 0x004396A0-0x00439728` is the central queued-sprite flush. When `spritesToDraw @ +0x128E4` is nonzero it sets texture-stage alpha/color arg2 to diffuse, selects FVF `0x144`, submits a triangle list with `2 * spritesToDraw` primitives from `vertexBufferStartPtr @ +0x2B28EC` using 0x1C-byte vertices, advances start to `vertexBufferEndPtr @ +0x2B28E8`, clears the count, and increments `flushesThisFrame @ +0x18`. | Full TH09 disassembly and data offsets plus committed TH08/TH095 source-family corroboration. Two byte-identical cold replays reproduce 137/137 bytes and all four `g_Supervisor +0x08` relocations. Maintained source is void-shaped; original return contract remains unresolved. |
| ANM-006 | exact / authored-game | `AnmManager::DrawNoRotation @ 0x0043A950-0x0043AA4D` rejects invisible VMs, VM flag bit 1 clear, or zero color alpha byte at `AnmVm +0x1F3`; computes half extents from `spriteSize +0x28/+0x2C` and `scale +0x18/+0x1C`; uses anchor bits 11/12 to form x/y edges around `pos +0x208`; writes x/y/z for four 0x1C-byte shared render vertices rooted at `0x004DC558`; and returns exact render core `AnmManager::DrawInner @ 0x0043A0F0(vm,1)`. | Fresh TH09 body/callees, exact caller relocation graph, clean committed TH08 source family, and two byte-identical 254/254 cold replays with twenty reviewed relocations. The shared scratch data owner remains unresolved; the render core is now independently canonical exact after Packet 417 internal-linkage closure. |
| BOUNDARY-089 | exact / authored-only | Packet 98 closes three logical authored extents with no owned table/tail: `0x00401390-0x004013D6` followed by nine `CC`, `0x004396A0-0x00439728` followed by seven `CC`, and `0x0043A950-0x0043AA4D` followed by two `CC` before independent `0x0043AA50`. | Bounded target disassembly across every seam plus repeatable target-bound canonical replay. Padding is excluded from authored-byte progress. |
| TOOLCHAIN-089 | exact / adjacent-migration | The established pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` profile reproduces Packet-98 viewport and flush routines on the first natural migrated source attempt. Natural TH08-family DrawNoRotation initially emitted 256 versus the 254-byte target only because `(color1 >> 24)` compiled as a dword mask; replacing that expression with the already target-proven alpha byte at `+0x1F3`, matching the adjacent natural `color1.a` source shape without copying its `#pragma var_order`, yields complete structural equality and stable cold canonical replay. No register/volatile forcing, pragma, padding, assembly, target-byte embedding, fake return, or broad flag search is used. | First-pass viewport/flush structural equality, the bounded 256/254 -> 254/254 alpha-field experiment, two cold canonical passes for all three functions, and target-local relocation review. |

| ANM-007 | exact / authored-game | `AnmManager::AddSpriteToDrawBuffer @ 0x00439730-0x004397D4` is the central textured-quad queue append helper. It copies the four input `0x1C`-byte vertices into six triangle-list vertices in order `0/1/2/1/2/3`, writes through `AnmManager +0x2B28E8`, advances that pointer by `0xA8`, increments `spritesToDraw @ +0x128E4`, and returns zero. | TH09-only body/caller ownership plus two byte-identical cold pinned-VC7.1 replays, 165/165 bytes with no relocations. Exact Packet-98 Flush and DrawNoRotation remain exact after the real method symbol replaces the provisional local-view call. |
| ANM-008 | exact / authored-game | `AnmManager::DrawInner @ 0x0043A0F0-0x0043A51A` is the central 1067-byte textured-quad render core used by multiple ANM draw entry points. TH09 independently fixes screen-shake addition, the four-value `FRNDINT` half-pixel rounding block, sprite UV application, viewport culling through the object referenced at `0x004B3448`, texture/shader state changes, VM color selection/mixing, exact `SetRenderStateForVm @ 0x0043A030`, and exact draw-buffer append. With `MixRenderColor @ 0x00401090` defined as a file-local helper in the authentic AnmManagerRender TU, VC7.1 naturally uses the target private AL/CL byte ABI and reproduces all 1067 bytes. | Target body/call graph, committed TH08/TH095 source-family corroboration after TH09-local closure, file-local visibility probe, 1067/1067 structural replay with all reviewed relocations, and connected exact-unit regressions. No register/volatile/pragma/padding bridge is used. |
| BOUNDARY-090 | exact / authored-only | Packet 99 closes two independent authored extents. `AddSpriteToDrawBuffer` owns exactly `0x00439730-0x004397D4`; eleven `CC` bytes at `0x004397D5-0x004397DF` remain unowned before independent `0x004397E0`. `DrawInner` owns exactly `0x0043A0F0-0x0043A51A`; five `CC` bytes at `0x0043A51B-0x0043A51F` remain unowned before independent `0x0043A520`. Both functions are now canonical exact; neither selected extent owns a switch table or compiler tail. | Native IDA function extents, bounded target-PE disassembly across both seams, and Packet 417 exact replay. |
| TOOLCHAIN-090 | exact / private-ABI visibility oracle | The Packet-99 `DrawInner` reconstruction retains only its pre-existing target-proven narrow x87 `FRNDINT` block. The former color-helper blocker was caused by testing external linkage: ordinary `/Gr` external byte arguments use CL/DL, but a file-local `static unsigned char MixRenderColor(unsigned char,unsigned char)` used by real AnmManagerRender callers is privately lowered by VC7.1 to AL/CL, exactly matching TH09. The natural TH08-family formula `((a*b)/128U)` with saturation at 255 emits `0x00401090` byte-for-byte and simultaneously closes DrawInner. | TH09 target color-call disassembly, minimal external-vs-static VC7.1 probes, authentic AnmManagerRender TU compile, and target-bound structural replay. No explicit register convention or bridge is used. |

| ANM-009 | exact / authored-game | `AnmManager::SetRenderStateForVm @ 0x0043A030-0x0043A0EB` is the shared per-VM render-state synchronizer. TH09 binds VM `blendMode` to flags bits 4-5, `disableZWrite` to bit 13, manager `currentBlendMode @ +0x12884`, `disableZWrite @ +0x12887`, and `renderStateChangesThisFrame @ +0x14`. Blend-mode changes flush and select destination blend values 6/2; Z-write changes forward state 14 through exact `Supervisor::SetRenderState`; every call increments the manager counter. | Full target body/callers plus committed TH08/TH095 `SetRenderStateForVm` source family and two byte-identical cold pinned-VC7.1 replays reproduce 188/188 bytes with six relocations. Maintained source is void-shaped; target leaves the incremented counter in EAX, so original return type remains unresolved. |
| ANM-010 | exact / authored-game | Neutral `AnmManager::DrawInnerFlippedX @ 0x0043A520-0x0043A94A` is a horizontal-U-reversed clone of exact `DrawInner`: after normalizing relocations the targets differ only at the two UV endpoint loads. The maintained source changes only those two U expressions and, with the same file-local `MixRenderColor` visibility, reproduces all 1067 bytes. | Canonical 1067/1067 replay with all reviewed relocations plus exact sibling comparison. No register forcing, padding, volatile or pragma steering is used. |
| BOUNDARY-091 | exact / authored-only | Packet 100 closes `SetRenderStateForVm @ 0x0043A030-0x0043A0EB` followed by four unowned `CC`, and the flipped render core `0x0043A520-0x0043A94A` followed by five unowned `CC` before exact `DrawNoRotation @ 0x0043A950`. Neither function owns an adjacent table or compiler tail. | Native IDA extents, bounded PE disassembly across both seams, and target-bound compile diagnostics. |
| TOOLCHAIN-091 | exact / source-shape closure | The natural TH08/TH095-style direct-field control flow remains compiler-significant for `SetRenderStateForVm`: local-cached source was too small, while direct compare/store/switch/if accesses reproduce the target. Packet 417 adds the missing file-local MixRenderColor visibility, making both `DrawInnerFlippedX` and its non-flipped sibling exact without changing their geometry source. | Focused pinned VC7.1 probes, exact helper/caller replays, target-to-target normalized sibling comparison, and connected exact regressions. |

| ANM-011 | exact / authored-game | `AnmManager::TranslateRotation @ 0x004397E0-0x00439810` is the shared 2D rotation/translation primitive used by multiple ANM geometry paths. It computes `x*cos-y*sin+xOffset` and `x*sin+y*cos+yOffset` into the first two floats of the supplied render vertex. The maintained TH08-family member source is byte-exact; callers pass AnmManager in ECX plus seven stack arguments, but the original source return contract is left unresolved because target EAX is not semantically consumed by the reviewed callers. | Fresh TH09 decompilation/callers, committed TH08 source-family comparison, and two byte-identical cold pinned-VC7.1 replays of all 49 target bytes with no relocations. |
| ANM-012 | exact + source-present hard cohort | Neutral `AnmManager::DrawNoRotationFlippedX @ 0x0043AA50-0x0043AB4D` is a 254-byte exact draw entry whose geometry/control flow matches exact `DrawNoRotation`, but whose final call is source-present `DrawInnerFlippedX(vm, 1)`; the original TH09 method spelling remains unknown. `AnmManager::Draw2D @ 0x0043AB50-0x0043AD3A` is separately authored/source-present but NON-EXACT: TH09 proves zero-rotation fallback, visibility/draw/alpha guards, inline x87 FSINCOS, four exact `TranslateRotation` calls, z propagation, anchor offsets and `DrawInner(vm, 0)`. | DrawNoRotationFlippedX has two byte-identical cold 254/254 canonical replays with 20 reviewed relocations. Draw2D is the same 491-byte physical size as target but repeats at 321/355 comparable bytes with 34 relocations; a natural declaration-order experiment using the TH08 var-order ordering is byte-identical, so no pragma/register/volatile forcing is retained. |
| BOUNDARY-092 | exact / authored-only | Packet 101 closes three ANM geometry extents: `TranslateRotation 0x004397E0-0x00439810` followed by 15 unowned CC bytes; `DrawNoRotationFlippedX 0x0043AA50-0x0043AB4D` followed by 2 unowned CC bytes; and source-present `Draw2D 0x0043AB50-0x0043AD3A` followed by 5 unowned CC bytes before independent `0x0043AD40`. No selected function owns an adjacent table or shared compiler tail. | Fresh IDA extents, bounded target bytes across every following seam, full disassembly for Draw2D, and focused VC7.1 comparisons. |
| TOOLCHAIN-092 | narrow x87 exception / negative source-shape oracle | TH09 `Draw2D` contains one target-proven inline x87 `FSINCOS` at `0x0043ABBD`; ordinary C `sin/cos` calls do not reproduce the target and committed TH08 uses the same minimal x87 source-shape family. Packet 101 therefore permits only this bounded FSINCOS block while keeping the rest of Draw2D natural C++. The old TH08 `#pragma var_order` is not migrated: expressing the same seven local declarations in that natural order produces byte-identical 491-byte output and leaves the 321/355 mismatch unchanged. | Target disassembly, committed TH08 `sincos` macro/source family, pinned VC7.1 baseline and natural declaration-order experiment. |


| ANM-013 | observed / source-present / non-exact | `AnmManager::Draw2DRotatedOrAxisAligned @ 0x0043AD40-0x0043AFD1` is the draw-mode-3 branch selected by the sole TH09 dispatcher `sub_436D70`. TH09 independently proves the visibility / flag / alpha rejects, inline x87 FSINCOS for nonzero rotation, four calls to exact `TranslateRotation`, z propagation, anchor corrections, inline axis-aligned zero-rotation construction, and final `DrawInner(vm, 0)`. Clean committed TH08 `Draw2DRotatedOrAxisAligned` and its draw-mode dispatcher strongly corroborate the maintained source-facing name and source family, but the original TH09 method spelling is not independently proven. | Fresh attested TH09 callers/callees/decompile/disassembly plus clean TH08 `a45e99fb1942714e6edded20847e32a654d56f97` committed implementation. |
| BOUNDARY-093 | reviewed / authored-only | The Packet-102 authored code extent is exactly `0x0043AD40-0x0043AFD1` (658 bytes), ending in `ret 4`. `0x0043AFD2-0x0043AFDF` is fourteen bytes of unowned `CC`; independent candidate `0x0043AFE0` begins immediately afterward. No table or shared compiler tail is attached. | Native IDA function extent/disassembly and bounded target bytes across the following candidate. |
| TOOLCHAIN-093 | stable non-exact / adjacent-migration | Natural pinned-VC7.1 `/O2 /Ob1 /Oy- /Gr + /Oi` source adapted from the TH08 body produces the exact 658-byte physical function and resolves all 48 relocations. Two genuine cold diagnostics are byte-identical (SHA-256 `737f214ab8b76fc02f52c39d916bd21c9ff570df9b0352030afbf90f8a129fe5`) and match 461/466 comparable bytes. The only residual is relative `+0x81..+0x85`: the object schedules `mov eax,sine` before the x87 half-width `fmul 0.5`, while the target performs the multiply first. Reordering declarations to the TH08 `var_order` intent without the pragma, reordering half-width versus x/y-offset assignments, and combining both produce byte-identical output. No pragma, register/volatile forcing, padding, fake return, or artificial encoding bridge is retained. All six existing exact functions sharing `src/AnmManagerRender.cpp` remain exact in both cold passes. | Packet-102 A/B diagnostic receipts plus exact regressions for FlushVertexBuffer, DrawNoRotation, AddSpriteToDrawBuffer, SetRenderStateForVm, DrawNoRotationFlippedX, and TranslateRotation. |

| ANM-014 | mixed exact / source-present | The remaining TH09 ANM draw-mode chain resolves as `ProjectCameraFacingQuad @ 0x00439820` -> exact `DrawCameraFacingQuad @ 0x0043B0E0`, exact `DrawNoRotationNoRound @ 0x0043AFE0`, and `Project3DQuad @ 0x00439A90` -> exact `DrawProjected3DQuad @ 0x0043B140`. TH09 independently binds the wrappers to draw modes 1/4/5 in `sub_436D70`. The camera-facing owner projects origin and camera-right through the context pointer at `0x004B3448`, derives screen-space half extents, translates the shared quad, propagates projected z and applies anchor offsets. The projected-3D owner uses VM matrices `+0x130/+0x170`, update/rotation bits in flags `+0x1F8`, manager cached world matrix `+0x12024`, four 0x14 source vertices at `+0x12894`, and four projections into the shared quad. | Fresh TH09 native IDA callers/decompilation/boundary bytes, two cold VC7.1 passes, and clean committed TH08 family after TH09 role closure. The complete projection-context and manager-data definition owners remain unresolved. |
| BOUNDARY-094 | mixed exact / authored-only | Packet 103 closes five independent authored extents: `0x00439820-0x00439A8D` then 2 unowned CC; `0x00439A90-0x00439CE2` then 13 unowned CC; exact `0x0043AFE0-0x0043B0DD` then 2 unowned CC; exact `0x0043B0E0-0x0043B137` then 8 unowned CC; exact `0x0043B140-0x0043B193` then 12 unowned CC. No table/shared tail is assigned to these functions. | Native IDA extents, bounded target bytes across every seam, and canonical wrapper replays. |
| TOOLCHAIN-094 | mixed exact / stable non-exact / adjacent-migration | Natural pinned-VC7.1 `/O2 /Ob1 /Oy- /Gr + /Oi` migration makes all three draw wrappers exact on the first attempt: 254 bytes/20 relocs, 88/3, and 84/3. Two cold passes are byte-identical (`d672a8e1...`, `b814972f...`, `e3c1af26...`). The two large owners are repeatable but remain non-exact: camera-facing geometry is 615 versus 622 bytes (A/B report SHA-256 `09a62c45086b6008ce8401c9971b9130fc5a6fbb08832c8d5cf23eee0b648076`), projected-3D is 568 versus 595 (`17b7f67589161951e5f332d1e26453697b5f03a3e6cfaf32762ff5b24894abcd`). Removing `/Oi` does not change either owner, and expanding camera anchor updates from a loop to four explicit statements is byte-identical; exact chasing stops without var_order, register/volatile forcing, padding, or artificial ABI bridges. Packet-102's 658-byte near-match and six existing exact functions in `AnmManagerRender.cpp` remain unchanged in both cold passes. | Packet-103 cold receipts and connected exact regressions; TH08/TH095 committed source supplies only the algorithm family, while TH09 rejects TH095-only positionOffset/render-state evolution where the target disagrees. |

| ANM-015 | source-present / authored-game / non-exact | `AnmManager::Draw3D @ 0x0043B1A0` is the TH09 3D VM draw path reached with `ECX=g_AnmManager` and the current VM pushed at the sole `0x00403352` callsite after fog handling. It checks visibility/draw-enabled/alpha, flushes queued sprites, rebuilds/scales/rotates VM matrices when dirty, applies anchored world position plus screen offset, invokes the independent 3D render-state helper at `0x00439E90`, updates world/texture transforms and texture/shader/stream caches, then issues a triangle-strip draw. Committed TH08/TH095 `AnmManager::Draw3D` supplied the natural source family only after this TH09-local closure; TH09 independently removes the TH08 vertex-buffer-disabled branch and preserves its own cache/layout details. | Fresh native IDA decompile/disassembly/call graph under passed attestation, bounded caller bytes, committed adjacent source comparison, and repeatable maintained-source VC7.1 diagnostics. |
| BOUNDARY-095 | reviewed / authored-only | `AnmManager::Draw3D` owns exactly `0x0043B1A0-0x0043B4CA` (811 logical bytes) and ends with `ret 4`. `0x0043B4CB-0x0043B4CF` are five unowned `CC` bytes; independent `sub_43B4D0` begins at `0x0043B4D0`. The direct render-state callee `0x00439E90` and widespread 12-byte float-absolute helper `0x0040F1C0` are not absorbed into this function or origin claim. | Native IDA function extent plus target bytes across the tail/next-function boundary and independent helper call/xref evidence. |
| TOOLCHAIN-095 | non-exact / natural-source boundary | Under the established pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` profile, a TH08-family/TH09-local Draw3D reconstruction progresses naturally from 776 bytes (wrong `/Gr` D3DX declarations) to 779 after using stack-based D3DX `WINAPI`, to 817 after restoring the target-observed out-of-line float-absolute helper, and to a stable 816-byte maintained source after TH09 byte-alpha/anchor-lifetime corrections. Two fresh maintained builds produce byte-identical 816-vs-811 diagnostic reports. Further manager-member alias reshaping does not change codegen, so the remaining five-byte residual is left as compiler scheduling/register-allocation mismatch rather than forced. All eleven existing `src/AnmManager*` / `src/AnmLoaded*` canonical units remain exact after the new declaration/TU is introduced. | Focused compiler-oracle experiments v1-v5, byte-identical maintained A/B diagnostics, full target instruction alignment, and connected exact-unit regression replay. |

| ANM-016 | source-present / authored-game / non-exact | `AnmManager::SetRenderStateForVm3D @ 0x00439E90-0x0043A02A` is the sole direct render-state callee of source-present `AnmManager::Draw3D`. TH09 independently binds VM blend mode to flags bits 4-5, alternate color selection to bit 17, Z-write disable to bit 13 and camera-mode selection to bit 15; manager caches are `currentTextureFactor @ +0x1287C`, `currentBlendMode @ +0x12884`, `disableZWrite @ +0x12887`, `cameraMode @ +0x12888`, with the state-change counter at `+0x14`. State transitions flush the exact ANM vertex buffer before D3D changes, update D3DRS_DESTBLEND/TEXTUREFACTOR/ZWRITEENABLE, and route camera-mode transitions through the still-independent helpers at `0x004013E0/0x00401410`. | Fresh native IDA body/call graph, complete target disassembly, exact Packet-100 2D render-state sibling, committed TH08/TH095 family comparison, and repeatable maintained VC7.1 diagnostics. |
| BOUNDARY-096 | reviewed / authored-only | `AnmManager::SetRenderStateForVm3D` owns exactly `0x00439E90-0x0043A02A` (411 bytes) and ends in `ret 4`. `0x0043A02B-0x0043A02F` are five unowned `CC` bytes; exact `AnmManager::SetRenderStateForVm @ 0x0043A030` begins immediately afterward. | Native IDA extent/disassembly plus bounded target bytes across the tail and exact sibling boundary. |
| TOOLCHAIN-096 | exact / corrected visibility hypothesis | `SetRenderStateForVm3D @ 0x00439E90` is target-sized at 411 bytes once genuine lexical lifetimes match the target. Its former 21-byte residual was entirely four calls through an external `MixRenderColor` declaration. Defining the same natural byte-multiply/saturate helper as file-local static in `src/AnmManagerRender.cpp` lets VC7.1 choose the target private AL/CL ABI and closes all 351 comparable bytes plus 15 relocations. | Authentic-TU static-linkage probe, 411/411 target-bound replay, and regressions of the existing exact AnmManagerRender units. No register forcing, `var_order`, volatile, padding, inline assembly or target-byte tricks were added for this closure. |

| SUPERVISOR-023 | exact / camera-owner | TH09 Supervisor owns three 0xF0 viewport/camera configurations at +0x78 and publishes the current record at +0x348. Packet 106 closes neutral camera-mode wrappers `Supervisor::ApplyCameraMode0 @ 0x004013E0` and `ApplyCameraMode1 @ 0x00401410`: each applies one camera formula to the current record, then calls the D3D device `SetViewport` with record +0xCC. Mode 0 is the fixed screen camera and mode 1 is the configured 3D camera. | Fresh TH09 callers from `AnmManager::SetRenderStateForVm3D`, raw target instruction setup, existing exact `ConfigureGameplayViewport`, and two cold VC7.1 replays per wrapper. |
| SUPERVISOR-024 | exact / layout | The TH09 0xF0 viewport record is target-backed as cameraPosition +0x00, cameraLookAtOffset +0x0C, cameraUp +0x18, cameraForward +0x24, cameraRight +0x30, cameraPositionOffset +0x3C, fieldOfView +0x48, viewMatrix +0x4C, projectionMatrix +0x8C, D3DVIEWPORT8 +0xCC, viewportMode +0xE4, and a two-float ANM screen-shake value +0xE8/+0xEC. `ApplyScreenCamera @ 0x00430590` derives a pi/10, near/far 1/10000 camera from viewport dimensions; `ApplyGameplayViewport @ 0x004306B0` uses the configured vectors/FOV, near/far 30/1800, updates cameraRight by cross+normalize, and both publish +0xE8/+0xEC to AnmManager +0x1C/+0x20. | Complete TH09 disassembly, exact D3DX/Float3 relocation destinations, existing ANM render consumers, and repeatable 285/285 plus 257/257 cold replays. TH08/TH095 camera families corroborate source shape only. |
| BOUNDARY-097 | exact / authored-only | Packet 106 closes four logical extents: `0x004013E0-0x00401409` then 6 CC, `0x00401410-0x00401439` then 6 CC, `0x00430590-0x004306AC` including `ret 4` then 3 CC, and `0x004306B0-0x004307B0` including `ret 4` then 15 CC. No attached tables or shared tails are owned by these functions. | Fresh IDA extents plus bounded target disassembly across all four seams and canonical replay. |
| TOOLCHAIN-097 | exact / adjacent-migration | Packet 106 reaches all four camera/viewport functions exactly under the established pinned VC7.1 `/O2 /Ob1 /Oy- /Gr + /Oi` profile using natural source. Two target-driven source-shape facts are required: deep helpers access the D3D device through global `g_Supervisor +0x08` rather than preserving helper `this`, and the fixed-camera distance expression remains inline as the first LookAt argument so MSVC right-to-left argument evaluation constructs `up`/`at` before calling the neutral cdecl tangent helper at 0x0042E910. No `var_order`, register/volatile forcing, padding, assembly, fake return or target-byte embedding is used. | First-pass wrapper equality, focused source-order diagnostics, two byte-identical cold canonical passes, exact existing ConfigureGameplayViewport replay, and nine exact AnmManagerRender regressions. |
| SUPERVISOR-025 | exact / authored-game | `Supervisor::InitializeViewports @ 0x004307C0-0x00430A9F` is the target-backed owner initializer for the three `0xF0` Supervisor viewport/camera records at `+0x78/+0x168/+0x258`. The sole direct caller loads `ECX = &g_Supervisor` before the call. The target initializes records in physical order 2, 0, 1: configuration 2 is the full `0/0/640/480` viewport with mode 2, configuration 0 is the left `16/16/288/448` viewport with mode 0, and configuration 1 is the right `336/16/288/448` viewport with mode 1. Each receives camera position `(0,0,1000)`, look-at offset `(0,0,0)`, up `(0,1,0)`, position offset `(0,0,0)`, `pi/6` field of view, and depth range `0..1`. The logical function ends at its `ret` on `0x00430A9F`; independent `0x00430AA0` begins immediately, so no padding/table/tail is assigned to this owner. Maintained source is `void`-shaped and byte-exact, while the original source return contract remains unresolved because the target naturally leaves the final Float3 component in EAX and the caller ignores it. | Fresh TH09 function/caller/callee/byte evidence, exact local `Float3::Float3 @ 0x004010B0`, complete target body and physical-boundary review, followed by committed-only TH095 `Supervisor::InitializeViewports` source-family comparison after TH09 had already fixed its distinct three-record layout/order/constants. Two cold pinned-VC7.1 canonical replays reproduce 736/736 bytes with twelve Float3 REL32 and sixty `g_Supervisor` DIR32 relocations. TH095 live dirty/untracked content was excluded. |
| TOOLCHAIN-098 | exact / adjacent-migration | The established TH09 `SupervisorViewport.cpp` pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` profile reproduces `Supervisor::InitializeViewports` on the first natural TH09-local adaptation of the committed TH095 source-family shape. The provisional comparison has 448/448 ordinary bytes plus 72 target-resolved relocation fields and no differences; the canonical 736-byte unit then replays exact in two independent builds. The same object keeps the five existing camera/viewport exact units unchanged. No `var_order`, register/volatile forcing, padding, inline assembly, fake return, target-byte embedding, or adjacent address/layout transfer is used. | First-pass TH09 structural equality, reviewed 72-relocation manifest, two byte-identical cold canonical receipts, connected Packet-106 viewport regressions, and committed TH095 source used only as a source-shape hypothesis after target-local closure. |
| SUPERVISOR-026 | exact / authored-game | The TH09 Supervisor audio playback seam is reconstructed canonically exact as `Supervisor::PlayMusic @ 0x00431930-0x00431A2D` (254 bytes), `Supervisor::PlayAudio @ 0x00431A30-0x00431AD8` (169 bytes), and `Supervisor::IsMusicPreloaded @ 0x0042ED40-0x0042ED4C` (13 bytes). `PlayMusic` searches 19 records at `0x004A21D0`, dispatches MIDI mode 2 through Supervisor `+0x5BC`, dispatches WAV mode 1 through `SoundPlayer::QueueCommand`, updates the TH09 unlock byte outside replay/flag-bit1 states, and publishes the record `+0x08` value to `0x004DC690` on the non-MIDI path. `PlayAudio(char *path, int bgmUnlockIndex)` is the path-based music-room entry: MIDI mode forwards the supplied path, WAV mode copies it into a 256-byte local buffer, rewrites the extension to `.wav`, queues BGM playback, and shares the unlock-state rule. `IsMusicPreloaded` extracts option bit 4 at Supervisor `+0x450`. Logical function ownership excludes two `CC` bytes after `PlayMusic` and seven `CC` bytes after `PlayAudio`; independent `sub_431AE0` begins at `0x00431AE0`. | Fresh TH09 target callers, stack frame, branches, callees, raw boundary bytes, and two byte-identical cold canonical replays per function. Committed TH08/TH095 Supervisor audio code supplies reusable source-family hypotheses only; TH09 independently fixes the extra 19-record table, globals, ABI, resource flow, extents, and relocation targets. |
| TOOLCHAIN-099 | exact / adjacent-migration / TU-partition | Packet 108 demonstrates two compiler-significant natural-source facts under the pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` profile. First, the TH09 path-based audio target is reproduced by the committed TH08 `PlayAudio` control-flow family with function-scope `wavPathBuf[256]` / `periodLoc` and branch-local unlock tests; no TH08 `#pragma var_order` is migrated. Second, defining `IsMusicPreloaded` in the same audio translation unit causes VC7.1 to inline its bit test and shortens `PlayMusic`; keeping the getter in the established render/configuration TU restores the target's explicit out-of-line call. Natural unsigned-index plus record-pointer traversal reproduces the target's 19-record search. Canonical replay hashes are `126bc6735fa2a7a644f14c6114b2b2d67fe7a91f87f3fa45099ea7aa43e2966b` for 254-byte `PlayMusic`, `2c9c501bef637aca8e18668b9908d032ae8ad6ae9afea13c93af15e02e7c94ff` for 169-byte `PlayAudio`, and `b377969d013b5f1fd4a4bb66dc721eee11dee546ad42d1ca92f87b4d9e0b9786` for 13-byte `IsMusicPreloaded`. | Structural target comparison, reviewed 26/11/0 relocation manifests, two deliberate cold object rebuilds, and committed adjacent source. No register forcing, volatile shaping, padding, inline assembly, fake return, target-byte embedding, or adjacent address/layout transfer is used. |
| SUPERVISOR-027 | exact / authored-game | `Supervisor::StopAudio @ 0x0042FD30-0x0042FD80` (81 bytes) and `Supervisor::FadeOutMusic @ 0x0042FE20-0x0042FEB3` (148 bytes) close the TH09 Supervisor audio-control continuation. `StopAudio` has no stack arguments: target callers load the Supervisor receiver in ECX. Mode 2 stops the MIDI object at `+0x5BC`; mode 1 selects exact `IsMusicPreloaded` and queues SoundPlayer opcode 4 for a preloaded BGM or opcode 3 otherwise; invalid music mode returns -1. `FadeOutMusic(float)` is independently bound by a GameManager caller that pushes one float argument and supplies the Supervisor receiver in ECX. MIDI mode multiplies seconds by `1000.0f`, converts through `__ftol2`, and calls the MIDI fade target; WAV mode reads the scaling multiplier from `this+0x5B8`, uses the duration unchanged when that value is zero or greater than one, otherwise divides by it, converts through `__ftol2`, and queues opcode 5 with the empty string. `StopAudio` owns no bytes after `0x0042FD80`; `0x0042FD81-0x0042FD8F` are fifteen `CC` alignment bytes before independent `sub_42FD90`. `FadeOutMusic` owns no bytes after `0x0042FEB3`; `0x0042FEB4-0x0042FEBF` are twelve `CC` bytes before independent `sub_42FEC0`. | Fresh TH09 callers, ABI, disassembly, physical boundary bytes, target callees, and two byte-identical cold canonical replays per function. Committed TH08/TH095 `StopAudio` and `FadeOutMusic` are source-family hypotheses only. TH09 independently proves that the fade multiplier is receiver-owned (`this+0x5B8`), matching the TH08 shape and rejecting transfer of the TH095 global-speed owner. |
| TOOLCHAIN-100 | exact / adjacent-migration | The pinned TH09 `SupervisorAudio.cpp` VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` profile reproduces `StopAudio` on the first natural committed-adjacent adaptation: 81/81 bytes with nine reviewed relocations. The first natural `FadeOutMusic` draft already matches 89/92 ordinary bytes but uses absolute `g_Supervisor+0x5B8` accesses because the provisional local view assigns the multiplier to the global object. TH09 target disassembly instead uses `[ECX+0x5B8]` three times. Changing only the natural object ownership to `this->framerateMultiplier` produces 148/148 bytes with eleven reviewed relocations and preserves the Packet-108 `PlayMusic`/`PlayAudio` exact units. Two cold canonical receipts are byte-identical: `StopAudio` SHA-256 `eec47c6245e1ac95079ea9afec42bef7e202ad4e244ae621f617a5707fb48b79`; `FadeOutMusic` SHA-256 `84030daaa0864f452193790e0e4796fe246239e97b5142266bba99733d6dc9ed`. | First-pass structural feedback, fresh TH09 `[ECX+0x5B8]` target evidence, reviewed 9/11-relocation manifests, two cold object rebuilds, and committed adjacent source. No `var_order`, register forcing, volatile shaping, padding, inline assembly, fake return, embedded target bytes, or adjacent address/layout transfer is used. |
| SUPERVISOR-028 | exact / authored-game | Packet 110 closes the TH09 Supervisor music-resource lifecycle cohort as `Supervisor::LoadMusic @ 0x0042FC20-0x0042FCEE` (207 bytes), maintained behavioral `Supervisor::StartLoadedMusic @ 0x0042FCF0-0x0042FD24` (53 bytes), and `Supervisor::ReleaseMusic @ 0x0042FD90-0x0042FE11` (130 bytes). `LoadMusic(int trackId)` searches the same 19-record table at `0x004A21D0`, whose target-proven `+0x00/+0x04` fields are track id and path; MIDI mode calls the ReadFileData-shaped target at `0x0043E0D0`, while WAV mode rewrites the path extension to `.wav` and queues opcode 1. `StartLoadedMusic()` dispatches MIDI mode to the Play-shaped target at `0x0043D750` and WAV mode to opcode 10; TH09 `SoundPlayer_ProcessQueues` independently traces opcode 10 to looping DirectSound playback. `ReleaseMusic(int trackId)` releases the MIDI file-data slot through `0x0043D550` or queues opcode 9, whose TH09 queue processor frees the matching preloaded BGM allocation. `StartLoadedMusic` is explicitly a maintained behavioral name; original TH09 source spelling remains unknown. | Fresh TH09 callers/callees, full decompilation/disassembly, SoundPlayer queue semantics, physical-boundary bytes, and two cold canonical VC7.1 replays per function. Clean committed TH08 and committed-only TH095 audio/MIDI source supply source-shape hypotheses only; TH09 independently fixes its table, ABI, opcodes, target addresses, extents, and resource ownership. |
| BOUNDARY-098 | exact / authored-only | Packet 110 reviews all three lifecycle seams. `LoadMusic` ends at `0x0042FCEE`; the single byte `0x0042FCEF` is unowned `CC` before independent `0x0042FCF0`. `StartLoadedMusic` ends at `0x0042FD24`; `0x0042FD25-0x0042FD2F` are eleven unowned `CC` bytes before exact `StopAudio`. `ReleaseMusic` ends at `0x0042FE11`; `0x0042FE12-0x0042FE1F` are fourteen unowned `CC` bytes before exact `FadeOutMusic`. No padding, table, or shared compiler tail is charged to the authored functions. | Native IDA extents plus bounded target-byte reads across all three seams and repeatable target-bound compiler replay. |
| TOOLCHAIN-101 | exact / adjacent-migration | Under the established `SupervisorAudio.cpp` pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` profile, the first natural TH09-local `LoadMusic` and `StartLoadedMusic` implementations are structurally exact at 207 and 53 bytes. The first `ReleaseMusic` draft has exactly one ordinary-byte mismatch at relative `+0x51`: a shorter null-MIDI branch produced by an early return. Re-expressing the already target-observed semantics with a natural shared success tail, without forcing, produces complete 130-byte equality. Two subsequent cold canonical passes reproduce all three functions exactly with reviewed relocation manifests of 11/6/8 fields, while connected `PlayMusic`, `PlayAudio`, `StopAudio`, and `FadeOutMusic` remain exact in both passes. Refining the 19-record local view's `+0x04` field from opaque integer storage to `char *path` is target-backed and leaves the earlier exact audio units unchanged. | Packet-110 positional diagnostics, two deliberate cold canonical object rebuilds, connected audio regressions, and committed adjacent source-family comparison. No `var_order`, register/volatile forcing, padding, inline assembly, fake return, target-byte embedding, or adjacent address/layout transfer is used. |
| SOUNDPLAYER-001 | source-present / codegen-blocked / authored-game | `SoundPlayer::ProcessQueues @ 0x0043F1F0-0x0043F775` is the central TH09 sound-command and queued-SFX dispatcher. Four target callers invoke it as a no-argument member with `ECX=&g_SoundPlayer`; the body consumes 32 command records beginning at `SoundPlayer +0x1F88`, handles command opcodes 0 through 10, shifts completed 0x10C-byte records toward the queue head, and then drains up to twelve queued SFX requests. TH09 independently fixes `bgmFileNames[32][256] @ +0x4108`, `currentBgmFileName @ +0x6108`, active BGM `+0x6208`, BGM volume `+0x6218`, SFX volume `+0x621C`, and the eight-byte sound-buffer metadata records rooted at `0x004A1F80`. Opcodes 9 and 10 are TH09-local extensions relative to the committed TH08 family: 9 frees one preloaded BGM resource and 10 starts the loaded BGM with looping DirectSound playback. Maintained natural C++ is present in `src/SoundPlayerProcessQueues.cpp`, but pinned stock VC7.1 remains non-exact; no canonical match is claimed. | Passed native `th09-ida` callers/callees/decompilation/disassembly/bytes, TH09-local startup/audio layout evidence, and focused stock-VC7.1 probes. Clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` supplies the main source-family hypothesis. TH095 committed `087fbb72a6232e620adaea161400779cb298aeb7` supplies only secondary hypotheses; its live untracked work and forcing pragmas are excluded. |
| BOUNDARY-099 | reviewed / authored-function-only | The logical authored extent of `SoundPlayer::ProcessQueues` is exactly `0x0043F1F0-0x0043F775` (0x586 / 1,414 bytes) and ends in `ret` at relative `+0x585`. The two bytes `0x0043F776-0x0043F777` are outside the logical function, followed by the ten-entry compiler switch jump table at `0x0043F778-0x0043F79F`; the next independent function starts at `0x0043F7A0`. The maintained VC7.1 probe independently emits `ret` at relative `+0x585`, then `8B FF` and a jump table in a 0x5B0 `.text` COMDAT, but this physical-shape agreement does not establish code equality. | Native IDA function extent, bounded bytes across the exit/table/next-entry seam, target disassembly switch references, and maintained-object section/disassembly inspection. The alignment and jump-table bytes are not charged to the authored-function denominator. |
| TOOLCHAIN-102 | source-present / non-exact | Packet 111 migrates the broad TH08/TH095 `SoundPlayer::ProcessQueues` source family only after TH09-local layout and opcode closure. The established `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` profile compiles the first natural source to a 1,472-byte object extent; `/O2 /Os` and `/O1` both contract to 1,283 bytes, so the 1,414-byte target is not explained by a simple size-optimization flag. Target-guided natural control-flow cleanup reduces the maintained probe to a 0x5B0 physical `.text` extent whose logical `ret` is exactly at target relative `+0x585`. Nevertheless relocation-aware comparison diverges in stack-slot assignment and switch CFG beginning near the first command cases, so the routine remains non-exact. A TH095 local-declaration-order experiment produces no codegen change, and an explicit shared restart label makes the result worse; neither is retained. No `var_order`, register/volatile forcing, inline assembly, inert padding, target-byte embedding, or oracle-specific fake path is used. | Packet-111 profile matrix, object disassembly, target disassembly and final cold maintained-source probe `9221eba4a28c9104d7a034537fe31ffe35882f87744cf1836cf147a980628eea`. Exactness credit remains none. |
| SOUNDPLAYER-002 | exact / authored-game | Packet 112 closes a 997-byte TH09 SoundPlayer lifecycle/queue cohort as `SoundPlayer::PreloadBGM @ 0x0043EA30-0x0043EBB3` (388 bytes), `SoundPlayer::StopBGM @ 0x0043EBC0-0x0043EC70` (177), `SoundPlayer::QueueCommand @ 0x0043EE40-0x0043EEA8` (105), and `SoundPlayer::LoadBGM @ 0x0043F0A0-0x0043F1E6` (327). PreloadBGM caches the indexed path, honors exact Supervisor preload policy, frees a prior preload, reads the target range from the current BGM container with target-backed `ThBgmFormat` fields and `g_ZunMemory` tag `./system\\global.h`, then fills the four preload arrays at `+0x1E80/+0x1EC0/+0x1F00/+0x1F40`. StopBGM stops streaming playback, shuts down the BGM thread/event handles, then conditionally invokes the deleting destructor and clears `+0x6208`. QueueCommand scans the first 31 of 32 0x10C-byte command slots at `+0x1F88`, writes opcode/argument/path and resets step. LoadBGM guards manager/music mode/DirectSound, falls back to `ReopenBGM` when preload is disabled, or creates the event/BGM thread and calls the streaming-from-memory target with `GUID_NULL`, flags `0x10100`, sixteen buffers and target-derived aligned notify size before storing `loadedBgmSlot +0x1F80`. | Native TH09 callers/callees/decompilation/disassembly/boundary bytes, existing target-backed Supervisor window `+0x74`, exact preload predicate, and two cold canonical VC7.1 replays. Clean committed TH08 and committed-only TH095 SoundPlayer source supply source-family hypotheses only; TH09 independently fixes its 32-path layout, offsets, opcodes, imports, thread parameter, resource ownership and extents. |
| BOUNDARY-100 | exact / authored-only | Packet 112 reviews all four helper seams. `PreloadBGM` ends at `0x0043EBB3` followed by twelve unowned `CC` bytes through `0x0043EBBF`; `StopBGM` ends at `0x0043EC70` followed by fifteen unowned `CC` bytes through `0x0043EC7F`; `QueueCommand` owns its complete `ret 0x0C` through `0x0043EEA8` followed by seven unowned `CC` bytes through `0x0043EEAF`; `LoadBGM` ends at `0x0043F1E6` followed by nine unowned `CC` bytes through `0x0043F1EF` before source-present/non-exact `ProcessQueues`. No padding/table/tail bytes are charged to these authored functions. | Passed native IDA function disassembly plus bounded target-byte reads across every exit/next-entry seam and repeatable target-bound compiler replay. |
| TOOLCHAIN-103 | exact / adjacent-migration | The committed TH08/TH095 SoundPlayer family transfers unusually well once TH09-local layout is substituted: under the established pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` profile, natural `PreloadBGM`, `QueueCommand`, and `LoadBGM` are structurally exact on the first successful compile. Natural `StopBGM` has one ordinary-byte mismatch at relative `+0x9E`: source clearing `bgm` unconditionally after `delete` emits a six-byte null skip, while the target skips both the deleting destructor and the clear with displacement `0x10`. Restoring the natural second null guard represented by the adjacent `SAFE_DELETE` family closes 177/177 without forcing. Two deliberate cold canonical passes reproduce all 997 bytes with 30 reviewed relocation fields (13/4/0/13); `ProcessQueues` remains independently non-exact at its established 1456-object/1414-logical diagnostic. No adjacent `var_order`, register/volatile forcing, padding, assembly, fake return or target-byte embedding is used. | Packet-112 structural probe, focused StopBGM branch diagnosis, two cold canonical target-bound passes, and connected dispatcher regression. Canonical pass receipts are `170d2205195a17a419a9d5bdb3230112b2197603b8534507a03df7e62c2957d6` and `9405b307ab66e299bb7a5d63e8808153d4214db2834cb83e8d7a2d6b77f6281b`. |
| SOUNDPLAYER-003 | exact / authored-game | Packet 113 closes a 513-byte SoundPlayer support/thread cohort as `SoundPlayer::GetFmtIndexByName @ 0x0043E1F0-0x0043E2BF` (208 bytes), `SoundPlayer::FreePreloadedBGM @ 0x0043E4A0-0x0043E4D0` (49), `SoundPlayer::ReopenBGM @ 0x0043E9E0-0x0043EA27` (72), and maintained `SoundPlayer::BGMPlayerThread @ 0x0043ED80-0x0043EE37` (184). GetFmtIndexByName strips either slash style into a 128-byte local filename and scans 0x34-byte format records at `+0x1F84`, falling back to zero on the empty sentinel. FreePreloadedBGM frees and clears only the indexed allocation pointer at `+0x1EC0`. ReopenBGM requires active `bgm +0x6208`, resolves the format index, obtains the streaming wave file and reopens it against the selected format. The BGM worker waits on `bgmUpdateEvent +0x620C`, drains `WM_QUIT`, and while the stream is playing toggles `isLocked +0x78` around `HandleWaveStreamNotification(1)`. | Passed native TH09 decompilation/disassembly/boundary bytes, Packet-112 exact dependencies, and two cold canonical VC7.1 replays. TH08 clean committed source and TH095 committed source supply source-family hypotheses only. The thread name is maintained reconstruction nomenclature rather than a claim that TH09 original symbols survived. |
| BOUNDARY-101 | exact / authored-only | Packet 113 reviews all four physical seams. `GetFmtIndexByName` owns its `ret 4` through `0x0043E2BF` and independent `0x0043E2C0` begins immediately. `FreePreloadedBGM` ends at `0x0043E4D0`, followed by fifteen unowned `CC` bytes through `0x0043E4DF`. `ReopenBGM` ends at `0x0043EA27`, followed by eight unowned `CC` bytes through `0x0043EA2F` before exact PreloadBGM. `BGMPlayerThread` owns its `ret 4` through `0x0043EE37`, followed by eight unowned `CC` bytes through `0x0043EE3F` before exact QueueCommand. | Bounded target-byte reads, native IDA function extents/disassembly, and repeatable target-bound compiler replay. No alignment byte is charged to the authored-function denominator. |
| TOOLCHAIN-104 | exact / adjacent-migration | Under the established pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` profile, the first natural TH09-local drafts of GetFmtIndexByName, FreePreloadedBGM and ReopenBGM are structurally exact. The first natural BGMPlayerThread draft is 178 bytes versus the 184-byte target because an `if / else if` wait-result dispatch does not reproduce target control flow. Native target disassembly shows `sub eax,0; jz case0; dec eax; jnz tail`, matching a natural `switch (waitResult)` family shape. Rewriting only that observed control-flow form closes 184/184 without forcing or inert code. Two deliberate cold canonical passes reproduce all 513 bytes with 14 reviewed relocation fields; the preceding Packet-112 four-function cohort remains exact, while ProcessQueues remains independently non-exact at 1456 object bytes versus 1414 reviewed logical bytes. | Packet-113 first-pass compiler feedback, target disassembly of the worker dispatcher, two cold target-bound canonical replays and connected regressions. Pass receipt SHA-256 values are `286f35158f44c86d96819333261ee3a582baf7d3c24a5da651c74a0e82205dbe` and `e3de037c05f8b56794f127be8a3ee4b0a05cdc6cac1bfe56c25697c35aaa3275`. No adjacent `var_order`, register/volatile forcing, padding, inline assembly, fake return or target-byte embedding is used. |

| SOUNDPLAYER-004 | mixed exact/source-present / authored-game | Packet 114 reviews the connected TH09 SoundPlayer core lifecycle cohort `InitSoundBuffers @ 0x0043EC80-0x0043ED74` (245 bytes), `Release @ 0x0043EEB0-0x0043EF97` (232), and `StartBGM @ 0x0043EFA0-0x0043F09B` (252), totaling 729 reviewed authored bytes. InitSoundBuffers is canonically exact: it clears 12 queued-SFX slots, loads 39 target-listed SFX through `LoadSound @ 0x0043E770`, logs failures through exact `GameErrorContext::Log @ 0x0042C5C0`, and configures 54 duplicate DirectSound buffers from the metadata table at `0x004A1F80`. StartBGM is canonically exact: it caches the path, guards manager/DirectSound, calls exact StopBGM, derives aligned notify size from the first BGM format, creates the event/thread with Supervisor window `+0x74`, and calls streaming target `0x004407A0` with flags `0x10100`, `GUID_NULL`, and 16 buffers. Release is authored and source-present but deliberately non-exact: target teardown frees the format table, releases 128 duplicate/original sound buffers, stops timer/BGM/init buffer, deletes BGM/manager, then calls exact FreePreloadedBGM for indices 0 through 18. | Fresh passed native TH09 caller/callee/decompile/disassembly/byte evidence, committed TH08/TH095 source-family comparison, two cold canonical VC7.1 replays for the exact members, and a maintained Release diagnostic. Adjacent source supplies hypotheses only; TH09 fixes all addresses, counts, layouts, extents, and exactness states. |
| BOUNDARY-102 | reviewed / authored-only | Packet 114 closes all three physical seams. `InitSoundBuffers` ends in `ret` at `0x0043ED74`; `0x0043ED75-0x0043ED7F` are eleven unowned `CC` bytes before exact BGMPlayerThread. `Release` ends in `ret` at `0x0043EF97`; `0x0043EF98-0x0043EF9F` are eight unowned `CC` bytes before StartBGM. `StartBGM` owns its complete `ret 4` through `0x0043F09B`; `0x0043F09C-0x0043F09F` are four unowned `CC` bytes before exact LoadBGM. No padding, table, or shared tail is charged to the authored-function denominator. | Native IDA full-function disassembly plus bounded target-byte reads across every exit/next-entry seam. |
| TOOLCHAIN-105 | mixed exact/non-exact / adjacent-migration | Under the established pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` profile, natural TH09-local StartBGM is structurally exact on the first compile. The first InitSoundBuffers draft is 241 bytes versus target 245 because an unsized metadata extern plus literal `54` produces a signed loop form; binding the target-proven 54-entry table as `g_SoundBufferIdxVol[54]` and expressing the loop through `sizeof(array) / sizeof(array[0])` naturally produces the target unsigned `0x1B0` byte-extent loop and exact 245-byte codegen. Two cold canonical passes reproduce InitSoundBuffers 245/245 and StartBGM 252/252 with 18 reviewed relocation fields total, while all eight Packet-112/113 SoundPlayer units remain exact. Natural Release remains 247 bytes versus target 232 because normal nonvirtual manager deletion expands destructor plus operator delete; the target instead directly calls scalar-deleting wrapper `0x0043E170`. A diagnostic virtual-destructor view contracts to 231 bytes but emits an indirect deletion call and is rejected as an unproven code-shaping model. Release therefore remains source-present/non-exact, and ProcessQueues remains independently non-exact at 1456 object bytes versus 1414 reviewed logical bytes. | Packet-114 focused compile probes, raw target/object instruction alignment, target scalar-deleting wrapper evidence, two cold canonical passes, and connected exact-unit regression replay. No var-order pragma, register/volatile forcing, padding, assembly, fake return, target-byte embedding, or oracle-specific path is retained. |

| SOUNDPLAYER-005 | exact / authored-game | Packet 115 closes the SoundPlayer WAV/SFX load cohort as `SoundPlayer::LoadSound @ 0x0043E770-0x0043E9D5` (614 bytes) plus file-local `GetWavFormatData @ 0x0043E1A0-0x0043E1E9` (74 bytes). LoadSound's sole caller is exact `SoundPlayer::InitSoundBuffers`; TH09 independently proves ECX=`this`, two stack arguments `(index,path)`, `ret 8`, `soundBuffers[index] @ +0x08`, exact `FileSystem::OpenFile @ 0x0042C970`, RIFF/WAVE/fmt/data validation, an 18-byte WAVEFORMATEX copy, a 0x24-byte DSBUFFERDESC with flags `0x80C8`, DirectSound CreateSoundBuffer/Lock/Unlock, split-buffer copies, and target-owned allocation cleanup. The helper has only the two LoadSound call sites and scans RIFF chunks by tag/size until the unsigned remaining length is exhausted. Error-message strings at `0x0049153C/0x00491518` remain target-facing extern data views whose defining owner is not claimed. | Fresh passed native TH09 caller/callee/full disassembly evidence, exact FileSystem dependency, committed TH08/TH095 source-family comparison only after target closure, and two byte-identical cold pinned-VC7.1 canonical replays. |
| BOUNDARY-103 | exact / authored-only | `GetWavFormatData` owns exactly `0x0043E1A0-0x0043E1E9`; six `CC` bytes at `0x0043E1EA-0x0043E1EF` remain unowned before exact `GetFmtIndexByName @ 0x0043E1F0`. `SoundPlayer::LoadSound` owns exactly `0x0043E770-0x0043E9D5`, including its complete `ret 8`; ten `CC` bytes at `0x0043E9D6-0x0043E9DF` remain unowned before exact `ReopenBGM @ 0x0043E9E0`. No alignment, table, or shared tail is charged to the authored denominator. | Native IDA function extents/full disassembly plus bounded target-byte review across both exit seams. |
| TOOLCHAIN-106 | exact / file-local linkage | The direct committed TH08/TH095 source family spells the WAV chunk scanner as a static SoundPlayer member. Under TH09's established pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` profile, retaining that externally named class-static form emits an 86-byte helper and a 560-byte LoadSound because the public `/Gr` fastcall ABI must be preserved. Making only the helper a natural file-static free function, justified by its two same-TU callers and no external xrefs, lets stock VC7.1 privately allocate the target register transport (ECX/EAX/EDI plus one stack argument) and immediately yields structural equality for both functions: helper 74/74 with one relocation and LoadSound 614/614 with thirty. Two independent cold cross-entry builds produce byte-identical receipts, SHA-256 `ad1937ba965130087c6e3781baea99fce6c76b99b4c42e30fc7c258182545547` and `049a59fe2b9b2059a8e4e266ad12b9e1822b017b004a42db2d0fea062126c90c`. No `var_order`, register/volatile forcing, padding, inline assembly, fake return, target-byte embedding, or custom explicit calling convention is retained. | Focused v1/v2 compiler-oracle comparison, target unique-call graph, object symbols/disassembly, two cold canonical passes, and exact InitSoundBuffers/StartBGM regressions. |

| SOUNDPLAYER-006 | exact / authored-game | Packet 116 closes `SoundPlayer::InitializeDSound @ 0x0043E500-0x0043E764` (613 bytes) and upgrades the Packet-114 teardown owner `SoundPlayer::Release @ 0x0043EEB0-0x0043EF97` (232 bytes) to canonical exact. InitializeDSound's sole caller is `_WinMain@16`, which passes one HWND with `ECX=&g_SoundPlayer`. TH09 independently proves a complete 0x6224 SoundPlayer zero/init, 128 metadata and 12 sound-queue `-1` initializations, one-pointer SoundManager construction, manager Initialize(window,2,2,44100,16), DirectSound handle publication, a 0x8000 looping initialization buffer in PCM stereo 44100/16, volume 100, timer 250 ms, window publication and exact GameErrorContext logging. Release retains the target's 128 duplicate/original buffer teardown and unusual `FreePreloadedBGM(0..18)` loop. | Fresh passed native TH09 caller/full-disassembly evidence, committed TH08/TH095 SoundPlayer/CSoundManager family comparison only after target closure, and two byte-identical cold pinned-VC7.1 canonical replays per function. |
| BOUNDARY-104 | reviewed / mixed authored-generated | InitializeDSound owns exactly `0x0043E500-0x0043E764`, including `ret 4`; eleven `CC` bytes at `0x0043E765-0x0043E76F` remain unowned before exact LoadSound. `SoundManager_scalar_deleting_destructor @ 0x0043E170-0x0043E190` is independently classified compiler-generated/excluded: the exact 33-byte VC7.1 artifact calls manager destructor body `0x0043F7A0`, conditionally calls operator delete `0x0047B249`, and ends in `ret 4`; fifteen `CC` bytes at `0x0043E191-0x0043E19F` remain unowned before exact file-local GetWavFormatData. Release retains its already reviewed `0x0043EEB0-0x0043EF97` authored extent and following eight unowned CC bytes. | Native target disassembly/bounded tail bytes plus stock-compiler generated-artifact comparison and canonical owner replays. The underlying manager destructor/source ownership remains independent. |
| TOOLCHAIN-107 | exact / local profile / generated cleanup | Packet 116 resolves the Packet-114 manager-delete blocker without changing the nonvirtual one-pointer manager model. Under the surrounding `/Ob1` probe, natural InitializeDSound emits 609 bytes because VC7.1 inlines the manager destructor+operator-delete sequence, saves EBX from function entry, and then shares later failure epilogues. A focused `/Ob0` probe preserves the naturally generated 33-byte scalar deleting destructor call and yields a target-sized 613-byte owner with all 18 relocations at target offsets; the only remaining ordinary-byte difference is the manager-cleanup `jz` displacement. Restoring the adjacent `SAFE_DELETE` family source shape—`if (manager != NULL) { delete manager; manager = NULL; }`—changes only that branch and produces 613/613 exact. The same `/Ob0` source naturally closes Release at 232/232 with six relocations; no virtual destructor, custom deleting wrapper, `var_order`, register/volatile forcing, padding, assembly, fake return or target-byte embedding is retained. | `/Ob1`/`/Ob0` focused compiler probes, exact compiler-generated wrapper diagnostic, one-byte branch localization, two cold cross-entry canonical passes. Initialize receipt SHA-256 is `db43fb1c070ad8ca1620c9febaef4211d8cdce415176e4dadb0fec1d53a8a2ad`; Release receipt SHA-256 is `dd6dcb62986ee5aa4db944efa887e98ebdcc19aaa25a8a2f22f57a435a6d4556`. |

| DSUTIL-001 | zero-difference diagnostic / third-party | Packet 117 identifies the TH09 SoundManager core as the Microsoft DirectX SDK `DSUtil.cpp` / `DSUtil.h` sample family rather than authored-game code. The unique reviewed extents are `SoundManagerProcessView::~SoundManagerProcessView @ 0x0043F7A0-0x0043F7B6` (23 bytes), `SetPrimaryBufferFormat @ 0x0043F7C0-0x0043F887` (200), and `Initialize @ 0x00440000-0x00440053` (84). Fresh TH09 target flow matches the sample family exactly: one-pointer DirectSound release, `CO_E_NOTINITIALIZED`, primary-buffer creation and PCM format setup, `DirectSoundCreate8`, cooperative-level setup, and the SetPrimaryBufferFormat call. Clean committed TH08 preserves the original `File: DSUtil.cpp` / `Copyright (c) 1999-2000 Microsoft Corp.` header; committed TH095 independently labels its implementation as Microsoft DSUtil and preserves the same `DSUtil.h` copyright header. | Fresh passed native TH09 extents/callers/disassembly/boundary bytes, clean TH08 `a45e99fb1942714e6edded20847e32a654d56f97`, committed TH095 `bd97b9e08be1e4d4d485857284757b483512915a`, and maintained natural `src/SoundManager.cpp/.hpp`. The three functions are `third_party / SoundManager / exclude / high`; codegen equality is diagnostic only and contributes no authored exact credit. |
| BOUNDARY-105 | reviewed / third-party | `SoundManager` destructor ends at `0x0043F7B6`; `0x0043F7B7-0x0043F7BF` are nine unowned `CC` bytes before SetPrimaryBufferFormat. SetPrimaryBufferFormat ends with complete `ret 0x0C` through `0x0043F887`; `0x0043F888-0x0043F88F` are eight unowned `CC` bytes before independent `0x0043F890`. Initialize ends with complete `ret 0x14` through `0x00440053`; `0x00440054-0x0044005F` are twelve unowned `CC` bytes before independent `0x00440060`. No padding is charged to the excluded third-party extents. | Fresh native disassembly plus bounded target-byte reads across all three seams. |
| TOOLCHAIN-108 | zero-difference diagnostic / adjacent-migration | A natural one-pointer `SoundManagerProcessView` implementation derived from the committed Microsoft DSUtil source family reproduces all three unique TH09 target functions under pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` without source shaping. Two genuine cold cross-entry builds reproduce destructor 23/23, SetPrimaryBufferFormat 200/200, and Initialize 84/84; Initialize has exactly two reviewed relocations, to `DirectSoundCreate8 @ 0x0044E5A6` and SetPrimaryBufferFormat `@ 0x0043F7C0`. A/B receipt hashes are `b8ace71ddb60416a39d63d5cd6649281447e0c8853ba4b59ba733f8ca6179c37`, `823a37915a04cdc17ac23d4f406475af410bab5d33b239d2616dddf0150fe221`, and `520a320c3a79265e0c9c4bdf85c3e6d9bd93ef579ef0619c48b5f4739ecd2c42`. | The replayable units remain in `config/match-units.toml` as third-party diagnostic Oracles only; there are deliberately no `matches.csv`, `implemented.csv`, or `reccmp-functions.csv` authored-canonical rows for them. No `var_order`, register/volatile forcing, padding, inline assembly, fake return, target-byte embedding, or custom ABI is used. |
| OWNERSHIP-001 | denominator correction / shared-folded physical code | Packet 117 withdraws the earlier unique authored ownership of the 9-byte target body `0x00433130-0x00433138`. Fresh TH09 xrefs prove two distinct logical roles: exact `SoundPlayer::InitializeDSound` directly calls it after allocating the one-dword SoundManager object, while exact `PbgArchive::AllocEntries` takes the same address as the PbgArchiveEntry element-constructor callback. Natural `SoundManagerProcessView` and `PbgArchiveEntry` constructors independently reproduce all 9 bytes. The physical body is therefore shared/folded and its unique source/origin owner is unknown; the prior authored Pbg canonical row is removed from `matches.csv`, `implemented.csv`, and `reccmp-functions.csv`, while its match unit remains as diagnostic codegen evidence. The separate 3-byte body `0x00435EC0` is likewise left `unknown/review`: although SoundManager GetDirectSound is one source alias, fresh TH09 analysis reports nineteen call sites spanning many unrelated functions, so no unique SoundManager physical owner is claimed. | Passed TH09 `get_xrefs_to(0x00433130)` gives a SoundPlayer code xref and PbgArchive data/callback xref; committed TH095 independently documents the same retail constructor fold in its own target. This correction reduces authored/exact totals rather than preserving an overclaimed denominator. |
| SOUNDPLAYER-007 | exact / authored-game / ZUN-modified DSUtil | Packet 118 reconstructs CreateStreaming @ 0x004407A0-0x004409E1 (578 bytes) and CreateStreamingFromMemory @ 0x004409F0-0x00440C11 (546). Both materially consume TH09/ZUN ThBgmFormat, raw WaveFile state, and extended streaming manager/event/lock fields; the memory path deliberately calls OpenFromMemory with flags 0 and ignores E_NOTIMPL after state publication. | Fresh TH09 evidence plus two cold pinned-VC7.1 /O2 /Ob0 exact replays: 578/578 with 18 relocations and 546/546 with 16; adjacent source is corroboration only. |
| SOUNDPLAYER-008 | exact / authored-game / wave-wrapper owner | Packet 118 closes WaveFile constructor @ 0x0043FDA0 (19), OpenFromMemory @ 0x0043FDC0 (60), and Open @ 0x00440560 (118). Target-backed size is 0x94 with chunkSize +0x08, fileSize +0x2C, flags +0x78, memoryMode +0x7C, data/dataCursor/dataSize +0x80/+0x84/+0x88, raw wave handle +0x8C and ThBgmFormat pointer +0x90. The raw handle and game-format path are material ZUN extensions, so these bodies are authored-game despite DSUtil ancestry. | Fresh TH09 field writes/callers and two cold exact replays; committed TH095 corroborates only provenance. |
| BOUNDARY-106 | exact / authored-only | WaveFile ctor ends 0x0043FDB2 then 13 unowned CC; OpenFromMemory ends 0x0043FDFB then 4 CC before independent GetSize 0x0043FE00; Open ends 0x004405D5 then 10 CC; CreateStreaming ends 0x004409E1 then 14 CC; CreateStreamingFromMemory ends 0x00440C11 then 14 CC before independent 0x00440C20. | Passed native IDA extents/disassembly, target-byte seam reads, and canonical replay. |
| TOOLCHAIN-109 | exact / same-TU visibility / local profile | Caller-only natural source leaves CreateStreaming/CreateStreamingFromMemory at 602/566 bytes (598/566 under /Ob0). Defining target-backed WaveFile ctor/Open/OpenFromMemory in the same TU naturally closes the memory creator and helpers; file creation additionally requires local /Ob0, reaching 578/578. Packet-117 /Ob1 core remains separately exact. A real function-scope int result around SetNotificationPositions is compiler-significant: removing it makes both creators four bytes larger. | Focused /Ob0-/Ob1 probes, object alignment, two cold cross-entry exact passes; no forcing, padding, assembly, fake return, target bytes, or broad flag sweep. |
| SOUNDPLAYER-009 | exact / authored-game / ZUN wave lifecycle | Packet 119 reconstructs `WaveFileProcessView::ResetFile @ 0x0043FE10` (215 bytes), `Close @ 0x0043FFD0` (36), and `Reopen @ 0x004405E0` (63). ResetFile handles memory cursor/size publication and file seeks through `g_SoundPlayer + 0x6214` plus `ThBgmFormat` start/intro/total offsets; Close owns the ZUN raw `HANDLE +0x8C`; Reopen rejects memory mode or `INVALID_HANDLE_VALUE`, republishes the format pointer, resets, and mirrors chunk size to file size. | Two byte-identical cold pinned-VC7.1 `/O2 /Ob0` replays for all three functions; exact `SoundPlayer::ReopenBGM` relocates to Reopen. TH095 committed source corroborates provenance only. |
| SOUNDPLAYER-010 | exact / mixed provenance / sound object hierarchy | TH09 target fixes a `0x60` `SoundProcessView` base below the existing `0x7C` streaming object: buffer array `+0x04`, buffer size `+0x08`, WaveFile `+0x0C`, count `+0x10`, still-unknown state `+0x30`, `isPlaying +0x34`, `DSBUFFERDESC +0x38`, manager `+0x5C`; derived streaming state begins at `+0x60`. The 135-byte base constructor is authored-game because it publishes the extended persistent state, while the exact base destructor and exact 60/11-byte derived constructor/destructor remain Microsoft DSUtil scaffold exclusions. | Fresh native TH09 disassembly plus two cold exact class-hierarchy replays. The neutral `unknown30` name deliberately preserves unresolved meaning. |
| BOUNDARY-107 | reviewed / mixed provenance | Packet 119 corrects the prior continuation hypothesis: `0x004405E0` is `WaveFile::Reopen`, not a destructor. The actual WaveFile destructor is the 5-byte tail jump at `0x00440550`; `0x00440620` is its 33-byte VC7 scalar deleting wrapper. Reviewed seams leave alignment unowned: ResetFile→`0x43FEF0` has 9 `CC`; destructor→Open has 11; Reopen→wrapper 1; wrapper→base ctor 15; base ctor→dtor 9; base dtor→stream ctor 7; stream ctor→dtor 4; stream dtor→CreateStreaming 5. | Passed native disassembly/read-memory plus exact relocation manifests. No padding is charged to any function extent. |
| TOOLCHAIN-110 | exact / natural inheritance / ownership shape | A natural `SoundProcessView` base plus `StreamingSoundProcessView : SoundProcessView` preserves all five Packet-118 streaming exact units and reproduces all nine Packet-119 lifecycle extents under pinned VC7.1 `/O2 /Ob0`. The base destructor first emitted 101 bytes versus target 105; expressing standard null-guarded array/WaveFile deletion (`SAFE_DELETE_ARRAY` / `SAFE_DELETE` semantics) naturally produces the target 105-byte ownership flow. No `var_order`, register/volatile forcing, padding, assembly, fake return, embedded target bytes, or broad flag sweep is used. | Two cold cross-entry passes are byte-identical for all nine functions; Packet-118 creators and all fourteen SoundPlayer canonical units remain exact after the shared-header refactor. |

| SOUNDPLAYER-011 | mixed exact/source-present / authored-game | Packet 120 closes the central ZUN-modified SoundProcess refill seam. `SoundProcessView::FillBufferWithSound @ 0x00440060-0x004401CC` locks the complete DirectSound buffer, resets/reads the WaveFile, fills zero/short reads with 8-bit silence `0x80` or zero through `WaveFile +0x90 -> ThBgmFormat`, loops the source when requested, unlocks and returns `S_OK`; two cold pinned-VC7.1 `/O2 /Ob0` replays reproduce all 365 bytes with five reviewed relocations. `StreamingSoundProcessView::HandleWaveStreamNotification @ 0x00440270-0x004404C7` is the connected authored streaming data plane used by exact `SoundPlayer_BGMPlayerThread`: it protects the write window, restores/refills a lost buffer, handles short/looped reads, tracks `lastPlayPosition/playProgress/nextWriteOffset/fillNextNotificationWithSilence`, stops after silent tail playback and wraps the next-write offset. Maintained natural source is present but remains non-exact at a stable 609-byte object versus the 600-byte target. | Fresh passed TH09 IDA extents/call graph/full disassembly, target-backed `WaveFile +0x90` ZUN format access, committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` and committed TH095 `fb9d07b0dd9ad069a67ac18f367497bbb536b665` source-family comparison only after TH09 closure, and two cold repository-defined Fill replays with byte-identical receipt SHA-256 `db07f14328361e60c185f2c4cc4aa56c546519e52a05ee2f2ceab7a73b18aec0`. |
| DSUTIL-002 | zero-difference diagnostic / third-party | `StreamingSoundProcessView::Reset @ 0x004404D0-0x00440542` clears the four streaming counters/flags, runs the ordinary DSUtil `RestoreBuffer`/optional `FillBufferWithSound` sequence, resets the WaveFile and sets DirectSound position zero. Unlike the ZUN-modified refill/notification routines, its own body consumes no TH09/ZUN-specific storage and matches the unchanged Microsoft DirectX DSUtil streaming-reset family, so Packet 120 classifies it `third_party / SoundManager / exclude / high`. | Fresh target body/call graph, Microsoft DSUtil family preserved in committed TH08/TH095 `zwave.cpp`, and two byte-identical pinned-VC7.1 `/O2 /Ob0` diagnostic replays at 115/115 with three reviewed relocations; receipt SHA-256 `ed21ac30e81ee463acfd347986a7be0ad94a9ed9a4bcfa43f561376133e4ad83`. Diagnostic equality gives no authored exact credit. |
| BOUNDARY-108 | reviewed / mixed authored-third-party | Packet 120 closes all three logical/physical seams independently. FillBufferWithSound owns `0x00440060-0x004401CC`, followed by three unowned `CC` bytes at `0x004401CD-0x004401CF`; HandleWaveStreamNotification owns `0x00440270-0x004404C7`, followed by eight unowned `CC` bytes through `0x004404CF`; Reset owns `0x004404D0-0x00440542`, followed by thirteen unowned `CC` bytes through `0x0044054F` before independent `0x00440550`. No padding/table/tail bytes are charged to any reviewed function. | Fresh native-IDA function objects/full disassembly plus target byte reads across every exit/next-entry seam under passed `target:th09-main / factory-native-stdio` attestation. |
| TOOLCHAIN-111 | compiler-observed / honest non-exact boundary | Natural Packet-120 `/O2 /Ob0` source makes FillBufferWithSound 365/365 exact and Reset 115/115 exact, but the notification path remains 609 bytes versus target 600. Restricting the RestoreBuffer `BOOL` lifetime and the later read/update locals to sibling lexical blocks naturally recovers the target 0x20-byte stack frame and the target reuse of `[ebp-0x0C]`, yet total code remains 609. `/Ob1` also stays 609; dropping `/Zi` happens to reach 600 but matches only 304/576 comparable bytes and is rejected. Natural nested short-read and direct-skip variants remain 609, while reversing the fill/read branch reaches 600 but only 260/576 comparable bytes and relocations/control-flow diverge. The retained source is therefore the target-like 0x20-frame natural form and exact chasing stops before `var_order`, register/volatile forcing, padding, assembly or encoding tricks. | Focused current-source and ignored VC7.1 probes, normalized target/object instruction alignment, and two cold final negative controls with byte-identical SHA-256 `fb904b517d7ee066c8a66746b36cf0e217d698f1755e63e501e23c37153420b8`. |
| SOUNDPLAYER-012 | exact / authored-game / runtime helper completion | Packet 121 reconstructs three additional ZUN-modified runtime owners and closes the previously non-exact streaming notification. `SoundProcessView::SetVolume @ 0x0043FB10-0x0043FB7A` (107 bytes) reads `g_SoundPlayer +0x6218` and applies the quadratic BGM-volume curve. `WaveFileProcessView::Read @ 0x0043FEF0-0x0043FFC9` (218) preserves the DSUtil memory path while the file path consumes ZUN raw `HANDLE +0x8C` and remaining `chunkSize +0x08` through Win32 `ReadFile`. `SoundProcessView::Play @ 0x004401D0-0x0044026F` (160) drives the connected runtime helpers, clears fade state `+0x14/+0x18/+0x1C`, publishes priority/flags `+0x20/+0x24`, clears `+0x2C`, sets TH09-only state `+0x30` plus `isPlaying +0x34`, and calls DirectSound Play. Completing this same-TU runtime family also makes `StreamingSoundProcessView::HandleWaveStreamNotification @ 0x00440270-0x004404C7` canonically exact at 600 bytes, superseding Packet 120's source-present/non-exact result. | Fresh passed native TH09 analysis, committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` and committed-only TH095 `f93406f4488acb29240c048a17d36f67d28abf5f` hypotheses, two cold pinned-VC7.1 `/O2 /Ob0` replays for each selected helper, two additional cold notification replays, and complete affected-TU regression. Adjacent source supplies source-family/provenance hypotheses only; TH09 target fixes every address, field, extent and exactness claim. |
| DSUTIL-003 | zero-difference diagnostic / third-party | Packet 121 separately classifies four unchanged Microsoft DirectX DSUtil helpers as exclusions despite exact source reproduction: `SoundProcessView::RestoreBuffer @ 0x0043F9F0` (121 bytes), `GetFreeBuffer @ 0x0043FA70` (116), base `Reset @ 0x0043FC50` (62), and `WaveFileProcessView::GetSize @ 0x0043FE00` (4). RestoreBuffer implements the lost-buffer Restore/Sleep loop, GetFreeBuffer chooses the first idle buffer or `rand() % bufferCount`, Reset ORs `SetCurrentPosition(0)` across buffers, and GetSize returns WaveFile `fileSize +0x2C`. | Natural committed DSUtil family in TH08/TH095 plus fresh TH09 bodies/callers and two byte-identical cold pinned-VC7.1 diagnostic replays for all four functions. These 303 bytes are `third_party / SoundManager / exclude / high`; zero-difference codegen contributes no authored exact credit. |
| BOUNDARY-109 | reviewed / mixed authored-third-party | Packet 121 closes the runtime-helper physical seams independently. RestoreBuffer ends with `ret 8` at `0x0043FA68` and is followed by seven unowned `CC` bytes; GetFreeBuffer ends `0x0043FAE3` plus twelve `CC`; SetVolume ends `0x0043FB7A` plus five `CC`; base Reset ends `0x0043FC8D` plus two `CC`; GetSize is exactly four bytes through `0x0043FE03` plus twelve `CC`; WaveFile Read ends `0x0043FFC9` plus six `CC` before independent Close; Play owns through `0x0044026F` and notification begins immediately at `0x00440270` with no padding. | Fresh native-IDA full-function disassembly, inventory of each independent following candidate, and bounded target-byte reads under passed `target:th09-main / factory-native-stdio` attestation. No padding/table/tail byte is charged to any function. |
| TOOLCHAIN-112 | exact / same-TU completeness / natural source shape | Packet 121 demonstrates that the Packet-120 609/600 notification residual was not evidence for a special compiler flag or register-forcing pragma. The first natural runtime-helper draft makes six of seven selected functions structurally exact immediately; WaveFile Read is target-sized 218 bytes but only 100/214 comparable bytes because the file path is written after an early-return memory block. Restoring the committed DSUtil-family natural `if (memory) { ... } else { ... }` topology alone produces 214/214 comparable equality. With the connected RestoreBuffer, WaveFile Read/GetSize and SoundProcess runtime helper definitions now present in the same `SoundManager.cpp` translation unit, unchanged natural notification source compiles to the target 600 bytes instead of the prior 609 and closes all six relocations. | Two cold A/B replays are byte-identical for all seven Packet-121 helper units; notification receives its own two cold exact replays with receipt SHA-256 `7b04b2810bd648cc5c823ccaa4bf7a961d16fc615cd1ccc72a3dcc610bd32855`. No `/Zi` removal, `var_order`, register/volatile forcing, padding, assembly, fake return, target-byte embedding, or custom ABI is retained. |
| SOUNDPLAYER-013 | exact / authored-game / control-and-fade closure | Packet 122 reconstructs the connected SoundPlayer/SoundProcess control seam. `SoundPlayer::UpdateFades @ 0x0042FBE0-0x0042FC15` calls the four streaming fade updates when `bgm +0x6208` is non-null; `StreamingSoundProcessView::InitSoundBuffers @ 0x0043F890-0x0043F9E5` rebuilds the DirectSound buffer array and installs sixteen notification positions; `SoundProcessView::Stop/Pause/Unpause` close the ProcessQueues/StopBGM controls; and the four ZUN fade updates implement full and partial fade-in/out over `fadeType/currentFadeProgress/totalFade`. Nine authored functions totaling 789 bytes are repeatably canonical exact under their established VC7.1 profiles. | Fresh passed TH09 native-IDA bodies/callers/boundaries, committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` plus committed-only TH095 `c1610c4a10cf44b93e263b30fe0ecbf91f46b42b` source-family hypotheses, two cold A/B target-bound replays for all ten selected methods, and a 12-object-group affected-source regression with 51/51 configured units exact. |
| DSUTIL-004 | zero-difference diagnostic / third-party | `SoundProcessView::GetBuffer @ 0x0043FAF0-0x0043FB0E` is the unchanged Microsoft DirectX DSUtil accessor family: null buffer array or out-of-range index returns null, otherwise it returns `buffers[index]`. Packet 122 reproduces all 31 bytes in two cold builds but classifies the function `third_party / SoundManager / exclude / high`, so the zero-difference diagnostic adds no authored exact credit. | Fresh TH09 body/callers and physical seam, committed Microsoft DSUtil family preserved by TH08/TH095, and byte-identical cold `/O2 /Ob0` receipts with SHA-256 `db6b9e7e46118fef898ead664a025b21597c21812e7c0088f84351b1c204b8c2`. |
| BOUNDARY-110 | reviewed / denominator expansion | Packet 122 recovers a missing source-level target function at `0x0043FD20-0x0043FD50`: `StreamingSoundProcessView::UpdatePartialFadeIn`, 49 bytes. IDA initially attaches this range as a distant tail chunk of `SoundPlayer::UpdateFades @ 0x0042FBE0` because the only target xref is the compiler tail jump at `0x0042FC0F`. Independent VC7.1 compilation emits a distinct `UpdatePartialFadeIn` COFF symbol whose complete 49-byte extent and SetVolume relocation match the target, while the exact 54-byte `SoundPlayer::UpdateFades` caller has a separate relocation resolving to `0x0043FD20`. The repository candidate inventory therefore expands from 2,160 to 2,161 rather than preserving the analyzer's folded ownership assumption. | Passed native-IDA disassembly/xrefs plus raw seam bytes, committed TH08/TH095 four-method fade family, two byte-identical cold exact `UpdatePartialFadeIn` receipts (`c3ab01ead0e23abefd4554ea515967b7e3213b91f0dd7a863fa887df243f39c8`), and two byte-identical exact `SoundPlayer::UpdateFades` receipts (`c37a6128acbf1cacd1a8b474576c541d5c370e273e1d076b25583bedcfe2a1e8`). IDA exposes no create-function metadata operation, so the shared database retains its tail-chunk graph with an explanatory comment instead of a forced structural edit. |
| TOOLCHAIN-113 | exact / natural owner restoration | Packet 122's migrated source is structurally exact on the first successful compile for all ten selected methods. Restoring `GetBuffer/Stop/Pause/Unpause` to the natural `SoundProcessView` base owner changes one pre-existing caller relocation symbol in exact `SoundPlayer::StopBGM` from a temporary derived-class `void Stop` spelling to the natural base-class `int Stop` spelling while leaving relocation offset/type/target and target bytes unchanged. Updating only that replay-manifest symbol restores the complete regression: twelve object/profile groups covering both `SoundManager.cpp` and `SoundPlayerProcessQueues.cpp` replay 51/51 configured units exact. The explicit ProcessQueues negative control remains 1,456 object bytes versus 1,414 target bytes. | Failed-first regression receipt showing only the relocation-manifest symbol mismatch, corrected natural owner manifest, then a full cold grouped replay. No var-order pragma, register/volatile forcing, padding, assembly, fake path, target-byte embedding, or new compiler flag is used. |
| SOUNDPLAYER-014 | exact / authored-game / reusable audio dependency closure | Packet 123 closes the last anonymous/out-of-line helper chain directly consumed by the hard `SoundPlayer::ProcessQueues` frontier: `WaveFileProcessView::GetFormat @ 0x0043E420` (7 bytes), `SoundProcessView::GetWaveFile @ 0x0043E430` (4), `StreamingSoundProcessView::FadeOut @ 0x0043E440` (38), and `SoundPlayer::FadeOut @ 0x0043E4E0` (23). TH09 independently proves the +0x90 ThBgmFormat pointer, +0x0C WaveFile pointer, fadeType/current/total fields, float argument and SoundPlayer `bgm +0x6208` wrapper behavior. The base `SoundProcessView` GetWaveFile owner supersedes the earlier provisional derived declaration. | Fresh passed native-IDA bodies/callers/xrefs/raw seams, committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` and committed-only TH095 `f18d7c5e25087d8134146220917e2e33bfb2ab28` source-family hypotheses, and two cold target-bound A/B replays. Receipt SHA-256 values are GetFormat `028bbfb2c2de5cf126a695bb02732d5170e9e625df66299911d74f070c263d1a`, GetWaveFile `51c484734b3196f16d750d39f707d7ad94443c783fa9d7cae5ef29b363d00228`, streaming FadeOut `58aa2621de467dfdb590b9269422c845f2a4abb1666af36d8b5aa9794987fb57`, and SoundPlayer FadeOut `67f5f23c127eb2784ee4685694977f86b76c54feaa552930f2663ae19af644b4`. |
| BOUNDARY-111 | reviewed / audio-helper physical seams and analyzer correction | Packet 123 corrects IDA's provisional `Concurrency::details::VirtualProcessor::GetMaskId` alias at `0x0043E420`: TH09 callers and the exact natural source prove this is WaveFile GetFormat. GetFormat owns `0x0043E420-0x0043E426` then nine unowned `CC`; GetWaveFile owns `0x0043E430-0x0043E433` then twelve `CC`; streaming FadeOut owns `0x0043E440-0x0043E465` then ten `CC`; SoundPlayer FadeOut owns `0x0043E4E0-0x0043E4F6` then nine `CC` before exact InitializeDSound at `0x0043E500`. | Fresh native disassembly plus bounded target bytes under passed `target:th09-main / factory-native-stdio` attestation. No alignment byte is charged to authored extents. Shared IDA receives neutral names/comments only; no target bytes or unproven return prototypes are changed. |
| TOOLCHAIN-114 | hard-frontier / stable non-exact ProcessQueues diagnosis | Packet 123 re-attacks `SoundPlayer::ProcessQueues @ 0x0043F1F0`. A complete physical comparison now includes the logical 0x586-byte body, two alignment bytes and ten-entry jump table: target and maintained object are both exactly 0x5B0 / 1456 physical bytes, with the target logical `ret` still at relative `+0x585` and all jump-table destinations structurally accounted for, but only 409/1224 comparable ordinary bytes match across 58 object relocations. The first residual is the `ebp-4`/`ebp-8` local-slot swap at relative `+0x24/+0x30`; the first material CFG residual is opcode-1 preload tail sharing at `+0x8C`. `/Ob0` and no-`/Zi` are byte-identical to maintained `/Ob1`; defining all newly closed helpers in the ProcessQueues TU lowers the score to 399/1224; a natural explicit `shiftCursor` model grows/reorders the switch and is rejected. The split maintained TU form preserves the best 409/1224 result. | Two byte-identical cold full-physical negative receipts have SHA-256 `f7a114facbb5bcf9c1532a0b9e872ac0dfe6d9bfe1eb0a297411a7a6fdaedad1`. A full affected-source cold replay after natural GetWaveFile base-owner restoration covers twelve object/profile groups and 55/55 configured units exact. The dispatcher remains authored/source-present/NON-EXACT; no pragma, identifier-hash forcing, register/volatile trick, padding, assembly, or exact ledger row is introduced. |
| SUPERVISOR-029 | source-present / authored-game / TH09 netplay | `SupervisorServiceUpdate @ 0x00430AA0-0x00431100` is the TH09 Supervisor netplay service called twice by source-present `Supervisor::OnUpdate`. Fresh target evidence binds `g_SupervisorNetworkState @ 0x004B42D0` as a pointer to DirectPlay transport/frame-sync state: the service creates device/host addresses, hosts or connects a peer, retries sessions on randomized 4-8 second and 6-10 second windows, maintains two ten-entry 12-byte frame queues, exchanges 12-byte handshake/frame packets, publishes local/remote input into the shared 0x8E-byte input states, and reports synchronized/wait/disconnect/abort states through returns 0..4. The maintained free `__fastcall` seam preserves the observed ECX-only ABI without claiming the original source spelling or member/free-function identity. | Fresh passed native-IDA body/callers/callees/xrefs, DirectPlay-shaped helper bodies at `0x004323B0/0x00432840/0x004328F0/0x00432A10/0x00432AC0/0x00432F70`, frame-queue helpers `0x0042E9E0/0x0042EAC0/0x0042EB80/0x0042ECF0`, live-input helpers, target UI-string bytes, and pinned VC7.1 source feedback. Clean TH08 `a45e99fb1942714e6edded20847e32a654d56f97` and committed-only TH095 `6b81088e4d79f92d592d3eee3793b5f93c3f2f57` contain no direct equivalent netplay module and are used only for generic Supervisor/input family context. |
| BOUNDARY-112 | reviewed / hard-frontier physical owner | The Supervisor netplay service owns exactly `0x00430AA0-0x00431100`, 1,633 logical bytes. Exact `Supervisor::InitializeViewports` ends immediately before it at `0x00430A9F`; the service ends in `ret @ 0x00431100`; `0x00431101-0x0043110F` are exactly fifteen unowned `CC` bytes; independent source-present `Supervisor::OnUpdate` begins at `0x00431110`. No table, tail or padding byte is charged to the authored owner. | Fresh native function object plus bounded raw target bytes across both seams under passed `target:th09-main / factory-native-stdio` attestation. |
| TOOLCHAIN-115 | compiler-observed / improved-nonexact hard frontier | Natural `SupervisorServiceUpdate` source under the already established Supervisor-local VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy-` profile converges from 1,444 bytes to a stable 1,639-byte object against the 1,633-byte target. Each large improvement is target-backed rather than compiler forcing: fixing `0x004B42D0` from an embedded object to a pointer and then removing a whole-function alias restores the target-style repeated global reloads and moves 1,457 to 1,591 bytes; spelling the target-observed repeated `60 / syncRate` expressions and shared packet-not-ready early-return topology moves 1,591 to 1,639. Two final cold builds have byte-identical `.text` SHA-256 `8efd47a2837ee724dcde4d876d26234d1b2bfc0ad8b74d98b72ae25fa7a7041b`. The six-byte residual remains NON-EXACT; target uses a 0x14-byte local frame while the maintained source naturally compiles with 0x0C, and no evidence justifies local-order forcing. | Focused v1-v5 source probes and cold A/B replay. No flag sweep, `var_order`, volatile/register forcing, inert padding, assembly, fake path, target-byte embedding or exact ledger row is retained. |
| MIDI-001 | exact / authored-game / reusable-module | Packet 125 reconstructs the TH09 WinMM MIDI runtime as a target-backed 0x300-byte `MidiOutput` family with `MidiTimer` (0x10), `MidiDevice` (0x8), `MidiTrack` (0x20) and 0x17-byte channel state. Thirty-one uniquely owned authored functions totaling 3,911 logical bytes are canonical exact, covering device/timer lifecycle, standalone `Ntohs`/`Ntohl`, SMF parsing, file ownership, playback, fade processing, timer dispatch and the 1,227-byte event processor. Clean committed TH08 supplies the direct source family; committed-only TH095 supplies later stock-VC7 source-shape lessons, but TH09 independently fixes every address, extent, field and ABI. `MidiTimer::OnTimerElapsed`'s one-byte RET at `0x0043D2B0` is a shared/folded physical body with unrelated xrefs and therefore remains `unknown/review` with diagnostic codegen evidence only. | Fresh passed native-IDA target/layout/caller evidence, two cold target-bound replay passes for the module, committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97`, and committed-only TH095 `39466b03c4d5877e99e47b7f41216c74f0bf2cde`; live TH095 dirty paths were excluded. |
| BOUNDARY-113 | exact / compiler-artifact-owned | `MidiOutput::ProcessMsg` owns authored code `0x0043D7F0-0x0043DCBA` (0x4CB / 1,227 bytes). VC7.1 attaches 347 bytes of switch tables at `0x0043DCBB-0x0043DE15`, producing a complete 0x626 / 1,574-byte compared physical extent. Fresh target bytes show `0x0043DE16-0x0043DE1F` are exactly ten unowned `CC` bytes before independent `MidiTimer::StartTimer @ 0x0043DE20`. The canonical unit therefore counts only 1,227 authored bytes while comparing all 1,574 owned code/table bytes and all 43 relocations/internal labels. | Fresh IDA logical extent, target byte read across the table/alignment seam, 1,574/1,574 structural comparison, and two byte-identical cold canonical receipts with SHA-256 `f308d21c9bdd2c916f8b6235f651059f899015ae3fdacd168c76792f0182b9af`. |
| TOOLCHAIN-116 | exact / MIDI TU profile | TH09's MIDI translation unit requires C++ exception handling disabled: with otherwise identical pinned VC7.1 `/MT /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src`, `/EHsc` emits a 112-byte `MidiOutput::~MidiOutput` against the 58-byte target, while replacing only `/EHsc` with `/GX-` naturally emits the exact 58-byte destructor and all six target relocations. The same `/GX-` object keeps all thirty pre-existing MIDI units structurally exact; adding the destructor and `ProcessMsg` yields 32 configured MIDI diagnostics, 31 uniquely owned authored canonical functions, and one shared one-byte RET diagnostic. No flag sweep, `var_order`, register/volatile forcing, padding, assembly, fake return or target-byte embedding is retained. | Focused `/EHsc` versus `/GX-` falsification, full 31-unit `/GX-` replay, formal cold A/B module receipts, exact destructor replay, and exact ProcessMsg body-plus-switch-table replay. |
| TITLE-005 | source-present / authored-game / embedded MusicRoom | `TitleScreenView::OnUpdateMusicRoom @ 0x00426E05-0x004276D6` is the TH09 TitleScreen-owned music-room state machine. Unlike TH08's separate MusicRoom owner, TH09 embeds 32 `0x292` music descriptors at TitleScreen `+0xC934`, followed by selected/count/listing/pause at `+0x11B74/+0x11B78/+0x11B7C/+0x11B80`, a song-script owner pointer at `+0x11B90`, and the shared VM pointer at `+0x11B98`; eight description VMs are embedded TitleScreen VMs 49..56. The routine loads `title/music00.png`, parses `sprt/musiccmt.txt`, draws unlocked/locked titles, scrolls a ten-row list, reveals eight description lines, drives pause/unpause/replay/exit input, and calls exact audio/control dependencies. TH09-specific behavior wins over adjacent source: locked titles use `"%5s "`, fade-out is `20.0f`, and song setup uses the TitleScreen member at `+0x11B90` while description reset uses the separate global loaded-ANM owner. | Fresh passed native-IDA decompilation/call graph/layout evidence, bounded target disassembly, clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` MusicRoom source family, committed-only TH095 source as a later hypothesis, and maintained `src/TitleScreenMusicRoom.cpp`. Adjacent owners/addresses are not transferred. |
| BOUNDARY-114 | reviewed / authored-only | The MusicRoom update owns exactly `0x00426E05-0x004276D6` (0x8D2 / 2,258 logical bytes). Fresh native IDA independently identifies `TitleScreen_UpdateMenuSelection @ 0x004276D7` as the next 20-byte function, so there is no alignment/padding/table tail between them and no byte after `0x004276D6` is charged to MusicRoom. | Fresh attested function objects for both addresses plus the target-sized MusicRoom body and direct seam review. |
| TOOLCHAIN-117 | source-present / stable non-exact / adjacent-migration | Natural TH08-family source under the established TitleScreen pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Gr /O1 /Ob1 /Oy- /I src` converges from 1,861 bytes to a stable 2,208-byte object against the 2,258-byte target without codegen forcing. The largest causal correction is structural and source-backed: replacing one abstract description-index block with the eight explicit fixed-offset description cases used by the adjacent family moves 1,855 to 2,210 bytes. Reordering active/init states to match target physical order reaches 2,213. Fresh target evidence then corrects a more important semantic/owner mistake: song-script setup uses `this +0x11B90`, not the global description-script owner, and the song script index is the live 159-based domain; retaining those target-correct facts yields final 2,208 bytes. A final equivalent loop-bound probe (`i < trackCount + 159`) improves the target-correct form by 20 bytes but leaves a 50-byte residual and a 0x20 object frame versus the target 0x1C frame. Two cold final builds have byte-identical `.text` SHA-256 `ee895dfc1ee196ca7483637fd4eaa10656a887e78e83cd8d5a4cda22333c14df`. Exact chasing stops before declaration-order, register/volatile, padding, pragma, assembly, or broad flag search. | Packet-126 v1-v6 focused compiler feedback, target/object control-flow alignment, two cold final negative controls, and exact regressions for the three existing `src/TitleScreen.cpp` canonical units. No canonical match row is added. |
| ANM-017 | exact / authored-game / reusable-module | Packet 127 reconstructs the TH09 ANM script-execution seam around `AnmManager::ExecuteScript @ 0x00436F30`. TH09-local evidence fixes the old-engine 0x2A4 `AnmVm` layout, the 90-opcode dispatch domain (`opcode + 1 <= 0x5A`), the out-of-line ZunTimer integer conversion and comparison operators, and four operand helpers: `GetIntVar @ 0x00436B80`, `GetFloatVarPtr @ 0x00436C40`, `GetIntVarPtr @ 0x00436CD0`, and `GetFloatVar @ 0x00436E30`. Clean committed TH08 supplies the closest source family; committed-only TH095 is a later 17 KiB interpreter family and is not transferred as TH09 source. TH09 independently adds float-variable IDs 10010/10011/10012 backed by `g_ReplayRng` random-float, signed-random-float, and pi-range calls. The four operand helpers and the complete 9,358-byte interpreter are canonical exact. | Fresh target callers/decompilation/stack evidence, target disassembly, exact-caller relocation manifests, clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97`, target-specific RNG correction, and target-bound canonical replay. |
| BOUNDARY-115 | exact / reviewed compiler-artifact ownership | ANM operand helpers have smaller IDA logical authored bodies than their VC7.1 symbols because the compiler appends switch tables: `GetIntVar` is 144 authored / 184 compared bytes, `GetFloatVarPtr` 116 / 132, `GetIntVarPtr` 106 / 148, and `GetFloatVar` 203 / 256. Complete physical comparisons including tables/alignment are zero-difference and relocation-complete. `ExecuteScript` owns logical authored code `0x00436F30-0x004393BD` (0x248E / 9,358 bytes), followed by two compiler alignment bytes and 416 bytes of compiler tables from `0x004393C0` through `0x0043955F`; independent `ExecuteScriptArray` begins at `0x00439560`. The canonical comparison therefore covers 9,776 physical bytes while authored progress counts only the 9,358-byte code body. | Fresh target bytes/disassembly across every seam, target-bound exact replay of the four helpers, and complete 9,776/9,776 interpreter replay with all 368 relocations. |
| TOOLCHAIN-118 | exact / split ANM execution profile | The four ANM operand helpers remain exact under pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src`. The complete `ExecuteScript` owner requires the same profile with `/Ob0`: this retains the target's out-of-line math wrappers, while the source-local `ZunTimer` view must declare comparison operators out of line to retain the target calls in Wait and the interpolation loop. That natural TU boundary produces 9,358/9,358 logical and 9,776/9,776 physical bytes with 368 relocations. TH08's historical `#pragma var_order` is still not transferred; no declaration-order, register/volatile, padding, assembly, fake return, or target-byte shaping is used. | Focused `/Ob1` plateau analysis, opcode-entry relocation alignment, `/Ob0` falsification/confirmation, and repeated canonical exact replay. |
| ANM-018 | exact / authored-game / reusable runtime module | Packet 128 closes the TH09 ANM runtime entry/dispatch seam around the Packet-127 interpreter: `AnmVm::SetInterrupt @ 0x00406790`, `AnmManager::SpriteHasTexture @ 0x00423469`, `DrawPlayerBullet @ 0x00436D70`, `SetInterruptArray @ 0x00436DE0`, `ExecuteScriptArray @ 0x00439560`, `AnmLoaded::SetAndExecuteScript @ 0x004395A0`, and `ExecuteAnmIdxArray @ 0x00439630`. TH09 independently fixes the 0x2A4 VM stride, pending-interrupt field `+0x1FE`, draw selector `+0x200`, sprite/script fields `+0x214/+0x218/+0x21A/+0x224`, loaded-file slot base/stride `+0x38/0x120`, and the execution/interrupt caller graph. Clean committed TH08 supplies the direct natural source family only after those TH09 facts are fixed; committed TH095 is later-family corroboration only. All seven functions are repository-canonical exact. | Fresh passed native-IDA callers/decompilation/raw bytes, existing exact caller relocation manifests, clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97`, committed-only TH095 snapshot, two genuine cold A/B unit passes, and a 58-unit affected-header/connected regression. |
| BOUNDARY-116 | exact / reviewed compiler ownership | Packet 128 reviews all seven logical seams independently: SetInterrupt `0x00406790-0x004067A1` plus 14 unowned CC; SpriteHasTexture `0x00423469-0x00423497` immediately followed by independent code; DrawPlayerBullet logical code `0x00436D70-0x00436DC3` plus owned six-dword switch table `0x00436DC4-0x00436DDB`, then four unowned CC; SetInterruptArray `0x00436DE0-0x00436E27` plus eight CC; ExecuteScriptArray `0x00439560-0x00439594` plus eleven CC; SetAndExecuteScript `0x004395A0-0x0043962E` plus one CC; ExecuteAnmIdxArray `0x00439630-0x00439671` plus fourteen CC. Authored-byte accounting counts only the 483 logical C++ bytes; the 24-byte DrawPlayerBullet compiler table is comparison coverage, not extra authored source. | Fresh attested function objects and bounded target-byte reads across every boundary; DrawPlayerBullet canonical unit compares the complete 108-byte code-plus-table physical extent with all 13 relocations. |
| TOOLCHAIN-119 | exact / local profile split / profile ambiguity retained | Six Packet-128 ANM runtime methods naturally reproduce TH09 under the established pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` family. Header-inline-family `SpriteHasTexture @ 0x00423469` is 51 bytes under O2/Ob1 and O2/Ob0 but exactly 47 bytes under both `/O1 /Ob1` and `/O2 /Os /Ob1`; Packet 128 therefore uses the demonstrated O1 profile for its canonical unit while explicitly leaving the original emitting TU/profile ambiguous. No register/volatile forcing, `var_order`, padding, assembly, or broad flag sweep is used. | Focused O2/Ob0/O1/Os falsification, raw target/object byte alignment, two cold exact passes for all seven units, and unchanged exactness across 21 affected object groups / 58 configured units. |
| ANM-019 | exact / authored-game | `AnmManager::Project3DQuad @ 0x00439A90-0x00439CE2` is repository-canonical exact at 595/595 bytes with 25 reviewed relocations. The target-backed natural source shape requires stack-based `WINAPI` declarations for D3DX matrix rotation/multiply and `D3DXVec3Project`, double `0.0` rotation comparisons, a single `flagsWord >> 11` anchor snapshot reused across both anchor branches, and two out-of-line float-absolute calls to still-independent `0x0040F1C0`. Four projected vertices use the shared quad at `0x004DC558`; the context pointer remains at `0x004B3448`. The maintained source is void-shaped, while the original TH09 return contract and the source/data owner of `0x0040F1C0` remain unresolved. | Fresh TH09 disassembly and call graph, target/object instruction alignment, clean committed TH08 source-family comparison, followed by two byte-identical cold canonical receipts (`3bfa8c193b70c7e0899058033dc597a1895198ae4bb82676724e9fc3c6586858`). |
| TOOLCHAIN-120 | non-exact / source-lifetime boundary | The same Packet-129 D3DX stack-ABI correction improves maintained `AnmManager::ProjectCameraFacingQuad @ 0x00439820` from the Packet-103 615-byte object to stable 619 bytes versus the 622-byte target. Fresh target alignment shows a 0x90-byte frame and an explicit `Float3::operator-` hidden-return buffer copied into a separate delta local. Broadly restoring all TH08 local declarations grows the object to 652 bytes, and a narrower default-constructed delta/lifetime experiment also overgrows; no register, padding, `var_order`, volatile or encoding-directed fix is retained. Camera-facing projection therefore remains source-present/NON-EXACT while Project3D is promoted independently. | Two byte-identical cold camera negative receipts (`fc584555f02ecb03c96bafa4460e6a5e1a1b48d17368bfb18554c081507666ff`), fresh target disassembly and bounded rejected natural-lifetime probes. |
| ANM-020 | mixed exact / authored-game / reusable loader module | Packet 130 reconstructs the TH09 version-3 ANM loader/lifecycle seam as fifteen authored source functions spanning 2,670 logical bytes. Thirteen are repository-canonical exact: `CreateEmptyTexture @ 0x0043BBA0`, `LoadExternalTextureData @ 0x0043BBE0`, `ReleaseAnmEntry @ 0x0043BC80`, `AnmLoaded::LoadSprite @ 0x0043BCB0`, `AnmGetFormat @ 0x0043C430`, `CreateTextureFromFile @ 0x0043C470`, `CreateTextureFromAnm @ 0x0043C4C0`, `PreloadAnm @ 0x0043C7A0`, `LoadTextureData @ 0x0043C800`, `ReleaseAnm @ 0x0043CA30`, `PostloadAnmEntry @ 0x0043CD40`, `ServicePreloadedAnims @ 0x0043CDD0`, and `LoadAnm @ 0x0043CF90`. `ReadAnmEntries @ 0x0043C610` and `AnmManager::AnmManager @ 0x0043CE30` remain maintained source-present/NON-EXACT. TH09 independently fixes a 0x120-byte per-file slot with the `AnmLoaded` payload at +0x24, release flag +0x40 and path +0x44, a 256-slot service domain, `ReadAnmEntries`' 20-slot file-index guard, version-3 raw-entry semantics, external `@` texture behavior, and Supervisor D3D/shutdown fields at +0x08/+0x6B4. | Fresh TH09 callers/disassembly/physical seams, target-bound VC7.1 comparison, clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` source-family migration after TH09 closure, committed-only TH095 corroboration, and two cold exact function-receipt passes for all thirteen promoted functions. |
| BOUNDARY-117 | reviewed / mixed exactness | Packet 130 reviews all fifteen ANM loader/lifecycle logical extents independently. Every candidate ends at a target `ret`/`ret N`; following alignment bytes are excluded: BBA0-BBD1 +14 CC, BBE0-BC75 +10 CC, BC80-BCAF then immediate BCB0, BCB0-BD39 +6 CC, C430-C461 +14 CC, C470-C4BA +5 CC, C4C0-C609 +6 CC, C610-C795 +10 CC, C7A0-C7F9 +6 CC, C800-CA2D +2 CC, CA30-CAD2 +13 CC, CD40-CDCC +3 CC, CDD0-CE26 +9 CC, CE30-CF84 +11 CC, and CF90-CFCA +5 CC. No compiler padding or neighboring table is counted as authored source. The existing 90-byte `PreloadAnm` inventory boundary was revalidated, not newly corrected. | Complete target seam disassembly plus individual function extents and target-bound compile feedback. |
| TOOLCHAIN-121 | exact / adjacent migration / owner recovery | The established TH09 VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` profile closes thirteen Packet-130 loader functions without `var_order`, register/volatile forcing, padding or assembly. Several bodies do not consume `this`, but fresh TH09 caller instructions preserve/restore the AnmManager receiver in ECX before `LoadExternalTextureData`, `LoadTextureData`, `ReleaseAnmEntry`, and the three texture-creation helpers; restoring natural member ownership simultaneously closes the callers and callees. `AnmLoaded::LoadSprite` becomes exact only with the committed-family repeated `this->sprites[index]` source shape; `ReleaseAnm` becomes exact with the natural direct raw-data check followed by a persistent slot pointer. Thirteen target-bound function receipts are identical across two cold passes; whole COFF object SHA differs because non-function/debug metadata is not part of the exact claim. A shared-header regression across 22 real object/profile groups and 70 configured units is 70/70 exact after refreshing only fifteen compiler-local `$L...` spellings whose relocation offsets/types/target destinations were independently unchanged. | Canonical match-unit receipts, focused source-shape probes, positional relocation verification, and full affected-header cold replay. |

| ANM-021 | exact / authored-game / reusable-module | Packet 131 closes the TH09 ANM text/surface utility cohort at `0x0043BD40-0x0043C42F`: `DrawTextInner`, `DrawTextLeft`, `DrawTextCentered`, `ReleaseSurface`, two backbuffer-copy variants, `CaptureToTexture`, and `CaptureToSurface`. TH09 independently fixes the 32-slot surface plane at primary `+0x123FC`, secondary `+0x1247C`, staged bytes `+0x124FC`, staged sizes `+0x1257C`, info records `+0x125FC`, and capture through the first texture entry of a 0x120-byte ANM slot. The text chain proves the renderer split, formatting buffers and VM sprite/font/visibility fields. Clean committed TH08 and committed-only TH095 corroborate source shape only; TH09 decides layout, ABI and exactness. | Fresh target-attested IDA caller/decompiler evidence, maintained natural source, eight target-bound match units, recovered A/B receipts, and independent recovery cold replay. |
| BOUNDARY-118 | exact / authored-only | Packet 131 reviews eight logical extents: `0x0043BD40-0x0043BDBE` then one CC; `0x0043BDC0-0x0043BE4A` then five CC; `0x0043BE50-0x0043BF28` then seven CC; `0x0043BF30-0x0043BF90` then fifteen CC; `0x0043BFA0-0x0043C0CF` abutting the next function; `0x0043C0D0-0x0043C1F5` then ten CC; `0x0043C200-0x0043C2C0` then fifteen CC; and `0x0043C2D0-0x0043C42F` immediately before independent `AnmGetFormat @ 0x0043C430`. No alignment byte or adjacent function is absorbed into authored extents. | Reviewed inventory extents, complete target-bound function comparisons, fresh target decompilation at the text and surface anchors, and adjacent-function boundaries. |
| TOOLCHAIN-122 | exact / adjacent-migration / shared-header-regression | All eight Packet-131 functions are natural pinned-VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` zero-diff units, totaling 1,723 authored bytes and 49 reviewed relocations. Recovered cold A/B receipt pairs are byte-identical per function; an independent recovery cold build again returns 8/8 exact. `AnmManager.hpp` affects 23 real object/profile groups and 78 configured units. The interrupted first pass exposed only compiler-local `$L...` spelling drift in DrawPlayerBullet and `ascii-menu-state4-update`; offsets/types/solved targets were preserved, only label spellings were refreshed, and both the recovered second pass and independent recovery replay return 78/78 exact. No `var_order`, identifier-bucket search, register/volatile forcing, padding, assembly or embedded target bytes are used. | Eight A/B canonical receipt pairs, label-only structural receipts, recovered regression summaries, independent recovery 8-unit cold replay and independent 23-group/78-unit replay. |

| ANM-022 | exact / authored-game / reusable primitive-render module | Packet 132 closes ten TH09 AnmManager primitive-render functions totaling 1,741 authored bytes: cache resets `ClearBlendMode @ 0x0040E370`, `ClearVertexShader @ 0x0040E380`, `ClearColorOp @ 0x00415D00`, `ClearZWrite @ 0x00415D10`; `DrawVertices @ 0x0043B4D0`; `QueueSpriteQuad @ 0x0043B5C0`; TH09-specific maintained `DrawTriangleStrip @ 0x0043B670`; `DrawTriangleFan @ 0x0043B7B0`; TH09-specific maintained `DrawTexturedTriangleFan @ 0x0043B8F0`; and `SetupVertexBuffer @ 0x0043B9C0`. TH09 independently fixes manager caches `+0x12880/+0x12884..+0x12887`, quad vertex buffer `+0x12890`, four 20-byte software vertices at `+0x12894`, queued-sprite count `+0x128E4`, and fallback quad `0x004DC628`. The two TH09-specific draw names are neutral/descriptive; original source spelling remains unknown. | Fresh target-attested decompilation/callers, raw physical seams, exact existing render dependencies, clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` family migration and committed-only TH095 `b39a3bcce1cfdb0dc2e5358c3b293ef2fe3f7852` corroboration, followed by canonical cold replay. |
| BOUNDARY-119 | exact / authored-only | Packet 132 reviews all ten logical extents independently. The four cache-reset helpers each own exactly eight bytes and are separated from neighboring code by compiler `CC` alignment. `DrawVertices` owns `0x0043B4D0-0x0043B5BD` then two unowned `CC`; `QueueSpriteQuad` owns `0x0043B5C0-0x0043B662` then thirteen `CC`; `DrawTriangleStrip` owns `0x0043B670-0x0043B7AD` then two `CC`; `DrawTriangleFan` owns `0x0043B7B0-0x0043B8ED` then two `CC`; `DrawTexturedTriangleFan` owns `0x0043B8F0-0x0043B9B7` then eight `CC`; `SetupVertexBuffer` owns `0x0043B9C0-0x0043BB97` then eight `CC` before independent Packet-130 `CreateEmptyTexture @ 0x0043BBA0`. No alignment, table or neighboring function is charged to these authored extents. | Candidate inventory plus bounded raw target bytes across every entry/tail seam and complete target-bound compiler comparison. |
| TOOLCHAIN-123 | exact / adjacent migration / source-shape / shared-header regression | Natural pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` source closes all ten Packet-132 functions with 90 reviewed relocations. Eight functions are zero-difference on the first compile. An attempted source-only abstraction of the two untextured primitive bodies into a file-static shared helper leaves only 30-byte wrapper methods, contradicting the two independent 318-byte target owners; restoring the natural duplicated bodies immediately yields 318/318 for both without forcing. Two genuine cold passes replay all ten units exact with byte-identical per-function JSON receipts. Because `AnmManager.hpp` is shared, Packet 132 cold-rebuilds 24 real object/profile groups and 88 configured units. The first pass finds only compiler-local `$L...` spelling drift in `DrawPlayerBullet` and `ascii-menu-state4-update`; independent structural comparison proves unchanged ordinary bytes and relocation offset/type/solved-target sets before only those 15 label spellings are refreshed. The second full replay is 88/88 exact. | Focused v1/v2 compiler probes, ten canonical A/B receipt pairs, two label-only structural reports and complete 24-group affected-header replay. |
| TEXTHLP-001 | mixed exact/source-present | TH09 TextHelper is a reviewed 0x24-byte GDI/DIB helper with format +0x00, width +0x04, height +0x08, image size +0x0C, row bytes +0x10, HDC +0x14, original/bitmap GDI objects +0x18/+0x1C, and buffer +0x20. Packet 133 reconstructs fourteen repository-canonical exact authored helpers totaling 677 bytes: GetBuffer, GetHDC, GetImageWidthInBytes, GetWidth, GetHeight, IsAllocated, constructor, ReleaseBuffer, GetFormatInfo, CreateTextBuffer, ReleaseTextBuffer, destructor, TryAllocateBuffer, and AllocateBufferWithFallback. The 24-byte-stride format table at 0x004A1ED8 and shared text-surface/color/font data remain external dependencies with independent data-definition ownership. `GetFormat @ 0x00435EC0` remains origin/physical-owner unknown because Packet 117 proved its 3-byte body is folded across unrelated source aliases. | Fresh target-attested bodies/call graph and field accesses, clean committed TH08 TextHelper source family, committed TH095 text-render family as later corroboration, tracked natural source in `src/TextHelper.cpp/.hpp`, two cold target-bound structural-exact passes per promoted function, and final repository-defined exact match-unit replay. |
| BOUNDARY-120 | reviewed / mixed exactness | The Packet-133 TextHelper region is physically split into independent logical functions separated by pure CC alignment, not switch/tail ownership: GetBuffer 0x00435EB0-0x00435EB3 then 12 CC; folded GetFormat 0x00435EC0-0x00435EC2 then 13 CC; GetHDC/GetImageWidth/GetWidth/GetHeight each 4 bytes followed by 12 CC; IsAllocated 0x00435F10-0x00435F1A then 5 CC; ctor 0x00435F20-0x00435F3C then 3 CC; ReleaseBuffer 0x00435F40-0x00435F8D then 2 CC; GetFormatInfo 0x00435F90-0x00435FD1 then 14 CC; InvertAlpha 0x00435FE0-0x004361B7 then 8 CC; CopyTextToSurface 0x004361C0-0x004362B1 then 14 CC; Create/ReleaseTextBuffer each 26 bytes followed by 6 CC; dtor 0x00436300-0x00436304 then 11 CC; TryAllocateBuffer 0x00436310-0x0043644A then 5 CC; fallback 0x00436450-0x004364B4 then 11 CC; Bold renderer 0x004364C0-0x004367AE then one CC; normal renderer 0x004367B0-0x00436A96 then nine CC before independent 0x00436AA0. Padding remains unowned. | Current native-IDA extents, verified-target byte reads across every seam, provisional function decompilation/disassembly, and exact/negative compiler replay. |
| TOOLCHAIN-124 | negative-oracle / source-shape-open | The TH09 TextHelper module is not one uniform exact-codegen result. Natural pinned VC7.1 `/O2 /Ob1 /Oy- /Gr` closes fourteen helpers exactly. TH09-corrected InvertAlpha reaches the exact 472-byte target size but remains structurally non-exact; CopyTextToSurface emits 224 versus target 242 because the target uses a 0x4C frame and different local/register scheduling. RenderTextToTextureBold/RenderTextToTexture emit 758/750 versus targets 751/743; target code visibly reuses dead incoming parameter stack slots for `previousFont`/surface output while stock maintained source retains a dedicated local. `/O2 /Ob0` preserves these blockers, while `/O1` or `/O2 /Os` shrink the renderers to 718/703 and Alpha/Copy to 424/196. TH08's reconstruction-only `#pragma var_order` is not copied, and no volatile/register forcing, inert padding, assembly, target bytes, fake returns, or encoding-directed workaround is introduced. | Packet-133 natural v1-v6 source-shape probes, corrected bounded O1/O2/Os/Ob0 profile falsification, full target disassembly for Bold/Copy, final negative target-bound reports, and exact neighbor regressions. |
| SCREENFX-001 | mixed exact/source-present / authored-game / reusable-module | Packet 134 reconstructs the TH09 ScreenEffect family around `0x00422200-0x00422EDE` plus four direct reusable dependencies. TH09 independently fixes a 0x38-byte `ScreenEffect` layout: context/side index `+0x00`, effect type `+0x04`, calc/draw `ChainElem *` at `+0x08/+0x0C`, overlay alpha `+0x14`, duration `+0x18`, three raw parameters `+0x1C/+0x20/+0x24`, release flag `+0x28`, and embedded 12-byte `ZunTimer` at `+0x2C`. `RegisterChain @ 0x00422D20` is the eight-way owner/dispatcher, allocating exactly 0x38 bytes under registry tag `ScreenInf`, installing added/deleted lifetime callbacks, adding calc priority 3, and optionally adding the draw callback at caller priority. TH09 materially differs from direct adjacent-game copying: `SetViewport` delegates to exact `Supervisor::ConfigureGameplayViewport(2)`, shake is side-indexed through Supervisor `+0x160/+0x164` with 0xF0 stride, effect 6 draws two playfield rectangles, and the envelope path writes ANM shake fields `+0x1C/+0x20`. | Fresh passed native TH09 IDA bodies/callers/callees and physical extents, natural maintained `src/ScreenEffect.cpp/.hpp`, clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` ScreenEffect family, committed-only TH095 `b39a3bcce1cfdb0dc2e5358c3b293ef2fe3f7852` family, focused VC7.1 probes, and repository canonical replay. Adjacent source supplies hypotheses only; TH09 target decides addresses, fields, control flow and exactness. |
| SCREENFX-002 | exact / authored-game / reusable dependencies | Fifteen functions totaling 721 authored bytes are repository-canonical exact after two independent cold probe builds and a separate repository-defined match-unit replay: `ZunTimer::Initialize @ 0x004014A0` (15), `ZunTimer::operator<= @ 0x00406720` (20), `AnmManager::ClearTexture @ 0x004015F0` (11), `AnmManager::ClearSprite @ 0x0040E390` (11), `ScreenEffect::Clear @ 0x00422200` (139), `SetViewport @ 0x00422290` (38), `InitializeTimer @ 0x004228F0` (13), `DeleteScreenEffect @ 0x00422900` (47), `BeginFadeRelease @ 0x00422930` (18), constructor `0x00422950` (15), `DrawFullFade @ 0x00422B70` (89), `DrawArcadeFade @ 0x00422BD0` (62), `DrawPartialFade @ 0x00422C10` (62), `DrawSplitFade @ 0x00422C50` (113), and `DrawArcadePulse @ 0x00422CD0` (68). Every canonical compare returns `exact` with reviewed relocation destinations. | Two cold compiler-run structural-zero-difference receipts, fifteen configured match units under pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src`, and final repository canonical replay with 15/15 `exact`. |
| SCREENFX-003 | exact / authored-game / callback cohort | `ScreenEffect::CalcFadeIn @ 0x004222C0` (103 bytes, 6 relocations), `CalcFadeOut @ 0x00422470` (123 bytes, 8), and `CalcArcadePulse @ 0x004225B0` (163 bytes, 7) are canonical exact under the existing ScreenEffect VC7.1 `/O2 /Ob1` profile. FadeIn/FadeOut require the target-observed duration value to remain live from before the zero-duration branch through the timer comparison; the former eight-byte ordinary-code residual was caused by stack-spill placement, not x87 operand ordering. ArcadePulse loads unsigned alpha before its counter gate and likewise keeps duration local, recovering the target's eight-byte frame. Target logical extents end at `0x00422326`, `0x004224EA`, and `0x00422652`; following CC alignment is excluded. This promotes 389 authored bytes without changing any ABI or adjacent-game ownership claim. | Attested TH09 target disassembly and independent boundaries, relocation-aware complete target/VC7.1 comparisons, two cold canonical exact passes for all three and 11/11 exact same-TU neighbor replays in each pass. |
| SCREENFX-004 | exact / authored-game / semantic correction | `ScreenEffect::CalcFadeHold @ 0x004224F0-0x004225A9` is canonical exact at 186 bytes with 11 solved relocations. The old 173-byte candidate contained a real release-decay error: target float constant `0x41FF0000` is `31.875f = 255.0f / 8.0f`, not the maintained `128.0f / 8.0f`. The target also spills the hold duration before its zero check and reuses that value for timer comparison and division. The corrected natural source matches every byte without compiler steering; exactness is function-local and does not imply product closure. | Passed factory-native TH09 IDA disassembly and target constant read, positional target/object comparison, canonical manifest with all 11 relocation destinations, and two cold 15/15 same-TU exact replays. |
| BOUNDARY-121 | reviewed / denominator expansion / authored-only | `ScreenEffect::DeleteScreenEffect @ 0x00422900-0x0042292E` is an independent 47-byte authored function omitted from the initial candidate inventory. Native IDA exposes an independent function object; `RegisterChain` stores it as the calc-chain deleted callback; exact source clears that callback, cuts the draw element, nulls the pointer, scalar-deletes the object and returns zero. The candidate/origin inventory therefore expands from 2,161 to 2,162 rather than preserving an incomplete denominator. Separately, `0x00422330` remains an independent low-level draw helper whose observed callers are outside this ScreenEffect family; it is deliberately not assigned ScreenEffect ownership. `0x004343D0` remains an independently unresolved constructor-shaped dependency used by the VC7.1 vector-constructor path and is not assigned the local maintained vertex type. | Native IDA function objects/call graph, RegisterChain callback stores, exact 47-byte target-bound replay, inventory audit, and explicit ownership exclusion for neighboring/unresolved helpers. |
| TOOLCHAIN-125 | hard-frontier / mixed profile / honest non-exact | Packet 134 originally listed nine connected non-exact authored functions (2,213 reviewed bytes). Packets 512-513 supersede the FadeIn, FadeOut, ArcadePulse and FadeHold residuals; five (1,638 bytes) remain source-present/NON-EXACT and excluded from canonical matches. `ZunTimer::ZunTimer @ 0x00403390` is target 12 bytes but tested `/O2 /Ob1` and `/Ob0` forms emit 8. `CalcShake` is 325/316 (`/O1` 257); `CalcShakeEnvelope` is 333/336 (`/O1` 262). `DrawSquare` is 540/527 under `/O2`, where element construction expands, while `/O1` or `/O2 /Os` uses the target-observed VC7.1 vector-constructor helper but emits 522. `RegisterChain` is 536/447 under `/O2 /EHsc`, 480 with EH disabled, 412 under `/O1 /EHsc`, and 385 under `/O1` without EH. These are function/TU-local falsification results only; no project-wide flag claim follows. No `var_order`, register/volatile forcing, padding, assembly, target bytes, fake return or encoding trick is retained. | Focused Packet-134 VC7.1 O2/O1/Os/EH/no-EH probes, target/object disassembly and relocation comparison, plus Packets 512-513 supersession and exact connected-neighbor replay. |
| EFFECTSYS-001 | mixed exact/source-present / authored-game / adjacent-family migration | Packet 136 reconstructs the TH09 EffectSys manager as a target-backed dynamic per-side system rather than copying TH08's fixed 653-record EffectManager layout. TH09 independently fixes a 0x2DC manager, dynamic 0xD8 Effect records, three embedded 0xD8 draw sentinels at +0x3C/+0x114/+0x1EC, pool pointer/counts +0x30/+0x34/+0x38, side/opponent pointers +0x10/+0x14, calc/draw chains +0x18/+0x1C, draw tails +0x2C4/+0x2C8/+0x2CC, update counter +0x2D0, and slot-8 `etama.anm` storage +0x2D4. The 0xD8 record has manager/VM/count at +0/+4/+8, position/velocity +0x0C/+0x18, ZunTimer +0xAC, update/draw callbacks +0xBC/+0xC0, active/id +0xC4/+0xC5, draw-group controls +0xC8/+0xCA/+0xD0, dynamic vertices +0xCC and draw-list link +0xD4. Nine maintained functions are repository-canonical exact: Clear, both draw callbacks, ReleasePool, Release, Effect constructor, manager constructor, Create, and the recovered EffectUpdateFromVm callback. ResetPool, InitializeEffect, OnUpdate, and AddedCallback remain maintained source-present/non-exact. | TH09 native target bodies/raw bytes, GameManager create/release call graph, `EffectSysInf`/`etama.anm` target literals, EffectTemplate data references, natural VC7.1 compile feedback and two cold plus repository-canonical replays. Clean committed TH08 supplies only the broad Effect update/render resource family; its object/pool layout is not transferred. |
| BOUNDARY-122 | reviewed / denominator expansion / EffectSys | EffectSys logical extents are kept separate from alignment. Clear owns `0x0040C950-0x0040C95D` then 2 CC; ResetPool `0x0040C990-0x0040CA51` then 14 CC; InitializeEffect `0x0040CA60-0x0040CBF6` then 9 CC; hard OnUpdate `0x0040CDD0-0x0040CFAA` then 5 CC; OnDraw `0x0040CFB0-0x0040D066` then 9 CC; OnDrawShared `0x0040D070-0x0040D101` then 14 CC; AddedCallback `0x0040D1C0-0x0040D1F2` then 13 CC; ReleasePool `0x0040D200-0x0040D27A` then 5 CC; Release `0x0040D280-0x0040D2B2` then 13 CC; Effect ctor `0x0040D2C0-0x0040D2D1` then 14 CC; manager ctor `0x0040D2E0-0x0040D31C` then 3 CC; Create `0x0040D320-0x0040D466` then 9 CC. The initial inventory incorrectly treated the following code as a gap: raw PE data contains five absolute references to `0x0040D470`, all mapping into EffectTemplate update-callback slots, and direct disassembly proves an independent 87-byte callback `0x0040D470-0x0040D4C6`; 9 CC bytes follow before independent `0x0040D4D0`. IDA has no function object at `0x0040D470`, so the repository inventory is corrected without forcing an unavailable database create-function operation. | Native target reads/disassembly, PE section/file-offset mapping, five `.data` callback references, exact natural callback replay, and adjacent candidate seams. |
| TOOLCHAIN-126 | mixed exact/non-exact / function-local profile | Packet 136's maintained `src/EffectManager.cpp` uses stock VC7.1 only. Nine exact units replay under `/MT /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` with no `/EHsc`; this no-EH fact is materially required by `EffectManager::Create`, where EH-enabled lowering is larger while the natural no-EH body is exactly 327 bytes with 27 reviewed relocations. Two independent cold builds produce byte-identical per-function comparison receipts for all nine exact units, and the repository-defined match units replay exact. The hard owner `OnUpdate` remains 472 bytes versus target 475 under O2/Ob1 and Ob0, while O1/Os emits 417; ResetPool is 182/194 (O1/Os 179), InitializeEffect 369/407 (O1/Os 324), and AddedCallback is target-sized 51 but matches only 28/35 comparable bytes because stock VC7.1 chooses the opposite preserved-register assignment; O1/Os emits 50. Exact chasing stops there: no register forcing, volatile address trick, padding, inline assembly, target-byte material or custom compiler front-end is retained. | Focused O1/O2/Os/Ob0/EH-enabled/no-EH probes, complete relocation-aware comparisons, two cold A/B passes and final repository-canonical replays. |
| EFFECTSYS-002 | exact / authored-game / adjacent-family migration | Packet 137 closes the three TH09 EffectSys spawn APIs without importing TH08 layout facts. `EffectManager::SpawnEffect @ 0x0040CC00` scans the manager's dynamic primary pool through target-backed `nextEffectIndex +0x00`, wraps at `primaryCount +0x34`, constructs a zero velocity through exact `Float3::Float3`, and returns the `primaryCount + secondaryCount` sentinel on exhaustion. `SpawnEffectWithVelocity @ 0x0040CCC0` preserves the same ring scan with caller-supplied velocity. `SpawnEffectInFixedSlot @ 0x0040CD70` selects `effects[primaryCount + slotIndex]`, initializes it with zero velocity, stores the slot index at `Effect +0x90`, and returns the record. Clean committed TH08 HEAD `a45e99fb1942714e6edded20847e32a654d56f97` supplied only the natural three-method spawn-family hypothesis; TH09 independently fixed the dynamic 0xD8 record stride, pool counts, argument lists, sentinel rule, fixed-slot field and all addresses. | Passed native TH09 decompilation/caller review, existing exact Float3 constructor and Packet-136 EffectSys layout, bounded physical seams, clean committed TH08 family comparison, two final cold VC7.1 zero-difference passes and repository-defined canonical replay. |
| BOUNDARY-123 | exact / authored-only / EffectSys | The three Packet-137 spawn APIs are independent authored extents with compiler alignment excluded. `SpawnEffect` owns `0x0040CC00-0x0040CCB7`, followed by eight `CC` bytes through `0x0040CCBF`; `SpawnEffectWithVelocity` owns `0x0040CCC0-0x0040CD6E`, followed by one `CC` at `0x0040CD6F`; `SpawnEffectInFixedSlot` owns `0x0040CD70-0x0040CDC4`, followed by eleven `CC` bytes through `0x0040CDCF` before hard `EffectManager::OnUpdate @ 0x0040CDD0`. The target caller sets are broad—11, 6 and 5 references respectively—supporting API ownership rather than compiler-helper/folded-code classification. | Passed native function decompilation/callers plus target byte reads over all three tail/next-entry seams and complete canonical compiler comparison. |
| TOOLCHAIN-127 | exact / natural local lifetime / no-EH EffectSys profile | Under the same demonstrated Packet-136 EffectManager profile `/MT /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src`, velocity and fixed-slot spawn source are structural-exact on the first compile. The first natural zero-velocity ring source is 185 bytes versus target 184 because VC7.1 reuses EBX for `count` while the target keeps the current ring index live in EBX and decrements the count argument in its stack slot. Restoring that target-observed semantic lifetime as a normal `effectIndex` local—initialized from `nextEffectIndex`, updated on wrap, and refreshed before pointer advance—naturally produces 184/184 with no register directive or volatile trick. Final source uses the repository's real `Float3` declaration from `AsciiManager.hpp`, not a packet-local duplicate. Two final cold builds produce byte-identical receipts: SpawnEffect `c92d85e67ecf62c0969d791588d6f3e074c7c23c819fad5803b488a1d5cb130e`, velocity `75aa0ad1ef45d44321dbd5b86820cf9d962345e44b85b18f2963ba1a66f904df`, fixed-slot `0b29ee9afd18aafba42a95333d6aa4a9eb1098b98efba3a69824ddab8cfbf496`. All twelve EffectManager exact units from Packets 136-137 replay exact after the source/header change. | Focused v1/v2 target/object alignment, final cold A/B receipt pairs, three repository match-unit replays and complete twelve-unit EffectManager regression. No `var_order`, register/volatile forcing, padding, assembly, target bytes or fake returns are used. |
| EXATTACK-001 | mixed exact/source-present / authored-game / TH09-specific | Packet 138 corrects the initial address-proximity hypothesis around EffectSys: target `0x0040C810/0x0040C860` load `g_GameManager + 0xAC`, already reviewed as `secondarySubsystem`, and call `0x004151F0`; they are ExAttack consumers, not EffectManager functions. TH09 independently identifies the subsystem through `Register @ 0x004157D0`, which allocates 0x4C80 bytes and registers the target literal `ExAttackCtrlInf`. The controller owns side limits at +0x14/+0x18 and 257 records of 0x4C bytes starting +0x1C; slot 256 at +0x4C1C is the exhaustion sentinel. Each record has active +0x0C, ZunTimer +0x10, dynamicData +0x1C, position +0x20, parameter +0x38 and update/draw/release callbacks +0x40/+0x44/+0x48. Clean committed TH08 and committed TH095 contain no ExAttack/AttackCtrl family, so no adjacent implementation is claimed or transferred. | Passed TH09 GameManager ownership, native decompilation/disassembly, `ExAttackCtrlInf` literal/registration evidence, pool arithmetic, caller/callee review and focused compiler feedback. |
| BOUNDARY-124 | reviewed / mixed authored-unknown / ExAttack | Packet 138 reviews six independent extents and excludes alignment. Hard authored Spawn owns `0x004151F0-0x00415330`, followed by fifteen CC bytes before `0x00415340`. Authored file-local ReleaseExAttackRecords owns `0x00415650-0x00415695`, followed by ten CC bytes; authored Release owns `0x004156A0-0x004156E1`, followed by fourteen CC bytes. `ExAttackRecord` ctor owns `0x004157A0-0x004157AE`, followed by one CC; `ExAttackController` ctor owns `0x004157B0-0x004157CB`, followed by four CC. Authored Register owns `0x004157D0-0x004158D7`, followed by eight CC bytes before independent `0x004158E0`. Constructor authorship remains `unknown/review`: focused diagnostics show identical target bytes can be emitted by both explicit empty out-of-line and implicit compiler-synthesized constructor source, so exact bytes do not resolve origin. | Native inventory plus bounded target-byte seams and explicit/implicit VC7.1 constructor diagnostics. |
| TOOLCHAIN-128 | mixed exact/non-exact / local EH visibility | Natural Packet-138 source gives ReleaseExAttackRecords 70/70 and Release 66/66 exact immediately. Register is 196 bytes without EH but exactly 264/264 under focused `/EHsc /O2 /Ob1`, including 26 reviewed EH/allocation/callback/Chain relocations; the exact result depends on retaining natural out-of-line constructor visibility. The 15-byte record ctor is diagnostic-exact under normal EH O2, while the 28-byte manager ctor is diagnostic-exact under focused `/EHsc /O2 /Os`; neither receives authored exact credit because explicit-versus-implicit origin is undecidable from target bytes. Hard Spawn remains 314 bytes versus target 321 under both no-EH and EH O2/Ob1. Natural bounded-for variants expand to 386 and `/Os` shrinks the owner dramatically, so exact chasing stops before register forcing, volatile tricks, padding, assembly or encoded NOPs. Final promotion cold A/B receipts are byte-identical: ReleaseExAttackRecords `362a0c06a6cfb5669007c46585549ce605a89af5c604da61ec054f0c598e9206`, Release `c2d31cbb339033820c29aae39c517c896f7cba835acab4c18fb570cfecbb258a`, Register `d091d080307c64579ba65629adedbfeb2356da79dc3658f15d350df50b72ab76`; the retained Spawn negative receipt is `42e18c1a659c864d61748ab346c6ce8f9baa11ce6b02ad2cb54909fa24dda49c`. | Focused no-EH/EH/Os source-profile probes, complete target/object disassembly, implicit-constructor diagnostic and two final cold promotion passes. |

| PLAYER-001 | exact / authored-game / adjacent-source-accelerated lifecycle seam | TH09 independently identifies `PlayerRegisterChain @ 0x0041F170` through heap size 0x30F70 and registry tag `PlayerInf`. The object prefix is side index `+0x08`, side/opponent GameManager pointers `+0x0C/+0x10`, calc/draw-high/draw-low `ChainElem *` at `+0x14/+0x18/+0x1C`, and selector `+0x20`. The routine initializes the timer at `+0x303C8`, invokes independent player-type initializer `0x0041BDE0`, registers calc callback `0x0041E900` with added callback `0x0041EE50` at side+15, and draw callbacks `0x0041EB10/0x0041CDC0` at side+12/side+14. `PlayerRelease @ 0x0041CED0` calls independent cleanup `0x0041CE60`, cuts draw-high/draw-low/calc in that order, clears the pointers and CRT-frees the object. Clean committed TH08 supplies the broad Player registration/lifecycle source family only after these TH09-local facts are fixed; TH09 uses a dynamic side-local player object rather than blindly transferring TH08's static owner. | Passed native TH09 disassembly/decompilation, exact GameManager setup/teardown call graph, clean TH08 `a45e99fb1942714e6edded20847e32a654d56f97` family hypothesis, committed-only TH095 `4dcddc39aa9239f1bdf1dace884b0cdae272f920` header corroboration, and two cold repository canonical replays per promoted function. |
| BOUNDARY-125 | exact / authored-only | `PlayerRelease` owns `0x0041CED0-0x0041CF25`; `0x0041CF26-0x0041CF2F` are ten unowned `CC` bytes before independent `0x0041CF30`. `PlayerRegisterChain` owns `0x0041F170-0x0041F2B7`; `0x0041F2B8-0x0041F2BF` are eight unowned `CC` bytes before independent code at `0x0041F2C0`. Constructor `0x0041F070`, cleanup `0x0041CE60`, and the four callback targets retain independent origin/source/exactness states. | Native IDA complete function disassembly plus bounded raw target-byte reads across both tails and full target-bound compiler comparison. |
| TOOLCHAIN-129 | exact / source-shape correction | Natural pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` source makes `PlayerRelease` structural-exact immediately. The first TH08-shaped Register draft emitted 348 bytes versus target 328 only because IDA's provisional decompiler represented `AddToCalcChain` failure as `return NULL`. Full target disassembly instead shows failure skips the two draw registrations and reaches the common epilogue returning the Player pointer. Expressing exactly that natural shared tail removes the extra 20-byte epilogue and produces 328/328 structural equality. Two true cold repository replays are byte-identical: Register SHA-256 `7b170258d8eea35c71d0c9f5635b68a77380ad7356fb78a73f49989fb319fd76` with 29 relocations; Release SHA-256 `08bdeb44e61b26aed3718fafc21342e2aefe95b66220bdee1fb4e4b37b4e431d` with 8. No `var_order`, register/volatile forcing, padding, assembly, fake return or embedded target bytes is used. | v1/v2 focused structural probes, full target disassembly, two cold canonical A/B receipt pairs and reviewed relocation manifests. |

| PLAYER-002 | mixed exact/source-present / authored-game / adjacent-source-accelerated runtime cohort | Packet 141 reconstructs the Player runtime seam around exact `PlayerLifecycleView::OnUpdate @ 0x0041E900` plus four connected maintained dependencies: `LoadPlayerShtFile @ 0x0041BBE0`, `UpdatePlayerCollisionRegions @ 0x0041C8E0`, `UpdatePlayerBombState @ 0x0041CA00`, and `PlayerLifecycleView::AddedCallback @ 0x0041EE50`. TH09 independently fixes the dynamic side-local Player layout, side-state flags/shot type, collision lists at `+0xB100/+0xB908` with counts `+0xC110/+0xC114`, shared runtime field `+0x1095C`, Player frame/flag fields `+0x3044C/+0x1B80`, SHT cache/load/fixup behavior, and the 400/300/200 bomb-mode thresholds. Clean committed TH08 supplies the closest Player algorithm family only after those TH09-local facts are fixed; committed-only TH095 is later-family corroboration. Exact OnUpdate directly calls the maintained non-exact Collision/Bomb helpers, so caller exactness does not promote either callee. | Passed native TH09 IDA bodies/disassembly/call graph, exact Packet-140 RegisterChain binding, clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97`, committed-only TH095 `679e9b245a6b69fd17483a9e4a424f340f48825c`, maintained `src/PlayerRuntime.cpp` / `PlayerLifecycleView.hpp`, two cold target-bound passes for all five candidates, and repository-defined OnUpdate replay. |
| BOUNDARY-126 | reviewed / mixed exactness / Player runtime | Packet 141 reviews five independent logical extents and excludes alignment: SHT loader `0x0041BBE0-0x0041BC8B`, then four unowned `CC` bytes before `0x0041BC90`; Collision helper `0x0041C8E0-0x0041C9F0`, then fifteen `CC` before `0x0041CA00`; Bomb helper `0x0041CA00-0x0041CB4E`, then one `CC` before `0x0041CB50`; exact OnUpdate `0x0041E900-0x0041EB09`, then six `CC` before `0x0041EB10`; AddedCallback `0x0041EE50-0x0041F005`, then ten `CC` before `0x0041F010`. No padding is charged to authored bytes. | Current candidate inventory, complete target function disassembly, bounded raw target-tail reads, and target-bound compiler comparison. |
| TOOLCHAIN-130 | mixed exact/stable-non-exact / same-TU private ABI | Natural pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` closes hard `PlayerLifecycleView::OnUpdate` exactly at 522/522 with 40 reviewed relocations. Two source-shape facts are required and target-backed rather than encoding-directed: full target CFG uses explicit shared transition tails for states 4/2/1, and defining the two same-TU helpers naturally reproduces the target private call convention (`UpdatePlayerCollisionRegions` receives Player in EDI while `UpdatePlayerBombState` reuses Player in ESI). Correcting AnmManager to the pointer global at `0x004DC550`, shared field `+0x1095C`, and splitting the timing expression into target-order metric/table locals closes the last residual. Cold A/B OnUpdate receipts are byte-identical SHA-256 `94cc9c4c3b146660a521c0901f92f394486883638077226a704427623e499946`; repository unit `player-on-update` independently replays exact. Connected negatives are stable across cold A/B: AddedCallback 438-byte target-size but 302/358 ordinary bytes (`2a0052ee59b484e2cb1eb664582a76334a44f2835958f01f62753e1c79ec2d81`), SHT loader 172 bytes with 123/148 ordinary (`c9b466ee2cd8e8c8ecd6af728e28c1f73acc5dc0344d994c5e3e88ea8c908b86`), Collision 312 vs 273 (`a221926750b3a471dd14a63b721039da7b64f1a0034114dda820ab8d00aacc42`), and Bomb 328 vs 335 (`bb227b56d3b0850e388fe97a2de8e9292c49a31302907bf14bcd9c5977804ed9`). The SHT loader target uses a TU-private path-in-ECX/out-in-ESI/file-in-EDI allocation that maintained natural source does not reproduce; exact chasing stops without register/volatile/pragma forcing, padding, assembly, or embedded target bytes. | v1-v8 Added/loader probes, Collision/Bomb source-family probes, exact OnUpdate v1-v3 progression, two final cold passes, full relocation review and repository canonical replay. |

| PLAYER-003 | exact / authored-game / adjacent-source-accelerated render-and-lifecycle seam | Packet 142 closes four functions that directly complete the Packet-140/141 Player lifecycle: `OnDrawLowPrio @ 0x0041CDC0` (31), file-local `ReleasePlayerOwnedResources @ 0x0041CE60` (75), `OnDrawHighPrio @ 0x0041EB10` (166), and `PlayerLifecycleView::PlayerLifecycleView @ 0x0041F070` (243), 515 reviewed authored bytes total. TH09 independently fixes side-local viewport setup, shared draw gates `+0x1095C/+0x10960`, GameManager draw suppression byte `0x004A7ECD`, Player position `+0x1B88`, main VM `+0xC0/+0x2C8`, SHT cache `+0x30338`, and constructor member/array offsets through the 0x30F70-byte object. Clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` supplies only the broad Player draw and static DeletedCallback families after those TH09 facts are fixed. TH095 `0efe897e94dbed5383ec96e3ced87fc2aaedf45e` is live dirty and is consulted only through committed `HEAD:`; no TH095 live content is evidence. | Native TH09 target disassembly/decompilation/raw seams, Packet-140/141 exact caller bindings, focused adjacent committed source review, two cold target-bound passes, and four repository-defined exact match units. |
| BOUNDARY-127 | exact / authored-only / Player render-lifecycle | Packet 142 independently reviews four physical extents and excludes compiler alignment. Low draw owns `0x0041CDC0-0x0041CDDE`, followed by one unowned `CC` at `0x0041CDDF`; resource cleanup owns `0x0041CE60-0x0041CEAA`, followed by five `CC` bytes through `0x0041CEAF`; high draw owns `0x0041EB10-0x0041EBB5`, followed by ten `CC` bytes through `0x0041EBBF`; constructor owns `0x0041F070-0x0041F162`, followed by thirteen `CC` bytes through `0x0041F16F` before exact `PlayerRegisterChain @ 0x0041F170`. Constructor child destinations (`0x0041EE00`, `0x00403A10`, `0x0041CEB0`, `0x00403390`, `0x004343D0`, `0x0041F010`, `0x0041F030`, `0x0041EE20`, `0x0041EE30`, `0x0041F060`) retain independent origin/source/exactness states. | Attested IDA function extents plus bounded target-byte reads over all four tail/next-entry seams and full target-bound compiler comparison. |
| TOOLCHAIN-131 | exact / mixed function-focused VC7.1 profile / private helper ABI | High draw and low draw are naturally exact under pinned `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src`, at 166/166 with 13 relocations and 31/31 with three. Resource cleanup first emitted 79 bytes as a normal member because VC7.1 saved/restored ESI; expressing the TH08-family/TH09-evidenced operation as a same-TU static `ReleasePlayerOwnedResources(PlayerLifecycleView *)` naturally reproduces the target private convention that reuses Player in ESI and gives 75/75 with six relocations while exact `PlayerRelease` remains 86/86. The typed-member empty-body Player constructor emits 263 bytes under `/O2 /Ob1` because arrays expand to loops; `/O1` and `/O2 /Os` are target-sized but differ in four ordinary bytes. Focused `/O2 /Ob0` naturally selects VC7.1 vector-constructor helper `0x00401470` and gives exact 243/243 with 159/159 ordinary bytes and 21 relocations. `/O2 /Ob0` also leaves OnUpdate/High/Low exact but AddedCallback remains the same 302/358 ordinary-byte non-exact result. These are function-focused facts only, not proof of original TU/project flags. Cold A/B comparison receipts are pairwise identical for cleanup, Release, High, Low, OnUpdate, and constructor; full OBJ hashes differ because debug/output metadata differ. No register/volatile/pragma forcing, padding, assembly, fake return, or embedded target bytes is retained. | Focused `/O2 /Ob1`, `/O1`, `/O2 /Os`, `/O2 /Ob0` probes, same-TU static-helper probe, cold A/B receipts, and repository units `player-release-owned-resources`, `player-draw-high-prio`, `player-draw-low-prio`, `player-constructor`. |
| PLAYER-004 | mixed exact/source-present / authored-game / adjacent-source-accelerated shot runtime | Packet 143 reconstructs the TH09 128-slot Player shot runtime from TH09 target evidence with clean committed TH08 `Player::UpdateShots` / `DrawActiveShots` / `DrawHitShots` used only as source-family hypotheses. TH09 independently fixes a 0x484 shot stride rooted at Player +0xC11C, VM at +0x000, position +0x2A4, velocity +0x43C, timer +0x454, state +0x462, shot type +0x464, tint byte +0x46E, fastcall update callback +0x474 and draw callback +0x478. `DrawActiveShots @ 0x0041F680` is canonical exact at 211 bytes and includes the TH09-specific callback-or-Draw2D dispatch; `DrawHitShots @ 0x0041F760` is canonical exact at 164 bytes and dispatches through `DrawPlayerBullet`. `UpdateShots @ 0x0041F580` is maintained source-present/non-exact at 251 target bytes: it implements the side freeze gate, optional update callback, framerate-scaled movement, shot-type-2 bounds exemption, ANM update and timer increment without forcing compiler registers or layout. | Native TH09 decompilation/disassembly/caller review, exact connected OnUpdate/High/Low draw units, clean committed TH08 HEAD `a45e99fb1942714e6edded20847e32a654d56f97`, two cold A/B VC7.1 comparisons, and repository-defined exact replay for both draw passes. TH095 HEAD `68f11dfcc83086b9c28c67a24f2168deac0163a7` was live-dirty and contributed no implementation evidence. |
| BOUNDARY-128 | reviewed / mixed exact-source-present / Player shot runtime | `UpdateShots` owns `0x0041F580-0x0041F67A`, followed by five unowned `CC` bytes through `0x0041F67F`; exact `DrawActiveShots` owns `0x0041F680-0x0041F752`, followed by thirteen unowned `CC` bytes through `0x0041F75F`; exact `DrawHitShots` owns `0x0041F760-0x0041F803`, followed by twelve unowned `CC` bytes through `0x0041F80F` before independent 1210-byte `PlayerChargeAttackView::UpdateChargeAttack @ 0x0041F810`. `PlayerLifecycleView::DrawTailStates @ 0x0041DF80` is now independently canonical exact. `UpdateChargeAttack` is independently maintained source-present/non-exact and remains TH09-specific rather than transferred from TH08. | Native function objects, callers/decompilation, bounded target-byte tail reads, exact DrawTailStates replay, and Packet 447 charge-attack source closure. |
| TOOLCHAIN-132 | hard-frontier / source-present non-exact / function-local profile falsification | Natural `PlayerLifecycleView::UpdateShots` source is a stable near match but not canonical exact. Under pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src`, the maintained external `PlayerPositionView::operator float *()` form emits 254 bytes versus the 251-byte target in both cold builds. `/O1 /Ob1` and `/O2 /Ob1 /Os` emit 252 bytes; `/O2 /Ob0` emits 254. The baseline extra three bytes arise from an 8-byte local frame where the target uses 4 bytes and rereads the converted position pointer for the bounds call. Natural alternatives were falsified rather than retained: direct x/y bounds fields emit 259 bytes, repeated external conversion expressions emit 282, an in-class TH08-style `operator float *(){ return (float *)this; }` with a local pointer emits 261, and the same in-class helper with TH08 repeated expressions also emits 261. No `#pragma var_order`, register/volatile forcing, padding, assembly, target-byte encoding, fake return or custom compiler trick is retained. | Focused source-shape/profile probes, object/target disassembly and two cold negative receipts. This is function-local evidence only and establishes no whole-TU/project optimization setting. |
| PLAYER-005 | mixed exact/source-present / authored-game / adjacent-source-accelerated added-state seam | Packet 144 reconstructs the Player post-resource Added-state initialization family around hard `PlayerLifecycleView::InitializeAddedState @ 0x0041EBC0` and three direct reset helpers. TH09 independently fixes a 512-record 0x44 collision pool at Player `+0x28BC`, active/released pointer arrays at `+0xB100/+0xB908`, 128 0x484 shot records at `+0xC11C`, position/history at `+0x1B88/+0x1BA0`, owner state at `+0x30410`, tail storage at `+0x30454`, and target-local timers/flags/state tables. Clean committed TH08 HEAD `a45e99fb1942714e6edded20847e32a654d56f97` supplies only the `Player::AddedCallback` / `PlayerCollisionRegion::Reset` source family; TH09 changes record size/layout, side state and mode logic. Three direct helpers are repository-canonical exact: `PlayerAddedCollisionRegionView::Reset @ 0x0041BDA0` (23), `PlayerAddedHeaderResetView::Reset @ 0x0041BDC0` (29), and `PlayerAddedOwnerStateView::Reset @ 0x0041D7E0` (41). The 552-byte hard owner is maintained source-present/non-exact. | Passed native TH09 decompilation/disassembly/callers, target-bound physical seam reads, clean committed TH08 family review, two cold VC7.1 A/B comparisons and three repository-defined exact match-unit replays. TH095 HEAD `ca41ed20798e1b5fd11a2d9b4c88607f9d841233` was live dirty and contributed no implementation source. |
| BOUNDARY-129 | reviewed / mixed exact-source-present / Player Added-state | Exact collision reset owns `0x0041BDA0-0x0041BDB6`, followed by nine unowned `CC` bytes; exact header reset owns `0x0041BDC0-0x0041BDDC`, followed by three `CC`; exact owner-state reset owns `0x0041D7E0-0x0041D808`, followed by seven `CC`. Hard `InitializeAddedState` owns the 552-byte logical body `0x0041EBC0-0x0041EDE7` plus a physically associated 20-byte five-entry switch table `0x0041EDE8-0x0041EDFB` whose target entries are `0x0041ED71, 0x0041ED71, 0x0041ED74, 0x0041ED79, 0x0041ED7E`; four `CC` bytes through `0x0041EDFF` precede independent `0x0041EE00`. The switch table is compiler-owned physical extent and is not added to the 552 authored-byte denominator. | Attested IDA function objects/call graph, verified-target PE byte reads through repository `target_identity.pe_bytes_at`, full 572-byte target/object comparison and candidate inventory seams. |
| TOOLCHAIN-133 | hard-frontier / source-present non-exact / owned-switch-table comparison | Natural `PlayerLifecycleView::InitializeAddedState` under pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` emits a 552-byte logical body followed by a 20-byte local switch table, exactly matching the target owned extent length and all 25 relocation destinations, but only 407/472 comparable ordinary bytes match. `/O2 /Ob0` and EH-disabled O2/Ob1 are byte-identical to the maintained result; `/O1` and `/O2 /Os` emit a 560-byte owned extent. Two natural lifetime probes were rejected rather than retained: moving collision pointer lifetimes before position setup drops to 378/472, while an explicit incrementing position-history pointer remains 407/472. No `#pragma var_order`, register/volatile forcing, padding, inline assembly, fake return or target-byte encoding is retained. By contrast, the three direct reset helpers naturally replay exact at 23/29/41 bytes. | Focused O1/O2/Os/Ob0/EH/no-EH and source-lifetime probes, full body+switch-table comparison, two cold negative hard-owner receipts and exact helper cold/canonical replays. |

| PLAYER-006 | source-present / reviewed-authored / hard death-transition seam | Packet 145 reviews the contiguous Player death seam: `PlayerLifecycleView::EnterDeathState @ 0x0041E420` (753 bytes), private state-4 updater `PlayerCheckState4 @ 0x0041E720` (400), and private trigger stage `PlayerUpdateStageC @ 0x0041E8B0` (74). TH09 independently fixes EnterDeathState's death sound, side counter decay, 200/300/400 attack-resource branches, fallback effect, state/timer transition, replay-RNG vector, transition color/frame writes, VM cleanup, global counter adjustment, and Packet-144 resets. Clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` supplies only the broad `Player::Die` / `UpdateDeathAndRespawn` family; TH09 fixes all addresses, fields, CFG, boundaries and ownership. | Fresh native TH09 decompile/disassembly/call graph under passed `factory-native-stdio` attestation, exact OnUpdate caller evidence, verified target bytes, maintained `src/PlayerDeath.cpp`, clean committed TH08 family review, and focused VC7.1 diagnostics. TH095 committed HEAD `3e4a51db1c1bac7841be1a83afe1e2e1b7e46a73` was live dirty and supplied no implementation source. |
| BOUNDARY-130 | reviewed / authored-only / Player death transition | EnterDeathState owns `0x0041E420-0x0041E710`; fifteen unowned `CC` bytes through `0x0041E71F` precede `PlayerCheckState4 @ 0x0041E720`. PlayerCheckState4 owns `0x0041E720-0x0041E8AF` and directly abuts private `PlayerUpdateStageC @ 0x0041E8B0`. PlayerUpdateStageC owns `0x0041E8B0-0x0041E8F9`; six unowned `CC` bytes through `0x0041E8FF` precede exact `PlayerLifecycleView::OnUpdate @ 0x0041E900`. | Candidate inventory, native IDA extents/disassembly, exact OnUpdate relocation evidence, and verified-target PE reads through repository `target_identity.pe_bytes_at`. |
| TOOLCHAIN-134 | hard-frontier / stable non-exact / private-ESI ABI blocker | Natural maintained EnterDeathState under pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` is stable at 705 bytes versus the 753-byte target in two cold builds; focused `/O2 /Ob0` also emits 705, while `/O1 /Ob1` and `/O2 /Os /Ob1` emit 680. The cold A/B full negative receipts are byte-identical (SHA-256 `09f4e35f57f23f15dfb83a82da924530d0aa531f76e9c79d16ffc5bf0df0fa90`) and their 705-byte prefix diagnostics are byte-identical (SHA-256 `e6762e309dc430ec43d2656ba2fbfccf971e6f777a91b10ba17ec28ac027cd9a`). Natural corrections remove accidental side helper calls, reproduce target zero-EBX lifetime, restore the 400-threshold block order and conditional counter writeback. No var-order/register/volatile forcing, padding, assembly, fake returns or target bytes are retained. The adjacent 400-byte PlayerCheckState4 and 74-byte PlayerUpdateStageC consume Player directly from caller-preserved ESI and never establish `this` from ECX, so standalone source is deliberately withheld rather than fabricating an ABI. All twelve existing Player canonical units from Packets 140-144 fresh-replay exact. | Packet-145 v1-v4/v4b diagnostics, O1/Os falsification, target disassembly for the private state-4 updater, private-entry ABI inspection, and fresh 12-unit Player replay. |

| PLAYER-007 | source-present / authored-game / hard collision-query owner | Packet 146 reconstructs `PlayerCollisionQueryStateView::FindCollision @ 0x0041D810-0x0041DA69` as the Player `+0x36C` embedded collision-query owner. TH09 independently fixes `player00 +0x00`, 128 records of 0x30 bytes at `+0x04`, `count1804 +0x1804`, Player query bounds at `+0x1C60/+0x1C6C`, position `+0x1B88`, hurtbox half-size `+0x1CA8`, and record fields position `+0x00`, origin `+0x0C`, half-size `+0x18`, radius `+0x24`, angle `+0x28`. Four target call sites in the 0x004049A0 gameplay owner pass center/half-size/extra-radius on the stack with ECX adjusted to Player+0x36C. The routine dispatches target-local box/laser/circle helpers and restores Player scratch on all exits. Clean committed TH08 provides only lower collision-helper source families; TH09 owns the combined query algorithm and layout. | Fresh passed native TH09 decompilation/callers, verified-target disassembly, maintained `src/PlayerCollisionQuery.cpp`, exact constructor/OnUpdate regressions, and two cold negative VC7.1 replays. |
| BOUNDARY-131 | reviewed / authored-only / Player collision query | `FindCollision` owns exactly `0x0041D810-0x0041DA69` (602 bytes), ending in `ret 0x0C`. Six unowned `CC` bytes occupy `0x0041DA6A-0x0041DA6F`; independent candidate `0x0041DA70` begins immediately after. The preceding exact `PlayerAddedOwnerStateView::Reset` ends at `0x0041D808` with seven unowned `CC` bytes through `0x0041D80F`, independently bracketing the hard owner. No alignment byte is charged to authored source. | Candidate inventory plus verified-target disassembly across both seams and target-bound compiler comparison. |
| TOOLCHAIN-135 | hard-frontier / stable 4-byte codegen residual | Packet 146 recovers the major VC7.1 source-shape facts without compiler forcing. Removing a file-static restore helper, keeping query-state in the object, expressing independent box/laser/circle hit tails, and splitting the two early-return guards moves natural `/O2 /Ob1` code from 399 to 461, 477/478, 615, then 606 bytes against the 602-byte target. The final target-shape source has the exact 0x24 local frame and the target loop/collision CFG. `/O2 /Ob0` also emits 606; `/O1 /Ob1` and `/O2 /Os /Ob1` emit 357 and are rejected. Pointer-parameter and several natural declaration-lifetime probes do not improve the final target shape and are not retained. Two final cold `/O2 /Ob1` negative receipts are byte-identical with SHA-256 `7fa6b8a18a26e8458292552578583b68ae070f0d525843bc804970d578b1f8f6`. Constructor 243/243 and OnUpdate 522/522 remain exact after the `+0x36C` typed-layout refinement. No `#pragma var_order`, register/volatile forcing, inert padding, assembly, fake returns or target-byte encoding is used. | v1-v12 focused source-shape probes, O1/O2/Os/Ob0 falsification, target/object instruction alignment, cold A/B negative receipts and connected canonical regressions. |
| PLAYER-008 | mixed exact/source-present / authored-game / collision helper module | Packet 147 closes the lower Player collision seam consumed only by the two reviewed `Player +0x36C` query owners. `PlayerLifecycleView::CalcItemBoxCollision @ 0x0041BDF0` is canonical exact 121/121 and compares caller-provided center/half-size against query bounds `+0x1C60/+0x1C6C`. `CalcLaserHitbox @ 0x0041BFC0` is canonical exact 422/422 with eleven reviewed relocations; TH09 independently fixes a pure geometry return contract (1 base overlap, optional 2 expanded-overlap, else 0) and, unlike the committed TH08 family, performs no Die/AwardGraze side effects. `CalcCircleCollision @ 0x0041BE70` and `PlayerCollisionQueryStateView::FindCollisionAtPlayer @ 0x0041DA70` are maintained source-present/NON-EXACT at exact target sizes 106/279. The sibling derives bounds from current Player position/hurtbox and scans the same 128 x 0x30 record pool; its sole caller is private `PlayerUpdateStageC`. | Passed TH09 native IDA callers/disassembly, verified private target bytes, clean committed TH08 collision-family hypothesis, natural `src/PlayerCollisionHelpers.cpp`, two cold A/B comparisons, and repository-defined exact replays for the box/laser helpers. |
| BOUNDARY-132 | reviewed / mixed exact-source-present / Player collision helpers | `CalcItemBoxCollision` owns `0x0041BDF0-0x0041BE68`, followed by seven unowned `CC` bytes through `0x0041BE6F`; `CalcCircleCollision` owns `0x0041BE70-0x0041BED9`, followed by six unowned `CC` bytes through `0x0041BEDF`; `CalcLaserHitbox` owns `0x0041BFC0-0x0041C165`, followed by ten unowned `CC` bytes through `0x0041C16F`; `FindCollisionAtPlayer` owns `0x0041DA70-0x0041DB86` before independent `0x0041DB90`. The first three tail gaps are directly confirmed from the verified local PE; the sibling function extent is independently native-IDA reviewed and its gap remains outside the authored extent. | Candidate inventory, complete native function disassembly, bounded verified-target PE reads, caller graph, and complete target-bound compiler comparison. |
| TOOLCHAIN-136 | mixed exact/near-match / natural C++ lifetime evidence | Natural pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` closes `CalcItemBoxCollision` immediately at 121/121 and hard `CalcLaserHitbox` immediately at 422/422. Circle source first emitted 89/106; restoring a target-visible hidden-return then local-copy lifetime produces the exact 106-byte extent, and the best natural arithmetic form matches 88/102 ordinary bytes, leaving only the x87/status-word comparison tail. `FindCollisionAtPlayer` starts at 282/279; shortening the Player lifetime to match target per-call reloads reaches exact size 279 with 175/243 ordinary bytes, but remaining bounds/base register scheduling is not forced. Cold A/B receipts are byte-identical: box `1589ac52827a945b032bc3567bfea0888dc1a9d5bd58cc33bd09bf17bdf08e4f`, laser `1715b72f577062fa632bf2d3c68539e82c78c0c7e7f192bc34f3e362acbce744`, circle negative `c446e5a3be664c6e66fdbf40a7cf6db0d5f72ca9d5076f5a2f584acfe7ac1328`, sibling negative `3a81451a12cb83474df0c434335b4543dc5a79497c58dd91a21ea60b01dcceea`. No `var_order`, register/volatile forcing, padding, assembly, fake return or target-byte material is retained. | v1-v6 natural source-shape experiments, complete target/object disassembly, cold A/B receipts, and two repository canonical units. |
| ECL-001 | reviewed-authored / hard interpreter frontier | `EclManager::RunEcl @ 0x004086C0` is TH09's enemy ECL interpreter. `EnemyManagerView::OnUpdate` passes its manager base in ECX (the first embedded ECL manager) and an `EnemyView *` stack argument, tests the return for `-1`, and owns the only normal per-enemy call site. Two additional helper owners provide four more call sites. The interpreter dispatches 187 main opcodes, has 60 target callees, and starts by binding the active context/call-stack pointers at Enemy `+0x2CE0/+0x2CE4` to `+0x7F4/+0xA20`. TH08 and TH095 contain useful ECL source families but are not layout evidence: TH09 independently fixes a `0x5430` Enemy, while the adjacent layouts differ. | Hash-attested TH09 IDA prologue, call instruction at `0x00410AC6`, callers/callees, complete switch audit, and local verified-target bytes. |
| ENEMY-001 | source-present / authored-game / death-reward hard subowner | `EnemyView::HandleDeathRewards @ 0x004102B0` is the sole death/reward dispatcher called by `EnemyManagerView::OnUpdate`. TH09 independently fixes Enemy manager pointer `+0x0000`, positions `+0x2D74/+0x2DD4`, reward token `+0x335C`, death-effect variant `+0x3368`, reward flags `+0x3380`, manager side pointers `+0x31C/+0x320/+0x324`, 128 enemies at manager `+0x5758` with `0x5430` stride, and the opposing reward-enemy cap at manager `+0x2AC3B8`. The routine resets/configures Player `+0x30410`, accumulates a target-observed defeat scalar through Player `+0x30388`, selects sound/effect/attack parameters, and spawns a cross-screen reward effect only for tiers 1-2, hit kinds below 2, and an opposing count below 25. | TH09 decompilation plus complete disassembly/callee/caller review, target constructor/reset evidence, maintained `src/EnemyManager.hpp` and `src/EnemyManagerUpdate.cpp`, and focused pinned VC7.1 feedback. Adjacent source was used only to identify the broad enemy-family vocabulary. |
| BOUNDARY-133 | reviewed / ECL tables and Enemy death/update seams | `EclManager::RunEcl` owns 14,792 logical code bytes at `0x004086C0-0x0040C087`. Corrected direct table decoding proves that its six-entry easing jump table comes first at `0x0040C088-0x0040C09F`, followed by the 187-entry opcode jump table at `0x0040C0A0-0x0040C38B`; four `CC` bytes through `0x0040C38F` are unowned before independent `0x0040C390`. This supersedes Packet 148's reversed table-order description without changing the total physical extent. `EnemyView::HandleDeathRewards` owns `0x004102B0-0x00410725`; ten `CC` bytes through `0x0041072F` precede `EnemyManagerView::OnUpdate @ 0x00410730-0x0041165A`. Compiler-owned ECL table bytes are physical comparison extent, not authored logical denominator. | Candidate inventory, complete IDA functions, switch instructions at `0x0040885E/0x004088AB`, direct target reads and table SHA-256 values, and verified PE bytes across both seams. |
| TOOLCHAIN-137 | hard-frontier / stable non-exact Enemy death rewards | Natural target-facing source for `EnemyView::HandleDeathRewards` under pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` emits 1,136 bytes against the 1,142-byte target. Correcting the six-stack-argument attack event helper from default `/Gr` to target-observed `__stdcall` moves the object from 1,113 to 1,136 bytes. `/O2 /Ob0` is identical in size to `/O2 /Ob1`; `/O1 /Ob1` and `/O2 /Os /Ob1` emit 1,075 bytes. Two cold builds have identical 1,136-byte `.text` SHA-256 `c5f3a30292647b4729089c4c1535210b5664a5ea7c35a6c2b783584aa4b718da`. The remaining six-byte size and register/local-lifetime residual is retained NON-EXACT; no pragma, identifier-hash search, register/volatile forcing, padding, assembly, fake return, or target-byte encoding is used. | Focused v1-v4 natural source-shape probes, target/object disassembly, calling-convention correction, O1/O2/Os/Ob0 falsification, and two cold builds. |
| ENEMY-002 | source-present / authored-game / hard update owner | `EnemyManagerView::OnUpdate @ 0x00410730` is the complete per-side Enemy runtime owner. TH09 independently fixes the callback receiver, four manager counters at `+0x2AC3AC/+0x2AC3B0/+0x2AC3B4/+0x2AC3B8`, four draw-list heads at `+0x2AC410`, priority/first-active pointers at `+0x2AC444/+0x2AC448`, and exactly 128 Enemy records at `+0x5758` with `0x5430` stride. The target body advances the side schedule, filters side categories, invokes `EclManager::RunEcl`, integrates movement and 0x1C-stride trail samples, executes three ANM VMs, performs collision/damage and nearest-target selection, dispatches four death modes plus the death ECL callback, updates damage feedback/Boss markers/attached effects, and rebuilds draw-group chains. Maintained `src/EnemyManagerCore.cpp` covers this whole owner with private target-facing overlays; unresolved helper identifiers remain role names. | Hash-matching TH09 IDA decompilation/disassembly/call graph, target tail bytes, Packet-148 target-backed Enemy layout, maintained source, and pinned VC7.1 compile feedback. Clean committed TH08 supplied only the broad update-owner source family; its 480 x 0x53D0 layout was not transferred. |
| BOUNDARY-134 | reviewed / Enemy update logical and physical ownership | `EnemyManagerView::OnUpdate` owns 3,883 logical code bytes at `0x00410730-0x0041165A`, ending in `ret`. Byte `0x0041165B` is compiler alignment, followed by the method's four-dword death-mode switch table at `0x0041165C-0x0041166B`, whose entries target four blocks within the reviewed body. Four `CC` bytes at `0x0041166C-0x0041166F` remain unowned before independent code at `0x00411670`. The logical authored denominator excludes the alignment and switch-table bytes. | Complete IDA function, target memory read across `0x00411640-0x0041166F`, and the four-case death dispatch in the maintained source. |
| TOOLCHAIN-138 | hard-frontier / stable non-exact Enemy update owner | Natural target-facing `EnemyManagerView::OnUpdate` under pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` emits a 3,156-byte function symbol against the 3,883-byte logical target. Two independent cold builds reproduce identical symbol bytes, SHA-256 `b3cddf80b261c51e757a376841ce7c45950c882f04b5ac6882816622588b9d51`, with 81 relocations. `/O2 /Ob0` emits 3,164 bytes and `/O1 /Ob1` emits 2,947, so neither closes the gap. The result is a maintained source-presence checkpoint, not an exact match; no `matches.csv` row, artificial padding, register/volatile forcing, pragma ordering, inline assembly, fake return, or target-byte material is used. | Three natural optimization profiles, two cold `/O2 /Ob1` symbol extractions, target-bound size comparison, and the ignored packet manifest. |
| PLAYER-009 | source-present / authored-game / hard gameplay-state owner | `PlayerUpdateSelectorState @ 0x004049A0` is the 3,238-byte Player gameplay owner reached only by exact `PlayerLifecycleView::OnUpdate`, which supplies `Player +0x24` in ECX. Maintained `src/PlayerGameplayState.cpp` now covers the complete target-observed timing gates, 12-cell pattern grid, three collision shapes `{32,32}`, `{10,10}`, and the current Player hurtbox, retry/alternate selection, primary/backup opponent-target fallback, eight-entry pattern histories, RNG fallback, and side-indexed `0x8E` protocol writes. It preserves the Player self-link at header `+0x78` and the four calls through the embedded `Player +0x36C` collision-query owner. The reconstruction name and small private leaf names remain neutral; pinned VC7.1 with the now-defined same-TU ResolvePatternOffset leaf emits 2,939 bytes versus the 3,238-byte target, so the owner is explicitly NON-EXACT. | Hash-matching TH09 IDA caller/decompilation/callee/dataflow review, exact Player OnUpdate relocation/call site, Packet-146 collision-query layout, maintained full owner source, target tail bytes, and focused pinned-VC7.1 comparison. |
| BOUNDARY-135 | reviewed / Player gameplay-state owner | `PlayerHeaderStateView::UpdateGameplayState` owns exactly `0x004049A0-0x00405645` (3,238 bytes), ending in `ret`. Ten `CC` bytes at `0x00405646-0x0040564F` remain unowned before independent `0x00405650`. No table or padding byte is added to the authored denominator. | Complete IDA function and verified target-memory read across `0x00405630-0x0040565F`. |
| ECL-002 | target-protocol / active-handler map | `EclManager::RunEcl` computes `opcode - 1`, accepts the inclusive wire domain 1-187, and dispatches through the 187-dword table at `0x0040C0A0`. Direct decoding plus the complete target decompilation yields 167 active opcode handlers and exactly twenty entries sharing the common advance/default destination `0x0040B410`: `03, 53, 54, 55, 5A, 5B, 5C, 7A, 7B, 8D, 8E, 9E, A4, A8, AE, B0, B3, B4, B5, B8`. `src/EclOpcodes.hpp` records all 187 numeric slots, uses explicit `UNHANDLED_xx` names for those twenty inactive TH09 entries, and adds neutral target-specific roles for 185-187. Adjacent TH08 semantic names are retained only as source-family hypotheses for active slots; they are not original-symbol or cross-game exactness claims. | Complete target switch decompilation, dispatch disassembly, direct 748-byte table read/hash `516ce17e4f4e2e2fad8ecb6b96e375fd7fc5c64f558fe911b5794af67ccb8de9`, and committed TH08 enum/source comparison. |
| ECL-003 | target-observed / context and call-stack geometry | TH09's fixed ECL instruction header is 0x0C bytes: time `+0x00`, opcode `+0x04`, next offset `+0x06`, difficulty mask `+0x09`, parameter mask `+0x0A`, followed by variable arguments. Each execution context is 0x22C bytes with current instruction `+0x04`, primary timer `+0x08`, per-frame callback/instruction `+0x14/+0x18`, secondary timer `+0x94`, and eight 0x30 interpolation slots at `+0xA0`. Enemy main context is `+0x7F4`; sixteen complete 0x22C snapshots begin at `+0xA20` and end exactly at active-context pointer `+0x2CE0`, followed by active call-stack pointer `+0x2CE4`, main/active depths `+0x2D28/+0x2D2A`, pending sub id `+0x2D70`, difficulty override `+0x3388`, and four child blocks at `+0x33D8`. | RunEcl prologue/subroutine entry/context-tail disassembly and decompilation, copy size 0x22C, depth cap 15, pointer arithmetic, and compile-checked offset assertions in `src/EclOpcodes.hpp`. |
| ECL-004 | partial maintained source / control-arithmetic family | `src/EclRunControl.inl` lexically reconstructs the complete wire-opcode family 1-53 without inventing a callable subowner inside `EclManager::RunEcl`. TH09 independently fixes 52 active handlers and inactive opcode 3; raw time/displacement operands for jump, a resolved decrement operand, integer/float assignment and arithmetic, random sign, trig/angle/polar/distance operations, interpolation-helper calls, the shared typed comparison helper for 40-51, raw subroutine id for 52, and context pop for 53. Target physical source-order clues are retained: integer/float arithmetic handlers are interleaved, opcode 5 falls into opcode 4's jump tail, and handlers 37/35/36/38/39 occur in that order. The helper source/TU ABI, remaining five opcode families, outer context/easing tail, and whole-function compiler result remain open; therefore the functions ledger intentionally has no source path and no exactness credit for `0x004086C0`. | Matching-IDB complete RunEcl decompilation/disassembly, decoded 187-entry table (family destinations `0x0040C0A0` entries 1-53), focused helpers `0x00407560`, `0x00407600`, `0x00407700`, `0x00407A80`, and `0x00406680`, target-bound source coverage report, and pinned VC7.1 lexical-include compile probe. Clean committed TH08 supplied only the natural lexical source-family hypothesis. |
| ECL-005 | partial maintained source / ANM-movement family | `src/EclRunMovement.inl` lexically reconstructs every active wire opcode 54-82. TH09 fixes the two manager-owned ANM banks at `+0x2AC420/+0x2AC424`, primary VM `Enemy+0x08`, alternate-bank bit `Enemy+0x337C bit 31`, movement geometry `+0x2DE0..+0x2E34`, clamp bounds `+0x3398..+0x33A4`, hitboxes `+0x2DBC/+0x2DC8`, six interaction transitions across primary/secondary flags `+0x337C/+0x3380`, and squared minimum-player distance `+0x33A8`. The maintained partial Enemy overlay is offset-asserted and keeps AnmManager's concurrent interface read-only. Opcode 82 belongs to the family semantically but its target handler is physically distant at `0x0040BB68`. Helper TU/ABI, later opcode families, outer interpreter tail, and complete compiler result remain open, so `RunEcl` still has neither source-presence nor exactness credit. | Matching-IDB RunEcl handlers `0x004094D5..0x00409FD3` plus `0x0040BB68`, focused target helpers `0x00403E00`, `0x00406740`, `0x004070A0`, `0x00407320`, `0x00407B20`, `0x00407C30`, `0x004101D0`, `0x00401500`, and `0x0041BB70`, decoded family table destinations, target-bound 29-case source audit, and pinned VC7.1 lexical-include compile probe. Committed TH08 supplied source-shape hypotheses only. |
| ECL-006 | partial maintained source / remote-spawn family | `src/EclRunRemote.inl` covers every wire opcode 83-95: inactive `83-85/90-92` explicitly share ordinary advance, while active `86-89` resolve remote variables/calls through the manager table at `+0x2AC388` and active context `Enemy+0x2CE0`; opcode 89 preserves the target's repeated remote-slot resolution before storing the pending short at `+0x2D70`. Active spawn opcodes 93-94 require positive life at `+0x2E48`, copy the raw 0x1C-byte packet, resolve its three position scalars, pass resolved operands 4-6 and active int variables `context+0x1C` to the manager spawn boundary, and add the parent position only for 94. Opcode 95 calls the manager clear routine with constants 8000 and 0. The family remains partial source and earns no `RunEcl` source-presence or exactness credit. | Matching-IDB RunEcl handlers `0x00409FD8..0x0040A163` and `0x0040B311..0x0040B541`, focused target helpers `0x0040F340`, `0x0040F640`, `0x00405730`, `0x00407A80`, and the operand lvalue/resolver boundaries, decoded family table destinations, target-bound 13-case source audit, and pinned VC7.1 lexical-include compile probe. Clean committed TH08 provided source shape only. |
| ECL-007 | partial maintained source / bullet-laser family | `src/EclRunBullet.inl` covers every wire opcode 96-123: nine pattern opcodes share positive-life/deferred-dispatch control, 105-110 own interval/defer/immediate-shot/offset state, 111 writes one of eighteen 0x18-byte transform records, 112-113 own transition-clear and sound protocol, 114-115 populate the persistent 0x214 laser descriptor, 116-121 select/rotate/aim/reposition/test/cancel one of 32 enemy laser slots, and inactive 122-123 explicitly share ordinary advance. The offset-asserted Enemy overlay fixes the bullet descriptor `+0x2E74`, deferred 0x2C-byte instruction `+0x3088`, interval/timer `+0x30B4/+0x30B8`, laser descriptor `+0x30C4`, slots `+0x32D8`, selected slot `+0x3358`, and defer bit `+0x337C bit 14`. The existing Bullet/Laser target view supplies 0x214/0x59C geometry, but callee source/exactness remains independent. RunEcl still earns no complete-source or exactness credit. | Matching-IDB handlers `0x0040A168..0x0040A945` and `0x0040B5B1..0x0040B63D`, focused target helpers `0x00408040`, `0x004048E0`, `0x00405650`, `0x00413290`, `0x0041BB70`, timer setter `0x00401500`, decoded family table destinations, target-bound 28-case source audit, and pinned VC7.1 lexical-include compile probe. Clean committed TH08 supplied source shape only. |
| ECL-008 | partial maintained source / boss-state-effects family | `src/EclRunState.inl` covers every wire opcode 124-157: positioned sound; the 32 short subroutine slots and shared pending-subroutine entry; boss registration/UI teardown; death mode/callback and life/timer callbacks; four independently allocated 0x24F4 child ECL blocks; immediate/repeating EX callbacks; death ANM bytes; effect spawning; item-drop state; ANM interrupts; timer/background/boss-marker protocol; bullet-rank bounds; laser-slot clearing; timeout/special-interaction bits; and trail geometry. Inactive 141-142 explicitly use ordinary advance. The offset-asserted family overlay fixes primary/secondary ANM VMs `+0x8/+0x2AC`, subroutine slots `+0x2D30`, pending id `+0x2D70`, rank values `+0x2E38`, boss timer `+0x2E64`, laser slots `+0x32D8`, boss/item/death fields `+0x335C..+0x336B`, flags `+0x337C`, callbacks/children `+0x33B0..+0x33D8`, trail state `+0x33E8..+0x53A6`, and attached effects `+0x53B4..+0x5418`. Resolver repetition and raw-vs-resolved operands follow TH09 target order. RunEcl remains partial and receives no complete-source or exactness credit. | Matching-IDB handlers `0x0040A290`, `0x0040A94C`, `0x0040AB5D..0x0040B2E0`, and `0x0040B54A..0x0040B86C`; focused target helpers `0x00401500`, `0x00406640`, `0x004067D0`, `0x004067F0`, `0x00406850`, `0x0040F560`, `0x00439CF0`, and `0x0043E380`; decoded table destinations; target-bound 34-case source audit; and pinned VC7.1 lexical-include compile probe. Clean committed TH08 supplied source-shape hypotheses only. |
| ECL-009 | partial maintained source / late manager-side family | `src/EclRunLate.inl` covers every wire opcode 158-187: 21 active handlers plus inactive/default 158, 164, 168, 174, 176, 179-181, and 184. The family owns draw group and damage-reduction timer state; target no-op bullet-radius/all cleanup calls; manager protocol `+0x2AC3DC`; main-ANM rotation; alternate polar conversion; laser angle/cap/length/offset controls; player-relative horizontal random angle; timer/no-damage flags; timeline-spawn suppression `manager+0x2AC440`; phase-start life; boundary-aware random movement; extra-ANM/side-category/secondary bits; and TH09's side-owner/background-VM trigger. Opcode 186 selects owner `+0xA4/+0xA8` by the `Enemy+0x3380 bits 10-11` category and, for category 3, rebuilds this side's spell VMs from the opposing Background side ANM/script base plus opposing Player `+0x303F4`. All 187 wire cases are now represented across six lexical fragments, but the shared outer owner and exactness remain open. | Matching-IDB handlers `0x00409945`, `0x0040A662..0x0040AB49`, and `0x0040B875..0x0040BDD6`; target helper/no-op boundaries `0x00403E00`, `0x00404910`, `0x004067B0`, `0x00407E30`, `0x00412590`, and `0x004125A0`; decoded full table; target-bound 30-case source audit; pinned VC7.1 lexical compile; and TH09-local Background/Player/Etama layouts. Adjacent TH08 supplied only source-shape hypotheses and no TH09 behavior or layout fact. |
| ECL-010 | source-present / authored-game / complete interpreter owner | `src/EclManager.cpp` completes maintained source for `EclManager::RunEcl @ 0x004086C0-0x0040C087`. The outer owner binds Enemy main context `+0x7F4` and its sixteen-entry stack `+0xA20`, preserves the active pointers/depths at `+0x2CE0/+0x2CE4/+0x2D28/+0x2D2A`, enters pending subroutines through the target-shared advance path, recomputes world position, enforces secondary-time and instruction-time gates, combines the global difficulty mask with Enemy byte `+0x3388`, and lexically dispatches all 187 cases. After dispatch it invokes the per-frame callback, advances all eight 0x30 interpolation slots with the target six-mode easing protocol, converts position interpolation into velocity/angle while restoring the scripted position, persists main or child stack depth, visits four `0x24F4` child blocks, restores the main context, and invokes the independent motion/ANM tail boundaries. The current pinned-VC7.1 owner emits 14,420 logical bytes plus the complete 772-byte compiler-table suffix versus the target's 14,792+772 physical geometry and remains explicitly NON-EXACT; local layout, code-block order, one direct-call site, relocations and exact bytes remain open. | Matching-IDB complete function/disassembly, focused helper bodies, target-bound 187-entry opcode and six-entry easing tables, offset assertions, maintained six-family lexical source, fail-closed owner/source and codegen reports, and pinned VC7.1 complete-TU comparison. |
| ECL-011 | target/compiler oracle / complete call census | Fresh byte-level and Capstone census establishes 379 calls inside target RunEcl: 375 immediate direct calls and four indirect sites (one register-indirect and three memory-indirect). The older 376+3 split was a counting error. Target-local operand handling proves resolver multiplicities 131/100/17/24. The maintained owner reproduces every immediate direct-call destination multiplicity. Restoring CANCEL_LASER as laser->timer = 0 selects exact ZunTimer::operator=(int) @ 0x00401500, and preserving separate resolved/raw branches for CALL_EX_INSTRUCTION restores the target two callback-table indirect call sites. The current candidate therefore also has 379 calls with the same 375+4 category split; call identity/count is no longer an open blocker. | Hash-attested target, target-byte E8 census, independent Capstone operand-class census, target opcode/easing tables, pinned VC7.1 candidate COFF relocations, and scripts/report-ecl-codegen.py. |
| ECL-012 | historical compiler oracle / structural RunEcl correction | An earlier bounded source-shape checkpoint replaced the float operand wrapper's per-call temporary, inlined synthetic flag assignment, and restored target-observed timer operators, ANM branches, boss-marker construction, separate spawn packets and the real SpawnEnemy member ABI. That intermediate candidate emitted 13,804 logical bytes plus 772 table bytes with a `0x15C` frame and is retained only as negative-Oracle history. ECL-016 supersedes its current codegen numbers after the shared spawn tail and helper-band integration; no exactness was ever claimed for this intermediate result. | TH09 target prologue and stack-frame accesses, focused opcode-93/94 and timer disassembly, matching-IDB call sites, and pinned VC7.1 source-shape experiments. |
| BACKGROUND-001 | mixed exact/source-present / authored-game / recovered owner seam | Packet 154 takes ownership of the interrupted Background campaign and lands an offset-asserted 0x65C4 `Background` view plus maintained lifecycle/update/draw source. TH09 independently fixes stage object/quad/header geometry, three embedded 0x2A4 stage VMs, the ANM slots, stage-script timers, spell-background state/32-VM bank, tint/fog/culling fields, side pointers, registry tag `BGScrollInf`, chain priorities, and stage-file rebasing. Exact promoted members are `ClearSpellBackgroundState @ 0x004011C0`, `DeletedCallback @ 0x004024D0`, and `LoadStageData @ 0x00402570`; `OnUpdate`, both draw callbacks, `AddedCallback`, the constructor, and `Create` remain maintained NON-EXACT. `RenderObjects` @ 0x004026C0 is now source-present and canonical exact; the remaining maintained NON-EXACT Background functions keep their existing status. | Matching-IDB functions/decompilation/call graph, target-bound layouts and caller relocations, recovered Packet-135 source/artifacts, clean cold pinned-VC7.1 rebuilds, and three repository-defined exact Background units. |
| BOUNDARY-136 | reviewed / mixed exact-source-present / Background and ANM support | Packet 154 reviews 18 independent logical extents totaling 3,093 target bytes. Exact functions own `0x004011C0-0x004011CA`, `0x00401600-0x0040160F`, `0x00401610-0x0040161D`, `0x00401620-0x00401632`, `0x00401640-0x0040165A`, `0x00401660-0x00401673`, `0x004024D0-0x00402524`, `0x00402570-0x004026B0`, `0x00403A10-0x00403A3C`, `0x00415CF0-0x00415CF5`, and `0x0042FEC0-0x0042FEF7`. The independently source-present/non-exact extents are Background update `0x00402350-0x004024C8`, high draw `0x004033E0-0x0040367E`, low draw `0x00403680-0x00403824`, added callback `0x00403830-0x00403A0A`, base VM constructor `0x004033A0-0x004033DD`, Background constructor `0x00403A40-0x00403ADF`, and Create `0x00403AE0-0x00403C12`. No alignment or neighboring stage phase is absorbed. | Current candidate inventory, bounded matching-IDB reads/decompilation, full target-bound comparison and explicit next-function seams. |
| TOOLCHAIN-139 | mixed exact/stable-non-exact / recovered Background profile | Natural recovered source under pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` yields 11 canonical functions totaling 620 exact bytes: five AnmManager helpers, the derived `AnmVm` constructor, three Background helpers, the six-byte stage-resource policy, and `Supervisor::EnableFog`. All reviewed relocations reproduce their TH09 destinations. Stable negatives are `AnmVmBase` 75/62 bytes, Background constructor 156/160, Create 311/307, OnUpdate 287/377, high draw 440/671, low draw 349/421, and AddedCallback equal-size 475 with only 330/399 comparable ordinary bytes. Equal logical size is not treated as exact. No register forcing, padding, assembly, fake return or embedded target bytes is retained. | Recovered Packet-135 objects as history only, fresh cold repository builds, 11 canonical relocation-aware replays, and focused negative comparisons from the current source. |
| BULLET-001 | mixed exact/source-present / authored-game / recovered Etama owner seam | The target-backed Etama/Bullet seam uses a 0x25E1C0 controller, 0x10C4 Bullet, 0x59C Laser, 0x214 spawn descriptor and the maintained transform/ex-state layouts. Maintained source now covers Initialize, the hard single-bullet allocator, both pattern wrappers, Bullet::AdvanceTransformProgram, nine transform helpers, the 536-bullet/48-laser update owner, OnDraw/AddedCallback, lifecycle Register/Release, VM copy, sprite selection, bucket reset/deactivate, and laser spawn/control families. `EtamaController::Initialize`, `EtamaController::AddedCallback`, and `Bullet::AdvanceTransformProgram` are independently canonical exact; `OnDraw` and `SpawnLaserPattern` remain source-present/non-exact. Do not infer whole BulletManager closure from this seam. | Current functions/matches ledgers, target-backed layout assertions, Packet 155 family evidence, and later exact/source closures including Packet 448. |
| BOUNDARY-137 | reviewed / mixed exact-source-present / Etama and Bullet | Packet 155 reviews 19 independent logical extents totaling 6,690 target bytes: `0x00412390-0x004123B6`, `0x004124D0-0x004124DF`, `0x004124E0-0x00412580`, `0x00412930-0x00412957`, `0x00412960-0x004130C4`, `0x004130F0-0x004131BA`, `0x004131C0-0x0041328A`, the seven non-wrap transform helpers `0x00413460-0x004139E6`, exact wrap helpers `0x004139F0-0x00413A60` and `0x00413A70-0x00413AE0`, Release `0x00414180-0x004141B3`, OnUpdate `0x004146F0-0x00414F82`, and Register `0x004150D0-0x004151EA`. Earlier interrupted artifacts incorrectly compared `ClearDrawBuckets` and `Deactivate` against 48-byte extents; fresh IDA boundaries and inventory rows establish their independent 39-byte and 40-byte extents, and current source replays both exactly. No alignment or neighboring constructor/initializer/transform-program function is absorbed. | Matching-IDB current function objects/decompilation, candidate inventory extents, target call graph, and full target-bound comparisons. |
| TOOLCHAIN-140 | mixed exact/stable-non-exact / recovered Bullet profile | Natural source under pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` yields seven canonical functions totaling 656 exact bytes with all 43 relocations replayed. Stable negatives are sprite selection target-sized 161 but byte-different; SpawnSingle 1392/1893; pattern wrappers 187/203 each; deceleration 116/110, vector 196/179, polar 152/158, relative 184/202, absolute 181/199, aimed 214/236, bounce 299/295; and OnUpdate 2236/2195. These mismatches remain useful maintained source without exact credit. No register forcing, padding, assembly, fake return, or embedded target bytes is retained. | Recovered Packet-139 probes as historical diagnostics, corrected current boundaries, fresh cold canonical builds and explicit relocation manifests. |
| TITLE-016 | mixed exact/source-present / authored-game / recovered options owner | Packet 156 consumes the last inherited dirty path and records maintained source for `TitleScreenView::OnUpdateOptions @ 0x004276EB`, the nine-entry options menu owner called from the Start-menu state machine. TH09 independently fixes the 0x1B388 owner view, VM bank at `+0x11BA0`, menu/timer/config snapshot fields, option and volume globals, input masks, screen transitions, selection sprite updates and audio-volume queueing. The 2,045-byte target owner remains NON-EXACT at a stable 2,050-byte compact-profile object. Connected `TitleScreenView::PlayMenuSound @ 0x00422F17` is canonical exact at 34 bytes and drains the sound queue after playing the requested UI sound. | Matching-IDB callers/decompilation and extents, recovered title-options probe history, current cold VC7.1 comparisons, and repository-defined exact helper replay. |
| BOUNDARY-138 | reviewed / mixed exact-source-present / Title options | `TitleScreenView::PlayMenuSound` owns `0x00422F17-0x00422F38` and is immediately followed by independent `ChangeCurrentScreen @ 0x00422F39`; no padding is assigned. `OnUpdateOptions` owns `0x004276EB-0x00427EE7`; its next target seam remains independent. The two functions contribute 2,079 reviewed target bytes, of which only the 34-byte helper is exact. | Current matching-IDB function objects/callers, candidate inventory and full target-bound compiler comparisons. |
| TOOLCHAIN-141 | mixed exact/stable-non-exact / Title options profile | Pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Gr /O1 /Ob1 /Oy- /I src` emits `PlayMenuSound` exactly at 34/34 with three reviewed relocations and emits `OnUpdateOptions` at 2,050 bytes versus the 2,045-byte target. `/O2 /Os /Ob1` keeps the same 2,050-byte owner size, while plain `/O2 /Ob1` grows it to 2,292 and is rejected. No five-byte padding, register forcing, pragma, assembly or target-byte encoding is used to manufacture equality. | Fresh cold compact-profile build and helper canonical replay plus focused `/O2` and `/O2 /Os` falsification. |
| ANM-023 | exact / authored-game / complete interpreter owner | `AnmManager::ExecuteScript @ 0x00436F30-0x004393BD` is repository-canonical exact. The decisive correction was not opcode semantics: all 91 wire values were already represented by 89 explicit handlers plus intentionally defaulted Nop/InterruptLabel. The local execution view incorrectly supplied class-inline `ZunTimer::operator==`, `operator<=`, and `operator>` bodies; the target instead relocates those comparisons to out-of-line members in Wait and the seven-slot interpolation loop. With that TU boundary restored and the original unit's `/Ob0` inline policy recovered, the natural source reproduces all 9,358 authored code bytes and the complete 9,776-byte physical extent. The latter includes two alignment bytes and three compiler tables: 91 opcode, six easing, and seven interpolation-field entries. | Fail-closed `scripts/report-anm-execute.py`, target table hashes, repeated pinned-VC7.1 canonical replay, 9,358/9,358 logical bytes, 9,776/9,776 physical bytes, and all 368 relocations including 104 table entries. |
| ENDING-001 | exact authored-game lifecycle module | `EndingInf` is a TH09 0x2AB8-byte heap owner registered by `Ending::RegisterChain @ 0x0040F080`. TH09 fixes two ChainElem pointers at `+0x00/+0x04`, background position/scroll at `+0x08..+0x10`, sixteen constructed 0x2A4 `AnmVm` objects at `+0x14`, script storage at `+0x2A54/+0x2AB4`, three timers at `+0x2A64/+0x2A70/+0x2A7C`, wait/text/fade state through `+0x2AB0`, and a fifteen-VM active update/draw loop. Eleven functions are repository-canonical exact, including `RunEndingScript @ 0x0040E8A0`, totaling 3,566 reviewed authored logical bytes. The exact `RunEndingScript` owned extent includes compiler tables beyond its 1,499-byte logical body; original translation-unit partition and product closure remain open. | Passed TH09 function/caller evidence, exact RegisterChain bindings, target-backed 0x2AB8 layout, canonical replays, and Packet 511's complete Ending interpreter extent and relocation review. |
| BOUNDARY-139 | reviewed / exact Ending owners | Ending logical extents are independently reviewed as `0x0040E0D0-0x0040E123`, `0x0040E130-0x0040E16B`, `0x0040E170-0x0040E28E`, `0x0040E2F0-0x0040E362`, `0x0040E480-0x0040E4E4`, `0x0040E4F0-0x0040E574`, `0x0040E580-0x0040E89F`, `0x0040E8A0-0x0040EE7A`, `0x0040EF60-0x0040EFD6`, `0x0040EFE0-0x0040F070`, and `0x0040F080-0x0040F15E`. `Ending::UpdateFade` has a 0x11F-byte logical body followed by one owned alignment NOP and a five-entry jump table through `0x0040E2A3`; twelve `CC` bytes at `0x0040E2A4-0x0040E2AF` remain unowned. `Ending::RunEndingScript` owns 1,499 logical bytes and compiler tables through `0x0040EF5E`; `0x0040EF5F` is unowned `CC` before the independent constructor. Canonical authored-byte totals count logical code only, while both exact match units compare complete owned extents. | Target boundaries and tables, pinned VC7.1 COFF relocation layouts, exact `UpdateFade` 0x134 and `RunEndingScript` 0x6BF owned-extent replays, and independent extents for the remaining Ending cohort. |
| TOOLCHAIN-142 | exact / Ending compiler profile with function-local caveat | Natural Ending source under pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` reproduces the interpreter and its connected exact helpers, including their compiler-owned tables. `RunEndingScript` replays 1,499/1,499 logical and 1,727/1,727 owned bytes with all 88 reviewed relocations. The earlier 1,695-byte candidate diagnostic measured the entire COFF section against only target logical code and is withdrawn. The natural typed `Ending` constructor separately requires focused `/O2 /Ob0` to reproduce its 119 bytes and seven relocations; that is a function-level observation, not a translation-unit or project-wide profile claim. No padding, register/volatile forcing, pragma ordering, assembly, fake return, embedded target bytes, or target patching is used. | Eleven repository-defined exact Ending units; Packet 511's two cold interpreter replays and table ownership review; separate constructor replay and connected exact-unit regression. |
| TITLE-006 | mixed exact/source-present / authored-game / character-selection seam | Packet 165 reconstructs the connected TH09 TitleScreen character-selection seam and corrects an earlier owner-name mistake. `TitleScreenView::OnUpdateCharacterSelect @ 0x004254A7` is the ordinary 14-entry one-side handler reached after normal difficulty selection; `TitleScreenView::UpdateScreen8Mode123 @ 0x00425ACB` is the two-side mode-1/2/3 screen-8 handler; `TitleScreenView::UpdateScreen16 @ 0x00426334` is their final-selection/launch transition; and the previously named `OnUpdateCharacterSelect @ 0x004289DB` is target-proved to be `TitleScreenView::UpdateScreen8Mode0`. Nineteen functions totaling 9,611 target bytes now have maintained source mappings. Five reusable helpers are canonical exact: three 89-byte unlock-aware cursor walkers at `0x0042304D/0x004230A6/0x004230FF`, the 217-byte four-way cursor helper at `0x00424671`, and the 168-byte character-setting updater at `0x00425163`, totaling 652 exact bytes. The remaining helpers and four large owners stay source-present/non-exact. | Passed native-IDA dispatcher/caller/callee/decompilation evidence under `target:th09-main` / `factory-native-stdio`; pinned VC7.1 target-bound comparisons; five repository match-unit replays; and clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` TitleScreen source-family comparison. TH08 supplies only state-machine/source-shape hypotheses; all TH09 addresses, unlock tables, input layout, ABI, extents and exactness are TH09-local. |
| BOUNDARY-140 | reviewed / contiguous TitleScreen character-selection owners | The packet's reviewed logical seam contains consecutive independent functions at `0x0042304D-0x004230A5`, `0x004230A6-0x004230FE`, `0x004230FF-0x00423157`, `0x00424671-0x00424749`, `0x0042474A-0x004247F0`, `0x00424EDD-0x00424FAF`, `0x00424FB0-0x0042505E`, `0x00425163-0x0042520A`, `0x0042520B-0x00425232`, `0x00425233-0x0042538F`, `0x00425390-0x004254A6`, `0x004254A7-0x00425ACA`, `0x00425ACB-0x00426333`, and `0x00426334-0x004266B4`. The hard-owner boundaries are especially strong because ordinary character selection, mode-1/2/3, and screen-16 begin at the immediately following byte with no absorbed padding/table. Existing `UpdateScreen8Mode0` remains independently reviewed at `0x004289DB-0x0042929B`, immediately before exact ResultName at `0x0042929C`. | Initial candidate inventory, passed native-IDA function extents and full decompilations, caller routing, exact helper whole-extent replays, and target-bound compiler feedback. No padding/table bytes are added to the authored denominator. |
| TOOLCHAIN-143 | mixed exact/stable-non-exact / TitleScreen character selection | Under pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Gr /O1 /Ob1 /Oy- /I src`, the five canonical helper units reproduce 652/652 target bytes with all reviewed relocations. Natural maintained negatives are: main-input horizontal cursor 145/167, specified-input cursor 158/167, active/reverse/inactive VM helpers 217/211, 183/175 and 183/175, setting sprites 488/479, setting indicator 43/40, `GetOptionState` equal-size 54/54 but only 22/54 ordinary comparable bytes, dual-selection visual helper 424/349, screen-16 visual helper 307/279, ordinary owner 1577/1572, mode-1/2/3 owner 2114/2153, mode0 owner 2244/2241, and screen16 owner equal-size 897/897 but only 247/653 ordinary comparable bytes after relocation masking. Equal size is explicitly not exactness. No pragma ordering, register/volatile forcing, padding, inline assembly, fake return, target bytes or oracle-specific source is retained. | Fresh repository VC7.1 builds and relocation-aware diagnostics after completion of the recovered source; two replay points for each canonical helper across the interrupted/recovered session; and the recorded negative comparison matrix. |
| TITLE-007 | mixed exact/source-present / authored-game / replay-menu migration | `TitleScreenView::OnUpdateReplayMenu @ 0x0042689A-0x00426E04` is TH09's screen-11 replay browser/loader on the existing 0x1B388 TitleScreen owner. TH09 independently fixes fifty 0x200 replay-path slots from `+0x28C`, fifty retained decoded-replay pointers at `+0x681C`, fifty 0x1EC replay snapshots at `+0x68E4`, enumeration/reset and slot-count fields at `+0xC900/+0xC904`, and selected replay at `+0xC908`. Init clears replay-active flag bit 3, optionally loads `title/replay00.png`, enumerates 25 fixed `th9_%.2d.rpy` files plus at most 25 `th9_ud????.rpy` user files, copies the decoded headers, and retains allocations for later cleanup. Selection navigates the 50 slots, then one of ten available `frameStart[0][stage]` entries, publishes the selected path/stage/replay header mode bytes to the existing game-global views, frees all retained decoded headers, and exits to the game-launch state. `TitleScreenView::MoveCursorVertical @ 0x0042457A` is the reusable up/down cursor dependency and is canonical exact at 135 bytes. | Passed native-IDA dispatcher/body/callee evidence under `target:th09-main` / `factory-native-stdio`; target-backed `ReplayDataView` from the maintained ReplayManager reconstruction; pinned VC7.1 TitleScreen builds; exact helper replay; and clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` `OnUpdateReplayMenu` as a source-family hypothesis only. TH08's 15+45 replay geometry and other game-local semantics are not transferred. |
| BOUNDARY-141 | reviewed / TitleScreen replay-menu seam | `TitleScreenView::MoveCursorVertical` owns exactly `0x0042457A-0x00424600` (135 bytes); independent candidate `0x00424601` begins at the next byte. `TitleScreenView::OnUpdateReplayMenu` owns exactly `0x0042689A-0x00426E04` (1,387 bytes), ending in `ret`; the already reviewed independent `TitleScreenView::OnUpdateMusicRoom` begins immediately at `0x00426E05`. No alignment, jump-table, or neighboring bytes are charged to either authored logical extent. | Initial candidate inventory, native-IDA function extents and complete replay-menu disassembly, dispatcher routing, exact 135-byte helper replay, and the independent MusicRoom boundary. |
| TOOLCHAIN-144 | exact helper / stable non-exact replay owner | Under the established TitleScreen pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Gr /O1 /Ob1 /Oy- /I src` profile, natural `MoveCursorVertical` source reproduces 135/135 target bytes with all seven reviewed input/sound relocations. The target-backed replay-menu source uses the same 0x598-byte stack frame as the target and, after correcting the `value1E5` out-of-domain path so it performs no launch-mode writes, emits 1,389 bytes versus the 1,387-byte target. The earlier 1,377-byte candidate was semantically wrong despite being numerically close. The remaining two-byte size residual coexists with broader register/local-lifetime and basic-block scheduling drift, so the owner remains NON-EXACT and no compiler-shaping source is retained. | Repository-defined helper replay, two target-bound replay-menu comparison iterations, complete target/object disassembly, and focused source-shape review. No pragma ordering, register/volatile forcing, padding, inline assembly, fake return, embedded target bytes, or target patching is used. |
| TITLE-008 | mixed exact/source-present / authored-game / embedded result view | Packet 167 reconstructs TH09 TitleScreen screen 13 as `TitleScreenView::OnUpdateResult @ 0x004266B5`. Start-menu item 4 enters this screen without leaving the 0x1B388 TitleScreen owner. The target loads `title/result00.png`, interrupts/executes the shared VM array, presents fourteen logical character choices, filters cursor positions through the Normal and Mode4 availability tables, maps the selected physical cursor through the 16-byte target table at `0x004A1DAC`, and returns to screen 1 on cancel. The exclusive dword at `0x004AC8D4`, now named `g_TitleResultUnlockStep` in the shared IDB, accepts a target-observed 3 + 4 + 1 + 2 input sequence with masks `0x2000`, `0x0400`, `0x0200`, and `0x4000`; reaching step 10 immediately sets all sixteen general character-unlock bytes plus the first fourteen Normal/Mode4 unlock bytes, plays sound 28, and resets the step. Its eight target xrefs are all inside this owner. Connected `TitleScreenView::MoveCursorHorizontal @ 0x00424601` is canonical exact at 112 bytes. | Passed native IDA dispatcher/caller/callee/xref/decompilation/disassembly and raw-boundary evidence under `target:th09-main` / `factory-native-stdio`; target-backed character tables from Packet 165; pinned VC7.1 compiler feedback; exact helper replay; and clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` ResultScreen character/hidden-unlock source family as hypothesis only. TH09 independently fixes the owner, storage, sequence, extents, and exactness. |
| BOUNDARY-142 | reviewed / TitleScreen result-view seam | `TitleScreenView::MoveCursorHorizontal` owns exactly `0x00424601-0x00424670` (112 bytes). The preceding vertical cursor ends at `0x00424600`, and exact `MoveCursorFourWay @ 0x00424671` begins at the next byte. `TitleScreenView::OnUpdateResult` owns exactly `0x004266B5-0x00426899` (485 bytes), ending in `ret`; independent source-present `OnUpdateReplayMenu @ 0x0042689A` begins immediately afterward. No padding, table, or neighboring bytes are charged to either logical authored extent. | Initial candidate inventory, attested function objects, bounded raw-byte reads, complete target disassembly, dispatcher routing, and exact helper whole-extent replay. |
| TOOLCHAIN-145 | exact helper / stable non-exact result owner | Under the established TitleScreen pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Gr /O1 /Ob1 /Oy- /I src` profile, natural `MoveCursorHorizontal` source reproduces 112/112 target bytes with all five reviewed input/sound relocations. The first result-owner draft emitted 454 bytes because an abstract `requiredInput` variable changed both semantics and branch shape: it delayed the step-10 unlock by one frame. Restoring the target-observed four threshold branches and same-frame unlock raised the owner to 482 bytes; expressing the target's 16-bit wrong-input test raised it to the retained 483-byte candidate versus the 485-byte target. Complete instruction alignment confines the final two-byte residual to VC7.1 materialization of the selected signed character byte before `SetCharacterCursorInactive`; moving the natural `char` local to function scope does not change codegen. The owner therefore remains NON-EXACT. No pragma ordering, register/volatile forcing, padding, inline assembly, fake return, embedded target bytes, or target patching is retained. | Successive target-bound 454/482/483-byte compiler probes, complete target/object instruction alignment, repository-defined 112-byte helper replay, and connected exact TitleScreen regressions. |
| TITLE-009 | mixed exact/source-present / authored-game / TitleScreen setup lifecycle | Packet 168 closes the TH09 TitleScreen added/deleted lifecycle cohort around the 0x1B388 owner. `TitleScreenView::ActualAddedCallback @ 0x00424BF7` resets ScreenEffect/Ascii state, sets the Supervisor frame-rate multiplier, applies replay/network/registration-context routing, prepares loading VMs, marks the owner loading, and launches `TitleSetupThread @ 0x004249E1` through `Supervisor::ThreadStart`. Natural `/Gr` source for `AddedCallback @ 0x00424D46` compiles to the target's five-byte tail jump into the actual member body, proving it is an authored wrapper rather than an import/library/compiler-generated exclusion. `Release @ 0x00424D4B` frees the active replay, dynamic VM array, and fifty retained replay allocations. `DeletedCallback @ 0x00424D9F`, previously absent from the repository candidate inventory, is registered directly by `TitleScreen::RegisterChain`; it discards D3D managed resources, releases Title ANM slots 15/17 and surface 0, cuts the draw chain, invokes `Release`, frees the TitleScreen object, and returns zero. Four lifecycle functions are canonical exact, totaling 520 bytes; the setup worker remains source-present/NON-EXACT at 537 candidate bytes versus 534 target bytes. | Passed native IDA registration/xref/call/decompile/disassembly/raw-byte evidence under `target:th09-main` / `factory-native-stdio`; repository VC7.1 exact replays; established Supervisor/ANM/ScreenEffect APIs; and clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` TitleScreen added/setup/deleted source family as a hypothesis only. TH09 independently fixes all local addresses, extents, state offsets, callback registration, ABI evidence and exactness. |
| BOUNDARY-143 | reviewed / expanded TitleScreen lifecycle denominator | The reviewed lifecycle seam is contiguous: `TitleSetupThread @ 0x004249E1-0x00424BF6` (534 bytes), `ActualAddedCallback @ 0x00424BF7-0x00424D45` (335), `AddedCallback @ 0x00424D46-0x00424D4A` (5), `Release @ 0x00424D4B-0x00424D9E` (84), and `DeletedCallback @ 0x00424D9F-0x00424DFE` (96); independent menu-selection helper `0x00424DFF` begins at the next byte. `DeletedCallback` was missing from the imported repository function inventory even though IDA recognized the 0x60-byte function and TitleScreen registration referenced its exact entry. Packet 168 adds that one authored candidate to the denominator. Raw target bytes independently show the Release `ret` immediately before DeletedCallback and DeletedCallback's `ret` immediately before the `push ebp` at `0x00424DFF`; no padding or table bytes are absorbed. | TitleScreen registration at `0x0042AB13`, native IDA function/xref evidence, bounded raw bytes around `0x00424D80`, complete target disassembly, canonical exact replays for four members, and independent next-function inventory. |
| TOOLCHAIN-146 | four exact lifecycle functions / stable non-exact setup worker | Under the established TitleScreen pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Gr /O1 /Ob1 /Oy- /I src` profile, natural source reproduces `ActualAddedCallback` 335/335 with 27 relocations, `AddedCallback` 5/5 with one relocation, `Release` 84/84 with three relocations, and `DeletedCallback` 96/96 with eleven relocations. The first natural actual-added draft was 330 bytes; rewriting only the target-observed registration-context `sub/dec` dispatch as a natural `switch` closes 335/335 exactly. The worker's retained natural source emits 537 bytes versus the 534-byte target. IDA exposes a 0x1C target frame with two branch-selected three-float temporaries for the result-text VM position, while the simple maintained value source emits a 0x14 frame. A direct two-temporary/pointer experiment grows to 551 bytes and is rejected; introducing the repository's wider `Float3` constructor into the shared TitleScreen TU is also rejected as unnecessary cross-function risk once four lifecycle functions are already exact. No pragma ordering, register/volatile forcing, inert padding, inline assembly, fake return, embedded target bytes, or target patching is retained. | Successive 330->335 actual-added source-shape probe, 537/551 setup-worker negative probes, complete target/object instruction review, four repository-defined match units, and connected TitleScreen exact regressions. |
| TITLE-017 | mixed exact/source-present / authored-game / shared menu UI | Packet 169 closes two reusable TitleScreen menu-selection helpers used across StartMenu, Options and other embedded menu states. `TitleScreenView::SetMenuSelectionSprites @ 0x00424DFF-0x00424E94` iterates the requested VM range, selects each non-active sprite variant (`baseSpriteIndex + 1`) with interrupt 8, then restores the selected VM base sprite with interrupt 7. `TitleScreenView::UpdateMenuSelection @ 0x004276D7-0x004276EA` forwards `keyboardSelection`, `menuVmStart` and `menuItemCount` into that helper. Natural `void` source reproduces both target bodies exactly; target EAX is incidental register residue and the original C++ return-type spelling remains unknown. The connected `OnUpdateOptions @ 0x004276EB` source is corrected to remove an SFX ones-digit VM-32 `SetSprite` call that fresh TH09 decompilation proves absent from the target. | Passed native IDA target/body/caller evidence under `target:th09-main` / `factory-native-stdio`, two repository-defined VC7.1 exact replays, and connected TitleScreen exact regressions. TH08 has no direct same-name helper and contributes no fact for these two target-local routines. |
| BOUNDARY-144 | reviewed / TitleScreen shared menu seam | `SetMenuSelectionSprites` owns exactly 150 bytes at `0x00424DFF-0x00424E94`; `UpdateMenuSelection` owns exactly 20 bytes at `0x004276D7-0x004276EA`, with the independent 2,045-byte `OnUpdateOptions` owner beginning at the next byte `0x004276EB`. Exact whole-function replay covers every logical byte and reviewed relocation for both new helpers; no neighboring padding or table bytes are included. | Native IDA extents/disassembly, initial candidate inventory, exact helper replays, and immediate-next-owner evidence. |
| TOOLCHAIN-147 | two exact shared helpers / corrected non-exact Options owner | Under the established TitleScreen pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Gr /O1 /Ob1 /Oy- /I src` profile, natural `void` source reproduces `SetMenuSelectionSprites` 150/150 with two SetSprite relocations and `UpdateMenuSelection` 20/20 with one relocation to the helper. Fresh target review supersedes the older misleading `OnUpdateOptions` 2050/2045 near-match: that source contained an extra SFX ones-digit `SetSprite` on VM 32 which the TH09 target never performs. Removing the behavior yields the more accurate maintained owner at 2005/2045. A natural function-scope help-VM pointer probe leaves 2005/2045 unchanged and is not retained. No register/volatile forcing, pragma ordering, padding, assembly, fake return, target-byte embedding or target patching is used. | Canonical helper replay, target decompilation, old/new Options compiler comparison, one bounded natural loop-shape probe, and exact regressions for shared TitleScreen callers/dependencies. |

| SCOREDATA-005 | exact / authored-game / PLST progression seam | The target-bound `PLST` record at `0x004A8180` remains exactly `0x1FC` bytes and Packet 170 closes its previously opaque tail: three 16-byte character flag banks begin at `+0x4C`, `+0x5C`, and `+0x6C`, followed by a `16 x 6` signed-int character-stat table at `+0x7C`. Six authored helpers are canonical exact: `IncrementCharacterStat @ 0x0040E3A0` (37), flag setters `0x0040E3D0/0x0040E3E0/0x0040E3F0` (16 each), `HasAnyPrimaryCharacterStat @ 0x0040E400` (75), and `GetCharacterStat5 @ 0x0040E450` (20). The maintained names describe behavior; the target leaves incidental EAX values in several helpers, so original return-type spelling is not inferred from decompiler output. | Fresh TH09 native disassembly/decompilation/callers, existing exact `ScoreFileView::LoadPlayStats`, exact PLST size/offset constraints, clean committed TH08 ScoreDat/Ending source family only after TH09 layout closure, independent pinned-VC7.1 builds, and repository-defined canonical replay of all six helpers. |
| GAMEMGR-011 | exact / authored-game / shared mode predicates | `GameManagerModeView::IsGameMode1 @ 0x00404970-0x0040497E` (15 bytes) and `IsGameMode0 @ 0x0040E470-0x0040E47D` (14 bytes) are canonical exact predicates over the shared GameManager field at `+0x118`. Packet 170 also uses target-proven `side0Character @ +0x20` and `difficulty @ +0x11C` in the Ending progression callback. The minimal `GameManagerModeView` and method names are reconstruction aliases and do not claim the original full class definition or identifier spelling. | Fresh target bodies/caller graph, connected exact Player/Replay/Ending uses, focused pinned-VC7.1 probes, and repository-defined canonical replay. |
| ENDING-002 | exact / authored-game / hard frontier | `Ending::AddedCallback @ 0x0040E580-0x0040E89F` is canonical exact at 800 bytes with 85 reviewed relocations. It sets Ending lifecycle state, clears ANM/render caches, initializes fifteen ending text VMs through `g_Supervisor +0x5CC`, positions them at `(64, i*16+400, 0)`, updates PLST progression for GameManager modes 0/1, applies the observed character-unlock thresholds, selects `end00.end` through `end13.end` from the target table at `0x004A0ED8`, loads the script, and releases the shared Title fade effect at `0x004A7C80`. A first natural `mode0 \|\| mode1` source emitted 783 bytes; full target control flow proves distinct `if` / `else if` arms, and that natural shape produces the exact 800-byte body. | Fresh TH09 target body, xrefs/data bytes, exact connected lifecycle/resource APIs, reviewed 85-relocation structural receipt, and repository-defined canonical replay. Maintained source is split into `src/EndingAddedCallback.cpp` only to keep pre-existing Ending switch-table local-symbol manifests stable; original target TU partition remains unknown. |
| BOUNDARY-145 | exact / authored-only / PlayStats-Ending seam | Packet 170 reviews nine independent authored extents: `0x00404970-0x0040497E`; `0x0040E3A0-0x0040E3C4`; `0x0040E3D0-0x0040E3DF`; `0x0040E3E0-0x0040E3EF`; `0x0040E3F0-0x0040E3FF`; `0x0040E400-0x0040E44A`; `0x0040E450-0x0040E463`; `0x0040E470-0x0040E47D`; and hard owner `0x0040E580-0x0040E89F`. The gaps after `0x00404970`, `0x0040E3A0`, `0x0040E400`, `0x0040E450`, `0x0040E470`, and exact `LoadEndingScript @ 0x0040E4F0-0x0040E574` are target `CC` alignment and remain unowned. The three flag setters are contiguous. `Ending::AddedCallback` ends immediately before independently owned `RunEndingScript @ 0x0040E8A0`; no tail/table/padding is charged to its 800-byte authored extent. | Candidate inventory, fresh native target-byte reads across every selected seam, complete function disassembly, and target-bound canonical replay. |
| TOOLCHAIN-148 | exact / adjacent-source-accelerated / source-shape and TU-partition limit | Natural pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Gr /O2 /Ob1 /Oy- /I src` reproduces all eight shared helpers exactly; the hard callback additionally uses the established natural `/Oi` profile and reproduces all 800 bytes/85 relocations. The only material callback source-shape correction is target-backed `if (mode0) ... else if (mode1) ...` instead of an OR expression, closing a 783/800 result without padding, register forcing, pragma, assembly, or target-byte encoding. Adding the callback to the existing `Ending.cpp` changed only compiler-local `$L...` names in an already-exact switch-table manifest, so the maintained callback is placed in a separate reconstruction TU; all existing Ending exact units then replay unchanged. This is a maintenance partition, not evidence of the original target TU layout. | Focused helper/callback probes, target/object control-flow alignment, split-TU A/B validation, existing Ending regression replays, and nine new repository-defined exact units. |
| ENEMY-003 | mixed exact/source-present / authored-game / EnemyManager lifecycle seam | Packet 171 reconstructs the EnemyManager lifecycle shell rooted at the target-proven 0x2AC450 object. `EnemyManagerView::Create @ 0x00412240` allocates and registers `EnemyCtrlInf`, calls the explicit constructor and Initialize, binds the 0x38-byte side/opposing-side records, and installs calc/high-draw/low-draw Chain elements. `EnemyManagerView::EnemyManagerView @ 0x004121D0` constructs two 0x188 EnemyEclManager members, one spawn template plus a 129-record EnemyView vector, timer/schedule members, then explicitly calls Initialize. `EnemyManagerView::Initialize @ 0x0040FA70` is maintained but non-exact and target-locally fixes the 0x5430 spawn-template defaults, four manager timeline slots at +0x2AC430, two secondary-VM script ids, 96 trail X sentinels, flags/timers/callback thresholds, rank/sound defaults, and minimum player distance. `EnemyManagerView::AddedCallback @ 0x00411DB0` is also maintained non-exact and is physically registered by exact Create as the calc added callback; it owns enemy ANM/ECL loading and the side-0 256-byte subroutine schedule. The runtime update still processes only 128 enemy records although the constructor vector-constructs 129; the extra constructed record role remains unknown. | TH09 native-IDB functions/decompilation, exact Create relocation graph, constructor/vector-constructor calls, target memory boundary reads, maintained `src/EnemyManager.hpp` / `src/EnemyManagerLifecycle.cpp`, and clean committed TH08 EnemyManager family used only after TH09 fixed the local layout and behavior. |
| BOUNDARY-146 | reviewed / EnemyManager lifecycle logical extents | Packet 171 closes four authored logical boundaries independently of codegen status: Initialize `0x0040FA70-0x0040FC99` (554 bytes), AddedCallback `0x00411DB0-0x00411F23` (372), constructor `0x004121D0-0x00412234` (101), and Create `0x00412240-0x00412387` (328). The following physical gaps remain outside authored extents: six `CC` bytes at `0x0040FC9A-0x0040FC9F`, twelve at `0x00411F24-0x00411F2F`, eleven at `0x00412235-0x0041223F`, and eight at `0x00412388-0x0041238F`. Create's relocation to the VC7.1 EH handler at `0x0048D27B` binds compiler support state but does not make that handler authored-function bytes. The 15-byte EnemyEclManager and 20-byte schedule-runtime constructors compile structurally exact as support definitions, but explicit-vs-implicit source authorship remains unresolved; they are not promoted into authored exact ledgers. | Attested TH09 function boundaries and raw adjacent bytes, fresh target-bound COFF comparisons, canonical Create/constructor match units, and current inventory/origin ledgers. |
| TOOLCHAIN-149 | compiler-observed / mixed exact-negative Enemy lifecycle packet | Fresh pinned VC7.1 feedback separates the Packet-171 lifecycle functions. Natural `EnemyManagerView::Create` under `/O2 /Ob1 /Oy-` replays 328/328 bytes with all 29 relocations exactly. The explicit manager constructor requires `/O2 /Ob0 /Oy-` for its target shape and replays 101/101 with 9 relocations; this is a match-unit fact, not a TU/project flag claim. Natural Initialize is repeatably 557 bytes under both `/Ob1` and `/Ob0` versus the 554-byte target; a single natural flags-lifetime probe instead emits 552, so the remaining shape is not promoted or register-forced. AddedCallback emits the exact 372-byte target size under `/O2 /Ob1` but matches only 242/276 ordinary relocation-masked bytes, proving that equal size is not exactness. No target-byte embedding, padding, pragma ordering, volatile/register forcing, inline assembly, fake return, or replacement compiler is retained. | `scripts/build-match-unit.py --unit enemy-manager-create`, `--unit enemy-manager-constructor`, `scripts/compare-coff-function.py --unit ... --json`, fresh `/Ob1`/`/Ob0` lifecycle probes, and the bounded flags-lifetime negative probe. |
| ECL-013 | canonical exact / authored-game / post-interpreter TU-private owners | EnemyPostEclUpdateMovement @ 0x00408180 and EnemyPostEclUpdateShotAndAnm @ 0x00408560 are called only by EclManager::RunEcl and are not ordinary EnemyView member-ABI boundaries. TH09 call sites prove compiler-private ESI and EDI transports. Natural file-static source in the EclManager TU reproduces movement across the complete 992-byte physical owner (967-byte body + NOP + six-entry easing table) and shot/ANM across all 349 bytes. Movement requires a target-observed primaryFlags snapshot and same-TU visibility of the ordinary non-inline Float3::FromAngleMagnitude; shot/ANM requires TU-private DispatchShotInstruction visibility plus an earlier direction=0 lifetime. | Two cold canonical VC7.1 match-unit replays for each owner; complete relocation-aware 992/992 and 349/349 comparisons; sole-callsite TH09 disassembly; ExAttack 7/8/9 and all pre-existing EclManager exact units regression-replayed after TU consolidation. |
| ANM-024 | exact / authored-game / indexed script bridge | `AnmLoaded::SetAndExecuteScriptIdx @ 0x00403E00-0x00403E28` stores the `AnmLoaded` receiver at `AnmVm+0x204`, stores the low script-index word at `AnmVm+0x21A`, loads `scripts[scriptIndex]` through `AnmLoaded+0x10`, and calls exact/source-present `AnmLoaded::SetAndExecuteScript @ 0x004395A0`. This is the common ECL-to-ANM bridge used once by `UpdateMovement`, five times by `UpdateShotAndAnm`, and throughout RunEcl's ANM opcode families. Natural source is repository-canonical exact at 41 bytes with its sole relocation reviewed. | Full target body/callers, established AnmVm/AnmLoaded offsets, two cold pinned-VC7.1 function hashes, and repository-defined canonical replay of all 41 bytes plus the one REL32 destination. |
| BOUNDARY-147 | reviewed / post-ECL and indexed-ANM seams | `AnmLoaded::SetAndExecuteScriptIdx` owns 41 logical bytes at `0x00403E00-0x00403E28`; seven following `CC` bytes precede independent `0x00403E30`. `EnemyView::UpdateMovement` owns 967 logical bytes at `0x00408180-0x00408546`; 25 following alignment bytes precede `UpdateShotAndAnm @ 0x00408560`. The latter owns 349 bytes through `0x004086BC`; three `CC` bytes precede `EclManager::RunEcl @ 0x004086C0`. The movement function's six-entry easing table is compiler-owned in the later ECL table area and is physical comparison coverage rather than part of the 967-byte authored denominator. | Candidate inventory, complete target disassembly, bounded raw/function seams, compiler jump-table relocations, and exact/non-exact focused Oracles. |
| TOOLCHAIN-150 | near-structural non-exact Enemy owners / exact ANM bridge | Under pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src`, natural `EnemyView::UpdateMovement` emits 968 logical bytes plus a six-entry DIR32 easing table versus the 967-byte target body; its 21 direct calls equal the target's 21 direct calls, with the target's one indirect player callback preserved. Natural `UpdateShotAndAnm` emits 357 versus 349 bytes and reproduces all nine target direct calls, including five calls to the now-exact indexed ANM bridge. Instruction review confines the size differences to the natural `this` setup/save/restore versus target compiler-private ESI/EDI entry state; no naked function, inline assembly, register/volatile forcing, pragma ordering, padding, fake return, target-byte encoding, or target patch is retained. Replacing RunEcl's provisional free ANM seam with the exact member bridge and natural Enemy tail calls produces a repeatable 13,756-code + 772-table candidate with unchanged `376/376` aggregate calls, resolver counts `131/100/17/24`, frame `0x15C/0x168`, and current function SHA-256 `403925b357ef8bc218ae988f76caba363615ee1ea7687fe4cd0e7d87569da915`. | `scripts/report-enemy-ecl-runtime.py`, corrected `scripts/report-ecl-codegen.py` output, complete target/object instruction review, two cold pinned-VC7.1 builds, and canonical `anm-loaded-set-and-execute-script-idx` replay. |
| ENEMY-004 | mixed exact/source-present / authored-game / draw owner seam | `EnemyManagerDrawImpl @ 0x00411670-0x00411D4D` is the shared four-group Enemy renderer reached through exact high/low draw wrappers. Maintained `src/EnemyManagerDraw.cpp` covers the secondary VMs, transformed primary VM, 96-sample trail history, sprite-based strip construction, taper/fade/hidden-head modes, draw-list traversal and target render calls. The body is source-present NON-EXACT at 1,740 candidate bytes versus 1,758 target bytes. `EnemyManagerView::OnDrawHighPrio @ 0x00411D50` and `OnDrawLowPrio @ 0x00411D80` are canonical exact at 35 and 38 bytes: both prepare the side viewport, then tail-jump into the shared owner for groups 0-1 or 2-3. | Complete TH09 target body/call/data audit, exact Create registration linkage, clean committed TH08 draw-family hypothesis after TH09 closure, two cold pinned-VC7.1 body builds, and two repository-defined wrapper replays. |
| ENEMY-005 | exact / authored-game / child ECL cleanup leaf | EnemyCoreReleaseChildEclBlocks @ 0x004100E0-0x0041010F closes the four-slot child-ECL cleanup helper already referenced by maintained EnemyManager::OnUpdate. TH09 independently fixes childEclBlocks33D8[4] at Enemy +0x33D8, the 0x5430 Enemy stride, and the 128-enemy runtime array at EnemyManager +0x5758. For each non-null child pointer the target frees it through exact ZunMemory::Free and clears that slot; null slots are skipped without a store. | Fresh TH09 decompilation/disassembly/callers plus the pre-existing EnemyManager/ECL layouts. A direct /Gr free-function source with the existing EnemyCoreView contract reproduces 48/48 bytes and both relocations in two builds. The first probe placed the clear outside the non-null branch and differed only at the conditional-jump displacement, providing the control-flow correction without any register/pragma/padding steering. |
| ENEMY-006 | exact / authored-game / private same-TU release seam | EnemyManagerReleaseCore @ 0x00411F30-0x00411F73 and ReleaseSubsystem4 @ 0x00411F80-0x00411FC9 close the EnemyManager teardown seam. TH09 independently fixes the manager layout: two 0x188 EnemyEclManagers at +0/+0x188, calc/high/low ChainElem pointers at +0x310/+0x314/+0x318, and 128 runtime enemies at +0x5758 with 0x5430 stride. The core releases each enemy's four child ECL blocks through exact ENEMY-005, releases ANM slot 9, releases both ECL raw files, and returns zero; the wrapper calls it, cuts the three chains, and frees the manager. The decisive compiler fact is natural TU ownership: a plain file-static core called only by the wrapper is compiled by VC7.1 with a private EBX receiver transport, exactly matching the target even though the source-level contract remains ordinary /Gr fastcall. | Fresh TH09 bodies/caller graph, existing exact EnemyManager Create/layout evidence, exact child-block/raw-file/ANM/Chain dependencies, an isolated same-TU ABI microprobe, and two cold repository-defined replays. Both core 68/68 and wrapper 74/74 are exact; EnemyManager::Create remains exact after the header declaration update. No asm, register forcing, volatile, padding, fake return or profile search is used. |
| ENEMY-007 | exact / authored-game / gameplay cleanup reset | `EnemyManagerView::ResetForGameplayCleanup @ 0x00411FD0-0x00412080` is the sole target of `GameManager_CleanupGameplayState` at this boundary. It iterates all 128 Enemy records at manager `+0x5758` with `0x5430` stride, releases each record's four child ECL blocks through exact ENEMY-005, and clears `flags337C` bit 0. For side 0 it rebuilds all 256 `g_EnemySubroutineSchedule` bytes using the primary ECL subroutine count plus a random high-bit selector, then resets schedule index `+0x2AC3D4`, selects the first scheduled subroutine at `+0x2AC400`, and assigns the two schedule timers to 200 and 0. | Fresh attested TH09 decompilation/disassembly/xrefs plus the established EnemyManager layout and exact child-ECL helper. Two cold repository-defined VC7.1 replays reproduce 177/177 bytes and all 13 relocations under the normal optimized profile; no asm, padding, register forcing, or profile search is used. |
| BOUNDARY-148 | reviewed / EnemyManager draw seam | The shared draw owner owns exactly `0x00411670-0x00411D4D` (1,758 bytes); two `CC` bytes precede the high wrapper. `OnDrawHighPrio` owns `0x00411D50-0x00411D72`; thirteen `CC` bytes precede the low wrapper. `OnDrawLowPrio` owns `0x00411D80-0x00411DA5`; ten `CC` bytes precede independent `EnemyManagerView::AddedCallback @ 0x00411DB0`. No alignment or neighboring lifecycle bytes are charged to the authored extents. | Candidate inventory, full target disassembly/raw seams, exact wrapper relocation replay, and independent next-function boundaries. |
| TOOLCHAIN-151 | exact wrappers / stable non-exact shared draw owner | Natural pinned VC7.1 `/O2 /Ob1 /Oy- /Gr + /Oi` source reproduces the 35-byte and 38-byte wrapper bodies with all three relocations each, including their tail jumps to the file-static helper. VC7.1 naturally transports the helper's manager/start/end values through private EDX/ECX/EAX state; this is compiler/TU evidence, not a public source ABI claim. The shared owner is cold-stable at 1,740 bytes with 53 relocations versus target 1,758; `/Ob0` remains 1,740, `/Os` emits 1,720, and a TH08-style function-scope-local lifetime emits 1,726. No register/volatile forcing, pragma ordering, padding, inline assembly, fake return, target-byte encoding, or target patch is retained. | Two cold function hashes, exact wrapper match units, focused `/Ob0`/`/Os`/local-lifetime probes, and complete target/object comparison. |

| ECL-014 | mixed exact / source-present operand resolver quartet | Packet 173 reconstructs the central TH09 ECL operand resolver quartet as natural C++ in `src/EclOperands.cpp`: exact `Th09EclRunControl::ResolveInt @ 0x00405760`, exact `ResolveIntLValue @ 0x00405F20`, exact `ResolveFloatLValue @ 0x00406210`, and source-present/non-exact `EnemyView::ResolveFloat @ 0x004068A0`. TH09 independently fixes the ABI split: integer and both lvalue resolvers are fastcall-shaped, while float rvalue resolution is an `EnemyView` thiscall-shaped owner. The target independently fixes the `0x2710..0x2775` selector namespace, context/enemy/manager/player access chains, and writable float selectors `0x273A..0x273C` at Enemy `+0x2D74/+0x2D78/+0x2D7C`. Clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` and committed-only TH095 `73c2b7238cf9b1ca1b0aea5e34a7791de81ffe08` supplied source-family hypotheses only; TH095 live untracked files were excluded. | Fresh attested TH09 decompilation/xrefs/callsite ABI evidence, complete selector review, committed adjacent source-family comparison, maintained target-backed layout assertions, pinned VC7.1 feedback, and canonical replay for the three exact owners. |
| BOUNDARY-149 | reviewed / ECL operand code-versus-switch-data ownership | Packet 173 keeps authored code and compiler switch data separate. `ResolveInt` owns code `0x00405760-0x00405D83` (1,572 bytes), followed by compiler switch data beginning `0x00405D84`; its physical canonical comparison runs through `0x00405F1B` (1,980 bytes total). `ResolveIntLValue` owns `0x00405F20-0x0040610A` (491 bytes), byte `0x0040610B` is alignment, and its physical compare runs through `0x004061F5` (726 bytes). `ResolveFloatLValue` owns `0x00406210-0x004064CD` (702 bytes), `0x004064CE-0x004064CF` are alignment, and its physical compare runs through `0x004065D7` (968 bytes). `EnemyView::ResolveFloat` owns `0x004068A0-0x00406F00` (1,633 bytes), `0x00406F01-0x00406F03` are alignment, and compiler switch data starts at `0x00406F04`; the natural source emits the target-sized 2,044-byte physical extent through `0x0040709B`. Only the four code extents enter the reviewed authored denominator. | Candidate inventory, native IDA extents, bounded target disassembly across each seam, COFF table-label offsets, and repository `compare_size` switch-owner precedent. |
| TOOLCHAIN-152 | three canonical exact switch owners / one stable non-exact hard owner | Under pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src`, Packet 173 canonicalizes three switch-heavy owners with explicit code-only `size` plus physical `compare_size`: ResolveInt 1,572/1,980 bytes with 165 relocations, receipt SHA-256 `60ca8755bbe13fabc24595616ce35d083677f24341ac83a94e9ab5e7c03b3580`; ResolveIntLValue 491/726 with 39 relocations, receipt `0e15b5ff65a122bac3d05e075276f2e30014bdcee95a952f92fd68214344117c`; ResolveFloatLValue 702/968 with 49 relocations, receipt `6304d640bb7ceca52d29541ab63940001723c07a8ee9c10e36327aa92f212e91`. Two repository-builder passes produce byte-identical JSON receipts for all three. `EnemyView::ResolveFloat` reaches the exact 2,044-byte physical code-plus-table size but remains repeatably NON-EXACT at 1,536/1,560 ordinary comparable bytes; the residual is localized to the opposing-player register-scheduling block. `/Ob0` and two bounded natural common-subexpression variants do not improve it, so exact chasing stops without register/volatile forcing, pragmas, padding, assembly, fake returns, or target-byte material. | Cold A/B compiler diagnostics, repository canonical A/B replay, reviewed relocation manifests, focused source-family corrections, and negative profile/source-shape probes summarized in this knowledge row. |
| ECL-015 | mixed exact/source-present / helper-band closure | The maintained source covers all fifteen reviewed ECL helpers in the hard `0x00406680-0x0040817A` band: context pop/call, primary and extra ANM-script setup, polar/relative/boundary/random movement, linear/Hermite interpolation and slot installation/application, typed comparison, and bullet-shot dispatch. `PopContext @ 0x00406680` is now repository-canonical exact at 147 bytes with two reviewed relocations: placing this sole-caller helper as `static` in the same `EclManager.cpp` TU as `RunEcl` lets VC7.1 naturally select the target-private EAX entry transport. `InterpolateLinear @ 0x00407450` remains canonical exact at 74 bytes with three reviewed relocations. The other thirteen helper owners remain source-present/NON-EXACT; `DispatchShotInstruction @ 0x00408040` remains a maintained 329-byte candidate for the 315-byte target. | Complete TH09 helper decompilation/disassembly and sole call-site review, target-local Enemy/ECL geometry, baseline external-fastcall mismatch, TU-local static compiler probe, two cold pinned-VC7.1 canonical replays, and relocation-aware target comparison. |
| ECL-016 | source-present/non-exact / RunEcl shared-spawn-tail convergence | The maintained `EclManager::RunEcl` source now expresses opcodes 93 and 94 as two target-observed packet/position lifetimes converging on one shared `SpawnEnemy` call tail. This closes the target's one-call-site shape without collapsing operand evaluation: the candidate preserves resolver multiplicities `131/100/17/24`, all `187` opcode table entries and six easing entries. The current cold VC7.1 result is 14,420 logical code bytes plus 772 table bytes versus target 14,792 plus 772; direct calls are 375/376, and the remaining gap is one unidentified direct call together with stack/local allocation and basic-block/TU ordering. `RunEcl` therefore remains NON-EXACT and receives no exactness promotion. | `scripts/report-ecl-codegen.py` over a fresh pinned-VC7.1 object, target switch/table boundaries, exact operand resolvers, one-site `SpawnEnemy` relocation count, and preserved source-family control flow. |
| BOUNDARY-150 | reviewed / ECL helper-band logical extents | The fifteen maintained helper owners have independently reviewed target extents: `0x00406680` 147 bytes, `0x00406740` 80, `0x004070A0` 346, `0x00407200` 288, `0x00407320` 296, `0x00407450` 74, `0x004074A0` 188, `0x00407560` 157, `0x00407600` 255, `0x00407700` 842, `0x00407A80` 149, `0x00407B20` 261, `0x00407C30` 510, `0x00407E30` 516, and `0x00408040` 315. Intervening alignment/gaps are not charged to authored functions. The band ends at `0x0040817A`; five following bytes precede independent `EnemyView::UpdateMovement @ 0x00408180`. | Candidate inventory, IDA extent readback, raw seam review, and function-origin ledger reconciliation. |
| TOOLCHAIN-153 | two exact helpers / natural private-ABI evidence and remaining TU-order limit | Natural pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` reproduces `InterpolateLinear` 74/74 with all three relocations and now `PopContext` 147/147 with both relocations. The latter closes only after target-local caller evidence motivates TU-private `static` visibility in `EclManager.cpp`; VC7.1 then chooses EAX naturally, whereas the externally visible `__fastcall` form reproducibly chose ECX and matched only 138/139 ordinary bytes. The remaining helper cohort stays intentionally natural C++: no naked functions, register/volatile forcing, pragmas, padding, assembly, fake returns, or target-byte encoding are used to imitate compiler-private handoffs. The full Player and RunEcl owners remain independent non-exact frontiers. | Baseline external helper build, TU-local static probe, formal EclManager TU build, canonical helper replays, target sole-caller disassembly, and source-forcing audit. |
| TOOLCHAIN-169 | exact / PopContext TU-private ABI | For `PopContext @ 0x00406680`, VC7.1 `/O2 /Ob1 /Gr` emits the target's private EAX argument transport from ordinary C++ when the helper has internal linkage in the same translation unit as its sole `RunEcl` caller. The externally visible `__fastcall` version emits ECX and differs in exactly the ModRM byte at function offset `+0x3`; the TU-private version reproduces all 139 ordinary comparable bytes plus both relocation fields over the complete 147-byte extent. The target caller independently executes `mov eax, edi; call 0x00406680`. No register forcing, inline assembly, padding, pragma ordering, or profile fishing is involved. | Hash-attested target caller/body, baseline 138/139 comparison, static-linkage compiler probe, formal `EclManager.cpp` object, and two canonical cold replays. |

## Packet 175 CardAttack knowledge

- `CardAttackInf` is a TH09-local registry anchor for a per-side authored owner, not a direct transplant of the TH08/TH095 classic `Spellcard` object. TH09 `CardAttack::Create @ 0x00404600` allocates `0x1368` bytes per side, while the consulted adjacent classic Spellcard family uses a materially different approximately `0x2644` layout. Adjacent source is therefore naming/lifecycle hypothesis material only.
- Reviewed CardAttack owner functions are `RunEndCallbacks @ 0x00403D60` (31), `RefreshResource @ 0x00403D80` (12), `OnUpdate @ 0x004041F0` (536), `OnDraw @ 0x00404410` (334), `AddedCallback @ 0x00404560` (8), `Destroy @ 0x00404570` (51), constructor `@ 0x004045B0` (66), and `Create @ 0x00404600` (264). The owned function bytes total 1,302; observed `CC` alignment between several owners remains unowned.
- The target-backed owner layout is size `0x1368`: side index `+0x00`, side/opponent state pointers `+0x04/+0x08`, calc/draw chains `+0x0C/+0x10`, presentation `AnmLoaded *` `+0x14`, `ZunTimer +0x98`, presentation counters `+0xA4/+0xA8`, two `0x2A4` AnmVm objects at `+0x0AC`, five at `+0x5F4`, and update/draw/end callback slots at `+0x1328..+0x133C`. The remainder and original source identifiers stay unknown.
- `Create` binds 0x38-stride side/opponent state records, installs `OnUpdate` on the calc chain at priority `side+17`, installs `OnDraw` on the draw chain at priority `side+24`, and returns null only when calc-chain insertion fails. `Destroy` runs the two end callbacks, cuts both chain elements, and frees the owner.
- Repository-defined pinned VC7.1 replays are exact for six CardAttack functions: `RunEndCallbacks` 31/31, `RefreshResource` 12/12, `AddedCallback` 8/8, `Destroy` 51/51, constructor 66/66, and `Create` 264/264. The constructor requires a function-focused `/O2 /Ob0` profile to preserve VC7.1 vector-constructor calls; the other five use the established `/O2 /Ob1` profile. This is not a TU- or project-wide flag claim.
- `OnUpdate` and `OnDraw` are maintained natural source but remain NON-EXACT. The current pinned `/O2 /Ob1` objects are 520 versus 536 target bytes and 324 versus 334 target bytes respectively. Their target-observed behavior/layout is retained without register forcing, inert padding, assembly, or target-byte embedding.

| FRONTSIDE-001 | mixed exact/source-present / authored-game | TH09 `FrontSideInf` is the per-side gameplay HUD owner, not the TH095 title `FrontEnd` family. `FrontSide::Create @ 0x0041A5D0` allocates/registers exactly `0xABE8` bytes, binds the two `0x38` GameManager side records, and installs calc/draw callbacks at priorities `side+23` / `side+26`. The first `0xA65C` bytes are 63 contiguous `0x2A4` AnmVm objects, but the target constructor groups them as 13 source arrays/singletons before `transitionState +0xA65C`, chain pointers, front ANM, side pointers, a `0x558` auxiliary owner at `+0xA678`, and two ZunTimers at `+0xABD0/+0xABDC`. Fifteen target-proven FrontSide functions are now authored/source-present; twelve are canonical exact for 927 bytes. `SetTimingDigits @ 0x00418950`, `OnUpdate @ 0x00418A90`, and `AddedCallback @ 0x0041A000` remain explicitly NON-EXACT. | Registry strings `FrontSideInf`/`front.anm`, passed native TH09 IDA callgraph/disassembly, Player/GameManager callers, pinned VC7.1 diagnostics, and two post-fix repository-defined cold replay passes for all eleven exact units. |
| FRONTSIDE-002 | source-family hypothesis / adjacent reuse discipline | Clean committed TH08 `a45e99fb1942714e6edded20847e32a654d56f97` supplies the gameplay `Gui`/`front.anm` source family. Committed TH095 `b3f6205633f78fb552887052d19de97567bcfe92` supplies `PhotoFront` lifecycle/front-ANM hypotheses, while its live tracked/untracked changes were excluded. TH09 independently disproves direct layout transfer: its `FrontInf` owner is `0x1293C` and its per-side `FrontSideInf` owner is `0xABE8`, with target-specific side bindings and VM grouping. Adjacent code therefore influenced subsystem naming and natural lifecycle shape only; no adjacent address, size, layout, ABI, ownership, or exactness was imported. | Adjacent committed-source search after TH09 string/registry anchoring, recorded adjacent HEAD/live status, and TH09-local target/compile validation. |
| FRONTSIDE-003 | negative Oracle / hard frontier | `FrontSide::OnUpdate @ 0x00418A90-0x004193DC` is the hard 2,381-byte HUD calc owner. Maintained source covers the target-observed status/score/stock digits, gauge scales and threshold colors, nested VMs, marker/rank visibility, transition state, `ZunTimer::operator--(int)` plus exact `ZunTimer::operator<=(int)` tail, and update timer. Final pinned VC7.1 `/O2 /Ob1` emits 2,458 bytes, so source presence is retained without exactness. Connected negative Oracles are `AddedCallback` 460/460 bytes but 334/384 ordinary comparable bytes and `SetTimingDigits` 319/319 but 261/299. The former `ResetTransitionState` 39/43 residual was not a compiler plateau: target disassembly exposed a stale field-owner hypothesis and FRONTSIDE-005 closes it exactly. No register/volatile forcing, padding, assembly, fake return, or target-byte embedding is retained. | Final target-bound negative comparisons plus source/target call-flow review. |
| FRONTSIDE-004 | exact diagnostic / origin unresolved | Constructor-shaped `0x0041A470` (28 bytes) and `0x0041A490` (307 bytes) both replay structural-zero-difference under focused pinned VC7.1 `/O2 /Ob0`; the outer constructor reproduces all 26 relocations and the thirteen VM construction groups. This does not decide explicit authored constructor versus compiler-synthesized implicit member construction. Both candidates deliberately remain `unknown/review` with no authored/source/exact ledger credit. | Final `/Ob0` compiler diagnostic and passed IDA constructor-shape review; origin kept independent from codegen equality. |
| FRONTSIDE-005 | exact / corrected auxiliary-state owner | `FrontSide::ResetTransitionState @ 0x0041A230-0x0041A262` is canonical exact at 51 bytes with two reviewed `ZunTimer::operator=(int)` relocations. The earlier 39/43 near match incorrectly mapped its conditional state to the independently proven meter-transition field `+0xA65C`. Native TH09 disassembly instead loads/stores `FrontSide +0xABCC`, which is exactly `auxA678 +0x554`; changing only that source owner produces 51/51 equality under the established natural FrontSide profile. The helper clears `updateTimer +0xABDC`, conditionally writes auxiliary state 2, and assigns 30 to `transitionTimer +0xABD0`. Exact `ActivateMeterVms`/`DeactivateMeterVms` continue to prove `+0xA65C` as a separate meter state. The sole target caller is `GameManager::CleanupGameplayState`, which applies the helper once per side. | Attested target body and sole caller, maintained `FrontSideAuxView` geometry with a new `+0x554` offset assertion, two cold canonical replays, and regression of all eleven pre-existing exact FrontSide units. |
| BOUNDARY-165 | reviewed / authored-only | The reviewed FrontSide seams leave only unowned `CC` alignment between functions: `0x418920`→`0x418930` 2 bytes, `0x418930`→`0x418950` 6, `0x418950`→`0x418A90` 1, `0x418A90`→`0x4193E0` 3, `0x41A000`→`0x41A1D0` 4, `0x41A1D0`→`0x41A210` 5, then gaps of 11/13/1/13/4/8/11/2/4/13 bytes through the constructor/factory band. `FrontSide::Create` ends at `0x0041A6EA` and independent `0x0041A6EB` begins immediately. No alignment bytes are charged to authored extents. | Target-bound `boundary-seams.txt` generated from the attested private PE and candidate ledger. |
| RUNTIME-001 | exact / authored-game / reusable ZunTimer arithmetic family | Packet 177 closes ten previously unclassified TH09 `ZunTimer` members for 224 authored bytes: `IncrementFloat @ 0x0042F490` (93), `IncrementInt @ 0x00406600` (19), `DecrementFloat @ 0x004065E0` (21), `DecrementInt @ 0x00406620` (18), integer/float add-assign wrappers at `0x00406640/0x00406650` (9/9), float subtract-assign at `0x00406660` (9), postfix decrement at `0x00406670` (10), modulo at `0x0040F830` (16), and greater-than at `0x00403DE0` (20). TH09 independently fixes the float core: `previous=current`; Supervisor game-speed `+0x5B8` selects direct versus scaled `subFrame` advance; `current` truncates `subFrame`; Supervisor flags `+0x5D4 bit 0x20` applies the observed `-999999` previous sentinel and adds `current` back to `subFrame`. Clean committed TH08 supplied only the broad Increment/Decrement naming family; its timer semantics were not transferred. Mutator aliases in the ledger distinguish overloads and do not claim original return contracts. | Passed native TH09 IDA bodies/callers, exact existing-caller relocation metadata, TH09-local Supervisor offsets, natural `src/ZunTimer.cpp`, complete PE-aware boundary review, and two independent repository-defined cold pinned-VC7.1 replays with every one of the ten units exact. |
| RUNTIME-002 | exact / authored-game / reusable support-leaf seam | Packet 177 adds six connected support functions for 277 authored bytes: `Supervisor::SelectSide @ 0x00401440` (26), `GameManager::IsReplayNeutral @ 0x00415D20` (13), `GameManager::IsGameMode2 @ 0x00415D30` (15), `Front::Reset @ 0x00415D40` (14), `PlayerAnmVmView::SetZRotation @ 0x0040F840` (20), and `AnmLoaded::SetSprite @ 0x00436AC0` (189). `Front::Reset` is owner-bound by the direct factory `0x00418840`, which allocates `0x1293C`, registers `"FrontInf"`, then immediately calls the reset. `SetSprite` is the packet's medium anchor: TH09 validates loaded state, publishes ANM/sprite ownership into the VM, copies sprite dimensions, initializes matrices `+0x130/+0x1B0`, derives scales, copies matrix1 to matrix2, and returns 0/-1. Its two matrix-identity relocations target `0x00401300`, which remains deliberately `unknown/review` because an exact relocation destination does not establish provenance. | Exact FrontSide/Player/Supervisor/Replay callers, direct FrontInf factory evidence, native TH09 SetSprite disassembly, committed TH08/TH095 source-family comparison used only as a hypothesis, natural maintained source, and two cold canonical replay passes with all six units exact. |
| BOUNDARY-RUNTIME-001 | reviewed / authored-only support seams | Packet 177 rechecks every promoted extent with PE-aware target disassembly after explicitly discarding an earlier invalid raw-file-offset experiment. Unowned alignment is: six `CC` bytes after `0x00401440-0x00401459`; twelve after `0x00403DE0-0x00403DF3`; eleven/13/14/7/7/7/6 after the `0x004065E0-0x00406679` timer band; no gap between `ZunTimer::Modulo @ 0x0040F830-0x0040F83F` and independent VM rotation at `0x0040F840`; twelve after rotation; three/one/two after `0x00415D20/30/40`; three after the 93-byte timer core `0x0042F490-0x0042F4EC`; and three after `AnmLoaded::SetSprite @ 0x00436AC0-0x00436B7C`. No padding is charged to authored extents. | Candidate inventory plus corrected PE-aware `objdump --start-address/--stop-address` review recorded in the reviewed candidate ledger. The earlier incorrect RVA-to-raw-offset byte dump was rejected and is not evidence. |
| TOOLCHAIN-RUNTIME-001 | exact / pinned VC7.1 support-leaf closure | Sixteen Packet-177 units totaling 501 authored bytes reproduce the target in two independent cold repository-defined replay passes. `ZunTimer`, Supervisor viewport, Front reset, Player VM rotation and SetSprite use the already-established `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` profile; GameManager predicates retain their existing TU-local profile without `/Oi`. The only source-shape correction needed for the 93-byte timer core was to express the target's natural `if (gameSpeed > 0.99f) direct; else scaled` branch rather than an algebraically equivalent reversed conditional, which changes VC7.1 x87 branch lowering. Existing exact `ZunTimer::SetCurrent`, Supervisor viewport, GameManager mode-0, and FrontSide/SetSprite caller units remain exact after the source changes. No register/volatile forcing, padding, pragma ordering, assembly, fake return, embedded target bytes, or target patching is used. | Configured canonical match units, two cold compiler/replay rounds, existing-unit regressions, and target-bound relocation manifests. |
| ANMINIT-001 | exact / authored-game / ANM initialization support chain | Packet 178 resolves the Packet-177 `0x00401300` dependency into a target-local shared 4x4 identity setter and closes the connected ANM initialization chain. `AnmMatrixRuntime::SetIdentity @ 0x00401300` is 57 bytes, writes twelve zero off-diagonal elements plus four 1.0f diagonal elements and returns `this`; the maintained type/name is a reconstruction alias and the original TH09 class/type spelling remains unknown. `AnmVm::Initialize @ 0x004127B0` is 66 bytes: it clears exactly the target-observed 0x208-byte initialization prefix, sets scale +0x18/+0x1C to 1.0f, color +0x1F0 to 0xffffffff, initializes matrix +0x130, sets flags word +0x1F8 to 7, then tail-jumps to exact `ZunTimer::Initialize` at +0x38. `AnmLoaded::InitializeAndSetSprite @ 0x00412800` is 41 bytes and calls exact `AnmVm::Initialize`, publishes `this` at VM +0x204, then calls exact `AnmLoaded::SetSprite`. | Fresh TH09 native-IDA bodies/xrefs, exact existing caller manifests, natural maintained `src/AnmMatrixRuntime.cpp/.hpp` and `src/AnmVmInitialize.cpp`, two independent cold repository-defined replay passes, and read-back shared-IDB metadata under passed target attestation. |
| ANMINIT-002 | exact / authored-game / math leaf plus SDK-origin exclusion | Packet 178 also closes `Float3::operator/=(float) @ 0x004010D0` at 38 bytes. Natural source computes one reciprocal `1.0f/scalar`, multiplies x/y/z in place, and returns `this`; two cold target-bound replays are exact with the sole DIR32 relocation to the shared 1.0f constant. Nearby `0x00401290/0x004012C0/0x004012E0` are intentionally not promoted as authored leaves: TH09 bodies/callers plus clean committed TH08 exact mappings/source guidance identify the family as `D3DXVec3Length`, `D3DXVec3LengthSq`, and `D3DXVec3Dot` SDK/header-inline COMDAT support. Their code identity does not make them ZUN-authored, and Packet 178 does not copy SDK bodies into TH09 source merely to manufacture exact symbols. | TH09 target bodies/xrefs, exact Float3 caller family, committed TH08 mapping/build guidance used only after TH09-local classification, and canonical Float3 replay/regression. |
| BOUNDARY-ANMINIT-001 | reviewed / authored-only ANM/math seams | PE-aware target disassembly fixes the four promoted logical extents and leaves alignment unowned: `Float3::operator/= 0x004010D0-0x004010F5` then ten `CC`; `AnmMatrixRuntime::SetIdentity 0x00401300-0x00401338` then seven `CC`; `AnmVm::Initialize 0x004127B0-0x004127F1` then fourteen `CC`; `AnmLoaded::InitializeAndSetSprite 0x00412800-0x00412828` then seven `CC` before independent `0x00412830`. No padding is charged to authored bytes. The target remains the operator-supplied read-only 685,056-byte PE with unchanged SHA-256/MD5. | `scripts/verify-target.py resources/th09.exe` plus PE-aware `objdump --start-address/--stop-address` review of all four seams. |
| TOOLCHAIN-ANMINIT-001 | exact / natural TU partition evidence / negative Oracle | The four new Packet-178 units use pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` and replay exact in two independent cold rounds; existing 189-byte `AnmLoaded::SetSprite` is rebuilt in both rounds and remains exact after both matrix calls are unified through the shared helper. Old exact Float3 ctor/add/subtract/multiply units also remain exact. Two source-shape negatives are durable: declaring the matrix setter `void` emits 55 bytes and misses the target's initial `mov eax,ecx`, while returning `this` naturally closes 57/57; defining the identity helper in the same TU as `AnmVm::Initialize` emits a natural 64-byte caller-saved EDX form, whereas placing the helper out-of-line in its own TU naturally produces the target 66-byte ESI-preserving form. No register/volatile forcing, padding, pragma ordering, assembly, fake return, embedded target bytes, or target patching is used. | Two cold compiler/replay rounds, the focused negative probes summarized here, configured match units, and exact shared-TU regressions. |
| BULLET-002 | exact/source-present / authored-game / transform-builder leaf seam | Packet 179 resolves the TH09 `0x00412610-0x0041277E` helper band as two parallel transform-program builder families over already target-backed layouts. `Bullet::InstallTransformRecord @ 0x00412610` operates on live-Bullet `transformFlags +0xDB8`, `transformIndex +0xDD4`, and 0x18-byte records at `+0xDD8`; `BulletSpawnDescriptor::InstallTransformRecord @ 0x00412650` operates on descriptor records at `+0x20` and flags at `+0x1FC`. Both cores are now canonical exact at 54 and 37 bytes. Six thin wrappers remain canonical exact: live-Bullet vector acceleration `0x00412680` (39), wait `0x004126B0` (31), sprite `0x004126D0` (37), and descriptor polar acceleration `0x00412700` (39), vector acceleration `0x00412730` (39), wait `0x00412760` (31). Their kind constants are TH09 target values `0x10`, `0x20`, `0x4000`, and `0x20000`, and the target fixes the payload word/float ordering independently of adjacent source. | Fresh TH09 native-IDB bodies/xrefs, target-backed Bullet/descriptor offsets already maintained in `BulletManager.hpp`, the 574-byte TH09 caller `0x0040C530`, committed TH08/TH095 transform families only after TH09 role closure, natural VC7.1 source, two cold canonical passes for both cores and all six wrapper regressions. |
| BULLET-003 | exact / authored-game / Etama lifecycle and Float3 support leaves | Packet 179 additionally closes `Float3::operator-= @ 0x004125B0` (36 bytes), `PreloadEtamaStartup @ 0x004125E0` (24), and `ReleaseEtamaAnm @ 0x00412600` (14). The Float3 helper is independently used by exact Supervisor loading draw code. The Etama pair is target-local: exact `Supervisor::StartupThread` is the sole preload caller, exact `Supervisor::DeletedCallback` is the sole release caller, and the target fixes AnmManager slot 8 plus literal `"etama.anm"`. All three functions are repository-canonical exact in two cold replay rounds. | Existing exact caller relocation manifests, fresh target disassembly/xrefs, exact AnmManager PreloadAnm/ReleaseAnm dependencies, natural pinned VC7.1 source, and two cold canonical replays. |
| BULLET-004 | exact / authored-game / TU-private transform updates | `UpdateBulletDeceleration @ 0x00413460-0x004134CD` is canonical exact at 110 bytes and eight relocations after natural source uses the target's folded `5.0f / 16.0f = 0.3125f` coefficient instead of two x87 multiplies. `UpdateBulletPolarAcceleration @ 0x00413590-0x0041362D` is canonical exact at 158 bytes and eight relocations when the two delta fields are read directly from Bullet `exStates[2]`: target uses Bullet-relative `+0xFF0/+0xFEC`, not shorter offsets from the live EDI timer alias. The same-TU private ESI Bullet transport remains compiler-derived; neither helper's exactness establishes the original translation-unit partition or whole-product build. | Factory-native attested TH09 disassembly and constant reads, complete byte/relocation comparisons, canonical manifests, and two cold 9/9 same-TU exact replays. |
| BOUNDARY-BULLET-001 | reviewed / authored transform-leaf seams | Packet 179 reviews the complete logical band without charging compiler alignment: `0x004125B0-0x004125D3` then 12 CC; `0x004125E0-0x004125F7` then 8 CC; `0x00412600-0x0041260D` then 2 CC; `0x00412610-0x00412645` then 10 CC; `0x00412650-0x00412674` then 11 CC; `0x00412680-0x004126A6` then 9 CC; `0x004126B0-0x004126CE` then one CC; `0x004126D0-0x004126F4` then 11 CC; `0x00412700-0x00412726` then 9 CC; `0x00412730-0x00412756` then 9 CC; `0x00412760-0x0041277E` then one CC. Independent `0x00412780` begins next and is not absorbed into the transform family. | Current candidate inventory plus PE-aware bounded target disassembly and the reviewed candidate ledger. |
| TOOLCHAIN-BULLET-001 | eleven exact leaves / natural statement-order scheduling closure | Packet 179 plus the exact-frontier follow-up now reproduce eleven Bullet/Etama/Float3 leaves totaling 381 bytes under pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src`. The former core size gaps were source-order effects, not profile requirements: writing `record->kind = kind` before `record->allowWhileActive = allowWhileActive` makes VC7.1 naturally load kind into EDX, preserve allowWhileActive in ESI, and emit the target scheduling. `Bullet::InstallTransformRecord` becomes 54/54 and `BulletSpawnDescriptor::InstallTransformRecord` becomes 37/37, while the compiler still schedules the target store order (allow first, then kind). Six exact wrappers regress unchanged. No register/volatile forcing, padding, pragma ordering, assembly, fake return, target-byte material, or target patching is used. | Two cold canonical core replays, all six exact wrapper regressions, target body review, and the earlier natural profile matrix retained as negative context. |

## Packet 180 Etama initialization leaves

- `EtamaController::Initialize @ 0x004123C0` is canonical exact at 173 bytes with no relocations. TH09 independently fixes the complete `0x25E1C0` owner clear, primary/secondary pool starts at `bullets[0]` / `bullets[176]`, sentinels at `bullets[175]` / `bullets[536]` with state 6, `cancelItemType=5`, and the five AnmVm `scriptIndex=-1` writes for each of 175 primary and 360 secondary live bullets. Exact `EtamaController::Register` already binds the source-level `Initialize` member name.
- Neutral reconstruction alias `EtamaController::ResetProjectiles @ 0x00412470` is canonical exact at 95 bytes with no relocations. Its sole target caller is `GameManager::CleanupGameplayState`; it clears 176 primary-pool records including the sentinel, 361 secondary-pool records including the sentinel, all 48 Laser records, then restores both pool pointers, state-6 sentinels, and `cancelItemType=5` without clearing sprite templates, Chain ownership, or side bindings.
- `fsincos @ 0x00412780` is not an authored leaf. TH09 exposes a 33-byte x87 helper with `ECX=sine`, `EDX=cosine`, stack `float angle`, one Etama draw caller, and an `FSINCOS` instruction. Clean committed TH08 independently classifies the same helper family as math-runtime/library. TH09 therefore records `library / MathRuntime / exclude`; no maintained authored replacement, canonical match, or inline-assembly workaround is added.
- Boundaries remain independent: three `CC` bytes after Initialize, one after ResetProjectiles, and fifteen after fsincos. Two independent cold pinned-VC7.1 `/O2 /Ob1` builds reproduce both authored functions exactly. Adding only the ResetProjectiles declaration to the shared Bullet header leaves the seven pre-existing Packet-179 BulletManager exact units exact.

## Packet 181 Player collision queue and Etama constructor knowledge

- `PlayerCollisionQueryStateView::AppendLaserRecord @ 0x00412830` is a TH09-local authored producer for the already reviewed Player `+0x36C` collision-query owner. The state is exactly `0x1808`: owner pointer +0x00, 128 records of 0x30 bytes at +0x04, count at +0x1804. Three target callers in `EtamaController::OnUpdate` pass laser geometry to this member. Natural source appends position/origin/half-size vectors, halves half-size x/y, sets radius to zero, records angle/tail dword, increments count, and replays canonical exact at 245 bytes with two `0.5f` relocations.
- `EtamaController` does not own one flat 537-element Bullet source array. Target constructor code emits independent vector-constructor calls for `primaryBullets[176] @ +0x1A900` and `secondaryBullets[361] @ +0xD2FC0`, followed by `lasers[48] @ +0x24D424`. Splitting the maintained layout on that physical source boundary preserves exact Initialize/Reset/Register and all reviewed shared-header regressions.
- `EtamaController::EtamaController @ 0x00415050` is explicit authored behavior rather than only an implicit empty special member: after constructing 32 BulletTypeSprites, the separate 176/361 Bullet arrays, 48 Lasers, and `ZunTimer +0x25E174`, the target explicitly calls `EtamaController::Initialize`. Natural `{ Initialize(); }` source with the target-evidenced member split replays exact at 121 bytes with ten relocations under a focused `/O2 /Ob0` unit. Child constructor candidates `0x00414FA0`, `0x00414FE0`, `0x00415020`, and `0x0041EE20` retain independent unknown/review origin/source states.
- Packet 181 corrects a known semantic omission in maintained `EtamaController::OnUpdate`: the three laser hitbox paths queue `AppendLaserRecord` entries into Player +0x36C rather than immediately calling the collision routine. The corrected hard owner remains NON-EXACT at 2256 object bytes versus the 2195-byte target; semantic correction is retained even though the byte-size gap grows relative to the older placeholder source.

## Packet 182 Etama, Supervisor, and ANM leaf knowledge

- `EtamaController::RemoveAllBullets @ 0x00412590` and `RemoveBulletsInRadius @ 0x004125A0` are authored parameterized gameplay APIs even though TH09 implements them as empty `ret 4` / `ret 8` bodies. ECL call sites independently prove the per-side owner and arguments. `ClearBulletsForTransition @ 0x00405650` calls `RemoveAllBullets(1)`. Exact codegen requires the natural callee/wrapper TU separation; putting the known-empty callee in the same TU allows VC7.1 to optimize the wrapper away.
- TH09 `Supervisor::LoadConfig` plus `GameConfiguration` layout closes the configuration-predicate family: Supervisor `+0x439` is the `windowed` byte and `+0x450` is the options dword. Packet 182 makes bit 0 force-16-bit-textures, bit 1 reference rasterizer, bit 3 disabled DirectInput, bit 5 disabled VSync, and bit 6 disabled text-background detection canonical exact, joining existing exact bit 2 fog-disable and bit 4 music-preload predicates.
- `AnmManager::ResetFrameDebugInfo @ 0x0042F760` clears the four frame counters in target store order `+0x10,+0x14,+0x0C,+0x18`; `ClearCameraSettings @ 0x0042D2E0` writes `0xff` to independently established `cameraMode +0x12888`. Both are canonical exact and sit inside the same target reset sequence as the existing exact render-cache clear helpers.
- `SupervisorSubthreadIsRunning @ 0x0042ED70` is a reconstruction-facing `__fastcall` alias for the TH09 predicate used by Supervisor update/network code. The target tests `runningSubthreadHandle +0x6AC != 0` and is canonical exact at 14 bytes. The original source identifier/member-vs-free spelling remains unclaimed.
- The constructor-shaped Etama children at `0x00414FA0`, `0x00414FE0`, `0x00415020`, and `0x0041EE20` remain `unknown/review`: target code and adjacent family source do not independently distinguish user-authored empty constructors from compiler-synthesized special members, so code similarity alone is not origin evidence.
| SUPERVISOR-030 | exact / authored-game / frame-reset network leaves | Packet 183 closes three TH09 Supervisor support leaves around the frame/reset path. `SupervisorNetworkMarkActive @ 0x0042E9A0` and `SupervisorNetworkMarkInactive @ 0x0042E9B0` are neutral reconstruction aliases for the paired 11-byte ECX-only setters of `g_SupervisorNetworkState->active @ +0xA8`; target callers independently reload `g_SupervisorNetworkState @ 0x004B42D0`. `ResetSupervisorServiceChannels @ 0x0042E9C0` is the existing 27-byte `Supervisor::OnUpdate` seam which calls `SampleNetworkInput @ 0x0042B850` with ECX 0 and 1 then tail-jumps with ECX 2. All three are repository-canonical exact in two cold VC7.1 rounds. The exact wrapper does not classify or promote the 1,507-byte `0x0042B850` callee, which remains `unknown/review`. | Fresh passed native-IDA bodies/xrefs and caller contexts, target-backed `SupervisorNetworkState` geometry, natural maintained source, and repeated repository-defined relocation-aware replay. |
| ANM-025 | exact / authored-game / Supervisor-frame ANM reset wrapper | `AnmManager::ResetForSupervisorFrame @ 0x0042F770-0x0042F7B9` is the 74-byte wrapper called only by `Supervisor::OnUpdate` with `g_AnmManager` in ECX. It calls exact `ClearVertexShader`, `ClearSprite`, `ClearTexture`, `ClearColorOp`, `ClearBlendMode`, `ClearZWrite`, `ResetFrameDebugInfo`, `ClearCameraSettings`, and `SetMixColorDefault` in target order, then clears target-proven screen-shake y at `+0x20` followed by x at `+0x1C`. Natural source in its own TU reproduces all 74 bytes and all nine REL32 relocations in two cold canonical rounds. | TH09 caller/disassembly, existing exact callee surfaces, independently maintained screen-shake layouts, and pinned VC7.1 replay. |
| BOUNDARY-151 | reviewed / Supervisor and ANM frame-reset leaves | Packet 183 independently reviews four logical extents: `0x0042E9A0-0x0042E9AA`, `0x0042E9B0-0x0042E9BA`, `0x0042E9C0-0x0042E9DA`, and `0x0042F770-0x0042F7B9`. The first three are followed by five `CC` bytes each; the ANM wrapper is followed by six `CC` bytes before exact `Supervisor::LoadConfig @ 0x0042F7C0`. No alignment byte is charged to authored code. | PE-aware target disassembly and candidate-ledger extents. |
| TOOLCHAIN-154 | exact / pinned VC7.1 / shared-owner cleanup | Packet 183 adds four canonical units totaling 123 authored bytes under the established `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` profile. Two independent cold rounds reproduce all four units; a final affected-surface replay also keeps 19 pre-existing AnmManager units exact, for 23/23 total. The maintained hard `Supervisor::OnUpdate` source was owner-corrected from the older local `g_SupervisorServiceState/Reset432F70` aliases to shared target-backed `g_SupervisorNetworkState/ResetSession`; its object remains the same 980-byte section versus the 844-byte target logical body and therefore stays SOURCE-PRESENT/NON-EXACT. No exactness is inferred from successful compilation of that hard owner. | Canonical two-round log, final 23-unit replay, pinned compiler diagnostics for `SupervisorUpdate.cpp`/`SupervisorNetwork.cpp`, and target caller/global-address evidence. |
| REPLAY-020 | exact / authored-game / shared RNG replay-runtime leaves | Packet 184 closes three target-backed RNG helpers used by Replay plus network/runtime callers. `ReplayRngView::GetSeed @ 0x0041A89E` returns the 16-bit seed at +0 and is called by `ReplayManager::CaptureFrameSyncState`, network initialization/service, and adjacent runtime code. `ReplayRngView::SetSeed @ 0x00420160` stores the 16-bit seed; TH09 callers pass `g_ReplayRng @ 0x004ACE0C` plus replay-stage/startup/network seeds. `ReplayRngView::ResetGenerationCount @ 0x00420170` clears the dword at +0x04 and is called by CaptureFrameSyncState immediately after GetSeed. Natural source reproduces 4, 14 and 8 bytes exactly in two cold VC7.1 rounds. `ReplayRngView` is a neutral reconstruction view; clean committed TH08 corroborates the Rng source-family names only. | Fresh native-IDA bodies/xrefs, exact Replay caller context, TH09 global/field dataflow, natural source in `ReplayRuntimeLeaves.cpp`, and repeated repository-defined replay. |
| RUNTIME-010 | exact Supervisor replay predicate plus constructor-origin guard | `SupervisorReplayView::IsSpeedhackDetected @ 0x00420180` is repository-canonical exact at 13 bytes: exact `ReplayManager::RecordInputAndFps` passes `g_Supervisor @ 0x004B3100` in ECX and the helper returns bit 3 of target-backed Supervisor flags `+0x5D4`. The maintained type is explicitly a reconstruction view rather than a claim about original TH09 class spelling. Nearby `0x00420830` is not promoted despite its 15-byte constructor shape: it calls constructor-shaped `0x0041A98B` on `this+0x1C`, which conflicts with the currently maintained ReplayManager stage-buffer interpretation, so owner/source/origin remain `unknown/review` until reconciled. | TH09 caller/disassembly evidence, existing Supervisor +0x5D4 layouts, two cold exact replays, and conservative constructor review. |
| BOUNDARY-152 | reviewed / RNG and Supervisor support leaves | Packet 184 fixes four independent logical extents without charging alignment: `GetSeed 0x0041A89E-0x0041A8A1` is immediately followed by independent `0x0041A8A2`; `SetSeed 0x00420160-0x0042016D` is followed by two `CC`; `ResetGenerationCount 0x00420170-0x00420177` is followed by eight `CC`; `IsSpeedhackDetected 0x00420180-0x0042018C` is followed by three `CC` before independent `ReplayManager::CaptureFrameSyncState @ 0x00420190`. | PE-aware target disassembly and candidate inventory extents. |
| TOOLCHAIN-155 | exact / pinned VC7.1 / shared Replay view refactor | Packet 184 moves the already-maintained `ReplayRngView` and `SupervisorReplayView` declarations from `ReplayInput.cpp` into `ReplayRuntimeLeaves.hpp`, exposing target-backed `generationCount +0x04` and Supervisor `flags5D4 +0x5D4` without changing object geometry. Four new units totaling 39 bytes are exact in two independent cold `/O2 /Ob1 /Oy-` rounds, and all seven pre-existing exact `ReplayInput.cpp` units replay 7/7 exact after the header extraction. `SetSeed` naturally retains the target frame-pointer prologue under the established `/Oy-` profile; no register/volatile forcing, assembly, padding, fake returns or target-byte material is used. | Two-round canonical log, seven-unit ReplayInput regression log, pinned compiler, and final source audit. |
| SCOREDATA-006 | exact / authored-game / startup score-load closure | Packet 185 closes the small startup seam immediately after `ScoreFileView::OpenScore`. `InitializeScoreDataStartup @ 0x00421EA0-0x00421F01` was already named by exact `Supervisor::StartupThread` relocation evidence; it opens target literal `score.dat`, copies the complete 0x18 `ScoreFileView` header into `g_ScoreFileHeader @ 0x004AC840`, invokes `LoadScoreRecords`, `LoadLastName`, exact `LoadPlayStats`, releases the same buffer, and returns 0. `ReleaseScoreFileBuffer @ 0x00421E90-0x00421E9B` is a neutral reconstruction alias for the independent ECX-pointer wrapper over exact `g_ZunMemory.Free`. Natural C++ reproduces 98 and 12 bytes exactly in two cold VC7.1 rounds. Ordinary `g_ScoreFileHeader = *scoreFile` assignment naturally emits the target's six scalar dword copies; no manual copy shaping is retained. | Exact StartupThread caller relocation, passed native-IDA bodies/xrefs/string readback, target-backed ScoreFileView geometry, natural pinned-VC7.1 source, and repeated canonical replay. |
| BOUNDARY-153 | reviewed / ScoreData startup seam | `ScoreFileView::OpenScore` ends at `0x00421E8A`; five `CC` bytes remain unowned through `0x00421E8F`. `ReleaseScoreFileBuffer` owns `0x00421E90-0x00421E9B`, followed by four `CC` bytes. `InitializeScoreDataStartup` owns `0x00421EA0-0x00421F01`, followed by fourteen `CC` bytes before independent source-present `SaveTitleScoreData @ 0x00421F10`. No alignment is charged to authored code. | PE-aware target disassembly and candidate inventory. |
| TOOLCHAIN-156 | exact / pinned VC7.1 / ScoreFileView header extraction | Packet 185 factors the already-maintained 0x18 `ScoreFileView` declaration into `ScoreFileView.hpp` and keeps the release wrapper in a separate TU from `InitializeScoreDataStartup`. Under the established ScoreData `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Gr /O2 /Ob1 /Oy- /I src` profile, both new functions pass two independent cold exact rounds. Three pre-existing exact ScoreData units plus exact `Supervisor::StartupThread` replay 4/4 after the header extraction. Exact startup equality does not promote its non-exact dependencies: `OpenScore`, `LoadScoreRecords`, and `LoadLastName` retain their independent SOURCE-PRESENT/NON-EXACT states. | Two-round canonical replay, four-unit regression log, full relocation manifests, and final source audit. |
| RNG-001 | exact / authored-game / shared RNG runtime chain | Packet 186 closes seven TH09 RNG runtime leaves with a neutral `RngRuntimeView`: `GetRandomU32InRange @ 0x004048E0`, positive `GetRandomF32InRange @ 0x00404910`, signed `GetRandomF32SignedInRange @ 0x00406200`, `NextU16 @ 0x0042AE20`, `GetRandomU32 @ 0x0042AE50`, `GetRandomF32 @ 0x0042AE70`, and `GetRandomF32Signed @ 0x0042AEA0`. TH09 itself fixes seed/generation-count offsets, the 0x9630/0x6553 seed transform, two-step U32 composition, unsigned-float correction constants, positive versus signed scaling, and the two physically distinct range wrappers. The maintained type is deliberately reconstruction-neutral because older exact callers use several temporary RNG view names and even conflate the two F32 range member names. | Passed native TH09 target disassembly/callers, existing exact caller relocation manifests, two cold repository VC7.1 canonical rounds (7/7 each), and clean committed TH08 source-family corroboration only after TH09-local behavior was fixed. |
| SUPERVISOR-031 | exact / authored-game / camera and network accessors | `SupervisorCameraTan @ 0x0042E910` is a 14-byte cdecl bridge used by exact `SupervisorViewportView::ApplyScreenCamera`; target code forwards its float to compiler-runtime `@tanf@4 @ 0x0042E900`. `SupervisorNetworkState::GetRemotePeerId @ 0x0042E980` is a 17-byte member leaf whose sole reviewed caller is `Supervisor_NetworkServiceUpdate`; target-backed fields `peerIds[2] @ +0x1C/+0x20` and `localSide @ +0xAC` make the natural expression `peerIds[1-localSide]` target-unique. Both replay exact twice under the pinned profile. | Exact caller relocation/call-site evidence, target-backed shared network-state layout, fresh passed IDA attestation/xrefs, two cold canonical replays, and persisted IDA name/size readback. |
| RUNTIME-011 | excluded / library compiler math runtime | `0x0042E900-0x0042E90D` is classified as `MathRuntime` `tanf`, not authored game code. Existing canonical authored callers resolve this address with decorated symbol `@tanf@4`; an independent VC7.1 `<math.h>` probe emits the same 14-byte EBP-frame/x87 `FPTAN` helper byte-for-byte. The adjacent authored `SupervisorCameraTan` bridge calls it through that exact runtime ABI. No source-presence or authored exactness credit is assigned to the runtime body. | Existing canonical relocation spelling, direct target body, independent VC7.1 compiler-emitted 14/14 structural comparison, and separate authored camera-bridge replay. |
| BOUNDARY-154 | reviewed / RNG, math-runtime, camera, network leaf seams | Packet 186 reviews exact logical extents `0x004048E0-0x00404901`, `0x00404910-0x0040491E`, `0x00406200-0x0040620E`, `0x0042AE20-0x0042AE46`, `0x0042AE50-0x0042AE67`, `0x0042AE70-0x0042AE92`, `0x0042AEA0-0x0042AEC8`, `0x0042E900-0x0042E90D`, `0x0042E910-0x0042E91D`, and `0x0042E980-0x0042E990`. Following gaps are respectively 14, 1, 1, 9, 8, 13, 7, 2, 2, and 15 `CC` bytes before independent functions. No alignment bytes are charged to authored or runtime function extents. | Candidate inventory sizes plus raw target disassembly across every seam and exact whole-logical-extent replays for the nine authored leaves. |
| TOOLCHAIN-157 | exact / small runtime-accessor compiler feedback | Natural Packet-186 source under pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Gr /O2 /Ob1 /Oy- /I src` yields nine canonical authored functions totaling 234 exact bytes in two independent cold rounds. U32 range initially used an ordinary early-return form and produced the right semantics but different branch layout; the equally natural ternary form independently corroborated by committed TH08 emits the 34-byte TH09 target exactly. Camera `tanf` must be declared through the target-proven external `@tanf@4` ABI because `<math.h>` makes VC7.1 intrinsic-expand it; no pragma, noinline, register/volatile forcing, assembly, padding, fake return, or embedded target bytes are retained. | Direct compiler probes including retained negative U32-range and intrinsic-camera probes, final two-round 18/18 repository exact replay, relocation-aware manifests, and exact existing caller replays. |
| RUNTIME-012 | exact / authored-game / ZunTimer equality leaf | `ZunTimer::operator== @ 0x00404950-0x00404963` is repository-canonical exact at 20 bytes. Exact `AnmManager::ExecuteScript` already relocates to decorated target symbol `??8ZunTimer@@QAEHH@Z`; this corrected an initial unsigned-return probe whose mangled symbol was wrong. Natural `int operator==(int)` reads target-proven `current +0x08`, compares the stack integer, and reproduces the whole target extent twice with no relocations. | Existing exact caller relocation, target ZunTimer +0x08 layout, corrected VC7.1 symbol ABI, and two cold canonical rounds. |
| INPUT-003 | exact / input bitmask leaf type discrimination | The earlier int-mask negative is superseded. TH09-local compiler feedback proves the small bitmask leaves use an unsigned-short mask surface: AsciiInputView::IsHeld @ 0x0040E2B0, AsciiInputView::WasPressed @ 0x0040E2D0, ReplayInputState::IsHeld @ 0x00415CB0, and ReplayInputPressedView::WasPressed @ 0x00415CD0 all naturally zero-extend their 16-bit state field, materialize the stack mask in ECX, AND EAX,ECX, and replay exact. Int-mask declarations fold the AND to a memory operand and create the old shorter negatives. | Exact caller relocation names, natural u16-vs-int type matrix, two cold canonical replays for the Replay leaves, and all nine ReplayInput.cpp canonical units remaining exact after the declaration correction. |
| SUPERVISOR-032 | exact / authored-game / audio and draw support leaves | Packet 187 closes four caller-bound support leaves: `SupervisorAudioGameManagerView::HasFlagBit1 @ 0x0041A9AC` reads target-backed GameManager flags `+0x134`; `SupervisorDrawMainAnmManagerView::GetSurface @ 0x0042F400` indexes target-backed AnmManager primary surfaces `+0x123FC`; `SupervisorDrawMainAsciiManagerView::SetAlpha @ 0x004302A0` writes the alpha byte of AsciiManager color at `+0x826B`; and `SupervisorDrawMainAnmManagerView::ResetDrawState @ 0x00439680` clears sprites-to-draw `+0x128E4` and points both vertex cursors `+0x2B28E8/+0x2B28EC` at storage `+0x128E8`. All four are exact twice with zero relocations. | Exact Supervisor audio/draw caller relocation manifests, previously established TH09 GameManager/AsciiManager/AnmManager layouts, direct target bodies, and two cold canonical rounds. |
| BOUNDARY-155 | reviewed / timer and Supervisor support leaves | Packet 187 owns exactly five logical extents: `0x00404950-0x00404963` followed by twelve `CC`; `0x0041A9AC-0x0041A9B7` immediately followed by independent `0x0041A9B8`; `0x0042F400-0x0042F410` followed by fifteen `CC`; `0x004302A0-0x004302AF` immediately followed by independent `0x004302B0`; and `0x00439680-0x0043969C` followed by three `CC` before independent `AnmManager::FlushVertexBuffer @ 0x004396A0`. No alignment is charged to authored code. | Candidate inventory sizes, raw target disassembly across every seam, and whole-extent exact replays. |
| TOOLCHAIN-158 | exact / VC7.1 TU-partition feedback | The five Packet-187 leaves total 94 exact authored bytes under pinned VC7.1 `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src`. A same-TU definition probe for the three draw helpers made `/Ob1` inline them into `SupervisorDrawMain`, shrinking the exact 312-byte caller to 301 bytes. Restoring `SupervisorDrawMain.cpp` to immutable HEAD and placing those helper definitions in separate `SupervisorDrawMainLeaves.cpp` restores the caller to 312/312 while all three helpers remain exact. Final source replays all 27 pre-existing exact units from the touched ZunTimer/SupervisorAudio/SupervisorDrawMain surfaces at 27/27. | Retained same-TU negative object, separate-TU exact objects, two-round leaf replay, exact 312-byte caller replay, and 27-unit modified-source regression. |
| PLAYER-010 | exact / authored-game / shared runtime and lifecycle type leaves | Packet 188 closes `PlayerSharedRuntimeView::IsBlocked @ 0x00404980-0x00404997` and `PlayerLifecycleView::InitializeType @ 0x0041BDE0-0x0041BDEC`. Exact Player OnUpdate calls the shared-runtime predicate twice through `g_PlayerSharedRuntime`; TH09 independently fixes the signed state at `+0xE94C` and the accepted values (`>= 0` or `-2`). Exact PlayerRegisterChain calls InitializeType with the player object in ECX and one byte on the stack; TH09 fixes that byte at Player `+0x04`, now exposed as `playerType04` without moving any later field. Natural source reproduces 24 and 13 bytes exactly in two cold rounds. | Exact caller relocation/call-site evidence, target bodies and seams, refined target-backed Player layout assertion, two-round canonical replay, and full affected-header regression. |
| TITLE-018 | exact / authored-game / Supervisor recording-warning reset | `TitleSupervisorView::ClearRecordingFpsWarningState @ 0x00423382-0x004233A2` is repository-canonical exact at 33 bytes. Exact `TitleScreenView::ActualAddedCallback` passes `g_Supervisor` through the maintained TitleSupervisor view. TH09 clears dwords `+0x77C`, `+0x784`, `+0x790`, `+0x788`, `+0x78C` in that exact order while independently known playback warning `+0x780` is left untouched. Extending the local TitleSupervisor layout through `+0x790` and defining the helper in the existing Title TU preserves the exact 335-byte caller. | Exact caller relocation, independent Supervisor playback-warning +0x780 evidence, direct target body, same-TU VC7.1 probe, two cold canonical rounds, and complete Title exact-surface regression. |
| BOUNDARY-156 | reviewed / Player and Title support leaves | Packet 188 owns exactly three logical extents: `0x00404980-0x00404997` followed by eight `CC` bytes before independent PlayerUpdateSelectorState; `0x0041BDE0-0x0041BDEC` followed by three `CC` bytes before independent `0x0041BDF0`; and `0x00423382-0x004233A2` followed immediately by independent `0x004233A3`. No alignment is charged to authored code. | Candidate inventory sizes, raw target disassembly across all seams, and whole-extent exact compiler replays. |
| TOOLCHAIN-159 | exact / affected-surface replay plus bounded negative | Packet 188 adds three canonical units totaling 70 exact authored bytes under the established VC7.1 profiles: Player leaves use `/O2 /Ob1 /Oy- /Oi`, while the Title helper uses the already-established Title `/O1 /Ob1 /Oy-` profile. All three pass two cold rounds (6/6). Thirty-two pre-existing exact units from `TitleScreen.cpp` and every direct `PlayerLifecycleView.hpp` includer replay 32/32 after one stale manifest repair: `title-screen-mode-select` had two relocation symbols naming `UpdateMenuSelection` as int (`QAEHXZ`) even though immutable HEAD source/object, the exact standalone UpdateMenuSelection unit, and target-bound direct replay all prove the maintained void symbol (`QAEXXZ`). `SupervisorDrawMainAsciiManagerView::SetScale @ 0x004234DD` remains unpromoted: natural 25-byte source is target-sized but only 15/25 ordinary bytes match because VC7.1 preloads the second float into EDX whereas target serializes both stores through EAX; direct-cast and concrete-field variants retain the same mismatch. | Immutable-HEAD cold compile, repaired 506-byte mode-select canonical replay, 32-unit regression, two-round new-leaf replay, and retained SetScale negative probes. |
| MIDI-002 | exact / authored-game / playback convenience leaves | Packet 189 closes two caller-bound MIDI convenience wrappers outside the Packet-125 core range. `MidiOutput::PlayFile(int) @ 0x00423361-0x00423381` calls exact StopPlayback/ParseFile/Play and is the pre-existing `Midi.hpp` inline body compiled under the established Title `/O1 /Ob1 /Oy-` COMDAT profile; that profile uniquely emits the target's direct `push [ebp+8]` 33-byte form, while established Supervisor/Midi `/O2` profiles emit 34 bytes. `MidiPlaybackPathView::PlayPath @ 0x0042EC00-0x0042EC21` calls exact StopPlayback/LoadFile/Play and is 34-byte exact under the established Supervisor `/O2` profile. The path type is deliberately reconstruction-neutral because target-local evidence fixes behavior and receiver family but not original overload spelling. | Exact Supervisor deleted/audio/music caller relocations, existing exact MidiOutput callee units, natural header/body source, established VC7.1 profile probes, and two cold canonical rounds. |
| ANM-026 | exact / authored-game / vertex-buffer release leaf | `AnmManager::ReleaseVertexBuffer @ 0x0042F3D0-0x0042F3EE` is repository-canonical exact at 31 bytes. Exact `SupervisorDeletedCallback` passes `g_AnmManager` in ECX; the TH09 constructor independently initializes `+0x12890` to null. Natural source checks that COM interface pointer, calls vtable slot `+0x08` (`Release`), then clears the field. The helper is maintained as a real `AnmManager` member in a separate TU and has no target relocations. | Exact deleted-callback relocation, target constructor/layout evidence, COM call shape, first-pass zero-diff probe, and two cold canonical replays. |
| ANM-027 | exact / authored-game / interpolation leaves | AnmVmView::ConfigurePositionInterpolation @ 0x004233A3-0x004233F3 and AnmVmView::ConfigureColorInterpolation @ 0x004233F4-0x00423468 close two source-absent VM interpolation helpers used only by the TH09 TitleScreen character-selection visual path. TH09 independently fixes the seven current timers at +0x50, seven end timers at +0xA4, interpolation modes at +0xF8, position endpoints at +0x238/+0x244, and RGB1 endpoints at +0x278/+0x27C. The maintained timer assignment mirrors TH09 ZunTimer::SetCurrent: current=value, subFrame=(float)value, previous=-999999; VC7.1 naturally schedules that source into the target bodies. | Fresh TH09 caller/decompile/disassembly evidence and the existing target-backed ANM interpreter layout establish all local facts. Clean committed TH08 contributes only the StartPositionInterpolation / StartColor1RgbInterpolation source-family hypothesis; no TH08 address, ABI or exactness is transferred. Two independent cold /O1 /Ob1 /Oy- /Gr canonical replays reproduce 81/81 and 117/117 bytes respectively, both with zero relocations. The maintained Configure... names are reconstruction aliases and do not claim original TH09 spelling. |
| BOUNDARY-157 | reviewed / MIDI playback and ANM resource leaves | Packet 189 reviews three independent extents exactly as initially inventoried: `0x00423361-0x00423381` ends with the three-byte `ret 4` and is immediately followed by independent `0x00423382`; `0x0042EC00-0x0042EC21` ends with `ret 4`, followed by fourteen unowned `CC` bytes before independent `0x0042EC30`; `0x0042F3D0-0x0042F3EE` ends with RET, followed by one `CC` before independent thunk `0x0042F3F0`. An early diagnostic mistakenly treated the two immediate bytes of `ret 4` as padding and compared 31/32-byte MIDI extents; that was corrected before any ledger promotion. | Raw PE disassembly including full RET encodings, candidate inventory rows, and full logical-extent canonical comparisons. |
| TOOLCHAIN-160 | exact / COMDAT profile split and stable existing surface | Packet 189 adds three canonical units totaling 98 authored bytes. The integer `MidiOutput::PlayFile` demonstrates real per-COMDAT profile selection: unchanged header source is 33-byte exact only under the already-established Title `/O1` profile and 34 bytes under Supervisor/Midi `/O2`; no source shaping is needed. The path wrapper and Anm release use ordinary `/O2 /Ob1` source. Existing regression returns 38/39 configured units exact; the sole configured failure is pre-existing `midi-th09-process-msg` `$L####` local-label manifest drift. Packet source restores `Midi.hpp` byte-for-byte to immutable HEAD, and direct target-bound comparison of the unchanged ProcessMsg physical extent is structural-exact at 1,574/1,574 bytes (1,402 ordinary comparable bytes plus 43 relocation fields, all destinations solved). The manifest's compiler-local label names are intentionally not rewritten for this packet. | Two-round 6/6 new-unit replay, 38 configured exact regressions, immutable Midi source audit, 1,574-byte direct ProcessMsg replay, and retained failed-manifest receipt. |
| FRONT-001 | exact / authored-game / startup-release seam | Packet 190 closes the TH09 front-resource pair: `PreloadFrontStartup @ 0x00415C30-0x00415C47` preloads exact AnmManager slot 10 from target literal `front.anm` and returns 1 only on NULL; `SupervisorDeletedReleaseSubsystemC @ 0x00415C50-0x00415C5D` releases the same slot through exact `AnmManager::ReleaseAnm`. Both natural helpers are canonical exact and mirror the independently exact Etama startup/release pattern without transferring adjacent-game layout. | Exact Supervisor startup/deleted callers, target literal `front.anm @ 0x0048EE68`, exact AnmManager PreloadAnm/ReleaseAnm callees, and two cold pinned-VC7.1 replays. |
| FRONT-002 | exact / authored-game / TU ownership | FrontReleaseView::ReleaseCore @ 0x00418270-0x00418291 closes the core teardown leaf behind exact ReleaseGameSubsystem. TH09 local evidence fixes the Front owner through the paired FrontInf setup path and the embedded message-runtime subobject at Front +0xE944. When ShouldReleaseStageResources is nonzero, the helper calls still-independent 0x00417520 with 10 * g_GameSide1Value20 and then returns zero. The decisive compiler fact is physical TU ownership: compiling the already-exact 6-byte ShouldReleaseStageResources definition in FrontRelease.cpp lets VC7.1 preserve ECX implicitly across the known leaf and emits the 34-byte target; a natural split-TU declaration forces push/mov/pop of ESI and emits 38 bytes. | Fresh TH09 sole-caller/disassembly evidence, pre-existing exact Front wrapper/factory/layout evidence, and two independent cold FrontRelease.obj replays. In each replay ShouldReleaseStageResources is 6/6 exact, ReleaseCore is 34/34 exact with three reviewed relocations, and ReleaseGameSubsystem remains 50/50 exact. No register forcing, volatile, asm, padding or profile search is used. The ReleaseForSideCount name for 0x00417520 is relocation-only and does not promote that callee. |
| SUPERVISOR-033 | exact / authored-game / screen-effect launch leaf | `SupervisorStartEffectView::StartEffect @ 0x00430500-0x0043052E` checks target global `g_SupervisorScreenEffect @ 0x004B30F8` and, only when null, stores the result of `ScreenEffect::RegisterChain(index + 5, 60, 0, 0, 0, 1, 2)`. Exact GameManager and TitleScreen callers pass the Supervisor receiver. The maintained view/name is reconstruction-neutral; the original class spelling is not claimed. | TH09 exact caller relocations, target body/global/callee dataflow, existing ScreenEffect ABI, committed TH08 source-family corroboration only after TH09 closure, and two cold canonical replays. |
| SOUND-002 | exact / authored-game / FMT-load leaf | `SoundPlayerLoadFmtView::LoadFmt @ 0x0043E2C0-0x0043E2E6` stores exact `FileSystem::OpenFile(path, NULL, 0)` into receiver `+0x1F84` and returns 0 for non-null data or -1 on failure. Exact Supervisor::StartupThread calls it on `g_SoundPlayer`. The minimal view asserts only the TH09-observed offset; adjacent TH08 naming/layout is not imported. | TH09 startup caller, target store/return sequence, exact FileSystem::OpenFile, target-backed SoundPlayer family, and two cold canonical replays. |
| BOUNDARY-158 | reviewed / Packet-190 small leaves | Four extents are independently bounded by return instructions and unowned alignment: `0x00415C30-0x00415C47` then eight CC bytes; `0x00415C50-0x00415C5D` then two CC bytes; `0x00430500-0x0043052E` then one CC byte; `0x0043E2C0-0x0043E2E6` then nine CC bytes before independent `0x0043E2F0`. No alignment is charged to authored code. | Raw TH09 PE disassembly, candidate inventory, and full-extent canonical comparisons. |
| TOOLCHAIN-161 | exact / four-leaf cold replay | Packet 190 adds four canonical units totaling 124 authored bytes under the established pinned VC7.1 `/O2 /Ob1 /Oy- /Oi /Gr` profile. First natural source shapes are structural-exact, then two independent cold repository-managed rounds pass 8/8. Seven pre-existing exact caller/callee units replay 7/7. The scalar deleting destructor candidate at `0x00431680` is deliberately not promoted as authored work; prior input-helper and SetScale negative results are not retried. | Repository match-unit replay, affected exact-surface regression, target-bound direct probes, and origin discipline. |
| GAMEMGR-012 | exact / authored-game / reset seam | Packet 191 closes the GameManager Supervisor-added reset pair. `GameManagerResetView::InitializeCoordinateBounds @ 0x0041A9B8-0x0041AA06` writes eight target constants to `+0x350..+0x36C`; `GameManagerResetView::ResetForSupervisorAdded @ 0x0041BA4F-0x0041BA7D` clears the 0x390-byte reset view, calls that initializer, clears `+0x13F`, and stores 10 at `+0x378/+0x37C`. Exact `Supervisor::AddedCallback` fixes the outer receiver as `g_GameManager`; the initializer has no other target xref. Both are canonical exact only with the target-evidenced split-TU `/O1` shape. | TH09 exact caller/xref/layout evidence, direct O1/O2 and same/split-TU compiler probes, and two cold canonical rounds. |
| TITLE-019 | exact / authored-game / screen-state leaf | `TitleChangeScreenView::ChangeCurrentScreen @ 0x00422F39-0x00422F66` copies current screen `+0x1B228` to previous screen `+0x84`, stores the requested screen, clears state fields `+0x2C/+0x1B230/+0x28/+0xC910`, and returns 0. Existing TH09 `TitleScreenView` independently names/asserts all of those offsets, while multiple exact Title callers fix the thiscall(int)->int ABI. A small `/O1` leaf TU avoids perturbing the large Title TU and reproduces all 46 bytes. | Exact Title caller relocations, existing target-backed Title layout, first-pass natural source, and two cold canonical replays. |
| TITLE-020 | exact / authored-game / shared VM selection leaves | Two previously source-absent TitleScreen helpers close naturally under the established pinned VC7.1 /O1 /Ob1 /Oy- /Gr profile. TitleScreenView::SetIndexedSpriteChoice @ 0x00424E95-0x00424EDC uses the TH09-local titleAnm +0x11B90 / vms +0x11B98 layout to select baseSpriteIndex + 2*choice for one VM and set interrupt 7; the screen-10 TitleScreen state machine repeatedly calls it for VM indices 47/48/49/42. TitleScreenView::SetRangeSelectionInterrupts @ 0x0042510E-0x00425162 is called only from OnUpdateMusicRoom and sets interrupt 8 across [start,start+count) before setting interrupt 7 on start+selected. | Direct TH09 IDA callers/decompilation/disassembly, existing target-backed TitleScreen/AnmVm layout, and two independent cold repository-defined replays for each helper. The first is exact 72/72 bytes with its sole SetSprite @ 0x00436AC0 relocation; the second is exact 85/85 bytes with no relocations. Both names are neutral reconstruction aliases and no adjacent-game ownership is imported. |
| TITLE-021 | exact / authored-game / two-digit VM value leaf | TitleScreenView::SetTwoDigitVmValue @ 0x00422FD1-0x0042304C closes a screen-10 display helper. All ten target xrefs are inside the same TitleScreen state machine and pass five pairs of consecutive 0x2A4-byte VMs plus signed 16-bit character-record values. Negative values clear flag bit 1 on both VMs; nonnegative values divide by ten, select baseSpriteIndex + 2*tens and baseSpriteIndex + 2*ones through TitleAnmView::SetSprite, then set flag bit 1 on both VMs. | Fresh TH09 xrefs/decompilation/disassembly plus the already target-backed TitleScreen titleAnm +0x11B90 and AnmVm offsets/stride. Plain maintained C++ under the established /O1 /Ob1 /Oy- /Gr Title profile reproduces 124/124 bytes with the two SetSprite relocations in two builds, while connected Title/ANM leaves remain exact. The maintained name is a reconstruction alias. |
| TITLE-022 | exact / authored-game / key-config member leaf | TitleScreenView::SwapDuplicateCharacterIndex @ 0x00422F67-0x00422FD0 closes the screen-10 record rewrite helper. All five TH09 target xrefs occur in OnUpdateKeyConfig @ 0x00427EE8; each pushes the active 54-byte side record, incoming key index, previous selected-slot index and a fourth 0/1 argument, then loads the TitleScreen receiver into ECX immediately before the call. The callee compares nine short slots in target order 0,1,2,4,5,6,7,3,8, replaces every incoming match with the previous value, and returns the record pointer with ret 0x10; optimized code does not read ECX or the fourth argument. | The earlier explicit-stdcall rendition reproduced the physical body but did not explain the five caller-side MOV ECX,this instructions. A non-static four-explicit-argument member rendition naturally explains both sides and reproduces 106/106 bytes with zero relocations under the established Title /O1 /Ob1 /Oy- /Gr profile in two cold canonical replays. The helper is therefore maintained in the same key-config TU as its sole owner; spelling and fourth-argument semantics remain reconstruction-neutral. |
| SUPERVISOR-034 | exact / authored-game / thread-start leaf | `SupervisorThreadStartView::ThreadStart @ 0x00431AE0-0x00431B24` closes the common subthread launch helper used by exact GameManager, Supervisor and Title lifecycle callers. It calls target-local `ThreadClose @ 0x00430230`, invokes imported `CreateThread(NULL,0,startFunction,startParam,0,&id)`, stores handle/id/state at `+0x6AC/+0x6B0/+0x6B8`, and returns 0 on success or -1 on failure. | Exact caller families, exact deleted-callback ThreadClose edge, TH09 field offsets/import target, and two cold `/O2 /Ob1` canonical replays. |
| EXATTACK-002 | exact / authored-game / callback-reset seam | Packet 191 closes `ResetExAttackRecords @ 0x004156F0-0x00415747` and `ExAttackController::AddedCallback @ 0x00415750-0x00415791`. Reset walks exactly 256 target-proven 0x4C records, invokes `releaseCallback +0x48`, frees `dynamicData +0x1C`, and clears active records. AddedCallback derives both target-proven `sideLimits +0x14` entries from GameManager selectors `+0x20/+0x58`, difficulty `+0x11C`, tables `0x004A117C/0x004A1128`, then calls reset. Table names remain reconstruction-neutral because TH09 proves addresses/dataflow but not original identifiers. | Exact ExAttack Register callback relocation, existing ExAttack layout, TH09 GameManager offset evidence, table-address relocations, and two cold canonical rounds. |
| BOUNDARY-159 | reviewed / Packet-191 small leaves | Six extents are independently bounded: `0x004156F0-0x00415747` then eight unowned CC bytes; `0x00415750-0x00415791` then fourteen CC bytes; `0x0041A9B8-0x0041AA06` immediately followed by independent `0x0041AA07`; `0x0041BA4F-0x0041BA7D` then two CC bytes; `0x00422F39-0x00422F66` immediately followed by `0x00422F67`; `0x00431AE0-0x00431B24` then eleven CC bytes. No alignment is charged. | Raw TH09 PE disassembly, xrefs, candidate inventory and full-extent canonical comparisons. |
| TOOLCHAIN-162 | exact / split-TU O1 evidence plus relocation-graph regression | Packet 191 adds six canonical units totaling 395 authored bytes. The two GameManager reset functions expose a specific VC7.1 source-partition constraint: same-TU visibility lets the compiler preserve `this` in EDX and misses the target; split-TU compilation restores ESI preservation, while `/O1` (not `/O2`) also emits the target three-byte constant-10 load and the target zero-float lowering. Title uses its established `/O1` profile; Supervisor thread and ExAttack use ordinary `/O2 /Ob1`. Two cold rounds pass 12/12 new-unit replays, and every pre-existing configured caller discovered by relocation graph replays 6/6 exact. | Repository match-unit graph, direct compiler-profile probes, two-round canonical replay, and automatically discovered caller regression. |
| GAMEMGR-013 | exact / authored-game / teardown-wrapper seam | Packet 192 closes three short GameManager-owned teardown edges without inferring identities from adjacency. `ReleaseSubsystem0 @ 0x00402530-0x0040256A` is the null-safe Background teardown: exact GameManager teardown passes the per-side subsystem0 pointer, the paired setup path calls `Background::Create`, and the body cuts the target-proven calc/draw-high/draw-low chain pointers at `+0/+4/+8` before CRT free. `ReleaseGameSubsystem @ 0x004182A0-0x004182D1` is the Front owner teardown: the paired setup path calls the `FrontInf` factory `0x00418840`, while the wrapper calls target-local `0x00418270`, cuts draw/calc chains at `+4/+0`, then frees. `ReleaseStageObject @ 0x00420C10-0x00420C39` is the alternate ReplayManager teardown: the paired setup path calls exact `ReplayManagerView::Create @ 0x00421670`, while the wrapper calls exact `ReleaseReplayManagerCore`, cuts `mainChain +0x150`, then frees. The target-local Front helper 0x00418270 is now independently closed by FRONT-002; wrapper exactness itself did not provide that exactness. | Exact `GameManagerSetupLayout::DeletedCallback` relocation bindings, fresh native-IDA sole-caller xrefs, paired GameplaySetupThread factory xrefs, target-backed owner layouts, and two cold repository-defined VC7.1 replay rounds. |
| BOUNDARY-160 | reviewed / Packet-192 release wrappers | All three promoted extents end at their inventoried RET and leave only unowned alignment: Background release ends `0x0040256A`, followed by five `CC` bytes before independent `0x00402570`; Front release ends `0x004182D1`, followed by fourteen `CC` bytes before independent `0x004182E0`; Replay stage-object release ends `0x00420C39`, followed by six `CC` bytes before independent `0x00420C40`. No alignment is charged to authored code. | Raw TH09 PE disassembly, candidate inventory, and full-extent relocation-aware comparisons. |
| TOOLCHAIN-163 | superseded negative / release-wrapper TU lesson | Packet 192 correctly rejected the then-tested split-TU / public-ABI forms for ReleaseSubsystem4 @ 0x00411F80: ordinary variants collapsed to 67 bytes or retained differences, and no register forcing/padding/asm was accepted. ENEMY-006 later supplies genuinely new evidence: when the target-observed release core is a file-static helper in the same TU, VC7.1 naturally gives that helper a private EBX transport and the wrapper becomes 74/74 exact under the ordinary established /O2 /Ob1 /Oy- /Oi /Gr profile. The old negative therefore remains useful as evidence against split-TU/public-ABI hypotheses, but is no longer the final status of the target function. | Packet-192 negative diagnostics plus the later same-TU microprobe and two cold ENEMY-006 exact replays. |
| SUPERVISOR-038 | exact / authored-game / viewport sibling | `Supervisor::ConfigureScreenViewport @ 0x00403D90-0x00403DD6` is the screen-space sibling of exact `Supervisor::ConfigureGameplayViewport`: select one of three 0xF0 configurations rooted at Supervisor +0x78, publish current +0x348, call exact `SupervisorViewportView::ApplyScreenCamera @ 0x00430590`, invoke D3D `SetViewport` on configuration +0xCC, and store currentIndex +0x34C. Exact Player/Enemy draw callers all target this helper. | Two cold VC7.1 replay rounds, exact viewport sibling/layout, and fresh target caller/xref review. |
| SUPERVISOR-039 | exact / authored-game / startup-thread reset | `SupervisorThreadCloseView::ThreadClose @ 0x0041A997-0x0041A9AB` is a reconstruction-neutral view over the Supervisor startup-thread state: clear handle +0x6AC, close-request +0x6B4, and active +0x6B8. Fresh target xrefs are exactly two calls inside exact `Supervisor::StartupThread`; this is distinct from the larger unrelated thread-close helper at `0x00430230`. | Two cold VC7.1 replay rounds, exact StartupThread relocations, target xrefs, and TH09 layout assertions. |
| PLAYER-026 | exact / authored-game / draw tail | `PlayerLifecycleView::DrawTailStates @ 0x0041DF80-0x0041DFE2` iterates four draw records with stride 0x2C4 from Player +0x30458. Each active record (+0x18) maps x/y through exact GameManager coordinate helpers, writes z=0.15 to the VM rooted at +0x1C, then calls exact `AnmManager::DrawNoRotation`. The maintained draw-only view intentionally does not claim the complete original record type. | Sole fresh target caller is exact `Player::OnDrawHighPrio`; two cold VC7.1 replays reproduce 99/99 bytes with six reviewed relocations. |
| TOOLCHAIN-164 | superseded negative / target-backed profile closure | Packet 193 correctly rejected the then-tested `/Ob1` and no-`/Oi` forms of `RotatePlayerCollisionVector @ 0x0042AF40`: both intrinsically lower ordinary `sinf/cosf` to a 42-byte FSIN/FCOS body. Later TH09-local evidence from exact Player/ANM families establishes a distinct `/Ob0` external-trig lowering, and the target helper itself contains two REL32 calls to compiler-emitted `@sinf@4/@cosf@4`. Testing that single target-backed profile on the same ordinary Rotate source produces the exact 62-byte target. The unrelated short-leaf negatives in this packet remain unchanged. | Historical Packet-193 diagnostics plus Packet 418 `/Ob0` structural replay. No volatile/register forcing, padding, asm, pragma or profile roulette is used. |
| BOUNDARY-161 | reviewed / Packet-193 leaves | Promoted extents exclude alignment: screen viewport ends 0x00403DD6 then nine `CC` bytes through 0x00403DDF; Player draw-tail ends 0x0041DFE2 then thirteen `CC` bytes through 0x0041DFEF; startup ThreadClose ends 0x0041A9AB and independent exact function 0x0041A9AC begins immediately. | Raw TH09 PE disassembly plus candidate inventory. |
| TOOLCHAIN-165 | exact / Packet-193 affected regression | Final affected-source replay is effectively 18/18 exact: all eight exact `SupervisorViewport.cpp` units, six standard-profile exact `PlayerRuntime.cpp` units, the separately built `/Ob0` Player constructor, the new startup ThreadClose leaf, exact `Supervisor::StartupThread`, and exact `SetupDInputInternal`. The first combined script produced one non-code `error` only because `supervisor-setup-dinput` names a separate object path not generated by the StartupThread build; building its own configured object makes it exact. There are zero byte/size mismatches in the affected regression. | Final affected-unit regression and configured exact match units. |
| SUPERVISOR-040 | exact / authored-game / thread lifecycle | `SupervisorThreadStartView::ThreadClose @ 0x00430230-0x0043029B` is the full Supervisor thread join/close helper, not the 21-byte startup-state reset at `0x0041A997`. If handle +0x6AC is non-null, target writes a DWORD close request at +0x6B4, waits 1000 ms, retries `WAIT_TIMEOUT` with `Sleep(1)`, closes the handle, then clears +0x6AC/+0x6B4. The target instruction width independently upgrades the prior local `unknown6B4[4]` view to an `int`; committed TH08 source only corroborates the family shape and is not layout/exactness evidence. | Two cold VC7.1 `/O2 /Ob1` repository replays reproduce 108/108 bytes with `WaitForSingleObject`, `Sleep`, and `CloseHandle` IAT relocations; exact ThreadStart remains 69/69. |
| PLAYER-027 | exact / authored-game / owner-state timer update | `PlayerUpdateOwnerState @ 0x0041D760-0x0041D7DD` is the exact Player owner-state timer helper called once by exact `Player::OnUpdate`. A local 0x44-byte view fixes timers +0x14/+0x20, state dwords +0x04/+0x38, counters +0x0C/+0x10/+0x3C, and owner->sideState +0x0C. Timer expiry invokes shared target helper `0x0040F860`; that helper is intentionally left independently `unknown/review` because its seven Player/Enemy call sites do not yet establish a unique owner/type. | Two cold VC7.1 `/O2 /Ob1` replays reproduce 126/126 bytes with nine reviewed relocations; exact Player OnUpdate and DrawTailStates remain unchanged. |
| ASCII-030 | exact / authored-game / scale setter | `AsciiManager::SetScale @ 0x004234DD-0x004234F5` stores the two float argument bit patterns into already layout-proven `AsciiManager::scaleX +0x826C` and `scaleY +0x8270`. Exact `SupervisorDrawMain` calls this target twice on `g_AsciiManager`. Natural `/O2` is target-sized but uses EDX for the second argument and is non-exact; natural VC7.1 `/O1 /Ob1` uses the target's sequential EAX loads and is 25/25 exact, with no source shaping. | Two cold repository replays under the dedicated `/O1 /Ob1` unit; existing O2 `ResetStrings`/`PauseMenu::OnDraw` and exact SupervisorDrawMain caller remain exact. |
| TOOLCHAIN-166 | exact / mixed-profile small-leaf replay | Packet 194 closes three authored leaves totaling 259 bytes while preserving per-function compiler evidence rather than forcing one profile: Supervisor thread close and Player owner-state use the established `/O2 /Ob1`; AsciiManager SetScale uses a dedicated `/O1 /Ob1` object because that is the natural target lowering. Two cold rounds pass 6/6 for the new units and final affected-source regression passes 14/14 exact. | Two cold canonical rounds, final affected-unit regression, and configured exact match units. |
| BOUNDARY-162 | reviewed / Packet-194 leaves | Thread close ends `0x0043029B`, followed by four unowned `CC` bytes before exact SetAlpha at `0x004302A0`; Player owner-state update ends `0x0041D7DD`, followed by two unowned `CC` bytes before independent owner reset `0x0041D7E0`; Ascii SetScale ends `0x004234F5` and independent `0x004234F6` begins immediately. No alignment is charged to authored code. | Raw TH09 PE disassembly plus candidate inventory and exact compare extents. |
| GAMEMGR-014 | exact / authored-game / per-side score leaf | `GameManagerSideStateView::AddScore @ 0x0040F860-0x0040F880` is the TH09 per-side score helper. TH09 itself fixes the receiver as a 0x38-byte GameManager side record: gameplay code directly forms `g_GameManager + side*0x38`, while the maintained setup layout fixes the side-state pointer at +0x1C. The target adds signed `score / 10` to side-state +0x08. Clean committed TH08 `GameManager::AddScore` is used only after TH09 closure to corroborate the natural source family; no TH08 address, layout or exactness is imported. | First natural VC7.1 /O2 /Ob1 probe is 33/33 structural-exact, then two cold repository unit rounds pass 2/2; existing exact Player owner-state callers remain exact through their provisional aliases. |
| PLAYER-029 | exact / authored-game / owner-state reset core | `PlayerOwnerStateResetView::PrepareReset @ 0x0040F8C0-0x0040F8DC` resets the established 0x44 Player owner-state: assign zero to ZunTimer +0x20, clear state +0x04 and state/counter +0x38. Exact `PlayerAddedOwnerStateView::Reset` calls this entry on its own receiver, while `Enemy_HandleDeathRewards` reaches the same embedded Player +0x30410 state. The reconstruction type/member name is neutral; original spelling remains unknown. | First natural VC7.1 /O2 /Ob1 probe is 29/29 structural-exact with the sole relocation to exact `ZunTimer::operator=(int) @ 0x00401500`; two cold canonical rounds pass 2/2. |
| BOUNDARY-163 | reviewed / Packet-195 shared gameplay leaves | AddScore ends at `0x0040F880`, followed by fifteen unowned `CC` bytes through `0x0040F88F`; PrepareReset ends at `0x0040F8DC`, followed by three unowned `CC` bytes through `0x0040F8DF`. No alignment bytes are charged. | Raw attested TH09 PE disassembly plus candidate inventory and full-extent canonical comparisons. |
| TOOLCHAIN-167 | exact / two-leaf closure with negative ownership discipline | Packet 195 adds two ordinary /O2 /Ob1 units totaling 62 authored bytes; two cold rounds pass 4/4 and the directly affected exact Player units plus exact Player OnUpdate pass 3/3. Short neighbors remain unpromoted: `0x0040F890` has only an EnemyManager caller and unresolved owner semantics; `0x0040F8B0` increments +0x38 then tail-jumps the still-unrecovered 1,519-byte core `0x0041D150` with five arguments; `0x0044AB20` is a 3-byte zero-return target shared by Enemy/Player and many data-table xrefs, so short size alone is not origin evidence. | Packet-195 direct probes, two-round canonical log, affected regression, xref/caller review and retained negative frontier evidence. |


## Packet 196 shared angle-runtime exact leaves

- `Th09EclRunControl::VectorAngle @ 0x00405700` is repository-canonical exact at 15 bytes. TH09 target is the direct x87 `FPATAN` wrapper over two stack floats; exact/maintained ECL, Enemy and Player call sites all converge on the same physical helper. Clean committed TH08 was used only after TH09 closure as source-family corroboration. The maintained namespace/calling-convention spelling remains reconstruction-facing rather than a claim about the original source owner.
- `PlayerAngleView::AngleToPoint @ 0x0041BB70` is canonical exact at 101 bytes with five reviewed relocations. It obtains Player `position +0x1B88` through the target-local float-pointer view, subtracts the target point, returns pi/2 for the zero vector, and otherwise calls exact `VectorAngle`. The neutral Player view avoids claiming the original class/TU spelling.
- `AddNormalizeAngle @ 0x0042AED0` is canonical exact at 111 bytes with six reviewed constant relocations. Natural source adds the operands, normalizes against +/-pi with +/-2pi, and preserves the target-observed `i++ > 16` guard in both directions. Existing exact `AnmManager::ExecuteScript` calls this physical helper repeatedly.
- The three new units pass two independent cold repository-managed rounds (6/6 exact, 227/227 authored bytes). Exact `AnmManager::ExecuteScript` still replays exact. `ecl-resolve-int` retains a pre-existing configured-manifest `$L####` local-label-number drift, but a fresh direct relocation-aware comparison of its full 1,980-byte code-plus-switch-data physical extent is structural-exact with every relocation destination solved; Packet 196 does not churn compiler-local label names.
- Reviewed seams are independent: VectorAngle `0x00405700-0x0040570E` plus one unowned `CC`; Player AngleToPoint `0x0041BB70-0x0041BBD4` plus eleven `CC`; AddNormalizeAngle `0x0042AED0-0x0042AF3E` plus one `CC` before independent `0x0042AF40`. No alignment is charged.
| MATH-005 | exact / authored-game / shared vector-angle helper | `Th09EclRunControl::VectorAngle @ 0x00405700-0x0040570E` is a 15-byte float angle helper. Natural `atan2(y, x)` under the pinned VC7.1 `/O2 /Ob1 /Oi` profile lowers directly to the target x87 `FPATAN` sequence with no relocations. TH09 ECL/Enemy/Player callers converge on the same physical entry; clean committed TH08 only corroborates the explicit VectorAngle source family after TH09 closure. The maintained namespace/calling-convention spelling is reconstruction-facing because the all-float stack ABI does not uniquely recover the original declaration. | Two cold repository replays (2/2 exact for this unit across the two rounds), current-source cold replay, target xrefs/caller bindings, and clean committed TH08 family corroboration. |
| MATH-006 | exact / authored-game / wrapped angle addition | `AddNormalizeAngle @ 0x0042AED0-0x0042AF3E` adds two float angles and repeatedly subtracts/adds `2*pi` until the result is within `[-pi,+pi]`, preserving the target-observed `i++ > 16` escape bound. The natural source reproduces all 111 bytes and six float-constant relocations. Exact `AnmManager::ExecuteScript` repeatedly relocates to this physical helper; Bullet/ECL/Enemy source also uses the same target entry. | Two cold canonical rounds, current-source replay, six reviewed constant relocations, and exact caller relocation evidence. |
| PLAYER-030 | exact / authored-game / angle-to-point leaf | `PlayerAngleView::AngleToPoint @ 0x0041BB70-0x0041BBD4` uses the target-backed Player position at +0x1B88, subtracts the input x/y, returns `pi/2` for the zero vector, otherwise calls exact `VectorAngle @ 0x00405700`. The neutral view avoids claiming the original Player class/TU spelling. | Two cold canonical rounds reproduce 101/101 bytes with five reviewed relocations; current-source replay is exact and the ECL physical caller still resolves the target entry. |
| BOUNDARY-164 | reviewed / Packet-196 angle leaves | `VectorAngle` ends at `0x0040570E` and leaves one unowned `CC` byte at `0x0040570F`; `PlayerAngleView::AngleToPoint` ends at `0x0041BBD4` and leaves eleven `CC` bytes before independent `0x0041BBE0`; `AddNormalizeAngle` ends at `0x0042AF3E` and leaves one `CC` byte before independent `0x0042AF40`. No alignment is charged to authored code. | Raw target disassembly, candidate inventory extents, and whole-extent canonical comparisons. |
| TOOLCHAIN-168 | exact / Packet-196 angle cohort plus ECL manifest-label caveat | Packet 196 adds three `/O2 /Ob1` canonical units totaling 227 authored bytes. Two cold rounds pass 6/6 and a fresh current-source replay passes 3/3. Exact `AnmManager::ExecuteScript` remains configured-exact. `ecl-resolve-int` still reports the pre-existing compiler-local `$L####` manifest-name drift, but an independent relocation-aware comparison of its full 1,980-byte physical code-plus-switch-data extent is structural-exact at 1,320/1,320 ordinary bytes plus 660 relocation-field bytes; all destinations, including Player AngleToPoint, remain correct. The packet does not churn unrelated local-label names. | `new-three-two-rounds.log`, final current-source JSON receipts, `affected-callers.log`, and `ecl-direct-physical.json`. |


## Packet 197 SoundPlayer SFX queue exact pair

- `SoundPlayer::PlaySoundByIdx @ 0x0043E2F0` is repository-canonical exact at 140 bytes. TH09 independently fixes `unconsumedMetadataBySound +0x408`, the 12-slot SFX queue +0x620, request counts +0x650, pan data +0x680, and metadata table `g_SoundBufferIdxVol @ 0x004A1F80`. Natural source scans/reuses a matching queue slot, caps each slot at 128 pending pan requests, or installs a new slot. Multiple exact TH09 GameManager/Title/Ascii callers target this physical entry; committed TH08 corroborates only the source family after TH09 closure.
- `SoundPlayer::PlaySoundPositionedByIdx @ 0x0043E380` is canonical exact at 153 bytes. TH09 gameplay callers pass world-X as the float second argument. Target data `0x004914AC` is 0x40DE38E4 = 6.9444447f; natural TH09 source computes `(int)(positionX * 6.9444447f)` and then uses the same queue semantics. This intentionally differs from committed TH08's positioned-pan formula; no adjacent-game constant or layout is imported.
- Both real-class methods pass two independent cold repository-managed rounds (4/4 exact, 293/293 bytes). After adding them at the end of the existing target-backed `SoundPlayerProcessQueues.cpp`, all 18 canonical units sourced from that TU replay exact, so the added definitions do not perturb the established large-TU codegen surface.
- Reviewed seams: plain SFX queue helper `0x0043E2F0-0x0043E37B`, followed by four unowned `CC`; positioned helper `0x0043E380-0x0043E418`, followed by seven `CC` before independent exact `0x0043E420`. No alignment is authored.


## Packet 198 loading-VM and Title indicator exact leaves

- `SupervisorLoadingSetupView::SetupLoadingVms @ 0x004302B0` is repository-canonical exact at 173 bytes. TH09 independently fixes `loadingAnm +0x5D0` from exact `Supervisor::AddedCallback`, setup flag `+0x740` from Title/Supervisor state, and three contiguous 0x2A4 loading VMs at `0x004B38B0`. Natural source executes ANM scripts 0/1/2 exactly once, sets the setup flag, then copies the caller Float3 into each VM position at +0x208. Fifteen relocations resolve to the loading-VM array and exact `AnmLoaded::ExecuteAnmIdx @ 0x00401560`.
- `TitleScreenView::SetCharacterSettingIndicator @ 0x0042520B` advances from source-present/NON-EXACT to canonical exact at 40 bytes. The previous equivalent `10 - (value != 0)` source emitted 43 bytes; ordinary target-shaped ternary `value ? 9 : 10` naturally emits the target `neg/sbb/add` lowering under the already-established Title `/O1 /Ob1` profile. No register forcing, volatile, pragma, padding, or assembly is used.
- Both units pass two cold rounds (4/4 exact, 213/213 bytes). All 18 existing canonical `TitleScreen.cpp` units replay exact after the source-expression refinement, and exact `Supervisor::AddedCallback` remains 238/238. Reviewed seams: loading setup `0x004302B0-0x0043035C` then three unowned CC before independent `0x00430360`; Title indicator `0x0042520B-0x00425232` immediately followed by independent `0x00425233`.


## Packet 199 pinned D3DX8 boundary/origin review

- Direct parsing of the pinned VC7.1 Platform SDK `d3dx8.lib` (SHA-256 `39a8e21889a7c1f0b966f04a9e7d392de14ddebb3e091dfa1e5ce3e19564fc28`) yields target-local provenance for 680 previously pending TH09 candidates totaling 176,461 bytes. Of these, 614 complete candidate extents match COFF function records after ignoring only explicit i386 relocation fields; 66 additional candidates lie wholly inside complete relocation-masked COFF contributions matched to the TH09 `.text` image.
- These 680 entries are now `library / D3DX8 / exclude / high`. The classification establishes reviewed boundary and library origin only; it grants no authored source-presence or codegen-exact credit. Every untouched ledger line remains byte-for-byte unchanged, including historical rows with extra CSV fields.
- The audit is reproducible through `scripts/audit-runtime-origins.py`; it verifies the private TH09 target hash before reading bytes and reports findings without modifying ledgers. `scripts/apply-runtime-origin-review.py` applies only the strong function/contribution/import evidence classes through line-preserving target-row replacement. Short matches below the five-nonrelocation-byte threshold remain pending rather than inheriting D3DX8 origin merely from shape.
- Tracking moves from 1,493 pending / 37 exclusions to 813 pending / 717 exclusions. Authored, source-present, and canonical exact totals remain independently unchanged at 634 / 570 / 503.


## Packet 200 pinned CRT and import boundary/origin review

- The same target-local audit closes 370 additional candidates totaling 60,862 bytes: 337 complete relocation-masked COFF function extents, 28 candidates contained by complete matched COFF contributions, and five exact six-byte `FF 25 <IAT>` import thunks proven from the TH09 PE import directory.
- The pinned library identities are VC7.1 `libcmt.lib` SHA-256 `6e2b3742e58245de52149137f64281b73db1487a07a31e165fff269fbf9b2ee8` and `libcpmt.lib` SHA-256 `c887292bc98d87ab18d151289c68ade30fa078f680b042911312a42a34d659f5`. Ledger classification is 351 `library / CRT`, fourteen `library / Library` where the identical body occurs in more than one pinned archive family, and five `import_thunk / Imports`; all are excluded from the authored denominator.
- The exact import-thunk extents are `DirectInput8Create @ 0x0044E5A0`, `DirectSoundCreate8 @ 0x0044E5A6`, `Direct3DCreate8 @ 0x0044E5AC`, `WINNLSEnableIME @ 0x0047B04E`, and `RtlUnwind @ 0x0048D204`. Each is exactly six bytes and names an IAT slot parsed from the attested TH09 image.
- Tracking becomes 443 pending / 1,087 exclusions. The remaining review cohort is deliberately not bulk-promoted: 49 findings have fewer than five stable non-relocation bytes and 394 candidates are unmatched by the pinned library archives. Authored/source/exact totals stay 634/570/503.


## Packet 201 target-context review of short runtime bodies

- Thirty-one of the 49 short shape-only findings are now resolved by independent target context, not by their one-to-six stable bytes alone. Twenty-eight lie inside the contiguous D3DX8 linker run and three lie inside the CRT run; every reviewed extent is bracketed by already proven runtime exclusions, and target xrefs/callees are consistent with that placement.
- The reviewed set totals 162 bytes and is classified `library / D3DX8` (28) or `library / CRT` (3), with `exclude / high` disposition. No source-presence or exact-codegen credit is granted. The three CRT cases are the `_free` and `_atol` forwarding entries at `0x0047B249` and `0x0047B7EA`, plus the three-byte helper at `0x004889B8`.
- `scripts/apply-context-origin-review.py` reproduces the selection conservatively: it requires the weak pinned-COFF audit result, pending ledger state, and reviewed neighboring runtime exclusions before changing a row. Eighteen short findings remain pending: eleven target-local global initializer/terminator wrappers at the end of `.text` and seven genuinely ambiguous game-side short bodies.
- Tracking becomes 412 pending / 1,118 exclusions; authored/source/exact remain 634/570/503.


## Packet 202 complete VC7.1 global-init tail boundaries

- The attested target's `.data` prefix at `0x004A0000` is a NULL-bounded initializer table: after `___security_init_cookie @ 0x00488291`, it contains 24 consecutive function pointers from `0x0048D7B0` through `0x0048DA20`, then two NULL dwords. The pointed wrappers occupy the final generated-code run in `.text`.
- Ten initializer wrappers each push one cleanup-wrapper address and immediately call the already proven CRT `_atexit @ 0x0047C323`. Those cleanup entries occupy `0x0048DA30-0x0048DAD9`, with `0x0048DADA` exactly equal to the exclusive `.text` end. Function ends are proven by terminal `ret`/tail-`jmp` instructions and intervening `CC` padding, not inferred from the incomplete IDA auto-function list.
- The complete run is 24 initializer wrappers (443 bytes) plus ten terminator wrappers (101 bytes). The initial IDA-derived ledger contained eleven of the 34 and omitted 23 valid starts; `scripts/review-static-init-tail.py` verifies the target hash, PE sections, full pointer table, all exact extents/padding, and all ten `_atexit` registrations before inserting or classifying anything.
- All 34 are `compiler_generated / Compiler / exclude / high`. Candidate inventory becomes 2,187, adding the 23 missing boundaries; pending falls from 412 to 401 because the eleven pre-existing candidates are closed. Exclusions become 1,152, while authored/source/exact remain 634/570/503.


## Packet 203 target functions recovered from global-init wrappers

- Four global-init/termination wrappers tail-jump to game-code entries omitted by the initial IDA inventory: `0x0042B0E0` (44 bytes), `0x0042D290` (18 bytes), `0x00435DC0` (227 bytes), and `0x0043E470` (34 bytes). Each target extent ends in `ret` and is followed only by `CC` alignment up to the next established function.
- `0x0042B0E0` is authored ZunMemory cleanup because it implements the policy of walking 0x1000 registry slots and freeing non-NULL allocations. `0x0042D290` explicitly clears a complete 0x44-byte object, and `0x0043E470` clears its object then assigns `-1` to a selected 0x200-byte member region; neither behavior can arise from an implicit VC7.1 default constructor, so both are authored game code.
- `0x00435DC0` is a member-construction sequence and its boundary/constructor role are proven, but target code alone does not distinguish an explicit empty constructor from an implicitly generated special member. It is added as a reviewed `unknown / review` candidate rather than forced into either denominator.
- Inventory becomes 2,191 candidates / 637 authored / 1,152 excluded / 402 pending. Source-present/exact remain 570/503.


## Packet 204 compiler helpers inside the game linker band

- `0x00401470` is the VC7.1 four-argument `??_H` vector-constructor iterator already named by many exact repository COFF relocations. Its complete 38-byte target body loops over `count` elements with the supplied stride/callback and returns with `ret 0x10`; it is compiler-generated, not authored game logic.
- Five pending 33-byte bodies at `0x0042CC30`, `0x0042CC60`, `0x00431680`, `0x00440C20`, and `0x00440C50` match the established VC7.1 scalar deleting-destructor template in all 25 non-relocation bytes. Each first calls its game destructor and then conditionally calls exact `j__free @ 0x0047B249` when flag bit zero is set.
- These six candidates become `compiler_generated / Compiler / exclude / high`; pending becomes 396 and exclusions 1,158. Authored/source/exact remain 637/570/503.


## Packet 205 complete game-band authored-origin sweep

- All 387 still-pending candidates below the `0x0044E000` runtime transition were boundary/origin reviewed. Target disassembly covers every declared byte and every logical extent ends in `ret` or a reviewed tail jump; the ledger set is non-overlapping and address ordered.
- The sweep promotes 353 candidates totaling 104,034 bytes to `authored_game / Game / authored / high`. Positive evidence is nontrivial game-visible policy, state mutation, callback, math, rendering, archive, input, or lifecycle behavior inside the game object-code band after pinned runtime bodies and compiler-generated helpers were independently removed. This grants origin/boundary credit only.
- At Packet 205, thirty-four candidates were retained `unknown / review`: member-construction/destruction-only special members plus the then-known shared/folded tiny bodies at `0x0042F3F0`, `0x0043D2B0`, and `0x00435EC0`. The sweep classified `0x004343D0` as authored at that time; Packet 318 supersedes that single origin assignment after later exact manifests and fresh xrefs proved shared/folded physical ownership.
- The exact promoted-address set is frozen by SHA-256 `6e7168c5f8cfd8de58cd1ce96f49fa26aa48024a3825fa2e1e42c7a8c111793a`; the updater fails if the cohort changes, so future candidates cannot be silently bulk-promoted. Tracking becomes 2,191 candidates / 990 authored / 1,158 excluded / 43 pending; source-present/exact remain 570/503.


## Packet 206 transition-record pseudo-functions

- Four provisional IDA entries at `0x0044E1C4`, `0x0044E1D4`, `0x0044E244`, and `0x0044E254` begin at byte four of complete 16-byte transition records. Their record-index bytes `C2`, `C3`, `CA`, and `CB` accidentally decode as x86 `ret`/`retf`; they are data rather than code boundaries.
- `scripts/review-transition-data.py` reattests the target and verifies all four complete records before assigning `data / Data / exclude / high`. Tracking becomes 2,191 candidates / 990 authored / 1,162 excluded / 39 origin-unresolved or unreviewed; source-present/exact remain 570/503.


## Packet 207 runtime-origin residual closure

- `0x00454650` is an 11-byte non-return D3DX8 exception helper that calls reviewed CRT `longjmp`; its only IDA xref comes from the enclosing reviewed D3DX8 routine. `0x0045E550` is a 266-byte terminal-ret D3DX8 initializer that writes 34 dispatch slots with targets entirely inside the reviewed D3DX8 linker band, and its sole caller is D3DX8.
- `__fptrap @ 0x004866AD` is the seven-byte CRT helper that calls reviewed `__amsg_exit(2)`. `_abort @ 0x00487119` is the 23-byte canonical CRT sequence `__NMSG_WRITE(10)`, `raise(22)`, `_exit(3)`.
- `0x0048A853-0x0048CB41` is the complete 8,943-byte terminal-ret4 `DXGetErrorString8A` code extent. It maps DirectX HRESULT families to symbolic names including `E_POINTER`, `DIERR`, `VFW`, and `DMUS`; its sole IDA caller is the game DirectPlay error formatter. The containing 10,672-byte target contribution and representative string references are hash/byte frozen by `scripts/review-runtime-residuals.py`.
- These five entries become `library / exclude / high` in D3DX8, CRT, or DXErr8. Tracking becomes 2,191 candidates / 990 authored / 1,167 excluded / 34 unresolved; source-present/exact remain 570/503.


## Packet 208 reviewed-unknown closure

- At Packet 208 every remaining candidate had received a target-bound boundary/origin review. The then-current 34-entry unknown set was frozen by SHA-256 `17bb6cebc7577183c2b339fca631bb1b41c754d3239916995324d9670eb67ef0`; Packet 318 later supersedes that set with one newly evidenced shared/folded body.
- The Packet-208 set contained thirty-one construction/destruction-only special members whose bodies were compatible with both explicit out-of-line source and implicit VC7.1 generation, plus three then-known shared/folded physical bodies: `0x0042F3F0`, `0x0043D2B0`, and `0x00435EC0`.
- Packet 208 established the reporting split between review completion and origin resolution: origin/boundary unreviewed was zero while 34 entries remained reviewed-but-unresolved. Packet 318 updates only that origin accounting after new folded-ownership evidence; historical source/exact totals at this packet remain unchanged.

## Source-presence ledger reconciliation

- A post-closure repository audit found 62 authored functions whose `functions.csv` rows already named maintained source files and recorded non-exact compiler evidence, but whose names and addresses had not been copied into `implemented.csv` and `reccmp-functions.csv`. The omission affected source-presence accounting only; no exact claim, origin, boundary, source body, or target byte changed.
- The source-present total is therefore 632 functions / 171,326 authored bytes. Exactness remains independently 503 functions / 72,353 authored bytes, leaving 129 maintained functions / 98,973 bytes honestly non-exact. Another 358 authored candidates / 104,604 bytes have no maintained source path; 354 of those still use the deliberately generic `Game` module pending bounded subsystem/semantic recovery.
- `scripts/validate-tracking.py` now requires source mappings to cover every authored function row with a maintained source file, preventing this accounting split from recurring.

## Packet 209 exact global-owner recovery

- `ZunMemory::~ZunMemory @ 0x0042B0E0-0x0042B10B` is repository-canonical exact at 44 bytes. The target terminator wrapper at `0x0048DA60` loads `g_ZunMemory @ 0x004AD018` into ECX and tail-jumps to this body. TH09 independently fixes a 0x1000-entry registry-pointer array followed by `bRegistryInUse @ +0x4000`; when enabled, the destructor scans all entries and calls CRT `_free @ 0x0047B163` on each non-null pointer. Natural maintained source reproduces all bytes and the sole relocation.
- `SoundPlayer::SoundPlayer @ 0x0043E470-0x0043E491` is repository-canonical exact at 34 bytes and supersedes the provisional `GameManager` routing hypothesis. The target initializer wrapper at `0x0048DA20` loads `0x004DC698`, already bound to `g_SoundPlayer` by exact SoundPlayer relocation manifests, then tail-jumps here. The body clears exactly `0x6224` bytes, matching `sizeof(SoundPlayer)`, then fills 128 dwords at `+0x408` with `-1`, exactly the target-backed `unconsumedMetadataBySound[128]` field.
- Both new owners pass two cold pinned-VC7.1 `/O2 /Ob1 /Oy- /Gr` canonical rounds. Existing `ZunMemory::Alloc/Free/AddToRegistry` and representative SoundPlayer queue/audio units remain exact after the layout/declaration additions. No padding, register forcing, inline assembly, target-byte material, or profile fishing is used.

## Packet 210 Player private-stage exact ABI

- `PlayerUpdateStageC @ 0x0041E8B0-0x0041E8F9` is repository-canonical exact at 74 bytes. The exact `PlayerLifecycleView::OnUpdate` caller already keeps the Player object in ESI; expressing the helper as a same-TU internal-linkage `static int helper(PlayerLifecycleView *)` makes VC7.1 naturally consume that ESI value with no source-visible register convention.
- The target logic is preserved with ordinary nested C++: require update state zero and timer `+0x1B74 <= 0`, query `PlayerCollisionQueryStateView @ +0x36C`, set the linked collision owner word at `+0xDBE` to 5 when present, call maintained `EnterDeathState`, and return 1; all failure paths return 0 from the target common tail.
- A flat early-return spelling emitted the same semantics but caused VC7.1 to hoist the zero-return block into the middle of the function and matched only 28/58 ordinary bytes. The nested source naturally reproduces all 58 ordinary bytes plus four REL32 fields. No register forcing, asm, padding, pragma ordering, or target-byte material is used.

## Packet 211 Player state-4 exact closure

- `PlayerCheckState4 @ 0x0041E720-0x0041E8AF` is repository-canonical exact at 400 bytes. Exact `PlayerLifecycleView::OnUpdate` already keeps the Player object in ESI; same-TU internal linkage with an ordinary `PlayerLifecycleView *` parameter makes pinned VC7.1 naturally use that private ESI transport.
- Before frame 60, the helper advances `position +0x1B88` by the vector at `+0x1CCC` scaled by `(60-timer)/20`, clamps x/y through the four target globals at `0x004A80F0-0x004A80FC`, and derives main-VM alpha at Player `+0x2B0` from the timer current value. At/after frame 60 it computes the target 400-or-`130-state*10` meter delta, updates state to 3, sets timer 60, and installs the two target transition records with `(16,20,8,0)` and `(16,20,10,8,0)`.
- A first natural probe that split the vector expression into named `remaining/displacement/step` locals emitted 407 bytes: VC7.1 stored/reloaded one x87 temporary. Writing the source as the ordinary chained expression `velocity * (60-timer) / 20` lets the compiler queue the hidden return for division while evaluating multiplication and removes exactly those seven bytes, producing 304/304 ordinary bytes plus all 24 relocation fields. This is source-lifetime evidence, not register or encoding forcing.
- Canonical relocations reuse exact `ZunTimer::operator float`, `ZunTimer::operator=` and `Float3::operator*`. The physical destinations for Float3 division/add-assign/pointer conversion, the timer-current folded getter, meter/state helpers, transition installers, and playfield data remain independently owned/source-tracked; exactness of this caller does not promote those dependencies.

## Packet 212 Player reward-tail allocator

- `PlayerLifecycleView::SpawnRewardTailState @ 0x0041DB90-0x0041DC19` is repository-canonical exact at 138 bytes. Its only two TH09 callers are the Enemy death/reward owner: one passes Enemy reward type `+0x335C`, the other a target RNG value in `[0,4)`, and both pass Enemy position `+0x2D74` to the side-local Player resolved through Enemy manager side state.
- The target scans exactly four `0x2C4` slots rooted at Player `+0x30454`. The slot layout is independently cross-checked by exact `DrawTailStates @ 0x0041DF80`, whose view starts four bytes later at `+0x30458`: creator fields are type `+0x00`, position `+0x04`, velocity `+0x10`, active `+0x1C`, and VM `+0x20`. A free slot receives velocity `(0,-1.5,0)` and runs ANM script `type+70` through the side-local ANM bank.
- Natural source-shape experiments explain the full VC7.1 loop/code order. Declaring the slot pointer before the shared-update gate naturally gives the target EDX pointer / ESI gate transport. Writing the scan as a four-iteration `for` whose free-slot branch fills the slot and returns keeps the active load at the aligned loop header and avoids both while-loop rotation and a redundant post-break bounds check. This produces 130/130 ordinary bytes plus both relocations without register forcing, padding, assembly, or profile fishing.

## Packet 213 Player graze collision leaf

- `PlayerLifecycleView::CheckGrazeCollision @ 0x0041E350-0x0041E41B` is repository-canonical exact at 204 bytes. TH09 `EtamaController::OnUpdate` calls it on the side-local Player with Bullet position, sprite collision size and Bullet pointer; return 1 sets the bullet grazed flag. The helper first delegates to the still independently source-absent `CheckBulletCollision @ 0x0041DFF0` and returns 2 when that core already reports a hit.
- TH09 independently proves the Player graze AABB layout. The authored Player owner at `0x0041C170` computes Player `+0x1C78/+0x1C84` by subtracting/adding `grazeHalfSize @ +0x1CB4` from the current position. The graze helper halves incoming bullet collision width/height, expands min/max by `60.0f` per side, and tests overlap against those stored bounds.
- The target `0x18` local frame corresponds exactly to two 12-byte `PlayerPositionView` temporaries `incomingMin/incomingMax`. A four-float spelling is semantically equivalent but VC7.1 keeps values on the x87 stack and emits only 184 bytes; restoring the two natural vector locals produces all 176 ordinary bytes plus seven relocation fields. No register forcing, padding, asm, or profile search is used.

## Packet 214 Etama draw helper and owner shell

- `DrawSingleBullet @ 0x00413AF0-0x00413BC6` is repository-canonical exact at 215 authored bytes. The sole target caller is the six-bucket loop in `EtamaController::OnDraw @ 0x00413BE0`, which keeps the current Bullet in EBX; ordinary same-TU `static int DrawSingleBullet(Bullet *)` therefore lets VC7.1 naturally consume that private EBX value. The exact physical comparison includes the compiler NOP at `+0xD7` and four-entry state switch table at `+0xD8..+0xE7` for 232/232 bytes with all sixteen relocations solved.
- TH09 independently fixes the helper behavior over the maintained Bullet layout: state 2/3/4/5 selects spawn-fast/spawn-normal/spawn-slow/despawn VM, default selects the main bullet VM; Bullet position `+0xD4C` is mapped through exact GameManager X/Y transforms into VM position `+0x208`, z becomes 0.05, RGB bits are ORed to white while alpha is preserved, nonzero VM word `+0x1FC` applies `AddNormalizeAngle(pi/2 + bullet->angle, 0)` through exact `AnmVm::SetZRotation`, then exact `AnmManager::Draw2D` runs.
- `EtamaController::OnDraw` is now maintained source-present but remains NON-EXACT at 568 candidate bytes versus the 600-byte target. The target-backed source covers side viewport selection, all 48 Laser body/start-cap draws, the six draw-bucket lists, and the side-local EffectManager tail. Adjacent TH08 supplied only the broad draw-family source shape; every TH09 offset, constant, condition and callee was independently checked. The outer owner is not promoted merely because its private helper is exact.

## Packet 215 Etama added callback exact closure

- `EtamaController::AddedCallback @ 0x00413E40-0x00414095` is repository-canonical exact at 598 authored bytes. Exact `EtamaController::Register` stores this address into `calcChain->addedCallback`, fixing ownership independently of adjacent games. The callback gets ANM slot 8, preloads `etama.anm` only when the returned slot state at +4 is empty, publishes it at controller +0x25E1BC, then initializes 23 bullet-template records.
- The 115-dword TH09 table at `0x0048E628` is recovered as 23 rows of five script indices. Each row drives the bullet/spawn-fast/spawn-normal/spawn-slow/despawn VMs; all five receive flags bit `0x2000`. TH09 also copies the bullet VM active sprite index to baseSpriteIndex and BulletTypeSprites +0xD44, truncates loaded-sprite height +0x30 to byte +0xD41, then selects the target collision size/draw bucket from height thresholds 8/16/32 and the TH09 primary-script cases 2-9 and 96-107.
- Natural source reaches exact codegen only when the shared `g_AnmManager` pointer has an ordinary local lifetime spanning GetAnm/PreloadAnm, while per-template height/collision values are read/written directly rather than cached. VC7.1 then reproduces the target register lifetimes without any register forcing. The complete physical comparison is 832/832 bytes: 598 logical code bytes plus 234 bytes of compiler switch data through `0x0041417F`, with all 30 relocations solved.
- This packet closes the final no-source owner in the routed Bullet `0x00413AF0-0x00414095` seam. Exact `DrawSingleBullet` remains the same-TU private-EBX leaf and `EtamaController::OnDraw` remains an independent maintained NON-EXACT owner; equality is not transferred between them.

## Packet 216 ANM surface load exact pair

- `AnmManager::LoadSurface @ 0x0043CAE0-0x0043CCC5` is repository-canonical exact at 486 bytes. Existing exact caller relocations already route this physical entry as LoadSurface; TH09 independently fixes the surface arrays at `+0x123FC/+0x1247C`, staged bytes/sizes at `+0x124FC/+0x1257C`, and image-info records at `+0x125FC`. The routine releases an existing primary surface, consumes staged bytes or exact `FileSystem::OpenFile`, creates a 640x1024 temporary image surface, decodes through D3DX8, creates the primary/render and restore surfaces, copies the decoded pixels into both, releases the temporary surface, and frees source bytes.
- `AnmManager::PreloadSurface @ 0x0043CCD0-0x0043CD35` is canonical exact at 102 bytes. It shares the same target-backed surface plane, releases an existing primary surface, opens the path, and stores the returned bytes and size for later LoadSurface consumption; both failure paths use exact `GameErrorContext::Fatal` with the TH09 string at `0x004910D4`.
- The target surface-info stride is `0x14` and the D3DX8 relocation at `0x004523F6` naturally matches a five-field DX8 `D3DXIMAGE_INFO`. This is distinct from the later 0x1C DX8.1 header shape; the local 0x14 definition is therefore an ABI reconstruction backed by TH09 offsets/codegen, not an adjacent-game layout import.
- Both functions are exact under the already-established ANM loader `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- /I src` profile with 25 reviewed relocations total. This closes the routed ANM `0x0043CAE0-0x0043CD35` no-source seam between exact `ReleaseAnm` and `PostloadAnmEntry`.

## Packet 217 Title music-room draw exact leaf

- `TitleScreenView::DrawMusicRoom @ 0x004244A9-0x00424579` is repository-canonical exact at 209 bytes. TH09 calc dispatcher `0x0042A9CB` routes currentScreen 12 to exact/source-present `OnUpdateMusicRoom`, while draw callback `0x00424898` routes only currentScreen 12 to this body; ownership/name therefore do not depend on adjacent-game numbering.
- The target-backed MusicRoom layout fixes `musicSelectedSongIndex +0x11B74`, dynamic `vms +0x11B98`, and `embeddedVms +0x11BA0`. In ready state the routine saves the selected song-row VM at index `musicSelectedSongIndex+159` position/color, draws it temporarily at `(208,293)` in white, restores it, then draws exactly `embeddedVms[49..56]`, the eight description VMs populated by OnUpdateMusicRoom.
- Natural Title `/O1 /Ob1 /Oy- /Gr` code reaches 209/209 only when the array index is held as an ordinary local `songVmIndex` rather than caching an AnmVm pointer or repeating the full member expression. VC7.1 then naturally emits the target 0x14 stack frame and common scaled-index lifetime. Six relocations resolve to the two float constants and exact `AnmManager::DrawNoRotation` calls; no register forcing, padding, assembly, or target-byte material is used.

## Packet 218 Title replay-save draw source closure

- `TitleScreenView::DrawReplaySave @ 0x004239F6-0x00423D15` is now maintained source-present/NON-EXACT at 800 target bytes. TH09 calc dispatcher `0x0042A9CB` maps screen 15 to `UpdateReplaySave`, and draw dispatcher `0x00424898` maps only screen 15 to this body, fixing the owner/name locally. The draw body calls only canonical exact `AsciiManager::AddFormatText @ 0x00434330` and `AddString @ 0x004342A0`.
- TH09 dataflow independently fixes the replay-save UI layout: `keyboardSelection +0x0`, `replayNameCursor +0x4`, `replaySaveState +0x28`, `stateTimer +0x2C`, `replayName +0x8C`, 50 replay records at `+0x68E4` with `0x1EC` stride, `selectedReplay +0xC908`, and `stateTimer2 +0x1B230`. Replay records contribute `magic +0x0`, `playTime +0xC4`, and `playerName +0xCE`. The target strings and `g_ReplayPlayTimeText @ 0x004AC879` close the displayed slot format and active-name line without adjacent-game assumptions.
- State 2 draws 25 replay slots, using target placeholders `--------` / `--/--/--` for empty records. States 3/4 move the selected slot toward `(224,224,0)` over ten frames, draw the replay name cursor, and state 4 draws the 16x6 alphabet grid with the target pulsing selected scale/color and 0x7F/0x80 final-row special glyphs. The common tail restores Ascii color/selection/scale.
- Under the established TitleScreen `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Gr /O1 /Ob1 /Oy- /I src` profile, two cold builds of the retained natural source emit 783 bytes versus the 800-byte target. Bounded natural probes moved `716 -> 732 -> 783` by restoring replay-pointer lifetime and the completed interpolation path; a target-spill-inspired scalar-local variant regressed to 771. The remaining 17-byte residual spans ordinary local/stack lifetime choices, including the target 0x34-byte frame versus the retained 0x2C frame. No register/volatile forcing, pragma ordering, inert padding, inline assembly, fake return, embedded target bytes, target patching, or profile roulette is retained. Exactness is therefore deliberately false.

## Packet 219 Title replay-menu draw source closure

- `TitleScreenView::DrawReplayMenu @ 0x004234F6-0x004239F5` is now maintained source-present/NON-EXACT at 1,280 target bytes. TH09 calc dispatcher `0x0042A9CB` maps screen 11 to `OnUpdateReplayMenu`, and draw dispatcher `0x00424898` maps only screen 11 here, fixing ownership/name from TH09 evidence. The body calls canonical exact `AsciiManager::AddFormatText @ 0x00434330` plus CRT `sprintf/strlen`; there are no unresolved game helpers.
- Screen-state 1 draws all fifty TH09 replay slots in two 25-row columns. The first column formats fixed replay numbers; the second column obtains the four user-replay identifier characters from target-backed `replayPaths[i][15..18]`, falling back to `user` for empty slots. Both columns use embedded `ReplayDataView` records at `+0x68E4` with `0x1EC` stride, `magic +0x0`, `playTime +0xC4` and `playerName +0xCE`.
- Screen-state 2 uses `selectedReplay +0xC908` and `stateTimer2 +0x1B230` to interpolate the selected list row toward `(216.5,160,0)`. TH09 target tables at `0x004A1D3C/0x004A1D50/0x004A1D54/0x004A1D58` provide difficulty and Story/Extra/Versus labels; `ReplayData.value1E4/value1E5` at `+0x1E4/+0x1E5` select the mode text. Ten detail rows then read character byte `+6` from `frameStart[0][i]` and `frameStart[1][i]` and format `%.8s vs %.8s` through the TH09 character-name table. TH08 contributes only the broad replay-menu draw family; none of its replay-count, stage-score, or UI geometry is transferred.
- Under the established TitleScreen `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Gr /O1 /Ob1 /Oy- /I src` profile, two cold builds of the retained natural source emit 1,244 bytes versus the 1,280-byte target. The first complete TH09-semantic draft emitted 1,187 bytes; restoring the target-observed independent Extra-mode `sprintf/strlen/center/AddFormatText` path raised it to 1,244. Explicit user-replay local-order shaping regressed to 1,239 and an `if/else-if + textLength` mode formulation regressed to 1,232, so neither is retained. The remaining 36-byte residual is ordinary local/control-flow scheduling across the selected-user and mode-title blocks. No register/volatile forcing, pragma ordering, inert padding, inline assembly, fake return, embedded target bytes, target patching, or profile roulette is used; exactness remains false.

## Packet 220 Title result draw source closure

- `TitleScreenView::DrawResult @ 0x00423D16-0x004244A8` is now maintained source-present/NON-EXACT at 1,939 target bytes. TH09 calc dispatcher `0x0042A9CB` maps screen 13 to `OnUpdateResult` and screen 14 to exact `OnUpdateResultNameEntry`, while draw dispatcher `0x00424898` routes both screens only to this body. The owner therefore remains TitleScreen even though the UI is the result/high-score family.
- TH09 already proves `g_TitleScoreTable @ 0x004A8380` as a `16 x 5 x 5` table of `0x2C` records. This draw owner selects a 25-record bank with `nameBankIndex +0x8` and emits five explicit difficulty blocks: Easy `(102,80)`, Normal `(358,80)`, Hard `(102,192)`, Lunatic `(358,192)`, and Extra `(102,304)`. Each block contains five rows, uses the target rank-label table at `0x004A1D68`, and formats `%s %.8s %.9d%d` from score `+0x0C`, name `+0x18`, and the signed tail byte `+0x2B`.
- Screen 13 sets an all-difficulty mask and no selected ranking row. Screen 14 uses `1 << g_TitleNameTableIndex` and `nameSlotIndex` to activate/highlight the current inserted record. The common alpha is `min(stateTimer2,30)*255/30` in integer arithmetic. In screen-14 name-entry state, the function also renders the TH09 16x6 alphabet keyboard at the target `(348,336)` origin, selected pulse scale/colors, and TH09 final-row glyphs `0x7F/0x80` through canonical exact `AsciiManager::AddString`.
- Clean TH08/TH095 result-draw sources corroborate an ordinary function-scope `char[16]` character buffer plus separate main/glyph Float3 locals; applying that natural local lifetime to the TH09 semantics changes the initial 1,859-byte draft to a stable 1,941-byte build and exactly reproduces the target `0x44` stack-frame size. However structural equality is still broad: target keeps `this` in EDI, score-group at `[ebp-8]`, and the per-row score-field pointer in EBX, while the retained candidate allocates them differently. Natural byte-pointer, function-scope-entry, and interior-record-view probes all remain 1,941 bytes with the same allocation. No register/volatile forcing, pragma ordering, inert padding, inline assembly, fake return, embedded target bytes, target patching, or profile roulette is retained. Exactness remains false despite the two-byte total-size residual.
- This closes maintained source coverage for the routed Ascii/Title `0x004234F6-0x00424579` no-source seam: `DrawMusicRoom` is exact, while `DrawReplayMenu`, `DrawReplaySave`, and `DrawResult` carry explicit reproducible NON-EXACT receipts.

## Packet 221 Float3 divide exact leaf

- `Float3::operator/(float) const @ 0x0040F5A0-0x0040F5D7` is repository-canonical exact at 56 bytes. TH09 computes `inverse = 1.0f / scalar`, multiplies x/y/z by the inverse, and constructs the hidden return object through exact `Float3::Float3 @ 0x004010B0`. Broad native callers in Player, EnemyManager and EtamaController establish the helper independently of adjacent-game naming; the mangled symbol already resolved by existing call-site manifests is `??KFloat3@@QBE?AU0@M@Z`.
- Natural VC7.1 `/O2 /Ob1 /Oi /Gr` source reproduces all 56 bytes with exactly two relocations: `1.0f @ 0x0048E2A4` and the constructor call. Clean TH08 source corroborates the source spelling only. This removes an anonymous math dependency from the Player collision/reward helper frontier.

## Packet 218 Player respawn-resource exact leaf

- `PlayerState4OpsView::AddRespawnResource @ 0x0041BC90-0x0041BD31` is repository-canonical exact at 162 bytes. Exact `PlayerCheckState4` already binds this symbol/address, and independent callers include both large Player update owners plus Enemy death rewards, fixing the shared Player resource role.
- The helper snapshots `int(scalar30388)/100`, adds the requested float, adds another 15% when target-backed `sideState->shotType20 == 1`, clamps the result to 400.0f, then calls exact `FrontSide::UpdateMeterHundreds` through `sideState->frontSide18` only when the hundred-bucket changes. All involved Player/side-state offsets were already maintained before this packet.
- Natural PlayerRuntime `/O2 /Ob1 /Oy- /Gr` source reproduces 162/162 with five reviewed relocations: two `__ftol2` calls, 0.15f, 400.0f, and the exact FrontSide helper. No register forcing, inline assembly, padding, or profile fishing is used.

## Packet 219 Player item-collection collision exact leaf

- `PlayerLifecycleView::CalcItemCollectionCollision @ 0x0041BEE0-0x0041BFB0` is repository-canonical exact at 209 bytes. Its sole caller is Player `UpdateBeforeState @ 0x0041DC20`, which passes a collectible position plus `Float3(itemCollectionBoxSize,itemCollectionBoxSize,16)` constructed from the target-backed primary SHT field +0x10.
- TH09 fixes Player `updateState00 @ +0x0` independently through the shared three-byte GetUpdateState body `mov eax,[ecx]; ret`. The collision helper accepts states 0 and 3, computes `position - size/2` and `position + size/2`, and compares those extrema against the distinct item-collection bounds `+0x1C90/+0x1C9C`. These are separate from the bullet-query bounds `+0x1C60/+0x1C6C`.
- Natural `PlayerPositionView` expressions reproduce 209/209 with four relocations: two view `operator/` calls resolve to canonical `Float3::operator/ @ 0x0040F5A0`, while view subtraction/addition resolve to exact `Float3::operator-/+`. This follows the already-established Player collision-view alias pattern and uses no forcing or padding.

## Packet 220 Player update-before-state exact closure

- `PlayerLifecycleView::UpdateBeforeState @ 0x0041DC20-0x0041DF4D` is repository-canonical exact at 814 authored bytes. Exact `PlayerLifecycleView::OnUpdate` is the sole target caller. The source updates four 0x2C4 reward-tail records at `+0x30454`, builds the item-collection box from the primary SHT, applies gravity/clamp, calls exact `CalcItemCollectionCollision`, handles resource/effect/owner-state reward paths, and publishes `targetOverride30F64` for uncollected records.
- A second TH09 call-site audit supersedes the initial negative receipt: `PlayerOwnerStateView::ApplyReward @ 0x0041D150` receives exactly five stack arguments (`position + four integers`) at all three reviewed callers. The float that IDA had inferred as an extra parameter is merely an unrelated FPU value, not a private ST0 argument. Removing that spurious public parameter naturally reduces the candidate physical contribution from 876 to the target's 852 bytes.
- Exact codegen additionally requires two ordinary source-lifetime facts: the tail-state pointer is established before constructing the local `Float3(itemCollectionBoxSize,itemCollectionBoxSize,16)`, and the gravity branch is written as `if (velocity.y < 3.0f) add 0.03f; else clamp 3.0f`. VC7.1 then reproduces all 584 ordinary comparable bytes and all 67 relocation fields over the complete physical extent: 814 code bytes, two alignment bytes, and 36 bytes of compiler switch data through `0x0041DF73`.
- This exact promotion uses no register forcing, inline assembly, padding, pragma ordering, embedded target bytes, fake returns, or profile roulette. The source-absent owner-state routine at `0x0041D150` remains independently unpromoted despite its corrected call signature.

## Packet 221 Player bullet-collision source closure

- `PlayerLifecycleView::CheckBulletCollision @ 0x0041DFF0-0x0041E34D` is now source-present under TH09-local ownership: canonical `CheckGrazeCollision` already relocates this exact symbol/address, and the additional Etama/laser callers establish it as the shared Player bullet-cancel collision service. The second `collisionSize` stack argument is target-ABI-preserved but not read by this body.
- The target scans a null-terminated pointer array at Player `+0xB100`. Each 0x44 region contributes center `+0/+4`, circle radius `+8`, half extents `+0x10/+0x14`, angle `+0x20`, hit count `+0x2C`, type `+0x38` and delay `+0x40`. Types 1/2/3 with nonpositive delay use circle, rotated-box, or axis-aligned collision tests. Type 1-style hits increment the region count and return 2 immediately; types 2/3 enter the Bullet reward path only for non-null bullet type 0.
- The reward path is completely TH09-bound: popup-transformed bullet position, side-switched randomized effect velocity, exact EffectManager spawn, effect `+0xA0/+0xA4/+0xA6/+0xA8/+0xAA` populated from Bullet speed/type/color/controller side/extra attribute, exact `AddRespawnResource(0.2f)`, sound 46, reward value from `ownerState30410 +0x38` capped/scaled to 1000 or x10, `ApplyReward(position,(mode>=3)+2,2,0,value)`, score +50, then hit-count increment.
- Exact codegen is deliberately not claimed. A compact natural source emitted 772 bytes; adopting the TH08-family function-level `delta/rotated/halfSize/boundsMax/xDelta/yDelta` organization and `goto hit/next` structure while retaining all TH09 data/semantics emitted 827 bytes; TH09-backed long-lived `player/currentBullet/slot` aliases plus direct `(*slot)` access recovered the target `0x34` stack frame and a repeatable 849-byte candidate. Two cold builds have identical disassembly hashes. The remaining 13-byte frontier is preserved-register allocation: target keeps Bullet in EBX from entry, while the best natural VC7.1 candidate caches position there. A `while(1)` loop-shape probe regressed to 830 bytes. Further pursuit would require register-lifetime steering, var-order forcing or equivalent encoding-directed source, so this owner remains honestly NON-EXACT.

## Packet 222 Player owner-state reward source closure

- `PlayerOwnerStateView::ApplyReward @ 0x0041D150-0x0041D73E` is now source-present under TH09-local Player ownership. Two physical callers contain three reviewed call sites: exact `UpdateBeforeState` passes `(position,1,0,400,0)` and `(position,0,0,0,70000)`, while `CheckBulletCollision` passes `(position,(rewardMode>=3)+2,2,0,rewardValue)`. All three use ordinary ECX receiver plus five stack arguments; the earlier decompiler-inferred ST0 parameter is disproven.
- The recovered 0x44 owner-state maintains count/max fields `+0x04/+0x08`, rounded score-like value and maximum `+0x0C/+0x10`, timers `+0x14/+0x20`, reward/spawn accumulators `+0x2C/+0x30/+0x34`, state/counter `+0x38/+0x3C`, and a fastcall-style callback at `+0x40`. TH09 target flow rounds the main value to tens, caps at 999990, fires target-local attack/configuration transitions when crossing 100k/300k/500k, queues side-indexed score popups on a count-dependent cadence, and drives two side-switched EffectManager reward loops from the established RNG/base/mode globals.
- The effect/timer tail is target-bound rather than imported from adjacent games: effect IDs are side+3 and side+1, velocity uses signed/positive TH09 RNG helpers, the first effect scale is `rand(0.5)*1/60 + base/600`, the second uses the five observed mode-specific affine formulas, and timer20 receives +30/+5/+2 below 30/50/60 before timer14 is assigned `timer20.current+45` when a new maximum is reached.
- Exact codegen is deliberately not claimed. Natural VC7.1 `/O2 /Ob1 /Oy- /Gr` probes bracketed the target with 1492-1544 byte candidates. The maintained candidate is 1544 bytes, matches the target `0x18` stack frame and ESI=this/EBX=old-value lifetime, but remains 25 bytes larger because VC7.1 does not reproduce the target popup-cadence tail merge/register allocation from ordinary source. TH095/TH10 contain no directly corresponding reward routine. Further work would require encoding-directed register/control-flow steering, so this owner is frozen NON-EXACT pending genuinely new source/TU evidence.

## Packet 223 Player short cross-subsystem leaves

- Four authored leaves in `0x0041BD40-0x0041BD94` are now repository-canonical exact under an isolated Player leaf TU. `SetBlendModeAdditive @ 0x0041BD40` is 19/19, `SetBlendModeNormal @ 0x0041BD60` is 8/8, `GetAnmRawData @ 0x0041BD70` is 19/19 with its sole relocation solved to exact `AnmManager::GetAnm @ 0x00401660`, and collision-region `Deactivate @ 0x0041BD90` is 5/5.
- TH09 independently fixes the two blend leaves: both callers pass Player `mainVm @ +0xC0`, and the maintained AnmVm bitfield proves `flagsWord +0x1F8` bits 4-5 are the two-bit blend mode. The first leaf clears bit 5 and sets bit 4 (mode 1/additive); the second clears both (mode 0/normal). Clean committed TH08 supplies only the `SetBlendModeAdditive/Normal` naming family after those local facts are established.
- `GetAnmRawData` is target-local Player preload glue rather than a new AnmLoaded ownership claim: the sole caller supplies g_AnmManager in ECX and slots 5/6, exact GetAnm returns the 0x120-stride slot, and the TH09 loader independently writes its raw-data pointer at `AnmLoaded +0x04`.
- `PlayerCollisionRegionRuntimeView::Deactivate` is fixed by two TH09 collision-update call sites and the maintained 0x44 region view: it clears only `active +0x3C`. Keeping these cross-subsystem definitions in a dedicated TU avoids perturbing compiler-local labels and private ABI choices in the existing PlayerRuntime object.

## Packet 224 Player two-side ANM preload exact closure

- `PreloadPlayerAnmResources @ 0x0041CDE0-0x0041CE51` is repository-canonical exact at 114 bytes. Its sole target caller is `GameManager::GameplaySetupThread`, and the body operates on the shared ANM manager rather than on a Player receiver.
- TH09 independently fixes the two resource records: `0x004A7DB0` and `0x004A7DE8` are the `+0x20` windows of two 0x38 GameManager side records, so the helper compares `shotType20` against `previousShotType24` and reads the side-mode dword at record `+0x30`. It maps those two sides to ANM slots 5 and 6.
- The 6-entry-per-character table at `0x004A1238` is target-read as `plNN.sht / plNN.anm / plNN.ecl / plNN.msg / plNN_match.msg / plNNb.anm`. The helper keeps an existing ANM only when exact `GetAnmRawData` reports non-null data and the current/previous shot type agrees; otherwise it chooses table column 1 or 5 from the side mode and calls exact/source-present `AnmManager::PreloadAnm`.
- Natural `/O2 /Ob1 /Oy- /Gr` source is exact once the ordinary for-loop increment is written in the target-observed source order `++side, ++slot`. The first spelling `++slot, ++side` was target-sized with all six relocations correct and only seven ordinary bytes reordered at the loop tail; no register forcing or encoding-directed construct is involved.

## Packet 225 Player respawn-transition exact closure

- `PlayerTransitionSpecial @ 0x0041CB50-0x0041CC74` is repository-canonical exact at 293 bytes. Exact `PlayerLifecycleView::OnUpdate` is the sole caller: update state 1 reaches it directly, while state 4/2 reach it only when their transition checks request the same path. The caller already keeps Player in ESI and emits a bare call.
- A same-TU internal-linkage `static void helper(PlayerLifecycleView*)` lets VC7.1 naturally consume that private ESI value without register forcing. The first natural probe kept explicit timer/main-VM pointer locals and emitted 284 bytes with swapped EBX/EDI lifetimes; removing those locals lets ordinary repeated member expressions CSE to target EDI=timer and EBX=mainVm. The remaining single comparable byte was the branch polarity of a semantically equivalent side check; writing the natural `if (sideIndex == 0) -176 else +176` order reproduces target `JNZ`.
- TH09 independently fixes the body: transition counter `+0x30334 = 60`, timer `+0x303C8`, main VM `+0xC0` scale/color, exact additive/normal blend leaves `0x0041BD40/0x0041BD60`, speed multipliers `+0x1CDC/+0x1CE0`, field `+0x3032C = 0`, side-dependent X `+/-176*value`, Y `64*value+384`, and exact update-state setter `0x00403C20` to state 3 at timer current >=30.
- Canonical comparison is 293/293 with 14 reviewed relocations, and the modified same-TU `Player::OnUpdate` remains 522/522 exact. No asm, padding, pragma/register forcing, fake ABI, or profile roulette is used.

## Packet 226 Player common-update exact closure

- `PlayerUpdateCommon @ 0x0041CC80-0x0041CDBF` is repository-canonical exact at 320 bytes. Exact `PlayerLifecycleView::OnUpdate` is the sole caller and emits a bare call while keeping Player in ESI; a same-TU internal-linkage `static void helper(PlayerLifecycleView*)` naturally reproduces that private transport with no source-visible register convention.
- TH09 fixes the state machine independently of TH08: `+0x30334` decrements when nonzero; update state 3 tracks an Effect pointer at `+0x303E0` whose position is `Effect +0x0C` and active byte `+0xC4`; timer `+0x303C8` decrements, drives an 8-frame red/white main-VM flash, then deactivates/clears the effect, sets update state 0 and resets the timer/color on expiry. Outside state 3 the timer increments. A second timer `+0x1B74` overlays a 4-frame red/white flash and resets to white when it reaches <=0.
- Natural source closure required only expression/control-flow facts. Whole-structure position assignment makes VC7.1 load the effect pointer once and emit the target 12-byte copy; spelling the first flash as `current % 8 < 2 ? red : white` yields the target short branchless setcc sequence, while spelling the second as ordinary `if (current % 4 < 2) red; else white` yields the target branch pair. Combined with cleanup-first timer control flow, the candidate closes from 328/339/325-byte probes to 320/320.
- All 12 relocations resolve to established TH09 Player/ZunTimer helpers, and both sibling `PlayerTransitionSpecial` and caller `Player::OnUpdate` remain canonical exact in the same fresh object. No asm, padding, register/volatile forcing, var-order pragma, target bytes, or profile roulette is used.

## Packet 227 Player collision-region creation family

- The complete authored `0x0041CF30-0x0041D140` collision-region creation family is now repository-canonical exact: allocator `0x0041CF30` (110 bytes) plus seven 43-61 byte geometry/type wrappers total 477 authored bytes. All eight first natural VC7.1 probes are structural-exact; no source-shape tuning was needed.
- TH09 fixes the allocator independently of adjacent games. Player collision update already proves the embedded 0x44 region at `+0xB0BC`, null-terminated/active pointer plane at `+0xB100`, released pointer plane at `+0xB908`, and counts `+0xC110/+0xC114`. `AllocateCollisionRegion` uses the embedded region while releasedCount<=1; otherwise it pops the released list, clears that entry, appends the region to the active list, calls exact `PlayerAddedCollisionRegionView::Reset @ 0x0041BDA0`, then writes active, center, lifetime and delay.
- The thin wrappers naturally reproduce the target field families: rectangle forms store half-extents at `+0x10/+0x14` using 0.5f; circle forms store radius/radiusGrowth at `+0x08/+0x0C`; value-bearing forms write `+0x28`; and the observed type tags are 0, 1, 2 and 4 at `+0x38`. TH08 was consulted only for the broad collision-region vocabulary; neutral TypeN names are retained because TH09 has not yet established every original Cancel/Damage spelling.
- The Type2 circle wrapper has a sole target caller in `Enemy_HandleDeathRewards` using the side-local Player; the Type1/0/4 wrappers are reached from independent Player/gameplay owners. Canonical relocation review closes the allocator->Reset edge, every wrapper->allocator edge and all 0.5f constants.

## Packet 228 Player tracking reset and forward-angle leaves

- `PlayerUpdateStageD @ 0x0041BA80-0x0041BAF8` is repository-canonical exact at 121 bytes. Exact Player::OnUpdate is the sole caller and reaches it through the already-established same-TU private ESI handoff. First natural source constructs two `Float3(-999,-999,0)` temporaries into Player `+0x30364/+0x30370`, clears `+0x30380`, and restores the established `+0x1CE8/+0x1CE4` multipliers to 1.0f; both constructor relocations resolve to exact Float3::Float3 and OnUpdate remains 522/522 exact.
- `PlayerForwardAngleView::AngleFromPlayerToPoint @ 0x0041BB00-0x0041BB64` is canonical exact at 101 bytes. TH09 callers pass a Player-layout receiver; one immediately reuses the same base at `+0x30364`. The helper obtains Player position `+0x1B88` through the shared 3-byte float-pointer identity view, computes target-player x/y deltas, returns pi/2 for zero delta, otherwise calls exact VectorAngle.
- This forward-angle body must remain distinct from canonical `PlayerAngleView::AngleToPoint @ 0x0041BB70`: the two target functions are the same size and share every relocation destination, but `0x0041BB00` computes target-player while `0x0041BB70` computes player-target. The separate neutral view/name preserves that target-local distinction rather than folding the two by family resemblance.

## Packet 229 Player enemy-damage source closure

- `PlayerBuildAabb @ 0x0041F2C0-0x0041F2F6` is repository-canonical exact at 55 bytes. Its two target callers are both inside the Player enemy-damage owner. A plain internal-linkage four-pointer source naturally makes VC7.1 select the target-private transport `ECX=center, EAX=size, ESI=topLeft, EDX=bottomRight` and reproduces all 39 ordinary bytes plus four 0.5f relocation fields. No register forcing or assembly is used.
- `PlayerDamagePlayerView::CalcDamageToEnemy @ 0x0041FCD0-0x004200B3` is now source-present under TH09-local Player ownership. Two maintained `EnemyManager::OnUpdate` call sites independently prove the normal thiscall receiver and five stack arguments `(position, hitbox, primaryAccumulator, bombHit, secondaryAccumulator)`. The first exact gate is timer303C8 current!=previous; the routine then builds target AABBs, scans 128 0x484 shots, and writes bullet damage to primaryAccumulator before scanning the null-terminated region pointer plane at `+0xB100`.
- TH09 shot layout is target-bound: hitbox `+0x430`, timer `+0x454`, damage `+0x460`, state/type `+0x462/+0x464`, animation index `+0x46C`, and a fastcall-style collision callback at `+0x47C`. Type 2 is evaluated every second timer frame; successful non-2/3 hits run the hit animation, set position.z to 0.1, switch state to 2 and scale velocity x/y by 1/8. Type 4 additionally spawns fixed-slot effect 34 and exact Type0 circle creation with radius 0, growth 1.6, value 6, lifetime 70 and delay 0, then sound 16.
- TH09 region damage also differs from the fixed-array TH08 family: active entries are pointers terminated by NULL, accepted type tags are 0/2/4, and delay plus lifetime%interval gate collision. Radius, axis-aligned and rotated-box tests use established `+0/+4/+8/+0x10/+0x14/+0x20` geometry; hits accumulate value `+0x28` into hitAccumulator `+0x2C`, clamp through hitCap `+0x30`, and type4 contributes its surviving value to secondaryAccumulator.
- Exact codegen is deliberately not claimed for CalcDamageToEnemy. First natural source emitted 971 bytes; restoring the TH08-family function-level four AABB locals and split state gates while retaining only TH09 semantics emits a repeatable 983-byte candidate versus the 996-byte target. The remaining 13-byte frontier is register/local allocation: target uses a 0x38 stack frame, keeps constant 2 in SI across the shot-hit path and spills savedRotation, while natural VC7.1 uses a 0x34 frame and keeps savedRotation in ESI. A target-motivated 16-bit hit-state local does not change codegen. Further work would require register-lifetime steering, var-order forcing or equivalent encoding-directed source, so this owner remains honestly NON-EXACT.

## Packet 230 ZunTimer HasTicked exact leaf

- `ZunTimer::HasTicked @ 0x0040F810-0x0040F820` is repository-canonical exact at 17 bytes. TH09 independently fixes the standard 0x0C timer layout with previous `+0x00`, subFrame `+0x04` and current `+0x08`; the body returns current != previous. Player CalcDamageToEnemy uses this entry as its per-tick damage gate.
- Clean committed TH08 corroborates the method name/source family only: its `ZunTimer::HasTicked()` body is the same comparison. The TH09 target bytes/layout establish ownership and exactness. First natural pinned-VC7.1 build is 17/17 with no relocations; the source lives in a dedicated TU to avoid perturbing already-canonical ZunTimer methods.

## Packet 231 ANM projection absolute-value leaf

- `AnmProjectionAbs @ 0x0040F1C0-0x0040F1CB` is repository-canonical exact at 12 bytes. Exact `AnmManager::Project3DQuad` already carries two reviewed relocations to this physical entry under the local symbol `AnmProjectionAbs`, so ANM ownership/call role is target-backed rather than inferred from adjacency.
- A plain dedicated-TU wrapper `long double __stdcall AnmProjectionAbs(float value) { return fabs(value); }` makes VC7.1 emit exactly the target x87 `fld / fabs / ret 4` sequence under the standard pinned profile, with no relocations and no asm/intrinsic forcing. Keeping it out-of-line also preserves the already-exact Project3DQuad call shape.
- The neighboring `0x00401060/0x00401070` entries are instead referenced by exact units as `@cosf@4/@sinf@4` and are not promoted here as authored ANM helpers; compiler/CRT ownership remains a separate question.

## Packet 232 Enemy ECL lookup and bullet-rank reset leaves

- `EnemyEclManagerView::GetSubroutineCount @ 0x0040F160-0x0040F166` and `GetSubroutine @ 0x0040F170-0x0040F17F` are repository-canonical exact at 7 and 16 bytes. TH09 `EnemyManagerView::AddedCallback` and reset logic call them on `primaryEclManager000` while building/consuming the 256-entry randomized subroutine schedule. The maintained `EnemyManager.hpp` already fixes `rawFile00`; target code reads signed short count at raw file `+0x06` and returns table entry `rawFile + 0x08 + index*4`.
- `EnemyView::ResetBulletRankInfluence @ 0x0040F180-0x0040F1B2` is canonical exact at 51 bytes. TH09 `EnemyManagerView::OnUpdate` calls it with an `EnemyView *` immediately before resetting death-callback call depth/threshold state; ECL state layout independently fixes the six rank-influence fields at `+0x2E38..+0x2E46`. The body stores `-0.5f/+0.5f` and four zero short bounds.
- Clean committed TH08 supplies only the `ResetBulletRankInfluence` family name after TH09 establishes the receiver, offsets, call point and values. All three first natural VC7.1 sources are exact with no relocations; no register forcing, asm, padding or profile search is used.

## Packet 233 ZunTimer integer subtract-assign leaf

- `ZunTimer::operator-=(int) @ 0x0041F300-0x0041F308` is repository-canonical exact at 9 bytes. Its sole TH09 caller in the Player heavy-state owner pushes either 5 or 10, keeps a `ZunTimer *` in ECX, and calls this entry; the body immediately tail-jumps exact `ZunTimer::Decrement(int) @ 0x00406620`.
- The natural source `void ZunTimer::operator-=(int value) { this->Decrement(value); }` reproduces the complete body with one reviewed REL32 relocation. A dedicated TU preserves the target-observed physical placement near Player code without claiming that placement changes logical ZunTimer ownership.

## Packet 234 Player resource subtraction clamp leaf

- `PlayerResourceSubtractView::SubtractResourceClamped @ 0x0041F310-0x0041F342` is repository-canonical exact at 51 bytes. Three TH09 Player heavy-state call sites pass 300.0f, 200.0f and 100.0f with the Player receiver, while maintained layout independently fixes the shared resource scalar at Player `+0x30388`.
- The natural source computes `value = resource30388 - amount`, clamps values below 1.0f to 1.0f, otherwise stores the subtraction result. First pinned VC7.1 compilation reproduces the complete x87 compare/store control flow and the sole 1.0f relocation; no source-shape tuning, asm, register forcing or profile search is required.
- The reconstruction name is deliberately neutral. Exact `AddRespawnResource` and several TH09 Player lifecycle/death consumers establish the same scalar, but no target-local evidence yet proves an original source-level method spelling for this subtraction helper.

## Packet 235 Player InitializeShot source closure

- `PlayerShotInitPlayerView::InitializeShot @ 0x0041F350-0x0041F484` is now source-present under Player ownership. Seven TH09 callers plus the full target body fix the fastcall shape `Player in ECX / shot in EDX / descriptor on stack`; TH09 independently fixes option stride 0x2F4, shot stride 0x484 and all descriptor/shot offsets used by the body. Clean TH08 contributes only the broad `InitializeShot` family name after those local facts are established.
- TH09 behavior is target-specific: source option index `+0x20` selects Player position or `optionStates[index-1].position`; descriptor offsets `+0x04/+0x0C/+0x14/+0x18/+0x1C/+0x22/+0x24/+0x26` feed position offset, hitbox, angle, speed, damage, shot type, animation and sound. The shot receives position `+0x2A4`, hitbox `+0x430`, velocity `+0x43C`, speed/angle `+0x44C/+0x450`, timer `+0x454=0`, fields `+0x470/+0x464/+0x460/+0x46C`, ANM script `animation+5`, and cleared byte `+0x46E`.
- Exactness is deliberately not claimed. The baseline `/Ob1` natural source emits 301 bytes because VC7.1 intrinsically lowers `cosf/sinf` to `fcos/fsin`; the single target-backed `/Ob0` hypothesis, already evidenced by exact `AnmManager::ExecuteScript` external trig lowering, emits the target size 309 and resolves all seven relocations to `Float3::operator float*`, `@cosf@4`, `@sinf@4`, timer assignment, sound, and ANM targets. Two cold tracked-source builds are identical and match 269/281 ordinary comparable bytes.
- The only remaining 12 ordinary bytes are local instruction scheduling: natural VC7.1 loads descriptor Y, stores independent position Z=0.495, then adds/stores Y; target completes the Y add/store before Z. Rewriting `+=` as explicit `offset + destination` leaves the same object. No volatile/register forcing, pragma ordering, asm, padding or further profile search is retained.

## Packet 236 Player shot frame-match wrapper

- `PlayerSpawnFrameView::SpawnShotWhenFrameMatches @ 0x0041F490-0x0041F4B4` is repository-canonical exact at 37 bytes. TH09 heavy-state code passes `ZunTimer::GetCurrent()` into the descriptor-chain dispatcher, whose default callback reaches this helper with Player in ECX, shot in EDX, and value/descriptor on the stack.
- The target semantics are deliberately kept TH09-local: the helper compares the supplied value directly with signed short descriptor `+0x00`, calls `InitializeShot @ 0x0041F350` only on equality, then returns 1; otherwise it returns 0. This differs from TH08's modulo/fire-frame helper, so the reconstruction keeps a neutral frame-match name rather than importing adjacent-game schedule semantics.
- First natural fastcall-member source reproduces all 37 bytes. The sole reviewed relocation resolves to maintained `InitializeShot`; exact caller status does not promote the non-exact callee.

## Packet 237 Player SpawnShots descriptor dispatcher

- `PlayerSpawnShotsView::SpawnShots @ 0x0041F4C0-0x0041F57D` is repository-canonical exact at 190 bytes. The reviewed IDA extent is 0xBE bytes; an earlier informal 188-byte mental estimate was discarded in favor of the tracked candidate boundary before promotion.
- TH09 heavy-state has two callers: each puts a `ZunTimer::GetCurrent()` result in EDX and power-level 0 or 1 on the stack. The helper selects `primaryShtFile + 0x42C` power-level descriptors, scans the 128 0x484-byte shot slots, and only processes inactive slots. Each 0x38-byte descriptor uses spawn callback `+0x28` when present, otherwise exact `SpawnShotWhenFrameMatches @ 0x0041F490`.
- On spawn result 1, TH09 sets shot VM flag `+0x1F8 |= 0x2000`, state `+0x462 = 1`, stores descriptor `+0x480`, and copies update/draw/collision callbacks from descriptor `+0x2C/+0x30/+0x34` into shot `+0x474/+0x478/+0x47C`. Descriptors advance by 0x38 until signed short `+0x00` becomes negative; result 0 retries the next descriptor on the same slot.
- First natural fastcall-member source reproduces the complete 190-byte body. The sole relocation resolves to the exact frame-match wrapper; no TH08 focus/power-selection behavior is imported into TH09.

## Packet 238 Enemy side-category query and Ascii boss-marker setter

- `EnemyManagerView::FindActiveEnemyBySideCategory @ 0x0040F7D0-0x0040F80D` is repository-canonical exact at 62 bytes. TH09 independently fixes the manager runtime array at `+0x5758` as 128 processed Enemy records of size `0x5430`, `flags337C bit0` as the active bit, and `(rewardFlags3380 >> 10) & 3` as the sideCategory already used by maintained EnemyManager::OnUpdate. The helper linearly returns the first active Enemy whose sideCategory equals the argument, otherwise null. Player reward/death code consumes it as a non-null mode/existence query but does not own it.
- `AsciiManager::SetBossMarkerState @ 0x0040F890-0x0040F8A3` is repository-canonical exact at 20 bytes. TH09 EnemyManager::OnUpdate loads `g_AsciiManager @ 0x004CE458`, computes a marker state from Enemy flags, pushes `bossSlot336B` and state, then calls this entry. The target writes exactly `this + 0x2254 + slot*4`; reconstruction exposes only that small boss-marker state plane through a private overlay rather than expanding the opaque AsciiManager region.
- Both leaves match on the first natural VC7.1 O2/Ob1 source shape with no relocations. Their callers come from different subsystems, so the ledger preserves EnemyManager ownership for the lookup and AsciiManager ownership for the marker setter instead of assigning ownership by caller location.

## Packet 239 Player shared transition and reward-state leaves

- `PlayerSharedTransitionView::GetTransitionBlockFlag @ 0x0040D4F0-0x0040D4F6` is repository-canonical exact at 7 bytes. Maintained `ApplyReward` and target `EnterDeathState` independently load the same global pointer `0x004A7E38` into ECX immediately before this call, then abort their transition/reward paths when the returned value is nonzero. The body simply returns the int at shared-runtime `+0x11EA8`; the neutral name records that observed role without claiming original spelling.
- `PlayerOwnerStateView::ApplyRewardWithStateAdvance @ 0x0040F8B0-0x0040F8BB` is canonical exact at 12 bytes. Two TH09 `Enemy_HandleDeathRewards` call sites pass Player owner-state `+0x30410` in ECX plus the same position/four-int stack ABI proven for `ApplyReward`. The wrapper increments `state38 @ +0x38` and naturally tail-jumps to `ApplyReward @ 0x0041D150`; the sole relocation solves to that target.
- The first probe used the previously exposed `activeFrameCounter3C @ +0x3C` and differed in exactly the ModRM displacement byte `0x3C vs 0x38`. The target therefore supplies positive layout evidence that `state38` and `activeFrameCounter3C` are distinct adjacent dwords. Splitting the former out of the public 0x44 owner-state view changes no size or later offsets and closes the wrapper without register/codegen steering.

## Packet 240 Player update-state setter exact leaf

- `PlayerState4OpsView::SetUpdateState @ 0x00403C20-0x00403C2B` is repository-canonical exact at 12 bytes. Multiple existing exact Player units already carry relocations to this address under the same neutral view/method name. Independent target callers outside Player source still pass a Player object: EnemyManager loads `sideState320->player04` into ECX, while the nearby reward/collision helpers use the same Player base before setting state 3.
- The natural source is only `updateState00 = state` on a one-field target-facing view and matches all 12 bytes on the first pinned VC7.1 O2/Ob1 build with no relocations. Keeping the implementation in a dedicated TU avoids unnecessary PlayerRuntime compiler-local label churn.
- This promotion does not alter the frozen ownership of `GetUpdateState @ 0x00435EC0`. That independent three-byte `mov eax,[ecx]; ret` body is already documented as shared/folded with many aliases and remains in the 35-entry origin-unknown set; exact setter ownership is not evidence for getter physical ownership.

## Packet 241 Pause menu GameManager flag and SoundPlayer resume leaves

- `AsciiGameManagerView::HasFlagBit0 @ 0x004343B0-0x004343B9` is repository-canonical exact at 10 bytes. PauseMenu::OnUpdate loads `g_GameManager @ 0x004A7D90` into ECX immediately before this call. Existing maintained `AsciiGameManagerView` fixes GameManager flags at `+0x134`, and the pre-existing source body returns `flags & 1`. The same AsciiManagerMenu TU under its established pinned profile reproduces the function exactly; this packet closes a source-present ledger omission rather than inventing new source.
- `AsciiSoundPlayerView::ResumeAfterPause @ 0x00423498-0x004234A6` is canonical exact at 15 bytes. PauseMenu closes by loading `g_SoundPlayer` as ECX and calling this wrapper. Natural source calls `QueueCommand(7, 0, unpause)` and reproduces the target push sequence; relocations resolve to string `0x0048F510` and exact `SoundPlayer::QueueCommand @ 0x0043EE40`.
- Ownership follows the receiver rather than the caller: the flag query remains GameManager-facing despite being used from the Ascii/Pause TU, while the resume wrapper is SoundPlayer-facing despite being called only by PauseMenu. Clean adjacent-game source provided no corresponding resume wrapper, so the maintained `AsciiSoundPlayerView` name remains a neutral TH09 reconstruction view.

## Packet 242 ZunMemory and GameErrorContext explicit constructors

- `ZunMemory::ZunMemory @ 0x0042B0D0-0x0042B0DC` is repository-canonical exact at 13 bytes. TH09 static initializer `0x0048D860` loads `g_ZunMemory @ 0x004AD018` into ECX and calls this body, then registers the already-exact destructor terminator. Existing ZunMemory layout fixes the 0x1000 registry-pointer plane followed by `bRegistryInUse @ +0x4000`; the constructor only clears that flag. This explicit policy initialization is why the body is authored rather than an implicit POD default constructor.
- `GameErrorContext::GameErrorContext @ 0x0042B110-0x0042B122` is canonical exact at 19 bytes. Static initializer `0x0048D8C0` loads `g_GameErrorContext @ 0x004B1020` into ECX before the call. Natural source sets `bufferEnd = buffer`, clears `buffer[0]`, and clears `showMessageBox +0x2004`. Existing exact Reset/Flush/Fatal paths independently prove `bufferEnd +0x2000` and `showMessageBox +0x2004`.
- Both first natural VC7.1 constructor bodies are zero-difference with no relocations. Constructor declarations are added to the existing target-backed class headers; implementation remains in dedicated TUs so established ZunMemory/GameError codegen is not reorganized.

## Packet 243 ReplayInputState explicit constructor

- `ReplayInputState::ReplayInputState @ 0x0042B010-0x0042B0C5` is repository-canonical exact at 182 bytes. TH09 static initializer `0x0048D880` invokes the VC7.1 vector-constructor helper with base `g_ReplayInputStates @ 0x004ACE18`, element size `0x8E`, count 3 and constructor callback `0x0042B010`. Existing replay/input exact units independently bind those same three 0x8E states.
- The constructor writes exactly 27 16-bit mapping values at `+0x58..+0x8C`. This closes the previous `unknown58[0x36]` span as a target-backed `short keyCodes58[27]` array without changing the 0x8E layout. Values are 0,1,2,4, four -1 sentinels, then the target key-code sequence ending in 0x1D.
- Signedness is codegen-proven rather than guessed. A first natural `u16` probe using 0xFFFF emits 184 bytes because VC7.1 loads `ECX=0x0000FFFF` with a five-byte MOV. Declaring the mapping as signed `short` and writing -1 lets the compiler use the target three-byte `or ecx,-1` while preserving the long-lived DX=1 constant; the result is 182/182 with no relocations.

## Packet 244 GameWindow constructor and AnmManager surface release

- `GameWindowView::GameWindowView @ 0x0042D290-0x0042D2A1` is repository-canonical exact at 18 bytes. TH09 static initializer `0x0048D8E0` loads the global object at `0x004B30B0` into ECX and tail-jumps to this entry. TH09 window creation and WinMain independently use the same object's first dword as the HWND and additional state inside the same 0x44-byte span. A plain `memset(this,0,sizeof(*this))` over an opaque 0x44-byte view reproduces all target bytes. Clean TH08 corroborates only the GameWindow family name, 0x44 size, and zeroing-constructor source shape; TH09 field names beyond the observed HWND/state uses are not imported.
- `AnmManager::ReleaseSurfaces @ 0x0042D2F0-0x0042D31A` is canonical exact at 43 bytes. WinMain loads `g_AnmManager @ 0x004DC550` into ECX immediately before this call when the D3D device reports not-reset; the other target caller is the device-reset helper and supplies the same manager receiver. Existing TH09 ANM reconstruction independently fixes 32 primary `IDirect3DSurface8*` slots at `+0x123FC`. Natural source releases each non-null COM surface and clears the slot.
- TH08's main loop independently uses the same broad `g_AnmManager->ReleaseSurfaces()` family at device reset, but TH09 alone decides receiver, 32-slot count, `+0x123FC` offset and exact codegen. Both TH09 first probes are zero-difference with no relocations or codegen steering.

## Packet 245 Score global constructors and PbgArchive destructor

- `ScoreFileView::ScoreFileView @ 0x00421C40-0x00421C57` is repository-canonical exact at 24 bytes. TH09 static initializer `0x0048D7F0` loads `g_ScoreFileHeader @ 0x004AC840` into ECX and tail-jumps to this entry. Existing target-backed `ScoreFileView` is exactly 0x18 bytes, and a plain full-object zeroing constructor reproduces the six target dword stores.
- `ScoreRecordCtorView::ScoreRecordCtorView @ 0x00421C60-0x00421C71` is canonical exact at 18 bytes. Static initializer `0x0048D800` vector-constructs `0x190` records of size `0x2C` at `g_TitleScoreTable @ 0x004A8380` using this callback, while `0x0048D820` constructs the singleton at `0x004AC858`. The maintained neutral type name avoids choosing between the existing TitleScoreRecordView and SetupScoreRecordView aliases for the same physical record.
- `PbgArchive::~PbgArchive @ 0x004335D0-0x004335D4` is canonical exact at 5 bytes. TH09 global-init/atexit wiring binds the terminator to `g_PbgArchive @ 0x004AD008`. Natural destructor source containing only `Release()` emits the exact tail jump to canonical `PbgArchive::Release @ 0x004333B0`; no additional cleanup or source shaping is present.

## Packet 246 Supervisor netplay queue synchronization leaf

- `SupervisorFrameQueueView::AreFrameQueuesSynchronized @ 0x0042ECF0-0x0042ED20` is repository-canonical exact at 49 bytes. Fresh TH09 call-site review proves the physical receiver is the Supervisor object, not the separate `g_SupervisorNetworkState` DirectPlay/session pointer: `SupervisorServiceUpdate` preserves its callback argument in ESI and passes ESI to all four frame-queue helpers, while `ResetTitleMode4Supervisor` passes `g_Supervisor` to insertion. The two 0x78-stride queue blocks begin at Supervisor `+0x47C`, with head frame dwords at `+0x480/+0x4F8`; the requested side must have nonzero head frame and the two head frame values must agree.
- Exact source shape is also bounded compiler evidence. Writing the same semantics as two sequential failure checks lets VC7.1 collapse the second comparison to a 47-byte `sub/neg/sbb/inc` boolean tail. The ordinary short-circuit conjunction `queue[side].frame != 0 && queue[1].frame == queue[0].frame` naturally preserves the target `cmp/jne` and explicit `mov eax,1` path, yielding 49/49 with no relocations and no volatile/register forcing.
- The repository initially attached these queue APIs to `SupervisorNetworkState`; direct target ECX evidence supersedes that reconstruction seam. Maintained `SupervisorNetwork.cpp` now uses a neutral `SupervisorFrameQueueView` over its Supervisor callback receiver and moves the full natural service candidate from 1,639 to a repeatable 1,625 bytes versus the 1,633-byte target. The adjacent InsertReceivedFrame/InsertPredictedFrame/PopFrame helpers remain unpromoted until their denser record/last-input source shape is modeled coherently.

## Packet 247 Bullet spawn descriptor constructor leaf

- `BulletSpawnDescriptorCtorView::BulletSpawnDescriptorCtorView @ 0x0040D500-0x0040D51B` is repository-canonical exact at 28 bytes. TH09 independently fixes the physical descriptor size as `0x214` and `transformSound @ +0x204`. EnemyManager constructs a 0x214-byte stack temporary before bullet dispatch, while ECL/Enemy initialization invokes the same body on persistent descriptors at Enemy `+0x2E74/+0x30C4`.
- The target clears the complete 0x214-byte object, writes `transformSound=-1`, then returns `this` in EAX, matching natural constructor lowering. A one-purpose opaque view with only the target-proven size and `+0x204` field reproduces all 28 bytes on the first probe with no relocations.
- The maintained `BulletSpawnDescriptor` class is intentionally not given this constructor yet: its reconstruction currently embeds a `Float3` type with an explicit three-argument constructor, so declaring an ordinary C++ default constructor there would introduce subobject-construction requirements absent from the target. The neutral ctor view preserves exact physical evidence without inventing the original TH09 subobject type model. Clean TH08 corroborates only the broad zeroing-plus-transformSound-sentinel constructor family.

## Packet 248 Supervisor mode-4 network reset exact helper

- `ResetTitleMode4Supervisor @ 0x0042EC30-0x0042ECE0` is repository-canonical exact at 177 bytes. Exact `TitleScreenView::OnUpdateModeSelect` is the sole caller and already binds the fastcall symbol with `g_TitleSupervisor/g_Supervisor @ 0x004B3100`. Fresh queue-owner review proves the two frame queues belong to the Supervisor receiver rather than the separate DirectPlay/session object `g_SupervisorNetworkState`.
- TH09 fixes the full reset protocol: clear Supervisor `+0x458/+0x45C/+0x460`; snapshot `timeGetTime` into `+0x464/+0x468/+0x474`; clear `+0x478`; zero exactly 0xF0 queue bytes at `+0x47C` and 0x18 last-received bytes at `+0x56C`; seed and insert sentinel frame -9999 for sides 0 and 1 through `SupervisorFrameQueueView::InsertReceivedFrame`; mark the separate network state active; and clear its `connectionState +0xB8`. Twelve relocation fields solve to already reviewed TH09 globals/imports/helpers.
- Compiler shape is source-meaningful rather than register-directed. A first natural version with six explicit tail dword assignments emitted 189 bytes because VC7.1 retained the original Supervisor base and used six long `+0x56C..+0x580` displacements. Expressing the two POD regions as two fixed-size memset operations makes ordinary O2 codegen naturally emit the target `lea edi,+0x47C; add esi,+0x56C; rep stosd` followed by six short ESI-based stores, closing 177/177 without volatile, asm, pragma or register forcing.

## Packet 249 ExAttack draw callbacks

- `ExAttackController::OnDraw0 @ 0x00415460-0x0041557D` and `OnDraw1 @ 0x00415580-0x00415640` are repository-canonical exact at 286 and 193 bytes. Exact `ExAttackController::Register` already installs them as draw callbacks at priorities 16 and 29, fixing owner and fastcall callback ABI independently of adjacency.
- TH09 draw bodies refine the 0x4C record/controller tail without importing adjacent-game layout: record `dynamicData +0x1C` points at 0x2A4-stride AnmVm storage, `vmCount +0x2C` controls iteration, `next +0x30` links draw records, `rotation +0x3C` feeds exact AnmVm::SetZRotation, and `drawCallback +0x44` runs after the VM loop. Controller draw-list heads are two side-specific pointers at `+0x4C68/+0x4C6C` plus a separate list at `+0x4C70`.
- Both callbacks use target-backed AnmVm fields: word `+0x1FC` gates rotation, position `+0x208/+0x20C/+0x210` is written from local offsets `+0x288/+0x28C` plus record position. Draw0 configures viewports 0/1 and applies exact GameManager X/Y transforms; Draw1 configures viewport 2 and keeps local coordinates. Both write z=0.06 and call exact AnmManager::Draw2D.
- Natural exact closure required no codegen forcing. The initial long-lived VM-pointer source emitted 266/181 bytes; expressing the loop in the target-evidenced VM byte-offset form closes Draw1 to 193/193 and Draw0 to target size. Draw0's final 13 ordinary bytes were only declaration order: initializing side before deriving the list-head pointer reproduces the target prologue/local slots and closes 286/286. No var_order, volatile, register forcing, asm, or profile roulette is retained.

## Packet 250 ExAttack update callback

- `ExAttackController::OnUpdate @ 0x00415340-0x00415456` is repository-canonical exact at 279 bytes. Exact `ExAttackController::Register` installs it as the calc callback, fixing owner and fastcall ABI. The callback is gated by TH09 GameManager flags `+0x134 & 0x1800` and side0 flags `+0x34 bit0`.
- TH09 target code completes the controller tail semantics established by the draw callbacks: three heads at `+0x4C68..+0x4C70`, three tails at `+0x4C74..+0x4C7C`, and two active side counts at `+0x0C/+0x10`. It scans exactly 256 0x4C records from `+0x1C`. Active records call updateCallback `+0x40`; completed records run optional releaseCallback `+0x48`, free dynamicData `+0x1C` through exact ZunMemory::Free, then clear active. Surviving records execute every 0x2A4 VM through exact AnmManager::ExecuteScript, append to the draw list selected by record `+0x00`, increment the side count selected by `+0x08`, clear next `+0x30`, and postfix-increment timer `+0x10`.
- Natural exact closure required only target-observed source lifetime/order. Moving the record pointer construction before the gate, spelling the six list resets in target tail-then-head order, clearing side counts in target order, and rereading the record list index instead of caching it moves the first 282-byte candidate to 279/279. No var_order, volatile, register forcing, assembly, padding, or profile roulette is retained.

## Packet 251 Supervisor received-frame queue source closure

- `SupervisorFrameQueueView::InsertReceivedFrame @ 0x0042E9E0-0x0042EAB4` is now source-present under the corrected Supervisor frame-queue owner. Exact `ResetTitleMode4Supervisor` and maintained `SupervisorServiceUpdate` both pass the Supervisor receiver; the separate DirectPlay/session object remains `g_SupervisorNetworkState`. The API arguments are target-proven as side, frame, packedInput and seed.
- TH09 fixes the storage geometry: two 10-entry queues begin at Supervisor `+0x47C`, each entry is 0x0C bytes `{short packedInput, short seed, int frame, int value}`, so the second queue begins at +0x4F4 and their head frame fields are the already-exact sync offsets +0x480/+0x4F8. Two same-shape last-received records occupy `+0x56C..+0x583`. The helper stores latest metadata, finds the ascending-frame insertion point, shifts later entries toward slot 9, writes the new entry and clears value08.
- Exactness is deliberately not claimed. Natural target-backed probes emitted 195, 227, and finally a stable 211 bytes versus the 213-byte target. The 211-byte form reproduces the target for-loop scan, side-base lifetime, flat queue indexing and field-wise 0x0C shift. The remaining two-byte frontier is register/temporary-slot allocation: the target retains side-base in ESI and reuses the dead side argument stack slot, while natural VC7.1 chooses alternate registers. No var_order, volatile, register forcing, assembly, padding or profile fishing is retained.

## Packet 252 Supervisor predicted/pop queue source closure

- `SupervisorFrameQueueView::InsertPredictedFrame @ 0x0042EAC0-0x0042EB7E` and `PopFrame @ 0x0042EB80-0x0042EBF6` are now source-present under the same corrected Supervisor queue owner as InsertReceivedFrame. All three share the TH09-proven two-side 10x0x0C queue layout at +0x47C and last-received records at +0x56C.
- InsertPredictedFrame performs the same ascending-frame insertion/field-wise shift as received insertion, copies packedInput from lastReceived[side], stores the supplied frame/seed, and sets predicted08=1. Two cold natural builds emit 176 bytes versus the 191-byte target. Target lifetime keeps side in EBX, reuses the dead side argument slot for side-base, and spills targetFrame while EDI becomes the shift counter; an ordinary sideIndex lifetime probe leaves codegen unchanged, so no register steering is retained.
- PopFrame has stronger type evidence: both target branches write AX only, and all maintained callers consume the result as u16. The shared declaration is therefore refined from int to `unsigned short`. Natural side-base source is target-sized 119/119 and solves the sole g_ReplayInputStates relocation; it returns head packedInput for received entries or ReplayInputState.currentInput for predicted entries, outputs seed/predicted, then shifts nine records left. Ten ordinary-byte differences remain solely from ECX/EDX side-base register allocation; direct array spelling grows to 123 bytes. Exactness is deliberately not claimed.

## Packet 253 Enemy fixed ExAttack spawn leaves

- Six Enemy-owned fixed-type ExAttack wrappers are repository-canonical exact at 41 bytes each: `SpawnExAttackType1 @ 0x0040C3C0`, Type7 `0x0040C440`, Type8 `0x0040C470`, Type9 `0x0040C4A0`, Type23 `0x0040C4D0`, and Type24 `0x0040C500`. TH09 target layout independently fixes Enemy `manager00 @ +0x00`, `position2D74`, and EnemyManager `sideIndex31C`.
- Every wrapper has the same natural body: call the global ExAttack controller at `0x004A7E3C` through maintained `ExAttackController::Spawn` with a fixed type, Enemy position, `1 - manager00->sideIndex31C`, and parameter 0. The first natural O2/Ob1 probe for all six is 41/41 structural exact; each has only the global-controller DIR32 relocation and Spawn REL32 relocation, both solved to TH09 targets.
- The `TypeN` names are intentionally behavioral/reconstruction-neutral. The six physical functions are clearly Enemy helpers, but original script-facing or character-specific source spelling is not claimed. No adjacent-game layout, profile search, register forcing, pragma, volatile, padding, or assembly is used.

## Packet 254 Bullet transform reset and ECL opcode1 exact leaves

- `BulletTransformResetView::ResetTransformRuntime @ 0x0040C390-0x0040C3B8` is repository-canonical exact at 41 bytes. TH09 helper `0x0040C530` calls it on live 0x10C4 Bullet records immediately before exact transform-install helpers. Existing Bullet layout fixes activeTransformFlags `+0xDB4`, transformFlags `+0xDB8`, transformIndex `+0xDD4` and eighteen 0x18-byte transform records from `+0xDD8`. Natural source clears the three runtime fields and every record kind; first pinned O2/Ob1 replay is exact with no relocations.
- `EnemyEclOpcode1 @ 0x0040C3F0-0x0040C43B` is repository-canonical exact at 76 bytes. TH09 RunEcl dispatch table `0x004A0B14` index 1 enters it with Enemy in ECX and the current ECL instruction in EDX. The handler reads instruction `+0x10`, assigns bit0 of both side-state flag dwords at `+0x34`, and plays sound 0x34 when the assigned value is nonzero.
- The exact VC7.1 XOR/AND lowering in opcode1 positively supports the side-state flag as an assignable one-bit bitfield rather than a whole-dword overwrite. The maintained `EnemyEclOpcode1` name is dispatch-index neutral; no original mnemonic is claimed. Both leaves close from TH09-local owner/layout evidence without register forcing, padding, asm or profile search.

## Packet 255 Additional Enemy ExAttack spawn wrappers

- Four more TH09 Enemy ExAttack leaves are repository-canonical exact in a dedicated target-facing TU: `SpawnExAttackType10 @ 0x0040C770` is 70 bytes, `Type11 @ 0x0040C7C0` is 69 bytes, `Type12 @ 0x0040C810` is 69 bytes, and `SpawnExAttackType22Burst @ 0x0040C860` is 226 bytes. Exact `ExAttackController::Spawn @ 0x004151F0` target identity and `g_ExAttackController @ 0x004A7E3C` are shared with the earlier fixed-type wrapper packet.
- Types 10/11/12 use Enemy `activeContext2CE0` directly. TH09 fixes the local three-float position from context `+0x6C/+0x70`; type 10 sets z=0 while types 11/12 copy context `+0x3C`. All derive spawn side as `1-manager00->sideIndex31C`. The context values remain private overlay fields so this packet does not over-name the public 0x22C ECL context.
- Type22 burst emits three calls. For each angle, target helper `0x00441890` creates a radius-64 XY vector, shared target `0x00405730` adds Enemy `position2D74`, local z stores the angle, and the local address is passed as Spawn parameter while Spawn position remains the Enemy position. Target angles are `0x3FC90FDB` (pi/2), `0x3FA0D97C` (0.4*pi) and `0x3FF1463A` (0.6*pi).
- First probes for the fixed wrappers were eight bytes short only because an analysis-only side helper was emitted out of line under /Ob1; spelling the side expression directly yields exact code. The first Type22 probe similarly collapsed to calls to an analysis-only helper; writing the three ordinary source blocks directly reproduces all 226 bytes. No force-inline pragma, register steering or profile search is retained.

## Packet 256 Effect bullet-layer draw exact leaf and polar-helper negative

- `BulletDrawEffectManagerView::DrawBulletLayerEffects @ 0x0040D110-0x0040D1B8` is repository-canonical exact at 169 bytes. Maintained `EtamaController::OnDraw` is the sole caller and passes `controller->sideState +0x0C`, independently binding an EffectManager receiver. The existing EffectManager layout fixes `drawSentinel1.nextInDrawGroup @ +0x1E8` plus Effect VM/count/position/draw-callback/next fields. The leaf walks that bullet-layer draw list, invokes per-effect draw callbacks when present, otherwise transforms Effect x/y through GameManager, writes VM z=0.08, adds vm pos2 and calls exact AnmManager::Draw2D.
- Natural source reaches exact code without ABI steering. The first same-size 169-byte probe spilled `vmCount` before the target's positive-count branch. Keeping a read-only count for the gate and creating a branch-local `remaining` counter reproduces the target delayed stack spill and closes all 141 ordinary bytes plus seven relocation fields. The result remains exact under the established EffectManager no-EH O2/Ob1 profile.
- Nearby `EffectPolarScaleView::SetFromAngleAxes @ 0x0040D4D0-0x0040D4EF` remains deliberately source-absent. Its two TH09 xrefs are inside EffectManager::OnUpdate and establish semantics `x=cos(angle)*xScale; y=sin(angle)*yScale`. Natural pinned VC7.1 `cosf/sinf` source emits 30 bytes with separate FCOS, angle reload and FSIN; target is 32 bytes with one FSINCOS and a spilled this pointer. No asm, volatile/register forcing, pragma or profile roulette is retained; reopen only with genuinely new source/TU evidence.

## Packet 257 AnmManager capture-request leaf

- `AnmCaptureRequestView::QueueCaptureRequest @ 0x0042F420-0x0042F484` is repository-canonical exact at 101 bytes. Its sole TH09 caller `0x004303C0` loads `g_AnmManager @ 0x004DC550` into ECX and passes a capture index followed by eight rectangle integers. Existing exact AnmManager constructor source independently initializes `pendingCaptureIndex @ +0x08` to -1.
- The request gate is target-local: if `pendingCaptureIndex08 >= 0` the helper returns -1 without changing state. Otherwise it stores the new capture index at `+0x08` and `srcX/srcY/srcW/srcH/dstX/dstY/dstW/dstH` consecutively at `+0x2B2914..+0x2B2930`, then returns zero. Natural pinned VC7.1 O2/Ob1 source reproduces all 101 bytes with no relocations.
- The maintained name is deliberately behavioral rather than original-spelling evidence. The caller currently requests `(8,0,0,640,480,0,0,640,480)`, but the helper itself remains generic and owns only request publication, not the later capture execution path.

## Packet 258 Supervisor loading setup plus capture

- `SupervisorSetupLayout::SetupLoadingVmsAndInitCapture @ 0x004303C0-0x00430495` is repository-canonical exact at 214 bytes. Exact GameManager::AddedCallback contains two calls and exact TitleScreen::ActualAddedCallback contains a third; all target the same physical function on the Supervisor object even though the caller TUs use different target-facing view names.
- TH09 independently fixes `loadingAnm +0x5D0`, `loadingVmsHaveBeenSetup +0x740`, and three contiguous 0x2A4 loading VMs at `0x004B38B0`. When setup is false, the method executes scripts 0/1/2 through exact AnmLoaded::ExecuteAnmIdx, sets the flag, then copies the caller 0x0C Float3 into each VM primary position at +0x208. It then unconditionally calls exact `AnmCaptureRequestView::QueueCaptureRequest` with `(8,0,0,640,480,0,0,640,480)`.
- Natural void-shaped source reproduces all 214 bytes and all 17 relocation fields. The trailing capture call's EAX is intentionally ignored by source callers; target bytes simply leave that value live on return, so no integer return contract is inferred. The GameManager-side `SupervisorSetupLayout` spelling is retained for the physical implementation while Title's `TitleSupervisorView` remains a caller-side alias pending later product-level type unification.

## Packet 259 Supervisor loading-state transitions

- Two 85-byte Supervisor loading-state leaves are repository-canonical exact. `TitleSupervisorView::HideLoadingVms @ 0x00430360` is called only by TH09 `TitleScreen::TitleSetupThread` on the Supervisor object. When loading state `+0x740 == 1`, it sends interrupt 1 to the three 0x2A4 loading VMs at `0x004B38B0` and clears the state to 0; it then fade-releases and clears `g_SupervisorScreenEffect @ 0x004B30F8`.
- `FinishLoading @ 0x004304A0` is the gameplay-thread counterpart. Two TH09 `GameplaySetupThread` call sites pass `&g_Supervisor` in ECX. When loading state is 1 it sends interrupt 2 to all three loading VMs and sets `+0x740 = 2`, then performs the same exact ScreenEffect::BeginFadeRelease/clear tail. Natural `void __fastcall FinishLoading(Supervisor*)` reproduces all 85 bytes and nine relocations.
- Both pre-existing caller declarations incorrectly used integer return types even though the callers ignore EAX and the target leaves do not synthesize a return value. This packet corrects both declarations to `void`; the machine call shape is unchanged. Exact closure uses only TH09-established loading state, exact AnmVm::SetInterrupt, exact ScreenEffect::BeginFadeRelease and the screen-effect global; no adjacent-game layout is imported.

## Packet 260 Supervisor critical-section lifecycle leaves

- `SupervisorCriticalSectionArrayView::InitializeCriticalSections @ 0x00430530-0x00430550` and `DeleteCriticalSections @ 0x00430560-0x00430580` are repository-canonical exact at 33 bytes each. TH09 WinMain loads `g_Supervisor @ 0x004B3100` into ECX immediately before both calls.
- Existing exact `Supervisor::EnterCriticalSectionWrapper/LeaveCriticalSectionWrapper` independently prove exactly five `CRITICAL_SECTION` objects at Supervisor `+0x6C0` with 0x18-byte stride and lock-count bytes starting at +0x738. Direct PE import-table parsing resolves target IAT `0x0048E0B8` to KERNEL32!InitializeCriticalSection and `0x0048E0BC` to KERNEL32!DeleteCriticalSection.
- Plain five-element loops over a private target-facing lock view reproduce both target bodies on the first pinned VC7.1 O2/Ob1 build, including the single IAT relocation in each. No new Supervisor fields outside the already-proven lock region are exposed and no compiler steering is used.

## Packet 261 Shared Float3 add exact leaf and FSINCOS negative

- `Float3::operator+= @ 0x00405730-0x00405753` is repository-canonical exact at 36 bytes. TH09 has a broad caller set across ECL, EffectManager, Bullet, Player, Supervisor loading draw and the newly exact Enemy ExAttack Type22 burst; existing exact caller manifests already relocate the same physical address under compatible target-facing Float3 aliases. Plain maintained `x/y/z += other` source in the existing `src/Float3.cpp` TU reproduces all 36 bytes with no relocations.
- Adding the real `Float3 &Float3::operator+=(const Float3&)` definition leaves all seven pre-existing canonical Float3 functions exact; the TU now has eight exact units. This is a shared authored math leaf rather than an SDK inline: it uses the game's member ABI and is independently consumed by numerous game-owned systems.
- Packet 380 supersedes the earlier source-absent status for `Float3::FromAngleMagnitude @ 0x00441890-0x004418AF`. TH09 target still uses one x87 FSINCOS and the natural maintained `cosf/sinf` body still emits 30 bytes with separate FCOS, angle reload and FSIN versus the 32-byte target, so the function remains NON-EXACT. New TH09-local same-TU evidence from exact type-8 initialization now requires this natural definition to be visible to the caller; source presence is therefore accepted without importing TH095's inline-x87-asm workaround or granting exactness.

## Packet 262 Effect player-follow update callback

- `EffectFollowPlayerUpdate @ 0x0040C960-0x0040C985` is repository-canonical exact at 38 bytes. TH09 `EffectTemplate` records begin at `0x004A0B48` with 0x18-byte stride; raw target data places this address in effectId 7's updateCallback slot at `0x004A0C00`. The same record has scriptIndex 0x3F and vmCount 1.
- Exact/local layouts independently fix `Effect::manager +0x00`, `EffectManager::sideState +0x10`, side-state Player pointer `+0x04`, Player position `+0x1B88` and Effect position `+0x0C`. The callback simply copies the owning side Player position into the Effect position and returns 1. Plain fastcall source reproduces all 38 bytes with no relocations.
- The maintained name is behavioral rather than original-spelling evidence. Ownership is nevertheless target-local and direct: InitializeEffect consumes the table's +0x10 updateCallback slot for each spawned effect, so this function is not inferred merely from adjacency.

## Packet 263 GameManager reset helper

- `ResetGameManager @ 0x0041A7C3-0x0041A826` is repository-canonical exact at 100 bytes. TH09 maintained `GameplaySetupThread` calls it at setup entry and `GameManagerSetupLayout::CleanupGameplayState` calls it during teardown, both on `g_GameManager`. The existing source-level helper name/fastcall ABI therefore come from local caller evidence rather than address proximity.
- The target begins with a three-iteration 0x38-stride loop clearing bit0 at manager offsets `+0x34/+0x6C/+0xA4`. This is stronger than the provisional two-side setup view: reset semantics prove three same-stride records for this flag plane. A private reset overlay preserves that fact without rewriting unrelated wider GameManager fields. The helper then writes target constants/reset values at +0xB0, +0xB8, +0xBC, +0xC0, +0xD0, +0x110, +0x128/+0x12C/+0x130, bytes +0x13C/+0x13D/+0x13E, and +0x380.
- Natural source is exact under the already-established `GameManagerSetup.cpp` canonical `/O2 /Os /Ob1` profile. Ordinary O2 emits 107 bytes; /Os is not profile roulette here, because all pre-existing canonical GameManagerSetup units already use that TU-local profile. No register/volatile forcing, padding, assembly or fake return is used.
- Adjacent `0x0041A796` remains independently source-absent. Although the large setup owner currently uses a provisional `ResetSetupBuffer` declaration for that call site, its target body invokes three CRT/runtime helpers and a string literal, so this packet does not promote or validate that alias.

## Packet 264 GameManager chain cut helper

- `GameManager_CutChain @ 0x0041A827-0x0041A888` is repository-canonical exact at 98 bytes. Maintained `Supervisor::OnUpdate` has six state-transition call sites to this existing helper alias. Target relocations independently bind side-state pointers through `g_GameManager +0x1C/+0x54`, Supervisor frame-rate multiplier `+0x5B8`, `g_GameManagerCalcChain @ 0x004A8120`, `g_GameManagerDrawChain @ 0x004A8140` and exact `Chain::Cut`.
- Before cutting the chains, the helper normalizes each side's setup state: if `value08 >= 1000000000` it clamps to 999999999, then copies value08 into value04. It restores Supervisor `frameRateMultiplier` to 1.0f and cuts calc then draw. These fields are already exercised by maintained GameManager setup/update source, so no new broad layout is invented.
- A first natural loop over two sides compiles to 80 bytes under the established GameManagerSetup /Os profile. Spelling the target-observed side0 and side1 operations as two ordinary explicit source blocks naturally reproduces the target's absolute reload/alias behavior and closes 98/98 with ten relocations. This is source-shape evidence, not register forcing or manual padding.

## Packet 265 Replay date formatter

- `FormatCurrentDateString @ 0x0041A796-0x0041A7C2` is repository-canonical exact at 45 bytes. TH09 GameplaySetupThread passes `g_ReplayPlayTimeText @ 0x004AC879` in ECX; independent ReplayInput and Title replay-save source consume the same global as the persisted/displayed replay play-time text.
- Target code creates a local `time_t`, calls pinned CRT `_time` and `_localtime`, then calls `_strftime(buffer, 10, "%y/%m/%d", ...)`. Natural source under the established GameManagerSetup `/O2 /Os /Ob1` profile reproduces all 45 bytes with four reviewed relocations to the three CRT functions and format literal.
- This packet removes the earlier provisional `ResetSetupBuffer` / `g_SetupBuffer` aliases from maintained GameManager setup source. Ordinary O2 emits 47 bytes; /Os is the already-pinned TU profile rather than a searched-for workaround.

## Packet 266 GameManager playfield frontier

- `GameManagerPlayfieldView::IsWithinPlayfield @ 0x0041A6EB-0x0041A75E` is now owner/ABI/semantics-resolved but deliberately remains source-absent. TH09 callers in EnemyManager, Bullet update, Etama update and Player shots all load `g_GameManager @ 0x004A7D90` into ECX before four float stack arguments. The body does not read this but returns with `ret 0x10`, consistent with the member ABI. Clean TH095 only corroborates the IsWithinPlayfield name family after TH09 closure.
- Target semantics are rectangle-vs-playfield intersection: half-width and half-height are derived with 0.5f; accepted bounds are x `[-144,144]` and y `[0,448]`. Natural source under the established GameManager `/O2 /Os /Ob1` profile emits 110-byte candidates for ordinary nested/boolean forms; a comparison-orientation variant emits 114 bytes; target is 116. Plain O2 is also 114.
- Remaining differences are x87 status-mask/pop control-flow choices, not unresolved gameplay semantics. No padding, asm, register/volatile forcing, pragma tricks or profile roulette are retained. Reopen only with new natural source/TU evidence.

## Packet 267 FrontSide auxiliary transition leaf

- `FrontSideAuxTransitionView::BeginAuxTransition @ 0x00415D50-0x00415D67` is repository-canonical exact at 24 bytes. TH09 GameplaySetupThread is the sole target caller of exact `FrontSide::Create @ 0x0041A5D0` and stores each return value into the corresponding 0x38 GameManager side record at `+0x18`. The two target callers of `0x00415D50` load exactly those side `+0x18` pointers into ECX, closing the owner independently of address proximity.
- Existing FrontSide layout proves auxiliary state `FrontSide +0xABCC` as `auxA678 +0x554` and `transitionTimer +0xABD0`. The leaf writes state 1 and assigns 0 to the timer through exact `ZunTimer::operator=(int) @ 0x00401500`. Plain O2/Ob1 member source in a private target-facing view reproduces all 24 bytes with the single timer relocation.
- The private `BeginAuxTransition` name describes behavior only; original spelling is not claimed. This evidence also proves the provisional `GameManagerSetup.cpp` side `subsystem6` slot is a FrontSide pointer. The large GameplaySetup source is deliberately left unchanged in this packet so its independent non-exact plateau is not mixed into the exact-leaf checkpoint.

## Packet 268 Front message-runtime reset

- `FrontMessageRuntimeResetView::Reset @ 0x00415C90-0x00415CA6` is repository-canonical exact at 23 bytes. TH09 `FrontInf` factory `0x00418840` allocates/registers the 0x1293C Front owner and installs `0x00418480` as its calc-chain added callback. That callback resets the embedded subobject at `Front +0xE944` through this entry immediately before loading the character-specific `plNN.msg` / `plNN_match.msg` script tables.
- The same subobject reset is independently reused by the message-table load/release path around `0x00417520/0x004182E0`. Target body is minimal: clear exactly 0x1D70 bytes, set dword `+0x08 = -1` and return zero. Plain pinned VC7.1 O2/Ob1 source reproduces all 23 bytes with no relocations.
- The private maintained name records only the target-proven embedded message-runtime role. It does not claim an original class/type spelling for the 0x1D70 subobject, nor does it transfer the neighboring Front owner layout beyond the already established `FrontInf` allocation and `+0xE944` embedding point.

## Packet 269 Enemy ECL opcode2 bullet retarget handler

- `EnemyEclOpcode2 @ 0x0040C530-0x0040C76D` is repository-canonical exact at 574 bytes. TH09 RunEcl's dispatch table at `0x004A0B14` points index 2 to this body. The dispatcher supplies Enemy in ECX and the current instruction in EDX; opcode2 does not read instruction payload and instead operates entirely on Enemy/manager runtime state.
- Enemy `manager00` selects side/opposing side states at manager `+0x320/+0x324`. Their target-backed `+0x08` pointers lead to EtamaController instances whose live Bullet pool begins at `+0x1A900`; both loops process exactly 536 0x10C4 Bullet records. Active state values other than 6 are retargeted. The first side refreshes the Bullet VM sprite when `spriteSize.y <= 16`; the opposing side adds a signed `BulletTypeSprites +0xD44 < 343` guard.
- For every selected bullet the handler zeros speed `+0xD70`, scratch dwords `+0xD74/+0xD78/+0xD80/+0xD84`, and velocity `+0xD58..+0xD60`; angle `+0xD7C` receives shared RNG signed range pi. It then calls exact `ResetTransformRuntime`. The common transform program is wait slot0/duration60 then vector-acceleration slot1/duration60 with magnitude from Enemy active-context pointer `+0x2CE0 -> +0x3C` and angle sentinel -999.0f.
- On the opposing pool, even indices whose VM `spriteSize.y == 16` instead set bulletType `+0x10C0 = 0` and install sprite slot0 (sprite 0,color15), wait slot1 and acceleration slot2. After both pools, sound 52 is played. Exact closure required only one target-backed signedness correction: the first target-sized candidate differed in one comparable byte (JAE versus JGE) until `BulletTypeSprites +0xD44` was compared as signed int. No register/pragma forcing, padding or assembly is used.

## Packet 270 ECL attached-effect release helper

- `Th09EclRunState::ReleaseAttachedEffects @ 0x0040F560-0x0040F59F` is repository-canonical exact at 64 bytes. Maintained `EclRunState.inl` already declared this helper and calls it from boss teardown; the same target-backed Enemy state overlay fixes `attachedEffects53B4[24]` and `attachedEffectCount5414`.
- The helper scans indices `0..count-1`. Every non-null Effect receives byte value 1 at `Effect +0xC6`, its Enemy attachment slot is cleared, and the count is reset to zero after the scan. The first natural probe wrote public `Effect::active +0xC4` and differed in exactly one displacement byte. Target code therefore positively distinguishes the +0xC6 field from the already-known active byte.
- The exact implementation intentionally accesses +0xC6 through a private byte offset instead of renaming `Effect::unknown0C6` globally. This closes the ECL helper without overstating the meaning of that Effect field and uses no ABI/codegen forcing.

## Packet 271 Enemy wrapped-angle interpolation helper

- `EnemyDrawInterpolateWrappedAngle @ 0x0040F5E0-0x0040F63A` is repository-canonical exact at 91 bytes. Maintained `EnemyManagerDraw.cpp` already declares the stdcall helper and calls it exactly twice while constructing trail-strip geometry, both with interpolation factor 0.5f.
- TH09 target sorts the two source angles implicitly through control flow, computes direct angular distance and the complementary `2*pi - direct` distance, keeps the shorter distance, then returns `lowerAngle + shorterDistance * factor`. Both 2*pi references relocate to target constant `0x0048E4C4`.
- Exact x87 lowering is source-shape sensitive but closes naturally. A first lower/upper-local spelling emitted 88 bytes. Reusing `angle1` as the lower-angle stack slot gives the target 91-byte frame; spelling the first branch as `angle1 < angle2` yields the target `test ah,5 / jp`, while retaining direct distance only when `direct < wrapped` yields target `test ah,5 / jnp` at the second compare. No asm, volatile/register forcing, pragma or profile search is used.

## Packet 272 ECL subroutine context initializer

- `Th09EclRunState::ManagerStateView::InitializeSubroutine @ 0x00406850-0x00406898` is repository-canonical exact at 73 bytes. This is not a new guessed API: maintained EclHelpers/EclRunState source already declared the method and calls it from ordinary subroutine calls, boss/death callback entry, manager-clear cleanup, and EnemyManager update paths.
- TH09 target proves a manager-side subroutine instruction table pointer at private `Manager +0x04`. For nonnegative short id, the helper assigns `context.currentInstruction004 = table[id]`, clears `time008` and `secondaryTime094` through exact timer integer assignment, writes `subroutineId228`, stores the manager pointer at context `+0x00`, and returns zero.
- Exact implementation leaves `ManagerStateView` public layout unchanged and reaches +0x04 through a private cast, avoiding unnecessary 14KB RunEcl TU churn. First natural existing-symbol source is 73/73 with the two timer relocations both solved to `0x00401500`.

## Packet 273 Enemy manager kill-non-boss source closure

- `Th09EclRunRemote::ManagerClearView::KillAllNonBossEnemies @ 0x0040F640-0x0040F7C7` is now source-present under TH09-local EnemyManager ownership. RunEcl wire opcode 95 is the sole caller and target disassembly proves the physical ABI: Enemy manager in ECX, stack transitionValue/startValue; maintained `EclRunRemote.inl` is corrected from its earlier free/fastcall declaration to the member thiscall form. The caller still ignores the integer return, exactly as target does.
- The helper walks 128 Enemy records from manager `+0x5758` with 0x5430 stride. It accepts primary active bit0, rejects boss bit1 and secondary bit3, and clears `life2E48`. For special-interaction bit0x80 it recomputes `worldPosition2DD4 = position2D74 + positionOffset2D80`, emits an Ascii score popup, accumulates its value, then raises popup value by 30 capped at transitionValue. If trail state is active it repeats the popup on every sixth 0x1C trail sample.
- Popup color is target-lowered from the natural equivalent `popupValue >= transitionValue ? -256 : -1`, yielding the observed setl/dec/and/dec sequence. Any nonnegative `deathCallbackSubId2D2E` is entered through exact `ManagerStateView::InitializeSubroutine` on `mainContext07F4` and reset to -1. With opcode95 inputs (8000,0), the method returns the accumulated popup values.
- Exact codegen is deliberately not claimed. The maintained candidate is repeatable 384 bytes versus the 392-byte target with identical cold disassembly hashes. The remaining frontier is compiler-local geometry: target contains an entry-time parameter self-copy and a jump/alignment block before the outer 128-enemy loop, while the candidate places alignment in the inner trail loop. Natural for/while/do-while and signed-color probes were bounded and documented; further pursuit would be alignment/temporary-slot steering, so no padding, asm, volatile/register forcing or profile roulette is retained.

## Packet 274 Ascii boss-marker VM helpers

- `AsciiBossMarkerVmView::SetBossMarkerInterrupt @ 0x004067D0-0x004067EF` is repository-canonical exact at 32 bytes. Both TH09 RunEcl callers load `g_AsciiManager @ 0x004CE458` into ECX, push Enemy `bossSlot336B` and interrupt 1 or 2, then call this helper. The target computes `(slot+9)*0x2A4` from the AsciiManager base and forwards to exact `AnmVm::SetInterrupt @ 0x00406790`.
- `AsciiBossMarkerVmView::SetBossMarkerPosition @ 0x004067F0-0x00406819` is canonical exact at 42 bytes. TH09 RunEcl and EnemyManager::OnUpdate both load the same AsciiManager receiver and pass boss slot plus a Float3. The helper copies three dwords to `AsciiManager + 0x19CC + slot*0x2A4`. Since `0x19CC = 9*0x2A4 + AnmVm::pos(0x208)`, this independently identifies the same boss-marker VM plane.
- The existing exact `AsciiManager::SetBossMarkerState @ 0x0040F890` writes the associated marker-state dword plane at `+0x2254 + slot*4`. Together the three helpers close a coherent TH09 boss-marker subsystem without widening the public AsciiManager opaque region: private reconstruction overlay starts the VM bank at `+0x17C4` and uses the already-proven 0x2A4 VM size/position offset. Both new helpers match on the first natural VC7.1 build; no codegen steering is used.

## Packet 275 Background spell start and Player position callback

- `BackgroundSpellStateView::BeginSpellBackground @ 0x004067B0-0x004067C4` is repository-canonical exact at 21 bytes. TH09 RunEcl opcode186 loads EnemyManager `sideState320` then side-state `+0x00` as ECX before this call. Existing Background layout independently fixes `spellBackgroundState +0xCCC` and `spellBackgroundFrame +0xCD0`. Natural source writes 1 and 0 respectively and reproduces the entire target body.
- `PlayerPositionCallbackView::InvokePositionCallback @ 0x00406820-0x0040682F` is canonical exact at 16 bytes. Enemy_UpdateMovement obtains `sideState320->player04` as ECX, passes `&Enemy.position2D74` on the stack, then tests EAX immediately after the call. The helper moves the position pointer into EDX and calls the Player function pointer at `+0x30404`. Exact Player AddedCallback independently initializes that field from `g_PlayerCallback30404[shotType]`, proving Player ownership and the ECX/EDX fastcall callback ABI.
- Both helpers use neutral target-facing view names because original source spelling is not established. No public layout is widened: the Background fields were already maintained, and the Player callback overlay exposes only `+0x30404`. Both first natural VC7.1 probes are zero-difference with no relocations.

## Packet 276 Enemy ECL raw-file release leaf

- `EnemyEclManagerReleaseView::ReleaseRawFile @ 0x00406830-0x0040684B` is repository-canonical exact at 28 bytes. TH09 teardown helper `0x00411F30` calls it twice with ECX equal to EnemyManager `primaryEclManager000 @ +0x000` and `opposingEclManager188 @ +0x188`. Existing maintained EnemyEclManager layout independently fixes `rawFile00` as the first field.
- Natural source checks `rawFile00`, frees it through exact `g_ZunMemory @ 0x004AD018` / `ZunMemory::Free @ 0x00401360` when non-null, then clears the pointer. It reproduces all 28 bytes and both relocation fields on the first pinned VC7.1 replay.
- The earlier Packet-192 split-TU negative for ReleaseSubsystem4 @ 0x00411F80 is superseded by ENEMY-006: target-local TU evidence shows the release core is file-static and receives a natural VC7.1 private EBX transport. With that missing TU fact restored, both 0x00411F30 and 0x00411F80 are exact without register forcing.

## Packet 277 PlayStats stat5 increment leaf

- `PlayStatsRecordView::IncrementCharacterStat5 @ 0x004158E0-0x00415900` is repository-canonical exact at 33 bytes. Its sole TH09 caller loads `g_PlayStatsRecord @ 0x004A8180` into ECX and passes a signed character byte. Existing exact PLST reconstruction fixes `characterStats[16][6] @ +0x7C` with 0x18 bytes per character row.
- Target address arithmetic `(character + 6) * 0x18` equals `0x7C + character*0x18 + 5*4`, independently proving that this helper increments stat index 5. It saturates at 999999, matching the existing generic `IncrementCharacterStat` policy, and forms the write companion to exact `GetCharacterStat5 @ 0x0040E450`.
- Adding the method to the real `PlayStatsRecordView` rather than a temporary overlay preserves the established 0x1FC PLST layout. Natural pinned VC7.1 source reproduces all 33 bytes with no relocations; no codegen steering is required.

## Packet 278 ReplayInput pressed-mask codegen frontier

- `ReplayInputPressedView::WasPressed @ 0x00415CD0-0x00415CE1` is owner/ABI/semantics-closed but deliberately remains source-absent. TH09 callers use `g_ReplayInputStates[2] @ 0x004ACF34` or `0x004ACE18 + side*0x8E` as ECX, pass one integer mask, and test AX. Exact `ReplayInputState::Update` independently fixes `historyPressed @ +0x32`; target returns `historyPressed & mask`.
- Two bounded natural VC7.1 source forms were tested: direct `return historyPressed & mask` and `mask &= historyPressed; return (u16)mask`. Both emit the same 16-byte body using `and eax,[ebp+8]`; target is 18 bytes because it emits `mov ecx,[ebp+8]; and eax,ecx`. No source-level semantic/type evidence distinguishes a third natural form.
- This is the pressed-edge sibling of the existing TOOLCHAIN-164 `ReplayInputState::IsHeld @ 0x00415CB0` negative, but it reads `+0x32` rather than `historyCurrent +0x2C`. Do not use volatile/register forcing, padding, assembly, arbitrary profile search, or other encoding-directed source to obtain the two target bytes.

## Packet 279 Input front-end exact leaf and codegen frontier

- `SanitizeKeyboardState @ 0x0042AD70-0x0042ADB0` is repository-canonical exact at 65 bytes. Its sole TH09 caller is WinMain. The target IAT resolves `0x0048E228` / `0x0048E22C` to USER32!GetKeyboardState / SetKeyboardState. Natural source reads a 256-byte keyboard-state array, clears bit7 on every byte, and writes it back; all 57 ordinary bytes plus both IAT relocation fields match.
- Adjacent `UpdateReplayInputFront @ 0x0042ACE0-0x0042AD62` is owner/ABI/semantics-closed but deliberately remains source-absent. Exact Supervisor_NetworkServiceUpdate calls it three times with ECX=0/1/2 and EDX carrying side0, side1, or combined input. Target indexes `g_ReplayInputStates @ 0x004ACE18` at 0x8E stride, rolls current/previous input, maintains sixteen held counters with threshold 26 and rollback 8, computes repeatOutput, then derives pressed/released edge words.
- Three bounded natural source shapes for the front update all emit a stable 128-byte body versus the 131-byte target. The residual is register allocation, not semantics: target keeps state base in ECX, repeat bit in EDI, held-counter pointer in ESI and loop count in EDX, while natural VC7.1 consistently chooses EAX/EDX/ECX/ESI. Parameter-width refinement from u16 to int and explicit byte-offset/reference spelling do not change that allocation. Do not force registers, volatile, pragma order, padding or profile search.

## Packet 280 Controller input poller source closure

- TH09's controller-input family is now source-closed around `Controller::GetControllerInput @ 0x0042B410-0x0042B737` plus two same-TU private helpers. Target callers pass `controllerIndex` in ECX, `joystickIndex` in EDX and an existing 16-bit input mask on the stack. TH09 independently fixes the 0x8E `g_ReplayInputStates` slot/mapping layout, Supervisor DirectInput controller pointers `+0x14/+0x18`, signed axis thresholds `+0x430/+0x432`, and the exact two-element JOYCAPSA array at `0x004AC8D8`. Clean TH095 was consulted only after those TH09 facts were closed and corroborates the broad Controller source family.
- `Controller::SetButtonFromDirectInputState @ 0x0042AC80` is canonical exact at 37 bytes and `Controller::SetButtonFromJoystickButtons @ 0x0042ACB0` at 47 bytes. Both are ordinary static four-argument C++ functions. When compiled in the same TU as the real poller call sites, VC7.1 naturally rewrites their private ABI into the target AX/ECX/EDX/ESI register transport; no source-visible register annotations, assembly, volatile, padding or pragma are used.
- The owner polls the selected DirectInput device when both TH09 controller slots exist, retries Acquire while `DIERR_INPUTLOST` for at most 400 attempts, samples a 0x110-byte DIJOYSTATE2, maps nine configured buttons to masks 1..0x100, and adds signed X/Y threshold directions. The fallback zeros a JOYINFOEX, calls `joyGetPosEx(joystickIndex != 0)`, maps the same nine buttons from `dwButtons`, then applies quarter-range JOYCAPS deadzones around the X/Y midpoint.
- Exactness is deliberately not claimed for `Controller::GetControllerInput`. The tracked natural O2/Ob1 source emits a repeatable 782-byte function versus the 808-byte target; two cold builds have identical function-disassembly SHA-256 `a61d827ca15194fce719c25d885bca4083bc047143139fa7983d2c1a13293c0d`. The 0x148 stack frame and both private helper ABIs match, but target/local register lifetimes diverge from the prologue onward. Further exact chasing would be register-allocation steering, so this owner remains honestly NON-EXACT.

## Packet 281 Replay stage dispatcher leaf

- `DispatchReplayStage @ 0x004217E0-0x004217F4` is repository-canonical exact at 21 bytes. TH09 GameplaySetupThread invokes it on the published ReplayManager with replayMode 0 for record setup and 1 for playback setup. The target body is a two-case switch: mode 0 tail-jumps maintained `ReplayManagerView::BeginRecordingStage @ 0x00421140`, mode 1 tail-jumps `BeginPlaybackStage @ 0x00420840`, and all other modes return immediately.
- A natural `void __fastcall` switch reproduces the exact `mov/sub/dec` lowering, both tail jumps and the default ret. The callers ignore EAX, and the default target path leaves only an incidental arithmetic residue, so no integer-return contract is invented. Clean TH08 corroborates the same broad existing-manager record/playback switch family only after TH09 fixed both destinations and calling context.
- This 21-byte closure is independent of the two destination owners' existing codegen plateaus: BeginRecordingStage remains source-present/non-exact with its four-byte scheduling residual, and BeginPlaybackStage remains source-present/non-exact with its two-byte seed zero-extension deficit. Their exactness is not upgraded here.

## Packet 282 Player collision box producer and Enemy wrapper

- `PlayerCollisionQueryStateView::AppendBoxRecord @ 0x0040F8E0-0x0040F9AC` is repository-canonical exact at 205 bytes. Existing Player collision-query layout fixes owner +0x00, 128 records of 0x30 bytes at +0x04 and count +0x1804. The helper appends position at record +0x00, copies caller size into halfSize +0x18, halves x/y, clears radius +0x24 and angle +0x28, writes the caller tail dword +0x2C and increments count while below 128.
- Exact source shape is target-backed rather than padded: caching `&records[count1804]` lets VC7.1 collapse the function to 130 bytes, while ordinary repeated member expressions `records[count1804].field` reproduce the target's repeated count/index address arithmetic and all 205 bytes, including two 0.5f relocations. This is the box sibling of exact `AppendLaserRecord @ 0x00412830`.
- `EnemyAppendCollisionView::AppendPlayerCollisionBox @ 0x00410250-0x004102A1` is now maintained source-present/NON-EXACT. EnemyManager::OnUpdate is the only owner and passes Enemy `worldPosition2DD4` plus `hitbox2DBC` or a derived hitbox. The wrapper computes `size / 1.5f` through exact PlayerPositionView divide, resolves manager sideState320 -> player04 -> collisionQuery36C, and calls exact AppendBoxRecord with tail value zero.
- The wrapper's direct-initialization form emits 65 bytes because VC7.1 elides the returned Float3 temporary. The target-backed declaration-then-assignment form restores the two 0x0C temporaries and exact 82-byte extent with both relocations correct, but only 43/74 ordinary bytes match because the target and compiler schedule the three-dword temporary copy and later argument preparation differently. No var-order/register/volatile forcing is retained.

## Packet 283 Player shot collision callbacks

- `PlayerShotCollisionCallbackType1 @ 0x00441880-0x0044188A` and `PlayerShotCollisionCallbackType2 @ 0x00443430-0x00443437` are repository-canonical exact at 11 and 8 bytes. They have no direct code callers because they are data-table callbacks, but TH09 Player SHT loading fixes their ownership unambiguously.
- `PlayerShtDescriptorView::collisionCallback` is descriptor `+0x34`. TH09 loader `0x0041BC40` maps that index through callback table `0x004A1C64`; table index 1 contains `0x00441880` and index 2 contains `0x00443430`. The maintained Player damage path independently proves the callback ABI as fastcall-style ECX=Player, EDX=shot, one stack target-position pointer, with integer return.
- Type1 increments Player byte `+0x30354` and returns 0. Type2 returns constant 1. Both natural three-argument fastcall sources reproduce the complete target bodies on the first pinned VC7.1 build with no relocations. The TypeN names intentionally denote collision-callback table indices rather than guessed original gameplay mnemonics.

## Packet 284 Effect fixed-slot accessor

- `EffectFixedSlotManagerView::GetFixedSlotEffect @ 0x00445560-0x00445579` is repository-canonical exact at 26 bytes. TH09 Player shot spawn callback table entry 5 calls it on `sideState->effectManager0C` before deciding whether a shot may spawn, independently fixing an EffectManager receiver.
- Existing TH09 EffectManager source fixes the effect-record pointer at manager `+0x30`, the primary dynamic pool at 256 records, and Effect size `0xD8`. The helper computes exactly `&effects[0x100 + slot]`; target uses the hardcoded 0x100 rather than loading primaryCount. Plain array-index source reproduces all 26 bytes with no relocations.
- The maintained target-facing view is intentionally narrow. It closes the fixed-slot lookup dependency used by Player spawn callbacks without widening the public EffectManager API or importing adjacent-game names.

## Packet 285 Player shot spawn callbacks

- Three TH09 Player SHT spawn-callback table entries are repository-canonical exact under the real callback ABI `ECX=Player, EDX=shot, stack=value+descriptor`. `PlayerShotSpawnCallbackType2 @ 0x004423D0` is 68 bytes, `Type4 @ 0x00445530` is 44 bytes, and `Type5 @ 0x00445CB0` is 72 bytes. TH09 SHT loading fixes descriptor `+0x28` as spawnCallback and maps its indices through table `0x004A1C10`; indices 2/4/5 point to these physical bodies.
- Type2 succeeds only when the caller value equals descriptor signed short `+0x00`. It invokes the maintained `InitializeShot @ 0x0041F350` ABI, zeros shot velocity x/y at `+0x43C/+0x440`, copies descriptor `+0x14` to shot `+0x450`, and returns 1; otherwise 0. Type4 never creates a shot: on schedule match it spawns exact fixed-slot effect 35 at Player position `+0x1B88` in slot 5 with color -1, then returns 0.
- Type5 first queries exact `GetFixedSlotEffect(5) @ 0x00445560`. Only when Effect `+0xC4` is inactive and schedule matches does it call InitializeShot and return 1. Initial early-return source for Type2/5 produced different VC7.1 block ordering; ordinary success-block source `if (condition) { ...; return 1; } return 0;` reproduces the target layout exactly. No register hints, volatile, pragma, padding or asm are used.

## Packet 286 Player shot update callback type5

- `PlayerShotUpdateCallbackType5 @ 0x004446A0-0x004446F5` is repository-canonical exact at 86 bytes. TH09 SHT loading fixes descriptor `+0x2C` as updateCallback and maps indices through table `0x004A1C34`; index 5 contains this physical body. Maintained Player shot dispatch fixes callback ABI as fastcall-style ECX=Player and EDX=shot.
- Target behavior is fully local: when shot short `+0x470 == 1`, clear velocity x/y `+0x43C/+0x440` and evaluate exact `ZunTimer::operator>=(90)` on timer `+0x454`; on expiry clear shot state `+0x462`. Byte `+0x472` is set to 1 on every path and the callback returns zero.
- Straight natural source reproduces all 86 bytes on the first pinned VC7.1 O2/Ob1 build; the sole relocation resolves to exact `ZunTimer::operator>= @ 0x00401520`. Type5 names the update-callback table index and does not claim an original gameplay mnemonic.

## Packet 287 Player shot turning update callbacks

- `PlayerShotUpdateCallbackType6 @ 0x00449C70-0x00449CD1` and `Type7 @ 0x00449CE0-0x00449D41` are repository-canonical exact at 98 bytes each. TH09 SHT descriptor `+0x2C` maps update-callback table `0x004A1C34` indices 6/7 directly to these bodies; callback ABI remains ECX=Player, EDX=shot.
- Both normalize shot angle `+0x450` through exact `AddNormalizeAngle @ 0x0042AED0`, using respectively +0.0785398185f and -0.0785398185f (±pi/40). They multiply speed `+0x44C` by exact target constant 0.96f at `0x00491644`, store it back, then compute velocity x/y `+0x43C/+0x440` via external `@cosf@4 / @sinf@4` and return 0.
- Exact profile selection is target-backed rather than searched. The existing Player InitializeShot frontier already proves that VC7.1 /Ob1 intrinsically lowers cosf/sinf while this target family uses external calls; the single justified /Ob0 hypothesis preserves those calls. An analysis-only shared helper remained out-of-line under /Ob0, so the maintained source simply spells the two ordinary mirror bodies separately. Both then match 98/98 with four relocations each.

## Packet 288 Player shot spawn callback type3

- `PlayerShotSpawnCallbackType3 @ 0x004432A0-0x004432F5` is repository-canonical exact at 86 bytes. TH09 SHT descriptor `+0x28` spawnCallback table `0x004A1C10` index 3 points to this body under the same ECX=Player, EDX=shot, stack=value+descriptor ABI as the already-closed indices 2/4/5.
- On schedule match it calls maintained `InitializeShot @ 0x0041F350`, writes shot velocity x=0 and y=-24 at `+0x43C/+0x440`, then writes Player speed multipliers `+0x1CE0` and `+0x1CDC` to 0.5 before returning 1; otherwise it returns 0. The two multiplier fields were already target-backed by Player lifecycle/update source.
- First natural source was target-sized and differed in only two ModRM displacement bytes because it wrote +0x1CDC before +0x1CE0. The target explicitly orders the independent stores +0x1CE0 then +0x1CDC; matching that source order yields 86/86 exact with the sole relocation to InitializeShot. No register/pragma/volatile forcing is used.

## Packet 289 Player shot spawn callback type6

- `PlayerShotSpawnCallbackType6 @ 0x00445D00-0x00445D76` is repository-canonical exact at 119 bytes. TH09 SHT spawnCallback table `0x004A1C10` index 6 points here under the already-proven Player/shot/value/descriptor fastcall ABI.
- On schedule match the callback invokes maintained `InitializeShot @ 0x0041F350`, obtains exact Effect fixed slot 5 through `GetFixedSlotEffect @ 0x00445560`, copies Effect position `+0x0C` into shot position `+0x2A4`, writes shot angle `+0x450 = -pi/2`, velocity x `+0x43C = 0` and velocity y `+0x440 = -descriptor[+0x18]`, then returns 1; otherwise 0.
- The first natural source reproduces all 119 bytes with two relocations and no codegen steering. The target-facing Type6 name records only the SHT callback-table index.

## Packet 290 Player shot spawn callback type7

- `PlayerShotSpawnCallbackType7 @ 0x0044AB90-0x0044AC1A` is repository-canonical exact at 139 bytes. TH09 SHT spawnCallback table `0x004A1C10` index 7 points here under the same proven Player/shot/value/descriptor ABI as indices 2-6.
- On schedule match it calls exact `PlayerForwardAngleView::AngleFromPlayerToPoint @ 0x0041BB00` using Player trackedEnemyPosition `+0x30364`, adds descriptor angle `+0x14`, and falls back to -pi/2 when tracked x is less than target constant -144.0f at `0x0048E808`. It then calls maintained InitializeShot, computes velocity x/y through external cosf/sinf multiplied by descriptor speed `+0x18`, stores shot angle `+0x450`, and returns 1; otherwise 0.
- As with turning update callbacks, the only target-backed compiler hypothesis is /Ob0 because TH09 explicitly calls external trig and the Player family already demonstrates /Ob1 intrinsic lowering. First source was six bytes too long solely because an analysis-only speed local caused a stack store/load; directly multiplying both trig results by descriptor +0x18 reproduces all 139 bytes and five relocations without forcing.

## Packet 291 Player shot spawn callback type1

- `PlayerShotSpawnCallbackType1 @ 0x00441EF0-0x00441F86` is repository-canonical exact at 151 bytes. TH09 SHT spawn-callback table `0x004A1C10` index 1 points here; indices 2-7 are already canonical exact, so the full non-null table is now closed under the proven callback ABI `ECX=Player, EDX=shot, stack=value+descriptor`.
- On signed schedule match against descriptor `+0x00`, the callback sets Player shot-state timer `+0x3031C = 30` and shot pointer `+0x30328`, copies descriptor source option `+0x20` to shot `+0x468`, descriptor floats `+0x04/+0x08` to shot velocity-z/auxiliary `+0x444/+0x448`, sets shot `+0x46A = 15`, invokes maintained `InitializeShot @ 0x0041F350`, then sets shot type `+0x464 = 2`.
- The final target loop writes -999.0f to trail-x for all 32 0x0C-stride entries at `+0x2B0` in descending index order, then to current shot position.x `+0x2A4`. Straight natural source reproduces all 151 bytes with only the exact ZunTimer assignment and InitializeShot relocations. Type1 denotes callback-table index only; no gameplay mnemonic is inferred.

## Packet 292 Player shot update callback type3

- `PlayerShotUpdateCallbackType3 @ 0x00443150-0x00443247` is repository-canonical exact at 248 bytes. TH09 SHT update-callback table `0x004A1C34` index 3 points here under the already-proven fastcall ABI `ECX=Player, EDX=shot`.
- The callback activates only for shot state `+0x462 == 1` and timer `+0x454` current==30. It aims toward Player trackedEnemyPosition `+0x30364/+0x30368` unless tracked x is below -144.0f; exact zero delta uses current shot angle plus pi/2, otherwise exact VectorAngle(yDelta,xDelta) is added to the current angle. The raw result is normalized once through exact AddNormalizeAngle, then target-observed external cosf/sinf times shot speed `+0x44C` rebuild velocity `+0x43C/+0x440` and store angle `+0x450`.
- First natural probe was 244 bytes for two source-shape reasons directly exposed by target disassembly: the shot has a 2-byte gap at `+0x460` before state `+0x462`, and target computes a branch-local raw angle before one common normalize call rather than normalizing inside each branch. Correcting those two TH09 facts and spelling the main branch as trackedX>=-144 yields 248/248 with all nine relocations solved. The /Ob0 profile is the already-established Player-shot external-trig profile, not a new search.

## Packet 293 Player shot update callback type4 source closure

- `PlayerShotUpdateCallbackType4 @ 0x00443930-0x00443A28` is now source-present under the TH09 SHT update-callback table index 4, but exact codegen is deliberately not claimed. The established callback ABI is `ECX=Player, EDX=shot`. Target behavior copies Player position `+0x1B88` into shot position `+0x2A4`, integrates velocityY `+0x440` into y then subtracts 1.0f, and restores Player speed multipliers `+0x1CE0/+0x1CDC` to 1.0 once timer `+0x454 >= 15`.
- While shot state `+0x462 == 1`, width is `float(timer)+80`. The target copies shot.position into a local center, subtracts 16 from center.y and `(width-96)*0.5` from center.x, then calls exact `CreateRectType0(width,40,value15,lifetime0,delay0)` and exact `CreateRectType1(width,30,lifetime0,delay0)`. ZunTimer operator>= and operator float destinations are also target-bound.
- Two natural source spellings compile identically and repeatably to 243 bytes versus the 249-byte target. The remaining frontier is preserved-register allocation only: target keeps EBX=player, ESI=shot, EDI=&shot.position, whereas pinned VC7.1 naturally chooses EBX=player, EDI=shot, ESI=&shot.position. Further pursuit would be register/lifetime steering, so no match row, pragma, volatile, padding or asm is retained.

## Packet 294 Player shot update callback type1 source closure

- `PlayerShotUpdateCallbackType1 @ 0x004415E0-0x004417B6` is now source-present under TH09 SHT update-callback table index 1, but exact codegen is deliberately not claimed. The table `0x004A1C34` fixes the callback ABI as `ECX=Player, EDX=shot`. Gate conditions are shot state `+0x462 == 1`, timer current >=40, and exact `ZunTimer::HasTicked`.
- With a tracked target (Player trackedEnemyPosition.x > -900), the callback computes delta from shot position, scales a steering term by `1 / max(distance/(speed*0.25), 1)`, adds it to current velocity, measures the resulting magnitude through target external sqrtf, clamps shot speed `+0x44C` to [1,10], and renormalizes velocity. With no tracked target and speed<10, speed increases by 1/3 while preserving current direction. Shot angle `+0x450` is always refreshed by exact VectorAngle(vy,vx).
- External sqrtf lowering is target-backed: pinned VC7.1 /Ob0 emits calls to `@sqrtf@4` while /Ob1 intrinsically emits FSQRT, matching the already-established Player-shot external-math profile. The simplest natural source cold-replays identically at 460 bytes versus target 471 (hash `cf661a8a29eb40ab...`). Two target-motivated local clamp spellings shrink further to 454/452, so the remaining 11-byte frontier is x87 temporary/source scheduling rather than missing semantics. No register/volatile/pragma/padding/asm steering is retained.

## Packet 295 Player shot update callback type2 source closure

- `PlayerShotUpdateCallbackType2 @ 0x00441F90-0x00442212` is now source-present under TH09 SHT update-callback table index 2, but exact codegen is deliberately not claimed. The callback ABI is the same proven `ECX=Player, EDX=shot` contract. Player shot-state timer `+0x3031C` is the controlling lifetime timer: if it is <=0, the callback resets it, clears Player activeShot `+0x30328`, clears shot state `+0x462` and returns 1.
- Otherwise shot position `+0x2A4` is rebuilt from Player position `+0x1B88`. Shot velocity.z `+0x444` contributes to x; z becomes 0.44f; hitbox.y `+0x434` captures the pre-halved y; VM field `+0x1C` receives y/14; then position.y is halved. A natural 31->1 history loop over 32 x 0x0C Float3 entries is automatically lowered by VC7.1 into the target's large partially-unrolled copy/decrement block, with every copied trail y reduced by 1.0f.
- `trailSegmentCount +0x46A` controls Type4 collision rectangles on every other history point (0,2,4,...) whose x>=-900; exact CreateRectType4 receives width4,height448,value1,lifetime1,delay0. Current shot position is restored into history[0] and the Player shot-state timer is post-decremented. The first probe mistakenly decremented shot timer `+0x454`; target local `[ebp-0x10]` proves the surviving timer pointer is Player +0x3031C.
- After semantic correction, natural source variants emit 627/640/643 bytes. The retained version is cold-stable and target-sized 643/643 with `546/579` comparable bytes matched and all 16 relocation fields solved (hash `911575724c38e730...`). Remaining differences are confined to the x/z store scheduling block and final history[0] load/store ordering. Further pursuit would be instruction-scheduling/source-spelling steering rather than missing semantics, so no match row, pragma, volatile, padding or asm is retained.

## Packet 296 Player shot draw callback type1 source closure

- `PlayerShotDrawCallbackType1 @ 0x00442220-0x004422EB` is now source-present under TH09 SHT draw-callback table `0x004A1C54` index 1, but exact codegen is deliberately not claimed. The established callback ABI is `ECX=Player, EDX=shot`; this callback does not otherwise use the Player argument.
- It saves shot VM alpha byte `+0x1F3`, iterates at most `2*trailSegmentCount(+0x46A)` Float3 history points from `+0x2B0`, stops on the -999.0f x sentinel, transforms point x/y through exact GameManager TransformPopupX/Y into VM position `+0x208/+0x20C`, zeros z, attenuates alpha by `alphaAccumulator/(2*count)`, calls exact AnmManager::Draw2D, accumulates the saved alpha, and restores alpha on exit.
- A first source that cached `2*count` emits 224 bytes. Removing that non-original cache, using the target -999.0f sentinel, and matching the target index-before-history increment order produces a cold-stable 190-byte candidate (hash `3f55de13e849e143...`) with target stack depth, loop fields and call dependencies. The remaining 14-byte gap is the optimizer's epilogue tail merge: canonical Player-shot /Ob1 merges the no-draw and normal alpha-restore returns, while the target keeps two equivalent epilogues. Explicit early-return source is still tail-merged. Further pursuit would require CFG/optimizer steering, so no exact match is claimed.

## Packet 297 ExAttack record side-state accessor

- `ExAttackRecordSideView::GetSideState @ 0x00440CB0-0x00440CBB` is repository-canonical exact at 12 bytes. Existing exact `ExAttackController::Spawn` initializes `ExAttackRecord.side @ +0x08` from its spawn-side argument, then dispatches type-specific callbacks on that record. Representative 0x44xxxx ExAttack callbacks call this helper on the same record and immediately treat the returned pointer as a 0x38-byte GameManager side state.
- Existing exact ExAttack setup source independently fixes `g_GameManager @ 0x004A7D90` as two `ExAttackGameManagerSideView` records with 0x38 stride. Natural source `return &g_GameManager.sides[side08]` reproduces all 12 bytes; the sole DIR32 relocation solves to g_GameManager.
- The physical owner is ExAttackRecord/ExAttackController, not Player: callers belong to the ExAttack template callback family installed by `g_ExAttackTemplates`. A dedicated target-facing view is retained so the shared public ExAttackRecord header does not need another method declaration or cause unrelated compiler-local label churn.

## Packet 298 ExAttack type4 release callback

- `ExAttackReleaseCallbackType4 @ 0x00442EC0-0x00442EDF` is repository-canonical exact at 32 bytes. Exact `ExAttackController::Spawn` consumes `g_ExAttackTemplates @ 0x004A0F78` as four callbacks per type in initialize/update/draw/release order; table slot 19 (type4 release column) points to this body under the established fastcall ExAttackRecord callback ABI.
- The callback tests record `+0x34`; when non-null it calls exact `ZunMemory::Free` through `g_ZunMemory @ 0x004AD018`, then clears +0x34 and returns 0. Plain natural source reproduces all 32 bytes and both relocation fields on the first pinned VC7.1 O2/Ob1 build.
- The +0x34 field remains deliberately neutral as `extra34`: this release leaf proves pointer lifetime and ownership by the type4 template callback, but not the original resource/class spelling. Type4 names the template index only.

## Packet 299 CardAttack category3 update callback

- `CardAttackUpdateNoCategory3 @ 0x00440CC0-0x00440CD7` is repository-canonical exact at 24 bytes. TH09 owner `0x00403E50` consumes table `0x004A00C0` as nine rows of four CardAttack callbacks: it invokes column 0 immediately and stores columns 1/2/3 into the maintained update/draw/end callback slots. Rows 2, 5 and 8 all use `0x00440CC0` in the update column, fixing its CardAttack callback role.
- Public `CardAttackUpdateCallback` already fixes the ABI as `int(CardAttack*)`. The helper selects the target-proven 0x38 GameManager side record using `cardAttack->sideIndex00`, loads `enemyManager10 @ side+0x10`, calls exact `EnemyManagerView::FindActiveEnemyBySideCategory(3)`, and returns 1 only when no such active Enemy remains.
- Natural source reproduces all 24 bytes. The DIR32 relocation is `g_GameManager + 0x10` and the REL32 relocation resolves to the exact EnemyManager query. The behavioral reconstruction name deliberately avoids claiming the original card/spell mnemonic represented by table rows 2/5/8.

## Packet 300 CardAttack timer update callback

- `CardAttackUpdateAfter192 @ 0x00441BC0-0x00441BD6` is repository-canonical exact at 23 bytes. TH09 CardAttack callback table `0x004A00C0` is consumed in begin/update/draw/end columns; rows 0, 1, 3, 4, 6 and 7 all use this physical body in the update column under the public `int(CardAttack*)` callback ABI.
- The target adds `CardAttack::timer98 @ +0x98`, calls exact `ZunTimer::operator>(192) @ 0x00403DE0`, then executes `neg eax; sbb eax,eax; neg eax` to normalize any nonzero unsigned result to integer 1. A direct source return of the overloaded operator emits only 17 bytes; natural `(timer98 > 192) != 0` source reproduces the complete 23-byte target with the single relocation solved.
- The exact boolean-normalization sequence is source-semantic evidence rather than register steering. The maintained behavior name intentionally avoids identifying the original card/spell rows 0/1/3/4/6/7.

## Packet 301 CardAttack end callbacks

- Two CardAttack end-column callbacks are repository-canonical exact at 38 bytes each. `CardAttackEndFree1360 @ 0x004413A0-0x004413C5` is used by callback-table rows 0/1/3/4/6/7; `CardAttackEndFree1364 @ 0x00441470-0x00441495` is used by rows 2/5/8.
- Each body loads its target-backed tail pointer (`CardAttack +0x1360` or `+0x1364`), conditionally calls exact `g_ZunMemory.Free`, clears the slot, and writes EAX=0. Natural source reproduces all target bytes and both relocations for each callback.
- The two tail pointers stay reconstruction-neutral as `extra1360/extra1364` because the callbacks prove ownership/lifetime but not original resource spelling. The target bodies synthesize a zero return even though the maintained CardAttack end-callback typedef is void; CardAttack callers ignore the value, so original return-type spelling remains independently unclaimed.

## Packet 302 CardAttack EnemyManager spawn wrapper

- `EnemyManagerCardAttackSpawnView::SpawnFromCardAttack @ 0x00440C80-0x00440CA5` is repository-canonical exact at 38 bytes. All six TH09 CardAttack begin callbacks obtain the side-local EnemyManager from the 0x38 GameManager side record at +0x10 and call this member with six stack arguments.
- The wrapper forwards those arguments to authored EnemyManager spawn owner `0x0040F1D0`, narrowing the first value to short and the fourth to char exactly as target calling convention requires, then appends a constant seventh argument of 1. At this packet that final role was still unknown; Packet 411 later proves from TH09-local target code that the sixth argument is the mirror flag and the seventh selects primary versus opposing ECL. Natural wrapper source reproduces all 38 bytes with the sole REL32 relocation solved.
- The maintained names are behavioral. This packet did not promote the underlying 364-byte owner; Packet 411 subsequently reconstructs `0x0040F1D0` independently and closes it exact.

## Packet 303 CardAttack begin callbacks

- All six unique CardAttack begin-column callbacks in table `0x004A00C0` are repository-canonical exact. `CardAttackBeginPl00Type0/1/2 @ 0x00441350/0x004413D0/0x00441420` are 75/75/78 bytes and use ZunMemory debug path `.\pl\pl00.cpp`; `CardAttackBeginPl01Type0/1/2 @ 0x00441B70/0x00441BE0/0x00441C30` are 75/75/78 bytes and use `.\pl\pl01.cpp`. Table rows map them as row0, row1, row2, rows3/6, rows4/7 and rows5/8 respectively.
- Every body allocates exactly 0x2B8 bytes through exact `ZunMemory::Alloc`, stores the pointer in CardAttack tail `+0x1360` for variants 0/1 or `+0x1364` for variant 2, obtains the current side's `enemyManager10`, then calls exact `EnemyManagerCardAttackSpawnView::SpawnFromCardAttack @ 0x00440C80`. Variant arguments are (subroutine,valueA,valueC) = (0,30,1000), (1,30,10000), or (2,1400,1000), with valueB=-2 and mirrored=0.
- The target reserves a 0x0C stack local and passes its address as the spawn position without writing it first. Reconstruction deliberately preserves that uninitialized local rather than silently initializing it. An initial analysis-only common helper was not inlined under /Ob1 and shrank the callbacks to 26/29 bytes; spelling the six ordinary callback bodies directly reproduces all target bytes without force-inline or register steering.
- CardAttack callback ABI metadata is also corrected to `__fastcall` for the already-promoted update/end callbacks: /Gr decorated symbols are `YI` and target callbacks receive CardAttack in ECX. This is ledger-only ABI repair and changes no source/exactness credit.

## Packet 304 ExAttack dynamic-data allocator source closure

- `ExAttackAllocateRecordView::AllocateDynamicData @ 0x00440D90-0x00440DE0` is now source-present under ExAttackController ownership. Twenty-five TH09 ExAttack initialize callbacks call this entry on the same `ExAttackRecord` receiver family. Existing exact ExAttack update/draw/release overlays independently prove `dynamicData +0x1C`, `vmCount +0x2C` and `extraData +0x34`.
- The helper stores the VM count, turns it into a `0x2A4`-stride VM byte count, adds caller-provided extra bytes, allocates through exact `ZunMemory::Alloc` with the established system/global debug string, zeroes the entire block, and points `+0x34` immediately after the VM array. This supplies a single maintained source dependency for the remaining large ExAttack initializer family.
- Exactness is deliberately not claimed. Natural source variants with explicit byte-count locals, in-place extra-size accumulation, pointer-return and void contracts converge at 84/83 bytes versus the 81-byte target. The best 83-byte source differs in entry register allocation: target keeps vmCount in EBX from function entry, while VC7.1 naturally loads it through EAX before moving the across-call byte count into EBX. Further pursuit would be register-allocation/source steering, so this is a durable NON-EXACT frontier.

## Packet 305 Player collision circle append leaf

- `PlayerCollisionCircleAppendView::AppendCircleRecord @ 0x00440D30-0x00440D8E` is repository-canonical exact at 95 bytes. Eleven TH09 ExAttack update callbacks resolve `g_GameManager.sides[record->side].player04` and pass ECX adjusted to Player `+0x36C`, independently binding the existing Player collision-query owner.
- The already-reviewed query state is owner pointer +0x00, 128 records of 0x30 bytes at +0x04, and count +0x1804. This leaf appends only `position +0x00`, `radius +0x24` and `unknown2C +0x2C`, then increments count while below 128. Target stores at state-relative +0x28 and +(count+1)*0x30 because the record bank begins after the state owner pointer; these address forms are exactly the same physical fields.
- This closes the third producer shape alongside exact `AppendLaserRecord @ 0x00412830` and `AppendBoxRecord @ 0x0040F8E0`. A private target-facing view avoids changing the shared Player header, and the first natural pinned VC7.1 build is 95/95 with no relocations or source steering.

## Packet 306 Player position range callback

- `PlayerPositionCallbackWithinRange @ 0x00440DF0-0x00440E3E` is repository-canonical exact at 79 bytes. Exact Player::AddedCallback selects `g_PlayerCallback30404[shotType]`, and exact `PlayerPositionCallbackView::InvokePositionCallback` independently fixes the table ABI as ECX=Player, EDX=position and integer result in EAX. Table indices 0/3/5/12/13 all point to this physical body.
- The callback computes `Player.position1B88 - other` through exact `PlayerPositionView::operator- @ 0x00401140`, squares the XY delta, loads one float from the object reached through Player `+0x368` at its `+0x7C` offset, and returns true exactly when XY distance squared is strictly below that scalar squared. Natural fastcall source reproduces all 79 bytes and the sole relocation.
- Player `+0x368` stays intentionally neutral. Other maintained TH09 code proves it participates in an active-object/VM lifetime path, but does not establish an original field or type name. The reconstruction therefore uses a private range-source overlay and claims only target-observed pointer/offset semantics, not gameplay naming.

## Packet 307 Player callback30408 Enemy-state table closure

- `g_PlayerCallback30408 @ 0x004A1A90` is now completely source/exact-closed with three unique TH09 fastcall bodies. Exact `Player::AddedCallback` selects the table entry by shotType into Player `+0x30408`, while Enemy movement invokes that slot with ECX=Player and EDX=Enemy. Indices 0/1/3/5-15 use `PlayerEnemyCallback30408Default @ 0x004418B0` (95 bytes), index 2 uses `PlayerEnemyCallback30408Type2 @ 0x00442550` (35), and index 4 uses `PlayerEnemyCallback30408Type4 @ 0x00443C10` (95).
- The default/type4 callbacks share one target shape. They write Enemy `+0x2D98` from exact shared RNG signed range +/-0.1, test exact `specialAttackTimer5424 > 120`, and write `+0x2D9C` either as -1.0/-1.5 or as exact timer-float times -1/120 or -1.5/120. Both clear `+0x2DA0`, postfix-increment the exact timer, and return zero. Index2 clears all three dwords/floats, increments the same timer and returns zero.
- Natural source initially emitted 99-byte default/type4 bodies because branch-local direct member assignment materialized the constant path as a memory immediate. Using an ordinary local `float value` for both branch results lets VC7.1 preserve the x87 result across the join and perform the single target `fstp [Enemy+0x2D9C]`, closing both to 95 bytes. No register forcing, pragma, volatile or profile search is used. Enemy `+0x2D98/+0x2D9C/+0x2DA0` remain neutral target-backed fields.

## Packet 308 Player callback30404 short predicate family

- Four additional `g_PlayerCallback30404` entries are repository-canonical exact under the already-proven ECX=Player / EDX=position / EAX=int fastcall ABI: index6 `0x00445580` is 71 bytes, index7 `0x00445E60` is 91, index14 `0x0044BC80` is 71, and index15 `0x0044C2E0` is 98. Together with shared range predicate `0x00440DF0`, these close the table's five small geometry predicates.
- All four subtract the caller point from Player `position1B88` through exact `PlayerPositionView::operator- @ 0x00401140` and use the neutral object reached through Player `+0x368` with scalar `+0x7C`. Index6 tests X only; index14 tests Y only; index7 tests a circle after shifting Y by `1.5*radius`; index15 returns true when either axis lies within the radius interval.
- Exact closure is source-shape ordinary rather than ABI steering. Index7 needs the source pointer cached while radius is re-read for the final square, matching target memory loads. Index15 reaches exact code when the two axis conditions are expressed as one `condX || condY` expression, naturally yielding the target shared true tail. No field beyond the target-backed neutral +0x368/+0x7C path is named.

## Packet 309 Player callback30404 type1 curved-range predicate

- `PlayerPositionCallback30404Type1 @ 0x004417F0-0x00441870` is repository-canonical exact at 129 bytes. `g_PlayerCallback30404` table index 1 binds it to the same ECX=Player / EDX=position fastcall ABI as the other Player position predicates.
- TH09 target order is fully source-expressible: first require `other.y < Player.position1B88.y + 32`; then `shiftedY = other.y - (Player.y + 32 - 448)` and `deltaX = other.x - Player.x`. The horizontal limit is `(200704 - shiftedY^2) * source(+0x84)^2`, clamped to a minimum of 196, and the callback returns `deltaX^2 <= limit`. All five float constants/relocations match the target.
- Player `+0x368` and the pointee `+0x84` scalar stay neutral private fields; this packet claims the exact observed formula, not an original gameplay name. First natural pinned VC7.1 source is 129/129 with no register/pragma/profile steering.

## Packet 310 Player callback30404 angular-sector predicate

- `PlayerPositionCallback30404Type2 @ 0x00442480-0x00442540` is repository-canonical exact at 193 bytes and is shared by `g_PlayerCallback30404` indices 2 and 10. The established callback ABI is ECX=Player and EDX=position.
- The callback first requires strict XY distance within neutral `rangeSource368->radius7C`. It then calls exact `PlayerForwardAngleView::AngleFromPlayerToPoint @ 0x0041BB00`, subtracts neutral `angle80`, and uses exact `AnmProjectionAbs @ 0x0040F1C0` to wrap values outside +/-pi by +/-2pi. Final acceptance is `abs(wrappedAngle) <= width84*0.5`. All nine target relocations, including pi/2pi/0/0.5 constants, resolve canonically.
- First natural source was already target-sized and semantically closed, with only two ordinary bytes wrong: a negated radial condition chose JNP instead of the target JNE, and comparing long-double fabs directly to a float pi literal selected qword pi. Expressing the body as a positive strict-in-circle block and explicitly casting the first fabs result to float yields the target branch and dword compare. No register forcing, volatile, pragma or profile search is retained.

## Packet 311 GameManager valueCC saturating subtract leaf

- `GameManagerValueCCView::SubtractSaturating @ 0x00403E30-0x00403E4A` is repository-canonical exact at 27 bytes. TH09 CardAttack owner `0x00403E50` and exact `Player_EnterDeathState` both load ECX=`0x004A7E40` before the call. Since `g_GameManager @ 0x004A7D90`, the receiver is the GameManager +0xB0 subview; existing maintained GameManager layout independently fixes absolute `valueCC @ +0xCC`, i.e. subview +0x1C.
- Natural source simply subtracts the caller amount and clamps the field to zero when the result is negative. The two observed callers subtract 500 and 1500. Pinned VC7.1 O2/Ob1 reproduces all 27 bytes with no relocations on the first probe.
- Maintained source uses `void` because both callers ignore EAX. The target happens to leave the stack argument in EAX after the subtraction; that incidental value is not treated as proof of an integer return contract.

## Packet 312 ZunTimer tick-interval leaf

- `ZunTimer::HasTickedEvery @ 0x00404920-0x00404940` is repository-canonical exact at 33 bytes. TH09 target reads `current +0x08` and `previous +0x00`, returns false when they are equal, and otherwise performs signed `current % interval` and returns whether the remainder is zero. Eleven callers span Player, EnemyManager, and ExAttack code under the same public 0x0C ZunTimer layout.
- Natural source `return current != previous && current % interval == 0;` reproduces all 33 bytes on the first pinned VC7.1 O2/Ob1 probe with no relocations. This is distinct from exact `ZunTimer::HasTicked @ 0x0040F810`, which checks only the tick edge.
- Clean TH095/TH10 source was consulted only after TH09-local layout/semantics were fixed; it corroborates the same changed-and-even/modulo idiom in local helpers but provides no trustworthy original member name. `HasTickedEvery` remains a neutral reconstruction name.

## Packet 313 Supervisor DirectPlay message thunk

- `SupervisorNetworkMessageThunk @ 0x00432EA0-0x00432EAE` is repository-canonical exact at 15 bytes. TH09 netplay setup helper `0x00432EB0` and restart helper `0x00432F70` both pass this address as the message-handler argument to IDirectPlay8Peer::Initialize. Their call shapes independently establish the standard three-argument stdcall callback surface `(userContext, messageId, message)`.
- The thunk does not own DirectPlay state itself. It reloads `g_SupervisorNetworkState @ 0x004B42D0` into ECX and tail-jumps to the instance handler `0x00432B70` while preserving the three callback stack arguments. Natural stdcall wrapper source reproduces all seven ordinary bytes plus the two reviewed relocation fields exactly.
- The 768-byte instance message handler remains independently authored/no-source. Exactness/source presence of this thin registration thunk does not promote that owner or infer original class/member spelling beyond the already-established Supervisor network-state object.

## Packet 314 Chain release source closure

- `ChainReleaseView::ReleaseSingleChain @ 0x0042CAE0-0x0042CBD0` is now source-present under Chain ownership. TH09 builds a temporary snapshot of the supplied chain using a stack ChainElem head plus registry-wrapped heap ChainElem nodes, stores each original node in snapshot `releaseTarget +0x18`, calls exact `Chain::Cut` on every original target, and finally deletes the snapshot nodes. Exact ChainElem constructor/destructor/scalar-delete, ZunMemory::AddToRegistry, the `"funcChainInf"` literal, and Chain::Cut independently bind the complete dependency set.
- Clean committed TH095 corroborates this unusual snapshot-before-cut algorithm, but TH09 independently proves the explicit registry wrapping and all addresses. Natural TH09 `/EHsc /O2 /Ob1` source emits a cold-stable 268-byte body versus the 241-byte target. The target keeps unwind for the stack ChainElem but omits new-expression constructor-failure cleanup for the two heap ChainElem constructions; the same natural source under the ChainCreate-family `/GX-` shape collapses to 218 bytes and removes too much EH. Exact pursuit would require mixed-EH/placement-new/local-lifetime steering, so the core remains honestly NON-EXACT.
- `ChainReleaseView::Release @ 0x0042CBE0-0x0042CC01` is repository-canonical exact at 34 bytes. Sole caller WinMain passes `g_Chain @ 0x004ACFC8`. The wrapper calls exact Supervisor ThreadClose on `g_Supervisor`, then calls ReleaseSingleChain on the calc root at +0x00 and draw root at +0x20. Natural source reproduces all 18 ordinary bytes plus four relocation fields exactly.

## Packet 315 SupervisorNetworkState explicit constructor and size correction

- `SupervisorNetworkState::SupervisorNetworkState @ 0x0042CC10-0x0042CC28` is repository-canonical exact at 25 bytes. TH09 WinMain allocates exactly `0xD4` bytes, calls this constructor, registry-wraps the allocation with debug label `"NetworkInf"`, publishes it as `g_SupervisorNetworkState @ 0x004B42D0`, and immediately enters the DirectPlay setup path.
- The constructor zeroes all 0xD4 bytes and then writes `syncRate +0xC4 = 60`. Natural `memset(this,0,sizeof(*this)); syncRate=60;` source reproduces all 25 bytes with no relocations on the first pinned VC7.1 build.
- This target evidence also corrects the maintained public SupervisorNetworkState tail from stale `0x480` padding to actual `sizeof == 0xD4`. Previous frame-queue owner review already moved the larger 0x78-stride queue storage to Supervisor itself; repository search finds no network-state field or sizeof dependency beyond +0xD4. The correction therefore removes an obsolete owner/layout conflation rather than truncating live target state.

## Packet 316 Supervisor Direct3D bootstrap leaf

- `SupervisorInitializeD3D @ 0x0042CD10-0x0042CD3D` is repository-canonical exact at 46 bytes. Sole TH09 caller is WinMain with no arguments and it immediately tests the returned status. The helper calls `Direct3DCreate8(D3D_SDK_VERSION=220)`, stores the interface pointer to `g_Supervisor +0x04`, and returns zero on success.
- On failure the helper calls exact `GameErrorContext::Fatal` with target CP932 text `Direct3D オブジェクトは何故か作成出来なかった\r\n` and returns one. Existing TH09 render/snapshot evidence independently fixes `Supervisor +0x08` as the D3D8 device; the maintained startup helper exposes only the +0x04 IDirect3D8 pointer and does not widen Supervisor layout claims.
- Natural no-argument C++ source reproduces all 26 ordinary bytes plus five reviewed relocation fields on the first pinned VC7.1 O2/Ob1 build. The source is kept as a free startup helper because target WinMain performs a plain no-argument call; no member-function spelling is inferred from global access alone.


## Packet 317 VC7 math intrinsic origin correction

- `0x00401060`, `0x00401070`, and `0x00401080` are not authored TH09 source functions. Under the target-backed pinned VC7.1 `/Oi /Gr /O2 /Ob1 /Oy-` profile, an ordinary translation unit that merely calls `cosf`, `sinf`, and `sqrtf` automatically receives three link-once COMDATs named `@cosf@4`, `@sinf@4`, and `@sqrtf@4`. Their emitted bodies are respectively the 12-byte x87 FCOS, FSIN, and FSQRT wrappers found at those TH09 addresses.
- Two independent cold compiler probes reproduce 12/12 bytes for each helper with zero relocations. Existing canonical TH09 caller manifests already name `0x00401060/70` as `@cosf@4/@sinf@4`, and the Player-shot external-math family independently calls `0x00401080` as `@sqrtf@4`. This closes origin as `compiler_generated/exclude`; it deliberately adds no authored source or canonical authored exact credit.
- The earlier residual game-origin sweep had classified these three by broad placement/callgraph heuristics. To keep that sweep honestly replayable after later owner/evidence refinements, its original 387-address cohort is now frozen in `config/game-origin-review-cohort.txt`. The authored closure replay accepts the 350 still-authored members, the then-frozen 34 unknown members, and these three explicit compiler reclassifications; all five handoff origin-closure commands again report zero pending updates.


## Packet 318 shared-folded identity-body correction

- `0x004343D0-0x004343D2` is a three-byte `mov eax, ecx; ret` physical body with no uniquely recoverable logical owner. Fresh attested TH09 xrefs show many ordinary code calls together with constructor-callback/data references, while already-canonical exact caller manifests independently bind this same target VA to `Float3::operator float *`, `ReplayManagerView` construction, and multiple Player vector/identity constructor views.
- That mixed use is the same ownership problem already enforced for other folded tiny bodies: choosing one convenient source alias would overclaim a physical owner. The row is therefore corrected from the broad Packet-205 authored sweep to `unknown / review`; no maintained-source or canonical exact credit is added. Committed TH095/TH08 Float3 material only corroborates that an identity `operator float *` can fold to this shape and is not used as TH09 ownership evidence.
- This is new folded-ownership evidence, so revisiting the previously frozen unknown set is intentional rather than churn. The reviewed unresolved set is now 35 entries: 31 explicit-versus-implicit special members and four shared/folded physical bodies (`0x0042F3F0`, `0x004343D0`, `0x00435EC0`, `0x0043D2B0`). Its frozen digest is `126885e1a6a78ac42b0d81852253714cc1c9eb99141066d495b0029a16ca5695`; the authored replay selection is correspondingly 349 entries with digest `4613b2669954fe86811080cab38c7281a7ef5dbef85795ee4527004f0c7a8e6a`.
- At that checkpoint, accounting was 2,191 candidates / 986 authored / 1,170 excluded / 35 reviewed unresolved, with 811 source-present authored mappings and 666 canonical exact functions. The authored/no-source frontier is 175 functions / 78,064 bytes; exact totals are deliberately unchanged.


## Packet 319 VC7 math-runtime COMDAT origin correction

- Three small broad-sweep entries are not authored TH09 game functions: @fmodf@8 at 0x00405710-0x00405721, @acosf@4 at 0x00436AA0-0x00436AAE, and @atanf@4 at 0x00436AB0-0x00436ABD. Existing canonical authored manifests already relocate to those exact decorated symbols, independently fixing their logical runtime role.
- A plain pinned VC7.1 <math.h> caller under the repository profile automatically emits all three link-once COMDATs. Two cold target-bound rounds reproduce fmodf as 14/14 ordinary bytes plus the reviewed __CIfmod at 0x0047B73A relocation, acosf as 11/11 ordinary bytes plus __CIacos at 0x0047D280, and atanf as all 14 inline x87 bytes with no relocation. No pragma, forced register, padding, assembly, target bytes, or profile search is involved.
- Provenance follows the existing tanf at 0x0042E900 precedent: these are library / MathRuntime / exclude, not authored source and not canonical authored exact credit. scripts/review-runtime-residuals.py now target-freezes all three bodies and replays them together with the five prior runtime residuals; the closure is idempotent at 8/8 already-applied.
- The original frozen game-origin cohort remains 387 addresses, but authored replay now explicitly subtracts three compiler intrinsics, these three MathRuntime reclassifications, and the 35 reviewed-unknown entries. The authored selection at that checkpoint was 346 entries with digest 594d5f8a64e294786d1ed0880d91b2ed9f402ef04934715e233b6b3bd14cfbcb. Accounting at that checkpoint was 2,191 candidates / 983 authored / 1,173 excluded / 35 reviewed unresolved; source-present/exact remains 811/666, and the authored/no-source frontier falls honestly to 172 functions / 78,017 bytes.


## Packet 320 D3DX8 header-inline provenance correction

- `0x00401290`, `0x004012C0`, and `0x004012E0` are not authored TH09 game helpers. Fresh attested TH09 disassembly fixes them as the three-vector length, length-squared, and dot-product shapes already identified in Packet 178; the pinned VC7.1 PlatformSDK `d3dx8math.inl` directly defines `D3DXVec3Length`, `D3DXVec3LengthSq`, and `D3DXVec3Dot` with those semantics.
- Compiling the unmodified SDK header under target-backed `/Gr /O2 /Ob0 /Oy-` naturally emits three link-once COMDATs with the exact decorated D3DX symbols. Two cold target-bound rounds reproduce `D3DXVec3Length` as 36/36 ordinary bytes plus its sole relocation to `@sqrtf@4 @ 0x00401080`, `D3DXVec3LengthSq` as 31/31 bytes with no relocations, and `D3DXVec3Dot` as 21/21 bytes with no relocations. `/Ob0` is not profile fishing: the target itself calls external `@sqrtf@4`, matching the already-established external-math lowering family, while `/Ob1` intrinsically lowers the square root.
- Provenance is therefore `library / D3DX8 / exclude`, not ZUN-authored source. No SDK implementation is copied into maintained TH09 source and no canonical authored exact row is created merely because the SDK COMDAT is byte-identical. `scripts/review-runtime-residuals.py` now hash-freezes these bodies and replays them with the existing runtime-origin closure.
- The original 387-address game-origin cohort remains immutable. Authored replay now explicitly subtracts the three compiler intrinsics, three MathRuntime COMDATs, these three D3DX8 header-inline COMDATs, and the 35 reviewed-unknown entries. The authored selection at that checkpoint was 343 entries with SHA-256 `34e71ba5aea3a0f72223997bc6c8e6d24445450d5bd2053d44edd92836cc7aaa`. Accounting at that checkpoint was 2,191 candidates / 980 authored / 1,176 excluded / 35 reviewed unresolved; source-present/exact remains 811/666 and the authored/no-source frontier is 169 functions / 77,925 bytes.


## Packet 321 RNG 16-bit range leaf

- `RngRuntimeView::GetRandomU16InRange @ 0x004048B0-0x004048D8` is repository-canonical exact at 41 bytes. Fresh TH09 callsites consistently load `g_Rng @ 0x004ACE0C` into ECX and pass small 16-bit maxima, while the body calls exact `RngRuntimeView::NextU16 @ 0x0042AE20`. This independently fixes the RNG owner and the unsigned-short argument/result width.
- The natural source returns zero when `maximum == 0` and otherwise `NextU16() % maximum`. VC7.1 integer promotion naturally emits the target `MOVZX / CDQ / IDIV` sequence even though the source-level values are unsigned 16-bit; no register forcing, volatile, pragma, padding, assembly, or profile search is used.
- Two independent cold repository-defined `/Gr /O2 /Ob1 /Oy-` rounds reproduce 41/41 bytes with the sole REL32 relocation to exact `NextU16`. The same rounds keep `NextU16`, `GetRandomU32`, `GetRandomU32InRange`, `GetRandomF32InRange`, and `GetRandomF32SignedInRange` exact, so the new member does not perturb the existing RNG TU.
- This adds one honest maintained/exact authored mapping without changing origin totals: live source-present/exact becomes 812/667 and the authored/no-source frontier falls to 168 functions / 77,884 bytes.

## Packet 322 GameManager inverse coordinate pair

- `AsciiGameManagerView::InverseTransformPopupX @ 0x00442420-0x0044244D` and `InverseTransformPopupY @ 0x00442450-0x0044247D` are repository-canonical exact at 46 bytes each. TH09 callers at `0x00442B57` / `0x00442B67` explicitly load `g_GameManager @ 0x004A7D90` into ECX before calling the pair, independently fixing the receiver and member ABI.
- The target pair is the algebraic inverse of the already-exact `TransformPopupX/Y @ 0x00401680/0x004016B0`: each converts the unsigned coordinate at the object referenced by `0x004B3448` using the VC7.1 `2^32` correction path, then computes `value - renderOrigin + g_GameManager.coordinateOffsetX/Y` at `+0x350/+0x354`. The maintained names are neutral behavior descriptions; no adjacent-game symbol name is imported.
- Ordinary member source in the existing coordinate view reproduces both targets naturally under the repository `/Gr /O2 /Ob1 /Oy-` profile. Two independent cold canonical rounds reproduce 46/46 bytes for both inverse helpers with exactly two reviewed DIR32 relocations (`g_RenderCoordinateOrigin` and `__real@4f800000`); the same rounds keep both existing forward transforms exact.
- No register forcing, volatile qualification, assembly, padding, or profile search is involved. This adds two honest maintained/exact authored mappings while leaving origin totals unchanged.

## Packet 323 EnemyManager immediate spawn wrapper

- `EnemyManagerView::SpawnEnemyUsingOpposingEcl @ 0x004417C0-0x004417E5` is repository-canonical exact at 38 bytes. Its sole TH09 caller at `0x00441AE1` loads ECX from `g_GameManager + side*0x38 + 0x10`, independently fixing the receiver as the side-local `EnemyManagerView` before passing six stack arguments.
- The wrapper narrows the public integer subroutine and item-drop arguments to the target `short` / `signed char` ABI, forwards the supplied position/life/score/initial-variable block to `EnemyManagerView::SpawnEnemy @ 0x0040F340`, and appends `useOpposingEclManager = 1`; reconstruction of `0x0040F340` proves this selects the opposing ECL manager at `EnemyManager +0x188`, not an immediate-run flag. Existing TH09 `EclRunRemote` evidence independently binds `0x0040F340` to the ECL-aware spawn path that copies the 0x78-byte initial-variable/context block.
- The complete target body is instruction-identical to the already-exact `EnemyManagerCardAttackSpawnView::SpawnFromCardAttack @ 0x00440C80` convenience wrapper except for the callee: that sibling routes to the separate mirrored spawn owner `0x0040F1D0`. This packet used the shared natural source shape only as wrapper-ABI evidence; Packet 411 later closes the mirrored owner independently against TH09.
- Two independent cold repository-defined `/Gr /O2 /Ob1 /Oy-` rounds reproduce 38/38 bytes with the sole REL32 relocation solved to `0x0040F340`; both rounds also keep the 38-byte CardAttack sibling exact. No register forcing, padding, assembly, volatile steering, or profile search is used.

## Packet 324 ExAttack type-2 update callback

- `ExAttackUpdateCallbackType2 @ 0x00441A70-0x00441AF8` is repository-canonical exact at 137 bytes. The already-proven `g_ExAttackTemplates @ 0x004A0F78` is a four-callback-per-type table; slot `0x004A0F9C` is type 2's update column and points uniquely to this body under the established `int __fastcall(ExAttackRecord *)` ABI.
- TH09 type-2 initialize callback `0x00441910` and the maintained allocation helper independently constrain record `+0x34` as a pointer to a 0x4C private extra block. While extra state `+0x00` is zero, the update callback evaluates record timer `+0x10` through exact `ZunTimer::operator float`, sends record position `+0x20` plus four 2D control points to target helper `0x0042AF80`, and tests exact `ZunTimer::operator>(90)`.
- On expiry the callback allocates a natural 0x78-byte local initial-variable block, writes only dword `+0x20` to target bits `0xBFC90FDB`, obtains the opponent side's `EnemyManagerView` from `g_GameManager + side*0x38 + 0x10`, and calls exact `SpawnEnemyUsingOpposingEcl @ 0x004417C0` with subroutine 8, the extra block's `+0x40` position, `(20,-2,1000)`, and that local context. The rest of the local context is deliberately left uninitialized because the target initializes no other bytes before the spawn call.
- The natural nested control flow (`state == 0`, then `timer > 90`, then common final zero return) reproduces the target's block layout. Two cold canonical rounds are 137/137 with five reviewed relocations to `0x004014F0`, `0x0042AF80`, `0x00403DE0`, `g_GameManager + 0x10`, and `0x004417C0`. At Packet 324 the interpolation helper remained independently no-source/non-exact; Packet 325 below closes it from its own source and replay evidence.

## Packet 325 ExAttack 2D Hermite interpolation helper

- `ExAttackInterpolate2D @ 0x0042AF80-0x0042B00D` is repository-canonical exact at 142 bytes. Nineteen TH09 code xrefs come from the ExAttack callback band, and exact `ExAttackUpdateCallbackType2` independently fixes the `/Gr` fastcall boundary: output in ECX, one endpoint in EDX, then three point/tangent pointers plus `time` and `duration` on the stack (`ret 0x14`). No caller consumes a meaningful return value, and the exact maintained contract is `void`.
- TH09 target math normalizes `t = time / duration`, computes the standard cubic Hermite basis `(t-1)^2(2t+1)`, `t^2(3-2t)`, `(1-t)^2 t`, and `(t-1)t^2`, then evaluates the output x/y from the two endpoints and two tangents. This formula and argument order are established from TH09 disassembly/decompilation plus the exact caller before consulting adjacent games.
- Straight VC7.1 source using ordinary `float *` arguments and four local weights reproduces all 142 bytes on the first probe. Two cold canonical rounds remain 142/142 with only four DIR32 relocations to the target 1.0f / 3.0f constants (`0x0048E2A4` / `0x0048E2A0`); the exact type-2 caller remains 137/137 after sharing the `void` declaration.
- Clean TH095 source corroborates the same Hermite basis but uses a `#pragma var_order` workaround in its own different target. TH09 does not import that pragma or any register/volatile/assembly/padding/profile steering: the repository's ordinary `/O2 /Ob1 /Oy- /Gr` profile emits the TH09 target naturally.

## Packet 326 Player reward callback table index 1

- `PlayerOwnerRewardCallbackType1 @ 0x00441B00-0x00441B63` is repository-canonical exact at 100 bytes. TH09 `PlayerLifecycleView::AddedCallback` indexes the 16-entry table at `0x004A19D0` by `sideState->shotType20` and stores the selected pointer in `ownerState30410.callback40`; index 1 at `0x004A19D4` points uniquely to this body.
- The maintained `PlayerOwnerStateView::ApplyReward` target path invokes callback40 with ECX=the 0x44-byte owner-state and EDX=the reward position. The callback's target body itself explicitly returns integer zero, so the function-specific maintained signature is `int __fastcall(PlayerOwnerStateView *, PlayerPositionView *)`; the broader non-exact ApplyReward TU currently ignores the return value, and its provisional pointer typedef is not used as exactness evidence.
- The callback loops while owner-state `+0x34 >= 3*(40-g_PlayerRewardBaseValue)`, calls `g_ExAttackController->Spawn(2, position, owner->sideIndex, 0)`, then adds `4*g_PlayerRewardBaseValue-120` to `+0x34`. Names beyond target-observed owner/value offsets remain private-neutral; `Type1` denotes callback-table index 1 only.
- The first ordinary `/Gr /O2 /Ob1 /Oy-` typed probe is structural-exact. Two cold canonical rounds reproduce 100/100 bytes with five reviewed relocations: three to `g_PlayerRewardBaseValue @ 0x004A7E44`, one to `g_ExAttackController @ 0x004A7E3C`, and one to `ExAttackController::Spawn @ 0x004151F0`. The latter remains independently source-present/non-exact; an exact Enemy spawn leaf sharing that relocation also regresses exact.

## Packet 327 Player reward callbacks type 2-4

- The same TH09 `g_PlayerCallback30450 @ 0x004A19D0` family now closes indices 2-4: `PlayerOwnerRewardCallbackType2 @ 0x00442EE0` (97 bytes), `Type3 @ 0x00443790` (85 bytes), and `Type4 @ 0x004444F0` (101 bytes). `PlayerLifecycleView::AddedCallback` selects the table by shot type, and `PlayerOwnerStateView::ApplyReward` fixes the callback ECX/EDX ABI; no address adjacency is used as ownership evidence.
- All three use the same target-proven private owner-state overlay and accumulator at `+0x34`. Type2 loops at `2*(45-base)` and adds `2*base-90` while spawning ExAttack type 3; Type3 loops at `70-3*base`, subtracts 70, and spawns type 13; Type4 loops at `5*(28-base)` and adds `5*base-140` while spawning type 14. Every spawn uses the supplied position, `owner->sideIndex`, and parameter 0, then each callback returns integer zero.
- Straight ordinary `while` source in the shared callback TU is exact on the first pinned-VC7.1 probe for all three. Two independent cold canonical rounds keep Type1 100/100 and reproduce Type2 97/97, Type3 85/85, and Type4 101/101. Their only relocations are the already-reviewed reward-base global, ExAttack controller global, and `ExAttackController::Spawn @ 0x004151F0`; the larger Spawn implementation remains independently non-exact.
- `TypeN` continues to mean callback-table index only. No original gameplay labels, adjacent-game names, register forcing, padding, volatile tricks, assembly, pragmas, or profile search are introduced.

## Packet 328 Player reward callbacks type 5-7

- `g_PlayerCallback30450` indices 5-7 are now repository-canonical exact under the same TH09-proven Player owner-state/position fastcall ABI: `PlayerOwnerRewardCallbackType5 @ 0x00445330` is 102 bytes, Type6 `@ 0x00445930` is 101 bytes, and Type7 `@ 0x00445D80` is 119 bytes.
- Type5 loops while state `+0x34 >= 25-base/2`, spawns ExAttack type 5, and applies `base/2-25`; Type6 uses threshold `100-3*base`, spawns type 15, and applies `5*base-140`; Type7 uses threshold `200-3*base`, spawns type 6, applies `3*base-200`, then decrements target-observed state `+0x30` by five with a zero clamp. The private callback overlay expands only enough to expose neutral `value30` and `value34` fields.
- Each callback's first ordinary pinned-VC7.1 probe is structural-exact. Two cold canonical rounds rebuild the shared TU and keep all seven recovered callbacks exact, including the earlier Type1-4 regressions. Type5/6/7 reproduce 102/102, 101/101, and 119/119 respectively; relocation ownership remains limited to the known reward-base global, ExAttack-controller global, and non-exact-but-source-present Spawn boundary.
- `TypeN` remains a table-index label only. No adjacent-game naming, register forcing, volatile/padding/assembly tricks, pragmas, or profile search are used.

## Packet 329 Player reward callbacks type 8-15

- The recoverable remainder of TH09 `g_PlayerCallback30450 @ 0x004A19D0` now closes naturally: table indices 8-15 map to `PlayerOwnerRewardCallbackType8 @ 0x00447A80` (98 bytes), Type9 `@ 0x00448640` (97), Type10 `@ 0x00449780` (101), Type11 `@ 0x0044A670` (101), Type12 `@ 0x0044AF80` (101), Type13 `@ 0x0044BA80` (101), Type14 `@ 0x0044C020` (101), and Type15 `@ 0x0044C680` (98). The same TH09 `AddedCallback` table selection and `ApplyReward` ECX/EDX call path establish ownership and ABI for every entry.
- All eight remain the same target-proven `state +0x34` accumulator family. Their `(threshold, ExAttack type, step)` triples are respectively `(100-3*base,16,3*base-100)`, `(2*(30-base),17,2*base-60)`, `(4*(25-base),18,4*base-100)`, `(5*(28-base),19,5*base-140)`, `(4*(40-base),20,4*base-160)`, `(5*(28-base),21,5*base-140)`, `(100-3*base,25,5*base-140)`, and `(80-3*base,26,3*base-80)`. Every callback spawns at the supplied position for `owner->sideIndex`, parameter zero, then returns integer zero.
- Every first ordinary pinned-VC7.1 probe is structural-exact. After replacing the probe macro with explicit maintained functions, two cold canonical rounds rebuild the shared TU and reproduce all fifteen recovered candidate callbacks Type1-15 exactly, totaling 1,503 bytes per round. The eight new entries contribute 798 exact authored bytes; relocations remain only the reviewed reward-base global, ExAttack-controller global, and `ExAttackController::Spawn @ 0x004151F0` boundary.
- The table's index-0 value `0x004412E0` is not a separate function candidate in the TH09 ledger/IDA boundary model. It is deliberately not manufactured into source merely to make the table look complete. `TypeN` names continue to denote table indices only; no adjacent-game labels or code-generation steering are used.

## Packet 330 D3DXVec3Cross SDK-inline provenance correction

- `0x0042E920-0x0042E972` is not ZUN-authored game source. A pre-existing canonical TH09 caller relocation already names the target `D3DXVec3Cross`, and the pinned VC7.1 PlatformSDK `d3dx8math.inl` directly defines the standard three-component cross product with a temporary vector for alias safety.
- Compiling that unmodified SDK header under the target-backed `/Gr /O2 /Ob1` profile naturally emits the link-once COMDAT `?D3DXVec3Cross@@YIPAUD3DXVECTOR3@@PAU1@PBU1@1@Z`. Two independent cold target-bound comparisons are 83/83 exact with no relocations; the target body digest is `551a67f468b441cda9b9a497588f0a54bf40b148e3af2329b9ef981d6eceeddf`.
- The runtime-origin closure now classifies this candidate as `D3DX8 / library / exclude` under its own `d3dx8-cross-header-inline-comdat-review-2026-09-21` evidence id. The frozen authored-sweep replay explicitly subtracts all four D3DX header-inline reclassifications and has selection digest `d2dee848a8123f23c8359907331139dba17f5f8f01795585e1021358117ba144`.
- No SDK body is copied into maintained TH09 source and no authored exactness credit is awarded. This is a provenance correction only: authored/excluded changes by one, while source-present/exact counts remain unchanged.

## Packet 331 Player position type-11 geometry seam

- `g_PlayerCallback30404 @ 0x004A1A50` index 11 (`0x004A1A7C`) points uniquely to `PlayerPositionCallback30404Type11 @ 0x00449D50`; the existing exact Player AddedCallback/invoke path fixes the callback ABI as ECX=Player, EDX=position. Its sole private helper is `PlayerType11OrientationSign @ 0x00449B50-0x00449B96`.
- The private helper evaluates the signed 2D orientation determinant and returns `+1`, `-1`, or `0`. Ordinary file-local `static` source naturally makes VC7.1 select the target private EAX/EDX/ECX register ABI; there is no explicit calling-convention or register forcing. Two cold canonical `/Ob0` rounds reproduce 71/71 bytes with exactly two DIR32 relocations to the shared 0.0f constant at `0x0048E314`.
- The maintained Type11 callback source subtracts Player position `+0x1B88`, constructs ten 12-byte points, builds five radius/angle directions separated by `1.2566371f`, derives the five interleaved golden-ratio points with `1.618f`, then performs the target's three groups of three orientation tests. The five direction calculations retain the existing `Float3::FromAngleMagnitude @ 0x00441890` boundary and exact `AddNormalizeAngle @ 0x0042AED0`; no attempt is made to reopen the durable FSINCOS negative.
- The target local point array is constructed through VC7.1 vector-constructor helper `??_H @ 0x00401470`, passing physical callback `0x004343D0`. The exact Player constructor independently exhibits this same `/Ob0` lowering. Switching this Type11 TU from `/Ob1` to the target-supported `/Ob0` changes the natural caller from 735 bytes with an in-function constructor loop to 725 bytes with the correct `??_H` call, versus target 732.
- Exact pursuit stops there. The remaining target stack/temporary lifetime layout is `0xDC` versus natural `/Ob0` `0xAC`, and adjacent TH095/TH10 source contains no matching 1.618/pentagon family to justify explicit temporary reconstruction. Physical `0x004343D0` remains the frozen shared/folded ownership unknown; it is not reclassified or defined merely to force code generation. Type11 therefore gains honest source presence only, while the 71-byte orientation helper gains independent exact credit.

## Packet 332 Player state-3 expanding-circle effect pair

- `PlayerStateEffectView::ConfigureState3Effect13 @ 0x00403C30-0x00403C8A` and `ConfigureState3Effect14 @ 0x00403C90-0x00403CEA` are repository-canonical exact at 91 bytes each. Multiple TH09 Player state callers pass the Player object in ECX, independently fixing member ownership; the two targets are instruction-identical apart from lifetime/timer `48` versus `64` and effect id `13` versus `14`.
- Each helper uses Player position `+0x1B88` to create an exact type-1 circle and type-4/value-2 circle, then sets update state 3, assigns `ZunTimer +0x303C8`, and returns the side-local `EffectManager::SpawnEffect` result with count 1 and color `0xFFFFFFFF`. Reading the already-exact `CreateCircleType1/4` callee ABI establishes the important semantics: the target pushes radius `0.0f` and radius growth `4.0f`, so these are expanding circles rather than fixed radius-four circles.
- Straight `/Gr /O2 /Ob1 /Oy-` source reproduces both bodies naturally. Two cold canonical rounds keep each function 91/91 with five reviewed REL32 relocations to exact `CreateCircleType1 @ 0x0041CFE0`, exact `CreateCircleType4 @ 0x0041D0D0`, exact `PlayerState4OpsView::SetUpdateState @ 0x00403C20`, exact `ZunTimer::operator= @ 0x00401500`, and exact `EffectManager::SpawnEffect @ 0x0040CC00`.
- The initial probe had radius/radiusGrowth reversed and therefore matched only 57/71 comparable bytes despite the correct total size and callees. Correcting that semantic argument order from TH09-local callee evidence immediately closes both targets; no register, padding, assembly, volatile, pragma, or compiler-profile steering is involved.

## Packet 333 foreground game-window activation helper

- `ActivateGameWindow @ 0x0042D0C0-0x0042D13B` is repository-canonical exact at 124 bytes. Its sole TH09 caller creates the HWND, stores it in the game-window globals, pushes that HWND as the only argument, then calls this helper; the callee returns with `ret 4` and has no member receiver dependency, so the maintained boundary is a behavior-neutral free `__stdcall(HWND)` function.
- The target gets the current foreground window thread and the new game-window thread, temporarily attaches their input queues, saves `SPI_GETFOREGROUNDLOCKTIMEOUT`, sets the timeout to zero, activates the game HWND, restores the saved timeout, and detaches the input queues. The exact target imports are `GetForegroundWindow`, `GetWindowThreadProcessId`, `AttachThreadInput`, `SystemParametersInfoA`, and `SetActiveWindow`.
- Straight Win32 source under the repository pinned VC7.1 `/Gr /O2 /Ob1 /Oy-` profile reproduces the complete body naturally. Two cold canonical rounds are 124/124 with six reviewed DIR32 relocations to the TH09 USER32 IAT slots at `0x0048E214..0x0048E224`; `AttachThreadInput` appears at both attach/detach callsites.
- Clean TH095 contains the same API sequence under `GameWindow::ActivateWindow(HWND)`, but that adjacent-game member identity is used only as source-shape corroboration after TH09 independently fixes the argument, imports, order, and return cleanup. TH09 keeps the neutral free-function name and does not import adjacent ownership.

## Packet 334 scalar cubic-Hermite helper

- `CubicHermiteInterpolateScalar @ 0x00401000-0x0040105A` is repository-canonical exact at 91 bytes. TH09 exposes a five-float stack ABI with `ret 0x14`; its sole caller family at `0x004016E0` invokes it three times. The target x87 data flow independently identifies the standard four cubic-Hermite basis weights already recovered in TH09 ECL and exact ExAttack interpolation code.
- Ordinary scalar source first computes `startWeight=(t-1)^2(2t+1)`, `endWeight=t^2(3-2t)`, `startTangentWeight=(1-t)^2t`, and `endTangentWeight=(t-1)t^2`, then forms the four-term weighted sum. This natural four-local formulation reproduces the target x87 evaluation order; a more aggressively fused expression compiled to 87 bytes and was rejected rather than steered.
- Two cold canonical `/Gr /O2 /Ob1 /Oy-` rounds reproduce 91/91 bytes. The only relocations are three uses of the shared `1.0f` constant at `0x0048E2A4` and one `3.0f` constant at `0x0048E2A0`.
- Clean TH095 contains the same scalar Hermite formula but requires a target-specific `#pragma var_order` for its own binary. TH09 does not use or import that pragma: its target is exact with ordinary local variables and no register forcing, volatile, assembly, padding, or compiler-profile search.

## Packet 335 Background pending stage-label resolver

- `Background::ApplyPendingStageLabel @ 0x004018A0-0x004018EE` is repository-canonical exact at 79 bytes. Its sole TH09 caller is `Background::OnUpdate @ 0x00402350`, independently fixing member ownership and ECX receiver ABI.
- Existing TH09 Background layout fixes `stageScript @ +0x28`, `stageScriptTimer @ +0xC58`, and `stageScriptInstructionIndex @ +0xC64`. This helper gives former neutral field `+0x18` a target-backed role: when nonzero it is the pending stage-label id. The helper resets the instruction index, scans 0x14-byte `RawStageInstr` entries until `opcode == 31 && args[0] == pending`, stops on frame `-1`, and on success advances the index to the following entry, assigns the matching frame to the exact timer boundary, then clears the pending id.
- The first ordinary `/Gr /O2 /Ob1 /Oy-` member probe is structural-exact. Two cold canonical rounds reproduce 79/79 bytes with the sole REL32 relocation to exact `ZunTimer::operator=(int) @ 0x00401500`.
- The maintained `pendingStageLabel18` name is intentionally distinct from the already-present far-tail `pendingStageScriptLabel` field; no equivalence between those two storage locations is claimed. No adjacent-game naming, register forcing, padding, volatile, assembly, pragma, or profile search is used.

## Packet 336 Supervisor DirectPlay packet send

- `SupervisorNetworkState::SendPacket @ 0x004323B0-0x0043240E` is repository-canonical exact at 95 bytes. Its only TH09 caller family is `SupervisorServiceUpdate`, which invokes the method on `g_SupervisorNetworkState` with `(packet, sizeof(packet))`; maintained handshake/frame packet callsites independently fix the two stack arguments.
- Existing TH09 state layout and exact `GetRemotePeerId` fix `peerIds[2] @ +0x1C/+0x20`, `active @ +0xA8`, `localSide @ +0xAC`, and `sendAsyncHandle @ +0x14`. The method returns zero unless active and a remote peer exists, constructs the SDK `DPN_BUFFER_DESC { size, data }`, then calls `IDirectPlay8Peer::SendTo` for `peerIds[1-localSide]` with one buffer, zero timeout/context, the async-handle address, and flags `0x9C`.
- The target keeps a pointer to the selected peer-id slot through the null check and dereferences it only immediately before `SendTo`. A natural eager-value spelling emitted 92 bytes; retaining the semantically meaningful slot pointer produces the exact 95-byte target without register/volatile/pragma steering.
- Two independent cold canonical `/Gr /O2 /Ob1 /Oy-` rounds reproduce 95/95 bytes with no relocations. DirectPlay8 ABI and vtable layout come from the pinned VC7.1 Platform SDK `dplay8.h`, not a hand-written vtable.

## Packet 337 Supervisor final DirectPlay resource release

- `SupervisorNetworkState::ReleaseDirectPlayResources @ 0x00432340-0x004323A0` is repository-canonical exact at 97 bytes. TH09 WinMain is its sole caller: after a distinct network cleanup boundary `0x00432F70`, it passes `g_SupervisorNetworkState` in ECX, calls this method, frees the 0xD4 `NetworkInf` allocation, then clears the global pointer. This proves final-object resource-release ownership and distinguishes the function from the session-reset member used repeatedly during gameplay.
- The method calls `IDirectPlay8Peer::Close(0)` when peer `+0x00` exists, Release/nulls COM pointers at `+0x04`, `+0x0C`, and `+0x10`, then Release/nulls the peer itself. Existing `deviceAddress +0x04` and `hostAddress +0x10` names remain; the newly exposed `+0x0C` slot stays neutral as `unknown00C` because TH09 proves only IUnknown-style lifetime, not a stable higher-level role.
- A minimal pinned-VC7.1 DirectPlay8 SDK overlay reproduces the complete target on the first probe. The maintained class source also cold-replays twice at 97/97 bytes with no relocations; SDK interface declarations supply the `Close` and `Release` vtable slots naturally.
- The maintained method is `void`. VC7.1 naturally leaves the final COM `Release` result in EAX exactly like target machine code, so no artificial return contract, register forcing, inline assembly, padding, or profile search is introduced.

## Packet 338 Player state-3 effect 15 setup

- `PlayerStateEffectView::ConfigureState3Effect15 @ 0x00403CF0-0x00403D53` is repository-canonical exact at 100 bytes. Three TH09 Player callers pass the Player receiver in ECX, independently fixing member ownership; the body is the third member of the already-exact `ConfigureState3Effect13/14` family.
- The method creates type-1 and type-4 collision circles centered at Player position `+0x1B88`, with radius `0.0f`, growth `3.5f`, lifetime `128`, delay `0`, and type-4 value `2`; it sets update state `3`, assigns `timer303C8 = 128`, then returns side-local `EffectManager::SpawnEffect(15, position, 1, -1)`.
- Relative to exact effect13/14, the 100-vs-91 byte increase is completely explained by three occurrences of `128`: VC7.1 encodes each as `push imm32`, whereas lifetimes/timers 48 and 64 use `push imm8`. The only other target-observed changes are collision growth `4.0f -> 3.5f` and effect id `13/14 -> 15`.
- Two cold canonical rounds reproduce 100/100 bytes and all five reviewed REL32 relocations (`CreateCircleType1`, `CreateCircleType4`, `SetUpdateState`, `ZunTimer::operator=`, and `EffectManager::SpawnEffect`). The same rounds keep effect13/14 exact. No register forcing, padding, assembly, volatile, pragma, or profile steering is used.

## Packet 339 GameWindow timestamp helper

- `GameWindow::GetTimestamp @ 0x0042CC90-0x0042CD0A` is repository-canonical exact at 123 bytes. Packet 365 supplies the caller evidence that was missing when this function was first closed: `GameWindow::Render @ 0x0042DC40` calls it twice; at the first call ECX is still the Render receiver preserved into ESI, and before the second call target explicitly executes `mov ecx, esi`. This distinguishes member-call syntax from the earlier neutral free-function interpretation even though the callee body itself never consumes ECX.
- The target reads performance frequency from absolute `g_GameWindow +0x14` (`0x004B30C4`), not through `this`. When nonzero it calls `QueryPerformanceCounter` and returns unsigned `LowPart / frequency` as double using the shared double `2^32` correction. When zero it brackets `timeGetTime` with `timeBeginPeriod(1)` / `timeEndPeriod(1)` and returns seconds.
- The fallback path is intentionally float-precision before the double return: `(float)timeGetTime() * 0.001f`. A direct double cast produces the same 123-byte layout but differs in exactly two x87 operand-size opcode bytes (`DC` versus target `D8`) because it selects qword `2^32`/0.001 constants. The float expression naturally selects target dword constants `0x0048E2A8` and `0x0048FBE8` and closes both bytes.
- Two cold canonical `/Gr /O2 /Ob1 /Oy-` rounds reproduce 123/123 bytes with eleven reviewed relocations. Clean TH095 corroborates only the `GameWindow::GetTimestamp` source family after TH09 provides the decisive member-call evidence; its larger GameWindow layout is not imported.

## Packet 340 DirectPlay error reporter

- `ReportDirectPlayError @ 0x00432410-0x004324B3` is repository-canonical exact at 164 bytes. Five TH09 netplay callers pass the HRESULT directly in ECX and ignore the return, fixing a free `__fastcall(HRESULT)` reporting boundary rather than a stateful member.
- The helper first requests an allocated system message through `FormatMessageA` with flags `FORMAT_MESSAGE_ALLOCATE_BUFFER | FORMAT_MESSAGE_FROM_SYSTEM | FORMAT_MESSAGE_IGNORE_INSERTS` (`0x1300`) and language `0x400`. When present it passes that string to exact `GameErrorContext::Fatal`, frees it through `LocalFree`, and returns. Otherwise it copies excluded-library `DXGetErrorString8A(error)` into a 2048-byte stack buffer, appends CRLF, and reports the result through the same Fatal boundary.
- The `LPSTR` output local is deliberately not initialized before `FormatMessageA`, matching the target's actual contract. An initial `= NULL` probe emitted a real seven-byte store and changed VC7.1 intrinsic-loop alignment. Removing that extra source semantics naturally gives the target copy-loop layout; no padding or alignment directive is used.
- Two cold canonical `/Gr /O2 /Ob1 /Oy- /Oi` rounds reproduce 164/164 bytes with nine reviewed relocations covering USER32/KERNEL32 imports, exact `GameErrorContext::Fatal`, excluded DXErr8 `DXGetErrorString8A`, and the shared CRLF literal.

## Packet 341 DirectPlay service-provider check source seam

- `SupervisorNetworkState::IsServiceProviderAvailable @ 0x00432770-0x00432836` is now source-present under TH09-local SupervisorNetwork ownership, but exactness is deliberately not claimed. Its only callers are the peer-create/initialize helper `0x00432EB0` and the session reset `0x00432F70`; both pass the TCP/IP service-provider GUID address as the sole stack argument.
- The target performs the standard DirectPlay8 two-pass `EnumServiceProviders` sequence against `CLSID_DP8SP_TCPIP`: zero count/size, expect `DPNERR_BUFFERTOOSMALL`, allocate the returned size through exact `ZunMemory::Alloc` using `./system\\global.h`, enumerate again, map an empty result to `E_FAIL`, free the temporary provider buffer, and return `SUCCEEDED(hr)`. The target English diagnostic is `Failed Enumerating Service Providers:  0x%x\r\n`; the Japanese Fatal path is `サービスプロバイダ列挙失敗:\t0x%x\r\n`.
- Public DirectPlay SDK tutorial source independently corroborates this two-pass enumeration shape after TH09 fixes the actual GUID, callers, allocation family and diagnostics. The maintained source follows that ordinary SDK shape and cold-compiles to 206 bytes versus the 199-byte target. A separate natural form caching the peer pointer emits 202 bytes.
- The remaining residual is preserved-register/zero-local lowering: target keeps `this` in EDI, HRESULT in ESI and the allocated buffer in EBX while sharing one zero register across initial locals and NULL API arguments; retained natural VC7.1 forms choose different preserved-register allocation and literal-zero lowering. No declaration-order fishing, register forcing, volatile, assembly, padding, pragma, or profile search is retained.

## Packet 342 DirectPlay session reset

- `SupervisorNetworkState::ResetSession @ 0x00432F70-0x00433037` is repository-canonical exact at 200 bytes. Five TH09 callers independently bind the role: WinMain invokes it before final DirectPlay resource release, `SupervisorServiceUpdate` invokes it on both connection-reset paths, and `Supervisor::OnUpdate` invokes it on both gameplay reset paths.
- The method cancels all DirectPlay async operations with `DPNCANCEL_ALL_OPERATIONS`, closes the peer, Release/nulls COM address slots `+0x04/+0x0C/+0x10`, sleeps 100 ms, and reinitializes the same peer with exact `SupervisorNetworkMessageThunk @ 0x00432EA0`. Initialization failure reports the target Japanese diagnostic, calls exact `ReportDirectPlayError @ 0x00432410`, and returns 1.
- After successful Initialize, the method calls maintained `IsServiceProviderAvailable @ 0x00432770` with `CLSID_DP8SP_TCPIP`. Provider failure emits the target `CLSID_DP8SP_TCPIP 使用不可\r\n` Fatal and returns 1; success clears both peer-id slots `+0x1C/+0x20` and returns 1. The provider helper remains independently source-present/non-exact; this caller receives exactness only from its own target-bound relocation.
- Natural source closure required two semantic/control-flow corrections, not encoding steering: do not cache `this->peer` across the early COM calls because target reloads it, and express provider failure as a guard-return so the failure block precedes peer-id clearing. With those forms, two cold canonical rounds reproduce 200/200 bytes and all eleven relocations; exact message-thunk and error-reporter regressions also remain exact. The SDK callback is adapted with an ABI-neutral function-pointer cast so the relocation resolves to the repository's existing `int`-return exact thunk symbol.

## Packet 343 Supervisor D3D capability-line private ABI negative

- `AppendD3DCapabilitySupportLine @ 0x0042CD40-0x0042CD89` is owner/semantics-resolved but deliberately remains source-absent and non-exact. Its sole TH09 owner `0x0042CD90` calls it 33 times while rendering D3D capability bits; the caller pushes `mask` then `flags`, passes the output cursor in ECX and the per-capability format string in EAX, and consumes the returned advanced cursor.
- The target first appends `sprintf(buffer, format)`, then appends CP932 `不可\r\n` when `(flags & mask) == 0` or `可\r\n` otherwise. A same-TU file-local ordinary VC7.1 helper naturally reproduces the target-private ECX/EAX transport, exact 74-byte extent, all three `_sprintf` calls, both target string relocations, and 52/54 comparable bytes.
- The only residual is two ModRM bytes before the commutative `test`: target loads flags from `[ebp+8]` then mask from `[ebp+0xC]`, while natural `flags & mask`, `mask & flags`, and by-value `flags &= mask` forms all load `[ebp+0xC]` then `[ebp+8]`. Caller disassembly independently fixes the actual stack argument order, so this is not a signature ambiguity.
- Further pursuit would be register/load-order or encoding-directed source steering. No explicit register ABI, volatile trick, pragma, assembly, padding, or profile search is retained; exact/source credit remains absent.

## Packet 344 Enemy movement-bound position clamp

- `EnemyView::ClampPositionToMovementBounds @ 0x004101D0-0x0041024E` is repository-canonical exact at 127 bytes. TH09 `RunEcl` and `EnemyManager::OnUpdate` call the helper on Enemy receivers, and existing ECL movement reconstruction independently fixes position `+0x2D74`, `ENEMY_CLAMP_POSITION = 0x00010000`, lower bounds `+0x3398/+0x339C`, and upper bounds `+0x33A0/+0x33A4`.
- The helper exits unless the clamp flag is enabled, obtains the position array through the existing `Float3::operator float*` boundary, then clamps x and y independently to the target-proven lower/upper bounds. The physical identity body at `0x004343D0` remains the frozen shared/folded ownership unknown; this exact caller only records a relocation to that boundary and does not reclassify it.
- A semantically equivalent 32-bit `flags337C & 0x00010000` spelling naturally emitted 130 bytes because VC7.1 used a full dword immediate test. TH09 target uses `test byte ptr [this+0x337E],1`; exposing the same already-proven flag as the natural byte view `+0x337E bit0` reduces the candidate to the exact 127-byte body.
- Two independent cold canonical `/Gr /O2 /Ob1 /Oy-` rounds reproduce 127/127 bytes with the sole REL32 relocation solved to `0x004343D0`. No register forcing, assembly, padding, volatile, pragma, or profile search is used.

## Packet 345 Enemy attached-effect updater

- `EnemyCoreUpdateAttachedEffects @ 0x0040F4C0-0x0040F556` is repository-canonical exact at 151 bytes. `EnemyManagerView::OnUpdate` is the sole TH09 caller, and its maintained source already routed each active Enemy through this boundary. Existing TH09 ECL/Enemy overlays independently fix `attachedEffects53B4[24]`, `attachedEffectCount5414`, `attachedEffectDistance5418`, and Enemy position `+0x2D74`.
- For each non-null attached effect, the helper copies Enemy position into effect `+0x48..+0x50`, increments effect float `+0x7C` by `0.3f` while it is below the Enemy `+0x5418` cap and otherwise clamps it to that cap, then advances effect angle `+0x80` through exact `AddNormalizeAngle @ 0x0042AED0` by `0.031415928f`.
- Defining the helper file-local in the real `EnemyManagerCore.cpp` translation unit naturally makes VC7.1 select the target-private EDI entry transport; no explicit calling convention or register mechanism is used. The natural `if (distance < max) increment; else clamp` form reproduces the target x87 `test ah,5 / jp` block order. The inverse `>=` spelling kept the same 151-byte extent and EDI transport but changed only the floating status test/block order, so it was rejected.
- Two independent cold canonical `/Gr /O2 /Ob1 /Oy-` rounds reproduce 151/151 bytes with two reviewed relocations: `0.3f @ 0x0048E4B4` and exact `AddNormalizeAngle @ 0x0042AED0`. Rebuilding the same TU leaves the maintained non-exact `EnemyManagerView::OnUpdate` candidate at its previous 3,156 bytes versus the 3,883-byte target.

## Packet 346 Enemy ECL raw-file loader

- `EnemyEclManagerView::Load @ 0x00405660-0x004056F9` is repository-canonical exact at 154 bytes. `EnemyManager_AddedCallback` is the only TH09 caller family and invokes it on the two adjacent 0x188-byte ECL-manager objects: the primary manager with literal `enemy.ecl`, then the side/character-specific manager selected by the current game-side record.
- TH09 independently fixes the raw-file layout through exact accessors: signed short subroutine count is `rawFile +0x06`, and the subroutine pointer table starts at `rawFile +0x08`. The loader opens through exact `FileSystem::OpenFile @ 0x0042C970`, requires version `0x900`, rebases all subroutine offsets in place, derives manager `subroutineTable04` as the table immediately following those entries, then rebases that second table using the signed short count at raw-file `+0x04`.
- The first typed-local source cached raw-file/table pointers and naturally emitted 133 bytes. Expressing both rebase loops directly through `this->rawFile00` and `this->subroutineTable04`, matching the target's observable member-reload semantics, naturally emits the full 154-byte target. No register forcing, volatile, pragma, padding, assembly, or profile search is used.
- Two cold canonical `/Gr /O2 /Ob1 /Oy-` rounds reproduce 154/154 bytes with five reviewed relocations: exact `FileSystem::OpenFile @ 0x0042C970`, target ECL load/version error strings at `0x0048E400/0x0048E3DC`, `g_GameErrorContext @ 0x004B1020`, and exact `GameErrorContext::Log @ 0x0042C5C0`. Exact `GetSubroutineCount @ 0x0040F160` and `GetSubroutine @ 0x0040F170` remain exact in regression. Clean TH095 only corroborates the two-table in-place-rebase source family after TH09 fixes its own version/layout.

## Packet 347 Front message-runtime initialization frontier

- `FrontMessageOwnerView::InitializeMessageRuntime @ 0x004181E0-0x00418264` is owner/ABI/semantics-resolved and now maintained source-present/non-exact. TH09 `FrontInf` construction/added-callback evidence fixes the 0x1293C Front owner and its 0x1D70 message runtime at `Front +0xE944`; `GameManager_CleanupGameplayState` independently loads the same Front/shared-runtime global `0x004A7E38` into ECX before calling this entry. Both callers ignore EAX, so the maintained logical contract is `void`.
- The target calls exact `GameManagerModeView::IsGameMode0 @ 0x0040E470` and `IsGameMode1 @ 0x00404970`, selects either signed short `+0x06` from neutral pointer global `0x004A7E80` or literal `201` under neutral override global `0x004A7E90`, calls still-independent message-runtime setup `0x00416160` on `Front +0xE944` with `(index, 0)`, then clears Front fields `+0x1095C`, `+0x10960`, `+0x10964`, and `+0x11EA8`.
- Ordinary typed VC7.1 source is now actually tracked in src/FrontMessageInitializeRuntime.cpp and is exact-sized at 133 bytes; all nine relocations solve to the target globals/callees and 91/97 comparable bytes match. Two cold rebuilds are byte-identical with function-text SHA-256 4066a4f667d8ba8bd2e826cfcb0f0d0868b2538e4658fc23cdab1b7155c28d79. The entire residual is the six-byte member-call evaluation order around the setup call: target emits push 0; lea ECX,[ESI+0xE944]; push EAX, while the natural candidate emits push 0; push EAX; lea ECX.
- Two additional bounded natural formulations do not justify further pursuit: an explicit runtime pointer local and an explicit mode local emit the same 133-byte/91-of-97 result; writing the four branch-local calls and allowing VC7.1 to tail-merge them emits 136 bytes. No explicit register convention, volatile, pragma, assembly, padding, or profile roulette is retained. This frontier is frozen until genuinely new TU/type evidence explains the evaluation order.

## Packet 348 Supervisor DirectPlay device-address creation

- `SupervisorNetworkState::CreateDeviceAddress @ 0x00432840-0x004328E4` is repository-canonical exact at 165 bytes. The only TH09 caller family is `SupervisorServiceUpdate`, which invokes this member on `g_SupervisorNetworkState` in both join/connect and host setup paths. Existing exact DirectPlay teardown/reset code independently fixes `deviceAddress +0x04`, peer IDs `+0x1C/+0x20`, and port `+0xA4`.
- The helper clears both peer IDs, creates `IDirectPlay8Address` through `CoCreateInstance(CLSID_DirectPlay8Address, IID_IDirectPlay8Address)`, selects `CLSID_DP8SP_TCPIP` with `SetSP`, then adds the `DPNA_KEY_PORT` DWORD component from state `+0xA4`. Each failing stage reports through exact `GameErrorContext::Fatal`; target-facing externs preserve error-string data ownership.
- A first ordinary SDK-typed source cached `this->deviceAddress` as an `IDirectPlay8Address *` across `SetSP` and `AddComponent`, naturally emitting 163 bytes. TH09 target instead keeps `&this->deviceAddress` live and reloads the member before `AddComponent`; spelling both COM calls directly through `this->deviceAddress` naturally emits the exact 165-byte target.
- Two cold canonical `/Gr /O2 /Ob1 /Oy-` rounds reproduce 165/165 bytes with all 14 GUID/IAT/string/error relocations reviewed. Existing `ResetSession @ 0x00432F70` and `SendPacket @ 0x004323B0` remain exact in both rounds. The DirectPlay8 interface/vtable and `DPNA_*` constants come from the pinned VC7.1 Platform SDK; no hand-written vtable, register forcing, pragma, padding, assembly, or profile search is used.

## Packet 349 Supervisor DirectPlay host-address creation

- `SupervisorNetworkState::CreateHostAddress @ 0x004328F0-0x00432A03` is repository-canonical exact at 276 bytes. Its sole TH09 caller is `SupervisorServiceUpdate`, which invokes it on `g_SupervisorNetworkState` on the join/connect path immediately after exact `CreateDeviceAddress`. Existing state layout independently fixes `hostname +0x24`, `hostAddress +0x10`, and `port +0xA4`.
- The helper converts the stored multibyte hostname to a local `wchar_t[256]`, creates `IDirectPlay8Address`, selects `CLSID_DP8SP_TCPIP`, adds `DPNA_KEY_HOSTNAME` as a string component using `(wcslen(hostname)+1)*sizeof(wchar_t)`, then adds `DPNA_KEY_PORT` as a DWORD. Failing stages report through exact `GameErrorContext::Fatal`; error-string data remain target-facing externs.
- The first ordinary pinned-VC7.1 SDK-typed source is structural-exact. The hostname-size expression naturally lowers to the target `2*wcslen + 2` form, and direct `this->hostAddress` member expressions preserve the same member-reload codegen already proven by `CreateDeviceAddress`.
- Two cold canonical `/Gr /O2 /Ob1 /Oy-` rounds reproduce 276/276 bytes with all 20 CRT/GUID/IAT/string/error relocations reviewed. Exact `CreateDeviceAddress @ 0x00432840` and `ResetSession @ 0x00432F70` remain exact in regression. No hand-written vtable, register forcing, pragma, volatile, assembly, padding, or profile search is used.

## Packet 350 Supervisor DirectPlay host/connect session pair

- `SupervisorNetworkState::HostSession @ 0x00432A10-0x00432AB6` and `ConnectSession @ 0x00432AC0-0x00432B67` are repository-canonical exact at 167 and 168 bytes. Their sole TH09 caller family is `SupervisorServiceUpdate`: the host path invokes `HostSession` after exact device-address setup, while the join path invokes `ConnectSession` after exact device- and host-address setup.
- Both methods construct the same 72-byte `DPN_APPLICATION_DESC` and local `wchar_t[128]` session name from `"TH09YoukaiMatch"`. They share one file-scope internal application GUID `{9FD5EC50-740A-4FDB-909D-2F24E15282EA}`. Canonical relocation replay resolves all four GUID dword loads in both methods to the same TH09 data object at `0x00490C3C`, independently establishing shared TU/data ownership.
- A function-local `const GUID` is a negative source-shape result: VC7.1 allocates an extra 16-byte stack object, expanding HostSession's frame from target `0x148` to `0x158` and emitting 204 bytes. Moving the immutable GUID to file scope is the natural source organization that restores the target frame and exact codegen; no register or layout forcing is involved.
- `HostSession` calls SDK `IDirectPlay8Peer::Host` with `deviceAddress +0x04`, one device address, and flags 1; failures log the target error and call exact `ReportDirectPlayError @ 0x00432410`. `ConnectSession` calls `IDirectPlay8Peer::Connect` with `hostAddress +0x10`, `deviceAddress +0x04`, and async handle `+0x18`; `S_OK` and `DPNERR_PENDING (0x0015800E)` are accepted without error reporting.
- Two cold canonical rounds reproduce HostSession 167/167 and ConnectSession 168/168. Exact `CreateHostAddress @ 0x004328F0` and `CreateDeviceAddress @ 0x00432840` remain exact in both rounds. The DirectPlay ABI and HRESULT constants come from the pinned VC7.1 Platform SDK; no hand-written vtable, register forcing, pragma, volatile, assembly, padding, or profile roulette is used.

## Packet 351 Supervisor DirectPlay peer initialization

- `SupervisorNetworkState::InitializePeer @ 0x00432EB0-0x00432F66` is repository-canonical exact at 183 bytes. TH09 WinMain is its sole caller: immediately after allocating, constructing, registry-wrapping, and publishing the 0xD4 `NetworkInf` object, WinMain invokes this member and treats any nonzero HRESULT as DirectPlay initialization failure.
- The method creates `IDirectPlay8Peer` into state `peer +0x00` through `CoCreateInstance(CLSID_DirectPlay8Peer, IID_IDirectPlay8Peer)`, registers exact `SupervisorNetworkMessageThunk @ 0x00432EA0` through `IDirectPlay8Peer::Initialize`, validates the TCP/IP service provider through source-present `IsServiceProviderAvailable @ 0x00432770`, and emits the target success/error diagnostics through exact `GameErrorContext` and `ReportDirectPlayError @ 0x00432410` boundaries.
- A straightforward early-return formulation naturally emitted 181 bytes because VC7.1 materialized provider-unavailable `E_FAIL` directly in EAX after the variadic Fatal call. The target keeps the HRESULT live in preserved ESI across that call. The natural control-flow formulation that assigns `result = E_FAIL`, logs either provider failure or success, then returns `result` after the branch reproduces this lifetime and the exact 183-byte body without any register directive.
- Two cold canonical `/Gr /O2 /Ob1 /Oy-` rounds reproduce 183/183 bytes with all 20 GUID/IAT/string/helper relocations reviewed. Exact message thunk 15/15 and exact ResetSession 200/200 remain exact in both rounds. No hand-written vtable, register forcing, volatile, pragma, assembly, padding, or profile search is used.

## Packet 352 Front side-enemy indicator

- `FrontEnemyIndicatorView::SetSideEnemyIndicatorPosition @ 0x00416080-0x00416154` is repository-canonical exact at 213 bytes. Both TH09 callers are in `EnemyManagerView::OnUpdate`; each loads global `0x004A7E38` into ECX before passing the EnemyManager side index and an Enemy position pointer. Independent `FrontInf` construction/lifecycle evidence already binds `0x004A7E38` to the 0x1293C Front owner, correcting the older temporary GameManager relocation alias used in `EnemyManagerCore.cpp`.
- TH09 layout closes the rendered object without adjacent-game transfer: the two side-local indicator VMs start at `Front + 0x123F4` with 0x2A4 stride; target byte `+0x125E7` is VM color alpha at `+0x1F3`, and `+0x125FC/+0x12600/+0x12604` are VM position `+0x208`. GameManager side records are 0x38 bytes, side `+0x04` is the Player pointer, and exact Player layout fixes Player x at position `+0x1B88`.
- The helper selects the Supervisor side, hides the indicator when Enemy x lies outside `[-112,112]`, otherwise computes `abs(player.x-enemy.x)`, maps distances below 64 to alpha `unsigned(distance)*3+64` and larger distances to `0xFF`, then writes transformed popup x, y=472, z=0 into the side VM. Exact `Supervisor::SelectSide @ 0x00401440` and `TransformPopupX @ 0x00401680` are reused as independent boundaries.
- A first semantically correct probe cached the VM pointer and Enemy x early and produced broad register/lifetime differences. Keeping the target-visible Enemy-position pointer live and directly indexing the side VM is the natural source organization that yields the target EBX/ESI/EDI lifetimes. Two cold canonical rounds reproduce 213/213 bytes with ten reviewed relocations; both exact dependencies regress exact, and `EnemyManagerView::OnUpdate` remains at its prior 3,156-byte natural candidate versus the 3,883-byte target. No register forcing, pragma, volatile, assembly, padding, or profile search is used.

## Packet 353 bounded Front decode and Enemy deactivation negatives

- `DecodeFrontMessageString @ 0x00415C60-0x00415C86` is owner/semantics-resolved but remains source-absent and non-exact. Its only two TH09 calls are inside the Front message owner `0x00416590`; each supplies encoded source in EAX and pushes a stack-buffer destination, and both overwrite EAX immediately afterward. The target XOR-decodes through the terminating NUL with key `0x77`, initial step `7`, then `key += step` and `step += 0x10` each byte.
- Ordinary same-TU static probes, including a realistic two-call member owner and a logical `void` return contract, naturally choose source in ECX and destination in EAX with a plain `ret`, emitting 36 bytes. The 39-byte target instead requires source in EAX, destination on the stack, an EBP frame and `ret 4`. Closing it would require explicit private register/stack steering, so no source/exactness credit is claimed.
- `EnemyView::CleanupAfterDeactivation @ 0x00410110-0x004101C7` is likewise owner/semantics-resolved but deliberately non-exact. Two `EnemyManagerView::OnUpdate` callers clear the Enemy active bit immediately before invoking it. The target clears remote-slot ownership, releases attached effects through exact `0x0040F560`, resets four life-callback thresholds and the timer threshold, releases four child ECL blocks through exact `0x004100E0`, and clears Player `homingTarget3037C` when it still points to this Enemy.
- Natural typed source is exact-sized at 184 bytes, both helper relocations solve, and 150/176 comparable bytes match. The residual is instruction scheduling around the five `-1` stores and child-ECL call: target performs the four life-threshold stores, then `mov ECX,ESI`, then the timer-threshold store; ordinary free-fastcall, member-boundary, same-TU callee-definition and chained-assignment forms all prepare ECX earlier or reorder stores. Further pursuit would be scheduling-directed source shaping, so the frontier is frozen.

## Packet 354 AnmManager pending capture service

- `AnmManager::ServiceCaptureRequests @ 0x0042D320-0x0042D3C6` is repository-canonical exact at 167 bytes. Its sole TH09 caller `0x0042DB50` invokes it on the AnmManager receiver. Existing constructor evidence independently initializes two pending indices to `-1`: surface capture at `+0x08` and texture capture at `+0x2B28F0`.
- The first request occupies `+0x2B28F0..+0x2B2910`: pending texture/ANM index followed by eight consecutive source/destination rectangle integers. When pending, the service forwards those nine values to exact `AnmManager::CaptureToTexture @ 0x0043C200` and resets the pending index to `-1`.
- The second request is independently producer-proven by exact `AnmCaptureRequestView::QueueCaptureRequest @ 0x0042F420`: pending surface index `+0x08` with eight rectangle integers at `+0x2B2914..+0x2B2930`. The service forwards them to exact `AnmManager::CaptureToSurface @ 0x0043C2D0` and resets `+0x08` to `-1`.
- The first ordinary typed member source is structural-exact. Two cold canonical `/Gr /O2 /Ob1 /Oy-` rounds reproduce 167/167 bytes with the only two relocations solved to the exact texture/surface capture methods; both callees independently remain 193/193 and 352/352 exact in both rounds. No adjacent-game source or codegen steering is used.

## Packet 355 game window procedure

- `GameWindowProc @ 0x0042D3D0-0x0042D4C8` is repository-canonical exact at 249 bytes. TH09 window-class registration helper `0x0042D4D0` stores this address as `lpfnWndProc`, independently fixing the Win32 `LRESULT CALLBACK(HWND,UINT,WPARAM,LPARAM)` ABI. The maintained free callback name is behavior-neutral; no original member/static spelling is claimed.
- TH09 target handles `WM_ERASEBKGND` by returning 1, `WM_CLOSE` by setting bit `0x80` in global `0x004B36D4`, `WM_ACTIVATEAPP` by writing active/inactive globals `0x004B30B8/0x004B30BC`, custom/MIDI message `969 (0x3C9)` by forwarding the MIDI header at `lParam` to exact `MidiOutput::UnprepareHeader @ 0x0043D790` through pointer global `0x004B36BC`, and `WM_SETCURSOR` through exact `Supervisor::IsWindowed @ 0x0042D2D0` plus USER32 cursor APIs. Other paths use `DefWindowProcA`.
- A straightforward equivalent source that returned `DefWindowProcA` directly inside the `WM_ACTIVATEAPP` case emitted 247 bytes and did not keep `wParam` live in a preserved register. Clean TH095 was consulted only after TH09 fixed all messages/globals/callees; its same-series source family uses `break` from `WM_ACTIVATEAPP` to the common final `DefWindowProcA`. Applying that natural control-flow shape to TH09 keeps `wParam` in EBX, matching the target 249-byte body without any register directive.
- Two cold canonical `/Gr /O2 /Ob1 /Oy-` rounds reproduce 249/249 bytes with all 16 global/IAT/helper relocations reviewed. Exact `Supervisor::IsWindowed` and `MidiOutput::UnprepareHeader` remain exact in regression. No register forcing, pragma, volatile, assembly, padding, or profile search is used.

## Packet 356 CreateGameWindow source and TU-owned exact oracle

- `CreateGameWindow @ 0x0042D4D0-0x0042D5EC` is now maintained source-present but deliberately not canonical-exact. TH09 WinMain is its sole caller; immediately before the call at `0x0042E4FD`, target executes `mov EBX,[ebp+hInstance]`. This positively proves a compiler-private same-TU HINSTANCE transport rather than a public source calling convention.
- TH09 independently fixes the whole helper behavior: initialize/register `WNDCLASSA` with exact `GameWindowProc @ 0x0042D3D0`, `BLACK_BRUSH`, `IDC_ARROW`, class `BASE`, active/inactive globals `0x004B30B8/0x004B30BC`; branch through exact `Supervisor::IsWindowed @ 0x0042D2D0`; create either a 640x480 overlapped window or metric-adjusted `WS_VISIBLE|WS_MINIMIZEBOX|WS_SYSMENU` window; publish the HWND to `0x004B30B0` and Supervisor slot `0x004B3174`; then call exact `ActivateGameWindow @ 0x0042D0C0`.
- Ordinary public reconstruction source under `/Gr /O2 /Ob1 /Oy-` emits 289 bytes because the compiler must accept the HINSTANCE through its public fastcall ABI before preserving it. A file-local static helper paired only with the target-proven WinMain call shape (`mov EBX,hInstance; call helper`) naturally compiles to the complete 285-byte target with 209/209 comparable bytes and all 19 relocations solved. No `var_order`, register forcing, volatile, assembly, padding, or profile search is needed.
- Canonical exactness is intentionally withheld because the repository does not yet maintain the real WinMain translation unit. A static helper compiled without a caller is eliminated entirely, and retaining a synthetic caller only to manufacture the private ABI would be dishonest. The 285/285 probe is therefore compiler/TU evidence, not a match-ledger entry. Clean TH095 corroborates only the source family; its target-specific `#pragma var_order` is not imported.

## Packet 357 executable identity checksum

- `ComputeExecutableChecksum @ 0x0042DA90-0x0042DB4F` is repository-canonical exact at 192 bytes. TH09 WinMain is the sole caller and invokes this no-argument leaf immediately before performance-frequency/window initialization. Existing Supervisor consumers independently bind the published fields as executable checksum `g_Supervisor +0x7A0` and executable size `+0x7A4`.
- The helper obtains the current module path into a 264-byte stack buffer while passing `0x105` to `GetModuleFileNameA`, selects the basename using `strrchr('\\')` with `'/'` fallback, opens that executable through exact `FileSystem::OpenFile` with external-resource flag 1, and sums signed 32-bit words for `fileSize/4 - 1` entries. It retains the OpenFile result simultaneously as the mutable ECX cursor and EDI allocation base, frees the latter through exact `ZunMemory::Free`, publishes size/checksum, and returns the checksum.
- Two natural source-shape corrections close the compiler layout without steering: exposing separate cursor/allocation lifetimes produces the target ECX/EDI roles, and expressing `GetModuleFileNameA` success as the outer block moves its failure `return -1` to the target final epilogue. The earlier equivalent early-return spelling emitted 185 bytes; the target-shaped control flow emits the exact 192 bytes.
- Two cold canonical `/Gr /O2 /Ob1 /Oy-` rounds reproduce 192/192 bytes with eight reviewed relocations: `GetModuleFileNameA`, two `strrchr` calls, exact `FileSystem::OpenFile @ 0x0042C970`, `g_ZunMemory`, exact `ZunMemory::Free @ 0x00401360`, and the two `g_Supervisor +0x7A4/+0x7A0` stores. Both exact dependencies regress exact. No adjacent-game source, pragma, register forcing, volatile, assembly, padding, or profile search is used.

## Packet 358 Background stage-object VM updater

- `Background::UpdateStageObjectVms @ 0x004011D0-0x0040128D` is repository-canonical exact at 190 bytes. Its sole TH09 caller is `Background::OnUpdate @ 0x00402350`, which passes the Background receiver in ECX. Existing `Background::LoadStageData` source independently fixes `stageObjectVms +0x24`, `stageObjectCount +0x828`, `stageObjects +0x82C`, `RawStageObject::flags +0x03`, `firstQuad +0x1C`, variable `RawStageQuadBasic::byteSize +0x02`, and `vmIndex +0x06`.
- For each active object, the helper walks its variable-size quad chain until negative type. Type 0 and type 1 each execute the referenced VM through exact `AnmManager::ExecuteScript @ 0x00436F30`; the helper then checks the target-proven `AnmVm::currentInstruction +0x220` and counts still-running scripts. If none remain after the chain ends, it clears object flag bit 0.
- The source shape matters semantically but needs no encoding tricks: writing separate `case 0:` and `case 1:` blocks with the same `ExecuteScript` call lets VC7.1 naturally tail-merge the duplicated calls into the target `movsx/sub/dec` dispatch. Grouped case labels compile to a 191-byte range check, and `if(type==0 || type==1)` produces a different `test/cmp` sequence. An explicit `if(activeScriptCount) continue;` naturally delays the object reload to the target location before clearing the flag.
- Two cold canonical `/Gr /O2 /Ob1 /Oy-` rounds reproduce 190/190 bytes with only two relocations, both reviewed: `g_AnmManager @ 0x004DC550` and exact `AnmManager::ExecuteScript @ 0x00436F30`. The complete 9,358-byte ExecuteScript unit (9,776-byte physical comparison extent) also replays exact afterward. No register forcing, pragma, volatile, assembly, padding, or profile search is used.

## Packet 359 Enemy per-frame motion integration plateau

- `EnemyView::IntegrateMotion @ 0x0040F9B0-0x0040FA6B` is now source-present but deliberately non-exact. `EnemyManagerView::OnUpdate` is the sole TH09 caller. Existing Enemy/ECL reconstruction independently fixes position `+0x2D74`, velocity `+0x2D8C`, alternate per-frame vector `+0x2D98`, previous position `+0x2DA4`, displacement `+0x2DB0`, flags `+0x337C/+0x3380`, and Supervisor frame-rate multiplier `+0x5B8`.
- The helper computes `displacement = position - previous` through exact `Float3::operator- @ 0x00401140`, snapshots `previous = position`, then integrates x/y. When `flags3380 & 0x1000` it uses the alternate vector; otherwise it uses velocity and mirrors x when `flags337C & 0x8000`, with y always advanced normally. The mirror-x ternary source form naturally produces the target `fsubr/fadd` branches with a common x store.
- The maintained member source is exact-sized at 188 bytes with all four relocations solved and 155/172 comparable bytes equal. The remaining residual is preserved-register assignment: target keeps `previousPosition` in EBX and `position` in EDI, while ordinary direct member source chooses the inverse. Introducing explicit position/previous pointer locals in either declaration order shrinks the function to 176 bytes instead of swapping those registers.
- This is a bounded register-allocation plateau. No `var_order`, volatile/register forcing, assembly, padding, target-byte encoding, or profile search is retained. Source credit is honest; canonical exactness remains false.

## Packet 360 FrontInf gameplay factory

- `CreateGameSubsystem @ 0x00418840-0x0041891B` is repository-canonical exact at 220 bytes and is now owner-corrected to the Front subsystem. Its sole TH09 caller is `GameManager_GameplaySetupThread`, which stores the return in the gameplay `gameSubsystem` slot later released by exact `ReleaseGameSubsystem`; independent Front lifecycle evidence fixes the allocation as the 0x1293C-byte `FrontInf` owner.
- The factory performs ordinary VC7.1 `new` on a 0x1293C object, calls constructor boundary `0x004187B0`, registers the allocation through exact `ZunMemory::AddToRegistry` with target literal `FrontInf`, calls exact `Front::Reset @ 0x00415D40`, creates the calc ChainElem for callback `0x00417630`, stores Front as its arg and `0x00418480` as added callback, inserts it at calc priority 22, then creates the draw ChainElem for callback `0x00417C10` and inserts it at draw priority 28.
- A local `FrontFactoryConstructView` is used only to express the target constructor relocation and natural VC7.1 exception cleanup. This exact factory does not reclassify physical constructor `0x004187B0`, which remains one of the frozen special-member origin unknowns, and it does not grant source/exactness credit to the 1491/1474/706-byte callback bodies referenced by the chain entries.
- Ordinary `/MT /EHsc /Gr /O2 /Ob1 /Oy-` source is structural-exact on the first probe. Two cold canonical rounds reproduce 220/220 bytes with all 22 EH/new/registry/reset/chain/callback relocations reviewed; exact `FrontSide::Create @ 0x0041A5D0` and `Front::Reset @ 0x00415D40` remain exact in regression. No register forcing, pragma, volatile, assembly, padding, or profile search is used.

## Packet 361 TitleScreen registration factory

- `RegisterTitleScreen @ 0x0042AB13-0x0042ABDB` is repository-canonical exact at 201 bytes. Its sole TH09 caller is `Supervisor::OnUpdate`, which loads `ECX = 1` immediately before the call; this fixes a free fastcall registration-context input rather than a TitleScreen `this` receiver. Existing TH09 TitleScreen lifecycle evidence independently fixes the owner size at 0x1B388 and the calc/draw chain fields at `+0x1B248/+0x1B24C`.
- The routine allocates a 0x1B388 object with ordinary `new`, invokes constructor boundary `0x00429D83`, publishes the pointer at `g_TitleScreen @ 0x004AC8C0`, zeroes the object through CRT `memset`, clears target byte `0x004A7ECC`, stores the registration context at `+0x1B22C`, then registers calc callback `0x0042A9CB` with exact `TitleScreenView::AddedCallback @ 0x00424D46` / `DeletedCallback @ 0x00424D9F` at priority 4 and draw callback `0x00424898` at priority 3.
- The constructor and the 0x0042A9CB/0x00424898 callback bodies remain independent authored/no-source candidates; this exact registration function only records relocations to them. A local 0x1B388 constructor view expresses ordinary VC7.1 `new`/EH cleanup without claiming the original constructor type spelling or TU partition.
- The repository's already-proven TitleScreen profile is decisive: `/O2` with `/Oi` emits a 254-byte body by inlining the EH prolog and `memset`, while the existing TitleScreen `/O1 /Ob1 /Oy-` profile without `/Oi` naturally emits target `__EH_prolog` plus `_memset`. Two cold canonical rounds reproduce 201/201 bytes with all 17 EH/CRT/global/Chain/callback relocations reviewed; exact TitleScreen Added/Deleted callbacks remain exact in both rounds. No profile roulette, register forcing, pragma, volatile, padding, assembly, or target-byte encoding is used.

## Packet 362 TitleScreen constructor layout

- `TitleFactoryConstructView::TitleFactoryConstructView @ 0x00429D83-0x00429E22` is repository-canonical exact at 160 bytes. Exact `RegisterTitleScreen @ 0x0042AB13` is its sole TH09 caller and invokes it on the freshly allocated 0x1B388 TitleScreen object, fixing both owner and constructor ABI.
- TH09 layout independently closes every non-trivial member range consumed by the constructor: 50 records of stride 0x1EC at `+0x68E4`, then the existing 0x38-byte field band `+0xC8FC..+0xC933`, 32 music descriptors of stride 0x292 at `+0xC934`, four ANM-VM groups of 14/21/14/8 elements with stride 0x2A4 covering `+0x11BA0..+0x1B223`, and the 0xCC configuration snapshot at `+0x1B2BC`.
- These are expressed as ordinary non-trivial C++ member arrays. VC7.1 therefore emits six natural calls to its excluded compiler helper `??_H @ 0x00401470`; no helper call is written by hand. The body then calls exact `GameConfiguration::Initialize @ 0x0041A8A2` on the final snapshot and explicitly clears the complete 0x1B388 object with `memset`, matching the target's unusual post-member-construction reset order.
- Two cold canonical TitleScreen `/O1 /Ob1 /Oy-` rounds reproduce 160/160 bytes with all eleven relocations reviewed, and the exact registration factory remains 201/201 in both rounds. Constructor boundary `0x00420C40` remains deliberately origin-unknown and `0x00422F00` remains relocation-only; caller exactness does not promote either dependency. No packing pragma, padding field invented for codegen, register forcing, assembly, or target-byte encoding is used; the sole explicit 0x38 byte band is already present in the maintained TH09 TitleScreen layout.

## Packet 363 ANM horizontal/vertical texture-strip pair

- InitializeHorizontalTextureStrip @ 0x00439CF0-0x00439DB6 and InitializeVerticalTextureStrip @ 0x00439DC0-0x00439E86 remain canonical exact at 199 bytes each, but fresh caller evidence corrects their ownership to AnmManager members. Their bodies never read ECX, so the previous free-stdcall source was accidentally byte-identical; TH09 callsites load g_AnmManager into ECX before the calls, and clean TH08/TH095 use the same manager-member form.
- TH09 local ANM layout independently fixes VM UV scroll at `+0x30/+0x34`, color at `+0x1F0`, loaded sprite at `+0x224`, sprite UV start/end at `+0x20..+0x2C`, and the 0x1C-byte `XYZRHW + diffuse + UV` vertex shape. Each helper rejects fewer than three vertices, computes a half-strip UV step, then fills alternating 0x38-byte vertex pairs with `rhw=1.0f`, the VM diffuse color, and evenly stepped horizontal or vertical UV coordinates.
- Clean TH08 and TH095 contain the same horizontal/vertical strip algorithms, but they are used only as source-family hypotheses after TH09 fixes the ABI and every field offset. Those adjacent targets carry `#pragma var_order`; TH09 does not: ordinary local variables under the already-canonical ANM `/O2 /Ob1 /Oi` profile reproduce both complete bodies naturally.
- Two independent cold canonical rounds reproduce 199/199 bytes for both helpers with zero relocations; neighboring exact `AnmManager::Project3DQuad @ 0x00439A90` remains 595/595 in both rounds. `EclManager::RunEcl` continues to receive no whole-function exactness credit merely because its trail-geometry dependency is now exact.

## Packet 364 GameWindow present/snapshot service

- `GameWindow::Present @ 0x0042DB50-0x0042DC37` is repository-canonical exact at 232 bytes. The sole TH09 caller is the GameWindow draw-loop owner at `0x0042DC40`, which ignores the return; the maintained static `void` contract therefore follows the observed callsite rather than IDA's residual-AL decompilation.
- TH09 independently fixes every runtime boundary used here: `Supervisor +0x08` is the D3D8 device, present parameters start at `+0x350`, screen-transition countdown is `+0x5A4`, and flags are `+0x5D4`; exact `AnmManager::ReleaseSurfaces @ 0x0042D2F0`, exact `AnmManager::ServiceCaptureRequests @ 0x0042D320`, exact `FileSystem::CheckIfFileAlreadyExists @ 0x0042C480`, and exact `Supervisor::TakeSnapshot @ 0x0042F540` are all replayed boundaries. A failed `IDirect3DDevice8::Present` releases ANM surfaces, resets the device with the same present parameters, calls still-independent render-state reset `0x0042D5F0`, and sets the countdown to 2.
- The snapshot path uses caller-proven `AsciiInputView::WasPressed @ 0x0040E2D0` with mask `0x800`, suppresses capture when Supervisor flags contain `0x400`, creates `snapshot`, scans `snapshot/thNNN.bmp` for the first free index below 1000, and invokes exact `Supervisor::TakeSnapshot`. The final countdown decrements only when target byte `0x004A7ECC` is clear. `WasPressed` retains its durable natural-codegen negative and render-state reset retains independent no-source status; exact caller relocations do not promote either dependency.
- Clean TH08/TH095 corroborate the `GameWindow::Present` source family only after TH09 fixes the actual offsets/callees. TH09 requires none of their `var_order` handling: ordinary `char snapshotPath[256]` and a natural integer loop under `/O2 /Ob1 /Oi` produce the exact 0x100-byte frame and target's branch-local lazy save/restore of ESI. Two cold canonical rounds reproduce 232/232 bytes with all 22 relocations reviewed; exact capture-service and snapshot regressions also remain exact.


## Packet 365 GameWindow frame/render loop

- `GameWindow::Render @ 0x0042DC40-0x0042DD85` is repository-canonical exact at 326 bytes. The target preserves the GameWindow receiver in ESI, calls exact `GetTimestamp` at entry and again before `Present`, and directly accesses GameWindow redraw/timing fields at `+0x10`, `+0x2C`, `+0x34`, and `+0x3C`. Together with the exact 0x44-byte zeroing constructor, this fixes a compact TH09 GameWindow view without importing adjacent-game fields that are not locally observed.
- Timing behavior is target-local: publish current time to `+0x2C`, reset frame schedule `+0x3C` on a backwards-clock observation, copy current time to `+0x34`, sleep with `Sleep(0)` until the schedule is due, then advance `+0x3C` by exact double `1/60` until it catches up. The draw/update path flushes Anm vertices, selects Supervisor gameplay viewport 2, runs the calc chain, processes sound queues, maps calc results 0/-1 to Supervisor thread close returns 1/2, and otherwise continues.
- Redraw cadence uses signed byte `framesSinceRedraw +0x10` versus Supervisor frameskip byte `+0x43A`. When a redraw is due the target calls D3D `BeginScene`, exact Anm draw-state reset, writes Supervisor fog state `+0x794 = 255`, disables fog, runs the draw chain, flushes vertices, clears texture stage 0, calls `EndScene`, and resets the redraw counter. It then refreshes `currentTimestamp` and calls exact `GameWindow::Present @ 0x0042DB50`.
- Ordinary pinned VC7.1 `/Gr /O2 /Ob1 /Oi /Oy-` source reproduces all 326 bytes with 30 reviewed relocations. Two cold canonical rounds keep Render 326/326, member `GetTimestamp` 123/123, `Present` 232/232, and the 0x44 GameWindow zeroing constructor 18/18. Clean TH095 corroborates only the broad Render/GetTimestamp source family after TH09 fixes every local offset, branch, API and callee; no adjacent `var_order`, register forcing, padding, volatile, assembly or profile search is used.

## Packet 366 GameWindow shortcut resolver

- `ResolveShortcut @ 0x0042D140-0x0042D220` is repository-canonical exact at 225 bytes. TH09 supplies the ABI directly: the sole caller `0x0042D900` loads `StartupInfo.lpTitle` into ECX, the destination buffer into EDX, pushes only destination size `0x104`, then calls this entry. The target is therefore a free `__fastcall(path, destination, destinationSize)` boundary; adjacent-game member syntax is not transferred.
- The target returns false immediately for a null destination, initializes COM, creates `CLSID_ShellLink` as `IID_IShellLinkA`, queries `IID_IPersistFile`, allocates a wide-character path buffer sized from the caller argument, converts the ANSI shortcut path with `MultiByteToWideChar(CP_ACP,0,...,-1,...)`, loads the shortcut with `STGM_READ`, and calls `IShellLinkA::GetPath` into the destination using a natural `WIN32_FIND_DATAA` local before releasing the allocation/interfaces and uninitializing COM.
- Clean TH095 contains the same ResolveShortcut source family but expresses it as a GameWindow member and uses a target-specific `var_order` pragma. TH09 local caller registers disprove that public member ABI here, and no pragma is needed: ordinary SDK-typed free `__fastcall` source under the repository `/Gr /O2 /Ob1 /Oi /Oy-` profile is structural-exact on the first probe.
- Two independent cold canonical rounds reproduce 225/225 bytes with nine reviewed relocations (`CoInitialize`, `IID_IShellLinkA`, `CLSID_ShellLink`, `CoCreateInstance`, `IID_IPersistFile`, operator new/delete, `MultiByteToWideChar`, and `CoUninitialize`). Exact `GameWindow::Render @ 0x0042DC40` remains 326/326 in both rounds. No register forcing, volatile, padding, assembly, pragma, or profile search is used.

## Packet 367 GameWindow single-instance and startup-path check

- `CheckForRunningGameInstance @ 0x0042D900-0x0042DA89` is repository-canonical exact at 394 bytes. TH09 `_WinMain@16` is the sole caller and invokes it with no arguments. The caller's EDI is merely a long-lived zero register; this function saves EDI and immediately repurposes it as the `rep stosd` destination for `STARTUPINFOA`, so the decompiler's apparent EDI argument is not a source ABI.
- The helper creates the target mutex `"Touhou 08 App"`, reports `ERROR_ALREADY_EXISTS` through exact `GameErrorContext::Fatal`, zero-initializes the trailing 64 bytes of `STARTUPINFOA`, captures module filename / console title / startup info, validates an existing startup title through exact `FileSystem::CheckIfFileAlreadyExists`, resolves chained `.lnk` paths through exact `ResolveShortcut @ 0x0042D140`, copies a non-link startup title otherwise, and compares the resulting startup path with the executable path.
- TH09 dataflow fixes `g_GameWindow +0x1C` (`0x004B30CC`) as the byte set when startup path differs from the executable; the public 0x44-byte GameWindow reconstruction therefore names this field `startupPathDiffers1C` while keeping unrelated bytes neutral. The helper also clears/sets bit `0x40` in the already-established runtime flags at `0x004B36D4`, and returns `0` only when the exclusive mutex handle at `0x004B30A8` is non-null.
- Clean TH095 corroborates the same source family but requires a target-specific `var_order` pragma. TH09 needs none: ordinary no-argument source under `/Gr /O2 /Ob1 /Oi /Oy-` naturally emits the target 0x254 stack frame, CRT inlining, register lifetimes and loop order. Two cold canonical rounds reproduce 394/394 bytes with 23 reviewed relocations, while `ResolveShortcut` remains 225/225 in both rounds. No register forcing, pragma, volatile, padding, assembly, or profile search is used.

## Packet 368 shared D3D render-state reset

- `ResetRenderState @ 0x0042D5F0-0x0042D8FE` is repository-canonical exact at 783 bytes. Exact `GameWindow::Present @ 0x0042DB50` already relocates this target as free no-argument `/Gr` symbol `?ResetRenderState@@YIXXZ`; the other TH09 callers are D3D startup and WinMain. The target body never consumes an entry receiver, so adjacent-game member syntax is not imported.
- The routine programs the TH09 Direct3D8 render-state table through `g_Supervisor +0x08`: Z enable, lighting/cull/alpha/shade/blend/Z/alpha-test states, fog enable through exact `Supervisor::IsFogDisabled`, density/table/vertex/color/start/end fog states, edge/multisample antialias state, then the stage-0 alpha/color/filter/transform/address table. TH09 specifically sets `D3DTSS_COLORARG2 = D3DTA_DIFFUSE` and preserves the observed `(Supervisor+0x5FC) | D3DPRASTERCAPS_ANTIALIASEDGES` condition; these differ from or expose quirks in adjacent-family source and are retained exactly.
- TH09 uses exactly one 32-bit stack local, reused for float bit patterns `1.0f`, `1000.0f`, and `5000.0f`. A single natural `float fogValue` therefore reproduces the target without `var_order`; clean TH095 uses two fog locals plus a target-specific pragma, which is deliberately not transferred.
- After the D3D state table, the function conditionally calls exact AnmManager cache resets in target order: `ClearBlendMode`, `ClearColorOp`, `ClearVertexShader`, `ClearTexture`, `ClearCameraSettings`. It then checks GameManager side 0 `subsystem0` and sets its byte `+0xCC8` to 1; the reconstructed overlay keeps that byte neutrally named `deviceResetFlagCC8` because TH09 proves the write/lifetime relationship but not a stronger original field name.
- The first ordinary pinned VC7.1 `/Gr /O2 /Ob1 /Oi /Oy-` probe is structural-exact. Two cold canonical rounds reproduce 783/783 bytes with all 47 reviewed relocations, while exact `GameWindow::Present` remains 232/232 and exact `Supervisor::IsFogDisabled` remains 13/13. No register forcing, pragma, volatile, padding, assembly, hand-written vtable slots, or profile search is used.

## Packet 369 raw controller-state sampler

- `Controller::GetControllerState @ 0x0042B740-0x0042B842` is repository-canonical exact at 259 bytes. Its sole TH09 caller in the TitleScreen options path moves the selected controller index into ECX before this call, fixing the one-argument `/Gr` fastcall boundary. Existing exact Supervisor DirectInput startup code independently fixes controller0/controller1 at global `g_Supervisor +0x14/+0x18`.
- The helper clears a target 128-byte raw-button buffer at `0x004B3028`. When the selected DirectInput device is null, it zeroes a 0x34-byte `JOYINFOEX`, requests `JOY_RETURNALL` through `joyGetPosEx(0)`, then maps the low 32 `dwButtons` bits into raw bytes (`0x80` when pressed). When a DirectInput device exists, it polls that selected device; a failed Poll calls Acquire and retries only `DIERR_INPUTLOST` up to 400 attempts before returning the zero buffer.
- On successful Poll, TH09 loads the selected device once into ESI, requests the complete 0x110-byte `DIJOYSTATE2`, then copies exactly its 128-byte `rgbButtons` region to the raw-button buffer. The target stack frame is therefore exactly `DIJOYSTATE2(0x110) + JOYINFOEX(0x34) = 0x144`; acquire counters and fallback button iteration stay in registers.
- A first semantically equivalent spelling cached the selected device before the zeroing/fallback branch and emitted 229 bytes. Restoring the target-natural null-device fallthrough and repeated global device-slot loads until Poll succeeds lets stock VC7.1 naturally reproduce the full 259-byte lifetime/register schedule; no register hints or storage forcing are used. Two cold canonical rounds reproduce 259/259 with 12 reviewed relocations, and exact same-family `SetButtonFromDirectInputState` / `SetButtonFromJoystickButtons` remain 37/37 and 47/47. Clean TH095 corroborates only the source family after TH09 fixes its own fallback/device policy.


## Packet 370 Front message-runtime release plateau

- `FrontMessageRuntimeView::ReleaseForSideCount @ 0x00417520-0x0041762E` is owner/ABI/semantics-resolved but deliberately remains source-absent and non-exact. Exact `FrontReleaseView::ReleaseCore @ 0x00418270` advances the proven Front owner to its embedded message runtime at `Front +0xE944`, pushes `10 * g_GameSide1Value20`, and calls this entry. Independent message-table reload owner `0x004182E0` calls the same entry on its message-runtime receiver before exact `FrontMessageRuntimeResetView::Reset @ 0x00415C90`.
- The target owns a 0x100-byte local dedup table. Outside game mode 2 it scans at most ten script-table entries from the caller start index, follows each non-null instruction stream using byte opcode `+0x02`, byte payload length `+0x03`, and dword argument `+0x04`, releases opcode-7 music IDs through exact `Supervisor::ReleaseMusic @ 0x0042FD90` only once per ID, and advances by `4 + payloadLength`. In game mode 2 it releases the signed-short music ID at neutral global-pointer `0x004A7E80 +0x0C`.
- Cleanup is independently target-backed: optional allocation `+0x0C` and primary table `+0x00` are freed through exact `ZunMemory::Free @ 0x00401360`, globals `0x004A7D88/0x004A7D8C` are cleared, and the exact 0x1D70 runtime reset is called. The logical maintained signature is `void __thiscall ReleaseForSideCount(int startIndex)`.
- Natural pinned VC7.1 `/Gr /O2 /Ob1 /Oi /Oy-` source reaches a stable 268-byte body versus the 271-byte target after the target-natural `!IsGameMode2()` fallthrough is restored. The complete residual is three codegen bytes: target carries the advanced instruction pointer through an extra two-byte `mov ecx,eax` before reading opcode into AL, and uses a six-byte absolute load into EDX for neutral global `0x004A7E80` where stock source uses the five-byte EAX moffs form. Explicit `next`, `source`, and `musicId` locals plus an assignment-in-`do/while` condition all collapse back to the same 268-byte form. Fresh independent reconstruction reproduces the same 268-byte plateau: a separate tableIndex is necessary for the target-like EBX/BL and EDI lifetimes, while bool[256], explicit next, and explicit mode2 source/music locals do not change it. Further pursuit would be register/encoding-directed steering, so source presence is recorded without exactness credit.


## Packet 371 Front death-transition side selector

- `PlayerDeathSharedView::SelectOpponentSide @ 0x00415D70-0x00415E4D` is repository-canonical exact at 222 bytes and is physically owned by the Front/shared-runtime subsystem. TH09 `Player_EnterDeathState` loads `g_Front/shared runtime @ 0x004A7E38` into ECX, computes `1 - playerSide`, and calls this entry; the existing non-exact Player death source already uses the neutral caller-facing `SelectOpponentSide(int)` alias, so the exact implementation preserves that link name without moving ownership to Player.
- The target sets Front transition block `+0x11EA8 = 1`, increments one global counter, increments the side-selected counter, and compares it with the threshold before touching the three contiguous 0x2A4 ANM VMs at `+0x10968/+0x10C0C/+0x10EB0`. On threshold, exact `AnmLoaded::ExecuteAnmIdx` starts scripts 60/61/62, the latter two VMs receive `-999.0f` in both `pos.x (+0x208)` and `pos2.x (+0x288)`, and exact `AnmLoaded::SetSprite` assigns sprites 44/45 to the side/opposite-side pair.
- The decisive TH09 type evidence is the contiguous global block `0x004A7E90..0x004A7E9C`. Modeling it as one `int[4]` state table gives counter index 0, threshold index 1, and dynamically indexed side counters at `2 + side`. That dynamic array access can alias the preceding slots, so VC7.1 naturally preserves the target load/store order. Modeling the same four addresses as unrelated globals produced the correct 222-byte extent and all ANM calls but only 167/186 comparable bytes because the optimizer hoisted the threshold load.
- The maintained function uses the pre-existing void caller contract; target EAX zero is incidental to the final zero stores and is not promoted into a return contract. Two cold canonical `/Gr /O2 /Ob1 /Oi /Oy-` rounds reproduce 222/222 bytes with nine reviewed relocations, while exact `AnmLoaded::ExecuteAnmIdx` and `SetSprite` remain 134/134 and 189/189. No register forcing, volatile, pragma, padding, assembly, or profile search is used.


## Packet 372 Front message-table loader source plateau

- `FrontMessageRuntimeView::LoadForSideCount @ 0x004182E0-0x00418474` is now maintained source-present but deliberately NON-EXACT. Its sole TH09 caller is the Front added callback: ECX is the embedded message runtime at `Front +0xE944`, followed by primary message path, optional match-message path, and a mode-dependent start index. The routine first invokes the reviewed `ReleaseForSideCount` boundary and exact `FrontMessageRuntimeResetView::Reset @ 0x00415C90`.
- TH09 fixes both on-disk tables as `{count; 8-byte entries[]}` with the instruction pointer at `table + 4 + i*8`. The primary cache at `0x004A7D88` is loaded through exact `FileSystem::OpenFile @ 0x0042C970` and rebases only nonzero entry offsets. Outside game mode 2, a 256-byte local music-dedup table scans at most ten entries from the caller start index, follows opcode/size/argument fields `+2/+3/+4`, and calls exact `Supervisor::LoadMusic @ 0x0042FC20` once for each opcode-7 music ID.
- In game mode 2 the secondary cache at `0x004A7D8C` is loaded/rebased through the same 8-byte table format, then the target loads the signed-short music ID from neutral source pointer `0x004A7E80 +0x0C`. Both primary and secondary load failures share the exact variadic `GameErrorContext::Log @ 0x0042C5C0` tail and return -1.
- The first typed probe was 418 bytes because it incorrectly placed the table pointer at entry `+4` (making the physical pointer `table+8`) and kept a redundant separate index local. Correcting the TH09 layout to pointer-at-entry `+0` and mutating the stack argument directly removes thirteen accidental bytes. Two cold maintained-source builds are stable at 403 bytes versus the 405-byte target.
- The remaining target-only two bytes are the same natural-codegen family seen in the adjacent release helper: after computing the next instruction pointer, target emits `mov ecx,eax` before storing the pointer and testing the next opcode through AL, while stock typed source stores EAX and tests through CL. Switching opcode signedness has no effect; modeling current/table pointers as raw byte pointers worsens the candidate to 407 bytes. Further pursuit would be register/encoding-directed steering, so source presence is accepted honestly without canonical exactness.


## Packet 373 TitleScreen draw callback source plateau

- `TitleDrawCallback @ 0x00424898-0x004249E0` is now maintained source-present but deliberately NON-EXACT. Exact `RegisterTitleScreen @ 0x0042AB13` installs this physical address as the TitleScreen draw-chain callback, independently fixing ownership and the fastcall callback ABI. TH09 layout already fixes `vms +0x11B98`, auxiliary VM `+0x11B9C`, `vmCount +0x1B224`, `currentScreen +0x1B228`, and `chainState +0x1B238`.
- The target configures Supervisor gameplay viewport 2, clears AnmManager current texture `+0x12880`, restores surface 0, then walks the TitleScreen VM array. Each VM is gated through exact `AnmManager::SpriteHasTexture`; its three-float `pos +0x208` is copied to a stack temporary, exact `Float3::operator+=` adds `pos2 +0x288`, rotation-z at VM `+0x08` selects exact `DrawNoRotation` versus `Draw2D`, and the original position is restored. A non-null auxiliary VM is drawn without rotation.
- Screen-local rendering stays delegated: screen 11 calls maintained `DrawReplayMenu`, 12 calls exact `DrawMusicRoom`, 13/14 call maintained `DrawResult`, and 15 calls maintained `DrawReplaySave`. The callback finishes with exact `AnmManager::FlushVertexBuffer` and returns 1.
- Two cold builds under the established TitleScreen `/Gr /O1 /Ob1 /Oy-` profile are stable at 318 bytes versus the 329-byte target. The entire 11-byte shortfall is the target `currentScreen <= 11` lowering: target retains a behavior-neutral `screen == 3` test sequence (`sub eax,3; jz common; dec eax; dec eax`) even though no screen-3 draw helper is called. Natural switch spellings containing case 3 alone, 3/4, 3/5, 4/5, or 3/4/5 all collapse to the same 318-byte body, and adjacent TH095/TH10 source provides no semantic action explaining the retained branch. Introducing an artificial no-op branch solely to manufacture those bytes would be encoding-directed, so source presence is accepted honestly without canonical exactness.


## Packet 374 ExAttack type-2 initialize callback

- `ExAttackInitializeCallbackType2 @ 0x00441910-0x00441A67` is repository-canonical exact at 344 bytes. TH09 `g_ExAttackTemplates @ 0x004A0F78` fixes `0x004A0F98` as type 2's initialize slot, paired with exact `ExAttackUpdateCallbackType2 @ 0x00441A70` in the update column. Existing ExAttack evidence independently fixes record dynamic storage `+0x1C`, position `+0x20`, extra pointer `+0x34` and the type-2 0x4C extra layout used by the update callback.
- Initialization allocates one 0x2A4 ANM VM plus the 0x4C extra block through the independently source-present but non-exact `ExAttackAllocateRecordView::AllocateDynamicData @ 0x00440D90`. Exact `ExAttackRecordSideView::GetSideState` resolves the current 0x38 GameManager side, whose `+0x0C` owner supplies `AnmLoaded* +0x2D4`; exact `AnmLoaded::ExecuteAnmIdx` starts script 37 on the allocated VM.
- The extra block is initialized with TH09-local RNG and coordinate rules: motion `+0x04` uses positive-range RNG with -pi and magnitude 2, control points `+0x28/+0x34` use signed pi angles and positive range 192 magnitudes, `+0x10` is the current record position converted through exact popup transforms, and spawn position `+0x40` uses signed RNG over `g_ExAttackPlayfieldWidth * 0.5f - 8.0f` with y=448. The callback switches Supervisor to opponent side, transforms spawn position into control point `+0x1C`, then restores the record side.
- Exactness required only natural C++ lifetime, not a compiler trick. The first faithful spelling cached side/VM pointers and two pairs of RNG results into persistent locals, producing 361 bytes. Keeping the side/VM lookup in the call expression and passing the two random angle/magnitude pairs plus spawn half-width as nested arguments lets VC7.1 use the target temporary argument-stack slots and collapses the body directly to 344 bytes.
- Two cold canonical `/Gr /O2 /Ob1 /Oi /Oy-` rounds reproduce 344/344 bytes with all 33 reviewed relocations; exact type-2 update remains 137/137 and exact side-state lookup remains 12/12. Exact caller closure does not promote `AllocateDynamicData` (stable 83/81) or durable `Float3::FromAngleMagnitude @ 0x00441890` (natural source lacks target FSINCOS); they remain independent non-exact boundaries.


## Packet 375 ExAttack type-0/type-1 initialize pair

- `ExAttackInitializeCallbackType0 @ 0x00440E40-0x00440FA2` and `ExAttackInitializeCallbackType1 @ 0x00440FB0-0x004410F5` are repository-canonical exact at 355 and 326 bytes. TH09 `g_ExAttackTemplates @ 0x004A0F78` fixes `0x004A0F78` as the type-0 initialize slot and `0x004A0F88` as the type-1 initialize slot; exact type-2 initialize remains at `0x004A0F98 -> 0x00441910`.
- Both callbacks reuse the same target-proven ExAttack record and 0x4C extra layout as type 2: allocate one VM plus 0x4C extra, execute ANM script 37 through exact side-state/AnmLoaded boundaries, initialize motion `+0x04` from positive-range -pi / magnitude 2, set extra state 0 and record state 2, generate control points `+0x28/+0x34` from signed pi angles and positive range 192 magnitudes, and transform current record position into control point `+0x10`.
- Type 0 is the side-switching/random-spawn variant. Its spawn x uses signed RNG over `g_ExAttackPlayfieldWidth * 0.5f - 8.0f`; spawn y uses positive-range RNG max 128; after selecting the opponent side it transforms the spawn point into control point `+0x1C` and restores the record side. This differs from type 2 only in spawn-y generation (type 2 uses 448.0f).
- Type 1 is the timed/current-position variant. It assigns record timer 90 through exact `ZunTimer::operator=(int)`, then creates spawn x and spawn y from two independent signed RNG max-64 values. TH09 target adds both results to `record.position.x`, including the y component; the reconstruction preserves that observed quirk rather than silently changing the second base to position.y. The resulting point is transformed without a Supervisor side switch.
- The exact source reuses the natural lifetime recovered for type 2: keep side/VM lookup in the call expression and pass random angle/magnitude values as nested arguments so stock VC7.1 uses the target temporary argument-stack slots. Two cold canonical rounds reproduce type 0 at 355/355, type 1 at 326/326, and type 2 at 344/344. Exact caller closure still does not promote `ExAttackAllocateRecordView::AllocateDynamicData @ 0x00440D90` or durable `Float3::FromAngleMagnitude @ 0x00441890`; both remain independent non-exact boundaries.


## Packet 376 shared ExAttack type-0/type-1 update source plateau

- `ExAttackUpdateCallbackType01 @ 0x00441100-0x004412DA` is now maintained source-present but deliberately NON-EXACT. TH09 `g_ExAttackTemplates` proves that type 0 and type 1 both use this same update slot, while their initialize slots are the exact callbacks at `0x00440E40` and `0x00440FB0`. The callback therefore inherits the already-proven ExAttack record layout and the shared 0x4C type-0/1/2 extra block without any adjacency guess.
- The recovered state machine is complete. State 2 returns 1 only after timer 20. State 1 performs nothing until timer 20; afterwards it checks side-local Player bullet collision using a 24x24 temporary, transitions to state 2 / timer 0 / VM interrupt 1 on collision result 2, otherwise advances record position by extra motion, reflects x at ±144 through one shared negate assignment, accelerates y by 0.04 and clamps to 3.0, appends an exact radius-18 circle to Player collision state `+0x36C`, and returns 1 when y reaches 480. State 0 evaluates exact `ExAttackInterpolate2D` for 90 ticks; on completion it starts ANM script 9 through Player `anmFile +0xBC`, advances extra state, copies extra spawn position `+0x40` into record position, writes opponent side to record `+0x00`, resets the timer and returns 0.
- The best natural pinned VC7.1 source is exact-sized at 475 bytes with all 23 relocation destinations solved and 340/383 ordinary comparable bytes matching in two cold builds. Reconstructing the missing semantics and target block order materially improved earlier probes: the first version was 433 bytes; descending state order plus spawn copy/owner write reached 455; restoring separate x-bound tests reached 480; sharing the negate through the natural OR condition reached 474; restoring the target timer gate produces the exact 475-byte extent.
- Focused type/lifetime probes falsify easy fixes. A raw `ZunTimer*` accessor shrinks the candidate to 469 bytes. An explicit `Float3 *position` lifetime remains target-sized but worsens to 329/383 comparable bytes. The remaining mismatch is ordinary register allocation / ModRM / argument-order scheduling across Player lookups and the state-0 interpolation/ANM tail. Pursuing it further would require register-directed steering, so no canonical exactness claim is made.


## Packet 377 ExAttack type-4 initialize source plateau

- `ExAttackInitializeCallbackType4 @ 0x00442A60-0x00442BCF` is now maintained source-present but deliberately NON-EXACT. TH09 `g_ExAttackTemplates` fixes this as the type-4 initialize slot. The callback allocates five 0x2A4 ANM VMs through exact `ZunMemory::Alloc`, executes scripts 14..18 through the record side's Player `anmFile +0xBC`, then allocates a 0x190 extra block.
- TH09 fixes the 0x190 extra layout as `state(4) + parameterVector(12) + history[32] (32*12)`. The callback selects the opponent side, converts record position through exact `InverseTransformPopupX/Y @ 0x00442420/0x00442450`, copies opponent side into record `+0x00`, copies parameter `+0x3C` to record `+0x3C`, copies the vec3 rooted at `*(parameter+0x34)+4` into extra `+0x04`, clears extra state, then fills all 32 history vectors from the converted record position.
- The first faithful indexed-array spelling compiled to 443 bytes because VC7.1 repeatedly reloaded `record->extra34` and unrolled the 32-entry history copy four vectors at a time. Keeping the allocated extra pointer in a local and expressing history as a 32-count pointer loop collapses the function naturally to the exact 368-byte target extent and reproduces all 22 relocation destinations.
- Two cold maintained builds are stable at 368 bytes with 264/280 ordinary comparable bytes. Every residual is the post-extra-allocation register assignment: target keeps extra in EDI and record position in EBX, while stock source chooses the reverse; declaring position before extra but assigning it only after `SelectSide` preserves the same 368/368 mismatch, while initializing position before the allocation shortens the body to 360. Further pursuit would be register-directed steering, so no canonical exactness claim is made.


## Packet 378 ExAttack type-3 initialize source plateau

- `ExAttackInitializeCallbackType3 @ 0x00442580-0x00442745` is now maintained source-present but deliberately NON-EXACT. TH09 `g_ExAttackTemplates` fixes this address as the type-3 initialize slot. The target calls the reviewed `AllocateDynamicData(5, 0x190)` boundary, keeps extra `+0x34` as a 0x190 block (`state + motion + 32 history vectors`), and starts five side-local ANM VMs with scripts 9..13 through exact `GetSideState` and `AnmLoaded::ExecuteAnmIdx`.
- The target then selects the opponent side, reads opponent Player position at `+0x1B88`, converts X then Y through exact `TransformPopupX/Y`, switches back to the record side, converts record position `+0x20`, stores the transformed position back with z=0, writes record state 2, computes exact `VectorAngle(opponentY-ownY, opponentX-ownX)`, stores it at record `+0x3C`, builds extra motion `+0x04` with magnitude 3.5 through the independently non-exact `Float3::FromAngleMagnitude` boundary, clears extra state, spawns ExAttack type 4 using the transformed local position and this record as parameter, then seeds all 32 history vectors from record position.
- The first natural spelling used two constructor-based `Float3` temporaries and compiled to 474 bytes. Replacing them with target-visible scalar/POD temporaries and preserving the target X-before-Y transform order reduces the body to a cold-stable 453 bytes versus the 454-byte target.
- TH09 target stack reserves one additional completely unreferenced 4-byte slot inside its `sub esp,0x1C` frame. No instruction in the function reads or writes that slot. Natural POD `ExAttackVec3` and the VC7.1 SDK `D3DXVECTOR3` (whose default ctor is empty inline) are both scalarized by VC7.1 and remain 453 bytes. Adding dummy storage, volatile state, padding, or a pragma solely to manufacture the unused slot would violate the reconstruction rules, so no canonical exactness claim is made.


## Packet 379 ExAttack type-5 initialize callback

- `ExAttackInitializeCallbackType5 @ 0x00444F00-0x00445041` is repository-canonical exact at 322 bytes. TH09 `g_ExAttackTemplates` fixes this address as the type-5 initialize slot. The first ordinary pinned VC7.1 `/Gr /O2 /Ob1 /Oi /Oy-` source probe is structural-exact without profile or register experiments.
- The target calls the reviewed `AllocateDynamicData(1, 0x58)` boundary, executes ANM script 37 through exact `GetSideState` / `AnmLoaded::ExecuteAnmIdx`, and fixes the 0x58 extra as `state + seven Float3 values`. The first two vectors are initialized to zero and `(0, 0.028f, 0)`; record state becomes 2.
- Two control vectors at extra `+0x34/+0x40` use signed-pi angles and positive-range 192 magnitudes. Extra `+0x1C` is the current record position transformed through exact popup X/Y. The spawn vector `+0x4C` uses opponent Player `+0x1B88.x` plus signed RNG max 32, y=16 and z=0; after selecting opponent side, exact popup transforms produce extra `+0x28`, then Supervisor is restored to the record side.
- Two cold canonical rounds reproduce 322/322 bytes with all 28 reviewed relocations; exact type-2 initialize remains 344/344 in both regression passes. Exact caller closure does not promote `Float3::FromAngleMagnitude @ 0x00441890` or `AllocateDynamicData @ 0x00440D90`; both remain independent non-exact source boundaries.


## Packet 380 ExAttack type-8 initialize and same-TU Float3 evidence

- `ExAttackInitializeCallbackType8 @ 0x00446C10-0x00446D73` is repository-canonical exact at 356 bytes. TH09 `g_ExAttackTemplates` fixes this address as the type-8 initialize slot. The target allocates one VM plus a 0x5C extra block, starts ANM script 37, initializes record state/timer and the extra motion/control/spawn vectors through the established RNG, popup-transform, side-select and ANM boundaries.
- The 0x5C extra is target-backed as state `+0x00`, scalar fields `+0x04/+0x08/+0x0C/+0x10`, motion `+0x14`, transformed points `+0x20/+0x2C`, RNG control vectors `+0x38/+0x44`, and spawn vector `+0x50`. The first motion angle is positive-range RNG with max `-pi` and magnitude 2; the same angle is stored at `+0x08`, record timer becomes 90, control vectors use signed-pi / positive-192 RNG pairs, spawn is `(144,192,0)`, opponent-side popup conversion fills `+0x2C`, and a final signed-pi RNG value is stored at `+0x0C` after restoring the record side.
- The decisive exactness evidence is natural translation-unit visibility. With only an external declaration of `Float3::FromAngleMagnitude`, otherwise identical source emits 357 bytes because VC7.1 spills the first angle to `[ebp-4]` and reloads it after the call. When the ordinary C++ definition `x = cosf(angle) * magnitude; y = sinf(angle) * magnitude; return this;` is visible in the same TU, VC7.1 knows the direct callee does not clobber EDX, preserves the angle there, emits target `mov eax,edx` after the call, and closes the caller at 356/356 without register forcing.
- This same-TU evidence also changes the source-presence status of `Float3::FromAngleMagnitude @ 0x00441890`. Its natural definition is now maintained in `the EclManager translation unit` because exact type-8 caller codegen depends on that visibility. The callee itself remains honestly NON-EXACT at 30 bytes versus target 32: stock VC7.1 emits FCOS, reload, FSIN while target uses one FSINCOS plus the historical this spill. TH095's inline x87 asm is not imported.
- Two cold canonical rounds reproduce type-8 at 356/356 with all reviewed relocations, while two direct diagnostics keep the same-TU `FromAngleMagnitude` body at 30/32. This is a source/TU discovery, not a backdoor promotion of the callee.


## Packet 381 ExAttack type-7 initialize callback

- `ExAttackInitializeCallbackType7 @ 0x00446320-0x0044649A` is repository-canonical exact at 379 bytes. TH09 `g_ExAttackTemplates` fixes this address as the type-7 initialize slot. The callback shares the same target-proven 0x5C extra layout and natural same-TU `Float3::FromAngleMagnitude` visibility as exact type-8 initialize.
- The target allocates one VM plus the 0x5C extra, starts ANM script 37, builds initial motion from positive-range -pi / magnitude 2, sets record state 2 and timer 90, generates the two signed-pi / positive-192 control vectors, and transforms the current record position into extra `+0x20`.
- Exact `GetRandomU32InRange(2)` selects the horizontal spawn side. Nonzero chooses x=-144 and angle `+0x08 = 0`; zero chooses x=144 and angle `+0x08 = pi`. Spawn y is 224 and z is zero. After selecting opponent side, exact popup transforms fill extra `+0x2C`, Supervisor returns to the record side, and a final signed-pi RNG value is stored at extra `+0x0C`.
- The first same-TU natural source probe is structural-exact. Two cold canonical rounds reproduce type 7 at 379/379, while exact type 8 remains 356/356 from the same maintained `the EclManager translation unit` translation-unit hypothesis. The natural `Float3::FromAngleMagnitude` definition remains independently 30/32 non-exact; exact callers depend on its visibility, not on its own target body being exact.


## Packet 382 ExAttack type-6 initialize source plateau

- `ExAttackInitializeCallbackType6 @ 0x00445EC0-0x00446056` is now maintained source-present but deliberately NON-EXACT. TH09 `g_ExAttackTemplates` fixes this address as the type-6 initialize slot. It shares the same 0x5C extra and same-TU `Float3::FromAngleMagnitude` family as exact type 7/8.
- The target allocates one VM plus 0x5C extra, starts ANM script 37, initializes record state 2 and the initial motion/control vectors, transforms current position, then uses exact `GetRandomU32InRange(2)` to choose the horizontal spawn side. The left branch sets x=-144 and uses a signed ±pi/6 angle directly; the right branch sets x=144 and normalizes `pi + signed ±pi/6` through exact `AddNormalizeAngle @ 0x0042AED0`. Extra `+0x10` receives signed ±pi/240 angular velocity, spawn y is random in [0,320), and the opponent-side popup conversion fills extra `+0x2C` before the record side is restored.
- The best natural same-TU VC7.1 source is exact-sized at 407 bytes with 249/255 ordinary comparable bytes and all relocation destinations solved. The complete six-byte residual is a scheduling window after the right-branch `AddNormalizeAngle`: target prepares `ECX=g_ReplayRng`, stores the branch result to extra `+0x08`, then pushes the pi/240 argument; stock VC7.1 pushes that argument first, stores `+0x08`, then loads ECX.
- Two bounded natural lifetime probes fail to distinguish a better source shape: naming the following RNG result leaves 407/407 and the same 249/255 bytes, and an ordinary `RngRuntimeView&` alias in the type-6 function does the same. Two cold maintained builds keep the 407-byte result while exact type 7/8 remain exact from the same TU. Further pursuit would be expression/register scheduling steering, so no canonical exactness claim is made.


## Packet 383 Ascii input held/pressed leaves and u16 mask ABI

- `AsciiInputView::IsHeld @ 0x0040E2B0-0x0040E2C0` and `AsciiInputView::WasPressed @ 0x0040E2D0-0x0040E2E1` are repository-canonical exact at 17 and 18 bytes. Their logical identities are not adjacency guesses: existing exact TH09 callers already relocate `Ending::OnUpdate` to IsHeld and `AsciiMenuState4::OnUpdate` / `GameWindow::Present` to WasPressed.
- TH09 target fixes a compact input view sufficient for these leaves: IsHeld masks the unsigned word at receiver `+0x00`, while WasPressed masks the unsigned word at `+0x06`. Both zero EAX, load the word into AX, load the stack mask as a full dword into ECX, perform `and eax,ecx`, and return with `ret 4`.
- A bounded VC7.1 type matrix distinguishes the original mask width. Reconstructed `unsigned int` / `int` mask declarations let `/O2` fold the operation to `and eax,[ebp+8]`, producing 15/16-byte bodies. A 16-bit mask parameter (signed or unsigned) naturally forces the target `mov ecx,[ebp+8]` plus register AND and reproduces 17/18 bytes. Existing callers consume the return through AX, so the maintained contract is `unsigned short method(unsigned short buttons)`.
- The three source TUs that already declared these boundaries were corrected from `unsigned int` to `unsigned short`. Direct and canonical replay prove the caller machine code is unchanged: `AsciiMenuState4::OnUpdate` remains 1172/1172, `Ending::OnUpdate` 145/145, `Ending::UpdateFade` 287/287 plus its switch-table compare extent, and `GameWindow::Present` 232/232. VC7.1 `/Zi` local switch-label names shifted when the decorated callee signature changed; only the manifest-local `$L...` symbol spellings were updated, with relocation offsets and target destinations unchanged.
- Two cold canonical rounds reproduce both new leaves and all affected callers exactly. No alternate optimization profile, register forcing, dummy use, volatile, padding, assembly, or pragma is used.


## Packet 384 ExAttack type-5 update callback

- `ExAttackUpdateCallbackType5 @ 0x00445190-0x00445326` is repository-canonical exact at 407 bytes. TH09 `g_ExAttackTemplates` fixes this as the type-5 update slot, paired with exact `ExAttackInitializeCallbackType5 @ 0x00444F00`. The exact initializer independently fixes the 0x58 extra layout: state `+0x00`, velocity `+0x04`, acceleration `+0x10`, Hermite endpoints/tangents `+0x1C/+0x28/+0x34/+0x40`, and spawn position `+0x4C`.
- The update constructs the target collision half-size vector `(6,32,0)` and implements the complete three-state machine. State 2 returns 1 after timer 12. State 1 waits for timer 12, then checks side-local Player bullet collision; result 2 sets state 2, clears the timer and interrupts the VM. Otherwise record position advances by velocity, velocity advances by acceleration, vertical velocity clamps to 4.5, exact `AppendBoxRecord` publishes the `(6,32,0)` box, and y>=480 returns 1.
- State 0 runs exact `ExAttackInterpolate2D` for 40 ticks using type-5 point/tangent fields, then starts ANM script 9 through the record side's Player `anmFile +0xBC`, advances extra state, copies `spawn4C` to record position, writes opponent side to record `+0x00`, and clears the timer.
- The first faithful source cached the opponent Player pointer across the collision and box-append calls and compiled to 401 bytes. TH09 target reloads `g_GameManager.sides[opponentSide].player04` independently at the two call sites; expressing that natural lifetime removes the saved local and reintroduces the second indexed lookup, producing the exact 407-byte target without register or layout forcing.
- Two cold canonical `/Gr /O2 /Ob1 /Oi /Oy-` rounds reproduce 407/407 bytes with all 19 reviewed relocations; exact type-5 init remains 322/322. Exact caller closure does not promote `PlayerLifecycleView::CheckBulletCollision @ 0x0041DFF0`, which remains its independent preserved-register NON-EXACT frontier.


## Packet 385 Player callback30404 type-4 source plateau

- `PlayerPositionCallback30404Type4 @ 0x00443B10-0x00443C08` is now maintained source-present but deliberately NON-EXACT. TH09 `g_PlayerCallback30404 @ 0x004A1A50` index 4 points uniquely to this body, while exact Player AddedCallback plus `PlayerPositionCallbackView::InvokePositionCallback` independently fix the callback ABI as ECX=Player, EDX=position, EAX=int.
- The target first computes `other - Player.position1B88` through exact `PlayerPositionView::operator-`. It reads the neutral range source at Player `+0x368`, normalizes source angle `+0x80` by pi/2 through exact `AddNormalizeAngle`, and builds an XY offset of magnitude `radius(+0x7C) * 0.5` through the maintained `Float3::FromAngleMagnitude` boundary. The dot product between offset and the initial delta selects `Player.position - offset` when positive, otherwise `Player.position + offset`; subtracting the input position again yields the final vector, which is accepted only when its XY squared length is strictly below radius squared.
- The best natural source uses the already-proven POD `PlayerPositionView` type and a conditional-expression center, then reuses the original delta for the final subtraction. Two cold `/Gr /O2 /Ob1 /Oi /Oy-` builds are stable at 235 bytes versus the 249-byte target. Same-TU visibility of the natural `Float3::FromAngleMagnitude` definition does not change the 235-byte result.
- The remaining difference is temporary-object lifetime/layout, not semantics or ABI. Target uses a 0x34 stack frame and reuses one 12-byte hidden-return slot for both conditional operator branches and the final subtraction before copying through separate logical locals. Stock VC7.1 uses a 0x3C frame and distinct branch hidden-return slots. Natural POD ternary, explicit if assignment, and explicit branch-local temporary spellings produce 235, 235, and 236 bytes respectively. Further pursuit would require artificial lifetime/padding or encoding-directed steering, so no canonical exactness claim is made.


## Packet 386 Player callback30404 type-8 source plateau

- `PlayerPositionCallback30404Type8 @ 0x00447620-0x00447718` is now maintained source-present but deliberately NON-EXACT. TH09 `g_PlayerCallback30404 @ 0x004A1A50` index 8 points uniquely here under the already-proven ECX=Player / EDX=position fastcall ABI.
- Type 8 is the same target geometry family as index 4: compute `other - Player.position1B88`, rotate neutral range-source angle `+0x80` by pi/2, build a perpendicular offset through the maintained `Float3::FromAngleMagnitude` boundary, select `Player.position +/- offset` from the dot-product sign, subtract the input position again, and test strict XY containment against `radius +0x7C` squared. TH09's only formula change is offset magnitude `radius * 0.86602539f` (sqrt(3)/2) rather than type-4's `radius * 0.5f`.
- Two cold natural builds under the established callback30404 `/Gr /O2 /Ob1 /Oi /Oy-` profile are stable at 235 bytes versus the 249-byte target. The target has the same 0x34 stack frame and shared 12-byte hidden-return temporary pattern as type 4; stock source uses a 0x3C frame and separate branch hidden-return storage.
- Packet 385 already bounds the natural lifetime search for this family: POD ternary, explicit if assignment, explicit branch-local temporary, and same-TU natural `FromAngleMagnitude` visibility do not recover the target temporary-slot reuse. Type 8 introduces no new ABI/type evidence capable of distinguishing those layouts, so repeating register/lifetime steering would be churn; source presence is accepted honestly without canonical exactness.


## Packet 387 Player shot draw callback type-2 source plateau

- `PlayerShotDrawCallbackType2 @ 0x00443300-0x00443424` is now maintained source-present but deliberately NON-EXACT. TH09 SHT draw-callback table `0x004A1C54` index 2 points uniquely here, and existing shot dispatch fixes the callback ABI as ECX=Player, EDX=shot.
- The callback saves VM color1 at `+0x1F0` as three widened channel locals (red/green/alpha) plus one byte blue, then walks at most four 12-byte history vectors from shot `+0x2B0` until the -999.0f x sentinel. For each entry it derives progress `index * 1/3`, fades red/green/alpha from per-channel accumulators through signed integer division by 4 plus 63, sets VM scale `+0x18 = 3-progress` and `+0x1C = 4-progress`, and draws through exact `AnmManager::Draw2D`. Each iteration advances the history pointer and accumulates the original red/green/alpha channels; the original BGRA bytes are restored before returning 0.
- Treating saved BGRA as one 4-byte struct compiled to only 253 bytes. Matching the target-observed widened locals improves the natural body to 289 bytes and reproduces the x87 progress/scale operations plus signed `/4` lowering.
- The remaining four bytes are register/spill allocation only. Target keeps loop index in EDI and red accumulator in EBX, spills the index to an extra stack local for x87 `FILD`, and keeps green/alpha accumulators in memory. Stock source uses EDI/EBX for red/green accumulators and keeps index directly in memory, yielding a 0x1C stack frame instead of target 0x20. Natural `for`-loop and declaration-order variants remain 289 bytes. Further pursuit would be storage/register steering, so no exactness claim is made.


## Packet 388 ExAttack type-22 initialize callback

- `ExAttackInitializeCallbackType22 @ 0x00444560-0x00444693` is repository-canonical exact at 308 bytes. TH09 `g_ExAttackTemplates` row 22 initialize slot at `0x004A10D8` points uniquely here. Existing exact Enemy type-22 burst spawning independently establishes type 22 as an authored ExAttack route, but the initializer's ownership/ABI comes directly from the template table.
- The callback allocates one ANM VM plus a `0xF48` extra block through the existing ExAttack dynamic-data boundary, resolves the record side through `GetSideState`, and starts ANM script 37. The front of the extra block reuses the target-proven type0/1/2 layout: state `+0x00`, motion `+0x04`, point `+0x10`, opponent-side point `+0x1C`, random control points `+0x28/+0x34`, and spawn position `+0x40`; this initializer additionally proves float `+0x50 = 4.0f` while leaving the rest of the large allocation neutral.
- Record `+0x38` is consumed as a three-float parameter. Parameter z is the initial motion angle at magnitude 2, while parameter x/y become spawn position x/y. The two control vectors use exact RNG helpers with signed-pi angles and positive-range 192 magnitudes. Current record position is converted through exact popup transforms into point `+0x10`; the callback then selects the opponent side and converts parameter x/y into point `+0x1C`. TH09 target does not restore the original Supervisor side before returning, and maintained source preserves that behavior.
- The first ordinary pinned VC7.1 `/Gr /O2 /Ob1 /Oi /Oy-` probe is structural-exact. Two cold canonical rounds reproduce 308/308 bytes with all 24 reviewed relocations, while exact type-0 init remains 355/355 in regression. Exact caller closure does not promote `ExAttackRecord::AllocateDynamicData @ 0x00440D90` or `Float3::FromAngleMagnitude @ 0x00441890`; both retain their independent non-exact status.


## Packet 389 ExAttack type-10 initialize callback

- `ExAttackInitializeCallbackType10 @ 0x00445050-0x0044518B` is repository-canonical exact at 316 bytes. TH09 `g_ExAttackTemplates` row 10 initialize slot at `0x004A1018` points uniquely here, independently fixing ExAttack ownership and the one-record fastcall ABI.
- Type 10 reuses the exact type-5-proven 0x58 extra layout: state `+0x00`, zero motion vector `+0x04`, secondary vector `+0x10`, transformed current point `+0x1C`, opponent-side point `+0x28`, random control vectors `+0x34/+0x40`, and raw spawn point `+0x4C`. The callback allocates one VM plus 0x58 extra, executes ANM script 37, sets timer 40 / record state 2, and initializes secondary-vector x from signed RNG max 0.001.
- TH09 target immediate `0x3CCCCCCD` proves secondary-vector y is exactly `0.025f`; copying type 5's 0.028f leaves exactly the three differing immediate bytes. Current record position is transformed into point `+0x1C`, Supervisor switches to opponent side, both control vectors are generated from signed-pi / positive-192 RNG pairs, raw record position is copied into spawn `+0x4C`, that point is transformed into `+0x28`, and the record side is restored.
- A whole-Float3 assignment for the spawn point naturally cached `&record.position` in EBX, inflating the body to 332 bytes and forcing three immediate-zero stores. Expressing the target-observed component copies keeps EBX as the function-wide zero register and collapses the candidate to the exact 316-byte body. Two cold canonical rounds reproduce 316/316 bytes with all 28 reviewed relocations; exact type-5 init remains 322/322 in regression. Exactness does not promote `AllocateDynamicData` or `Float3::FromAngleMagnitude`, which retain their independent non-exact status.


## Packet 390 ExAttack type-9 initialize callback

- `ExAttackInitializeCallbackType9 @ 0x004467C0-0x0044691F` is repository-canonical exact at 352 bytes. TH09 `g_ExAttackTemplates` row 9 initialize slot at `0x004A1008` points uniquely here, and rows 8/9 share the same update callback at `0x00446920`. That table relationship independently supports reuse of the already target-proven type-8 0x5C extra layout.
- Type 9 follows the exact type-8 initialization family: allocate one VM plus 0x5C extra, run ANM script 37, generate the initial motion from positive-range RNG max -pi at magnitude 2, preserve that angle in `angle08`, set state 0 / record state 2 / timer 90, generate the two signed-pi / positive-192 control vectors, and transform current record position into point `+0x20`.
- The TH09-specific difference from type 8 is the fixed left-side spawn: `spawn50=(-144,192,0)` and `angle08=0`. Type 8 uses the mirrored right-side spawn `(144,192,0)` with `angle08=pi`. After switching to opponent side, popup transforms fill point `+0x2C`; the callback restores the record side and writes final signed-pi RNG to `angle0C`.
- Exactness reuses the same-TU constraint proven by type 8: the natural C++ `Float3::FromAngleMagnitude` definition remains visible in `the EclManager translation unit` so VC7.1 preserves the first angle in EDX across the direct call instead of spilling/reloading it. The first type-9 probe is structural-exact. Two cold canonical rounds reproduce 352/352 bytes while type 8 remains 356/356 and type 7 remains 379/379. No register forcing, pragma, volatile, padding, asm, or profile search is used.


## Packet 391 ExAttack type-16/type-23 initialize pair

- `ExAttackInitializeCallbackType16 @ 0x00447720-0x00447882` and `ExAttackInitializeCallbackType23 @ 0x00447AF0-0x00447C33` are repository-canonical exact at 355 and 324 bytes. TH09 `g_ExAttackTemplates` rows 16 and 23 both point their update column to `0x00447890`, independently fixing a shared 0x50 extra layout before either initialize body was reconstructed.
- The shared 0x50 extra is target-backed as `state00`, `motion04`, scalar `+0x10`, popup points `+0x14/+0x20`, RNG control vectors `+0x2C/+0x38` and raw spawn `+0x44`. Both callbacks allocate one VM plus 0x50 extra, execute ANM script 37, initialize motion from positive-range RNG max -pi at magnitude 2, set state/record-state, generate two signed-pi / positive-192 control vectors, transform current record position into point `+0x14`, switch to opponent side to transform the spawn into point `+0x20`, then restore record side.
- Type 16 generates spawn x from signed RNG with bound `g_ExAttackPlayfieldWidth * 0.5f - 64.0f` and spawn y from positive RNG max 128. Type 23 instead assigns timer 90 and copies the record's current x/y plus z=0 directly into spawn `+0x44` before the opponent-side popup conversion.
- A first source spelling factored the shared prefix into a static helper. VC7.1 `/Ob1` correctly refused to inline that nontrivial helper, shrinking the two bodies to 182/151 bytes; target therefore requires the common initialization to be directly present in each callback. Duplicating the natural source block immediately reproduces both complete targets with no register steering.
- Two cold canonical `/Gr /O2 /Ob1 /Oi /Oy-` rounds reproduce type 16 at 355/355 and type 23 at 324/324, while exact popup X/Y helpers remain 46/46 in regression. Exact caller closure does not promote `Float3::FromAngleMagnitude`, which remains an independently source-present NON-EXACT boundary.


## Packet 392 ExAttack shared type-11/type-12 initialize callback

- `ExAttackInitializeCallbackType11And12 @ 0x00446D80-0x00446ED6` is repository-canonical exact at 343 bytes. TH09 `g_ExAttackTemplates` rows 11 and 12 both point their initialize column to this one physical address, so the maintained name explicitly records shared ownership instead of assigning the body to one row. Their distinct update slots at `0x004A102C/0x004A103C` are now maintained as `ExAttackUpdateCallbackType11 @ 0x00446EE0` and `ExAttackUpdateCallbackType12 @ 0x004471B0`; both reuse this initializer-proven 0x5C extra layout.
- The callback reuses the target-proven 0x5C extra family: allocate one VM plus 0x5C extra, execute ANM script 37, then deliberately call positive-range RNG with max -pi and discard the returned float. Target disassembly confirms the side effect exactly: `call GetRandomF32InRange` followed by `fstp st(0)`.
- Motion `+0x14` is not built from that discarded RNG value; its angle comes from `record.position.z` at magnitude 2. The same record z is stored into extra `angle08`, then extra state becomes 0, record state becomes 2, timer becomes 90 and record z is cleared. Two signed-pi / positive-192 RNG control vectors fill `+0x38/+0x44` and current popup position fills `+0x20`.
- After switching to opponent side, raw current record x/y plus z=0 are copied into spawn `+0x50` and popup-transformed into `+0x2C`. The record side is restored and a final signed-pi RNG value is stored at `angle0C`.
- The target reloads `record.position.z` after the `FromAngleMagnitude` call, so this callback intentionally uses the ordinary external member boundary rather than the type8 same-TU non-clobber optimization. The first standalone probe is structural-exact. Two cold canonical rounds reproduce 343/343 bytes while exact timer assignment remains 9/9 and exact ANM script execution remains 134/134. No register forcing, pragma, volatile, padding, assembly, or profile search is used.


## Packet 393 ExAttack type-17 initialize callback

- `ExAttackInitializeCallbackType17 @ 0x00448320-0x00448484` is repository-canonical exact at 357 bytes. TH09 `g_ExAttackTemplates` row 17 initialize slot at `0x004A1088` points uniquely here, independently fixing ExAttack ownership and the single-record fastcall callback ABI.
- The target allocates one VM plus a 0x50 extra block, executes ANM script 37, then deliberately advances RNG once with positive-range max -pi and discards the result. Extra state becomes 0 and record state becomes 2. Two signed-pi / positive-192 control vectors fill `+0x28/+0x34`; current popup position fills `+0x10`.
- Spawn `+0x40` uses signed RNG with bound `g_ExAttackPlayfieldWidth * 0.5f - 8.0f` for x and positive RNG max 128 for y. After opponent-side popup conversion into `+0x1C` the record side is restored. The callback then initializes scalar `+0x04 = 0`, scalar `+0x0C = 0`, and scalar `+0x08 = GetRandomF32InRange(2.0f) + 0.5f`. The untouched trailing dword completes the 0x50 allocation.
- The first ordinary pinned VC7.1 probe is structural-exact; no TU trick is required. Two cold canonical rounds reproduce 357/357 bytes and exact type-16 init remains 355/355 in regression. The discarded RNG side effect is target-observed (`call` followed by `fstp st(0)`), and exactness does not promote `Float3::FromAngleMagnitude`, which remains independently NON-EXACT.


## Packet 394 ExAttack type-18 initialize callback

- `ExAttackInitializeCallbackType18 @ 0x00449070-0x004491D5` is repository-canonical exact at 358 bytes. TH09 `g_ExAttackTemplates` row 18 initialize slot points uniquely here. The target allocates four VMs plus a 0x158 extra block, but this initializer touches only the already-proven type0/1/2 front layout through `+0x4B`.
- The visible prefix is the exact type-0 initialization family: execute ANM script 37 on the first VM, generate initial motion from positive-range RNG max -pi at magnitude 2, set extra state 0 / record state 2, generate two signed-pi / positive-192 control vectors, transform current record position, generate a random spawn x within `playfieldWidth*0.5-8` and y within [0,128), switch to opponent side for popup conversion, then restore record side.
- The sole structural difference from type 0 at initialization time is `AllocateDynamicData(4, 0x158)` instead of `(1, 0x4C)`. The unreferenced tail `+0x4C..+0x157` is deliberately kept neutral; later shared update code, not this initializer, owns its semantics.
- The first ordinary pinned VC7.1 probe, derived from the exact type-0 source while extending only the allocation layout, is structural-exact. Two cold canonical rounds reproduce 358/358 bytes while exact type-0 init remains 355/355. No register forcing, pragma, volatile, padding, assembly, or profile search is used; exactness still does not promote `Float3::FromAngleMagnitude` or `AllocateDynamicData`.


## Packet 395 ExAttack type-24 initialize callback

- `ExAttackInitializeCallbackType24 @ 0x004497F0-0x00449936` is repository-canonical exact at 327 bytes. TH09 `g_ExAttackTemplates` row 24 initialize slot points uniquely here, and rows 18/24 share update callback `0x004491E0`; this independently fixes reuse of the type-18 0x158 extra/front-layout family.
- The callback allocates four VMs plus a 0x158 extra block, executes ANM script 37 on the first VM, initializes motion from positive-range RNG max -pi at magnitude 2, clears extra state, assigns timer 90 and record state 2, generates the two signed-pi / positive-192 control vectors, and transforms current record position into point `+0x10`.
- Unlike type 18, type 24 does not generate a random spawn. It copies current record x/y plus z=0 component-wise into spawn `+0x40`, then switches to opponent side for popup conversion into `+0x1C` and restores the record side. This is the only visible policy change from the type-18 initializer besides timer 90.
- The first source probe derived from exact type18 is structural-exact at 327 bytes. Two cold canonical rounds reproduce 327/327 while exact type18 remains 358/358. The untouched tail `+0x4C..+0x157` remains neutral, and exactness does not promote `Float3::FromAngleMagnitude` or `AllocateDynamicData`.


## Packet 396 ExAttack type-15/type-25/type-26 initialize family

- `ExAttackInitializeCallbackType15 @ 0x004455D0-0x00445738`, `ExAttackInitializeCallbackType25 @ 0x0044BCD0-0x0044BE38`, and `ExAttackInitializeCallbackType26 @ 0x0044C350-0x0044C4B8` are repository-canonical exact at 361 bytes each. TH09 `g_ExAttackTemplates` fixes all three initialize slots independently; all three reuse the already-exact type-0 0x4C extra/init skeleton.
- The common body allocates one VM plus 0x4C extra, executes ANM script 37, initializes motion from positive-range RNG max -pi at magnitude 2, sets state/record-state, builds two signed-pi / positive-192 control vectors, transforms current record position, generates spawn x with signed RNG over `g_ExAttackPlayfieldWidth * 0.5f - 8.0f`, performs opponent-side popup conversion, then restores record side.
- Only the spawn-y policy differs. Type 15 uses `GetRandomF32InRange(200.0f) + 128.0f`, type 25 uses `GetRandomF32InRange(128.0f) + 32.0f`, and type 26 uses `GetRandomF32InRange(128.0f) + 200.0f`. Those constants and their x87 addition order are directly target-observed; no shared semantic label is inferred beyond the template indexes.
- The first ordinary source probes, derived from exact type0 while changing only spawn-y policy, are structural-exact for all three bodies. Two cold canonical rounds reproduce 361/361 for types 15, 25, and 26, while exact type0 remains 355/355 in regression. No register forcing, pragma, volatile, padding, assembly, or profile search is used.


## Packet 397 ExAttack type-14 initialize callback

- `ExAttackInitializeCallbackType14 @ 0x00443C70-0x00443DE2` is repository-canonical exact at 371 bytes. TH09 `g_ExAttackTemplates` row 14 initialize slot points uniquely here; rows 14 and 22 share the same update `0x00443DF0` and draw `0x00444130` callbacks, independently fixing reuse of the type-22 0xF48 extra layout.
- The callback allocates one VM plus the 0xF48 extra, executes ANM script 37, and follows the type-0 random-spawn source family rather than type22's parameter-vector source family. Its initial motion angle is `GetRandomF32InRange(pi/16) + pi/2` at magnitude 2; extra state and record state are initialized, two signed-pi / positive-192 control vectors are generated, and current popup position fills the first point.
- Spawn x uses signed RNG over `g_ExAttackPlayfieldWidth * 0.5f - 8.0f` and spawn y uses positive RNG max 128. Opponent-side popup conversion fills the second point, record side is restored, and target writes `extra.value50 = 4.0f` at `+0x50`.
- Deriving source from exact type22 produced only 357 bytes because its parameter-vector local/lifetime is not the target type14 source shape. Deriving from exact type0 while changing only the TH09-observed allocation size, initial-angle expression and value50 write is structural-exact on the first probe. Two cold canonical rounds reproduce 371/371 bytes while exact type22 remains 308/308. No register forcing, pragma, volatile, padding, assembly, or profile search is used.


## Packet 398 ExAttack type-19/type-21 trail draw callback

- `ExAttackDrawCallbackType19_21 @ 0x0044B940-0x0044BA7D` is repository-canonical exact at 318 bytes. TH09 `g_ExAttackTemplates` fixes the draw-column slots for type 19 (`0x004A10B0`) and type 21 (`0x004A10D0`) to this same physical callback, independently establishing shared ExAttack ownership and the one-record fastcall callback ABI.
- The target extra layout is self-consistent and target-derived: `state00` at `+0x00`; 33 contiguous 0x1C textured vertices at `+0x4C` occupy exactly 924 bytes and end at `+0x3E8`; 32 contiguous Float3 history samples then occupy `+0x3E8..+0x567`; the draw-pending flag is `+0x684`. When state is at least 1, the callback reads alpha from the first record VM at `+0x1F3`, copies history[0] into the center vertex, popup-transforms its x/y, and writes center diffuse as `(alpha << 24) | 0x00FFFFFF`.
- A 31-iteration ring loop consumes history[1..31]. Each vertex receives the history position, popup-transforms x/y, keeps target color `0x008080FF`, and advances an observed angle local from -pi by `0.2026834f` with target wrap at +pi by subtracting 2pi. The angle does not feed another visible value in this callback, but is retained because its floating compare/subtract/increment is observable target code rather than padding. The loop-end vertex pointer then receives a full 28-byte copy of the first ring vertex to close the 33-vertex fan.
- Exact `AnmManager::DrawTexturedTriangleFan @ 0x0043B8F0` draws the resulting 33 vertices with record VM `+0x1C`. The callback clears extra `drawPending684` on both draw and early-out paths and returns 0.
- Exactness depends only on natural pointer lifetime. A direct indexed close `vertices[32] = vertices[1]` emitted an extra six-byte destination LEA and produced 324 bytes. Keeping one `vertex` pointer live from the center vertex through the ring loop, incrementing it naturally, then assigning `*vertex = vertices[1]` lets VC7.1 select the target register lifetime `EBX=extra, EDI=vertex, ESI=history` and emits the target `rep movsd` close directly. Two cold canonical rounds reproduce 318/318 bytes with 13 reviewed relocations; exact textured-fan draw and popup X/Y helpers remain exact. No register forcing, pragma, volatile, padding, assembly, or profile search is used.


## Packet 399 Player pattern-offset selector leaf

- `PlayerGameplayMethods::ResolvePatternOffset @ 0x00404710-0x00404869` is repository-canonical exact at 346 authored bytes. TH09 supplies five call sites, all inside maintained `PlayerUpdateSelectorState @ 0x004049A0`: ECX is the Player object, followed by stack arguments `pattern, alternate, outX, outY`. Callers ignore the returned pointer, but target naturally leaves `outY` in EAX and the existing same-TU declaration therefore keeps the neutral `float *` return contract.
- Existing TH09 Player layout independently fixes `primaryShtFile +0x30338` and speed multipliers `+0x1CDC/+0x1CE0/+0x1CE4/+0x1CE8`. The selected SHT movement block exposes axis/diagonal values at `+0x14/+0x18/+0x1C/+0x20`. Alternate/focused mode uses `+0x18/+0x20`; normal mode uses `+0x14/+0x1C`. Selector semantics are: 1 up, 2 down, 3 left, 4 right, 5 up-left, 6 up-right, 7 down-left, 8 down-right. Final X is multiplied by `+0x1CE4 * +0x1CDC` and final Y by `+0x1CE8 * +0x1CE0`.
- The decisive source-shape fact is case text order, not numeric case order. TH09's two switch tables point to blocks laid out as `4,3,1,2,5,7,6,8`, which is the natural semantic order `RIGHT, LEFT, UP, DOWN, UP_LEFT, DOWN_LEFT, UP_RIGHT, DOWN_RIGHT`. Writing cases numerically 1..8 preserves semantics but moves every switch block and fails comparison. Clean TH095 PhotoGame independently uses this same semantic source order for its analogous movement-speed family only after TH09 fixes its own offsets and selector mapping.
- VC7.1 COFF auxiliary size is `0x19C` because the function COMDAT also owns two bytes of alignment and both 8-entry jump tables. TH09 has the same physical layout: authored code ends at `0x0040486A`; `8B FF` alignment occupies `0x0040486A-0x0040486B`; jump tables begin at `0x0040486C` and `0x0040488C`; four `CC` bytes at `0x004048AC-0x004048AF` remain outside the comparison before independent `0x004048B0`. The canonical unit therefore records `size=0x15A` but `compare_size=0x19C`, following existing Ending/ECL/ANM precedents.
- Two cold canonical same-TU rounds reproduce all 346 authored bytes and the complete 412-byte comparison extent, including 18 reviewed local jump-table DIR32 relocations. Only 346 bytes enter exact authored totals. Defining this exact same-TU leaf changes the independently NON-EXACT `PlayerUpdateSelectorState` natural candidate from 2,942 to 2,939 bytes versus its 3,238-byte target; that owner remains non-exact and is not promoted.


## Packet 400 Front line-strip draw wrapper

- `FrontDrawLineStrip @ 0x00422330-0x0042246D` is repository-canonical exact at 318 bytes. This resolves the owner that BOUNDARY-121 intentionally left open: the target has exactly two code xrefs, both inside the Front draw callback `0x00417C10`. They pass ECX = `Front+0xE86C` / `Front+0xE8D0` and EDX = 4. The two buffers are 0x64 bytes apart, exactly five 0x14-byte diffuse XYZRHW vertices, so the helper is a Front-specific five-vertex line-strip draw wrapper rather than a neighboring ScreenEffect routine.
- The helper unconditionally flushes Anm vertices, then programs the Direct3D8 stage/render/FVF state through `g_Supervisor +0x08`: stage-0 alpha/color ops select argument 1, both argument-1 sources use diffuse, Z writes are disabled, destination blend is inverse source alpha, and vertex shader/FVF is `D3DFVF_XYZRHW | D3DFVF_DIFFUSE = 0x44`. It calls `DrawPrimitiveUP(D3DPT_LINESTRIP, primitiveCount, vertices, 0x14)`.
- After the draw it calls exact AnmManager cache resets in target order: `ClearVertexShader`, `ClearSprite`, `ClearTexture`, `ClearColorOp`, `ClearBlendMode`, and `ClearZWrite`. It then restores stage-0 alpha/color ops to modulate and argument-1 sources to texture; the final `SetTextureStageState` HRESULT is returned naturally.
- Ordinary SDK-typed `__fastcall(vertices, primitiveCount)` source is structural-exact on the first probe. The source deliberately repeats direct `g_Supervisor+8` device access for each COM call because TH09 reloads that global pointer before every virtual call; no hand-written vtable indices or cached-device lifetime are introduced. Two cold canonical rounds reproduce 318/318 bytes with 26 reviewed relocations, while exact `AnmManager::FlushVertexBuffer` and the clear-state leaves remain exact. No register forcing, pragma, volatile, padding, assembly, or profile search is used.


## Packet 401 Front message-runtime setup

- `FrontMessageSetupView::Setup @ 0x00416160-0x004162C4` is repository-canonical exact at 357 bytes. TH09 has three callers, all on the 0x1D70 runtime embedded at `Front +0xE944`. Two calls from the Front calc callback pass the character-message index and mode 0; the Front message-owner initializer independently passes its selected index and mode 0. No caller prepares an x87 argument, disproving the decompiler's apparent ST0 input; the source boundary is a normal `__thiscall Setup(int messageIndex, int mode)`.
- The routine preserves the primary table pointer at `+0x00` and secondary table at `+0x0C` across a complete 0x1D70 zero-fill, restores both, then chooses primary versus secondary when mode equals 1. The table entry layout agrees with the independently recovered message loader: the instruction pointer is physically `table + 4 + index*8`, not `+8`. The selected pointer becomes runtime `+0x04`, with index at `+0x08` and mode at `+0x10`.
- Two embedded ANM VMs at `+0x1534` and `+0x17D8` have scriptIndex initialized to -1, then exact `AnmLoaded::SetAndExecuteScriptIdx @ 0x00403E00` starts scripts 0 and 1 through neutral global `0x004B36CC`. Runtime text palette fields `+0x1D20..+0x1D3C`, glyph/config fields around `+0x1D50..+0x1D6D`, and VM bytes `+0x298/+0x299` are initialized exactly as observed; exact `AnmManager::DrawTextLeft @ 0x0043BDC0` draws one blank glyph on both VMs.
- The final one-byte codegen issue is resolved by TH09-local type evidence rather than encoding steering. Byte `+0x1D67` has no proven signed arithmetic role; spelling it as signed -1 lets VC7.1 retain CL=-1 from the two script-index stores and emits 356 bytes. Keeping it as the target-observed unsigned byte value `0xFF` lets VC7.1 use EAX=-1 for both scriptIndex stores, clear EAX, and later emit the target seven-byte immediate byte store, yielding 357/357 naturally.
- Two cold canonical `/Gr /O2 /Ob1 /Oi /Oy-` rounds reproduce all 357 bytes with ten reviewed relocations. Exact indexed-script setup and DrawTextLeft remain 41/41 and 139/139. No register forcing, padding, pragma, volatile, assembly, or profile search is used.


## Packet 402 ExAttack type-17 update callback

- `ExAttackUpdateCallbackType17 @ 0x00448490-0x00448633` is repository-canonical exact at 420 bytes. TH09 `g_ExAttackTemplates` row 17 update slot points uniquely here, paired with exact `ExAttackInitializeCallbackType17 @ 0x00448320`. The update therefore inherits the init-proven 0x50 extra layout: state `+0x00`, motion Float3 `+0x04`, point/control vectors `+0x10/+0x1C/+0x28/+0x34`, spawn `+0x40`, and a signed/short variant selector at `+0x4C`.
- The state machine is target-local and fully recovered. State 2 returns 1 after timer 20. State 1 is also gated by timer 20: exact side selection resolves the opponent Player, `CheckBulletCollision` uses a natural 24x24 temporary, collision result 2 moves the extra to state 2, resets timer, interrupts the VM with 1, and returns 0. Otherwise record position advances by extra motion and a radius-28 versus radius-18 circle is appended to Player collision state `+0x36C` according to `variant4C < 2`; y >= 512 returns 1.
- State 0 runs exact `ExAttackInterpolate2D @ 0x0042AF80` for 90 ticks. On completion exact `RngRuntimeView::GetRandomU16InRange(4)` selects variant 0..3, exact `AnmLoaded::ExecuteAnmIdx` starts script `9 + variant` on the allocated VM, extra state increments, spawn `+0x40` is copied back into record position, record `+0x00` becomes opponent side, and exact timer assignment resets to zero.
- Exactness depends on a natural control-flow distinction rather than register steering. A first faithful source materialized `radius = variant >= 2 ? 18.0f : 28.0f` and compiled to 414 bytes. TH09 target does not materialize that local: each branch separately reloads the side-local Player expression, pushes the literal 28.0f or 18.0f, and merges directly at the common circle-append call. Writing the source in that direct if/else form naturally restores the missing six bytes.
- Two cold canonical `/Gr /O2 /Ob1 /Oi /Oy-` rounds reproduce 420/420 bytes with all 20 reviewed relocations. Exact type-17 init remains 357/357, exact interpolation 142/142 and exact RNG-u16 range 41/41. Exact caller closure does not promote `PlayerLifecycleView::CheckBulletCollision @ 0x0041DFF0`, which remains its independent preserved-register NON-EXACT frontier.


## Packet 403 ExAttack type-13 initialize callback

- `ExAttackInitializeCallbackType13 @ 0x00443440-0x004435D2` is repository-canonical exact at 403 bytes. TH09 `g_ExAttackTemplates` row 13 initialize slot points uniquely here. The callback reuses the target-proven one-VM plus 0x4C extra layout shared by the early ExAttack families and starts ANM script 37 through the exact side-state/AnmLoaded boundary.
- Record state becomes 2. Extra control vectors `+0x28` and `+0x34` are initialized from exact positive-range 192 magnitudes and signed pi angles; `+0x10` receives popup-transformed record position. Spawn `+0x40` is generated by adding independent signed-128 RNG values to the opponent Player position at `+0x1B88/+0x1B8C`, then y is capped at 432, x is clamped to [-128,128], and z is cleared.
- The callback switches Supervisor to the opponent side, popup-transforms the clamped spawn into control point `+0x1C`, clears its z, restores the record side, and returns 0. TH09 target does not explicitly initialize extra state/motion in this callback; the maintained source preserves that observable behavior rather than borrowing type0/type1 initialization.
- Exactness depends on natural side-Player lifetime. A first faithful source cached the opponent Player pointer and compiled to 387 bytes. TH09 target recomputes `g_GameManager.sides[opponentSide].player04` separately for the x and y spawn calculations; writing those two direct expressions naturally restores the missing 16 bytes. Two cold canonical rounds reproduce 403/403 bytes with 34 reviewed relocations; exact type0 init and popup X/Y remain exact. Exact caller closure does not promote `Float3::FromAngleMagnitude @ 0x00441890`, which remains its independent natural-codegen NON-EXACT frontier.


## Packet 404 ExAttack type-13 update callback

- `ExAttackUpdateCallbackType13 @ 0x004435E0-0x00443785` is repository-canonical exact at 422 bytes. TH09 `g_ExAttackTemplates` row 13 update slot points uniquely here, paired with exact type-13 init at `0x00443440` and the same target-proven 0x4C extra layout.
- State 2 returns 1 after timer 20. State 1 emits effect 9 every two timer ticks through exact side-local `EffectManager::SpawnEffect` using record position, count 1 and color -1. Once timer exceeds 20 it checks opponent Player collision against a natural 24x24 temporary; collision result 2 moves extra state to 2, resets the timer and interrupts the VM. Otherwise a radius-14 circle is appended to the opponent Player collision query. The common path then enforces a 300-tick timeout, again moving to state 2, resetting timer and interrupting the VM.
- State 0 uses exact `ExAttackInterpolate2D` for 180 ticks. When timer exceeds 180 it starts ANM script 9 on the record side, increments extra state, copies extra spawn `+0x40` into record position, writes opponent side to record `+0x00` and resets the timer. No motion integration occurs in this update family.
- The first natural source spelling reproduces all 422 bytes and 21 reviewed relocations; no compiler-specific tuning is required. The maintained control flow intentionally keeps the post-collision timer-300 check common to both collision outcomes, matching the target even though the collision-hit path has just reset the timer to zero.
- Two cold canonical `/Gr /O2 /Ob1 /Oi /Oy-` rounds reproduce 422/422 bytes. Exact type-13 init remains 403/403, exact `ZunTimer::HasTickedEvery` remains 33/33, and exact `EffectManager::SpawnEffect` remains 184/184. Exact caller closure does not promote `PlayerLifecycleView::CheckBulletCollision @ 0x0041DFF0`, which remains independently NON-EXACT.


## Packet 405 ExAttack type-26 update callback

- `ExAttackUpdateCallbackType26 @ 0x0044C4C0-0x0044C67D` is repository-canonical exact at 446 bytes. TH09 `g_ExAttackTemplates` row 26 update slot points uniquely here, paired with exact type-26 init at `0x0044C350` and the target-proven 0x4C ExAttack extra layout.
- State 2 returns 1 after timer 20. State 1 is gated by timer 20; every three ticks it constructs a natural 0x214 `BulletSpawnDescriptor` stack object through exact ctor boundary `0x0040D500`, copies record position, configures bullet type 19/color 2, aim mode 2, count1/count2 = 1, angle/angleStep/speed2 = 0, speed1 = 0.2, then installs exact vector acceleration transform index0 / allow0 / duration120 / magnitude 1/150 / angle -999.0. The target's resulting transformFlags is 0x10.
- The descriptor is dispatched through the opponent side's EtamaController at GameManager side `+0x08` using `SpawnBulletPatternPrimary`. That wrapper remains independently source-present NON-EXACT (187/203); this exact caller only proves its member ABI. After the optional spawn, timer > 50 returns 1.
- State 0 uses exact `ExAttackInterpolate2D` for 90 ticks. On completion it runs ANM script 8 on the record side, increments extra state, copies extra spawn `+0x40` to record position, writes opponent side to record `+0x00`, resets timer and returns 0.
- The first faithful source was already exact-sized with all 14 relocation destinations solved; its only 17 comparable-byte mismatch was descriptor short-field store order. TH09 target writes those semantic fields as `bulletType -> aimMode -> count1 -> count2 -> color`. Reordering the ordinary source assignments to that natural sequence closes the body without any register or encoding steering.
- Two cold canonical `/Gr /O2 /Ob1 /Oi /Oy-` rounds reproduce 446/446 bytes. Exact descriptor ctor remains 28/28, exact descriptor vector-acceleration installer 39/39, exact type-26 init 361/361, and exact interpolation 142/142. No target-byte arrays, hand-written vtable slots, register forcing, padding, pragma, assembly, or profile search is used.


## Packet 406 ExAttack type-25 update callback

- `ExAttackUpdateCallbackType25 @ 0x0044BE40-0x0044C01A` is repository-canonical exact at 475 bytes. TH09 `g_ExAttackTemplates` row 25 update slot points uniquely here, paired with exact type-25 init at `0x0044BCD0` and the same target-proven 0x4C ExAttack extra layout used by the neighboring spawn-pattern families.
- State 2 returns 1 after timer 20. State 1 is gated by timer 20; every three ticks it constructs a natural 0x214 `BulletSpawnDescriptor` through exact ctor `0x0040D500`, copies record position and adds 24.0 to y, then configures bullet type 19/color 1, aim mode 3, count1/count2 = 1, angle = pi/2 and speed1 = 1.2.
- The descriptor receives exact polar-acceleration transform index0 / allow0 / duration120. C++ right-to-left argument evaluation naturally pushes the constant speed delta `0.010833333f` first, then evaluates exact signed RNG max `0.015707964f` for the angle delta, reproducing TH09's observed stack order. The final descriptor has angleStep/speed2/unknown1FA = 0 and transformFlags = 0x20, then is submitted through opponent-side `EtamaController::SpawnBulletPatternPrimary`. Timer > 60 returns 1.
- State 0 uses exact `ExAttackInterpolate2D` for 90 ticks; completion runs ANM script 8 on the record side, increments extra state, copies spawn `+0x40` to record position, writes opponent side to record `+0x00` and resets timer.
- The first natural source spelling is structural-exact with no codegen steering. Two cold canonical `/Gr /O2 /Ob1 /Oi /Oy-` rounds reproduce 475/475 bytes with 17 reviewed relocations. Exact type-25 init remains 361/361, descriptor ctor 28/28, polar-acceleration installer 39/39, and interpolation 142/142. Exact caller closure does not promote `SpawnBulletPatternPrimary @ 0x004130F0`, which remains independently NON-EXACT.


## Packet 407 ExAttack type-15 update callback

- `ExAttackUpdateCallbackType15 @ 0x00445740-0x00445920` is repository-canonical exact at 481 bytes. TH09 `g_ExAttackTemplates` row 15 update slot points uniquely here, paired with exact type-15 init at `0x004455D0` and the target-proven 0x4C ExAttack extra layout.
- State 2 returns 1 after timer 20. State 1 is gated by timer 20 and then emits two primary bullet patterns from one natural 0x214 `BulletSpawnDescriptor` stack object. The first descriptor uses bulletType 19, explicit color 0, aimMode 5, count1 = 8, count2 = 1, exact signed-pi RNG angle, speed1 = 0.2, and exact vector acceleration index0 / allow0 / duration120 / magnitude1/60 / angle -999.0, producing transformFlags 0x10 before the opponent-side spawn.
- The descriptor is then reused for a second immediate pattern: aimMode becomes 3, color becomes 4, and exact vector acceleration is replaced with duration100 / magnitude0.016 / angle -999.0. The second opponent-side `SpawnBulletPatternPrimary` is followed by return 1. The independently maintained spawn wrapper remains NON-EXACT and gains no exactness credit from this caller.
- State 0 uses exact `ExAttackInterpolate2D` for 90 ticks. On completion exact ANM script 8 runs on the record side, extra state increments, spawn `+0x40` is copied to record position, record `+0x00` becomes opponent side, and the timer resets to zero.
- A first faithful source omitted the explicit `color = 0` assignment because the exact descriptor constructor already zeroes the object; that candidate was 477 bytes. TH09 target nevertheless performs the semantic zero assignment. Writing it explicitly lets VC7.1 keep `EBX = 0` live from the state dispatch, so the later transform calls naturally use one-byte `push ebx` for zero parameters and the function expands to the exact 481-byte target. This is ordinary source-state preservation, not padding or register forcing.
- Two cold canonical `/Gr /O2 /Ob1 /Oi /Oy-` rounds reproduce 481/481 bytes with 17 reviewed relocations. Exact type-15 init remains 361/361, descriptor ctor 28/28, vector-acceleration installer 39/39, and interpolation 142/142. No register forcing, pragma, volatile, padding, assembly, target-byte arrays, or profile search is used.


## Packet 408 shared ExAttack type-16/type-23 update callback

- `ExAttackUpdateCallbackType16_23 @ 0x00447890-0x00447A7F` is repository-canonical exact at 496 bytes. TH09 `g_ExAttackTemplates` rows 16 and 23 both point their update column to this same physical callback, paired with exact initialize callbacks at `0x00447720` and `0x00447AF0`. Those init bodies independently establish the shared 0x50 extra layout: state `+0x00`, motion `+0x04`, scalar `+0x10`, point/control vectors `+0x14/+0x20/+0x2C/+0x38`, and spawn `+0x44`.
- State 2 returns 1 after timer 20. State 1 is gated by timer 20: opponent-side Player collision uses a natural 24x24 temporary; collision result 2 moves the extra to state 2, resets timer, interrupts the VM and returns 0. Otherwise record position advances by motion, motion.x is incremented by the extra scalar, x outside ±144 reflects the x velocity, y >= 464 returns 1, and a radius-13 circle is appended to the opponent Player collision query.
- State 0 uses exact `ExAttackInterpolate2D` for 90 ticks. Completion increments extra state, copies spawn `+0x44` into record position, writes opponent side to record `+0x00` and resets the timer. If the new x is negative the record-side VM runs script 11 and scalar becomes -0.03; otherwise script 12 runs and scalar becomes +0.03. Motion y is then set to 2.0 and motion x to 0.
- A first faithful source was only four bytes short (492 vs 496). The entire difference was the reflection expression: `motion.x = -motion.x` lets VC7.1 use two-byte `fchs`, while TH09 target multiplies by the shared -1.0f constant. Writing the semantically natural compound form `motion.x *= -1.0f` emits the target six-byte `fmul` and closes the function without register manipulation.
- Two cold canonical `/Gr /O2 /Ob1 /Oi /Oy-` rounds reproduce 496/496 bytes with 23 reviewed relocations. Exact type-16 init remains 355/355, type-23 init 324/324, exact interpolation 142/142, and exact circle append 95/95. Exact caller closure does not promote Player `CheckBulletCollision`, which remains independently NON-EXACT.


## Packet 409 ExAttack type-20 update source plateau

- `ExAttackUpdateCallbackType20 @ 0x0044ADE0-0x0044AF74` is now maintained source-present but deliberately NON-EXACT. TH09 `g_ExAttackTemplates` row 20 update slot points uniquely here. The callback uses a 0x5C extra block with state `+0x00`, ExAttack control/spawn vectors through `+0x40`, speed `+0x4C`, angle `+0x50`, angle step `+0x54`, and an integer color counter `+0x58`.
- State 2 returns 1 after timer 20. State 1 returns 1 once timer is no longer less than 30; while active it fires every two ticks. Each firing constructs a natural 0x214 BulletSpawnDescriptor through the exact neutral ctor boundary, copies record position, computes color as `counter % 3`, uses bullet type 21 / aim mode 5 / count1 8 / count2 1, speed1 from extra `+0x4C` and angle from `+0x50`, then submits the descriptor through opponent-side `EtamaController::SpawnBulletPatternSecondary`. Afterward angle advances by `+0x54`, counter increments, and speed increases by 0.2.
- State 0 uses exact `ExAttackInterpolate2D` for 90 ticks. On completion extra state increments, spawn `+0x40` is copied to record position, record `+0x00` becomes opponent side, timer resets, and record VM flag bit 1 at `+0x1F8` is cleared.
- Two cold natural builds are stable at the exact 405-byte target extent with all 11 relocation destinations solved and 299/361 ordinary comparable bytes matching. The remaining mismatch is isolated to the independent descriptor field-store scheduling block; the state machine, stack frame, constructor, timer helpers, GameManager owner and spawn ABI all match.
- Bounded source-shape probes do not justify further pursuit. Reordering semantic field assignments improves the original 274/361 candidate to 299/361, while a short or int color local and a speed-first spelling worsen the result substantially. The maintained BulletSpawnDescriptor intentionally continues to use the neutral 0x214 constructor view because the public reconstructed descriptor cannot yet honestly claim the original default-subobject construction model. Further field-order permutation would be register/store-scheduling directed, so source presence is accepted without canonical exactness.


## Packet 410 EnemyManager spawn boundary cleanup

- `EnemyManagerView::SpawnEnemy @ 0x0040F340-0x0040F4B4` is now maintained source-present but deliberately NON-EXACT. Existing exact wrapper `0x004417C0` and TH09 ECL spawn handlers already fixed the seven-argument member ABI; direct target reconstruction now resolves the argument roles as subroutine id, position, life, defeat-token type, score, 0x78 initial-variable block, and a final primary/opposing-ECL selector.
- The last argument had been mislabeled `runImmediately` in maintained declarations and old notes. TH09 target proves otherwise: zero initializes/runs through the primary ECL manager at EnemyManager `+0x000`, while nonzero uses the opposing ECL manager at `+0x188`. The exact 38-byte wrapper at `0x004417C0` is therefore renamed from `SpawnEnemyImmediate` to neutral `SpawnEnemyUsingOpposingEcl`. Its canonical replay remains 38/38, and exact `ExAttackUpdateCallbackType2 @ 0x00441A70` remains 137/137 after the symbol rename.
- SpawnEnemy searches only the first 128 records beginning at manager `+0x5758` with stride `0x5430`. If every runtime slot is active, the search pointer naturally lands on record 128 at `+0x2A6F58`, the extra 129th Enemy record already proven by constructor layout. The routine returns that sentinel pointer and sets manager overflow flag `+0x2AC42C`. This resolves the previous “129 constructed versus 128 processed” asymmetry without inventing another owner.
- For a free slot the routine copies the full 0x5430-byte spawn template at `+0x328`, stores sequence index `+0x2E58`, optional life `+0x2E48` and position `+0x2D74`, initializes main ECL context `+0x7F4` through the selected manager, copies 0x78 initial variables to context `+0x810`, and runs ECL once. On failure it clears active bit `flags337C bit0` and marks overflow; on success it publishes VM color to `+0x2E70`, defeat-token type `+0x335C`, optional life/score, then mirrors life into max-life/phase `+0x2E4C/+0x2E50`.
- The best natural VC7.1 source is cold-stable at 361 bytes versus the 373-byte target. Collapsing primary/opposing ECL execution through one selected pointer over-optimizes to 322 bytes; explicitly duplicating failure/goto tails expands to 403. The maintained source preserves the two target-visible ECL execution arms with a shared semantic tail. The residual 12-byte failure/epilogue allocation difference is not pursued with register forcing, padding, assembly, or profile roulette.


## Packet 411 EnemyManager mirrored spawn owner exact closure

- EnemyManagerCardAttackSpawnView::SpawnEnemyRecord @ 0x0040F1D0-0x0040F33B is now canonical exact. TH09's exact SpawnFromCardAttack @ 0x00440C80 fixes the member-call stack ABI: short subroutine id, position pointer, life, signed-byte defeat-token type, score, mirror flag, and a final ECL-manager selector. Fresh target reconstruction proves the sixth argument writes flags337C bit15 and, when nonzero, negates spawned position.x; the seventh selects primary ECL manager +0x000 when zero and opposing ECL manager +0x188 when nonzero.
- The owner scans exactly 128 runtime Enemy slots at manager +0x5758 with stride 0x5430, copies the full spawn template at +0x328, stores sequence index +0x2E58, optional life +0x2E48, position +0x2D74, and the mirror bit. It initializes main ECL context +0x7F4, runs the selected ECL manager once, clears active bit 0 and marks overflow on failure, or publishes VM color +0x1F8 -> +0x2E70, defeat-token type +0x335C, optional score +0x2E54, and life into max-life/phase +0x2E4C/+0x2E50 on success. Manager +0x2AC42C records whether the scan/run failed.
- Natural source shape matters but does not require compiler steering. A merged runResult form emits 339 bytes and a while-loop form 355 bytes. Expressing the source as the natural for/continue slot scan with primary/opposing ECL calls and failure handling kept inside each branch emits the target 364-byte body. Two ECL initialize calls relocate to 0x00406850; two ECL run calls relocate to 0x004086C0.
- TH08 EnemyManager::SpawnEnemy1 supplied only the broad source-shape hypothesis. TH09 independently fixes the layout, 128-slot bound, dual ECL managers, mirror semantics, overflow field, ABI, target extent and all relocation destinations. No pragma var_order, register forcing, volatile steering, padding, assembly, fake return, target-byte encoding or profile search is retained.


## Packet 412 ExAttack type-20 initializer exact closure

- `ExAttackInitializeCallbackType20 @ 0x0044AC20-0x0044ADD3` is now canonical exact. TH09 `g_ExAttackTemplates` row 20 points its initialize slot uniquely here, while the maintained type-20 update independently fixes the 0x5C extra layout through `state00`, motion/control/current/spawn vectors, `speed4C`, `angle50`, `angleStep54`, and `counter58`.
- The initializer allocates one VM plus 0x5C extra, executes script 37, initializes a magnitude-2 random motion, two random 192-radius control vectors, popup-space current position, and a random spawn with x in the signed playfield half-width-minus-8 range and y in `[32,176)`. It switches to the opponent side for spawn popup conversion, restores the record side, then sets speed 1, random angle, counter 0, and a random signed pi/30 angle step.
- The first direct branch assignment of the two angle-step literals is semantically correct but emits 429 bytes: VC7.1 stores each float as an immediate dword and hoists the counter clear. Keeping the branch result in an ordinary local `float angleStep`, followed by the common counter/field stores, naturally preserves the target x87 constant lifetime and tail duplication. That source emits the complete 436-byte target with 268/268 ordinary comparable bytes and all relocation destinations solved.
- Exactness uses the standard pinned `/Gr /O2 /Ob1 /Oi /Oy-` family profile already proven by neighboring ExAttack init callbacks. No `var_order`, register/volatile steering, padding, assembly, target-byte encoding or alternate-profile search is used.


## Packet 413 IP.txt value parser owner/ABI closure

- `ParseNetworkConfigValue @ 0x00432240-0x00432332` is semantically and ABI-closed and is now maintained as source-present/non-exact in the same TU as its exact owner. Its sole TH09 caller `0x004324C0` invokes it four times for `IPAddress`, `Port`, `Sync`, and `Side` while parsing `IP.txt`. The helper clears a 256-byte output buffer, compares the requested key prefix, skips spaces/tabs around `=`, decrements the shared remaining-byte count as input is consumed, copies at most 255 value bytes, and stops on space, tab, LF, CR, or semicolon. Key mismatch returns null; recognized-key paths return the current input cursor.
- TH09 fixes a private intra-TU ABI: cursor arrives in EAX, `remaining` in EBX, key/output are the only two stack arguments, and the helper returns with `ret 8`. This ABI does not need any manual register bridge: an ordinary file-local static helper compiled under the established VC7.1 `/Gr /O2 /Ob1` profile naturally selects the same EAX/EBX transport when called from the same TU.
- The remaining frontier is code layout only. A nested natural form emits the exact 243-byte extent, matches target bytes through offset 0x88, and differs afterward only in which equivalent `return cursor` branch receives a duplicated epilogue. A flattened guard spelling emits 239 bytes; a decompiler-shaped `charsLeft` loop emits 248. Further pursuit would be block-layout/source steering rather than new semantic evidence, so no exactness credit is taken.



## Packet 414 IP.txt network loader exact closure

- `PrepareTitleMode4Network @ 0x004324C0-0x00432766` is now canonical exact. TH09 WinMain constructs the 0xD4 `SupervisorNetworkState`, publishes it at `0x004B42D0`, and immediately passes that object to this helper. Exact TitleScreen mode-select reaches the same physical global through the older reconstruction alias `g_OptionPointers`; its relocation also resolves to `0x004B42D0`, and alias index 42 is exactly byte offset `+0xA8`, the independently proven network `active` field.
- The loader clears `active`, requires `IP.txt`, opens it through exact `FileSystem::OpenFile`, zeroes the 0x80-byte hostname at `+0x24`, defaults `syncValue +0xBC` to 0 and `syncRate +0xC4` to 30, then parses `IPAddress`, `Port`, `Sync`, and `Side` through the same-TU private parser. `LAN` maps to `(syncValue=1,syncRate=60)`, `High` to `(0,60)`, `Low` to `(0,30)`, `Bad` to `(0,20)`, while `Auto/1P/2P` map `joinMode +0xC0` to `2/0/1`. The file allocation is released through exact `ZunMemory::Free`.
- Natural source initially emitted 689 bytes because the `Side` miss path duplicated the generic one-byte skip. Sharing that ordinary `SKIP_CHARACTER` control-flow tail, exactly as TH09 does, reduced the body to 679 bytes. The sole remaining 18-byte mismatch was the semicolon comment loop; spelling the target-visible top-tested loop with a file-local `SKIP_COMMENT` label preserves normal C++ semantics and lets VC7.1 emit the target block without padding or register control. The resulting body is 679/679 structural-exact with 591/591 ordinary comparable bytes and all 22 relocation destinations solved.
- `ParseNetworkConfigValue @ 0x00432240` remains independently non-exact even though it now lives in the same maintained TU. Its natural static-helper placement is important evidence: VC7.1 itself selects the target private EAX/EBX plus two-stack-argument ABI. Exactness of the loader does not promote the parser.


## Packet 415 Background camera interpolation private-ABI frontier

- `InterpolateBackgroundCameraVector @ 0x004016E0-0x00401886` is now owner/semantics/TU-closed but deliberately remains source-absent/non-exact. Its only caller is `BackgroundRunStageScriptPhase @ 0x004018F0`, and that 2472-byte owner is itself called only by `Background::OnUpdate @ 0x00402350`. The three TH09 calls cover interpolation slots 0/1/2 and pass the Background owner in ECX, slot index in EAX, output camera vector in EDI, plus four stack pointers for start/end/start-tangent/end-tangent.
- TH09 independently fixes the algorithm. The helper advances `ZunTimer` slot `+0xC08 + index*0x0C` against duration `+0xBF4 + index*4`; modes 1/2/3 are quadratic/cubic/quartic ease-out, 4/5/6 are the corresponding ease-in curves, ordinary modes evaluate `(end-start)*t+start` through exact Float3 operators, and mode 7 evaluates x/y/z through exact `CubicHermiteInterpolateScalar @ 0x00401000`. The mode array begins at Background `+0xC44`.
- Clean TH08 and TH095 source corroborate this as the Background camera-interpolation family only after the TH09 owner and layout are fixed. A TH09-local file-static `/Gr /O2 /Ob1` probe naturally selects the important private ABI shape—EAX index, ECX owner, four stack pointers—without any register annotation. The remaining EDI/ESI allocation depends on the real caller: target `0x004018F0` holds Background in ESI and output in EDI, while a small standalone probe naturally chooses the reverse and emits 460 bytes versus the 423-byte target.
- Do not manufacture a dummy caller or add register/volatile/pragma steering merely to swap ESI/EDI. Revisit this helper together with the real `0x004018F0` stage-script owner, where VC7.1 can see the authentic caller lifetime and choose the private ABI naturally.



## Packet 416 Effect type-27/type-36 strip draw source closure

- `EffectDrawCallbackType27 @ 0x00445A40-0x00445C2A` and `EffectDrawCallbackType36 @ 0x0044C090-0x0044C289` are now maintained source-present/non-exact. TH09 `EffectManager::InitializeEffect @ 0x0040CA60` proves the template base `0x004A0B48` and 0x18-byte row stride: row 27 begins at `0x004A0DD0` and carries draw callback `0x00445A40`; row 36 begins at `0x004A0EA8` and carries draw callback `0x0044C090`. Both rows use script `0x58`, step 0, one VM, and share initializer `0x004459A0`; their update callbacks differ.
- Existing TH09 EffectSys evidence fixes VM pointer `+0x04`, position `+0x0C`, vertices-dirty byte `+0xC9`, and dynamic vertex pointer `+0xCC`. These callbacks additionally prove that scratch field `+0x7C` is a float radius for this family, consistent with the repository rule that Effect scratch fields are callback-family-local semantics rather than one global interpretation.
- Type 27 generates a six-vertex horizontal triangle strip around `position.x +/- radius`, spanning popup y 0..448. Type 36 is the vertical sibling around `position.y +/- radius`, spanning popup x -144..144. Each 0x14-byte `VertexDiffuseXyzrhw` copies the Effect position first, sets `rhw=1`, uses outer diffuse `0x70600040` and center diffuse `0x30600040`, then the callback calls `AnmManager::DrawTriangleStrip(vm, vertices, 6)` and leaves EAX=1.
- The natural pinned VC7.1 source is exact-sized for both functions and all 26 relocation records per function solve to TH09 targets. Type 27 matches 379/387 ordinary comparable bytes; type 36 matches 394/402. In each function the only residual is two commutative x87 additions on the positive-radius edge: object `fld coord; fadd radius` versus target `fld radius; fadd coord`. Reversing the C++ addition operands does not alter VC7.1 lowering; ordinary local-value and compound-assignment spellings make the bodies longer. This is therefore an honest eight-byte codegen plateau, with no register/volatile/padding/asm/pragma/profile steering retained.

## Packet 417 MixRenderColor internal-linkage closure

- `MixRenderColor @ 0x00401090-0x004010A8` is now canonical exact at 25 bytes. Nineteen TH09 calls come from Background/AnmManager color-mixing paths, and the target algorithm is `(channelA * channelB) / 128` saturated to 255. Clean TH08 supplies the same readable formula only after TH09-local caller/byte closure.
- The decisive compiler property is internal linkage, not a forced calling convention. A normal external `/Gr` definition receives the two byte arguments in CL/DL. The same ordinary function declared file-local `static` and called from a real four-channel color path is privately transported in AL/CL, exactly matching the target. The canonical source therefore keeps the helper in `src/AnmManagerRender.cpp`; no register annotation, volatile, pragma, padding, assembly, fake argument or profile roulette is involved.
- That one visibility correction simultaneously promotes `AnmManager::SetRenderStateForVm3D @ 0x00439E90` (411 bytes), `AnmManager::DrawInner @ 0x0043A0F0` (1067 bytes), and `AnmManager::DrawInnerFlippedX @ 0x0043A520` (1067 bytes) to structural exactness. All prior residuals were the same external CL/DL versus private AL/CL call-site lowering. Existing exact AnmManagerRender units replay unchanged.

## Packet 418 Player collision Rotate exact closure

- `RotatePlayerCollisionVector @ 0x0042AF40-0x0042AF7D` is canonical exact at 62 bytes. Five TH09 xrefs span `PlayerLifecycleView::CalcLaserHitbox`, maintained bullet-collision and damage owners, plus two additional gameplay call sites. All callers pass output in ECX, input in EDX, and angle on the stack; the helper calls `@sinf@4` then `@cosf@4` and computes `x = cos*x - sin*y`, `y = cos*y + sin*x`.
- Clean TH08 contains the same ordinary `Rotate(Float3*, Float3*, float)` source family, but TH09 independently fixes the two-component use, physical ABI and call targets. Under `/Ob1` VC7.1 emits a 42-byte intrinsic FSIN/FCOS body, matching the earlier negative. Under the target-evidenced `/Ob0` profile it naturally preserves the two compiler trig calls and emits the TH09 helper byte-for-byte: 54/54 comparable bytes plus both relocations.
- The canonical source is isolated in `src/PlayerCollisionRotate.cpp` and uses no `#pragma var_order`, explicit register convention, volatile, padding or assembly. Exact caller `CalcLaserHitbox` now resolves its Rotate dependency to an independently canonical exact authored leaf.

## Packet 419 FileSystem decrypt-table source closure

- `FileSystem::TryDecryptFromTable @ 0x0042C290-0x0042C36B` is now source-present/non-exact rather than source-absent. The sole caller is exact `FileSystem::OpenFile`; it supplies data in ECX, the file-size pointer in EDX (unused by this helper), and the byte count on the stack. The target first validates three encoded signature bytes, selects one of eight 12-byte decrypt records by the adjusted fourth-byte key, calls exact `FileSystem::Decrypt` on the payload after the four-byte header, frees the original allocation, and returns the decrypted buffer.
- The maintained source deliberately declares `g_DecryptParams[8]` and `g_CryptSignature[3]` as extern only. TH09 target bytes prove the table contents and show that at least records 2 and 3 differ from clean TH08; therefore this packet claims function source coverage, not data-definition/TU ownership, and does not transfer adjacent-game values.
- Natural pinned VC7.1 `/O2 /Ob1 /Gr` emits the exact 220-byte function extent but with a different whole-function register allocation. Moving the natural scan index from block scope to function scope is byte-identical; already-recorded `/GB`, `/G6`, `/G7`, `/O1`, and `/Od` probes do not close the target. The plateau is retained without `#pragma var_order`, register/volatile forcing, padding, asm, or further profile search.



## Packet 420 ExAttack type-6 update source closure

- `ExAttackUpdateCallbackType6 @ 0x00446060-0x00446319` is now maintained source-present/non-exact. Ownership is not inferred from address adjacency: TH09 `g_ExAttackTemplates @ 0x004A0F78` has a data xref at `0x004A0FDC`, exactly row 6's update column, to this physical body under the established one-record fastcall ABI. The already-maintained type-6 initializer independently fixes the shared 0x5C extra layout.
- The target state machine is fully covered. State 0 performs exact `ExAttackInterpolate2D` over point/control vectors for 90 frames, switches the side-local ANM VM to script 9, copies `spawn50`, changes owner to the opponent side and resets the timer. State 1 waits 20 frames, emits type/color 6 bullet pairs at interval `20 - g_PlayerRewardBaseValue/8` using wait + vector-acceleration transforms, tests the opponent Player with a 24x24 collision box, otherwise advances angular motion and exits outside y=[-32,480] or x=[-176,176]. A hit enters state 2, interrupts the VM and resets the timer; state 2 expires after 20 frames.
- The first natural source is already exact-sized at 698 bytes with all 30 relocation destinations solved. Reordering only the descriptor `aimMode/count1/count2/angle` assignments to the order visible in TH09 removes the one semantic scheduling block and improves ordinary-byte agreement from 523/578 to 535/578. The remaining 43 bytes are same-length volatile-register/store scheduling around identical movement, interpolation and copy operations.
- A bounded authentic-family TU probe concatenating the update with the real `Float3::FromAngleMagnitude` definition is byte-identical to the separate-TU 535/578 result. This is important because same-TU visibility is genuinely decisive for exact type-7/8 initializers, but it does not explain this update's residual. Further pursuit would require register/store-scheduling steering, so no `var_order`, register forcing, volatile, padding, assembly or profile roulette is retained.



## Packet 421 ExAttack type-11/type-12 update source closure

- `ExAttackUpdateCallbackType11 @ 0x00446EE0-0x004471A8` and `ExAttackUpdateCallbackType12 @ 0x004471B0-0x00447475` are now maintained source-present/non-exact. TH09 `g_ExAttackTemplates @ 0x004A0F78` directly fixes row 11 update slot `0x004A102C` and row 12 update slot `0x004A103C`; both use the canonical-exact shared initializer at `0x00446D80`, which independently fixes the 0x5C extra layout.
- The update family is fully covered. State 0 uses exact `ExAttackInterpolate2D` for 90 frames, then switches the side-local ANM VM to script 9, copies `spawn50`, transfers record ownership to the opponent side and resets the timer. State 1 waits 20 frames, emits one alternating type-0/type-6 color-2 bullet every 10 frames, applies wait and vector-acceleration transforms whose magnitude reads the opposite side's Player header `+0xA4`, checks a 24x24 Player collision box, advances 1.5-magnitude motion and exits outside the same [-176,176] x / [-32,480] y bounds. A hit enters state 2, resets the timer and interrupts the VM; state 2 expires after 20 frames.
- Type 11 advances `angle0C` by pi/30 and uses the resulting local value as the bullet angle; type 12 still advances `angle0C` but uses fixed `angle08` for the bullet. This naturally accounts for the target's three-byte size difference. The first straightforward source was uniformly three bytes too large because it called `motion14.FromAngleMagnitude(...)` and then separately passed `&motion14` to `operator+=`. Spelling the natural returned-pointer chain `position20 += *motion14.FromAngleMagnitude(...)` lets VC7.1 reuse ECX exactly as the target does and closes the physical sizes to 713/713 and 710/710.
- After size closure, type 11 is stable at 549/593 ordinary comparable bytes and type 12 at 546/590, with all 30 relocation destinations solved for each. Replacing the state-0 named Player/ANM local with the direct nested expression visible in the target is byte-identical. The remaining 44-byte residual in each function is volatile-register/store scheduling across otherwise identical collision/movement/state0 operations. No register forcing, `var_order`, volatile, padding, assembly or profile roulette is retained.


## Packet 422 ExAttack shared type-8/type-9 update source closure

- `ExAttackUpdateCallbackType8_9 @ 0x00446920-0x00446C0B` is now maintained source-present/non-exact. TH09 `g_ExAttackTemplates` rows 8 and 9 both point their update column to this physical body (`0x004A0FFC` and `0x004A100C`), so shared ownership and the one-record fastcall ABI are target-local facts rather than adjacency guesses. Exact type-8/type-9 initializers independently fix the shared 0x5C extra layout.
- The source covers the complete target state machine. State 0 runs the exact 90-frame ExAttack Hermite interpolation, switches to ANM script 9, copies `spawn50`, changes the owner to the opponent side and resets the timer. State 1 waits 20 frames, periodically constructs a BulletSpawnDescriptor using the Player state-derived count, installs wait plus vector-acceleration transforms, spawns through the opponent-side EtamaController, checks a 24x24 Player collision box, otherwise advances the record along `angle08` and exits outside y=[-32,480] or x=[-176,176]. A hit enters state 2, interrupts the VM, stores rotation, and state 2 expires after 20 frames.
- The pinned VC7.1 `/Gr /O2 /Ob1 /Oi /Oy-` candidate is exact-sized at 748 bytes. All 32 relocation destinations solve to TH09 targets and 613/620 ordinary comparable bytes match. The seven-byte residual is confined to equivalent local register/ModRM scheduling around the collision-hit / `FromAngleMagnitude` transition; there is no unresolved call, constant, branch meaning or layout field.
- This plateau is retained without `#pragma var_order`, explicit register control, volatile steering, padding, assembly or alternate-profile search. Exactness remains false even though source/owner/ABI/semantics and full extent are closed.


## Packet 423 Front result-bonus source closure

- FrontResultBonusView::InitializeResultBonus @ 0x00415E50-0x00416078 is now maintained source-present/non-exact. TH09's sole caller at 0x00416E23 loads the registered Front owner from 0x004A7E38 into ECX. The neighboring exact Front transition helper independently fixes Front +0x08 as the Front ANM owner and +0x1095C/+0x10960/+0x10964 as update/draw/side state. The five 0x2A4-byte result VMs begin at +0x11154; the six bonus terms plus total occupy +0x11E88..+0x11EA0.
- The target starts ANM scripts 63 and 66, then computes result-bonus terms from the side-local Player: 100000 * +0xA8; 10000 * +0x30418 capped at 9,999,990; 150000 * +0xB0 capped after 66; two 300000 * +0xB4/+0xB8 terms capped after 33; and, when GameManager +0xFC == 8, a side-runtime float term multiplied by 10,000,000 and capped at 99,999,990. It sums them into +0x11EA0.
- TH09 then applies the exact GameManager mode-0/mode-1 predicates. On side 0 it unlocks the target-observed character byte through exact PlayStatsRecordView::SetCharacterUnlocked0, conditionally starts script 64 and writes 9999 into side-0 runtime +0x10, and under g_GameConfiguration->valueAC plus mode 0 starts script 65 and clears the total. The function finally calls exact side AddScore with the total.
- Natural /Gr /O2 /Ob1 /Oi /Oy- source reaches a stable 549-byte body versus the 553-byte target and reproduces the target's important lifetime shape: ESI holds Front, EDI holds side, EAX holds side*0x38 only through the pre-call bonus phase, and the final AddScore receiver is rebuilt from this->side. The four-byte size residual is localized to the +0x30418 threshold: VC7.1 CSEs the loaded integer across compare/multiply; target compares memory and then reloads the field from the already-live Player pointer. Repeating the full owner expression instead emits 559 bytes. Further work would be deliberate CSE inhibition rather than new semantic evidence, so exactness remains false.


## Packet 424 Front weighted random-message setup source closure

- FrontMessageRandomView::SetupRandomForSide @ 0x004162D0-0x00416584 is now maintained source-present/non-exact. Its sole TH09 caller at 0x00417B2D passes the proven Front +0xE944 0x1D70 message runtime in ECX and Front +0x10964 side as the only stack argument. No caller prepares ST0, so the decompiler's apparent floating input is rejected.
- Existing exact FrontMessageSetupView::Setup, exact runtime reset, and the maintained message loader independently fix primary table +0x00, secondary table +0x0C, 8-byte table entries, the two text VMs at +0x1534/+0x17D8, and the palette/font tail. This routine preserves both table pointers across the full reset and uses the secondary table only for side 1.
- TH09 fixes the weighted selection. The first RNG sample is promoted to int and walks ten entries beginning at opponent-shotType * 10, subtracting each non-null entry weight until the sample falls below a weight. If that window does not select an entry, a second RNG sample checks entries 210..219 in two five-entry groups with count guards; a miss or out-of-range result falls back to entry 210. The chosen entry's instruction pointer becomes runtime +0x04.
- The tail mirrors the exact Setup family: both VM script indices become -1, glyph size is 24, four text colors and four zero shadow colors are installed, font/selector bytes are initialized, scripts 0/1 are started through g_FrontMessageAnm, glyph-size bytes are copied into both VMs, and one blank glyph is drawn through AnmManager::DrawTextLeft for each VM.
- Natural pinned VC7.1 source emits 682 bytes versus the 693-byte target. Changing RNG/weight arithmetic from unsigned to ordinary int is target-required and restores signed JL branches. The remaining gap is local allocation/block scheduling: target keeps side transiently in EDX, opponent shot type in EBX and the selected index in EDX, while the natural candidate chooses equivalent EBX/EDX/EDI lifetimes and a slightly shorter five-entry fallback layout. No register forcing, var_order, volatile steering, padding, assembly or alternate-profile search is retained.


## Packet 425 Front added-callback source closure

- FrontAddedCallback @ 0x00418480-0x00418741 is now maintained source-present/non-exact. TH09 CreateGameSubsystem @ 0x00418840 allocates/registers the 0x1293C Front owner and installs this address directly as the calc-chain added callback, so callback ownership and ECX argument identity are target-local facts.
- The callback reuses AnmManager slot 10 when raw data is already present, otherwise preloads front.anm; failure returns -1. It executes Front scripts 0..6 in the seven leading 0x2A4 VMs, starts two Ascii VMs at Front +0x123F4/+0x12698 and clears their alpha bytes, and in GameManager modes 0/1 starts result scripts 85/86 at +0x11EAC/+0x12150 and selects the target character sprite +73.
- Front +0xE86C is independently exposed by target arithmetic as ten consecutive 20-byte diffuse vertices. The callback initializes them as a 2x5 loop with rhw 1.0 and diffuse 0x80FFFFFF, then writes two closed playfield-panel outlines using x coordinates 15/304 and 335/624, y coordinates 15/464, and z 0. The target loop itself is naturally reproduced by iterating the diffuse field pointer with nested do/while counts; no artificial padding or register directives are used.
- The callback then resets the exact 0x1D70 message runtime at Front +0xE944. In modes 0/1 it loads the character-specific plNN.msg path with no secondary path and start index 10*side1; otherwise it loads both plNN_match.msg paths with start index zero. Load failure returns -1, success calls the maintained Front message-runtime initializer and returns zero.
- The best natural pinned VC7.1 source is exact-sized at 706 bytes. Caching g_AnmManager in an ordinary local reproduces the target EDI lifetime; the field-pointer double loop reproduces the target vertex common loop instruction-for-instruction. Exactness remains false because independent panel-position stores are legally reordered/coalesced by VC7.1 and downstream block scheduling follows that alias/lifetime choice; the structural candidate has 292/558 ordinary comparable bytes. Further exact pursuit would be codegen steering rather than unresolved behavior.


## Packet 426 ExAttack type-21 trail initializer source closure

- ExAttackInitializeCallbackType21 @ 0x0044B220-0x0044B4F6 is now maintained source-present/non-exact. TH09 g_ExAttackTemplates row 21 fixes this address as the type-21 initialize callback. The exact draw callback shared by types 19 and 21 independently fixes the trail storage: 33 textured 0x1C vertices at extra +0x4C, 32 Float3 history samples at +0x3E8, and drawPending at +0x684.
- The initializer allocates one VM plus 0x68C extra bytes, starts script 37, initializes the usual ExAttack motion/control/current/spawn vectors, uses signed playfield-half-width-minus-24 for spawn x and RNG[0,128)+128 for spawn y, converts the spawn through the opponent side and restores the record side. History slot 0 receives the spawn and type21's additional +0x688 float is -pi.
- Vertex 0 receives rhw 1 and centered UV (0.5,0.5). The remaining 32 vertices/history entries are generated around a ring: angle starts at -pi and advances by 0.2026834, UV direction has magnitude 0.5, each history vector receives random magnitude 80 plus signed range 8 and is translated by the spawn, and a parallel 32-float angular-step array begins at +0x5FC. The angular step starts from signed range 0.06666667, receives signed range 0.033333335 each iteration, and clamps to +/-0.06666667.
- The maintained target-visible array form uses the magnitude array as the common loop pointer and addresses the angular-step array as magnitude[33], matching the target's physical +0x84 relationship and preserving the target 0x1C stack frame. It emits 714 bytes versus the 727-byte target; ordinary bytes remain aligned through offset 0x187 before the compiler folds the center-vertex pointer into the later UV pointer. A separate stored-step pointer spelling instead emits 728 bytes and a 0x20 frame. Closing the remaining gap would require deliberate anti-optimization/register-lifetime steering, so exactness remains false.


## Packet 427 ExAttack type-19 trail initializer source closure

- ExAttackInitializeCallbackType19 @ 0x0044A030-0x0044A32A is now maintained source-present/non-exact. TH09 g_ExAttackTemplates row 19 fixes 0x004A10A8 as this initialize slot, while row19 draw slot 0x004A10B0 and row21 draw slot share exact ExAttackDrawCallbackType19_21 @ 0x0044B940. Ownership, callback ABI and the trail layout are therefore target-local facts.
- The initializer allocates one VM plus 0x688 extra bytes and starts script 37. It initializes front motion/control/current-popup vectors, chooses a signed playfield-half-width-minus-24 spawn x and y = 448 - RNG[0,256), converts the spawn through the opponent side, restores the record side, and seeds history[0] from the spawn.
- The exact shared draw callback independently fixes 33 textured 0x1C vertices at extra +0x4C, 32 Float3 history samples at +0x3E8 and drawPending +0x684. The type19 initializer additionally fixes magnitudes +0x578 and angular steps +0x5FC. Vertex 0 receives rhw 1 and centered UV (0.5,0.5); the remaining 32 ring entries use angle step 0.2026834, UV radius 0.5, random history magnitude 80 plus signed range 8, and a signed angular step that starts within +/-0.06666667, receives +/-0.033333335 noise and clamps to +/-0.06666667. Two final drift values use signed range 0.0083333338.
- Natural pinned VC7.1 source is stable at 765 bytes versus the 763-byte target. Moving the next-vertex UV pointer declaration before the angular-step RNG is a bounded natural source-order probe and remains 765 bytes. Target keeps EDI as the center vertex base, advances it by 0x1C and then 0x14 into the next UV pointer, and thereafter advances EDI inside the loop before negative-offset UV stores. Stock VC7.1 folds the center pointer into fixed stores and computes the UV pointer directly. Reproducing that preserved-register lifetime would be deliberate anti-optimization/register steering, so exactness remains false.


## Packet 428 ExAttack type-19 trail update source closure

- ExAttackUpdateCallbackType19 @ 0x0044A330-0x0044A664 is now maintained source-present/non-exact. TH09 g_ExAttackTemplates row19 update slot at 0x004A10AC points uniquely here, while the maintained row19 initializer fixes the 0x688 extra layout and exact ExAttackDrawCallbackType19_21 fixes the 33-vertex/history draw family.
- State 0 is the standard 90-frame ExAttack Hermite transition from point10/control28 to point1C/control34. On completion it advances extra state, copies spawn40 to record position, transfers record ownership to the opponent side, resets the timer, starts side-local ANM script 9 and clears VM flag bit 1. State 2 exits after timer >20.
- State 1 updates the center vertex u/v and each of the remaining 32 ring vertices by drift67C. Any negative u or v triggers the target-visible inline 11-iteration, three-vertices-per-iteration wrap loop that adds 1.0 to that coordinate for all 33 vertices. Each ring magnitude at +0x578 is advanced by its paired angular step at +0x5FC (the target +0x84 relationship), history is rebuilt from angle/magnitude then translated by spawn40, and angle advances by 0.2026834.
- While 20 < timer < 300, the callback computes opponent Player position minus record position with exact Float3::operator-. At squared x/y distance <=4096 (radius 64), Player multipliers +0x1CE4 and +0x1CE8 are each multiplied by 0.40000001. The state1 result is 1 only while VM flag bit0 at +0x1F8 is clear.
- Extracting the repeated UV-wrap code into ordinary static helpers emits only 598 bytes and is therefore not the target source shape. Keeping all four target-visible loops inline, removing an unnecessary cached drift local, preserving the center-vertex pointer progression, using the target switch/common-return shape and retaining a side-record pointer for the collision block naturally closes the local frame to 0x20 and the complete extent to 821/821.
- Exactness remains false at 502/685 ordinary comparable bytes. The dominant residual is preserved-register allocation: target keeps EDI=record and ESI=extra, while stock VC7.1 assigns ESI=record and EDI=extra; downstream ring/collision/store scheduling follows that swap. Further pursuit would require register/lifetime steering rather than new semantic evidence, so no var_order, volatile, padding, assembly, register forcing or profile roulette is retained.


## Packet 429 ExAttack type-21 bullet-conversion update source closure

- ExAttackUpdateCallbackType21 @ 0x0044B500-0x0044B931 is now maintained source-present/non-exact. TH09 g_ExAttackTemplates row21 update slot 0x004A10CC points uniquely here. The maintained type21 initializer independently fixes the 0x68C extra block through angle688, and exact ExAttackDrawCallbackType19_21 fixes the 33-vertex / 32-history trail family.
- State 0 runs the standard 90-frame ExAttack Hermite transition and on completion advances state, copies spawn40 into record position, transfers owner to the opponent side, resets the timer, starts ANM script 11 and clears VM flag bit1. State 2 exits once timer >20.
- State 1 rebuilds UVs for ring vertices 1..32 from the rotating angle688 at radius 0.5, normalizing the UV angle by +0.2026834 each vertex. Independently, the history angle starts at -pi; each magnitude +0x578 advances by its paired angular step +0x5FC, history is rebuilt from that angle/magnitude then translated by spawn40, and angle688 itself advances by normalized +0.052359879 after the loop.
- The two long scans are target-backed Bullet pools rather than anonymous byte arrays. The opponent EtamaController primary pool has 175 usable 0x10C4 Bullet records and the secondary pool has 360. Each candidate must have nonzero Bullet::state +0xDBE, zero Bullet::unknown10BE +0x10BE, and sprites.unknownD44 !=330. At squared x/y distance <=1024 from the ExAttack position, the source bullet is marked unknown10BE=1 and a fresh 0x214 BulletSpawnDescriptor creates bullet type22 at the source position/angle, count1=count2=1, aimMode=1, no transform flags, with speed source+1.0 for primary or source+0.7 for secondary. The spawned bullet is also marked unknown10BE=1.
- Target code contains two physical descriptor-construction blocks. Keeping that source duplication is necessary: extracting a helper emits only 786 bytes. Likewise, caching the EtamaController shortens the natural body; the target repeatedly follows the GameManager side owner for primary pool, spawn, secondary pool and spawn.
- The target stack frame is 0x250 and contains one delta local plus distinct hidden 12-byte return temporaries for the primary and secondary Float3 subtraction calls. The reconstruction Float3 type has no default constructor, so a three-float raw lvalue is used to express this trivial target local without introducing a non-target constructor. That type/lifetime correction moves the candidate from 1027 to the exact 1074-byte extent with the exact 0x250 frame.
- Exactness remains false at 636/958 ordinary comparable bytes. The dominant residual is the same preserved-register family as type19: target EDI=record / ESI=extra versus stock VC7.1 ESI=record / EDI=extra, plus equivalent descriptor field-store scheduling. Further work would be register/store-order steering, so no var_order, volatile, padding, assembly, forced registers or profile roulette is retained.


## Packet 430 ExAttack type-3 trailing-beam update source closure

- ExAttackUpdateCallbackType3 @ 0x00442750-0x00442A5B is now maintained source-present/non-exact. TH09 g_ExAttackTemplates row3 update slot 0x004A0FAC points uniquely here, and maintained ExAttackInitializeCallbackType3 fixes the extra block as 0x190 bytes: state +0x00, motion Float3 +0x04, and 32 history Float3 entries beginning at +0x10.
- State 0 shifts the complete history tail backward by one sample and writes the current record position into history[0]. The four trailing VMs use target-proven AnmVm pos2 +0x288 and receive history[6], history[12], history[18], and history[24] minus the current position. These correspond exactly to target dynamic-data offsets +0x52C/+0x7D0/+0xA74/+0xD18 for VMs 1..4.
- A 12x12 collision-size vector is constructed, Supervisor selects the opponent side, and current/history12/history24 positions are converted through exact inverse popup X/Y helpers. The opponent Player is tested against all three samples through exact CheckBulletCollision. A hit resets the timer, sets extra state to 1, and sends interrupt 1 to all five VMs. State 1 returns completion when timer >=15.
- When no collision occurs and timer >=30, record position advances by extra motion. The callback returns completion once the oldest history sample leaves x[-32,672] or y[-32,512]. This target tests history31 rather than the just-updated record position.
- Four direct vm.pos2 = history - position expressions are retained in the update body because target contains four physical Float3 subtraction/copy blocks; extracting a helper shrinks the update to 723 bytes. A normal ExAttack-family switch source also reproduces target SUB/DEC state dispatch.
- The best natural pinned VC7.1 body is stable at 793 bytes versus the 780-byte target. The residual is a coherent preserved-register/lifetime frontier: target keeps EDI=extra and EBX=record-position, so the history-shift destination/source cursors remain in EAX/ECX and only the count spills. Stock VC7.1 keeps EBX=extra and EDI=position, spills both cursors, and uses a 0x6C local frame rather than target 0x64. This is the same EBX/EDI allocation family already documented for the type4 initializer. Further pursuit would be register/lifetime steering, so no var_order, volatile, padding, assembly, forced registers or profile roulette is retained.


## Packet 431 ExAttack type-4 companion-trail update source closure

- ExAttackUpdateCallbackType4 @ 0x00442BD0-0x00442EB2 is now maintained source-present/non-exact. TH09 g_ExAttackTemplates row4 update slot 0x004A0FBC points uniquely here. Maintained ExAttackInitializeCallbackType4 fixes the 0x190 extra block as state +0x00, parameterVector +0x04 and history[32] +0x10; it also proves parameter38 is the source ExAttack record from which type4 copied its motion vector. Exact type4 release closes extra ownership.
- State 0 shifts the history backward, writes current position to history0, and assigns VM1..4 pos2 from history[6/12/18/24] minus record position. The four history pointers are intentionally retained because target later reuses those same addresses for collision-queue insertion.
- The callback follows parameter38->extra34 to the source trail. If that pointer is null or source state is nonzero, type4 resets its timer, enters state1 and interrupts all five VMs. State1 completes when timer >=15.
- While the source remains active and timer >=30, type4 advances its position by parameterVector04. It appends five radius-12 circle records with tail value 0 to the opponent Player collision-query owner at +0x36C: current position followed by history[6], history[12], history[18] and history[24]. Lifetime is tied to the source trail: completion occurs when sourceExtra->history[24] leaves x[-32,672] or y[-32,512].
- The target local frame is exactly 0x50. An indexed do/while history source lets stock VC7.1 derive the target EAX/ECX induction variables with only the count spilled at [ebp-4], reducing an initial 763-byte pointer-loop candidate to 740 bytes and matching target EBX=extra / EDI=position plus the four retained history-pointer locals. This is strong natural source-shape evidence.
- Exactness remains false by one byte of extent (740 versus 739), with additional stack-slot differences among otherwise equivalent compiler temporaries. Target places the collision-size object below the four Float3 subtraction return temporaries and uses the constructor-return EAX for the first six-byte x=12 store; the natural candidate assigns the opposite temporary-stack order and emits that store through EBP, adding one byte. Moving sourceExtra declaration after collision-size initialization emits 737 bytes and moves sourceExtra to the wrong lifetime/stack slot, so it is rejected as a worse natural hypothesis. Further pursuit would be scheduler/temp-order steering; no var_order, volatile, padding, assembly, forced registers or profile roulette is retained.


## Packet 432 ExAttack type-7 double-shot update source closure

- ExAttackUpdateCallbackType7 @ 0x004464A0-0x004467BE is now maintained source-present/non-exact. TH09 g_ExAttackTemplates row7 update slot 0x004A0FEC points uniquely here. Exact ExAttackInitializeCallbackType7 fixes the 0x5C extra block shared with the type8/9 family: state +0x00, angle08 +0x08, angle0C +0x0C, motion +0x14, Hermite points/controls +0x20/+0x2C/+0x38/+0x44, and spawn +0x50.
- State 0 runs the target-standard 90-frame Hermite transition. At completion it starts script 9, advances state, copies spawn50 to record position, transfers record owner to opponentSide04 and resets the timer. State 2 completes after timer >20.
- State 1 begins only after timer >20. Every HasTickedEvery(10 - GameManager difficulty) edge, angle0C advances by normalized +0.10471976. A target-sized 0x214 BulletSpawnDescriptor is constructed at the bottom of the 0x224 frame. The first primary pattern uses bulletType 6, color 2, aimMode 5, count1 = opposing Player +0xA0 / 5 + 2, count2=1 and the updated angle. It installs wait transform 80 and vector acceleration 120 with magnitude ((PlayerA0*0.1)+2)*0.0083333338 at angle -999, then spawns through the opponent EtamaController.
- The same descriptor then becomes the mirrored second pattern by setting bulletType=0, aimMode=3 and angle=AddNormalizeAngle(pi,-angle0C); color/count/transforms remain shared. Target field-store order confirms aimMode is written before the Player count calculation and color follows count2.
- After emission, opponent Player collision is checked with a 24x24 box. A hit enters state2, resets the timer, interrupts the VM, stores extra angle08 into record rotation +0x3C and returns zero. Otherwise motion14 is rebuilt from angle08 at speed 1.5, record position advances, and state1 completes if y >=480, y <=-32, x >=176 or x <=-176. The rotation field receives angle08 on every surviving state1 frame.
- The natural pinned VC7.1 source reproduces the exact 0x224 frame and is stable at 783 bytes versus the 799-byte target. The remaining 16-byte family is not missing gameplay: target shares the early state2 return-one epilogue at 0x004464EC with all four bounds checks, producing four six-byte backward near branches; ordinary repeated C++ returns produce local two-byte short branches. An explicit shared-label probe is itself block-reordered by VC7.1 and emits 778 bytes, so it is rejected rather than used as layout steering.
- A second corroborating ABI clue comes from adjacent committed generations: TH08, TH095 and TH10 all declare Float3::FromAngleMagnitude as void, and TH09 target 0x00441890 likewise never writes EAX. The row7 target therefore reuses ECX immediately after the call when forwarding motion14 to Float3::operator+=. The current TH09 reconstruction-wide declaration returns Float3*, causing stock source to reload motion14. Changing that shared declaration here would perturb established exact/source units, so no TU-local symbol/ABI trick is retained. Exactness remains false without var_order, volatile, padding, assembly, forced registers or profile roulette.


## Packet 433 ExAttack shared type-18/type-24 trail update source closure

- ExAttackUpdateCallbackType18_24 @ 0x004491E0-0x0044977B is now maintained source-present/non-exact. TH09 g_ExAttackTemplates rows 18 and 24 both point their update slot to this body (0x004A109C and 0x004A10FC). Exact ExAttackInitializeCallbackType18 and Type24 independently fix one shared callback ABI, four ANM VMs, and the 0x158 extra family.
- The extra tail is target-proven structure rather than an opaque blob: trailPositions58 is 16 consecutive Float3 values and trailAngles118 is 16 floats. State 0 runs the standard 90-frame Hermite transition; on completion it starts side-local scripts 9/10/10/10, advances state, copies spawn40 into the record, transfers ownership to opponentSide04, resets the timer, computes angle4C with the opponent Player AngleToPoint boundary, sets speed50=0.2/reflected54=0, and seeds all 16 trail positions/angles from current position/rotation.
- State 1 shifts positions/angles backward by one entry. VC7.1 naturally lowers the 15-entry reverse shift into the target 5-item-unrolled x3 outer loop. VM0 receives trail angle 0; VMs1/2/3 receive pos2 from trail positions 1/3/5 minus the current record position and rotations from angles 1/3/5. Three named Float3 deltas are target-natural: removing them collapses the 0x48 target frame to 0x24 and shortens the body from 1435 to 1362 bytes.
- After timer >20, motion04 is rebuilt from angle4C/speed50 and speed grows by 0.1. A 24x24 Player collision enters state2, resets the timer and interrupts all four VMs. Otherwise position advances by motion. While reflected54 is zero, the target physically contains separate left-x<-144 and right-x>144 reflection bodies; keeping two independent C++ if statements is required, moving the natural candidate from 1395 to 1435 bytes. Each body negates motion.x, normalizes pi-angle4C through exact AddNormalizeAngle and sets reflected54=1. A forward radius-12 circle is appended to the opponent Player collision-query owner, and y>=480 completes the callback.
- State 2 completes after timer >20. The best natural switch source cold-replays stably at 1435 bytes versus the 1436-byte target and reproduces the target 0x48 frame plus ESI=record / EDI=extra lifetime. A direct-expression trail-delta spelling shrinks to 1362 with a 0x24 frame; caching dynamicData/Player lifetimes produces 1371-1395-byte variants.
- The final one-byte extent frontier is control-flow layout, not missing gameplay. The switch spelling preserves target SUB/DEC/DEC state dispatch but VC7.1 tail-merges the state2 return-one epilogue with the state1 y-bound return. A natural nested-if spelling keeps a separate state2 epilogue but emits a CMP chain, physically moves state0, and grows to 1451 bytes. No padding, explicit goto bridge, var_order, volatile/register forcing, assembly or profile roulette is retained merely to choose the target block layout.


## Packet 434 ExAttack shared type-14/type-22 update source closure

- ExAttackUpdateCallbackType14_22 @ 0x00443DF0-0x00444114 is now maintained source-present/non-exact. TH09 g_ExAttackTemplates rows 14 and 22 both point their update slot to this physical body (0x004A105C and 0x004A10DC). Exact type14/type22 initializers independently fix one VM and the shared 0xF48 extra front through value50 +0x50.
- The shared draw callback at 0x00444130 independently closes the extra tail. Extra +0x54 is a 33-entry 0x14-byte diffuse center fan; +0x2E8 is four 33-entry 0x14-byte fan groups; +0xD38 is four groups of 33 floats. These regions end exactly at +0xF48, so the update no longer needs opaque post-0x54 storage assumptions.
- State 0 runs the standard 90-frame ExAttack Hermite transition, starts script 9 through the side-local Player ANM owner, advances state, copies spawn40, transfers record ownership to opponentSide04 and resets the timer. State 1 computes opponent-player minus record-position, advances by motion04, completes on a 24x24 Player hit or y>480, and otherwise waits until XY distance is below 96 before playing sound 16 with side-dependent pan, clearing VM flag bit0/current field, setting VM +0x1F0 to -1, advancing state and resetting the timer.
- State 2 applies the target quadratic ease-out value50 = (1-(1-t/48)^2)*64. After timer>1 it either advances state on collision or appends a Player collision-query circle at record position with radius value50*0.8. Timer>=30 also advances state and resets the timer.
- State 3 maintains four 32-sample random amplitude groups. Angle starts at -pi, wraps at pi, advances by 0.2026834 per sample and receives an extra +0.050670851 per group. Every sample rewrites the group's first fan vertex z to zero and stores signed RNG with max equal to the timer float. The four fan-group base pointers advance by 0x294 and the random pointers by 0x84, directly matching the draw-proven layout. Timer>=30 completes the callback.
- The best natural source uses direct repeated Player lookups across calls and a separate collision-size local. The key lifetime discriminator is the state1 delta: direct/component forms let VC7.1 elide the unused z copy and produce an 800-byte symbol; declaring PlayerPositionView delta first and assigning the operator- result preserves the target hidden-return temporary, three-dword copy and 0x34 frame. That source is stable at 816 bytes because VC7.1 counts the 16-byte dense-switch table at object offsets +0x320..+0x32F inside the function COMDAT; the TH09 executable tracks an 805-byte code body and places its jump table separately at 0x00444118. Exactness is therefore not claimed, and no padding or block-layout steering is used.


## Packet 435 ExAttack shared type-14/type-22 draw source closure

- ExAttackDrawCallbackType14_22 @ 0x00444130-0x004444EE is now maintained source-present/non-exact. TH09 g_ExAttackTemplates rows 14 and 22 both point their draw slot to this physical callback at data slots 0x004A1060 and 0x004A10E0. Exact type14/type22 initializers and the maintained shared update independently fix the common 0xF48 extra layout.
- The draw path activates for extra state >=2. It derives alpha from full opacity for state2 or from 255 - timer*255/30 otherwise, writes a 33-vertex diffuse triangle fan centered on record position with radius value50, and draws it through AnmManager after setting the VM field +0x1F0 to -1. The target-observed center alpha shift by 23 and ring alpha shift by 24 are retained literally because they are physically distinct target operations.
- After flushing the fan, the callback switches Direct3D texture-stage and blend/z-write state, then renders four 32-segment line strips from the four fan groups at extra +0x2E8 using the matching random-radius arrays at +0xD38. Each group adds a 0.050670851 angular phase step. The device uses FVF 0x44 and DrawPrimitiveUP line-strip primitive count31/stride20. The AnmManager cached state is then cleared and the original texture-stage operations are restored before drawPending +0x4C is cleared.
- Natural pinned VC7.1 source emits 974 bytes versus the 959-byte target. A bounded source-order probe moves the center-fan pointer before the alpha branch, matching the target early push-ESI and center-pointer lifetime, but the size remains unchanged. The 15-byte surplus is localized to the outer line-strip inner loop: target keeps EBX=record and EDI=extra, freeing EBX for the random pointer while ESI is the line vertex. Stock VC7.1 chooses EDI=record / EBX=extra, which requires an adjusted position pointer plus repeated random-pointer reload/store instructions. Further exact pursuit would be preserved-register/lifetime steering, so exactness remains false without var_order, volatile, padding, assembly, forced registers or profile roulette.


## Packet 436 Supervisor DirectPlay message-handler source closure

- SupervisorNetworkMessageView::HandleMessage @ 0x00432B70-0x00432E6F is now maintained source-present/non-exact. Exact SupervisorNetworkMessageThunk is its sole caller: the thunk reloads g_SupervisorNetworkState @ 0x004B42D0 into ECX and preserves the DirectPlay callback stack surface userContext/messageId/message. The pinned VC7.1 Platform SDK dplay8.h fixes the message constants and DPNMSG structure offsets rather than relying on hand-written vtables.
- The handler recognizes DPN_MSGID_RECEIVE, CREATE_PLAYER, DESTROY_PLAYER, SEND_COMPLETE and CONNECT_COMPLETE. All recognized cases enter Supervisor critical-section id4 and share the exact leave boundary; CREATE_PLAYER has the target-observed error return after leaving. CONNECT_COMPLETE maps successful state1 connection to state3/localSide1. SEND_COMPLETE sets packetReady. CREATE_PLAYER performs the normal two-pass IDirectPlay8Peer::GetPeerInfo query, allocates/zeros a 24-byte DPN_PLAYER_INFO buffer through VC7.1 operator new, tests DPNPLAYER_LOCAL, stores the new DPNID into peerIds[localSide] or peerIds[1-localSide], and deletes the temporary. DESTROY_PLAYER clears matching peer IDs.
- RECEIVE uses the target packet protocol. Type0 is the 12-byte frame packet {type, signed side, seed, frame, 16-bit input/checksum}; remote-side packets are copied to network-state +0xC8 and forwarded to exact SupervisorFrameQueueView::InsertReceivedFrame. Type1 validates the TH09-local token pair, sets packetReady and snapshots the neutral receive-time global. Type2 is the existing 12-byte handshake {type, signed side, seed, syncRate, syncValue}; it reconciles localSide, restores exact ReplayRng seed, copies sync state, and prefills paired sentinel frames beginning at -9998 when 60/syncRate requires it. All RECEIVE paths update the target-observed receive timestamp pair.
- Source ordering is codegen-relevant but natural: RECEIVE, CREATE_PLAYER, DESTROY_PLAYER, SEND_COMPLETE, CONNECT_COMPLETE matches the target's physical switch block order. With signed packet-side fields, the target-observed global localSide lookup on frame receive, and a while-form sentinel prefill loop, VC7.1 emits the authored code body with the same case entry offsets: RECEIVE +0x27, CREATE_PLAYER +0x19D, DESTROY_PLAYER +0x26F, SEND_COMPLETE +0x29E, CONNECT_COMPLETE +0x2B6, common leave +0x2EB and ret 0xC at +0x2FD.
- The COFF function symbol spans 0x328 bytes because VC7.1 places the 24-byte six-entry jump table and 16-byte byte-map immediately after the code body. TH09 places the same 40 bytes at 0x00432E70-0x00432E97, outside the authored 768-byte function ledger extent; all six jump-table relocations solve to the exact TH09 case entry addresses. Exactness is still false: same-length differences remain in the 16-bit frame argument load schedule, a few volatile registers, and the common-leave mov/push order. Further pursuit would be scheduler/register steering, so no var_order, volatile, padding, assembly, forced registers or profile roulette is retained.


## Packet 437 GameManager opponent-selection source closure

- GameManagerOpponentSelectionView::SelectOpponentConfiguration @ 0x00415910-0x00415C2E is now maintained source-present/non-exact. GameManager_GameplaySetupThread is the only caller family and invokes this address four times with g_GameManager @ 0x004A7D90 in ECX, fixing GameManager ownership and the ordinary thiscall ABI.
- TH09 exposes a 16-byte sentinel-terminated opponent-entry table at 0x004A13B8. The reconstruction view names key +0x00, selectedValueF4 +0x02, character +0x04 and weight +0x0E; the next entry begins at +0x10 and a negative key terminates scans. The pointer table at 0x004A14C8 selects a per-character list for game modes 0/1. The exact RNG at 0x004ACE0C, exact IsGameMode0/1 and exact PlayStatsRecordView::IncrementCharacterStat5 bound all external calls.
- In game modes 0/1 the callback filters the selected character list by GameManager counterFC +0xFC, builds up to sixteen candidate pointers in the target 0x40 local frame, sums 16-bit weights, performs a weighted RNG selection, skips characters already marked in the GameManager +0xD8 used-character array, writes side1 character +0x58, selected entry +0xF0 and selected value +0xF4, increments PlayStats stat5, then chooses side1 +0x64 from the flat int table at 0x004A1500 using 5*(counterFC + 9*setupMode). When side0 state +0x14 is set or state float +0x00 is non-positive, the neutral runtime value at 0x004A7E90 is clamped to 3 and selects one of the following four table variants.
- Outside modes 0/1 the callback first sets counterFC=9. selector +0x138 == -1 chooses one of the two current side characters through exact RNG; selector == -2 chooses randomly among the TH09 character-unlock bytes at 0x004A81CC with network-active override from g_SupervisorNetworkState +0xA8; other selector values name a character directly. All three paths scan the same 16-byte master table, then write both side selectedParameter2C fields from the setupMode-indexed value table at 0x004A1884 plus selected entry/value fields.
- Source-shape probes are bounded. Extracting the repeated master-table scan into a static helper emits only 685 bytes, while retaining the three physical scans produces the best 797-byte candidate versus target 799 and reproduces the target 0x40 frame. TH09 shows 32-bit wanted-character values compared against sign-extended 16-bit table fields, and the weighted selector compares AX/SI as signed; those types are retained. Making the roll itself a short spills it and grows to 813 bytes. A shared post-scan fallback spelling also grows to 813 and is rejected. Modeling 0x004A1500 as a flat int table preserves 797. The final two-byte extent frontier is compiler block/register scheduling, so exactness remains false without var_order, volatile, padding, assembly, forced registers or profile roulette.


## Packet 438 Player type-9 position callback source closure

- PlayerPositionCallback30404Type9 @ 0x00447FB0-0x00448312 is now maintained source-present/non-exact. TH09 data table 0x004A1A50 is the 16-entry Player callback30404 family; slot 9 at 0x004A1A74 contains this address. Neighboring entries independently map to maintained type6/7/8/11/14/15 callbacks, so Player ownership and the two-register fastcall callback surface are table-local facts rather than address-neighborhood inference.
- Existing TH09 Player callback sources independently fix rangeSource +0x368, range radius +0x7C, angle +0x80 and Player position +0x1B88. The callback first computes Player-position minus the queried point. Squared XY distance below radius² succeeds immediately; squared distance above (radius+24)² fails immediately.
- In the annulus between those bounds, the callback physically evaluates six sample centers around the Player. The first uses source angle directly and the next five normalize source angle plus 1.0471976, 2.0943952, pi, 4.1887903 and 5.2359877. Every sample has magnitude radius+24, is translated by Player position, subtracts the queried point, and succeeds when XY squared distance is below 576 (24²). The six bodies are retained explicitly because TH09 contains six physical FromAngleMagnitude/add/subtract/check blocks.
- Exact helper relocations from the natural candidate resolve to PlayerPositionView/Float3 operator- @ 0x00401140, operator+= @ 0x00405730, Float3::FromAngleMagnitude @ 0x00441890 and AddNormalizeAngle @ 0x0042AED0. The final source reproduces target stack frame 0x1C, ESI=Player, EDI=query point, EBX=&Player.position and the full 867-byte target extent.
- A bounded lifetime probe demonstrates why no cross-block range-source cache is retained. Caching rangeSource/radius/outerRadius makes VC7.1 keep ESI as rangeSource and emits only 796 bytes. Repeating the target-visible Player +0x368 loads restores the target preserved-register lifetime and exact extent. Exactness remains false at 583/719 ordinary comparable bytes because the compiler swaps equivalent x/y FPU evaluation order and schedules the hidden-return three-dword position copies differently. Further work would be scheduler/register steering, so no var_order, volatile, padding, assembly, forced registers or profile roulette is used.


## Packet 439 Controller keyboard/input aggregator source closure

- Controller::GetInput @ 0x0042BE40-0x0042C172 is now maintained source-present/non-exact. Two TH09 callers in Supervisor_NetworkServiceUpdate explicitly place the requested input index in ECX immediately before calling this address: the local network side on the normal sample path and literal 2 on the combined/abort check path. This fixes the one-int fastcall API even though this TH09 implementation does not consume the parameter in its body.
- The callback starts with a 256-byte keyboard-state local and a zero 16-bit mask. It skips keyboard sampling when g_WindowActivateWParam @ 0x004B30B8 is zero. When global g_Supervisor keyboard +0x10 is null it uses USER32 GetKeyboardState; otherwise it calls IDirectInputDevice8::GetDeviceState(256, state), clears the mask, and calls Acquire when the result is DIERR_INPUTLOST or any other nonzero HRESULT.
- Both keyboard paths map the same TH09 key family. X/Z produce shoot/bomb; Shift contributes shoot|focus; Escape/menu, Control/skip, Return/enter, Home or P/home, D/Q/S/R and the four cardinal arrows plus numpad cardinal/diagonal keys produce the target 16-bit Touhou mask. The target has no L-key contribution present in the later TH095 family. The four diagonal numpad keys produce 0x50/0x90/0x60/0xA0 combinations exactly as the target bit-packing shows.
- After keyboard sampling, g_Supervisor.config bytes +0xB7 and +0xB8 select controller assignments. Values 0 or 1 call maintained Controller::GetControllerInput with controller index 0 or 1, joystickIndex = assignment != 0, and the accumulated keyboard mask; other assignment values leave the mask unchanged. The second call can return directly in AX.
- Clean TH095 Controller::GetInput was consulted only after TH09 had independently fixed the window-active global, keyboard pointer, VK/DIK offsets, button masks, assignment bytes and GetControllerInput boundary. It corroborates the broad natural source family and the inputIndex surface, not TH09 exactness.
- Natural pinned VC7.1 source reproduces the target 0x100 local frame, ESI accumulator, DirectInput error branches and controller tail but emits 835 bytes versus target 819. A bounded reorder of the key statements to the target-observed load sequence does not change a byte of codegen or the extent, demonstrating that VC7.1 reassociates the independent OR expression tree. Further exact work would be expression-tree/local-allocation steering, so exactness remains false without var_order, volatile, padding, assembly, forced registers or profile roulette.


## Packet 440 Front message-runtime draw source closure

- FrontMessageRuntimeView::Draw @ 0x00417130-0x00417512 is now maintained source-present/non-exact. Exact CreateGameSubsystem/FrontFactory @ 0x00418840 registers FrontDrawCallback @ 0x00417C10 for the 0x1293C Front object. That draw callback uniquely invokes 0x00417130 with ECX = Front + 0xE944. Exact FrontAddedCallback and the maintained message load/setup family independently fix +0xE944 as the embedded 0x1D70 FrontMessageRuntime, so owner and thiscall receiver identity are direct TH09 facts.
- The runtime contains eleven consecutive 0x2A4 AnmVm objects beginning at +0x14. Existing Setup source already fixes text VMs 8/9 at +0x1534/+0x17D8 and the complete tail through +0x1D70. Draw uses selector byte +0x1D64 to order VMs 0/1/4/5/6/7 in three target-observed permutations, then draws VMs 2/3 through exact AnmManager::DrawNoRotationFlippedX. The final VMs 8/9/10 are always drawn through exact DrawNoRotation.
- A ZunTimer at +0x1D58 controls the message-box overlay. When current >=40, height is 60; otherwise target calls exact ZunTimer::operator float and multiplies by 1.5. Only positive height draws the overlay. The four local 20-byte diffuse vertices are (64,364), (576,364), (64,364+height), (576,364+height), z=0/rhw=1. Top diffuse is 0xD0000000 and bottom diffuse is 0x90000000. Byte +0x1D6D gates the actual D3D draw after the vertex buffer flush.
- The overlay path directly performs the TH09 D3D8 state sequence: SELECTARG1/diffuse texture stages, Supervisor::SetRenderState(ZWRITEENABLE,false), FVF 0x44 and DrawPrimitiveUP triangle strip count2/stride20; then exact AnmManager clear-cache helpers restore vertex/color/blend/z-write state and the texture-stage ops return to MODULATE/TEXTURE.
- The natural target-visible source under the standard Front /O2 /Ob1 /Oy- profile emits 1000 bytes versus target 995. Up to the local vertex construction, instruction offsets and the 0x60 frame follow target. The sole cumulative five-byte extent delta is VC7.1 local-array construction: target calls generic vector-constructor iterator 0x00401470 with four 0x14-byte vertices and folded constructor 0x004343D0, while stock /O2 expands a four-iteration ctor loop and preserves EBX around it.
- This is the same mixed-profile hard frontier already documented for ScreenEffect::DrawSquare. A bounded /O1 probe and a compiler-feature-motivated /O2 /Os probe both emit 998 bytes but switch the whole function to size-style code (0x64 frame, extra saved registers, different argument generation), so they are rejected rather than retained. No explicit compiler-helper call, pragma, padding, assembly, volatile/register forcing or profile roulette is used merely to reproduce the iterator.


## Packet 441 Front calc-callback source closure

- FrontCalcCallback @ 0x00417630-0x00417C02 is now maintained source-present/non-exact. Exact CreateGameSubsystem/FrontFactory @ 0x00418840 allocates the 0x1293C Front object and installs this address directly as its calc-chain callback at priority 22, fixing both owner and ECX callback argument.
- Existing TH09 Front sources independently close the layout used here: seven base VMs at +0x0C; ten 0x14 diffuse panel vertices at +0xE86C; two panel colors +0xE934 and timers +0xE93C; embedded 0x1D70 FrontMessageRuntime +0xE944; dynamic VM +0x106B4/sprite +0x10958; transition count/frame/side +0x1095C/+0x10960/+0x10964; seven transition VMs +0x10968; transition-start/state +0x11EA4/+0x11EA8; and result/marker VMs through +0x12698.
- Every calc tick executes the seven base VMs, the two result VMs and the dynamic VM. A changed nonnegative dynamic sprite restarts Front script 87 and selects sprite global+82. The two five-vertex panel groups then pulse between their side color, full white and 0x80FFFFFF according to the two countdowns before the embedded message runtime Update call.
- When transitionVmCount is zero the callback returns active immediately. It always executes transition VMs 0..2 and, for count>=2, VMs3..6. When the message runtime has ended, game modes 0/1 update the target side-completion/ending state using transition side, stage counter and exact replay-neutral predicate; other modes set the alternate completion byte. The duplicated mode0/mode1 source spelling is retained because VC7.1 naturally produces the target shape with mode0 logic physically ahead of the mode1 cold test.
- The count==1 transition state machine is target-closed. Frame 0 rewrites GameManager bits 0x1800 using the physical two-stage (1 << (1-side)) << 11 expression, clears the opposing transition timer, creates the four radius-500 Player collision regions centered at (0,224,0), and resets both embedded Player +0x30410 owners. Frame 30 plays sound49, writes opposing side-state +0x1C/+0x20 = 1/30, then creates two side-specific ScreenEffect shakes.
- Frame 60 uses exact IsGameMode2. Non-mode2 side0 loads neutral message index +8. Side1 decrements side0 runtime value00/increments value34 while positive and marks transitionStarted; once nonpositive it loads neutral message index +10. Mode2 compares the contiguous runtime counters at 0x004A7E90..0x004A7E9C and either runs Front message random setup for transitionSide or marks transitionStarted. Independent transitionStarted conditions handle frame160 (both FrontSide BeginAuxTransition calls) and frame190 (GameManager cleanup plus mode0/1 result-table selection into side1 selected parameter).
- The best natural pinned VC7.1 /O2 source is 1476 bytes versus target 1491. The dominant stable residual is the panel pulse loop. Target keeps EDI=diffuse cursor, EDX=color cursor, EBX=middle diffuse cursor and spills the two-iteration outer count; stock VC7.1 proves middle=diffuse+0x14, folds the diffuse cursor into middle-0x14 and uses EBX for the count. A target-natural flat ten-vertex view and a 2x5 panel view compile identically, so further pursuit would require defeating CSE/register allocation rather than resolving behavior. No var_order, volatile, padding, assembly, forced registers or profile roulette is retained.


## Packet 442 Front draw callback canonical exact closure

- FrontDrawCallback @ 0x00417C10-0x004181D1 is now repository-canonical exact. Exact CreateGameSubsystem @ 0x00418840 installs this address directly as the draw-chain callback for the 0x1293C Front owner at draw priority 28, fixing owner and the single ECX callback argument. Native target disassembly confirms IDA's provisional extra-EBX usercall argument was spurious: the function never reads or writes EBX, while EDI is initialized internally to viewport index 2.
- The natural source draws the seven base VMs, two marker VMs, both five-vertex Front line strips, and the embedded message runtime; conditionally draws and screen-transforms transition VMs across viewport 0/1; queues the seven result values through exact AsciiManager::AddFormatText; restores viewport 2; emits mode-dependent time/stage text; then draws the dynamic and two result VMs.
- Existing TH09 exact/maintained dependencies independently close all external boundaries used by the exact body: Supervisor::ConfigureScreenViewport @ 0x00403D90, AnmManager::DrawNoRotation @ 0x0043A950, FrontDrawLineStrip @ 0x00422330, maintained FrontMessageRuntimeView::Draw @ 0x00417130, AsciiGameManagerView::TransformPopupX/Y @ 0x00401680/0x004016B0, AsciiManager::AddFormatText @ 0x00434330, Float3::Float3 @ 0x004010B0, and GameManager::IsGameMode2 @ 0x00415D30.
- The exact source shape required two target-visible lifetime/CFG corrections. First, the game-time /3600 and (/60)%60 expressions must remain inside each IsGameMode2 branch; caching them before the branch shortened the candidate from the target 1474 bytes to 1413. Second, the transitionVmCount>=2 guard for result text is a separate if from the transitionSide==1 transition-VM draw block; nesting text under the first count check let VC7.1 eliminate the target's second 12-byte count guard and emitted 1462 bytes.
- With those ordinary C++ shapes, pinned VC7.1 /MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- reproduces all 1474 bytes. The canonical unit records 127 relocations and returns exact 1474/1474. Two independent cold unit rebuild/replay passes both return exact with an identical relocation map digest. No register forcing, volatile, padding, assembly, fake return, target-byte embedding or profile roulette is used.


## Packet 443 Replay input IsHeld source closure and TU visibility

- Historical note: the int-mask exactness conclusion in this packet is superseded by Packet 446. The TU-visibility experiment remains valid.
- ReplayInputState::IsHeld @ 0x00415CB0-0x00415CC1 was initially maintained source-present/non-exact under an int-mask declaration. Exact ReplayInputState::Update @ 0x004200F0 independently fixes historyCurrent as the 16-bit field at +0x2C; exact ReplayManager callers already declare and invoke IsHeld on the same 0x8E-stride ReplayInputState family. The target body zero-extends historyCurrent, intersects it with the caller mask, returns the low 16-bit result in AX, and pops the one stack argument.
- Natural pinned VC7.1 source returns historyCurrent & mask in 16 bytes. The 18-byte target performs an otherwise equivalent separate load of the stack mask into ECX followed by AND EAX,ECX; stock source folds those instructions to AND EAX,[EBP+8]. Return-expression and ordinary mask-assignment forms collapse to the same 16-byte code, so exact pursuit stops before register/volatile steering.
- TH09 also provides a useful translation-unit visibility fact. Adding the tiny IsHeld definition anywhere inside the existing ReplayInput.cpp causes canonical ReplayManagerView::PlaybackInputAndFps @ 0x004204B0 to grow from its exact 299-byte body to 300 bytes. Restoring ReplayInput.cpp and compiling IsHeld in a standalone TU restores PlaybackInputAndFps to canonical 299/299 exact. The maintained separate source therefore preserves a target-observed compiler-visibility boundary rather than inventing a split solely for organization.
- TH095 was checked only for source-shape corroboration and independently has the same +0x2C historyCurrent / +0x32 historyPressed input-family layout; no TH095 address, owner, ABI or exactness claim is promoted into TH09.


## Packet 444 Etama laser-spawn source closure

- EtamaController::SpawnLaserPattern @ 0x00413290-0x00413451 is now maintained source-present/non-exact. Its sole TH09 code xref is EclManager_RunEcl's laser-create opcode path, and maintained EclRunBullet already names the call Controller(enemy)->SpawnLaserPattern(laserDescriptor). This is reinforced by the target-backed EtamaController layout: lasers[48] begin at +0x24D424, each Laser is 0x59C bytes, spawnSuppressionFrames is +0x25E170, sideState is +0x25E190, and bulletAnm is +0x25E1BC. BulletManager.hpp already carried the method declaration before this packet.
- The method starts at lasers[0]. A nonzero spawn-suppression counter blocks creation unless descriptor transformFlags bit 2 is set. Otherwise it scans up to 48 Laser records for inUse==0. A full pool returns the one-past-end scan pointer, matching the target rather than manufacturing a null failure.
- On a free slot, exact AnmLoaded::SetAndExecuteScriptIdx initializes the body VM with bulletType+10. Exact SetSprite adds descriptor color to the body's current sprite, and exact InitializeAndSetSprite initializes the cap VM from the signed-color table at 0x004A0F18 plus 146. Cap VM flags clear bit 0x20 and set 0x10. The target then copies descriptor position/color, marks inUse, installs the descriptor angle and conditionally adds exact PlayerAngleView::AngleToPoint when aimMode==0.
- The remaining descriptor-to-Laser copy is exact in layout and semantics: transformFlags low word -> flags; timer=0; laserStartOffset/endOffset/startLength/width; speed1; laserStartTime, hitboxStartTime, duration, despawnDuration and hitboxEndDelay. hideCapDuringStartup is cleared and initial state becomes startTime==0.
- A first natural source cached bulletAnm and still emitted 449 bytes, but changed the preserved-register lifetime: VC7.1 used EBX for ANM and spilled this. Removing that cache reproduces target EBX=this and all three target-visible reloads from +0x25E1BC while staying 449/450.
- The remaining one-byte extent frontier is the pool scan. Natural while and infinite-for spellings both produce the same 449-byte rotated loop. A bounded do/while spelling reproduces the target xor/alignment/top-test/bottom-jl scan exactly, but ordinary source then requires an extra post-loop full-pool check and emits 459 bytes. An index-bounded spelling triggers VC7.1 six-way unrolling and emits 572. Further pursuit would be block-layout steering rather than new behavioral evidence, so no goto-only reshaping, volatile, forced registers, padding, assembly or profile roulette is retained.


## Packet 445 CardAttack start/install canonical exact closure

- CardAttackStartView::StartAttack @ 0x00403E50-0x004041EA is now repository-canonical exact. Multiple TH09 Player callers pass the side-local CardAttack owner in ECX and four stack arguments. Exact CardAttack lifecycle reconstruction independently fixes the 0x1368 owner, its two presentation counters, seven VMs, primary callback slots +0x1328..+0x1330 and secondary slots +0x1334..+0x133C.
- The target consumes 0x004A00C0 as nine rows of four callbacks. The repository had already closed all six unique begin-column callbacks, both update bodies, the folded draw stub family role, and both end-column resource callbacks. This method selects a row from the caller, invokes the begin callback immediately and installs update/draw/end into either the primary or secondary quartet according to attackClass.
- attackClass 2 is the secondary path. It aborts while the side-local EnemyManager still has category-3 Enemy state, optionally marks an opposing category-3 Enemy flags337C bit 0x100, updates the opposing Player +0xB4/+0xB8 counters, retires an existing secondary callback set, records row/value/class in +0x1354/+0x1358/+0x135C, copies the caller text, installs the selected callback row, clamps/increments both +0xA4 Player counters to 16, and plays sound 53. Classes 0/1 analogously replace the primary callback set, store +0x1348/+0x134C/+0x1350, increment opposing Player +0xB0 and +0xA0, and play sound 14.
- The common presentation path starts secondaryVms5F4[0..4] with scripts 30..34. The last two VMs show the target display counter's decimal tens/ones using sprites 25+, and VM2 uses attackClass+21. Exact GameManagerValueCCView::SubtractSaturating @ 0x00403E30 provides the initial 500-point decrement. A target-visible two-bit field at GameManager +0x134 naturally emits the 0x1800 transition-bit read/modify/write sequence; timer98 is reset and the two side-state records receive 1/30 and 2/30 transition values.
- The final HUD seam is fully TH09-local. GameManager subsystem6 is exact FrontSide::Create/Destroy ownership. The opposing FrontSide +0xA678 aux object supplies sideAnm00 and VMs at +0xA684/+0xA928, while the opposing Player +0x303F0 table value chooses the two scripts: base/base+1 for CardAttack side 1 and base+2/base+3 otherwise.
- Natural VC7.1 source first reached exact size 923 with 804/807 comparable bytes. Modeling the GameManager +0x134 state as a two-bit bitfield restored the target bitfield RMW; avoiding callback-row and final FrontSide caches restored the target absolute-column relocations and owner reloads. The last three bytes were only the initial display-value branch ordering: the natural equivalent attackClass < 2 spelling emits target JGE and the A0/A4 block order.
- The maintained local GameManager overlay includes the actual +0x1C value inside the +0xB0 valueCC subview and asserts flags134 at +0x134. Therefore all relocations solve against the true g_GameManager base 0x004A7D90. Two independent cold probe compilations and two independent canonical unit rebuild/replays reproduce 923/923 bytes and the same 29-relocation map. No register forcing, volatile, padding, assembly, fake return, target-byte embedding or arbitrary profile search is used.


## Packet 446 Replay input u16 exact upgrade and scrolling source closure

- ReplayInputState::IsHeld @ 0x00415CB0 and ReplayInputPressedView::WasPressed @ 0x00415CD0 are now canonical exact at 18 bytes each. The decisive evidence is TH09-local, not an imported adjacent-game assumption: the already exact AsciiInputView::IsHeld/WasPressed pair demonstrates that VC7.1 needs an unsigned-short mask declaration to emit the target stack-mask load into ECX instead of folding AND against the stack operand.
- Applying only that type correction to the standalone ReplayInputState::IsHeld source changes the former 16-byte int-mask body into the complete 18-byte target. Updating the declaration in ReplayInput.cpp changes only the decorated relocation symbol; ReplayManagerView::PlaybackInputAndFps remains canonical exact at 299/299, and all nine existing ReplayInput.cpp canonical units replay exact.
- ReplayInputPressedView::WasPressed uses the same target-proven u16 convention on historyPressed +0x32 and independently replays exact 18/18 twice. Both new leaf units have zero relocations and require no register forcing, volatile, padding, assembly or profile changes.
- InputView::IsPressedScrolling @ 0x00423158-0x00423181 is now maintained source-present/non-exact. Exact TitleScreen callers retain its existing int-mask ABI. Ordinary u16 temporaries reproduce the target's two physical checks of frontState +0x06 then repeatOutput +0x04, including XOR EAX / MOV AX / AND / TEST, but VC7.1 lowers the boolean return tail to 43 bytes versus target 42. A u16-mask probe is also 43 bytes and does not justify changing the caller-proven ABI. Further pursuit would be return/block codegen steering.


## Packet 447 Player charge-attack update source closure

- PlayerChargeAttackView::UpdateChargeAttack @ 0x0041F810-0x0041FCC9 is now maintained source-present/non-exact. Its sole TH09 code xref is Player_OnUpdate @ 0x0041EABD, fixing Player ownership and the ordinary thiscall receiver. Existing PlayerLifecycleView independently fixes sideIndex +0x08, sideState +0x0C, opponentState +0x10, flags +0x1B80, primaryShtFile +0x30338, charge scalar/limit +0x30384/+0x30388 and the timer block +0x3038C..+0x303D4.
- Input gating is TH09-local and now rests on canonical u16 leaves ReplayInputPressedView::WasPressed @ 0x00415CD0 and ReplayInputState::IsHeld @ 0x00415CB0. The callback handles the initial charge-delay timer, normal-shot timer shortening, hold timer, side-mode gate, and release-to-attack transition. Starting a charge plays sound43 and activates the local FrontSide meter. Charge growth uses the Supervisor framerate multiplier times the SHT +0x24 rate, clamps at scalar30388, tracks hundred-point level crossings for sound50, and forces release after the full-charge timer reaches 300.
- Release at >=100 sets Player flag bit4 and starts the special-shot timer. The >=400 tier additionally requires no active Enemy category3 and starts CardAttack row/class 2 with SHT path +0xAC, subtracts 300 resource and applies state effect15. The >=300 tier uses row/class1, path +0x6C, cost200 and effect14. The >=200 tier uses row/class0, path +0x2C, cost100 and effect13. Canonical CardAttackStartView::StartAttack @ 0x00403E50 plus maintained EnemyManager/resource/effect helpers close these seams. The meter is then deactivated through local sideState +0x18, not opponentState; this receiver is directly fixed by target [Player+0x0C] -> +0x18.
- The special-shot path uses timer30398: exact HasTicked, target current-value helper, SpawnShots(mode1), increment, and SHT +0x28 duration cutoff before clearing flag bit4. The normal-shot path analogously uses timer303A4, SpawnShots(mode0), increment and resets to -1 at 15. Natural source preserves both target-visible paths and their exact helper call order.
- The best natural pinned VC7.1 body is 1197 bytes versus target 1210. The remaining extent is a stable preserved-register/local-spill frontier. Target uses EDI for side, EBX for the repeated 0x8E ReplayInputState receiver, and spills sideMode to [EBP-1], therefore retaining an EBP frame. Stock VC7.1 natural forms instead keep side in EBX and the replay-input receiver in EDI, or recompute side, so sideMode stays in a register and the frame disappears. Pointer, reference, direct g_ReplayInputStates[side], declaration-order and cross-branch owner-cache probes span 1194-1197 bytes and never reproduce the target allocation. Removing CardAttack/SHT caches was retained because it restores the target-visible repeated owner loads; no register forcing, volatile, var_order, padding, assembly or profile roulette is used.


## Packet 448 Bullet transform-program canonical exact closure

- Bullet::AdvanceTransformProgram @ 0x004141C0-0x00414693 is now repository-canonical exact. Maintained EtamaController::OnUpdate @ 0x004146F0 calls this address with ECX equal to the current Bullet immediately before testing activeTransformFlags and dispatching the nine transform-update helpers. BulletManager.hpp already carried the missing member declaration, while exact transform installers/builders and target-backed Bullet offsets independently bind transformIndex +0xDD4, the 18 records at +0xDD8, active/available masks +0xDB4/+0xDB8, transform sound +0xDD0, and the ex-state region.
- The callback advances through transform records until it reaches a record it must activate or terminate. kind==0, disallowed-while-active records, and index>=18 terminate. Records whose kind bit is absent from transformFlags are skipped and scanning continues. Cull-delay, sprite-change and positioned-sound records execute immediately then continue scanning; active transforms initialize their dedicated ex-state and return after the common transformIndex advance.
- The active cases cover deceleration, vector/polar acceleration, three direction-change modes, both bounce modes, wait, both axis wraps and despawn. Vector/polar setup optionally plays transformSound only after the first record. Bounce copies either the record speed or current Bullet speed through an explicit if/else, matching the target integer-copy lowering. WRAP_X and WRAP_Y are separate source cases even though their bodies are semantically parallel.
- Child-pattern kind 0x01000000 constructs a 0x214 spawn descriptor, copies parent position, decodes fade/aim/type/color/start-index from the packed first payload word, consumes the next transform record for count2/flags/angle/angleStep, copies all 18 transform records, and calls exact SpawnBulletPatternPrimary. Target-visible source shape reads bulletType/color directly from payload bytes and materializes only the fade high-bit mask as a local. After spawn, non-fade children continue scanning; fade sets the parent to despawning and falls through the common post-switch transformIndex increment.
- The logical function body is 1236 bytes and ends with RET at 0x00414693. The immediately adjacent 0x00414694-0x004146E7 region is compiler-owned switch data: five 32-bit case targets followed by the 64-byte byte-map. This follows the same owned-extent convention as exact Chain::RunCalcChain/RunDrawChain. The canonical match unit therefore replays 0x528 / 1320 physical bytes while functions.csv and matches.csv continue to count the 1236-byte logical authored body.
- Pinned VC7.1 /MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /O2 /Ob1 /Oy- reproduces all 1320 owned bytes with 26 reviewed relocations. Two independent cold canonical rebuild/replay passes return exact 1320/1320 and identical relocation maps. Clean TH095 PhotoBulletView::AdvanceTransformProgram was consulted only after TH09 had independently fixed the owner, layout, constants, helper destinations and switch CFG; it corroborated the natural bounce/wrap/despawn/child control-flow spelling. No TH095 address or layout is imported, and no var_order pragma, register forcing, volatile, padding, assembly, fake return, byte embedding or profile roulette is used.


## Packet 449 Enemy schedule-runtime source closure

- EnemyScheduleRuntimeView::Run @ 0x0040FCC0-0x0041008D is now maintained source-present/non-exact. EnemyManager_OnUpdate @ 0x00410110 is its sole TH09 caller and invokes it with ECX = EnemyManager + 0x2AC3E0, matching the already-observed manager+0x2AC3E0 schedule-runtime call seam. Existing EnemyManager source independently fixes the manager back-pointer at runtime +0x1C, current instruction at +0x20, remote Enemy slots at manager +0x2AC388 and four event slots at +0x2AC430.
- The serialized instruction header is target-closed as time i32, opcode i16, size u8 and mask u8, with arguments beginning at +8. The runtime first checks exact PlayerSharedRuntimeView::IsBlocked, handles the delay timer at +0x0C, and then executes same-time instructions gated by the global timeline mask. Instructions advance by their serialized size; future-time instructions stop the loop. Negative-time completion clears the instruction pointer, resets timer0 and returns 1; otherwise the runtime adds the neutral global timeline step and returns 0.
- TH09 opcodes 0/1 share ordinary fixed-position spawn; 15 is the unconditional sibling; 17 additionally sets spawned Enemy flags3380 bit 0x200. Opcodes 12/11 spawn with defeat-token -1 and copy two timeline parameters to Enemy +0x3360/+0x3364. Opcodes 4/2 and 5/3 set runtime mirrorMovementX +0x18 and spawn from a random X range or random playfield X. Opcode8 writes a remote Enemy pending ECL subroutine, opcode10 waits while a remote active Enemy remains, opcode13 consumes matching event slots, opcode14 publishes to empty event slots, and opcode16 sets the Front completion byte. Opcodes6/7/9 are no-op entries in this TH09 build.
- Exact SpawnEnemyRecord @ 0x0040F1D0 is called six times. Target caller code passes the timeline's serialized integer fields at full 32-bit width; a caller-local int-width view naturally reproduces those call sites while the maintained callee remains free to interpret low short/char portions. The random-range case must pass its float range expression directly so VC7.1 materializes the argument on the stack exactly as target; an explicit temporary emits a three-byte-longer sequence.
- TH08 EclTimeline::Run and clean TH095 PhotoEnemyTimelineView::Run were consulted only after TH09 had fixed the owner, offsets, opcode meanings and helper destinations. Their ancestral lexical case order explains the TH09 switch layout: retry/completion first, then ordinary spawn, the TH09-added flagging spawn, unconditional spawn, drop-count spawn, random-range/playfield spawn, remote/wait and event cases. TH09 differs materially from TH08 in that opcodes0 and1 share one entry and runtime +0x18 is persistent; only 4/5/12 set it.
- The best natural pinned VC7.1 source has a 977-byte logical body versus target 974 and a 1052-byte physical COMDAT versus target 1048 including alignment and the 18-entry switch table. Every jump-table case entry through opcode13 starts at the exact target offset. The remaining mismatch is isolated to event13/14 register allocation/CSE and the common timer receiver hoist. Direct member expressions and scoped or function-level i/matchCount/j declarations were bounded probes; the latter changes no codegen. TH08's own exact reconstruction requires var_order for this local-allocation family, which is deliberately not imported. No var_order pragma, register forcing, volatile, padding, assembly, fake return or profile roulette is retained.

## Packet 450 SampleNetworkInput canonical exact closure

- SampleNetworkInput @ 0x0042B850-0x0042BE32 is the TH09 input sampler used by the exact Supervisor service wrapper. Exact ResetSupervisorServiceChannels @ 0x0042E9C0 calls it with ECX=0, 1, and 2; the target body indexes three 0x8E input-state records at g_ReplayInputStates @ 0x004ACE18.
- TH09-local target evidence fixes the complete behavior: activation gating through g_WindowActivateWParam, Win32 or DirectInput keyboard sampling through g_Supervisor +0x10, per-side keyboard-layout selectors at g_Supervisor +0x43F/+0x440, layout modes 2/3/4, controller aggregation through Controller::GetControllerInput @ 0x0042B410, and the 16-bit current/previous/repeat/pressed/released update with sixteen held counters, repeat threshold 26, and rollback 8.
- Two cold canonical pinned-VC7.1 /O2 /Ob1 /Oy- /Gr unit replays reproduce the full 1,507-byte function byte-for-byte. The diagnostic comparison has all 1,455 non-relocation bytes structurally identical, and the canonical unit records thirteen relocations: g_WindowActivateWParam, g_ReplayInputStates, g_Supervisor, GetKeyboardState, and two calls to Controller::GetControllerInput. The maintained source uses ordinary if/else plus switch; no register forcing, volatile, padding, assembly, pragmas, or profile roulette is used.
- Clean TH08/TH095 Controller sources were consulted only after TH09 independently fixed the ABI, offsets, key layouts, and state-update semantics; they corroborate the broad source family only and do not supply TH09 ownership or exactness.

## Packet 451 D3D capability formatter source closure and private-ABI frontier

- AppendD3DCapabilitySupportLine @ 0x0042CD40 and FormatD3DCapabilities @ 0x0042CD90 are now maintained together as file-local source-family functions. TH09 itself fixes the formatter's sole caller at 0x0042DD90, the D3DCAPS8 object at 0x004B36D8, the 8 KiB output buffer, all 33 capability helper calls, the D3DCAPS8 field/mask pairs, every Japanese report label, and the final separator.
- The former Packet 343 helper negative is superseded by a source-level signature correction, not register steering. With the natural family order capabilityName, capabilityFlags, mask, buffer, VC7.1 internalizes the helper and chooses the target-private ECX=buffer / EAX=capabilityName transport by itself. The diagnostic same-TU probe is structural-exact at 74/74 bytes with 54/54 comparable bytes and all five relocation fields solved; the prior buffer-first source signature was what caused the two ModRM load-order residual.
- The same ordinary static source reproduces FormatD3DCapabilities structurally at 809/809 bytes with all 481 non-relocation bytes identical. Clean TH08 was consulted only after TH09 independently fixed the TH09 field offsets, masks, strings, order and sole-caller transport; it corroborates the broad source family rather than supplying TH09 exactness.
- Canonical exact credit is intentionally withheld for both functions until maintained source for the real sole caller 0x0042DD90 exists in the same translation unit. Compiling these static functions alone lets VC7.1 eliminate them, while retaining a synthetic keepalive caller would not establish the real TU. No dummy wrapper is retained, and no register forcing, volatile, padding, assembly, pragmas, or profile roulette is used.

## Packet 452 D3D initialization TU canonical exact closure

- InitializeD3DRendering @ 0x0042DD90-0x0042E3A8 is now maintained together with the file-local FormatD3DCapabilities @ 0x0042CD90 and AppendD3DCapabilitySupportLine @ 0x0042CD40 in src/GameWindowInitD3D.cpp. WinMain @ 0x0042E3B0 is the sole caller and tests the return value immediately after the exact D3D-interface and window startup steps.
- TH09-local control flow fixes the natural source shape rather than requiring codegen steering: fullscreen handling precedes the windowed branch; the device creation fallback is nested HAL hardware-TL -> HAL software-TL -> REF with the observed reference-rasterizer entry label; failed refresh/VSync paths continue the retry loop; and CheckDeviceFormat is compared to D3D_OK rather than interpreted through the generic FAILED macro. Expanding the three observed viewport records directly, instead of introducing a synthetic helper, produces the target stack/local lifetime.
- Under the pinned VC7.1 /O2 /Ob1 /Oy- /Gr profile the maintained real TU is structural-exact for all three authored functions: 74/74 bytes with 5 relocations for AppendD3DCapabilitySupportLine, 809/809 with 82 relocations for FormatD3DCapabilities, and 1,561/1,561 with 158 relocations for InitializeD3DRendering. Two cold canonical rebuild/replay passes are byte-for-byte stable for every unit.
- This promotes the two Packet 451 private-ABI frontiers without a synthetic keepalive caller. Their target-private ECX/EAX and EAX/ESI transports arise naturally from same-TU VC7.1 optimization. Clean TH08 was used only as late source-family corroboration after TH09 independently established the relevant CFG, object offsets, D3D constants, messages, capability masks and ownership. No register forcing, volatile, padding, assembly, pragmas, fake returns or profile roulette is present.

## Packet 453 WinMain and real startup-TU canonical closure

- WinMain at 0x0042E3B0-0x0042E8F6 is repository-canonical exact at 1,351 bytes. TH09-local target evidence fixes the complete startup/restart/shutdown control flow: executable-directory normalization at 0x004ACD08, Windows power-policy save/disable/restore, critical-section and config/checksum startup, D3D interface/window/device setup, SoundPlayer/controller initialization, the 0x2B2934 AnmManager allocation, DirectPlay state construction at 0x004B42D0, the message/render/cooperative-level loop, device-reset path, runtime release, option-change restart, configuration writeback, MIDI teardown, COM teardown, and final system-policy restoration.
- The executable-directory scan is intentionally target-shaped rather than simplified into a symmetric strrchr/while loop: TH09 first tests the final byte for backslash, then scans slash/current and backslash/previous-byte conditions with two distinct "./" fallback blocks. Expressing those actual source-level branches restores the target CFG naturally; no label or byte padding is retained for codegen.
- CreateGameWindow at 0x0042D4D0-0x0042D5EC is now canonical exact in the same maintained translation unit. This supersedes the earlier exactness-withheld state: the real WinMain caller keeps hInstance in EBX, and file-local visibility lets VC7.1 choose the 285-byte target-private EBX transport without any explicit register ABI. The helper reproduces all 285 bytes and 19 reviewed relocations.
- WinMain uses ordinary delete expressions. Under the locally established /O2 /Ob0 /Oy- /Gr profile, VC7.1 naturally preserves scalar deleting-destructor calls; the generated MainAnmManager and MidiOutput deleting-destructor relocations resolve to the already excluded compiler artifacts at 0x0042CC30 and 0x0042CC60. /Ob1 expands those cleanup paths and grows the caller to 1,369 bytes. This is the same compiler-generated-cleanup distinction already observed elsewhere in the repo, not profile roulette.
- Two cold repository-managed canonical rounds reproduce WinMain 1,351/1,351 and CreateGameWindow 285/285 with 152 and 19 reviewed relocations respectively. Clean TH08/TH095 source was used only after TH09 fixed the local behavior and as broad family context; no adjacent-game var_order, register forcing, volatile, assembly, fake wrapper, target-byte embedding or inert padding is used.

## Packet 454 Background stage-script and camera source closure

- BackgroundRunStageScriptPhase at 0x004018F0-0x00402297 is now source-present under TH09-local Background ownership. Background::OnUpdate is the sole target caller and loads its Background receiver into ECX immediately before the call. Fresh target analysis closes all 34 opcodes rather than importing TH08's whole OnUpdate: stage-position/fog commands, pause/jump, camera position/look-at/up/FOV interpolation, explicit Hermite endpoints/tangents, stage VM commands, viewport offset, camera-motion mode and the final fog interpolation are all represented.
- The campaign corrects two stale layout assumptions in Background.hpp. The former opaque 0x834..0xBF3 range is four 0xF0 camera records: target at +0x834, start at +0x924, end tangent at +0xA14 and start tangent at +0xB04. The old +0x6408 pending-label hypothesis is false: exact ApplyPendingStageLabel already fixes the pending label at +0x18, while the stage phase proves +0x6408 target position, +0x6414 end frame, +0x6418 initial position, +0x6424 start frame and +0x6428 compensate-camera-jump. Existing tint/culling/motion fields remain at +0x642C/+0x6430/+0x6434/+0x6438.
- InterpolateBackgroundCameraVector at 0x004016E0 is source-present in the same maintained TU. TH09 independently proves duration +0xBF4, five 0x0C timers +0xC08, modes +0xC44, private ECX owner/EAX index/EDI output transport and four stack vector pointers. Natural source implements the exact timer progression, ease-out modes 1-3, ease-in modes 4-6, Float3 linear interpolation and mode-7 cubic Hermite through exact 0x00401000.
- Exactness is deliberately not claimed. Under the bounded /O2 /Ob0 form selected because the target camera-motion block calls the already-classified sinf/cosf wrappers, the owner has the target 0x14 stack frame and the same position/fog stack-home classes. Its real return is at +0x99A versus target +0x9A7. The remaining dominant frontier is preserved-register allocation: target keeps ESI=Background and EDI=instruction/output, while stock VC7.1 keeps those long-lived values reversed. The helper's ordinary path reaches the same +0x141 return, while its Hermite path remains about 12 bytes longer for the same ESI/EDI output-lifetime reason.
- Bounded negative controls included owner member/free spelling, same-TU static helper visibility order, a semantic owner alias, an instruction-frame local and one natural helper parameter-order hypothesis. /Ob1 was also checked because existing canonical Background units use it; it inlines x87 sin/cos unlike this target, while /Ob0 naturally preserves the target wrapper calls. No var_order, register forcing, volatile, assembly, padding, synthetic register-pressure caller or broad profile search is retained. Clean TH08 was consulted only after TH09 fixed the split boundary and offsets; its pragma and full OnUpdate tail are not imported.

## Packet 455 Front message runtime update source closure

- FrontMessageRuntimeView::Update @ 0x00416590-0x004170B3 is the 2,852-byte TH09 message-script owner embedded at Front +0xE944. The source-backed FrontCalcCallback @ 0x00417630 passes that receiver directly in ECX; exact Reset/Setup/Draw plus the message loader independently constrain the runtime to 0x1D70 bytes.
- TH09-local target evidence fixes the message VM layout and the complete external call surface: 5/2/3/2 timer assignment/increment/>=/< calls, 7 SetAndExecuteScriptIdx, 11 SetSprite, 11 ExecuteScript, 4 DrawTextLeft, two text-decode calls, four input-held tests, one pressed test, three game-mode predicates, and the expected Front result/music/ScreenEffect/Player/FrontSide seams. The maintained source reproduces those call counts exactly.
- Opcode behavior is target-derived rather than copied from TH08. In particular, opcode 0x0F with a negative side writes interrupt 4 only to the first two portrait pairs (four VMs at target offsets +0x212/+0x75A/+0x4B6/+0x9FE), correcting the broader all-four-pairs interpretation suggested by a loose decompiler reading. Opcode 0x11 naturally shares its second SetSprite tail across the special and normal portrait branches, closing the target's eleven-call SetSprite surface.
- Two cold pinned VC7.1 /Gr /O2 /Ob1 /Oy- builds produce identical 2,724-byte Update text with SHA-256 904276bd4b519a9d90ed86ded3c195756884402bd4a85a06219d978eac450ed8; target is 2,852 bytes. Candidate and target both contain 64 calls and 46 conditional branches, while the residual is distributed instruction-selection/addressing pressure: target uses many more six-byte absolute/global forms, whereas the candidate emits more instructions overall but shorter register/CSE encodings. This is maintained source, not an exact claim.
- DecodeFrontMessageString @ 0x00415C60-0x00415C86 now has honest maintained same-TU source because its real owner exists. It remains non-exact: the natural helper is cold-stable at 36 bytes versus target 39 and selects destination in ECX/source in EAX with plain RET, while target requires source in EAX plus stack destination, an EBP frame, and RET 4. The inverse parameter order only swaps the two register arguments and does not solve the target ABI.
- Clean committed TH08 GuiImpl::RunMsg was consulted only as a broad script-runner source-family hypothesis. TH09 independently determines the opcode set, side mirroring, VM offsets, globals, constants, calls, and non-exact status. No var_order, explicit register ABI, volatile, padding, assembly, fake wrapper, or profile roulette is retained.

## Packet 456 Player movement/options owner source closure

- PlayerLifecycleView::UpdateMovementAndOptions @ 0x0041C170-0x0041C89A is the 1,835-byte Player movement/options owner called only by canonical PlayerLifecycleView::OnUpdate. The exact caller supplies the Player receiver in ECX and ignores the return value; the target owner returns zero after completing its update.
- TH09-local evidence independently fixes the complete source behavior. The function samples the side-indexed 0x8E replay-input state, maps held direction masks 0x50/0x60/0x90/0xA0/0x20/0x10/0x40/0x80 to the eight movement directions, selects focus either from g_GameConfiguration->valueB4[side] plus input auxiliary2A >= 8 or IsHeld(4), and updates the low two Player flag bits. Focus entry creates two fixed-slot effects through the side EffectManager; focus exit interrupts the primary effect VM and deactivates the secondary effect.
- The two eight-way movement switches read the TH09 SHT speed fields already proven independently by canonical PlayerGameplayMethods::ResolvePatternOffset @ 0x00404710: normal axis/diagonal +0x14/+0x1C, focused axis/diagonal +0x18/+0x20, followed by Player multipliers +0x1CDC/+0x1CE0/+0x1CE4/+0x1CE8. The owner updates movement angle +0x1CD8, horizontal/vertical state +0x3035C/+0x30360, main-VM scripts 1..4, framerate-scaled velocity and position, playfield clamps, collision/graze/item min/max bounds, four 0x2F4 option records, and the 16-entry position history.
- Two cold pinned-VC7.1 /O2 /Ob1 /Oy- /Gr builds of the maintained source are byte-identical at 1,864 bytes (function-text SHA-256 e057f310ad4647e6ef820538c956b294c1877ed5583f0ddabf6f57ebf70f98dd) versus target 1,835. Bounded source-shape tests establish why this is not promoted: caching the SHT pointer over-optimizes to 1,752 bytes; target-like per-case SHT reloads yield 1,864; preserving both the side*0x8E offset and ReplayInputState pointer naturally recovers the target EDI/EBX input lifetime; angle-before-speed source expands to 1,880; enum direction typing is codegen-neutral; and member/free spelling does not change the plateau. External call counts are identical between candidate and target. No register forcing, volatile, padding, assembly, var_order, or profile roulette is retained.
- This packet also retires the earlier UpdateStateValue reconstruction alias for 0x0041C170. The exact PlayerLifecycleView::OnUpdate match unit now relocates to PlayerLifecycleView::UpdateMovementAndOptions and replays 522/522 unchanged, so the semantic correction does not disturb canonical caller codegen. Clean committed TH08 Player::UpdateMovementAndOptions was consulted only as a broad source-family hypothesis after TH09 fixed the owner, offsets, calls, constants and side-local behavior.

## Packet 457 TitleScreen key-config owner and member-helper correction

- TitleScreenView::OnUpdateKeyConfig @ 0x00427EE8-0x004289DA is the 2,803-byte screen-10 owner selected only by the Title calc dispatcher when currentScreen == 10. TH09 target evidence fixes a two-side key/controller configuration state machine rather than the earlier adjacency-based character-select possibility: local 0x6C binding snapshot at TitleScreen +0x1B250, ten help VMs, five displayed key-number pairs, controller-state sampling, duplicate-binding repair, side/profile switching, color gating, reset, and exit/runtime synchronization.
- Existing canonical Title leaves close the owner surface: MoveCursorVertical @ 0x0042457A, SetIndexedSpriteChoice @ 0x00424E95, SetTwoDigitVmValue @ 0x00422FD1, UpdateMenuSelection @ 0x004276D7, Controller::GetControllerState @ 0x0042B740, and the corrected member SwapDuplicateCharacterIndex @ 0x00422F67. The maintained owner reproduces every target external call count, including 21 indexed-sprite updates, ten two-digit updates, five duplicate-key rewrites, three memcpy calls, two scrolling checks, three menu-sound calls and two direct SoundPlayer calls.
- Target-local field relationships independently identify g_Supervisor.config +0xB4/+0xB7 as the per-side configuration selectors and a 0x36 stride per side in the 0x6C serialized controller snapshot. Only menu rows 3..7 edit binding shorts; side/profile changes recolor VM ranges 40..44 and 50..59, and exit copies the first/second side binding records into the process-lifetime g_ControllerMapping projections.
- The target-proven Title profile is /O1 /Ob1 /Oy- /Gr without /Oi. Retaining /Oi incorrectly inlines the three target memcpy calls. Natural source-shape closure proceeds from 2,504 bytes before the missing side-toggle color refresh to 2,800 after restoring that TH09 behavior; member ownership of the duplicate-key helper plus target branch signedness/type lifetimes and an ordinary two-value confirm/reset switch reach an exact-sized 2,803-byte candidate with the same 751-instruction count.
- Two cold builds of the maintained source are byte-identical with function-text SHA-256 05d03d9a6b44a3cc72712e41a0b61aa1837b2e0cdb3b33f29bf758e419dc87a6. The honest plateau is 2,803/2,803 bytes and 2,323/2,331 ordinary comparable bytes with 118 object relocations. The eight remaining comparable bytes are narrowly bounded compiler residuals: two direct SoundPlayer calls place MOV ECX,g_SoundPlayer between the two pushes in the target but after them in the candidate, which also shifts two DIR32 relocation fields, and two otherwise-identical OR-memory instructions choose the opposite SIB base/index encoding. Function-local SoundPlayer pointer/default-argument probes do not change the scheduler result; no register forcing, volatile, padding, assembly, var_order, or profile roulette is retained.
- Clean TH08 TitleScreen::OnUpdateKeyConfig was consulted only after TH09 fixed the screen owner and supplies broad source-family organization. TH10/TH095 review did not provide a closer same-family source explanation for the final scheduler/SIB residual.

## Packet 458 FrontSide draw-owner source closure

- FrontSide::OnDraw @ 0x004193E0-0x00419FF5 is the 3,094-byte FrontSide draw-chain owner. Canonical FrontSide::Create @ 0x0041A5D0 installs this physical entry as the draw callback at priority sideIndex + 26, independently fixing the owner and fastcall callback ABI.
- TH09-local target evidence fixes the whole HUD draw surface. It clears the selected Supervisor viewport shake pair, calls exact Supervisor::ConfigureScreenViewport, then walks the existing FrontSide VM groups in target physical order: 11 status, 5 stock, 7 gauge, 2 auxiliary, one vm3CBC, 10 score, one vm59C8, 4 meter, 3 marker, 6 timing, 2 state, one vm7430, 7 rank and 5 counter VMs. The player-relative groups add the side Player position before TransformPopupX/Y; marker Y subtracts 24 while transition state is nonzero. Draw mode is target-observed per group (Draw2D versus DrawNoRotation).
- The transition owner is FrontSideAuxView::unknown554 at FrontSide+0xABCC with the established transition timer at +0xABD0. When active, target builds four black XYZRHW diffuse triangle strips, caps the first timer contribution at 30 frames, uses vertical rates 14.933333f and horizontal rates 9.6000004f, and mirrors direction according to transition mode. It sets the four texture-stage states to diffuse/selectarg1, disables Z writes, uses FVF 0x44, issues four DrawPrimitiveUP(D3DPT_TRIANGLESTRIP,1,...) calls, then restores the ANM/D3D state through the exact clear helpers and texture/modulate stage states.
- The best natural pinned-VC7.1 /O2 /Ob1 /Oy- /Gr source is cold-stable at 3,106 bytes, 845 instructions and 208 relocations in two independent builds; both function texts have SHA-256 41ce09f321ab8dffeb913fa8289c32bec16aa8e03e48c1faa88b860ebf1f9ac0. Target is 3,094 bytes / 840 instructions. All 103 direct calls appear in identical order and resolve to the same target families; after preserving the target-observed sideState lifetime across the single vm7430 block, the complete HUD prefix enters the transition branch at relative offset 0x582 in both candidate and target.
- Bounded natural source/toolchain probes do not justify exact promotion. Re-reading sideState shrinks the wrong lifetime and emits 3,122 bytes; the target-backed local cache emits 3,106. /O1 and /O2 /Os both emit 3,156 and still lower the four-element vertex construction as an explicit loop rather than target's generic vector-constructor helper. An implicit vertex constructor through a Float3 default constructor is size-neutral. Five named Float3 temporaries recover the target 0xA4 stack frame but inflate to 3,160; six inflate to 3,173. Replacing the third-point branch-local Y value with an inline ternary over-optimizes to 3,020. The remaining 12-byte residual is therefore recorded as transition-overlay constructor/branch scheduling rather than steered with register forcing, volatile, padding, assembly, var_order, a hand-written iterator call, or further profile search. ScreenEffect::DrawSquare independently documents the same four-vertex VC7.1 constructor-helper frontier.

## Packet 459 Background RenderObjects canonical exact closure

- Background::RenderObjects @ 0x004026C0-0x00403389 is now canonical exact. TH09-local ownership is fixed by Background::OnDrawHighPrio calling it for modes 0/1 and Background::OnDrawLowPrio calling it for modes 2/3. The target iterates 0x10-byte stage-object instances, performs object-level sphere/frustum culling, then dispatches raw stage quads by type before restoring fog/render state.
- TH09 distinguishes two camera owners that earlier scratch hypotheses incorrectly conflated. Object-level culling subtracts fixed g_Supervisor configuration-0 camera position plus position offset and dots configuration-0 forward. Type-0/type-1 per-quad fog distance instead subtracts the current g_AnmProjectionContext camera position plus offset, while D3DX projection uses that current context viewport/view/projection.
- Type 1 constructs four 0x1C diffuse/texture vertices, projects both segment endpoints, derives screen-space half-widths, applies endpoint fog colors, validates projected Z and queues the sprite quad. Type 0 updates the VM world position/scale and either draws ordinary 3D with fog enabled or camera-facing projected geometry with fog disabled, DrawNoRotationNoRound, ClearTexture and optional point collection at Background +0x6444.
- The target-visible type-0 fog path reuses one float local: it first holds camera distance, then is overwritten by (near-distance)/(near-far). If that ratio reaches 1.0, the target advances the quad immediately. The same reused slot is later read by the special-effect point gate, so keeping separate distance/fogRatio locals is not source-equivalent for code generation.
- Compiler behavior is target-backed rather than profile roulette. Under pinned VC7.1 /O2 /Ob0 /Oy- /Gr, natural source emits both target-special lowerings: generic vector-constructor iterator 0x00401470 with folded constructor callback 0x004343D0 for four 0x1C vertices, and external @sqrtf@4 @ 0x00401080 for projected two-dimensional length. /Ob1 instead expands the constructor loop and inlines sqrt. Two cold /Ob0 builds produce identical 3274-byte function text with SHA-256 50fcd1c8aa4aa1b8d35e0634da6e1153534ab7b939e01ea83836cc4f802c7c16.
- The canonical background-render-objects match unit verifies all 3274 bytes and 113 relocations, including Supervisor configuration addends +0x78/+0x9C/+0xB4, current projection context 0x004B3448, D3DX helpers, __ftol2, vector construction, @sqrtf@4, fog controls, QueueSpriteQuad, DrawNoRotationNoRound, ClearTexture and Draw3D. No register forcing, volatile, padding, assembly, var_order or hand-written compiler-helper calls are retained. Clean TH08 Background::RenderObjects supplied only broad family organization after TH09-local evidence fixed the actual behavior.

## Packet 460 Front message release source promotion

- FrontMessageRuntimeView::ReleaseForSideCount @ 0x00417520 is now maintained source-present after an independent rebuild from TH09 target evidence rather than relying on the earlier negative note. The exact release core and message loader again fix the embedded Front +0xE944 receiver and integer start-index ABI.
- The natural source uses a separate tableIndex while repurposing the original startIndex lifetime for the ten-entry end bound. This is not cosmetic: directly incrementing startIndex emits a 258-byte body, while the separate-index form naturally restores the target EBX/BL and EDI register lifetimes and emits the previously reported 268-byte body.
- Two cold pinned VC7.1 /O2 /Ob1 builds are byte-identical at 268 bytes with function-text SHA-256 e541c6c2ab4dce8bb541344cd3dd82d824a0b927f06a590d8ec74042327a18bb versus target 271. bool[256] versus unsigned char[256], an explicit next pointer, and explicit mode2 source/musicId locals are codegen-neutral at 268.
- The residual remains three target bytes: an extra mov ecx,eax before testing the advanced instruction opcode, plus the longer EDX absolute load of the neutral 0x004A7E80 source pointer. No register or encoding steering is retained, so this is source-present/non-exact rather than a canonical match.

## Packet 461 Enemy deactivation cleanup source closure

- EnemyView::CleanupAfterDeactivation @ 0x00410110-0x004101C7 is now maintained source-present/non-exact. Two TH09 EnemyManager::OnUpdate call sites clear the active bit and immediately invoke this member, fixing the Enemy receiver and teardown role.
- TH09 target dataflow independently fixes the behavior: clear active when the 0xE0000 state mask is absent; release boss/remote ownership through manager +0x2AC388 and bossSlot +0x336B; call exact Th09EclRunState::ReleaseAttachedEffects when attachedEffectCount5414 is nonzero; clear any remaining boss slot; set four life callback thresholds +0x33B0..+0x33BC and timer threshold +0x33D0 to -1; call exact EnemyCoreReleaseChildEclBlocks @ 0x004100E0; then clear manager->sideState320->player04->homingTarget3037C only when it points to this Enemy.
- Two cold pinned VC7.1 /O2 /Ob1 builds produce identical 184-byte function text with SHA-256 291cc074d1bbc7271b583dee85f2b03f3918765fe1acc2f39a2e1d0f64760d7d. Both external helper relocations solve to exact targets and 150/176 ordinary comparable bytes match.
- The residual is bounded scheduling only. Target stores the four life thresholds, then emits mov ECX,ESI, then stores the timer threshold before the child-ECL cleanup call. Natural typed free-fastcall source prepares ECX before all five stores; a member-call spelling is byte-identical and chained assignment is slightly worse at 149/176. No register forcing, volatile, padding, assembly, var_order or scheduler-directed source is retained.

## Packet 462 Front message initialization source promotion

- FrontMessageOwnerView::InitializeMessageRuntime @ 0x004181E0 is now genuinely source-present in the ledger and filesystem, correcting the prior documentation/tracking mismatch where Packet 347 described maintained source but the function row had no source_file.
- Fresh reconstruction from TH09 target evidence independently reproduces the same 133-byte natural body: exact GameManager mode0/mode1 predicates, the target-visible duplicated branch structure selecting signed short source +0x06 or literal 201 under override global 0x004A7E90, exact Setup(messageIndex,0) on Front +0xE944, then clears +0x1095C/+0x10960/+0x10964/+0x11EA8.
- Two cold pinned VC7.1 /O2 /Ob1 builds are byte-identical with SHA-256 4066a4f667d8ba8bd2e826cfcb0f0d0868b2538e4658fc23cdab1b7155c28d79. An explicit runtime pointer and explicit mode local are byte-identical to baseline. The only residual remains the six-byte member-call evaluation order, so source presence is recorded without exactness credit.

## Packet 463 shared zero-return callback exact closure

- PlayerCheckState2 @ 0x0044AB20-0x0044AB22 is now maintained and canonical exact. Fresh attested TH09 evidence shows three ordinary code callers, two in EnemyManager::OnUpdate and one in canonical PlayerLifecycleView::OnUpdate, together with many data-table references. The callsites pass no stack arguments, and the physical body is exactly xor eax,eax; ret.
- Natural pinned VC7.1 /O2 /Ob1 /Oy- /Gr source consisting only of an integer zero return emits the complete 3-byte target body with no relocations. The PlayerCheckState2 spelling is explicitly a reconstruction alias retained from the already-exact Player caller; the shared Enemy/data-table usage does not establish unique Player ownership or original source spelling.
- This supersedes the older size-only negative note that deliberately withheld the leaf while owner semantics were unresolved. The new evidence is the combination of the canonical caller ABI, full cross-owner xref review and direct 3/3 target-bound replay; no register assumptions, assembly, padding, pragmas or profile search are involved.

## Packet 464 ApplyNetworkInput canonical exact closure

- ApplyNetworkInput @ 0x0042ACE0-0x0042AD62 is now canonical exact. The decisive correction is the return contract: maintained SupervisorServiceUpdate already declared and called this entry as int __fastcall ApplyNetworkInput(int,int), while the target copies EDX input to EAX on entry and preserves it through return. Earlier ledger/probes incorrectly modeled the function as void and therefore stopped at 128 bytes.
- With the correct int return, ordinary pinned VC7.1 /O2 /Ob1 /Oy- /Gr source is target-sized 131/131. The remaining four comparable bytes close when pressed/released are written as two natural repeated current-xor-previous expressions rather than caching a changed local. This source shape is independently present in clean TH095 input code, consulted only after TH09 fixed the ABI/layout/behavior.
- The canonical target body indexes g_ReplayInputStates at 0x8E stride, rolls current/previous, maintains sixteen held counters with threshold 26 and rollback 8, builds the repeat mask, writes released at +0x08 and pressed at +0x06, and returns the original input. No register forcing, volatile, padding, assembly, pragma or profile search is used.

## Packet 465 final authored source-frontier closure

- GameManagerPlayfieldView::IsWithinPlayfield @ 0x0041A6EB-0x0041A75E is now maintained source-present/non-exact. TH09 callers fix a thiscall GameManager receiver and four float arguments; target x87 dataflow fixes rectangle intersection against x [-144,144] and y [0,448]. Clean TH08 independently supplies the same four early-return source family, adapted only to TH09's bounds.
- Pinned VC7.1 emits 114 bytes for that natural early-return source versus target 116. Bounded alternatives do not justify promotion: nested guards remain 114, a boolean chain is 117, constants-left forms are 118, and a local-result form happens to be 116 bytes but diverges near the start by carrying an ECX result value absent from the target. The remaining two-byte frontier is x87 status/pop scheduling; no source is retained merely for matching size.
- EffectPolarScaleView::SetFromAngleAxes @ 0x0040D4D0-0x0040D4EF is now maintained source-present/non-exact. TH09 target evidence fixes x = cos(angle) * xScale and y = sin(angle) * yScale; both ordinary float and double sin/cos spellings compile to the same 30-byte FCOS/reload/FSIN body, while the target is 32 bytes with one FSINCOS and a spilled receiver.
- Clean TH08 compiler notes independently classify its fsincos helper as runtime/library machinery and explicitly reject manufacturing inline-assembly wrappers when VC7 sin/cos intrinsics do not fuse. Accordingly the TH09 source keeps ordinary cosf/sinf semantics and records the 30/32 plateau rather than introducing assembly, fake runtime calls, volatile/register steering, pragmas or profile roulette.
- With these two source mappings, every one of the 979 confirmed authored functions now has maintained source. This closes the authored/no-source frontier at an honest plateau; canonical exact work remains incomplete and semantic/port work must still wait.

## Packet 466 RunEcl call-structure closure

- EclManager::RunEcl remains non-exact, but its complete call structure is now closed. Fresh target byte/Capstone census corrects the older oracle: the 14,792-byte target contains 379 calls made of 375 immediate direct calls, one register-indirect call and three memory-indirect calls.
- The previous maintained source called ZunTimer::SetCurrent directly in CANCEL_LASER. TH09 target instead preserves the ordinary laser->timer = 0 wrapper call through exact ZunTimer::operator=(int) @ 0x00401500. Restoring that source expression fixes the sole immediate-call identity mismatch.
- Opcode CALL_EX_INSTRUCTION previously merged resolved and raw callback indices before one shared table call. TH09 target has two source-visible branches, each invoking the callback table independently. Restoring those two natural branches adds the missing memory-indirect site. Candidate and target now both have 379 calls with the same 375 immediate + 4 indirect split, and every immediate direct-call destination multiplicity matches.
- These changes improve the logical code result from 14,420 to 14,440 bytes against target 14,792 while retaining the complete 6-entry easing and 187-entry opcode tables and resolver multiplicities 131/100/17/24. The major remaining blocker is local lifetime/layout: candidate frame 0x2C0 versus target 0x168, together with block ordering/ordinary bytes. No register forcing, var_order, padding, assembly or profile roulette is used.

## Packet 467 RunEcl direct operand-expression convergence

- RunEcl's integer operand wrapper previously forced each inlined use through a named const int rawValue before choosing raw versus resolved input. Fresh VC7.1 /Z7 diagnostics showed large anonymous stack/home-slot pressure, while clean TH08's related RunEcl family uses the raw operand expression directly in its resolver ternary.
- Removing only that intermediate local and spelling ReadInt as mask ? ResolveInt(enemy, RawInt(...)) : RawInt(...) preserves all target semantics and all 131 integer-resolver calls, but changes VC7.1 code shape across the interpreter. The logical candidate grows from 14,440 to 14,696 bytes against the 14,792-byte target, reducing the gap from 352 to 96 bytes in one source-level correction.
- Two cold pinned builds reproduced 14,696 logical bytes, the complete 193 compiler-table entries, 375 immediate direct calls, four indirect call sites and the same resolver multiplicities 131/100/17/24. This near-size result is superseded by Packet 468: later target-local float-call review proved that the 0x2C0 frame came from an artificial inlined wrapper parameter home and therefore was not an acceptable source-shape plateau.

## Packet 468 RunEcl false-near-plateau correction

- The previous 14,696-byte RunEcl candidate was a false near-size plateau. In the simplest SET_FLOAT handler, the target tests the operand mask, calls EnemyView::ResolveFloat directly when needed, merges resolved/raw values into one scratch, then calls ResolveFloatLValue. The wrapper candidate inserted an extra register copy plus a separate rawValue home before every float resolver call; across 100 calls this inflated the frame to 0x2C0 even though target is 0x168.
- Replacing only that wrapper edge with the direct member call keeps exactly 100 float-resolver calls, all other resolver multiplicities, 375 immediate calls, four indirect calls and the complete 193 compiler-table entries. Two cold pinned builds now emit 14,028 logical bytes with a 0x164 frame. The larger aggregate byte gap is accepted because the local instruction/dataflow is materially closer to the target; exact reconstruction must not optimize for total size while retaining a disproven source shape.
- A separate target review also corrects SHOOT_NOW. Target 0x0040A36C loads the cached position2D74 pointer and adds shootOffset2E04 before writing bulletDescriptor2E74.position. The maintained source previously used worldPosition2DD4; that was a semantic reconstruction error. CREATE_LASER remains a distinct path that does use worldPosition.
- Bounded alias experiments were negative: forcing SET_POSITION through the outer position pointer changes direct-call folding to 376/375, while aimed/orbit aliases are codegen-neutral. Four case-local float-assignment temporaries increase the frame to 0x174 without changing logical size. None of those probes is retained. The live frontier is now honest CFG/local lifetime and physical block-order recovery from the 14,028-byte, 0x164-frame baseline.

## Packet 469 RunEcl child-context scan lifetime

- TH09 target tail independently distinguishes the active child-context index from the next slot being scanned. At 0x0040BF3B it loads the active index, increments a separate scan register, walks childEclBlocks33D8 without modifying the active index, and writes the active-index home only after a non-null child is selected.
- The maintained source previously incremented childIndex itself while skipping null slots. Clean TH095 RunEcl supplies the adjacent source-family shape: for next = activeChildContext + 1, scan until a child is found, then assign activeChildContext = next immediately before redispatch.
- Restoring that ordinary two-variable control flow in TH09 grows the natural candidate from 14,028 to 14,044 logical bytes, reducing the gap from 764 to 748 without changing the 0x164 frame, 375 immediate calls, four indirect calls, 597 relocations, resolver multiplicities or 193 compiler-table entries. Candidate disassembly now likewise increments a separate ECX scan value, loops over +0x33D8, writes context ordinal from next+1, and stores childIndex only on the selected-child path.
- Two cold builds reproduce the same 14,044-byte result and the full target-independent CI remains green. This is a source-lifetime correction, not a size-directed trick; no volatile, register forcing, var_order, padding, assembly or profile search is retained.

## Packet 470 RunEcl laser-slot loop recovery

- Dispatch-table index 153 corresponds to wire opcode 154 CLEAR_LASER_SLOTS. TH09 target 0x0040A94A clears the 32 laser-slot pointers with 32 ascending dword stores. The maintained source had reconstructed this as one memset, which pinned VC7.1 /O2 lowers to rep stos and collapses the handler to roughly one eighth of the target code shape.
- Clean TH08 supplies direct adjacent source-family evidence for this exact behavior: a fixed for loop from 0 through 0x1F assigning zero to laserSlots[i]. Restoring that ordinary loop in TH09 makes pinned VC7.1 fully unroll it into the same 32 ascending stores; no unroll pragma, assembly, padding or manual repeated statements are needed.
- The change grows the RunEcl logical candidate from 14,044 to 14,216 bytes against target 14,792, reducing the gap from 748 to 576. The frame remains 0x164 versus target 0x168, and the already-closed 375 immediate + 4 indirect call structure, 597 relocations, resolver multiplicities 131/100/17/24 and 193 compiler-table entries are unchanged.
- Two cold builds reproduce the 14,216-byte result and target-independent CI remains green. This closes a large false simplification in the maintained interpreter source rather than adding match-directed filler.

## Packet 471 RunEcl interaction-flag view coalescing

- Wire opcode 79 SET_INTERACTION_FLAGS is target-backed as six semantic interaction-bit assignments: inverted accepts-damage/collision/damageable, direct no-sprite/allow-offscreen, and direct no-death. The maintained source previously called PrimaryFlagBits(enemy) or SecondaryFlagBits(enemy) afresh for each field assignment.
- TH09 target 0x00409E8A does not perform six memory read/modify/write sequences. It resolves operand 0 once, builds all five affected primary bits in registers, masks the old primary dword once with 0xFDFFFFA3, stores primaryFlags337C once, then updates secondaryFlags3380 bit 3.
- Caching the two typed bitfield-view pointers once in the case while retaining the same six field-level assignments is sufficient for pinned VC7.1 to discover the target-style coalescing. The opcode's reachable CFG shrinks from 163 bytes to 116 versus target 118, with 36 candidate instructions versus target 38. No target instruction mask was written into the source.
- Two alternate source shapes were rejected. Copying the secondary bitfield struct by value or rewriting secondary bit 3 as a whole-word mask each produces a 119-byte handler but introduces an EBP-4 spill absent from target. The simpler pointer-only form avoids that false near-size shape.
- The whole RunEcl candidate shrinks from 14,216 to 14,168 bytes, so the aggregate gap increases from 576 to 624. This is accepted deliberately: the prior larger candidate carried 45 bytes of disproven repeated-RMW code inside this opcode. Calls, relocations, resolver multiplicities, frame size and compiler tables remain unchanged. Two cold builds and target-independent CI are green.

## Packet 472 RunEcl late-laser relookup shape

- Wires 170 SET_LASER_START_CAP_HIDDEN, 171 SET_LASER_START_LENGTH and 172 SET_LASER_OFFSETS shared one reconstruction error. The maintained source cached laserSlots32D8[index] in a lateLaser pointer before the null check and reused that pointer for the later write.
- TH09 target does not preserve the laser pointer. Each handler resolves and retains only the slot index, performs one lookup for the null check, and performs a second indexed lookup on every resolved/raw write path. Wires 171/172 likewise re-index immediately after EnemyView::ResolveFloat returns.
- Rewriting only these three cases as the natural if (slots[index]) slots[index]->field = ... form makes VC7.1 reproduce that lookup lifetime. Their reachable CFGs move from 100/101/130 bytes to 116/117/153 versus target 125/126/162. Each handler is now uniformly short by 9 bytes / 3 instructions, isolating the remaining difference to the wider instruction-home/advance-tail frontier instead of laser semantics.
- The whole RunEcl candidate grows from 14,168 to 14,220 bytes, reducing the aggregate gap from 624 to 572. The 0x164 frame, 375 immediate + 4 indirect call census, 597 relocations, resolver multiplicities 131/100/17/24 and 193 compiler-table entries are unchanged. Two cold builds and target-independent CI are green.

## Packet 473 RunEcl child-ECL slot relookup

- Wire 135 SET_CHILD_ECL had the same over-caching pattern as the late-laser family. The maintained source kept the allocated ChildEclBlock pointer in a childEcl local through memset, subroutine-id write, InitializeSubroutine and the 0x78-byte context-variable copy.
- Clean TH08 supplies the adjacent source-family shape: cache only the child slot index, write the allocation into childEclBlocks[index], and re-index that slot for subsequent field/context accesses. TH09 target 0x0040AEAE independently confirms it. The target stores the resolved index to a home slot, writes the allocation into +0x33D8[index], zeroes the returned allocation, then reloads childEclBlocks[index] separately for subroutineId00, context08 and the copy destination.
- Restoring those natural re-lookups raises wire135 from 226 to 250 reachable bytes versus target 257. The whole RunEcl candidate grows from 14,220 to 14,248 bytes, reducing the aggregate gap from 572 to 544 while preserving the 0x164 frame, complete 375 immediate + 4 indirect call structure, 597 relocations, resolver multiplicities 131/100/17/24 and all 193 compiler-table entries.
- The remaining seven bytes in this opcode are not chased locally. Target has the global instruction-home write on its ordinary advance path and reloads the owner after rep stos; candidate still differs in those whole-owner register/tail choices. Two cold builds and target-independent CI are green; no register steering or filler is retained.

## Packet 474 RunEcl polar operand reevaluation

- The maintained POLAR_TO_CARTESIAN_ALT handler previously read angle and magnitude once into shared locals, then reused them for both sine and cosine outputs. Clean TH08 source independently shows the original source-family shape instead reevaluates both operands separately for each output expression.
- Fresh TH09 target disassembly agrees with that source family: the two trigonometric passes use four distinct resolved/raw float scratch values before WriteFloat and Sin/Cos. Restoring the nested expression shape makes VC7.1 allocate four scratch slots rather than reusing two, improving the local handler extent from 194 to 203 bytes against target 224.
- The full RunEcl logical candidate grows from 14,248 to 14,272 bytes against target 14,792, reducing the gap from 544 to 520. Two cold builds reproduce 14,272 bytes, frame 0x170, 375 immediate plus four indirect calls, 597 relocations, resolver multiplicities 131/100/17/24 and all 193 compiler-table entries. The target frame is 0x168, so exact stack-slot layout remains open.

## Packet 475 RunEcl timer assignment and cancel-laser relookup

- Fresh TH09 call census shows RunEcl makes no direct calls to exact ZunTimer::SetCurrent @ 0x004014D0. Its timer writes route through exact ZunTimer::operator=(int) @ 0x00401500 instead. Clean TH08 RunEcl source independently spells secondaryTime, shootIntervalTimer, bossTimer and damageReductionTimer as ordinary assignment.
- The remaining TH09 reconstruction SetCurrent spellings were therefore corrected to assignment. Pinned VC7.1 changes the relocation identities without changing the aggregate function size or resolver/table multiplicities, removing all direct SetCurrent relocations from the RunEcl candidate.
- CANCEL_LASER had a separate source-shape error: one cached laser pointer was used for the entire handler. TH09 target uses the initial pointer for null/inUse/state checks and state=2, then reloads laserSlots[index] for the timer assignment and reloads it again for width=currentWidth. TH08's macro-style laser access independently has the same relookup family. Restoring those lookups grows the logical candidate from 14,272 to 14,288 bytes, reducing the gap from 520 to 504.
- Two cold builds reproduce 14,288 logical bytes, frame 0x170 versus target 0x168, 375 immediate plus four indirect calls, 597 relocations, resolver multiplicities 131/100/17/24 and all 193 compiler-table entries. Exactness remains open.

## Packet 476 RunEcl laser angle relookup

- TH09 target disassembly shows ROTATE_LASER does not carry one laser pointer across the handler. It checks laserSlots[index], resolves the angle delta, reloads laserSlots[index] to read the current angle, calls AddNormalizeAngle, then reloads the slot again for the final angle store. Clean TH08 source independently expresses the same behavior through repeated laser-slot macros.
- AIM_LASER_AT_PLAYER follows the same source family: after the initial null check and resolved angle offset, target reloads the slot to obtain the laser position for PlayerAngleToPoint and reloads it again for the final angle store. The old TH09 reconstruction cached one laser pointer across both operations.
- Restoring those repeated lookups changes ROTATE_LASER from 97 to 113 physical bytes against target 116 and AIM_LASER_AT_PLAYER from 112 to 128 against target 126. The full RunEcl candidate grows from 14,288 to 14,320 bytes, reducing the logical gap from 504 to 472 while preserving calls, relocations, resolver counts and compiler tables. Two cold builds reproduce frame 0x170 versus target 0x168.

## Packet 477 RunEcl direct destination staging removal

- Three RunEcl source abstractions were found to create compiler temporaries absent from TH09 target. SET_REMOTE_FLOAT now repeats the remote lookup and calls EnemyView::ResolveFloat directly on that remote; the physical handler improves from 148 to 140 bytes versus target 132 and removes a raw-float home-store introduced by the reconstruction wrapper.
- Spawn-position resolution no longer returns EnemyFloat3 by value. The resolver writes x/y/z directly to the final spawnPosition93/94 slots, matching target dataflow and eliminating a target-absent three-load/three-store structure copy. The opcode93 physical span improves 295 to259 versus target263; opcode94 improves342 to306 versus target268.
- CREATE_LASER's ReadLaserFloat/ReadLaserInt/ReadLaserColor helpers previously named each raw operand before the mask test. TH09 target directly pushes/loads the instruction field or resolves it, then stores the descriptor field. Removing those named helper locals closes the shared CREATE_LASER/CREATE_LASER_AIMED block to exactly417 bytes versus target417; candidate has127 instructions versus target128 and the same12 direct calls.
- The whole candidate becomes14,172 bytes with frame0x13C versus target0x168. The worse aggregate size is a deliberate diagnostic consequence of removing target-absent temporaries: many EBP accesses become short disp8 encodings, exposing that other target long-lived locals/home slots are still missing. Calls,597 relocations,resolver counts131/100/17/24 and all193 compiler-table entries remain unchanged. Two cold builds reproduce the result; exactness remains open.

## Packet 478 RunEcl late laser-angle relookup

- TH09 target opcode 167 resolves the laser-slot index, uses laserSlots[index] only for the null test, then reloads laserSlots[index] after the float operand is resolved before writing angle +0x554. The maintained source previously cached the first lookup in lateLaser.
- Clean TH08 SET_LASER_ANGLE independently has the same source-family shape: test LASER(index), then evaluate LASER(index) again for the assignment. Restoring the indexed relookup in TH09 adds the target-visible second slot load without changing any helper call count.
- Two cold pinned builds emit 14,184 logical bytes versus target 14,792, improving the gap from 620 to 608 while keeping frame 0x13C, 375 immediate plus four indirect calls, 597 relocations, resolver multiplicities 131/100/17/24 and all 193 compiler-table entries unchanged.

## Packet 479 RunEcl side-owner relookup

- TH09 target TRIGGER_SIDE_OWNER_STATE repeatedly reloads manager side/opposing-side ownership rather than keeping owner/background pointers live across the handler. Primary/alternate owner-state tests, background start, spell-VM count, opposing script-base/player contribution, ANM manager and primary VM array are each reached through fresh owner chains.
- The maintained source previously cached lateOwner, lateBackground and lateOpposingBackground. Removing those caches and spelling the repeated member chains directly grows the handler from 245 to 260 reachable bytes against target 270, while preserving semantics and all helper-call identities.
- Two cold builds grow RunEcl from 14,184 to 14,224 logical bytes, reducing the target gap from 608 to 568. Frame 0x13C, 375 immediate plus four indirect calls, 597 relocations, resolver multiplicities 131/100/17/24 and all 193 compiler-table entries remain unchanged.

## Packet 480 ANM strip ownership and ECL trail-buffer correction

- Fresh TH09 callsite review resolves a prior exact-source ambiguity for 0x00439CF0 and 0x00439DC0. Both strip helpers return with ret 0x0C and never read ECX, so free stdcall and thiscall-member source can emit identical 199-byte bodies. The callers disambiguate ownership: each observed target call prepares g_AnmManager in ECX, and clean TH08/TH095 source calls the helpers as AnmManager members.
- Natural VC7.1 member-method probes for both horizontal and vertical helpers reproduce 199/199 target bytes with zero relocations. The canonical source and ledger therefore move to AnmManager::InitializeHorizontalTextureStrip and AnmManager::InitializeVerticalTextureStrip without changing exact counts.
- The same review corrects the RunEcl state overlay. TH09 draw/lifecycle evidence and target opcode 157 split the trail storage into 96 0x1C-byte samples at +0x33E8, 192 0x1C-byte render vertices at +0x3E68, then a 0x38-byte gap before trail flags +0x53A0. The old ECL overlay incorrectly modeled the whole +0x33E8..+0x539F range as 290 samples.
- RunEcl SET_TRAIL now passes trailVertices3E68 to the AnmManager member helper. Its callsite emits the target g_AnmManager relocation and enemy+0x3E68 address; the handler reachable extent is 166 bytes versus target174. The remaining eight bytes belong to the broader RunEcl instruction-home/tail layout frontier, not trail semantics.


## Packet 481 ECL SetExtraAnmScript private-TU exactness

- Fresh TH09 caller review proves Th09EclRunMovement::SetExtraAnmScript @ 0x00407B20 has exactly two callers, both inside EclManager::RunEcl at the opcode 57/61 handlers. Both target callsites enter the helper with Enemy in EDI and the instruction pointer in ESI; the helper has no stack arguments. This is compiler-private same-TU transport, not a public source calling-convention claim.
- The previous reconstruction placed the helper in EclHelpers.cpp under the project /Gr default. That external-TU form emitted only 125 bytes and required ECX/EDX argument moves, while the target is 261 bytes. The target helper also visibly reevaluates ECL operands inside each animation-bank branch rather than preserving one index/script pair across the function.
- Moving the natural source into EclManager.cpp as a TU-local static helper and preserving the target-observed repeated ReadInt expressions makes VC7.1 choose the private EDI/ESI transport on its own. Two cold canonical replays reproduce all 261 bytes and all nine REL32 relocations: seven ResolveInt @ 0x00405760 calls and two AnmLoaded::SetAndExecuteScriptIdx @ 0x00403E00 calls.
- The same correction improves the live RunEcl candidate from 14,224 to 14,244 logical bytes, reducing its gap from 568 to 548 while preserving frame 0x13C, 375 immediate direct calls, four indirect calls, 598 relocations, resolver multiplicities 131/100/17/24, and all 193 compiler-table entries. No register forcing, padding, assembly, volatile steering, or profile roulette is used.


## Packet 482 ECL SetPrimaryAnmScripts private-TU exactness

- TH09 native caller review fixes exactly two SetPrimaryAnmScripts callers, both shared RunEcl tails: opcodes 55/56 converge on the call at 0x0040953C and opcodes 59/60 converge on 0x0040968D. Each target call pushes six script values, places Enemy in EAX and passes no instruction register/stack argument.
- The previous external EclHelpers.cpp source emitted the correct 80-byte semantic body but used the project /Gr receiver in ECX. The source-level instruction parameter is unused. Moving the unchanged semantic helper into EclManager.cpp as TU-local static lets VC7.1 remove that unused transport and choose EAX naturally.
- Two cold canonical replays reproduce all 80 target bytes with zero relocations. The movement overlay is correspondingly refined with the target-observed signed one-byte animation-direction field at Enemy +0x3386; script fields remain +0x338A/+0x3390/+0x3392/+0x338C/+0x338E/+0x3394.
- The caller correction removes two target-absent argument-move pairs and changes live RunEcl from 14,244 to 14,240 bytes. The aggregate gap therefore grows from 548 to 552 even though the local ABI is target-correct. Frame 0x13C, 375 immediate direct calls, four indirect calls, 598 relocations, resolver multiplicities 131/100/17/24 and all 193 compiler-table entries remain unchanged. No register forcing or filler is retained.


## Packet 483 ECL ConfigurePolarMotion private-TU exactness

- TH09 native caller review fixes exactly one ConfigurePolarMotion caller inside RunEcl at 0x00409AA7. The target caller reloads Enemy from the RunEcl argument into EDI, copies the current instruction from EBX into ESI, then calls directly with no ECX/EDX or stack argument transport. The helper therefore belongs to the same compiler-private TU ABI family as PopContext and the ANM helpers rather than a public __fastcall contract.
- Complete target disassembly fixes a 0x0C local frame and three deliberate operand lifetimes. Operand 2 is normalized once into the angle home. Operand 3 speed is evaluated separately for x and y; operand 0 duration is evaluated separately for x, y and the final movement timer/duration write. The old external helper cached speed and duration across those uses and emitted only 288 bytes against the 346-byte target.
- Moving the helper into EclManager.cpp as TU-local static and restoring repeated target-observed expressions reaches the exact 346-byte extent. One source-order detail is compiler-visible: natural Cos(angle) * speed * duration and Sin(angle) * speed * duration make VC7.1 evaluate speed before duration while still emitting x87 fimul duration then fmul speed, matching the target. The inverse source order is exact-sized but permutes both resolver blocks and is rejected.
- Two cold canonical replays reproduce all 346 bytes and eleven reviewed REL32 relocations: ResolveFloat x3, ResolveInt x4, AddNormalizeAngle, compiler cosf/sinf wrappers and Th09EclTimerStorageView::operator=. The same caller correction moves live RunEcl from 14,240 to 14,244 bytes, gap 548, with frame 0x13C, 375 immediate plus four indirect calls, 598 relocations, resolver multiplicities 131/100/17/24 and all 193 compiler-table entries unchanged. No register forcing, padding, asm, volatile steering or profile roulette is used.


## Packet 484 ECL ConfigureRelativeMotion target corrections and one-byte plateau

- Fresh TH09 review of Th09EclRunMovement::ConfigureRelativeMotion @ 0x00407320 fixes one caller, RunEcl @ 0x0040981D. The target caller places the current instruction in EAX and Enemy in ECX before the call. Moving the helper from the artificial external helper TU into EclManager.cpp as a TU-local static function makes VC7.1 choose that private entry transport naturally.
- The target body corrects two stale source assumptions. The requested point is converted with target - worldPosition(+0x2DD4), not target - position(+0x2D74), while the interpolation origin is still copied from position. The final Float3(0,0,0) is stored to velocity at Enemy +0x2D8C, not positionOffset +0x2D80. These offsets agree with independent TH09 Enemy update/integration evidence.
- A POD three-float target local plus an anonymous zero Float3 gives the target 0x18 local frame and reuses the hidden-return temporary for the later constructor. Preserving the movement flag result in an ordinary local produces a 295-byte candidate against the 296-byte target. The residual tail is localized: the target schedules mov edi,eax before the second zero-argument push and emits or edi,0x400; the natural scalar form emits or eax,0x400 before assigning EDI.
- TH08 and TH095 exact ancestors were consulted only as source-shape hypotheses. Both use movement-easing/movement-mode bitfields, but applying the corresponding natural bitfield form to TH09 shrinks this helper to 289 bytes rather than reproducing the target. Signedness and split scalar assignments also leave the 295-byte form unchanged. The function therefore remains NON-EXACT; no register forcing, volatile steering, filler, assembly, or alternate optimization profile is retained.


## Packet 485 ECL timed/boundary movement private-TU exactness

- TH09 proves StartTimedPolarDisplacement @ 0x00407200 is a shared private movement helper with exactly two target callers, BeginBoundaryAwareMove and MoveRandomBiased. Both callers preserve Enemy in EDI and instruction in ESI and pass only the angle on the stack. The target helper reevaluates speed operand 2 for x and y, duration operand 0 for x and y, then duration again for the timer/duration write.
- Moving the helper into the RunEcl translation unit and spelling the target-observed repeated expressions directly yields an exact 288-byte body. Two cold canonical replays reproduce all 288 bytes and nine REL32 relocations: ResolveFloat x2, ResolveInt x4, cosf/sinf wrappers, and Th09EclTimerStorageView::operator=. This is the same compiler-visible speed-before-duration expression order established independently by exact ConfigurePolarMotion.
- Fresh TH09 body review of BeginBoundaryAwareMove @ 0x00407C30 also exposes a previously missing Float3 pointer-identity call at 0x004343D0 after the initial player-biased angle choice. A single returned float pointer is then reused for all four x/y boundary tests. Keeping the target-observed right-boundary expression that reads current movementAngle, the duration<=0 inline polar setup, and the positive-duration StartTimed tail produces a 510-byte exact body.
- Two cold canonical replays reproduce all 510 BeginBoundaryAwareMove bytes and 27 reviewed relocations, including RNG state/constants, positive-range RNG, AddNormalizeAngle, Float3::operator float*, ResolveInt/ResolveFloat, timer assignment, and StartTimedPolarDisplacement. The sole RunEcl caller naturally uses EAX=instruction / ECX=Enemy private transport.
- MoveRandomBiased @ 0x00407E30 was moved into the same TU at the same time so the exact StartTimed helper has one maintained implementation. Its target caller independently uses EAX=instruction / ECX=Enemy. The current natural body is 411 bytes versus the 516-byte target and remains NON-EXACT; no exact credit is claimed. RunEcl itself remains 14,244 bytes with gap 548 and unchanged call/resolver/table census.

## Packet 486 ECL random-biased movement reconstruction and Y-pointer plateau

- Fresh TH09 target review of Th09EclRunLate::MoveRandomBiased @ 0x00407E30 independently fixes the source shape that the earlier 411-byte reconstruction had hidden. The target repeatedly walks Enemy -> manager +0x320 -> side +0x04 -> Player and reloads Player +0x1B88 during the horizontal wrap comparisons instead of preserving playerX/enemyX locals. The four random-angle branches also distinguish three AddNormalizeAngle paths from one raw rng(pi/2) - pi/4 path.
- Restoring those repeated TH09-observed chains and branch-specific expressions, then adding the target call to Float3::operator float* @ 0x004343D0 before the vertical bounds checks, grows the natural same-TU VC7.1 body from 411 to 517 bytes against the 516-byte target. The target and candidate now share the private EAX=instruction / ECX=Enemy entry family and the exact StartTimedPolarDisplacement tail; the reconstructed instruction stream follows the target through relative offset +0x133.
- The remaining size residual is localized to the stable Y alias after Float3::operator float*. Target emits lea ecx,[eax+4] and then performs both Y comparisons through [ecx]. The best natural source preserves the returned base pointer and performs both comparisons through [ecx+4]; the second disp8 makes the candidate one byte longer. This is an encoding consequence of alias lowering, not an unresolved gameplay behavior.
- Bounded natural probes for float *positionY = base + 1, a float & alias, a float *const alias, and float *positionY = &base[1] all emit 518 bytes because VC7.1 chooses mov ecx,eax; add ecx,4 rather than the target LEA. The repository therefore keeps the clearer 517-byte base-pointer source and records the helper as NON-EXACT. No register forcing, volatile steering, inert padding, assembly, or alternate-profile roulette is retained.

## Packet 487 Post-ECL private owners close and RunEcl narrows to forty bytes

- TH09 call-site evidence supersedes the old member-ABI assumption for 0x00408180 and 0x00408560. RunEcl calls 0x00408180 with Enemy already in ESI and, without preparing ECX, copies ESI to EDI before 0x00408560. Each target has one xref. File-static helpers in the real EclManager translation unit make VC7.1 select those private transports naturally.
- EnemyPostEclUpdateMovement becomes structural-exact only after restoring the target single primaryFlags337C snapshot across easing, mirror and final mode clear, and after making the ordinary non-inline Float3::FromAngleMagnitude definition visible in the same translation unit. Its canonical owner is physical rather than body-only: 967 logical bytes through 0x00408546, one NOP at 0x00408547 and the six-entry easing table at 0x00408548-0x0040855F. Two cold match-unit replays reproduce the full 992-byte extent and every reviewed relocation.
- The same-TU Float3 fact was independently regression-checked against exact ExAttack type7/type8/type9 initializers. Marking that definition inline is a negative result: it changes both ECL and ExAttack caller codegen. Keeping one ordinary out-of-line definition later in the shared TU preserves all four caller families; the ExAttack exact units were rehomed to the EclManager TU and replay exact.
- EnemyPostEclUpdateShotAndAnm exposes a second nested private edge. Target prepares pending instruction in ESI, copies Enemy to EBX and calls 0x00408040. Making DispatchShotInstruction TU-private naturally gives the target EDI receiver allocation in the 0x00408560 owner. Moving the ordinary direction=0 source lifetime before the mirror test then produces the target xor ebx,ebx at +0x71. Two cold canonical rounds reproduce 349/349 with every relocation solved.
- DispatchShotInstruction itself remains separate and honestly non-exact. TU co-location improves its natural candidate from the old 329 bytes to 321 bytes versus target 315; no public fastcall claim, register forcing, volatile steering, padding, assembly or profile search is retained.
- The TU correction has a large transitive effect on EclManager::RunEcl. A fresh canonical-source build emits 14,832 logical bytes versus target 14,792, replacing the superseded 14,244-byte / 548-byte-short baseline with a 40-byte overage. The frame is 0x150 versus target 0x168. All 375 immediate direct-call destination multiplicities, four indirect calls, 193 table entries and resolver counts 131/100/17/24 remain closed. This is a genuine large-owner advance from corrected side-effect/TU modeling, not size padding.

## Packet 488 ECL shot dispatch closes through operand lifetimes

- Th09EclRunBullet::DispatchShotInstruction @ 0x00408040 was already proven to be a TU-private owner by its two TH09 callers: Enemy arrives in EBX and the instruction pointer in ESI. Moving the maintained natural body into the EclManager translation unit had reduced the old split-TU 329-byte candidate to 321 bytes while keeping the exact 0x00408560 caller unchanged.
- The remaining six bytes were ordinary source-lifetime differences, not a compiler profile or register-allocation problem. For bulletType, the target sign-extends instruction +0x0C once before testing parameterMask bit 0 and reuses that int on both raw and ResolveInt paths. Introducing one ordinary int local changes VC7.1 from two branch-local MOVSX operations to the target MOVSX-plus-register-copy form and removes two bytes.
- The same pattern at color closes the final four bytes: target sign-extends instruction +0x0E once before testing bit 1, copies the int into EDX only on the ResolveInt path, and otherwise reuses EAX directly. An ordinary int color local gives that lowering naturally.
- Two cold pinned-VC7.1 canonical replays reproduce all 315 bytes. The relocation-aware comparison has 275 ordinary comparable bytes and ten REL32 relocation fields, all resolving to the reviewed TH09 destinations. EnemyPostEclUpdateShotAndAnm remains exact at 349/349. The large RunEcl aggregate remains 14,832/14,792 with the same 0x150 frame and closed call/resolver multiplicities, so this leaf closure does not disguise the remaining large-owner frontier.
- Adding these two locals changes one compiler-private COFF label identifier in the already-exact movement owner's manifest from $L6417 to $L6419. A direct full 992-byte comparison remains structural-exact and all target destinations are unchanged; all other EclManager exact-unit manifests replay unchanged. The local label spelling is compiler bookkeeping, not semantic evidence.

## Packet 489 RunEcl SET_BOSS control-flow and lifetime correction

- Fresh TH09 review of the SET_BOSS handler disproves the maintained cached stateIndex shape. After the first ReadInt, target clears ESI and compares the result against that zero. The non-negative path is physical fall-through; only the negative teardown path is taken by JL. Rewriting the source as stateInt >= 0 reproduces this branch order naturally instead of relying on a source-equivalent but compiler-visible inverted if.
- On the positive path the target evaluates operand 0 again for the boss-slot array write, evaluates it a third time for bossSlot336B, then reloads bossSlot336B for SetBossMarkerState. On the negative path it reloads bossSlot336B independently for the slot clear, marker-state call and marker-position call. Removing the stale local cache lets VC7.1 select the target zero lifetime in ESI and reuse it for the null slot value and minimumPlayerDistanceSquared33A8 = 0.
- The teardown path constructs Float3(-999,-999,0) and immediately passes the constructor return in EAX to SetBossMarkerPosition. Natural direct temporary source, taking the address of Float3(-999,-999,0) in the call expression, emits target push-eax behavior; the prior named local forced a second LEA. Clean TH08 source shows the same source family, but the TH09 constructor/use sequence is the accepting evidence.
- The corrected handler keeps all twelve canonical EclManager exact units exact and moves the live RunEcl aggregate from 14,832 to 14,820 logical bytes against target 14,792. The candidate is now 28 bytes over target; frame remains 0x150 versus target 0x168, and all 375 immediate direct calls, four indirect calls, 193 compiler-table entries and resolver counts 131/100/17/24 remain closed.
- The near-equal aggregate size is not an exactness claim. Opcode-table entry mapping still proves substantial physical block-order differences: for example target opcode 1 begins near relative +0x1F2 while the current candidate begins near +0x332. The next frontier is therefore the main-loop and after-dispatch control-flow shape, not blind size trimming.

## Packet 490 RunEcl main-loop CFG restoration

- The prior 14,820-byte RunEcl checkpoint was aggregate-size-near but physically misleading: the first opcode case was emitted near relative +0x332 while TH09 begins the opcode table body at +0x1F2. Fresh target review therefore treats block placement and branch structure as stronger evidence than whole-function size.
- TH09 hot-path disassembly proves an inner dispatch-loop shape. At relative +0xBF VC7.1 keeps a pointer to activeContext->secondaryTime094 live, uses it for the timer conversion and decrement, and reloads activeContext separately for time008. A positive secondary timer and an instruction-time mismatch both leave the inner loop for the same frame-tail block. Replacing the earlier long-goto shape with a natural for(;;)/break loop moves the opcode switch back to the target region without changing behavior.
- The frame tail has a second lifetime constraint. Target tests life2E48 before loading activeContext, then derives the per-frame callback and the address of interpolationSlots0A0[0]. That slot-base address is live across the callback together with the position snapshot. Iterating the eight interpolation owners as a slot pointer plus countdown reproduces this call-surviving alias family more faithfully than indexing from a long-lived context pointer.
- The conservative maintained form deliberately leaves opcode-2/JUMP timer source unchanged while fixing the main CFG. It emits 14,872 logical bytes versus target 14,792, with frame 0x154 versus target 0x168. This is +80 rather than the previous +28, but the first opcode entry is now exactly relative +0x1F2 instead of about +0x332. All 375 immediate direct calls, four indirect calls, 193 compiler-table entries and resolver counts 131/100/17/24 remain closed.
- Bounded direct-activeContext probes for SET_SECONDARY_TIME are negative results. Straight assignment, explicit if/else and branch-with-break forms all let VC7.1 tail-merge the two target-distinct Timer::operator= callsites, reducing immediate direct calls to 374. A manually duplicated advance/redispatch form can preserve the target callsites but is not retained because it overstates evidence for the original source shape.
- The first downstream size drift now exposes stack-layout debt rather than wrong arithmetic. Target SET_FLOAT stores its temporary at [ebp-0xA4], so three accesses use disp32 encodings; the current 0x154-frame candidate places the same semantic temporary in disp8 range, accounting for nine bytes of later entry drift. The remaining frontier is therefore natural local lifetimes/frame layout and the few timer-case aliases, not padding or arbitrary size matching.
- Reordering RunEcl changes only compiler-local labels used by the already-exact post-ECL movement easing table. A direct full 992-byte comparison remains structural-exact with identical target destinations; the canonical manifest is refreshed from $L6419 to $L6421 and the six local table labels from $L4429..$L4434 to $L4431..$L4436. These names are COFF bookkeeping, not semantic facts.

## Packet 491 EnemyManager OnUpdate type and call-surface recovery

- Fresh TH09 disassembly and the attested IDA body for `EnemyManagerView::OnUpdate @ 0x00410730-0x0041165A` independently show 69 direct call sites and a `0x2A8` stack frame. The previous maintained 3,156-byte candidate had only 59 direct call sites and a `0x25C` frame, so the large residual was not a register-allocation plateau.
- The missing structure was target-backed ownership and source shape. TH09 calls canonical `Float3::Float3`, `operator+`, `operator-`, `operator*`, `operator/`, and `operator float *`; constructs a full `0x214` bullet spawn descriptor through `0x0040D500`; dispatches it through side `+0x08` to `EtamaController::SpawnBulletPatternPrimary @ 0x004130F0`; keeps four static playfield-test callsites; emits three separate death-effect spawns; and calls `AnmProjectionAbs @ 0x0040F1C0`.
- Replacing the artificial scalar vector helpers and the stale 0x200 `SpecialAttack` owner with those target-proven types and call boundaries grows the natural pinned-VC7.1 `/O2 /Ob1` body to 3,376 bytes. Its frame is now `0x2B0`, only eight bytes larger than target, and 67 direct call sites align in order with the target call sequence. The two remaining static call-surface differences are one extra target normal-path `Float3::operator+` site and a separate target out-of-bounds `EnemyView::CleanupAfterDeactivation` site.
- The same source file owns canonical `EnemyCoreUpdateAttachedEffects @ 0x0040F4C0`; two independent cold canonical replays after the large-owner corrections still reproduce all 151 bytes and both relocations. The `OnUpdate` improvements therefore do not spend accepted exactness elsewhere in the TU.
- This remains a source-present NON-EXACT large frontier. Aggregate size is diagnostic only; follow-up should continue from the remaining owner/CFG and local-lifetime differences rather than padding, volatile/register steering, assembly, or optimization-profile roulette.

## Packet 492 EnemyManager trail-history assignment recovery

- The largest remaining `EnemyManagerView::OnUpdate` size jump was localized between the normal world-position update and the first playfield test. TH09 does not lower the history shift as one trivial 0x1C-byte aggregate assignment: for `trailHistoryLength53A2 - 1 >= 4` it naturally contains a four-sample unrolled loop, then a scalar remainder loop, copying each sample's position Float3, velocity Float3 and angle independently.
- The maintained `sample[i] = sample[i - 1]` source let VC7.1 collapse each iteration to `rep movsd`, which erased roughly 340 bytes of target structure. Rewriting the same semantics as three member assignments (`position00`, `velocity0C`, `angle18`) makes pinned VC7.1 `/O2 /Ob1` produce the target-shaped four-way unroll and remainder loop without pragmas, volatile qualifiers, register steering, assembly or padding.
- This single natural source correction moves the large candidate from 3,376 to 3,716 bytes, reducing the logical gap from 507 to 167 bytes. The frame remains `0x2B0` versus target `0x2A8`, while direct call sites rise from 67 to 68 because VC7.1 no longer tail-merges the second target-observed `CleanupAfterDeactivation` path.
- The direct-call surface is now aligned in order except for one target-only normal-path `Float3::operator+` site. The same source file's canonical `EnemyCoreUpdateAttachedEffects @ 0x0040F4C0` still replays 151/151 exact in two cold passes.

## Packet 493 EnemyManager call-owner and ANM layout correction

- TH09 target callsites disprove several convenience free-function aliases in the maintained `EnemyManagerView::OnUpdate` source. `Supervisor::SelectSide @ 0x00401440` receives `g_Supervisor` in ECX with the side index on the stack; `EnemyEclManagerView::GetSubroutine @ 0x0040F170` receives the offset-zero primary ECL manager in ECX with the schedule index on the stack; all four `GameManagerPlayfieldView::IsWithinPlayfield @ 0x0041A6EB` sites load the GameManager receiver; both `EnemyAppendCollisionView::AppendPlayerCollisionBox @ 0x00410250` sites use Enemy as ECX plus two stack pointers; and `SoundPlayer::PlaySoundPositionedByIdx @ 0x0043E380` receives the SoundPlayer singleton in ECX with sound id / x on the stack.
- The previous free aliases happened to preserve some control flow under the global `/Gr` profile, but they were not faithful source ABIs. The maintained owner now calls the repository-proven member interfaces instead. `EnemyManagerView::primaryEclManager000` is independently asserted at offset zero, so the target's `ECX=EnemyManagerView` GetSubroutine call is naturally explained rather than promoted to a private register ABI claim.
- The private Enemy ANM VM overlay was also stale by eight bytes. Independent TH09 ANM owners place color1 at VM `+0x1F0`, color2 at `+0x1F4`, flags at `+0x1F8`, type at `+0x1FC`; after correction the damage-flash block naturally accesses Enemy `+0x1FB/+0x1FC..+0x1FF/+0x200`, exactly the target offsets.
- These locally target-correct changes make the aggregate OnUpdate object shorter: 3,684 versus 3,883 target, frame `0x2B0` versus `0x2A8`, and 67 versus 69 direct calls. This is an honest regression from the prior 3,716-byte diagnostic, not evidence against the corrected owners. The current compiler now tail-merges one target-distinct `CleanupAfterDeactivation` site; a second target-only normal-path `Float3::operator+` site is also still merged. Follow-up should recover the surrounding source lifetimes/CFG naturally rather than restoring target-disproved free ABIs.
- The same source file's canonical `EnemyCoreUpdateAttachedEffects @ 0x0040F4C0` still reproduces all 151 bytes and both relocations in two cold replays after the owner/layout corrections.

## Packet 494 EnemyManager death-entry child ECL free loop

- TH09 target bytes at `0x00411244-0x0041126C` disprove the maintained death-entry shortcut that called `EnemyCoreReleaseChildEclBlocks` once after finding the first non-null slot. The target walks all four pointers at Enemy `+0x33D8`, calls exact `ZunMemory::Free @ 0x00401360` for every non-null block, immediately stores NULL to that slot, advances by four bytes, and continues until four entries are consumed.
- Replacing the shortcut with the natural four-slot `g_ZunMemory.Free` loop makes pinned VC7.1 emit the same loop organization (pointer cursor, count=4, null test, member Free call, zero store, pointer increment, decrement/backedge); only the ESI/EDI cursor/count allocation differs. The large body grows from 3,684 to 3,696 bytes, reducing the diagnostic gap to 187 bytes without forcing registers or adding filler.
- This loop is distinct from the later death-callback reset path, which still calls `EnemyCoreReleaseChildEclBlocks @ 0x004100E0` after resetting callback state. Collapsing the two ownership paths into one helper was therefore a semantic error, not just a codegen spelling difference.
- The same-TU canonical `EnemyCoreUpdateAttachedEffects @ 0x0040F4C0` remains exact at 151/151 in two independent cold replays after the correction.

## Packet 495 EnemyManager death-callback ECL owner and sub-id lifetime

- TH09 target bytes at `0x004113BE-0x00411443` and the canonical exact callee prove that the death ECL callback uses `Th09EclRunState::ManagerStateView::InitializeSubroutine @ 0x00406850`, not the maintained convenience `g_EnemyCoreEclManager.CallSub` alias. The call receives the current EnemyManager / offset-zero primary ECL manager in ECX, `&Enemy+0x7F4` and the callback short on the stack.
- The target does not preserve `deathCallbackSubId2D2E` across `ResetBulletRankInfluence`, threshold resets, `ReleaseChildEclBlocks`, and the `0x214` template copy. It reloads the short only after those operations, zero-extends it for the call, then writes `-1` back after `InitializeSubroutine`. Removing the early source local recovers that lifetime/order directly.
- Natural pinned VC7.1 now emits the exact callee symbol and late callback-id read. Aggregate `OnUpdate` becomes 3,692/3,883 bytes with frame `0x2B0` and 67/69 direct calls, four bytes shorter than Packet 494. The shorter aggregate is retained because both the old callee owner and the early cached-id lifetime are independently target-disproved.
- The same TU canonical `EnemyCoreUpdateAttachedEffects @ 0x0040F4C0` remains 151/151 exact in two cold replays.

## Packet 496 EnemyManager side/player lifetime recovery

- TH09 target code repeatedly reloads the side owner from EnemyManager `+0x320` at distant blocks: inactive-enemy homing cleanup, side-category filtering, special-attack effects/Etama dispatch, pause-state checks, collision gates, damage/homing work, death score/effect handling, and player-state transition. The maintained source instead kept `side` and `player` pointers live from before the 128-enemy loop through the entire 3.8KB owner.
- Removing those two function-wide pointer locals and expressing each use through `manager->sideState320` / `->player04` makes pinned VC7.1 naturally reproduce the target `0x2A8` stack frame exactly. The prior candidate frame was `0x2B0`; the exact eight-byte closure matches the two removed 32-bit pointer lifetimes and is corroborated by the target's repeated owner reloads.
- The same change grows the natural body from 3,692 to 3,828 bytes against the 3,883-byte target, reducing the logical gap from 191 to 55 bytes. Two independent cold builds produce identical OnUpdate disassembly at 3,828 bytes, frame `0x2A8`, and 67 direct calls. No register forcing, volatile steering, padding, assembly, or profile change is involved.
- The direct-call surface still has two target-distinct sites that VC7.1 currently merges under the maintained source: one normal movement `Float3::operator+` and one out-of-bounds `CleanupAfterDeactivation`. The remaining 55-byte gap is therefore a bounded CFG/lifetime frontier rather than a broad missing-owner problem.
- The same TU canonical `EnemyCoreUpdateAttachedEffects @ 0x0040F4C0` remains 151/151 exact in both cold passes.

## Packet 497 EnemyManager canonical member-owner correction

- TH09 `EnemyManagerView::OnUpdate` target callsites establish canonical receivers that the maintained large-owner source had still represented through convenience free/global aliases. At `0x00410AC0`, ECX is the current EnemyManager before `RunEcl @ 0x004086C0`; because `primaryEclManager000` is independently fixed at offset zero, this is naturally `enemyManager->primaryEclManager000.RunEcl(enemy)`, not a separate global ECL manager.
- The same target/local-source evidence binds movement and lifecycle helpers to existing `EnemyView` members: two `ClampPositionToMovementBounds` calls, `IntegrateMotion`, `ResetBulletRankInfluence`, and `CleanupAfterDeactivation`. The public reconstruction header now declares the already-maintained cleanup member and the primary ECL RunEcl member without changing object layout.
- Natural pinned VC7.1 with those real owners is cold-stable at 3,824/3,883 bytes, target-exact frame `0x2A8`, and 67/69 direct calls. The four-byte aggregate regression from Packet 496 is retained because the replaced global/free receiver forms are target-disproved; aggregate size does not override receiver identity.
- Two cold replays continue to reproduce canonical `EnemyCoreUpdateAttachedEffects @ 0x0040F4C0` at 151/151 bytes. The residual large-owner frontier remains bounded to 59 bytes and two compiler-merged target callsites.

## Packet 498 EnemyManager OnUpdate bounded register/lifetime plateau

- After Packet 497, `EnemyManagerView::OnUpdate` is cold-stable at 3,824/3,883 bytes with the target-exact `0x2A8` frame and 67/69 direct calls. The two remaining target-distinct physical callsites are the normal movement branch's second `Float3::operator+` and the later shared offscreen `CleanupAfterDeactivation` block.
- Bounded natural-C++ probes do not justify steering those encodings. Explicit target-shaped labels/gotos are re-merged by VC7.1; branch-local `Float3&`/`Float3*` aliases, operand references, an explicit EnemyManager primary-ECL prefix, function-scope integer declarations, and explicit trail-sample cursors all return the same 3,824-byte / 67-call baseline. Splitting the offscreen short-circuit into explicit branches is worse (3,793 bytes / 66 calls) and is rejected.
- A branch-local named movement `Float3 worldPosition` does recover the third operator+ call and reaches 3,864/3,883, but its generated copy reloads the named stack object after the call whereas the target copies directly from the operator return EAX. It is therefore not retained merely for the attractive aggregate size.
- TH09-maintained `BackgroundRender.cpp`, plus TH08/TH095 as corroboration only, establishes that an empty inline `Float3()` source shape is plausible. Even with that shape, moving `trailHitbox` before the marker does not swap the target/candidate `-0x28/-0x34` stack identities. Moving all TH08-like locals to function scope expands the frame to `0x2C0`; moving only `trailIndex/damage/hitKind/enemyIndex` is codegen-identical to baseline. No such declaration-order experiment is retained.
- The target trail-collision / damage / homing chain uses ESI/EDI in the opposite roles from the current natural candidate: target ESI=trail-sample cursor then primary-damage pointer, EDI=trailIndex then damage; the candidate mirrors those roles. Explicit sample-cursor and damage-pointer locals do not swap the allocator choices. A target-shaped phased homing cache (side lives across the first subtraction, player cached only afterward) recovers the local lifetime ordering but remains shorter at 3,797 bytes and keeps the old/new Float3 homes swapped. This is useful structural evidence, not an exact-source promotion.
- Further work on this 59-byte frontier should require new TH09-local evidence about a real source lifetime/type/owner that changes the allocator naturally. Register forcing, `var_order`, volatile steering, filler, assembly, or profile roulette remain out of scope.

## Packet 499 Player selector timer semantics and call-surface recovery

- Fresh TH09 target disassembly of `PlayerUpdateSelectorState @ 0x004049A0-0x00405645` shows three timer-current reads through the shared/folded physical body `0x00435F00`. At each site ECX points at a 0x0C ZunTimer-shaped object and the body returns `[ecx+0x08]`; the physical body retains its independently reviewed folded ownership rather than being uniquely reassigned to ZunTimer.
- The maintained selector source had three semantic timer errors: opening and secondary timing paths assigned zero where the target calls exact postfix `ZunTimer::operator++ @ 0x00401510`, and the final frame timer likewise assigned zero instead of postfix-incrementing. The selector also inlined timer-current/modulo/zero tests that the target keeps as physical calls.
- Natural source now reproduces the target call boundaries for all three `0x00435F00` current reads, all three postfix timer increments, exact `ZunTimer::HasTickedEvery @ 0x00404920`, and exact `ZunTimer::operator== @ 0x00404950`. The pinned VC7.1 candidate grows from 2,939 to 2,991 bytes. Its call-relocation sequence now aligns in order with every target selector call except the entry `??_H @ 0x00401470` vector-constructor iterator.
- The target entry uses `??_H` on three 0x0C elements with constructor callback `0x004343D0`; the current natural `/O2 /Ob1` candidate still emits an explicit three-iteration constructor loop. Merely moving the array declaration earlier does not change codegen, and one evidence-backed `/Os` probe is worse (frame `0x70`, fewer calls) and is rejected. No broader profile search is justified.
- The selector target frame is `0x5C`; the current candidate remains `0x68`, so 12 bytes of local-lifetime/layout debt remain independently of the now-closed call surface. Same-TU canonical `PlayerGameplayMethods::ResolvePatternOffset @ 0x00404710` remains 346/346 exact with its 412/412 compiler-owned switch-table extent in two cold replays. The source change only shifts two VC7.1 internal switch-table labels (`$L1676/$L1677` to `$L1690/$L1691`); relocation offsets, types, target addresses and all compared bytes are unchanged.

## Packet 500 Player selector target-pattern branch duplication

- Packet 499's timer correction is retained, but its statement that the entry vector-constructor helper was the only remaining physical-call difference was too strong. Fresh target/candidate alignment shows target `SelectTargetPattern` keeps two separate `ResolvePatternOffset @ 0x00404710` callsites: one for the target-left branch and one for the target-right branch, before those paths merge into the shared collision test.
- The maintained source previously selected pattern 5/7 or 6/8, merged the branches, and called `ResolvePatternPosition` once. Expressing the target CFG naturally—declare one candidate position, call `ResolvePatternPosition` inside each horizontal branch, then share the final collision test—makes VC7.1 retain both physical `ResolvePatternOffset` calls without attributes or register steering.
- The pinned `/O2 /Ob1` selector grows from 2,991 to 3,147 bytes against the 3,238-byte target. It now has 33 physical call relocations and all five target `ResolvePatternOffset` callsites. The remaining 91-byte frontier is no longer a missing-call problem; the target entry still uses `??_H @ 0x00401470` whereas the candidate emits an explicit constructor loop, and the candidate frame remains `0x68` versus target `0x5C`.
- Two independent cold replays after this CFG correction keep same-TU canonical `PlayerGameplayMethods::ResolvePatternOffset @ 0x00404710` exact at 346/346 bytes and 412/412 owned compare extent. No existing exact Player unit is spent to improve the large owner.

## Packet 501 Player selector geometry and manager lifetime

- TH09 target bytes at `0x00404C5E-0x00404C8E` correct an earlier maintained assumption: the first collision half-size is `28.0f x 28.0f`, not 32x32; the second is 10x10 and the third is copied from Player hurtbox `+0x1CA8`. The target performs these assignments only after the selector cell is derived and immediately before the alternate-pattern gate. The maintained source now uses those target values and timing.
- The selector's old `manager = PrimaryTargetManager(player)` local lived from the alternate-pattern check near the middle of the function through the late periodic event gate. Target code does not preserve that pointer across the intervening large CFG: it consumes the early manager locally and reacquires the side/target manager near `0x004054E8`.
- Scoping the two manager uses independently is a pure lifetime correction under pinned VC7.1: selector code size remains 3,147 bytes and all 33 call relocations remain, while the stack frame shrinks from `0x68` to `0x64`. A focused lifetime matrix independently shows the maintained long-lived `manager`, `protocol`, and `player` source pointers each account for one four-byte frame home; removing all three yields the target `0x5C` frame, but naively recomputing protocol/player at every use over-expands code and is not retained.
- The remaining lifetime work therefore concerns target-shaped local caching of protocol/player, not register forcing or declaration-order roulette. Same-TU `ResolvePatternOffset @ 0x00404710` remains canonical exact at 346/346 bytes and 412/412 owned compare extent in two cold replays; only its two compiler-internal table labels advance from `$L1690/$L1691` to `$L1691/$L1692`, with identical relocation offsets/types/target addresses.

## Packet 502 Player selector Player-pointer lifetime

- TH09 target `PlayerUpdateSelectorState` repeatedly reloads the Player owner from selector header `+0x78`: more than twenty distinct reloads are present from the opening-timer logic through pattern search, target selection, periodic signaling and the late state gate. This is incompatible with treating one Player pointer as a function-wide source lifetime merely because the value is semantically stable.
- After the Packet-501 manager-lifetime correction, replacing the function-wide `player` source local with direct `header->player78` owner accesses makes pinned VC7.1 shrink the frame from `0x64` to `0x60`. The resulting selector has 33/33 physical calls and a cold-stable 3,138-byte body versus target 3,238.
- The body becomes nine bytes shorter despite the frame improvement. That is retained rather than reverting the target-disproved long lifetime: exact reconstruction treats target-backed owner/lifetime evidence as stronger than aggregate size. The generated candidate now contains 27 header `+0x78` Player reloads, closely matching the target's repeated-reload organization.
- Same-TU `ResolvePatternOffset @ 0x00404710` remains canonical exact at 346/346 bytes and 412/412 owned compare extent in two cold replays. The only manifest-only effect is that its two compiler-internal table labels return from `$L1691/$L1692` to `$L1690/$L1691`, with relocation offsets/types/targets unchanged.
- One four-byte frame gap remains. The function-wide `protocol` pointer is the remaining obvious long source lifetime: removing it naively gives the target `0x5C` frame but over-expands selector code, so follow-up should recover target-shaped short protocol scopes/recomputations rather than mechanically expanding every use.

## Packet 503 Player selector protocol lifetime and exact frame

- After removing target-disproved function-wide manager and Player lifetimes, one four-byte selector frame gap remained. The remaining long-lived source pointer was `protocol`, rooted in `g_PlayerSideProtocols[player->sideIndex]`.
- TH09 target protocol accesses are not uniform. Before `pattern_selected`, flag updates repeatedly reconstruct the side-indexed address from header `+0x78` -> Player side index -> 0x8E stride. After `pattern_selected`, the target enters a long cohesive pattern/finalization region where the same side/protocol state can naturally remain locally available. This rules out both a function-wide protocol pointer and blind per-use recomputation as faithful source shapes.
- A focused lifetime matrix confirms the compiler consequence. Keeping protocol function-wide gives frame `0x60`; recomputing it at every use gives the target `0x5C` frame but over-expands the selector to 3,352 bytes. Recomputing in the early region and introducing one short-lived protocol pointer only after `pattern_selected` produces a cold-stable 3,271-byte body with target-exact frame `0x5C` and all 33 physical call relocations preserved.
- This is a structural convergence rather than aggregate-size steering: target-observed protocol address reconstruction determines the early lifetime, while the later cache is bounded to one natural CFG phase. Same-TU `PlayerGameplayMethods::ResolvePatternOffset @ 0x00404710` remains canonical exact at 346/346 bytes and 412/412 owned compare extent in two independent cold replays.
- The selector is now only 33 bytes larger than the 3,238-byte target with exact frame and call surface. Remaining work should compare local instruction/lifetime shapes and the entry construction lowering. Target uses `??_H @ 0x00401470` for three 0x0C elements; the candidate still emits an explicit constructor loop. Do not trade the now-exact frame for declaration/register tricks.

## Packet 504 Player selector target-helper Player lifetime

- With the main selector frame and call surface already closed, target/candidate segment alignment localized the largest remaining excess to the final target-selection helper. The maintained `SelectTargetPattern` still cached `header->player78` across opponent-target resolution and both horizontal pattern branches.
- TH09 target code explicitly reacquires Player at `0x004052B0` via `mov ecx,[esi+0x78]` after opponent-target resolution, immediately before comparing the target position against Player position. This independently disproves a helper-wide Player source lifetime.
- Replacing that helper local with target-shaped `header->player78` reacquisition reduces the natural pinned VC7.1 selector from 3,271 to 3,256 bytes. The target frame remains exact at `0x5C` and all 33 physical call relocations remain represented. The logical residual is now only 18 bytes.
- Same-TU canonical `PlayerGameplayMethods::ResolvePatternOffset @ 0x00404710` remains 346/346 exact with 412/412 owned compare extent in two cold replays. This source change again shifts only the two compiler-internal switch-table labels (to `$L1689/$L1690`); relocation offsets, types, target addresses and all compared bytes are unchanged.
- Further selector work should now be instruction/lifetime comparison on the 18-byte residual plus the entry constructor lowering. Do not disturb the target-exact frame or the closed 33-call surface merely to chase aggregate size.

## Packet 505 Player selector failure tail and protocol storage width

- TH09 target failure-tail code at `0x004055A0-0x0040563F` has a bounded Player lifetime: after the flags74 test it loads Player once into EDI and reuses that value across GetUpdateState, timer equality, scalar30384 and the first protocol writes. After setting the event flag at protocol `+0x32`, it deliberately reloads Player from selector header `+0x78` before setting the pattern flag at `+0x2C`. The maintained tail now follows that target owner/reload boundary.
- The private protocol view's `+0x32` and `+0x34` fields were previously declared as bytes. TH09 selector accesses only their low flag byte, while the surrounding 0x8E record family has established 16-bit word storage at neighboring flag/input fields. A focused stock VC7.1 experiment fixes the codegen distinction: `unsigned char field |= 1` emits LEA-of-field followed by OR through EAX, while `unsigned short field |= 1` emits the target form—direct `or byte ptr [index*0x8E+field],1` followed by LEA of the field—even when the enclosing function returns void. The maintained view therefore uses 16-bit storage at `+0x32/+0x34` with explicit offset assertions.
- Combining the target-bounded tail Player cache/reload with those field widths reduces the natural pinned VC7.1 selector from 3,256 to 3,237 bytes. The target is 3,238 bytes; frame is target-exact at `0x5C`; all 33 physical call relocations are present. This is still NON-EXACT: entry construction remains target `??_H @ 0x00401470` versus an explicit candidate constructor loop, and local branch/instruction layout differs.
- An EH-disabled probe on the strongest selector source is codegen-identical for the selector and does not recover `??_H`; the ReplayManager EH-lowering precedent therefore does not transfer here. Replacing the artificial 0x0C constructor view with default-constructible `PlayerPositionView[3]` adds unwanted constructor calls for other PlayerPosition locals and is rejected.
- Same-TU canonical `ResolvePatternOffset @ 0x00404710` remains 346/346 exact and 412/412 owned-extent exact in two cold replays. Selector changes shift only VC7.1 internal switch-table label names in that exact unit; relocation offsets, types, target addresses and bytes remain unchanged.
- Do not chase the one-byte aggregate difference through explicit jump forcing, padding, volatile/register tricks, `var_order`, assembly or profile roulette. Further Player selector work requires new TH09-local evidence about the constructor/TU/type or another real lifetime/CFG distinction.

## Packet 506 EtamaController OnUpdate owner and laser-timer recovery

- Fresh TH09 target disassembly of `EtamaController::OnUpdate @ 0x004146F0-0x00414F82` disproves three convenience sprite helper calls in the maintained large owner. Bullet culling reads `bulletVm.loadedSprite +0x224` directly, and laser geometry loads `bodyVm.loadedSprite` once before dividing width by sprite `+0x34` and length by sprite `+0x30`. Removing the three target-absent `GetLoadedSprite` calls preserves the target field accesses naturally.
- The no-hit bullet path is not a private `CheckBulletCancelCollision` helper. Target sets ECX to Player `+0x36C` and calls canonical `PlayerCollisionQueryStateView::AppendBoxRecord @ 0x0040F8E0` with bullet position, collision size and the Bullet pointer transported as the record's third dword. Maintained source now uses that canonical owner.
- The function-entry Supervisor call is canonical `Supervisor::SelectSide @ 0x00401440`, not the stale private `UpdateSideTimer` name. Laser state timing also had six unnecessary float-to-int conversions: the target uses one folded `[timer+8]` current getter, exact `ZunTimer::operator>= @ 0x00401520`, and four exact `ZunTimer::operator< @ 0x00401540` comparisons. Only the two alpha calculations retain `ZunTimer::operator float + __ftol2`, matching target.
- These target-backed corrections move the cold-stable natural candidate from 2,236 to 2,200 bytes against the 2,195-byte target. Its external DISP32 call-relocation count is now 62; the target PE separately has 62 immediate direct calls. COFF disassembly splits several relocation-bearing call encodings, so the relocation count is useful call-surface evidence but is not itself a physical-call-count equality claim, and `__ftol2` falls from eight calls to the target-observed two. The stack frame remains `0x54` versus target `0x40`; the five-byte aggregate residual therefore does not imply near-exactness.
- Two independent cold pinned-VC7.1 builds after the changes preserve every canonical unit from `src/BulletManager.cpp`: ClearDrawBuckets 39/39, CopyBulletAnmVmCore 16/16, Bullet::Deactivate 40/40, horizontal wrap 113/113, vertical wrap 113/113, Release 52/52 and Register 283/283. Follow-up should recover local lifetimes/stack layout rather than chase the aggregate five bytes.

## Packet 507 Etama bullet CFG and WAIT lifetime recovery

- Packet 506's 2,200-byte candidate was locally smaller but still semantically wrong in the bullet loop. TH09 target bytes at `0x00414770-0x00414784` show that when the side freeze flag is set, the function computes the bullet draw-bucket pointer and jumps directly to the shared queue tail. It does not execute the two timer increments at `0x00414AED/0x00414AFA`. The maintained source previously fell through `updateTimers` on that path.
- The three spawning cases also converge physically on one target completion block at `0x00414864-0x004148A2`: one ExecuteScript callsite, one cancelled-during-spawn test, one DESPAWNING store, one FIRED store and one timer reset. Expressing that as a shared natural `finishSpawning` source tail removes the candidate's long-lived hoisted constant 5 and restores the target's immediate DESPAWNING store shape.
- Target WAIT handling at `0x00414939-0x0041496C` loads `activeTransformFlags` once, tests the WAIT bit, calls exact `ZunTimer::operator<= @ 0x00406720`, XORs the cached flags value with `0x20000` on expiry, or postfix-decrements the wait timer otherwise. Maintaining the cached flags value reproduces this data flow and avoids the target-absent long-lived WAIT-bit constant.
- Combining these three target-proven CFG/lifetime corrections with Packet 506's owner/timer fixes produces a deterministic 2,228/2,195-byte candidate and reduces the stack frame from `0x54` to `0x50` against target `0x40`. This aggregate regression from 2,200 is deliberately retained because the old source encoded a target-disproved freeze behavior. The remaining work is still substantial stack/lifetime recovery, not a five-byte near-match.
- Two independent cold VC7.1 replays preserve every canonical unit in `src/BulletManager.cpp`: ClearDrawBuckets 39/39, CopyBulletAnmVmCore 16/16, Bullet::Deactivate 40/40, horizontal wrap 113/113, vertical wrap 113/113, Release 52/52 and Register 283/283.
- Bounded negative probes after Packet 506 establish that merely moving laser arrays to function scope, changing them from float arrays to Float3 with an empty default constructor, naming the four spawn velocity temporaries, using TH08-style function-level locals without its forbidden `var_order`, or substituting the canonical Supervisor type does not reduce the frame. Those spelling-only directions should not be repeated without new TH09-local evidence.

## Packet 508 Etama laser geometry and cold despawn placement

- TH09 target `EtamaController::OnUpdate` does not implement the maintained laser-size rule `startOffset > 0 ? currentLength * 0.7f : currentLength`. After clamping negative startOffset to zero, target bytes at `0x00414BF1-0x00414BFE` compute `endOffset - startOffset` once and store that value directly as the laser X half-size/current-length slot. The only 0.7f constant in the complete target PE is at an unrelated address and this function has no xref to it. The stale 0.7 branch is removed rather than preserved for size.
- The five-entry bullet-state jump table at `0x00414F84` maps state 1 to the common FIRED update, states 2/3/4 to fast/normal/slow spawning, and state 5 to the out-of-line despawn body at `0x00414C64`. Reordering the maintained switch so FIRED precedes DESPAWNING makes VC7.1 naturally move the cold despawn code out of the WAIT/offscreen region and place it immediately after the laser `AddNormalizeAngle / SetZRotation` geometry block, matching target physical call order. Clean TH08 has the same source case ordering, but TH09 target bytes and the TH09 compiler probe are the accepting evidence.
- With Packets 506-507 retained, these two corrections move the cold-stable candidate from 2,228 to 2,208 bytes against target 2,195; frame remains `0x50` versus target `0x40`. Capstone disassembly of the raw COFF function section confirms 62 actual call instructions, matching the target's 62 calls; this also resolves the earlier GNU-objdump relocation-boundary ambiguity.
- The call sequence around the most sensitive transition is now aligned: target/candidate AddNormalizeAngle, SetZRotation, despawn Float3 divide, Float3 add-assign, despawn ExecuteScript and Bullet::Deactivate occur in the same physical order, with call offsets within two bytes through that block. The remaining 13-byte aggregate difference is therefore secondary to the still-large 16-byte stack-frame gap.
- Two independent cold pinned-VC7.1 builds continue to reproduce all seven canonical `src/BulletManager.cpp` units exactly.

## Packet 509 Etama laser current-width lifetime recovery

- TH09 target state-0 laser code computes one transient current width after the startup timer gate, publishes that value to Laser `+0x568`, and keeps the same floating value live for body-VM X scale and collision half-width. In state 2 the target computes the shrinking width on the x87 stack and uses it for scale/collision size, but there is no store to Laser `+0x568`. The maintained source previously wrote `laser->currentWidth` in both states.
- Restoring one natural function-local `float currentWidth` reproduces the target semantic split: state 0 assigns the local to the member, while state 2 updates only VM scale and the collision-size local. This moves the deterministic candidate from 2,208 to 2,188 bytes against the 2,195-byte target while preserving the 0x50 frame and all 62 actual call instructions.
- A stronger lifetime probe also cached state-0 `startTime` and state-2 `despawnDuration` across timer calls. VC7.1 then reproduces the target laser stack identities exactly: denominator `-0x10`, half-width `-0x18`, current-length/size X `-0x1C`, and laser center X `-0x28`. However the four compiler-generated bullet Float3 return buffers move deeper, expanding the frame to `0x58`; that source is not retained.
- Additional natural probes do not fix the early temporary coloring: moving laser arrays to function scope, changing them from float arrays to Float3 with a TH09-supported empty default constructor, naming the four velocity-step temporaries, and using TH08-style ordinary function-level locals without its forbidden `var_order` are either codegen-identical or worse. Prefix/postfix/state-assignment spellings also do not explain the target's state-0 byte load/inc/store sequence.
- The remaining stack debt is therefore bounded to VC7.1 reuse of four compiler-generated Float3 return buffers against later laser locals. No register forcing, `var_order`, volatile steering, padding, assembly, or profile sweep is justified by current evidence.
- Two independent cold pinned-VC7.1 builds after the semantic correction preserve all seven canonical units from `src/BulletManager.cpp` exactly.

## Packet 510 SpawnSingleBullet pool, sprite and initializer recovery

- Fresh TH09 target review of `EtamaController::SpawnSingleBullet @ 0x00412960-0x004130C4` invalidates the maintained generic `poolStart + poolCount` allocator loop. The target branches on `poolIndex` and owns two separate constant-count searches: primary 175 entries and secondary 360 entries. Natural VC7.1 `/O2 /Ob1` then emits the target-shaped five-at-a-time primary search and six-at-a-time secondary search without manual unrolling.
- Sentinel handling also reloads the embedded array bases rather than the cursor fields: target repeatedly emits controller-relative LEAs for primary `+0x1A900` and secondary `+0xD2FC0`. Keeping the initial cursor load from `primaryPoolStart/secondaryPoolStart` but resetting sentinels to `&primaryBullets[0] / &secondaryBullets[0]` reproduces those repeated target LEAs and closes roughly forty bytes of the remaining allocator gap.
- Bullet position initialization copies descriptor position, then calls the folded/shared three-byte identity body `0x004343D0` on `&bullet->position` before writing Z=0.1 through the returned pointer. Direct `.z` source had erased that physical call. The maintained source now uses the natural Float3 pointer conversion seam without assigning unique physical ownership to the folded body.
- Despawn-VM sprite selection is inline in the target. After copying bullet/despawn VMs and setting the main bullet sprite, target compares the despawn active sprite and directly selects small/medium/default color offsets from the main bullet VM's loaded sprite geometry, then calls `AnmLoaded::SetSprite` a second time. The maintained helper call at this point was target-absent. Branch-local natural SetSprite calls make VC7.1 partially tail-merge to the target physical call layout; the second SetSprite call lands at the target relative offset.
- TH09 target `SelectBulletSprite @ 0x004124E0` and the inline despawn path both read loaded-sprite field `+0x30` for the 16.0/32.0 thresholds. The private `BulletLoadedSpriteView` therefore names this field widthPx; the previous +0x34/height usage was target-disproved. The helper remains NON-EXACT despite target size and is not promoted.
- These source recoveries move the deterministic large owner from the old ~1.39KB candidate (fresh baseline about 1.36KB) to 1,888/1,893 bytes with the target-exact `0x14` frame and all 22 target call boundaries represented. From entry through the second direct SetSprite, call offsets are aligned. The last five-byte displacement begins at the spawn transform-flags checks: target tests FAST directly as a byte in memory and only on the false path reloads the dword for NORMAL/SLOW, while natural VC7.1 CSE keeps one dword value live. A target-shaped false-path local is optimized back to the same candidate; TH08 independently uses ordinary 32-bit transformFlags and the same if/else-if family. No bitfield alias, volatile, register forcing, var_order, padding or profile roulette is justified.
- The function tail after spawn offset is structurally closed: Float3 multiply/subtract, 0x6C-dword transform copy, transform flags/index publication, `AdvanceTransformProgram`, spawn-suppression test, return value and `ret 0x14` all have the same instruction shape; the five-byte displacement simply carries through from the flags region.
- Two independent cold pinned-VC7.1 builds after the tracked source change preserve all seven canonical units from `src/BulletManager.cpp` exactly.

## Packet 511 Ending RunEndingScript logical/physical exact closure

- The earlier `Ending::RunEndingScript @ 0x0040E8A0` diagnostic compared unlike extents: the 1,695-byte VC7.1 COFF section included compiler-owned switch/map tables, while the tracked target function size 1,499 covered only logical code through `0x0040EE7A`. Fresh target ownership review shows four compiler-generated tables immediately after the body: a 3-entry dword table, a 65-byte dispatch map, an 18-entry dword table and a 75-byte dispatch map with required alignment. The complete accepted target-owned extent is `0x0040E8A0-0x0040EF5E` = 1,727 bytes. `0x0040EF5F` is one independent CC byte before `Ending::Ending @ 0x0040EF60`.
- Natural source recovery closes the logical body without byte padding or profile changes. The `@F/@R` path directly loops over all 16 ending VMs and writes `scriptIndex +0x21A = 0`; the previous private `EndingResetVmScriptIndex` call boundary was target-absent. The `@m` BGM path no longer caches one command byte across external audio calls: target reloads `currentCommand[1]` after StopAudio for LoadMusic and reloads it again after that call for PlayMusic. The final text path preserves separate held/not-held `lineWaitTimer = ...` assignments before joining, matching the target's two physical `ZunTimer::operator=(int)` callsites.
- With those source shapes, pinned VC7.1 `/O2 /Ob1` emits a 1,499-byte logical body whose final `ret` ends exactly at relative `+0x5DB`. Relocation-masked direct diagnostics report zero ordinary-byte differences for all 1,499 logical bytes and all 1,727 owned bytes. The canonical unit owns 88 relocations across code and compiler tables; every relocation target is solved from the attested TH09 target and replays exactly.
- Two independent cold canonical builds reproduce `RunEndingScript` at 1499/1499 logical and 1727/1727 owned extent with all 88 relocations. Removing the now-unused `EndingResetVmScriptIndex` definition changed only VC7.1 compiler-internal `$L` names in the interpreter and `UpdateFade` manifests; relocation offsets, types, destinations and compared bytes remain unchanged. All ten exact functions built from `src/Ending.cpp` replayed after this cleanup, including `UpdateFade` at 287/287 logical and 308/308 owned bytes. The separately profiled `Ending::Ending` constructor remains 119/119 exact.
- This packet withdraws the old “candidate 1,695 vs target 1,499” non-exact evidence. Section size is not function logical size when VC7.1 compiler tables share the function COMDAT; future diagnostics must keep logical code, compiler-owned tables and unowned alignment/padding as separate extents.

## Packet 512 ScreenEffect short callback exact closure

- Fresh target-bound review confirms independent authored extents for `ScreenEffect::CalcFadeIn @ 0x004222C0-0x00422326` (103 bytes), `CalcFadeOut @ 0x00422470-0x004224EA` (123), and `CalcArcadePulse @ 0x004225B0-0x00422652` (163). Each returns within its inventoried logical extent; the following `CC` bytes are alignment before distinct functions, not authored code.
- The earlier FadeIn/FadeOut candidates were already target-sized with all relocation destinations correct, but each had eight ordinary-byte differences. Target/object alignment localizes these to VC7.1's duration spill before versus after the zero-duration branch, not to a changed x87 arithmetic chain. A natural `int duration` read before that branch, reused for division and timer comparison, reproduces both target bodies.
- ArcadePulse initially emitted 157 bytes against the 163-byte target. TH09 target order loads the high-byte unsigned alpha before the screen-effect-counter exit and retains duration in an eight-byte stack frame for timer comparison and alpha interpolation. Hoisting those ordinary source locals reproduces the target without an ABI change or codegen directive.
- The established ScreenEffect `/O2 /Ob1` VC7.1 profile now gives complete canonical equality for all three functions: 103/103 with six solved relocations, 123/123 with eight, and 163/163 with seven. Two cold target-bound passes each replay all three plus the eleven pre-existing exact functions from `src/ScreenEffect.cpp`; no byte mismatch, padding, target-byte copy, register forcing, volatile trick, or adjacent-game layout transfer is involved. Exact function codegen does not establish native product closure.

## Packet 513 FadeHold and two Bullet transform-update exact closures

- `ScreenEffect::CalcFadeHold @ 0x004224F0-0x004225A9` had a genuine semantic error in the release branch: the attested target multiplies timer by `31.875f` (`0x41FF0000`, or `255.0f / 8.0f`) before subtracting from 128, while maintained source used `128.0f / 8.0f`. Independently, target stores duration in a local slot before the zero-duration test and reuses that value for the hold comparison and division. Both ordinary source corrections produce 186/186 bytes, 142/142 comparable bytes, and eleven target-solved relocations under the established profile. This supersedes Packet-134/512 descriptions of FadeHold as non-exact.
- `UpdateBulletDeceleration @ 0x00413460-0x004134CD` uses the TH09 target constant `0x3EA00000 = 0.3125f = 5.0f / 16.0f`. The old expression caused VC7.1 to emit two multiplies; expressing the exact 16-frame coefficient directly makes the natural source 110/110 bytes with eight solved relocations. `UpdateBulletPolarAcceleration @ 0x00413590-0x0041362D` was 152/158 solely because two state-delta accesses used `[EDI+0x10/+0x0C]` rather than the target's `[ESI+0xFF0/+0xFEC]`. Reading the same fields through `bullet->exStates[2]` retains the target's distinct owner-relative access and yields 158/158 with eight solved relocations. The remaining transform helpers are not promoted by association.
- Two independent cold builds of each changed translation unit replay all 15 ScreenEffect and all nine BulletManager configured exact units, including the three new units. Reviewed logical extents exclude following alignment. No target bytes, padding, assembly, volatile/register forcing, fake ABI, or profile change is used; the faithful Windows i386 product gate is still open.

## Packet 514 four Bullet transform-update exact closures

- `UpdateBulletVectorAcceleration @ 0x004134D0-0x00413582` is 179/179 bytes with eleven target-solved relocations. The attested target scales the ex-state vector through exact `Float3::operator* @ 0x00401180`, adds it through exact `Float3::operator+= @ 0x00405730`, tests both velocity coordinates through exact `AnmProjectionAbs @ 0x0040F1C0`, and calls exact `VectorAngle @ 0x00405700`. The maintained constructor-plus-inline-atan2 source was 196 bytes; a natural direct vector multiplication and shared calls reproduce the target. The explicit float conversion around the neutral long-double absolute-value declaration selects the target's single-precision threshold comparison at `0x0048E800`; without it only the two `FCOMP` operand-width bytes differ.
- `UpdateBulletRelativeDirectionChange @ 0x00413630-0x004136F9` (202 bytes), `UpdateBulletAbsoluteDirectionChange @ 0x00413700-0x004137C6` (199), and `UpdateBulletAimedDirectionChange @ 0x004137D0-0x004138BB` (236) each replay exactly with eight, eight and ten target-solved relocations. The target snapshots integer interval frames before the timer comparison, retains a timer alias, and accesses other direction-state fields through the owning Bullet. Expressing that ordinary source shape naturally reproduces the target's ESI/EDI ownership, long-offset accesses, stack duration slot, and integer x87 division. The aimed function's `AngleToPoint` and `AddNormalizeAngle` relocations resolve to independently reviewed TH09 entries.
- Two independent cold pinned VC7.1 `/O2 /Ob1` rounds replay all 13 configured `src/BulletManager.cpp` exact units, including these four. The complete new logical extent is 816 bytes. This is function-level exactness only; neither the enclosing controller update nor the Windows i386 product build has closed.

## Packet 515 ECL helper source hygiene and Hermite diagnostic

- `src/EclHelpers.cpp` had an unused `HelperPlayer` function referring to an undefined `EclHelperPlayerView`. This committed stale helper made the configured `ecl-interpolate-linear` Oracle fail at compile time, independently of target matching. Removing only that unreferenced function restores compilation; two cold target-bound replays preserve `InterpolateLinear @ 0x00407450` at 74/74 exact. No ECL match credit is added for this cleanup.
- `InterpolateHermite @ 0x004074A0` remains non-exact at 182 candidate bytes versus 188 target bytes. The target and source resolve the same four parameters and write the same destination; the residual includes a different local-slot/FPU summand schedule. Reversing the four source summands produced the same 182-byte codegen, so it was not retained. Do not promote from formula similarity or force registers/stack order.
- Attested entry disassembly disproves the old standard-`__fastcall` target-ABI tags for three other non-exact ECL helpers. `ApplyInterpolationOperation @ 0x00407560` receives Enemy in EDI and instruction in ESI; `InstallInterpolationSlot @ 0x00407600` receives Enemy in EBX and instruction in ESI; `CallSubroutine @ 0x00407A80` receives Enemy in EBX and instruction in EAX, plus the raw subroutine ID on the stack with `RET 4`. Their maintained `__fastcall` declarations are source proxies, not target ABI claims. The ledger convention fields are now blank and the precise live-in observations are in their notes. This is provisional IDA/byte evidence, not exactness credit.

## Packet 516 TitleScreen short option and cursor helpers

- `TitleCharacterConfigView::GetOptionState @ 0x004234A7-0x004234DC` reads the six-int-per-character option table at receiver +0x7C. TH09 tests the option sign, executes the nonnegative indexed-read branch first, and otherwise ORs entries 3 through 0. The old negative-first source was the right value but selected an inverse physical CFG and matched only 22/54 bytes. A natural positive-first branch reproduces all 54 bytes with no relocations; two cold canonical rounds also preserve every other accepted `TitleScreen.cpp` unit.
- `TitleScreenView::MoveCharacterCursorHorizontal @ 0x0042474A` did not read a single fixed `g_TitleInput`: target bytes multiply the cursor index by `0x8E` and address the physical input-state table at `0x004ACE18`. Corrected source now selects that indexed state. Both this helper and `MoveCharacterCursorHorizontalForInput @ 0x004247F1` use distinct decrement/increment branches with one directional wrap test each, rather than one generic `cursor += direction` followed by two wrap tests. Under the maintained `/O1 /Ob1` profile both are now target-sized at 167 bytes, versus old 145 and 158 candidates, but remain non-exact: register, input-pointer lifetime, and callsite scheduling differ. Direct relocation-aware diagnostics report only 28/139 and 73/139 comparable bytes, so neither enters `config/matches.csv`.
- Two independent cold rounds replay all 24 configured units sourced from `src/TitleScreen.cpp` across its two distinct compiler profiles. The Windows i386 product graph remains open.
